# 에이전트 규칙

> 살아 있는 참조 — **이 저장소에서 일하는 모든 에이전트 세션이 따르는 약속.** 세션이 이와 다르게 움직이면 그건 버그다.

## PR 은 한국어로, 다섯 살 아이에게 설명하듯 쓴다

운영자 지시(2026-09-19). PR 제목과 본문 모두. 스쿼시 머지라서 PR 제목이 그대로 main 의 커밋 한 줄이 된다.

- **쉬운 말, 짧은 문장, 해요체.** 한 문장에 한 가지만. 어려운 낱말은 빼거나, 처음 나올 때 한 번 쉬운 말로 풀어 준다.
- **비유와 그림으로 보여 준다.** 글로만 설명하지 않는다.
  - **비유**: 바꾼 것의 핵심을 일상의 비유로 먼저 말한다. "디코드 스텝마다 MoE 전문가 가중치를 다시 스트리밍한다" 대신 → "모델 안에는 전문가가 여럿 있는데, 글자 하나를 쓸 때마다 전문가들의 공책을 처음부터 다시 읽고 있었어요."
  - **그림**: 흐름·구조·전후가 있으면 그린다. GitHub 은 PR 본문의 `` ```mermaid `` 블록을 상자와 화살표 그림으로 그려 준다. 코드 블록 안의 글자 그림, 전/후 표도 좋다.

    ```mermaid
    flowchart LR
      A["전: 글자마다 공책을 처음부터 읽어요 🐢"] --> B["후: 필요한 쪽만 펴서 읽어요 🐇"]
    ```

  - **HTML**: PR 본문에는 GitHub 이 허용하는 HTML 을 쓸 수 있다 — `<table>` 로 전/후를 나란히, `<details>` 로 접기, `<img>` 로 그림(`<style>`·`<script>`·`style=` 은 GitHub 이 지운다). 한 장으로 모자란 큰 변경은 HTML 설명 페이지(Claude 아티팩트)를 만들어 본문 맨 위에 링크한다.
- **제목**: `종류(범위):` 꼬리표(`feat(qwen38):`, `fix(dense):`, `ci:` …)는 그대로 두고, 뒤는 무엇이 좋아지는지 쉬운 한국어 한 문장.
  - 예: `ci: 검사를 네 대가 나눠 맡아서 더 빨리 끝나요`
- **본문은 네 칸.** 칸 이름은 이대로 쓴다.
  1. `## 한 줄로` — 이 PR 이 하는 일, 한 문장.
  2. `## 무엇이 문제였어요?` — 바꾸기 전에 무엇이 안 좋았는지. 비유로.
  3. `## 무엇을 바꿨어요?` — 바꾼 것. 그림이 들어갈 자리는 주로 여기다. 파일 이름은 `백틱` 그대로 두고, 옆에 그게 무엇인지 쉬운 말 한 줄.
  4. `## 확인한 것 · 아직 모르는 것` — 돌려 본 테스트와 잰 숫자, 그리고 못 해 본 것("GPU·플릿에서는 아직 안 돌려 봤어요"). 못 한 것을 빼먹지 않는다. 측정했으면 여기엔 결과만 쉬운 말로 쓰고 "자세한 측정 기록은 아래 댓글에 있어요" 라고 적는다(아래 절).
- **쉽게 쓰는 것이지 틀리게 쓰는 것이 아니다.** 코드 이름·경로·명령·숫자는 원래 모습 그대로 두고, 쉬운 말은 그 옆에 붙인다. 숫자는 무슨 뜻인지 말해 준다("793초 → 288초, 세 배 가까이 빨라졌어요"). 쉽게 만들려고 숫자를 둥글리거나 부풀리지 않는다. 비유와 그림도 실제로 일어난 일과 맞아야 한다.
- 긴 표·로그·파일 목록은 맨 아래 `<details><summary>자세히</summary> … </details>` 안에 접어 둔다.
- 속도를 주장하는 PR 은 `.github/pull_request_template.md` 의 D17 칸을 본문에 그대로 채운다(판정 숫자, `engine/CHARTER.md` D17). 그 숫자가 나온 전체 기록은 측정 댓글에 둔다.
- 에이전트는 `gh pr create --body …` 로 PR 을 만들어서 웹 양식(템플릿)을 보지 못한다. 그래서 규칙은 여기에 두고, 템플릿은 같은 네 칸을 보여 주기만 한다. 규칙을 바꾸면 둘 다 고친다.

## 측정했으면 자세한 기록은 PR 댓글로 단다

운영자 제안(2026-09-19). 본문은 누구나 읽게 쉽게, 측정 기록은 다시 잴 수 있게 자세하게 — 둘을 나눈다.

- **측정**은 PR 이 기대는 숫자를 잰 모든 것이다: 플릿 onepass·screen, 프로브·마이크로벤치, CPU 에서 시간 재기, 품질·수용률 평가. 테스트가 통과했는지는 측정이 아니다 — 그건 본문에 쓴다.
- PR 을 연 **바로 뒤, 첫 댓글로** 단다: `gh pr comment <번호> --body-file <파일>`. 운영자는 PR 이 열리자마자 머지하기도 하니, 댓글 파일은 PR 을 열기 전에 다 써 둔다.
- 첫 줄은 `## 측정 기록 — <날짜> · <잰 커밋 SHA>`.
- 쉽게 쓸 필요가 없다. **다른 사람이 똑같이 다시 잴 수 있을 만큼** 자세하고 정확하게:
  - 무엇을 알고 싶었나 — 질문 한 줄
  - 무엇과 비교했나 — 잰 트리와 비교 대상의 커밋 SHA
  - 어디서 — 노드·GPU·이미지, 플릿 세션 이름, 전/후가 같은 부팅인지
  - 어떻게 — 그대로 복사해 다시 돌릴 수 있는 명령, 설정(C·K·ctx·샘플러·환경 변수), 판 수, 콜드/웜
  - 숫자 — 판마다 전부(중앙값만 말고), 전/후 차이와 흔들림 크기, `engine_shape`, 접두사 캐시를 맞은 판 표시
  - 기록 위치 — `measurements/<폴더>/`, 원장 `MEASUREMENTS.md` 항목, 저장소 밖 로그 경로(srv2 등)
  - 못 잰 것 — "GPU·플릿 미검증" 처럼
- 긴 로그는 `<details>` 로 접는다.
- 나중에 더 재면 그 댓글을 고치지 말고 **새 댓글**을 단다. 그날의 기록은 그대로 둔다. 머지된 뒤에 잰 것도 그 PR 에 단다.
- 댓글은 원장을 대신하지 않는다. 판정과 숫자는 지금처럼 `MEASUREMENTS.md` 에도 적는다 — 저장소에 남는 것은 원장이고, 댓글은 GitHub 에만 있다.
