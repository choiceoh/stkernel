# 실측 원장 (measurement ledger)

이 스택에서 내린 모든 성능 판정과 그 수치. **여기 없는 주장은 미실측이다.**
방법론·함정은 [README의 bench 표](README.md#bench--측정-도구와-함정) 참조.
기준 환경: 4× GB10(SM121) · TP=4 · CRS812.

**두 스택이 한 원장에 섞여 있다.** 아래 기준선 표와 08월 항목은 프로덕션 `dsv4`
(hy4, `production-hybrid-1.6` 이미지)의 것이고, 09월의 메가커널·MK-\*·드래프터 W4
항목은 커널 캠페인 스택 `glm53`(GLM-5.3-Flash NVFP4, `glm53:v13-b12x`)의 것이다.
어느 노브가 지금 켜져 있는지는 [profiles/README.md](profiles/README.md), 디코드
스텝이 무슨 커널로 이루어지는지는 [STEP_KERNEL_MAP.md](STEP_KERNEL_MAP.md),
부팅이 필요한 다음 실험은 [RUNBOOK_KERNEL_CAMPAIGN2.md](RUNBOOK_KERNEL_CAMPAIGN2.md).

## 이 원장을 쓰는 규칙

여덟 줄이 이 파일의 판정 규율 전부다. 항목마다 되풀이하지 않는다.

1. **여기 없는 주장은 미실측이다.** 프로브·모델·추정은 원장 항목이 아니라 그 항목의 재료다.
2. **판정 채널은 C=1 step/s**(`bench/bench-dec.py`), 부팅 CV **1.7%**. 그보다 작은 기대값은
   단독 부팅으로 판정하지 않고 다른 팔에 얹는다.
3. **브래킷은 base→cand→base**, 노브 하나. **base 팔의 값은 명시**한다 — 프로필 기본값이
   움직인 뒤로는 "노브 없음"이 곧 stock 이 아니다(28차 §8).
4. **채택 게이트**: 품질 9/9 · 한국어 0/16 · pos-1 수용률 ±2 pct · 프리필 사다리 무회귀.
   수용률을 움직이는 변경은 tok/s 와 tokens/step 을 함께 적는다.
5. **무장 ≠ 서빙.** 노브가 1 이라는 사실은 커널이 돈다는 뜻이 아니다(22차·28차). 증거는
   셋 중 하나 — 부팅 로그의 증명 줄, 트레이스, 또는 거부가 스스로 말하는 로그.
6. **오프라인 프로브 숫자는 상한이거나 형상이 다르다.** 27차에서 상한 하나가 프로브 하나에
   절반 이하로 줄었다. 상한은 목표와 **같은 측정 형태**로 만든다.
7. **CUPTI 시간은 트레이스 사이에서 비교하지 않는다.** 개수가 정본이고 시간은 같은 트레이스
   안에서의 상대 비교로만 쓴다([STEP_KERNEL_MAP.md](STEP_KERNEL_MAP.md)).
8. **그리디 텍스트 diff 로는 판정하지 않는다** — 같은 구성의 두 부팅에서도 온도 0 응답이
   갈린다(28차 §8). 판정은 게이트와 브래킷이다.

## 찾아보기

파일이 시간순이 아니다(주제별로 붙었고 메가커널 시리즈는 28 → 2 로 내려간다). 제목으로
검색해서 열면 된다.

| 무엇을 찾나 | 어디 |
|---|---|
| 지금 무엇이 서빙되나 | 위의 두 기준선 표 · [profiles/README.md](profiles/README.md) |
| 디코드 스텝이 무슨 커널로 이루어지나 | "스텝 재분해"(08-31) · "스텝 커널 지도 최신화"(09-04) · [STEP_KERNEL_MAP.md](STEP_KERNEL_MAP.md) |
| 프리필은 어디서 시간을 쓰나 | "커널 귀속 — 프리필의 완전 분해"(08-09) · "GLM-5.3 프리필 첫 프로파일"(09-03) |
| 메가커널이 무엇을 얼마나 먹었나 | 아래 시리즈 색인 · "메가커널 결산"(09-02) · 21차 · 25차 · 28차 |
| 통신(AR)의 바닥은 어디인가 | 19차·20차 · "osar 에필로그 융합"(09-04, 기각) · "NCCL 채널 스윕"(09-03) |
| 부팅이 왜 느리거나 죽나 | 13~18차 · 23차 · 26차 · "GB10 메모리 예산"(08-30) · "부팅 시간 901 → 345초" |
| 수치·품질 축의 판정 | "한국어 토큰 손상"(08-30) · "sm_121a 텐서코어 ISA"(09-03) · "nvfp4 밀집 GEMM"(09-03) · "정정: fp4 는 fp8 의 2배"(09-03) |
| 기각된 것과 그 이유 | "정찰 일괄 기각"(08-11) · "REFINE_PASS A/B"(08-12) · EXP-8 기각(09-03) · 20차 · "MK_SEG_MLA 전용 명령 검토"(09-03) |
| 왜 우리 커널이 더 빠른데 스텝이 안 줄었나 | 29차 · **30차**(발사 위치별 분해) · 21차 |
| 발사 수를 줄이는 묶음(EXP-20 계열) | 31차 · 32차 · RUNBOOK EXP-20 |
| 다음에 무엇을 부팅하나 | [RUNBOOK_KERNEL_CAMPAIGN2.md](RUNBOOK_KERNEL_CAMPAIGN2.md) 의 상태 표 |

**메가커널 시리즈 한 줄 색인** (`메가커널 24차` 처럼 번호로 검색; 21·25~28차는 제목이 `메가커널` 없이 시작하기도 한다)

| 차 | 한 줄 |
|---|---|
| 34 | 프로덕션 스텝의 첫 노드 인구조사(1,166 커널) · 남은 자리 넷 · 드래프터 앞 호스트 갭 |
| 33 | 프리필 축 첫 표적 완결 — 콜드 JIT 세금을 부팅으로(`PREFILL_WARMUP=1`) |
| 32 | 레버 2~7 브래킷 체인 — **EXP-7 이 +7%**, 나머지는 0 이거나 죽었다 · srv4 가 rank 3 이라는 사실이 측정을 오염시켰다 |
| 31 | 자체 소유 미세 융합 묶음 2(EXP-20) 의 오프라인 게이트 결과 |
| 30 | **mk_gemm 이 deep_gemm 과 같은 14 ms 인 이유** — 상주 커널은 MoE 옆에서 진다(SM smem 이 셋을 못 앉힌다, 노출 1,177 µs/스텝). §12 에 v2 재캡처(185발 13.86 → 7.78 ms, 쌍 노출 1.16 → 0.15) |
| 29 | 공유 전문가 GEMM 쌍의 고정비 — 배리어 없는 로컬 양자화는 단독으로 손해, MoE 아래에서는 발사 순서가 전부 |
| 28 | 드래프터 W4 는 컴파일 캐시 때문에 서빙된 적이 없었다 · MK-MLA 사망 = 스크래치 재할당 · W4 채택(+1.8%) |
| 27 | 서빙 PDL 이 꺼져 있었다(−7.6%/발사) · MoE 커널은 우리 레인과 같은 속도(MK_SEG_MOE 닫힘) · L2-warm 7% |
| 26 | 부팅의 메모리 절벽 — fp8-dense 패스가 로드 중 여러 번, W4 팩 빌더가 예약 메모리 유출 |
| 25 | 24차 온디바이스 마감 · 스텝의 꼬리 7.5~8.5 ms 는 드래프터이고 임계경로다 |
| 24 | W4 팩이 프로덕션 그룹을 전부 클램프하고 있었다 |
| 23 | srv3 호스트 OOM — nvfp4 첫 부팅이 덴스 패스 안에서 죽었다 |
| 22 | **"무장"이 "서빙"을 뜻하지 않았다** — KDA 레이아웃 게이트의 침묵 |
| 21 | 무장 디코드 스텝 실측 — 대역폭 포화, 커널 여지 끝 (표의 ms 는 25차가 절반값으로 정정) |
| 20 | two-shot all-reduce 기각 — 병목은 바이트가 아니라 고정비 22.7 µs |
| 19 | all-reduce 의 wait 38.7 µs 가 무엇인지 |
| 18 | "가중치 로드 296초" 는 우리 벤치가 만든 숫자였다 |
| 17 | KV 캐시를 토큰 수로 고정 (운영자 지시) |
| 16 | 부팅 — cudagraph 메모리 프로파일러 끄기 |
| 15 | 부팅 — `init engine` 192초를 쪼갤 스탬프 |
| 14 | 부팅 — 형제 자체 커널 둘도 영속 캐시로 |
| 13 | 부팅 — 자체 커널 컴파일을 영속 캐시로 |
| 12 | MHC 발행 배치 채택 · KDA delta 3-sync 기각 |
| 11 | 인접 커널 협력 — 둘 다 접음 |
| 10 | MK-KDA 2라운드 — 스팬 199 → 176 |
| 9 | MHC 재구성 — T=8 30.7 → 27.4, T=32 59.4 → 42.0 µs |
| 8 | MK-KDA 단계 귀속과 재작성 — 402 → 199 µs |
| 7 | MK-KDA state-index 계약 확정 |
| 6 | W8 팔 제거, W4 단일 레인 (운영자 결정) |
| 5 | W4 확장을 prmt 테이블 조회로 |
| 4 | W8 팔도 stock 을 넘었다 — smem 정렬 + 밀집 W 행 |
| 3 | W4 팔이 stock 을 넘었다 |
| 2 | 벤치 함정 넷 · W4 −33% · MHC −22% · PDL · deepgemm 과의 진짜 격차 |

## 기준선 — `dsv4` (슬리밍+인시던트픽스, 노브 전부 OFF)

| 지표 | 값 |
|---|---|
| 프리필 (단일, 60K, 캐시무효) | **2,866–2,872 tok/s** (08-11 컴팩션 스톨 근절 후 3회, 스프레드 6; 이전 2,798–2,830은 UMA 마이그레이션 요동 오염 하한 — 아래 ★★UMA 챕터. 노브 전부 OFF 원기준선은 2,503–2,528) |
| 패브릭 (ib_write_bw, 노드쌍) | **196.0 Gb/s** (98.01 × 2 half; 단일 half 단독 109.13) |
| 디코드 (bench-dec, 768tok 생성) | C=1 57.4 · C=2 95.6 · C=4 141.6 tok/s |
| 짧은 디코드 (bench-tp4) | 73–110 진동 — **원인 실측 확정: dspark 수용률 41–60%/샘플 (평균 ~52% = 5중 ~2.6개 수용)**, acc와 tok/s 동행 |
| 장문 디코드 (IndexCache freq=4) | 32K +13.6% · 128K +16.0% vs freq=0 |
| GPU KV cache | **2,656,602 tokens (21.36 GiB/rank, 430K 동시 ~6.2×)** — GPU_MEM 0.60 기준 정상값. **★08-11 런북 플릿 재부팅 후 신기준: 2,918,776–2,948,142 tokens (+10~11%, GPU_MEM 0.60 동일·drop_caches 동일)** — 고차 페이지 재건+컴팩션 영구화 효과, 3개 독립 부트 재현. 이후 KV 비교는 재부팅 후 대역 기준. ★과거 기록 3,190,072(28.6GiB)와의 −16% 어트리뷰션 **최종 확정 = 예산 노브 차이**: 회계 분해(예산 72.96 − 가중치 41.08 − KV 21.36 = 오버헤드 10.52GiB, 그래프 0.74 포함)상 0.60에서 28.6이 나오려면 오버헤드 3.3이어야 하는데 실측 10.5로 불가능; **0.65 부트면 79.04−41.08−28.6=9.4로 정합** → 과거 수치는 0.65 수동 부트의 것. 기각된 가설 2건: #49236 스크래치풀(산수 43MB)·호스트 free/페이지캐시(클린 재부팅 후에도 21.36 재현으로 반증). **★KV 수치는 GPU_MEM 명기 없이 비교 금지** |
| 품질 | 리트리벌 9/9 (2K/32K/128K × 사실 3) |

## 기준선 — `glm53` (메가커널 세트 기본값, 2026-09-04 20:36 부팅)

캠페인 스택의 현행 기준선. **새 수치가 아니라 28차 §8 부팅의 요약**이고, 브래킷 근거는
각 항목의 원장 절이다. 노브 목록은 [profiles/README.md](profiles/README.md).

| 지표 | 값 |
|---|---|
| 구성 | `MEGAKERNEL`·`MK_MHC`·`MK_GEMM`·`MK_MLA`=1 · `MK_KDA`=0 · 드래프터 W4(`DFLASH2_FP8_DENSE=1`) · bf16 원본 해제(타깃 3.67 + 드래프터 0.73 GB) |
| ⚠ 프로필과의 차이 | **이 부팅에는 `MK_PDL` 이 없다** — PDL 기본값은 PR #290(20:57 머지)이 넣었고 근거는 27차의 오프라인 프로브(발사당 58.0 → 53.6 µs, 그래프 비트 동일)다. 즉 **현행 프로필 = 이 기준선 + PDL 한 칸**이고, 그 한 칸의 종단 수치는 아직 없다 |
| C=1 디코드 | **step/s 16.6 / 16.3** (첫 rep 15.0 은 워밍업) · raw acc 18~25% |
| 프리필 2048 (5.8K tok) | 2,523 cold / 2,646 warm tok/s |
| 프리필 8192 (23K tok) | 2,696 / 2,645 tok/s · **TTFT 8.55 / 8.71 s** |
| KV | 24.02 GiB/rank (bf16 해제분 +0.54) |
| 부팅 | 헬스까지 345 s (웜 컴파일 캐시) |
| 무장 증거 | `armed={'mhc': True, 'gemm': True, 'kda': False, 'mla': True}` · `drafter lane serving: 30 of 31` · `mla shadow rel=1.34e-03 -> ARMED` |
| 게이트 | 품질 9/9 · 한국어 0/16 · pos-1 수용률 ±2 pct (그리디 텍스트 diff 는 부팅 간 비재현이라 판정에 쓰지 않는다) |

이 구성의 커널 구성표는 [STEP_KERNEL_MAP.md](STEP_KERNEL_MAP.md) — 단 지도의 무장
트레이스는 이 부팅보다 한 발 앞(18:42, MLA off · 드래프터 fc 만 W4)이라, 세 줄이
미실측으로 남아 있다(보충 분해 5).

## 커널 귀속 — 프리필의 완전 분해 (2026-08-09)

> ★08-11 갱신판이 "★★UMA 페이지 마이그레이션 스톨" 챕터에 있다 (comms
> 23.4%는 100G+스큐 오염 시절 수치 — 클린 실측 17.2%).

torch profiler 32K 프리필 캡처, rank0 (`--profiler-config` CLI로 라우트 개방,
트레이스 오프라인 파싱). **GPU busy 99.6% — 런치갭/CPU 병목설 사망.**

| 버킷 | 비중 | 비고 |
|---|---|---|
| MoE (b12x W4A16) | 24.2% | probe 계측(27.6%)과 정합 |
| **comms (NCCL AllReduce ×846)** | **23.4%** | 단일 최대 커널 3,116ms. 회당 3.7ms ≈ 4랭크 링 이론치 = **대역폭 바닥** |
| GEMM (deep_gemm 투영들) | 18.6% | tf32 hc_prenorm 487ms 포함(mHC 계열) |
| attn/indexer (sparse MLA) | 12.5% | |
| mHC 계열 합산 | **~16%** | mhc_post 1,034 + mhc_pre 620 + hc_prenorm 487 — 버킷 3곳에 분산 |
| norm/rope/quant 나머지 | ~5% | |
| memops | 0.9% | |

## 디코드의 실체 — 콘텐츠 바운드 (2026-08-09)

단일 스트림 디코드는 **스텝 시간(43–63ms, TP4 동기+LPDDR 대역폭 바닥) × 스텝당
토큰(1+수용, dspark)** 이고, 후자가 콘텐츠로 최대 5배 갈린다 (2×2 실측, 동일 엔진):

| max 600tok, temp 0.95 | thinking True | thinking False |
|---|---|---|
| 소수 나열 (정형) | 115.4 | 116.6 tok/s |
| 한국어 산문 (자유) | 58.7 | 22.0* tok/s |

*22.0의 기전 규명: 600tok/27.3s를 스텝 43–45ms로 역산하면 **스텝당 토큰 ≈ 1.0 = 그 요청은 스펙 디코드가 무력화된 채 완주** (드래프터 폴백/배치 엣지 — 프로브 충돌 유력). 콘텐츠 탓 아님 — 동일 프롬프트 8회 재시도 전부 정상(58–66, acc 30–36%, 산수 정합). bench-dec의 per-call acc 병기가 재발 시 acc~0%로 즉시 검출. thinking 플래그는 무관.

**위치별 수용률** (`spec_decode_num_accepted_tokens_per_pos_total`, drafts 10,526):
pos1 **78.5%** · pos2 56.4% · pos3 39.6% · pos4 28.6% · pos5 21.5% (기하 감쇠 ~0.72)
→ SPEC_TOKENS>5 무가치 확정 (6·7위치 기대 수용 ~15/11%).

**실사용 기대치**: 한국어 에이전트 산문 ~55–66 tok/s (가끔 20대 outlier),
정형 출력(코드·목록) 90–117, C=4 집계 ~145. bench-tp4의 "평균 ~95"는 정형
콘텐츠 프롬프트가 만든 낙관치 — 산문 판정은 bench-dec이 정답.

**★고동시성 집계 특성화 (08-11, bench-dec sweep() C=8×4/C=16×3, 프로덕션)**:
C=8 중앙값 **173.5 tok/s** · C=16 **288.4 tok/s** (min 283.7/max 289.3, acc
52–54% 무붕괴). C=1 대비 **4.5×** — 집계 스루풋은 MAX_NUM_SEQS=16 캡까지
계속 오르며, C=8→16 구간이 초선형(+66%; 배치 48→96토큰 커널 버킷 효과 추정).
배치 가능한 워크로드라면 동시성이 최대 무코드 스루풋 레버. 잔여 축:
MAX_NUM_SEQS>16 A/B(재기동; KV 2.54M tokens ÷ 컨텍스트 길이가 상한 결정).
부수 확인: 서빙 기본 temp 0.8/top_p 1.0 활성이 부팅 로그로 실증
(generation_config 오버라이드 경고) — 명시 temp 클라이언트만 이탈 가능하나
서버는 요청 파라미터를 로그하지 않아 클라이언트 측 감사만 유효.

**남은 디코드 레버** (SPEC_TOKENS 스윕·draft method 소진 후): 샘플링 온도(0.95)가 수용률의 최대 소프트웨어 변수 —
낮추면 드래프터 적중↑ = 디코드↑, 단 출력 다양성과의 품질 트레이드라 운영자
결정 사항 (temp 0.7 A/B는 미실측). 스텝 시간 자체는 하드웨어 영역.

## 디코드 스텝 예산 — 커널 귀속 (2026-08-09, with_stack=false 캡처)

> ★08-11 갱신판(채택 4건 반영, busy 39.6ms/step·MoE 41.7% 최대화)이
> "★★UMA 페이지 마이그레이션 스톨" 챕터 말미에 있다.

600tok 생성(acc 32.9%, 227스텝 = 41.9ms/step), rank0, GPU busy **97.4%**:

| 버킷 | ms/step | 비중 |
|---|---|---|
| GEMM (M=6 극소 행렬곱 — 가중치 스트리밍) | 20.28 | 42.1% |
| MoE (b12x — expert 가중치 읽기) | 14.85 | 30.9% |
| comms (AllReduce 2/layer, 회당 68µs) | 6.75 | 14.0% |
| mHC/기타 | 2.45 | 5.1% |
| attn (sparse MLA decode) | 2.20 | 4.6% |
| idle/glue | 1.10 | 2.6% |

**결론: 디코드 스텝의 84%가 가중치 스트리밍** — 토큰당 랭크당 ~9.6GB를 LPDDR
(273GB/s)에서 읽는 물리적 바닥. 검산: 무스펙 1tok/step → 1000/42 = 23.8 tok/s
이론치 = 관측된 22.0 outlier와 일치 (스펙 바이패스 서명의 독립 확인).
실디코드 = 이 바닥 × (1+5×수용률). 클럭·통신·커널 튜닝 여지 거의 없음
(comms 14%·idle 2.6%). 유일 관찰 항목: 드래프터 GEMM이 cutlass_80_wmma
(SM80 세대 커널, 3.4ms/step) — 교체 시 최대 ~8% 이론 여지.

## 디코드 수용률 배터리 (2026-08-09, 동일 엔진·연속 5창 — 상대 비교 유효)

수필체 한국어 600tok ×3/창, C=1, /metrics 델타(per-pos 포함). 절대값은 위
스텝 예산과 검산 일치: 바닥 23.8 × (1+5×0.16) ≈ 43 ≈ 실측 46–48 tok/s.

| 창 | tok/s | acc(수용/드래프트) |
|---|---|---|
| W0 기본값(temp/top_p 미지정) | 46.1–47.7 | 15.9% |
| W1 t0.95 명시 | 47.6–48.7 | 16.8% |
| W2 t0.95 + top_p 0.44 | 46.1–48.9 | 16.4% |
| W3 t0.80 | 50.5–51.4 | **18.9%** |
| W4 t0.70 | 49.0–52.3 | 18.7% |

**판정 3건:**

1. **top_p는 수용률에 무영향 (드래프트 미러링 가설 기각)** — W2=W1.
   speculator.py L317("draft는 temperature만 고려, top_p 무시")로 세운
   지지집합 불일치 가설은 이 포크의 기각샘플링이 top_p에 무감한 것으로
   반증. 부수 확인: 모델 generation_config가 t1.0/top_p1.0이라 미지정
   요청의 실효 top_p는 원래 1.0 — 런처의 default-chat-template-kwargs
   0.95/0.44는 샘플링 기본값이 아님(W0≈W1로 행동 일치).
2. **온도 곡선은 0.8에서 포화** — 0.95→0.8: acc +2%p·tok/s +7% (난문
   기준). 0.8→0.7: 개선 없음(18.9→18.7%). → **0.8 채택** (런처
   `--override-generation-config`; TEMP 노브로 가변).
3. **k=6 강등** — 이 콘텐츠의 절대 pos5 수용 ≈ 1.3%/스텝 → pos6 기대
   ~0.5% = 이득 ~0.3%. 버킷 경계는 안전(window 128 + k=5/6/7 모두 512
   버킷 — 부수 확인: k≥1이면 무조건 128→512 폭 커널 = dspark 고정 비용)
   이나 재기동 비용 대비 기대가 없어 미실행.

~~미완: 22 tok/s 바이패스 프로브-충돌 재현 실험은 착수 시점에 supervisor
재기동과 겹쳐 보류 (스택 안정 시 재개).~~ **08-11 재개·종결: 0/12 미재현**
(장문 C=1 생성 중 supervisor형 초소형 프로브 3발 인터리브 ×12; median 55.4,
min 38.1 = 수용률 운 범위, 23 tok/s 시그니처 무발현) — 프로브-충돌 가설로는
재현 불가, 클래스 비활성 추정. bench-dec acc 열 상시 감시로 전환 (재발 시
그 시점 포렌식으로 재개).

## 판정 원장

### 채택 (프로덕션)

| 항목 | 수치 근거 | 일자 |
|---|---|---|
| 업스트림 5건 (#51209/#51042/#51252/#49059/#51202) | 장문 디코드 +13~16%, 정합성/크래시 가드, 프리필 무회귀 | 08-08 |
| `VLLM_DSV4_INDEXER_SP=1` | +5.4%, bit-exact | 08-08 |
| 슬리밍(PR#1–4) + threshold 인시던트 픽스 | 무회귀 검증 (9/9, 프리필/디코드 동급) | 08-09 |
| IDXFREQ=4 | freq=2 대비 짧은디코드 우위, 장문 동급 | 08-08 |
| SPEC_TOKENS=5 | 3은 −7% (5 위쪽 미탐) | 08-08 |
| **서빙 기본 temperature 0.8** (`--override-generation-config`) | 난문 수용률 15.9→18.9% · tok/s +7% (배터리 W1→W3), 0.7은 무익(포화). 명시 temperature 요청 불변 · 실효 top_p 1.0 유지 | 08-09 |
| **#49236 슬림 포팅** (eager-scratch 풀: fp8 인덱서/글로벌라이즈 버킷, q·fp4 버킷 제외, model.py 무접촉 싱글턴) | 품질 9/9 · **성능 중립**(프리필 2,570–2,593, TTFT 이득은 클럭 몫과 구분 불가, 디코드 동급) — 운영자 결정으로 유지(무해·업스트림 정렬). None-폴백 설계라 파일 단위 롤백 가능 | 08-09 |
| **temp 0.95→0.8** (serve default_chat_template_kwargs) | 운영자 결정 (0.5의 +7%와 문체 건조화 사이 절충) | 08-09 |
| **★MARKOV_TOPK=512** (PR#12, launcher 기본) | C=1 +1.5%(61.7@acc50, 2부트 재현) · 트레이스 gemm −0.95ms/step · 품질 9/9+256K 3/3 · 프리필 무영향 | 08-10 |
| **★타깃 lm_head fp8** (`VLLM_DSV4_TARGET_LM_HEAD_FP8=1`, overlay/nvidia_model.py) | **C=1 +2.7%**(acc=50 정규화 63.3, 물리예측 정확 적중 — bf16 샤드 265MB/step 제거) · acc 50.0% 무이동 · 9/9 · **그리디 발산 게이트 통과**(A↔B 중앙값 55자 = A↔C 자연발산 52자와 무구분; 이 스택은 temp0에서도 부트 간 ~50자 내 자연발산) | 08-10 |
| **★컴프레서 fp8** (`VLLM_DSV4_COMPRESSOR_FP8=1`, overlay/attention.py) | 디코드 스텝 −1.6%(44.34→43.62ms) · C=1 +3.5%(61.5, acc 동등) · 품질 9/9 · 프리필 중립 · wmma 스트리밍 6.35→2.93ms/step | 08-10 |
| **★패브릭 200G** (CRS812 4포트 `auto-negotiation=no speed=200G-baseCR4`) | **프리필 +9.1%** (2,589–2,591→2,798–2,830) · RDMA 98.0→196.0 Gb/s · **디코드 ±0**(comms 커널 67.8→67.6µs, LL 프로토콜=지연 바운드) — 채택 항목 중 최대 | 08-10 |
| **★융합 라우터 게이트** (`VLLM_DSV4_GATE_FUSED=1`, 런처 GATEFUSE 기본 1, overlay/nvidia_model.py) | **C=1 @acc50 +3.5%** (브래킷 62.11/64.28/62.14, 후보 σ0.26) · 9/9 · 그리디 발산 93자=자연 부트발산 96자 무구분 · 프리필 중립(2,870/2,801) · 트레이스: 게이트 체인 3커널 1.71→단일커널 1.18ms/step | 08-10 |
| **GPU 클럭캡 1800→2000** (`gpu-clock-cap.service`) | 프리필 +2.4% (2,524–2,532→2,589–2,591) · 디코드 C=1 ±0 / C=2 +5.6% / C=4 +2.3% (단일스트림=대역폭 바운드라 클럭 무관, 동시성에서만 이득) · 부하 64→66°C | 08-09 |
| **★★`vm.compaction_proactiveness=0`** (srv3·srv4, 런타임 sysctl) | 프리필 간헐 −15% 요동 근절(발별 pgmigrate 1:1 인과) · 60K 기준선 2,798–2,830→**2,866–2,872**(+1.4~1.6%, 스프레드 32→6) · 32K 트레이스 wall −9.2% — ※재부팅 시 증발, 영구화(/etc/sysctl.d) 운영자 결정 대기 | 08-11 |
| **★mhc small-M 타일 튜닝** (`VLLM_DSV4_MHC_SMALLM_TUNED=1`, 런처 MHCTUNE 기본 1, overlay/mhc_tilelang.py) | P2b 스윕 승자 (256,6,4) vs 스톡 (2,8): 마이크로 M=6 +16.3%/M=5 +19.5% · **인그래프 물리확인 per-call 15.6→13.1µs(−16%), −0.22ms/step** · residual bit-exact·yp rel 1.5e-7 · 9/9 · 브래킷 @acc50 64.18→**64.46**→64.01 = +0.57%(물리 예측 정확 일치). M≥8 스몰fma 영역 무접촉. 롤백 MHCTUNE=0 (env-only) | 08-11 |
| **★mhc 라운치 구성 R2** (`VLLM_DSV4_MHC_TUNED_R2=1`, 런처 MHCTUNE2 기본 1) | 전 승자 **bit-exact**(rel=0): mhc_post M분기 — 프리필 (512,4096) 마이크로 +3.6%·**인그래프 per-call 1,270→1,187µs(−6.5%)**, C=4 디코드 (256,2048) +12.7~15.3%; mhc_fused M≤10 (128,4,4) +14.3%(C=2 draft, M=12 verify는 스톡 최적 유지). e2e 브래킷은 부트롤 ±1%에 묻힘(프리필 B1 2,860→B2 2,882→B3 2,887) — **채택 근거 = bit-exact + 물리확인 + 무회귀**(C=1 64.3/C=2 104.2/C=4 143.0 정상 대역) | 08-11 |
| **★mhc big_fuse R3** (`VLLM_DSV4_MHC_BIGFUSE_TUNED=1`, 런처 MHCTUNE3 기본 1, 튜닝 커널 사본 in overlay/mhc_tilelang.py) | h_blk 1024→4096 (M>64만): 마이크로 +5.6%가 **인그래프 실현 +0.1%**(−11ms/32K캡처)로 축소 — 정직 기록. layer_input bf16-1ulp 클래스라 품질 게이트 적용: 9/9 + 디코드 무회귀. tilelang 제약: n_thr {96,160}만 컴파일 가능 | 08-11 |
| **★★one-shot RDMA AllReduce** (`VLLM_DSV4_ONESHOT_AR=1`, 런처 ONESHOT 기본 1, overlay/dsv4_oneshot_ar.cu+shim+cuda_communicator) | **디코드 C=1 +12.6% / C=2 +11.0%** (브래킷 1부트: NCCL 62.8/102.6 → osar 70.7/113.9) — 이 세션 최대. 소형 bf16 디코드 AR을 NCCL(67.6µs) 대신 커스텀 host-register one-shot(엔진내 self-test div=0, 27µs급)으로. 품질 9/9. **3중 폴백**(env·self-test 게이트·프록시 워치독)이라 실패=NCCL 자동. ★1부트 브래킷·장시간/C4/재부팅 재빌드 안정성 감시 후속 | 08-11 |
| **★IDXFREQ 기본 4→6** (serve hf-overrides `index_topk_freq`, IDXFREQ=4로 복원) | **128K 디코드 +7~11%** — 2부트 동일시드 3×3 재현 (기준 65.0/68.8/64.1 → 부트1 73.0/71.7/68.1 → 부트2 69.7/73.6/76.8, 전 표본 우위) · 2K/32K 중립 · TTFT 불변(48.4–49.0s) · 리트리벌 9/9 · **256K 니들 3/3 + 멀티팩트(한 생성 내 3사실 순차 회수) PASS (08-11 재검 완결)** · **freq 곡선 완성으로 6=봉우리 확정** (아래 스윕 섹션) | 08-11 |
| **★MAX_NUM_SEQS 기본 16→32** (런처, MAX_NUM_SEQS=16으로 복원) | **집계 디코드 C=16 290 → C=24 340 → C=32 386 tok/s (+33%)** — 고동시성 초선형(C8→16 +66%)의 연장 수확, raw acc 31~32% 평평(무붕괴) · **C=16이 cap 16/32 부트에서 290.1/289.7 동일 = cap 자체 오버헤드 0** · C=1 무회귀 · admission 상한이라 저동시성 무영향 · M=32×6=192 < GRAPH_CAP 256 (그래프 한계 ~C=42, 스트림당 12 tok/s 지연 트레이드는 명시) | 08-11 |

### 기각 — 측정으로 (재론 금지, 수치가 근거)

| 항목 | 수치 | 일자 |
|---|---|---|
| `TRIMIDX=1` (skip-topk KV 미할당) | 프리필 −3.4% (2,431), KV +0.3% (주장 +10%) | 08-09 |
| `C4AREUSE=1` / `SPFAST=1` | 완전 중립 (2,508–2,528 / 2,518–2,521) | 08-09 |
| `MAX_NUM_BATCHED=8192` | 중립 (2,507–2,511) → **잔차는 토큰 비례 비용** 확정 | 08-09 |
| `NCCL_PROTO=Simple` | 중립 (2,489–2,510) → comms는 프로토콜 아닌 대역폭 바닥 | 08-09 |
| #47474 포팅 (token_to_req 캐시) | 완전 중립 (포크에 빠른 경로 기존재) | 08-08 |
| #51430 포팅 (eager 축소) | 프로덕션 경로에서 데드코드 + BRKCG=1에선 CUDA illegal access | 08-08 |
| `VLLM_USE_BREAKABLE_CUDAGRAPH=1` | 장문 디코드 ~5배 저하 + 엔진 사망 | 08-08 |
| `B12X_INDEXER_STREAM/KV_STREAM=1` | 디코드 45–62로 폭락 (기준 85–102) | 08-08 |
| SP(#46789류) / EP / DP / DCP | b12x가 EP 거부 · DCP 프리필 반토막 · DP 디코드 −47% | 08-08 |
| SPEC_TOKENS=3 | 디코드 평균 83.5 vs 90 | 08-08 |
| **SPEC_TOKENS=7** | **C=1/2/4 전부 −10%** (51.8/89.2/126.4) — "대역폭상 위치 추가 공짜" 가설 반증: 드래프터 직렬 스텝 +2/사이클 + 커널 스윗스팟 이탈이 기대이득(+0.26tok/step)을 압도 | 08-09 |
| `draft_sample_method=greedy` | 완전 동급 (57.6/100.3/143.5; acc 46–59%) — 운영자 과거 실험은 "더 느림", 현 스택에선 동급까지 회복됐으나 이길 이유 없음 → probabilistic 유지 | 08-09 |
| **MARKOV_SCALE 0.7/1.3/1.6** | acc 49.2/48.7/49.5% = 1.0 대역(48.1–52.9)과 무구분 — **평평한 변수, 1.0 유지·재제안 금지.** 이로써 수용률 노브 축 전수 소진(temp·SPEC_TOKENS·method·scale·confidence 스케줄러=C1 구조무익) → 잔여 수용률 레버는 모델측(드래프터 재훈련/대체)뿐 | 08-10 |
| **MAX_NUM_BATCHED 4096→8192** | 프리필 2,656–2,882(평균 2,745, −2~3%+분산↑) + **KV −35%**(2.66M→1.72M tok) + 첫 부팅 재컴파일 ~3.5h — **기각.** 가설 오류의 교훈: AllReduce는 호출 절반이어도 회당 2배라 바이트 불변, 프리필 comms는 이미 대역폭 지배라 절약분=고정비뿐 | 08-10 |
| temp 0.95→0.7→0.5 (라이브 A/B) | acc 30.2→31.9→33.6%, tok/s 59.5→61.7→63.6 (**+7%뿐**) — top_p 0.44가 이미 분포를 잘라놔 temp 여지 없음; 문체 건조화 대비 무가치 → 0.95 유지 | 08-09 |
| **REFINE=1 (2-pass 드래프트 자기정제)** | **수용률 ×0.55 붕괴** (브래킷 L/R acc 41–63% ↔ C 24.8–30.7%; 배터리 창 합산 27.0%→14.5%) · C=1 중앙값 64.2/**43.2**/64.3 = **−33%**, C=2·C=4 동률 −33% · 품질 9/9 + 기동/캡처 정상 (커플링의 "q 무관 타깃분포 불변" 실증 — 메커니즘은 설계대로, 가설이 틀림). 기전: 마스크(노이즈)로만 학습된 DFlash계 backbone에 실토큰을 되먹이면 OOD로 제안이 절반 붕괴 — **backbone은 마지널 예측기가 학습 계약이라 추론시 조건화 주입 불가 확정, 재제안 금지.** "잔여 수용률 레버는 모델측뿐" 판정 실측 재확정. 코드는 FP8HEAD처럼 킬스위치 잔존(기본 0) | 08-11 |
| **ngram/copy 하이브리드 (프롬프트-룩업)** | **부팅 없이 기각** — `bench/ngram-ceiling.py` 상한 실측 (실토크나이저 · 온폴리시 12문서 13.1K tok, 툴에코 카피성 25% 포함): hybrid 정책 **+3.7%** / oracle 라우터 상한 +8.5% < 채택 바(후반 한자리), 매치 커버리지 18.8%뿐 · FULL 그래프와 싸울 엔진 구현 착수 금지. 결과 JSON `srv2:~/refine-ab-results/ngram-ceiling.json` | 08-11 |

### 비가용 — 구조적으로 (환경이 근거)

| 항목 | 이유 | 일자 |
|---|---|---|
| `VLLM_USE_B12X_MHC=1` (mHC cute 교체) | **3회 에스컬레이션 전부 컴파일 호스트 OOM** (기본 → GPU_MEM 0.45 → 0.5+inductor 1스레드; 22–25분 지점). 가중치 로드 상태의 inductor 재컴파일이 128GB 통일 메모리에 안 들어감 | 08-09 |
| #49236 / #51323(HiSparse) 직접 포팅 | 신규 `.cu` — 단, **컨테이너에 nvcc 13.2+cmake+ninja 존재 확인** → JIT 확장 빌드 경로는 열려 있음 (미착수) | 08-08 |
| #51318 / #51395 / #48993 / #50911 / #49302 | 대상 코드경로가 이 빌드에 부재/비활성 | 08-08 |
| **#47808 적응형 검증 (재실사)** | 상한 +5~10%로 기준선엔 걸치나: 코어 4파일(model_runner 148+·async_utils 94+·input_batch 61+·cudagraph_utils 43+ — 전부 미오버레이 최다분기 파일) + k별 cudagraph 다중캡처 + **init 고정 use_flattening 경계를 런타임 k가 넘나듦** + 업스트림에서도 아직 OPEN(이동 과녁) → 오버레이 비가용 확정. 이미지 세대교체 시 수령 | 08-09 |

## ★★패브릭 200G 복구 — 프리필 +9.1% (2026-08-10, 채택)

**원장의 `comms 23.4% = 4랭크 링 이론치 = 대역폭 바닥` 판정은 틀렸다.** 근거는
`NCCL_PROTO=Simple` 중립(**프로토콜 축**)뿐이었고 대역폭 축은 측정된 적이 없었다.
실제로는 패브릭이 **설계의 절반**으로 돌고 있었다.

### 실측 (ib_write_bw, srv4↔srv2)

| | 변경 전 | 변경 후 |
|---|---|---|
| 단일 half 단독 | 98.01 (100G 와이어 포화) | **109.13** ← 100G 초과 = 링크가 진짜 200G |
| 양 half 동시 | 49.01 + 49.01 = **98.02** | 98.01 + 98.01 = **196.0** |
| 스위치 링크 | 100Gbps | **200Gbps** |

**패브릭 대역폭 정확히 2배.** 변경 전엔 두 half를 동시 구동해도 합계가 단독
(98.01)과 같고 정확히 반씩 나눠 가졌다 = 100G 링크 하나를 공유. DGX Spark는
물리 QSFP 하나에 100G MAC 2개(각 PCIe Gen5 x4)를 실어 200GbE를 낸다.

변경 후 구조: half 하나 = **PCIe Gen5 x4 상한 ~109 Gb/s**, 둘 다 = **200G
와이어가 상한**이라 98씩. 196.0은 재현되며(엔진 가동 중 3회 + 목적지 분산
195.9 + `-q 4 -s 1M` 196.1) 포럼[^spark]의 동일 구성 실측 196–198과 일치한다.
**패브릭에 남은 여지 없음.**

> ★**링크 변경 후 2분 이상 기다리고 측정할 것.** 포트 강제 40초 뒤 잰
> 169.9(84.96×2)는 링크 트레이닝/FEC 정착 전 아티팩트였다. 재측정 시 196.0.

### 원인 = 스위치 포트가 오토네고로 100G에 머무름

**CRS812의 QSFP56/QSFP56-DD 포트는 오토네고를 지원하지 않는다** — 속도를 명시
지정해야 한다. `auto-negotiation=yes` 상태에선 `advertise=`를 200G만으로 좁혀도
`no-link`가 되고 100G로 되돌아간다(실측). 정답은:

```
/interface ethernet set qsfp56-1-1 auto-negotiation=no speed=200G-baseCR4
/interface ethernet set qsfp56-2-1 auto-negotiation=no speed=200G-baseCR4
/interface ethernet set qsfp56-dd-1-1 auto-negotiation=no speed=200G-baseCR4
/interface ethernet set qsfp56-dd-2-1 auto-negotiation=no speed=200G-baseCR4
```

적용 후 4포트 전부 `link-ok 200Gbps`, `fec: fec91`(RS-FEC 자동 활성), 호스트 양쪽
인터페이스 `speed=200000`. **케이블(Amphenol NJAAKK-N911)은 문제가 아니었다** —
운영자 확인대로 직결에서 200G가 나오던 물건이고, 스위치 설정만이 원인이었다.

### 성능 이득 (bench-tp4, 60K 프리필)

| | tok/s |
|---|---|
| 기준선 (클럭 2000 적용 후) | 2,589–2,591 |
| **200G 적용 후 (4회)** | **2,798 / 2,824 / 2,824 / 2,830** |
| **개선** | **+9.1%** |

**원장 채택 항목 중 최대**(이전 최대는 GPU 클럭 +2.4%). comms가 프리필의 23.4%
(단일 최대 커널 3,116ms)였다는 귀속과 정합한다.

### 디코드는 구조적으로 면역 — 대역폭 축이 아님 (트레이스 A/B)

동일 스크립트(`profile-step.py decode:600`)로 600tok 생성 스텝 예산을 100G/200G
양쪽에서 캡처:

| 디코드 스텝 | 100G | 200G |
|---|---|---|
| comms | 6.747 ms/step | **6.708 ms/step** |
| `ncclDevKernel_AllReduce_Sum_bf16_RING_LL` 회당 | 67.8 µs | **67.6 µs** |

**대역폭 2배에 AllReduce 커널이 0.3% 변했다 = 안 변했다.** 결정적 증거는 NCCL이
고른 프로토콜이 **`LL`(Low-Latency)** 이라는 것 — LL은 작은 메시지 전용으로,
데이터 4B마다 플래그 8B를 실어 **와이어를 2배 쓰는 대신 지연을 줄이는** 설계다.
즉 디코드 AllReduce는 대역폭이 남아도는 영역이라 더 줘도 쓸 데가 없다.

end-to-end도 같은 말: C=1 59.4(기준 57.4) · C=2 97.1(기준 ~101) · C=4 145.0
(기준 ~145) — **부호가 섞임 = 이득 0.** 프리필만 오르는 것이 정상이다
(프리필 AllReduce는 4,096토큰 배치 통째, 회당 3.7ms의 순수 대역폭).

**따라서 향후 통신 개선은 프리필/TTFT 항목으로만 계상할 것.** 디코드 개선을
기대하고 패브릭·NCCL을 건드리는 제안은 이 A/B로 선기각한다.

### NCCL 프로토콜 A/B — 무효 판정 + 부트침식 노출 (2026-08-10)

디코드 AllReduce(회당 48KB, 홉당 24KB) 프로토콜 축을 A/B하려 했으나 **두 셀 모두
프로토콜이 실제로 안 바뀌었다**: `NCCL_PROTO=LL128`에서도 `NCCL_PROTO=Simple`에서도
트레이스의 AllReduce 커널은 전부 `RING_LL` — env는 컨테이너에 반영됐는데(inspect
확인) 커널 선택이 안 따라왔다(원인 미규명 — 서드파티 이미지의 NCCL 빌드 의심,
코드 오버라이드는 배제: batch_invariant 경로 미사용). Tree만 유효 결과(기동 실패
= 후보 탈락).

대신 이 실험이 노출한 것: **동일 커널(RING_LL)의 per-call이 부트마다 뒤바뀐다**
— 67.6µs → 87.0 → 96.1 → **64.7**(그 다음 부트). 단조 침식이 아니라 **비단조
부트별 분산**(NCCL 채널/튜닝 롤). e2e 디코드는 오버랩이 흡수해 부트 간 평평하다
(C=1 mean 57.3/57.4/57.4). 따라서 "LL128 −4~5%", "Simple 지연지배" 류의 셀
판정은 **부트분산 노이즈와 교락 — 무효.** 커널 단위 A/B는 단일 부트 셀을 믿지
말고 브래킷(기준→실험→기준) 필수.

유효하게 남는 것(엔진 무관 실측): ib_write_lat 홉 분해 — 홉 = 고정 4.0µs +
0.10µs/KB → 6홉 41.6µs = 커널 67.6µs의 62%. KB당 성분의 실효 대역폭 80Gb/s =
와이어의 41%만 사용. **24KB 메시지는 DMA/도어벨 왕복이 한계라, 프로토콜/대역폭
어느 쪽을 흔들어도 지연 성분(62%)은 그대로다** — 통신 축의 이론 상한 자체가 작다.

★A/B 함정 2건: ①`systemctl --user restart dsv4-tp4`는 슈퍼바이저만 재시작하고
기존 스택을 adopt — env 변경 미반영. 4노드 `docker rm -f` 후 재기동 + `docker
inspect` 확인 필수. ②**연쇄 재기동 자체가 측정을 오염**(부트침식) — 엔진 A/B는
플릿 재부팅 후 정착 상태에서, 셀 순서를 브래킷(기준→실험→기준)으로.

### ★컴프레서 fp8 — 디코드 바이트 절감 채택 (2026-08-10)

wmma 정체 규명이 원장의 "드래프터 GEMM 교체 여지 ~8%" 가설을 뒤집었다:
- 두 wmma 커널의 실체 = **compressor.fused_wkv_wgate**(41레이어, [2048,4096] bf16
  복제)와 **indexer.compressor**(21레이어) 입력 GEMM + 별개 bf16-out GEMM 1종.
  드래프터 아님(드래프터 mtp 가중치는 전부 fp8 스케일 보유).
- 실효 대역폭 13.3MB/48.9µs=272GB/s, 23.4MB/85.7µs=273GB/s — **이미 LPDDR
  바닥에 정확히 붙어 있다. 커널 교체 여지 = 0.** 유일 레버는 바이트 절감.
- aux 스트림 오버랩(sum 50.3 vs wall 44.3)으로 wall 기여는 숨겨져 있으나,
  BW 포화 칩에선 바이트가 경합한다 → fp8로 절반.

**조치**: `VLLM_DSV4_COMPRESSOR_FP8=1` (기본 on, overlay/attention.py) —
**attn.compressor(밸류 경로)만** `torch.mm(bf16→fp32)`를 fp8 deepgemm(마르코프
w2와 동일 lazy-quant 패턴)으로. **인덱서 컴프레서는 운영자 결정으로 bf16 유지**
(선택 경로 — top-k가 이산적으로 뒤집히는 곳인데 바이트 절감의 ~11%뿐 = 리스크/
이득 비대칭). weights_proj도 bf16(N=64<128블록). 초기 실험은 인덱서 포함이었고
위 배터리 수치는 그 구성의 것 — 축소 후 재검증은 9/9 + bench-dec로 갱신.

**3구성 완전 비교 (08-10, 동일 조건 · acc=50% 회귀 정규화가 정본 지표)**:

| | A 원본(bf16) | B 전체 fp8 | **C 인덱서만 bf16 (채택)** |
|---|---|---|---|
| C=1 @acc50 (회귀, 잔차σ 0.2–0.5) | 59.5 | **+3.0%** (61.3) | **+2.1%** (60.8) |
| C=1 @acc55 | 62.9 | +2.9% | +2.4% |
| 디코드 스텝 | 44.34 ms | 43.62 | 42.97 (B↔C 차는 캡처노이즈) |
| wmma 스트리밍 | 6.35 ms/step | 2.93 | 3.23 (**C−B=+0.31 = 인덱서 몫 예측과 일치**) |
| 프리필 | 2,798–2,830 | 2,802–2,810 | 2,826–2,837 |
| 3.76% 오차 노출 | 없음 | 밸류+**선택(top-k)** | **밸류만** |

★원평균 비교 금지 — 수용률 운이 오염시킴(예: C 3회 표본이 38.9% 저수용을 굴려
58.9로 보였으나 6회 재측정 61.3). **C = B 이득의 70–80% + A의 리스크 표면**
(선택·라우팅 경로 원본 정밀도) → 채택 확정.

| 지표 | bf16 | fp8 |
|---|---|---|
| 품질 (2K/32K/128K) | 9/9 | **9/9** |
| 프리필 | 2,798–2,830 | 2,802–2,810 (중립) |
| 디코드 스텝 | 44.34 ms | **43.62 ms (−1.6%)** |
| bench-dec C=1 | 57.3–59.4 (acc~50%) | **61.5 (acc 50.4%, +3.5%)** |
| bench-dec C=2 / C=4 | 92.7–101 / 141.6–145 | 102.2 / 136.5(acc 52.9% 교락) |
| wmma 스트리밍 | 6.35 ms/step | **2.93 ms/step** (fp32-out 계열 소멸) |

메모리 비용: fp8 사본 +~0.4GB/rank (bf16 원본 유지 — 로더 안전; 후속으로 원본
해제 검토 가능). 롤백 = `COMPRESSOR_FP8=0`.

**품질 정밀 검증 (운영자 의심에 대한 답, 08-10)**:

| 증거 | 결과 |
|---|---|
| fp8 GEMM 상대오차 (실가중치, 오프라인) | **3.76%** — 본체 모델 모든 linear가 이미 쓰는 fp8 블록양자화와 동일 등급 |
| 하류 fp8 캐시양자화 오차와 비교 | 2.66% (0.7×) — ★"캐시에 가려진다" 논거는 **불성립**, 오차는 합성됨 |
| 인덱서 프록시 top-512 교집합 | 503/512 (선택 ~1.8% 변동) |
| 수용률 (스텝당 타깃분포 프로브) | 50.4% vs 49.8% — 무이동 |
| 리트리벌 | 2K/32K/128K 9/9 + **256K 니들 3/3**(10/50/90% 깊이, 그리디 정확일치) |

**판정: 유지 (lightweight 역할 엔진 기준).** 남은 미검증: 430K 풀레인지,
bf16 대비 로그확률 A/B(브래킷 부트 필요), DeepSeek이 컴프레서를 의도적으로
bf16으로 남긴 이유. **재검토 조건**: 로컬 dsv4가 main/품질민감 역할로 승격되면
그리디 발산 A/B를 브래킷으로 뜨고 재판정.

★검증 함정(실사고): 오프라인 오차 정량화를 **서빙 컨테이너 안에서 docker exec로
돌리지 말 것** — 제2 CUDA 컨텍스트가 TP 콜렉티브를 정지시켜 슈퍼바이저 프로브
만료→스택 강제교체를 유발했다. GPU 검증은 엔진 정지 후 또는 다른 노드에서.

**잔여 후보**: 살아남은 bf16-out wmma(2.37ms/step, 회당 50µs=13.4MB — 정체
미규명, 컴프레서 아님은 소거 확정)와 ffn.gate(43×2.1MB, 라우터 정밀도 리스크로
보류). 전자를 규명하면 ~2ms/step 추가 여지.
→ **08-10 전량 규명·종결**: 아래 "융합 라우터 게이트" 섹션 참조.

### ★융합 라우터 게이트 — 잔여 wmma 전량 규명 + 채택 (2026-08-10)

잔여 bf16 wmma 3개체군을 신규 트레이스 + 스트림 인접성 + External-id 링크 +
config/체크포인트 대조로 전부 규명:

| 개체군 | 정체 | 처분 |
|---|---|---|
| [8,2,8] ×46.2/step, 25.2µs | **MoE 라우터 게이트** (43 레이어 + mtp 3, bf16 [256,4096]) — mm+splitKreduce+fp32캐스트 3커널 = **1.71ms/step (스텝 4.2%)**, 디폴트 스트림 | **융합 커널로 채택 (+3.5%)** |
| [4,1,1] ×6.0/step, 78.8µs | `indexer.weights_proj` [64,4096], **F-레이어 6개**(21 C4A ÷ freq4 = 정확히 6)에서만 실행, aux 스트림 | 무가치 — 실바이트 0.5MB×6=3MB/step(~11µs), 지속시간은 경합 스트레치 |
| fp32-out [8,4,8] ×6.0/step | 인덱서-컴프레서 입력 GEMM (운영자 동결 bf16) | 불변 — C−B=+0.31ms 검산과 정합 |

**게이트가 느렸던 구조적 원인**: 이미지 `GateLinear`의 가속 티어 장치 판정이
전부 SM90/SM100-family 전용(`is_blackwell`=family 100) → GB10(sm_121)은 항상
Tier-4 폴백(bf16 F.linear → bf16 왕복 → fp32 캐스트). #49921 기각 판정("캐스트
1커널 융합뿐")은 Tier-3(cuBLAS 교체)만 본 것 — small-M 전용 융합은 별개 축.

**채택 구현** (`DenebGateLinear`, overlay/nvidia_model.py): M≤32에서 트리톤
단일 커널(BN16/BK512/**SK1**/stages4(BM16)·3(BM32)/warps4)이 bf16 입력 그대로
**fp32 누산 직출력** — bf16 왕복 제거로 로짓 오차 6e-2 → 1.3e-4 (500×
개선, 실가중치 오프라인 검증). 결정성 비트단위 확인. M>32(프리필)는 Tier-4
그대로 = 프리필 numerics 불변. init 프리워밍이 트리톤 스페셜라이제이션 버킷
(M∈{1,6,16,17,32})을 캡처 전에 전부 컴파일, 실패 시 영구 폴백. MoE forward가
custom-op 경계 안(eager)이라 **컴파일 그래프 무접촉 → 오버레이 소스 변경에도
재컴파일 미발생**(부트 200-221s 실증 3회).

**★신규 방법론 함정 — in-graph 소수-CTA 커널 스트레치**: 최초 SK=4 버전의
`part.sum(0)`(제네릭 reduce)이 오프라인 2.2µs → **디코드 그래프 안 12.4µs**로
5.7× 늘어짐(소수 CTA 지연바운드 커널이 경합에 최약) → e2e 이득 소멸(@acc50
61.7=기준 동률). 교훈: **스텝당 수십 회 도는 자리는 커널 개수 자체를 줄여라.**
SK=1 단일 커널 전환 후 +3.5% 실현. 오프라인 프록시는 46개 가중치 순환
CUDA-graph 콜드-사이클(sweep 243+270 구성)이 유효했다 — 단독 핫루프 타이밍은
런치갭·L2-핫으로 3중 왜곡.

**브래킷 (C=1 @acc50 회귀 정규화)**: 기준L 62.11(σ2.05) → **후보 64.28
(σ0.26)** → 기준R 62.14(σ0.66) = **+3.5%**. C=2 중앙값 101.3→103.1(+1.8%) ·
C=4 135.2→140.0(+3.6%, B3 대비). 품질 9/9. **그리디 발산 게이트 통과**: 구성
변경 발산 중앙 93자 = 동구성 부트간 자연발산 96자와 무구분 (이 프로브 셋
기준; 발산 방향도 정밀도 상승 쪽 — near-tie 라우팅이 fp32 정답 쪽으로 정렬).
프리필 2,870/2,801 = 중립. 트레이스 물리확인: 구 [8,2,8]+splitKreduce+캐스트
소멸, `_deneb_gate_partial_kernel` ×46.0/step 25.7µs 단독.

롤백 = `GATEFUSE=0` (env-only, 재컴파일 없음). 부트2 초반 C=1 3표본 이상
(acc 대비 -15~20%)은 그 부트 한정 과도현상으로 판명 — 동일 코드 부트2'에서
재현 무 (단일 부트 셀 불신 원칙 재확인 사례).

### ★MARKOV_TOPK=512 채택 — PR#12 실기기 검증 (2026-08-10)

타 에이전트 draft PR#12(GPU 없는 워크스페이스에서 작성)의 knob 2종을 5-부트
브래킷 A/B로 검증. **전제 정정 2건이 판을 바꿨다**: ①`VLLM_DSPARK_IMPL=upstream`은
**이미지가 ENV로 굽는 기본값**(docker inspect 실증) — 프로덕션은 처음부터 upstream
구현이었고 "fork vs upstream 전환 리스크"는 허상 ②매니페스트의 sparse_swa
preimage가 실이미지와 불일치(오기록) — fail-closed 프리플라이트가 전 기동을
차단했을 것. 정정 후 preimage 9/9 라이브 일치.

| arm (acc=50% 회귀 정규화, C=1) | tok/s | 잔차σ |
|---|---|---|
| 프로덕션 기준 ×2부트 (브래킷 L/R) | 60.8 / 60.9 | 0.45 / 0.33 |
| **+MARKOV_TOPK=512 부트1** | **61.7 (+1.5%)** | **0.14** |
| +MARKOV_TOPK=512 부트2 (확인) | 트레이스 wall 42.04ms · **gemm −0.95ms/step**(마르코프 W2 GEMV 제거 물리확인) · C=2/4 재현 · 9/9 + 256K 3/3 · 프리필 2,840–2,863(최고 대역) | — |
| +FP8HEAD=1 (rowwise `_scaled_mm`) | 60.6 — **기각** | 0.50 |

- **FP8HEAD 기각 근거**: acc 51.1% = 레거시(50.4–50.7%)와 동일 — 레거시의 "top6
  overlap 93%" 손실이 수용률로 번역되지 않음. 속도도 동급(지배 비용 = vocab 샤드
  132MB 스트리밍이 양쪽 fp8 동일; `_scaled_mm`=cuBLASLt는 sm_121에서 deepgemm
  수제 커널 대비 무이점 + 스케일 에필로그 elementwise 2회가 커널 밖). knob은
  실험용 잔존(기본 0).
- **채택 배선**: main 머지(#12) 후 매니페스트 런처를 프로덕션 경로(~/start-hy4-tp4.sh)로
  배포, MARKOV_TOPK 기본 512. 롤백 = `MARKOV_TOPK=0`. 이전 런처 백업
  `~/start-hy4-tp4.sh.pre-topk-adopt`.
- **★벤치 방법론 함정 (신규)**: **256K 인제스트 직후 C=1 bench-dec 금지** — 확인
  부트에서 C=1이 acc 62–67%인데 34–45 tok/s로 역전(스텝 ~2.4× 폭증), C=2부터
  자가 해소. 장문 잔류 상태가 单스트림 벤치만 오염. 배터리 순서: 벤치 먼저,
  장문 프로브는 뒤에.
- ARM1(IMPL 명시만) 58.2·σ2.56은 부트 롤 오염 표본 — 단일 부트 셀 불신 원칙의
  재확인(같은 구성의 다른 부트 4개가 전부 60.8–61.7·σ<0.5).

### 이 판정이 뒤집는 것

"소프트웨어 개선 경로 종결 선언"은 **comms를 바닥으로 오인한 상태에서 내려졌다.**
종결 자체가 무효는 아니지만(업스트림 역포팅은 실제로 소진), **"③하드웨어" 영역으로
분류해 손대지 않은 것 중에 설정 한 줄짜리 두 자릿수 이득이 있었다**는 뜻이다.
하드웨어/패브릭 영역을 "소프트웨어 밖"으로 밀어두는 분류를 재고할 것.

### 부수 확정 (재조사 불요)

- `VLLM_USE_B12X_MOE=0`인데 프로파일이 b12x 귀속되던 불일치 = 런처의 `--moe-backend b12x`
  CLI가 env보다 우선. **24.2% 귀속은 유효.**
- GDR(GPUDirect RDMA) **미가동**: `nvidia_peermem` 로드 불가(GB10 통합메모리라 PCIe P2P
  BAR 모델 부재), dmabuf는 `mlx5dv_reg_dmabuf_mr` 심볼버전 불일치. 200G 복구 후 남은
  comms 여지를 볼 때 재검토 대상.
- RoCE GID 인덱스가 노드마다 다름(srv1=3, srv2·srv4=4, IPv6 활성). 런처가 노드별 자동
  탐지해 동작엔 문제없으나 포럼[^spark]이 지목한 취약점 — 다음 정비 때 정리.
- W4A4/W4A8: `swiglu_limit=10.0` → 클램프 지원 백엔드가 FLASHINFER_TRTLLM뿐인데 그건
  SM100 게이팅(GB10=sm_121) → **b12x에서 A16이 구조적으로 강제됨**. 체크포인트 재양자화도
  무의미(로드할 백엔드 없음). **재개 조건**: ①swiglu_limit 없는 변종 발행 ②B12X 클램프
  구현 ③sm_121용 TRTLLM MXFP8 커널.
- **플릿 위생 관찰 (08-11)**: ①srv2·srv3의 bluetoothd가 만성 CPU 100% 폭주
  (누적 2~3시간; 코어 낭비일 뿐 GPU 스톨과는 무관 확인 — BT 재시작/비활성
  후보) ②~~vllm-tp2 좀비~~ **제거 완료(08-11, srv1·srv2 docker rm -f — 런북 5번 실행)** ③srv4 stkernel의
  stash@{0}은 main에 전량 흡수된 구본(런처 매니페스트 WIP) — drop 안전
  ④서버 리포 배포 정합: 4노드 -b12x 오버레이 SHA = repo main 5356be1 일치
  실사 완료 (srv2 리포 최신·클린, srv4 리포는 5커밋 뒤 — 배포는 -b12x
  디렉토리 기준이라 무영향).

[^spark]: https://forums.developer.nvidia.com/t/connectx-7-200gbe-via-mikrotik-crs812-qsfp-dd-400g-2xqsfp56-200g-breakout/357162
  (동일 구성: CRS812 + GB10 4노드. 그들의 튜닝 후 도달치 196–198 Gb/s)

## ★★UMA 페이지 마이그레이션 스톨 — 프리필 간헐 −15% 요동 근절 (2026-08-11, 채택)

채택 4건(TOPK512·lm_head fp8·컴프레서 fp8·게이트 융합) 반영 후 **트레이스
재귀속**(200G 이후 최초 실측)이 발단: 프리필 comms가 추정(~9%)의 3배인
25.2%로 나왔고, 그 모순을 파니 통신이 아니라 **호스트 커널의 메모리
컴팩션이 GPU를 간헐적으로 세우고 있었다.**

### 인과 사슬 (전 단계 실측)

1. **4랭크 스큐 분해**: rank1(srv3)만 컴퓨트 11,053ms vs 나머지 8,987–9,736
   — 나머지 3랭크의 AllReduce 3.2–3.9s는 srv3 대기였다 (srv3 자신은 1.96s).
2. **커널별 대조**: `tf32_hc_prenorm` 633→1,337µs/call (2.11×),
   `fp8_gemm<4096,12288>` 2.10× — **grid/블록/콜수 완전 동일**, per-call만 2배.
3. **타임라인**: 폭주는 캡처 13초 중 ~2.5초 구간에만 (avg 619µs 구간 ↔
   avg 5,715µs·max 8,788µs 구간). 간헐 이벤트다.
4. **외부 GPU 프로세스 없음**: 90초 0.5s 폴링에 Worker_TP1 단독.
5. **호스트 카운터 대조**: srv3 kcompactd0 누적 CPU **14분1초**(srv2 19초의
   44배), thp_fault_alloc 3,666(srv2 0), buddyinfo order≥10 **0**(고차 고갈).
   `compact_memory` 강제로도 불변 = unmovable 오염, 런타임 재건 불가.
6. **발별 인과 (스모킹건)**: 6발 중 느린 2발(2,477/2,469 tok/s)에서만
   `pgmigrate_success` +562/+318, 빠른 4발(2,88x–2,91x)은 vm 활동 0 — 1:1.

**기전**: 이동량 ~2MB라 대역폭 도둑이 아니다. UMA/ATS에서 페이지
마이그레이션이 걸어오는 **TLB shootdown이 그 페이지를 참조하는 GPU를 ms
단위로 세운다** (tf32 커널 8.8ms 스트레치의 정체). CPU 100% kcompactd가
느린 프리필 직후 top에 반복 등장(00:54:18, 00:54:50 실관측)한 것과 정합.

### 처방 + 검증

```
echo 0 | sudo tee /proc/sys/vm/compaction_proactiveness   # srv3·srv4 적용(08-11)
```

| | 적용 전 | 적용 후 |
|---|---|---|
| 34K 프리필 (연발) | 2,469–2,914 (6발 중 2발 −15%) | **2,895–2,911 ×6/6, vm 활동 0** |
| 60K bench-tp4 ×3 | 2,798–2,830 (구 기준선, 스프레드 32) | **2,871 / 2,866 / 2,872 (스프레드 6)** |
| 32K 트레이스 wall | 13,088 ms · comms 25.2% | **11,885 ms (−9.2%) · comms 17.2%** |

기준선 +1.4~1.6%에 **분산 붕괴가 더 큰 가치** — 이후 A/B의 판별 바닥이
낮아졌고, 실사용 TTFT 최악치(-15% 꼬리)가 사라졌다. 과거 "부트별 분산"
미스터리 노이즈 일부의 유력 기전이기도 하다 (NCCL per-call 67.6→96.1µs 롤
등 — 소급 단정은 불가, 기전 후보로만 기록).

### 노드 위생 현황 (08-11 실측)

| 노드 | kcompactd 누적 | thp_fault_alloc | 판정 |
|---|---|---|---|
| srv1 | 3s | 1,276 | 건강 (클럭캡 서비스 부재는 별건, 아래) |
| srv2 | 19s | 0 | 건강 |
| srv3 | 14m01s | 3,666 | **병소 — proactiveness=0 적용** |
| srv4 | **16m18s** | **16,734** | **병소 — proactiveness=0 적용** |

### 잔여 리스크 / 후속 (운영자 결정 대기)

- **적용이 런타임 sysctl이라 재부팅에 증발** — 영구화는 4노드
  `/etc/sysctl.d/`(+THP defrag 정책 동시 결정) 필요.
- THP direct-compaction 경로(defrag=[madvise])는 살아 있음 —
  `thp_fault_alloc` 증가 재개 시 direct 스톨로 재발 가능, 카운터 감시.
- 고차 페이지는 재부팅만이 재건 (proactiveness=0은 억제일 뿐 치유 아님).
- srv1은 **gpu-clock-cap.service 자체가 없어 2398–2444MHz로 가동**(부하
  실측; 나머지 3노드 1976–1989). TP 동기라 성능 이득 0, 열·의도 위반만 —
  캡 설치 여부 운영자 결정.

### 갱신된 커널 귀속 (클린 상태, 2026-08-11)

**프리필 32K** (스톨 제거 후): MoE 25.8% · GEMM 19.0% · **comms 17.2%**(구
25.2%는 오염, 구추정 ~9%는 과소) · attn/indexer 13.0% · mHC 계열 합산 ~16%
(mhc_post 1,029 + mhc_pre 621 + tf32 prenorm 490ms/캡처) · idle 0.2%.

**디코드 스텝** (600tok·250스텝, busy 39.6ms/step — 08-09 41.9 대비 채택
4건 반영): **MoE 16.5ms(41.7%, 실효 4.37GB÷16.0ms=273GB/s 바닥 재확인 —
커널 레버 0)** · GEMM 17.0ms(구 20.28 — 다이어트 실현분) · comms
6.5ms(64.5µs/call) · attn 2.3 · norm/quant 2.1 · idle 1.0. 신규 식별 항목:

| 항목 | ms/step | 비고 |
|---|---|---|
| `per_token_group_quant` 3종 ×~284콜 | 1.18 (3.0%) | fp8 입력 양자화 armada, 회당 3.5µs 소CTA — 게이트 융합과 동형의 "커널 개수" 함정. 융합패스(fuse_norm/act_quant=True)가 남긴 잔여 |
| AllGather ×2/step | 0.44 (1.1%) | 검증 로짓 수집 추정 (217µs/call). bf16 전송화 시 절반 이론 |
| `topkGatingSoftplusSqrt` ×43 | 0.48 (1.2%) | 라우터 후처리 |

셋 다 상한 1–3%로 착수 하드 룰 미달 — 다음 세대 오버레이 작업 시 동반
픽업 후보로만 잔존.

## ★REFINE_PASS A/B — 수용률 레버 실측, 기각 (−38%, 수용률 반토막) (2026-08-12)

"포화≠효율" 통찰의 직접 실현 시도. 통찰이 옳게 가리킨 효율 레버 =
**수용률**(포화 MoE 대역폭 34-expert 로드 중 실제 쓰이는 비율). 이미지의
유일한 수용률 소프트웨어 노브 = VLLM_DSPARK_REFINE_PASS. 주석 주장:
"two-pass draft self-refinement, pass-1 tokens replace noise slots, same
Gumbel keys → target 분포 구조적 불변, only acceptance moves [up]." 원장
A/B-queue 등재 항목. **실측으로 판정(선언 아님).**

동일 tg128 하드-프로즈 워크로드, /metrics 카운터 delta + usage.completion_tokens:

| | tok/s(med) | accepted/step | raw accept | per-pos |
|--|--|--|--|--|
| REFINE=0 | **58.57** | 1.07 | 21.4% | .62/.29/.12/.04/.01 |
| REFINE=1 | **36.45(−38%)** | 0.45 | **9.0%** | .33/.09/.03/.01/0 |

**주장과 정반대**: 수용률이 오르긴커녕 21.4→9.0%로 반토막, tok/s −38%
폭락, 전 position 악화. worker 로그는 기능 활성 확증("DSpark(v2) two-pass
draft refinement enabled") 하나 APIServer는 `WARNING: Unknown vLLM env
VLLM_DSPARK_REFINE_PASS` — **이 production-hybrid-1.6 이미지에서 half-wired,
refinement가 draft를 개선이 아니라 손상**(coupling 불성립 → 제안 분포 열화
→ 수용률 급락 + 추가 pass 지연 이중 손실). 부팅/osar self-test div=0 정상,
순수 REFINE 효과.

**판정: 기각, REFINE=0 유지.** 통찰(수용률=포화 대역폭 효율 레버)은
원리적으로 옳았으나, 이미지의 유일한 수용률 노브는 깨져 있어 실사용 불가.
드래프터 품질은 그 외엔 고정(딥시크 학습 dspark). → 수용률 레버는 이
이미지에서 실질적으로 닫힘. UNMEASURED→MEASURED-REJECTED로 원장 갱신.
저자 신규 이미지에서 REFINE 재구현되면 refine_ab.py로 재검(도구 리포 상주).

## ★MBU 효율 분석 — 포화≠효율, MoE 56%가 speculative 대역폭 (2026-08-12)

"GPU/대역폭 포화여도 활용이 100% 효율적이지 않을 수 있다"는 통찰로 MBU
분해. **이 세션에서 가장 깊은 재해석 — "물리 바닥"의 실체를 바꿈:**

MoE config: 256 routed expert, topk 6, W4 랭크당 ~3.15MB/expert. 단일 토큰
활성 = 6×43layer ≈ 810MB/step이어야 하나 **실측 MoE read 4.58GB/step ≈ 34
expert/layer** (이론 최대 36). 즉 **spec decode 6 position(1 verify + 5
draft)이 각자 topk 6 활성 → union ~34** (position 간 94% 비겹침).

metrics 정량: draft 12,350 생성 / 6,361 수용(raw 51.5%) → 스텝당 draft 5 중
**1.56 수용·3.44 reject**. per-pos 84→67%→기하감쇠.

**→ MoE 4.58GB 중 ~56%(2.5GB)가 reject될 draft position의 expert 로드.**
안 읽으면 스텝 37→28ms(−24%)나, **구조적으로 불가**: spec decode 병렬
검증은 모든 draft position에서 target(MoE 포함)이 무엇을 예측하는지 알아야
accept/reject 결정 — reject는 계산 **후** 판명, 예측 skip 불가. 즉 이
"낭비"는 spec decode가 **대역폭 효율(토큰당 2.2× read)을 희생해
latency(스텝당 2.56× 토큰)를 사는** 구조적 트레이드.

**판정 — 통찰은 옳고, 소프트웨어 여지는 여전히 수용률(모델)로 귀결**:
포화의 56%가 speculative지만, 그 speculation이 없으면 1토큰/스텝(2.56×
느림). 대역폭 효율↑ = 수용률↑ = draft 수용↑ = reject 로드↓ — 결국
드래프터 품질(모델측, 노브 소진). draft position MoE pruning은 구조 수술 +
정확도 리스크(confidence scheduler 기각 선례). **"포화=바닥"을
"포화의 56%가 spec decode 대가"로 정정 — 하지만 소프트웨어 결론 불변
(수용률=모델).** 디코드 최적화의 본질은 대역폭 효율이 아니라 latency(토큰/
스텝)이며 spec decode가 그 최적해임을 대역폭 관점에서 재확인.

## 정밀 관측 — idle 뒤에 여지 없음, 4스트림 포화 확증 (2026-08-12)

"관측창 세분화·정밀화 재측정" 요구로 버킷 합산 뒤를 파봄
(`scratchpad/trace_precise.py`, `gap_concurrent.py`):

- **main compute stream(tid 2746) 커널간 idle-gap = 총 5.8초** (버킷의 "idle
  0.9%"가 완전히 가림). 최대: `gemm<6,4096>→_save_partial_states` **1.53ms/회
  ×1728**.
- **그러나 진짜 idle 아님 — 4-CUDA-스트림 오버랩**: 그 갭 시간대에 다른 3
  스트림(tid 2747/2748/2750)이 **120% 채움** (MoE b12x + 병렬 gemm). 디코드가
  이미 4스트림 병렬로 GPU 완전 포화 → union busy 99%의 정체.
- **k_wait outlier 18.7×**(med 21µs, max 397µs)도 총 초과분 5ms(0.02ms/step)
  미미. gemm/mhc per-call outlier(2~6×)도 총량 작음.

**판정: 정밀 관측이 "물리 포화"를 데이터로 확증.** 뭉개진 idle 뒤에 숨은
여지를 기대했으나 그 자리엔 4스트림 병렬 작업이 꽉 차 있었다. GPU는 완전
포화, 미세 이익 없음. 소프트웨어 최적화 종결의 최종 확증.

## 미세 이익 라운드 — 실측 종결 (2026-08-11~12, "미세라도 여러개 모으면")

"물리 바닥" 선언이 이 세션 내내 여러 번 뒤집혔으므로(노브소진→컴팩션,
comms바닥→one-shot AR +12%) 선언 대신 실측으로 미세 후보 전수 점검:

| 후보 | 실측 | 판정 |
|---|---|---|
| **osar 커널 벡터화** (k_copy_in float4·k_reduce bfloat162, Ctrl alignas16) | numerics bit-exact(maxerr 0)지만 **k_reduce 75→72.6ms(−3% 노이즈)·copy 무변화** | **무이득 롤백** — 48KB 커널이 이미 grid-stride 대역폭 근처, 벡터화가 처리량 못 올림. ★함정: Ctrl 헤더 248B라 tx 16B 미정렬 → cudaErrorMisalignedAddress(alignas로 해결) |
| osar 커널 병합 5→2 (앞 섹션) | 29→56µs | 단일블록 병렬성 손실 |
| 작은 GEMM 융합 (앞 섹션) | 마이크로 +20~40% | 저자가 이미 fused_wqa_wkv로 realize |
| **one-shot AllGather** (comms 잔여 0.4ms/step) | 미착수 | dspark.py:735 all_gather=**spec decode 경로** → one-shot AR급 통합+shadow 검증+수용률 리스크. 상한 −0.9% |
| **attn/indexer flashinfer template** | 미착수 | sparse_mla_sm120 = 컴파일 .cu/.cuh, JIT 재컴파일 = one-shot AR급 작업, 상한 −2% |

**판정: 즉시·저리스크 미세 이익 전부 무이득/소진 확정.** 남은 2후보
(AllGather −0.9%·flashinfer −2%)는 one-shot AR급 통합·재컴파일 + spec
decode/재컴파일 리스크 = 별도 집중 세션 규모. 이 세션(PR 44+, 15 태스크)에서
안전 완결 불가로 다음 세션 우선순위 등재. **소프트웨어 realize 가능분은
전부 소진, 남은 것은 리스크·규모 있는 대작업 2건뿐.**

## 전 축 재확인 — 프리필·장문·attn/indexer 전수 점검 (2026-08-11, "다른 걸 더")

단일 GEMM 완결 후 리더보드 다른 test type 축까지 전수 확인:

| 축 | 우리 실측 | 판정 |
|---|---|---|
| **프리필 pp2048** | ~2,900 tok/s (TTFT 0.75s) | GPU busy 99.8% 물리 바닥 — 패브릭 200G·클럭·컴팩션으로 이미 최적, 여지 0 |
| **장문 ctx_tg@128K** | ~70 tok/s | IDXFREQ=6 봉우리 (곡선 완성 확정), 최적 |
| **디코드 tg128 c1** | 65 (리더보드 최고 52.82 초과) | comms one-shot AR·GEMM 이미융합·MoE 대역폭 = 소진 |
| **attn/indexer** (sparse MLA decode) | 2.2ms/step (4.9%) | **flashinfer 컴파일 CUDA 커널**(.cu/.cuh, template=컴파일타임 tile) — deep_gemm처럼 autotune 블랙박스, 튜닝=재컴파일 큰 작업 + 이득 상한 ~2% = 착수 바 미달 |
| **norm/rope/quant** | 1.7ms/step | 이미지 소유 커널(per_token_group_quant armada), 오버레이 밖 |

**★소프트웨어 최적화 전 축 완전 종결 (최종)**: 측정 가능한 모든
축(디코드/프리필/장문/attn/norm)이 물리 바닥이거나 컴파일-커널 튜닝
불가/이득 미미. 이 하드웨어·이 이미지에서 소프트웨어로 realize 가능한
것은 전부 realize. 남은 이득은 오직 ①모델(DeepSeek 드래프터=수용률)
②하드웨어(NVLink급 인터커넥트) ③저자 신규 이미지(감시 가동) — 전부
우리 통제 밖. **스택은 공개 리더보드(spark-arena) 4노드 dsv4 tg128 1위
상태로 종결.**

## 단일 스트림 극한 — 작은 GEMM 융합 여지 조사·이미 소진 판정 (2026-08-11)

리더보드 1위 확인 후 "현재 동시성·클럭에서 소프트웨어로 c1을 더" 요구.
디코드 GEMM 재검산이 **"통째로 대역폭 바닥"이라던 앞선 판정을 부분 정정**:

| GEMM | 실효 대역폭 | 판정 |
|---|---|---|
| M6 N4096 (48.8µs) | **344 GB/s** | 대역폭 바닥 (물리) |
| M6 N2048 (43.6µs) | 192 | 미달 |
| M6 N1024 (42.8µs) | 98 | 미달 (채우면 15µs, 2.8×) |
| M6 N512 (16.1µs) | 130 | 미달 |

작은 N GEMM은 M=6 극소 배치에서 grid=48(SM 1개씩) 블록당 작업 부족 →
레이턴시 바운드. **융합 마이크로벤치로 이득 물리 확정**
(`probes/gemm_fuse_bench.py`, fresh container): concat으로 N 키우면 —

| projection | 개별 → 융합 | 이득 |
|---|---|---|
| N512×3 | 34.7µs(182GB/s) → 20.9µs(301) | **+39.7%** |
| N512+1024+2048 | 48.1(305) → 32.3(455) | +33% |
| N2048×2 | 44.8(375) → 32.5(516) | +27% |

**그러나 실제 적용 규명 결과 = 이미 소진.** model.py:1657 stacked-params:
`attn.fused_wqa_wkv`(q_a+kv projection **이미 concat**) ·
`compressor.fused_wkv_wgate`(컴프레서 융합). 즉 트레이스의 큰 GEMM
N4096(344GB/s=대역폭 바닥)이 저자가 이미 융합한 결과물이고, 남은 개별
작은 GEMM들(q_b/kv_b)은 **서로 다른 input**(q_lora vs kv_lora 출력)이라
구조적으로 concat 불가. **융합 여지는 저자가 이미 realize** — 우리가 발견한
"여지"는 이미 처리된 것(오늘 밤 반복 패턴: 낡은 결론 뒤 이미 처리된 항목).

**판정: 단일 스트림 c1 = 물리 바닥 + 이미 융합 최적 확정.** 소프트웨어
여지 없음. 남은 것은 수용률(모델측 소진)·M 키우기(=동시성, 제외)·하드웨어.
c1 65 tok/s가 리더보드 최고(NVFP4 52.82) 초과 상태로 종결.

## ★P2b — mhc small-M 타일 채택·tf32 prenorm 기각 (2026-08-11, "소소한 이익 묶음" 1호)

운영자 지시("큰 레버 부재 시 소소한 이익 다수 합산")로 착수 바 완화 후 첫
실행. 엔진-다운 윈도우(~15분) fresh container에서 P2a 하니스 패턴(46가중치
순환 CUDA-graph 콜드-사이클 + 수치 게이트) 재사용:

- **디코드 `mhc_fused_tilelang`** (M≤16 small-fma 경로, 1.41ms/step): 스톡
  휴리스틱 (n_thr256, tile_n2, split_k8)은 저자 자신이 "TODO: investigate
  autotuning"을 남긴 자리. 29개 유효 구성 스윕 → **(256, 6, 4) 채택**:
  M=6 11.47→9.60µs(+16.3%) · M=5 +19.5% · residual bit-exact(yp rel 1.5e-7
  = split-k fp32 재배열뿐). M≥8 영역은 미스윕이라 스톡 유지(가드).
- **인그래프 물리확인**: 배포 부트 트레이스 per-call 15.6→13.1µs(−16%),
  −0.22ms/step — 오프라인 예측이 그래프 안에서 그대로 실현 (단일 커널
  유지라 게이트 SK=4류 소수-CTA 스트레치 함정 비적용 확인).
- **브래킷 (C=1 @acc50 회귀 정규화)**: 기준L 64.18 → 후보 **64.46** → 기준R
  64.01 = **+0.57%** (물리 산수 +0.55%와 일치; 개별 부트 σ보다 작은 효과는
  물리확인이 정본). 품질 9/9. 부트 210s ×3 = custom-op 경계라 재컴파일 무.
- **프리필 `tf32_hc_prenorm` n_splits 기각**: 1(현행) 620µs가 최적 —
  2/4/8 = 633/644/662µs 단조 악화. grid 32/48SM 미달은 split으로 못 채운다
  (저자 휴리스틱 재승리). mhc_post/mhc_pre 내부 타일은 커널 소스 수술이라
  차기.

배선: overlay/mhc_tilelang.py (base c7968ca8 전문 사본 + small-M 분기만),
manifest 17번째 행, 런처 `MHCTUNE`(기본 1). ※이 배포에 인코딩 팩(tok_* 4
파일, 타 세션 작업)이 동승 — 브래킷은 동일 코드베이스 env-only(B1/B2/B3)라
비교 무오염, 단 **인코딩 팩 자체의 품질 브래킷(A/B 큐 #4)은 별도 숙제로
잔존.**

### R2 — mhc 라운치 구성 2차 묶음 (2026-08-11, 같은 밤 연속 실행)

같은 하니스로 콜사이트-소유 가능한 나머지 전부 소진 (mhc_post는 kwargs
기본값 n_thr=128/h_blk=1024를 프로덕션이 그대로 쓰고 있었다):

| 커널·영역 | 스톡 | 승자 | 마이크로 | 비고 |
|---|---|---|---|---|
| mhc_post M=4096 (프리필) | (128,1024) | **(512,4096)** | +3.6% | 인그래프 실측 −6.5% (961.5 vs 1,028.5ms/캡처) |
| mhc_post M=24/20 (C=4) | 〃 | **(256,2048)** | +12.7/+15.3% | M분기 경계 64 |
| mhc_fused M=10 (C=2 draft) | (256,3,4) | **(128,4,4)** | +14.3% | |
| mhc_fused M=12 (C=2 verify) | (256,3,4) | — | 스톡 최적 | +1.4%가 최고 = 노이즈 급, 유지 |
| tf32_prenorm / b12x / 게이트 | | | | 기존 판정 불변 |

**전 승자 bit-exact (rel=0.0)** — 순수 라운치 구성이라 수치 리스크 0.
e2e 브래킷(3부트)은 프리필 부트롤 ±1%가 +0.3%급 효과를 삼켜 판별 불가
(B1 2,860 / B2 2,882 / B3 2,887 — 기준R이 후보보다 높음). **방법론 확정:
bit-exact 라운치 튜닝은 물리확인(인그래프 per-call) + e2e 무회귀로 채택
판정한다** — e2e 증명을 요구하면 이 등급의 개선은 영원히 채택 불가.
잔여: big_fuse 커널(threads=96 커널내 하드코딩)·quant 에필로그 융합은
tilelang_kernels.py 소스 수술 급 — 콜사이트 축 소진 선언.

### R3 — big_fuse 소스 수술 (2026-08-11, 채택하되 실현치 정직 기록)

콜사이트 축 소진 후 첫 소스 수술: `mhc_pre_big_fuse_with_norm`의 커널 내
하드코딩(threads=96, hidden_block=1024)을 파라미터화한 사본을
overlay/mhc_tilelang.py **안에** 정의 (tilelang_kernels.py 무접촉 — lazy
JIT 팩토리로 GPU-less 임포트 안전성 유지). M>64에서만 사용, 게이트
`MHCTUNE3` 기본 1.

- **스윕**: tilelang 레이아웃 제약으로 n_thr∈{96,160}만 컴파일 가능
  (128/192/256/384 전부 "no available layout" 거부 — 워프0 sinkhorn 분기
  구조). 승자 **h_blk=4096** (Pipelined 1블록): 마이크로 M=4096 +5.6%
  (892→842µs). 디코드 M≤6은 스톡 정확히 최적 → M분기.
- **실현 (인그래프)**: 캡처당 −11ms (+0.1% 프리필) — **마이크로 +5.6%가
  인그래프에서 크게 축소** (경합 환경에서 파이프라인 제거 이득 소멸).
  단일부트 프리필 3발 2,926–2,928(+1.5%)은 대부분 부트롤 상방으로 귀속.
- **수치**: comb_mix bit-exact · layer_input rel 1.2e-3 = bf16-1ulp 리듀스
  순서 클래스 → **품질 게이트 필수 적용, 9/9 통과** + 디코드 무회귀
  (C=1 64.9 / C=2 107.5 / C=4 145.2).
- **사고 1건 (수정됨)**: 커널 사본 추출 시 다음 커널의 데코레이터가 딸려
  들어가 부트 NameError — ★교훈: **오버레이 파일 변경은 py_compile로
  부족, 이미지 컨테이너 임포트 스모크 필수** (GPU 불요:
  `docker run --rm -v <file>:<target>:ro <image> python3 -c "import ..."`).
- 이로써 mhc 축은 R1+R2+R3로 완전 소진. quant 에필로그 융합은 custom-op
  시그니처 변경(재컴파일+인터페이스 수술) 대비 +0.4%라 기각 유지.

## 정찰 일괄 기각 — 잔여 후보 전수 소진 2회차 (2026-08-11)

착수-전-상한 하드 룰 적용. 부팅 없이 코드/산수/실사로 종결:

| 후보 | 판정 | 근거 |
|---|---|---|
| `VLLM_ENABLE_PCIE_ALLREDUCE=1` | **구조 비가용** | custom_all_reduce.py 첫 게이트 "그룹이 노드 걸침→무조건 disable". 단일노드 다중GPU 전용 — 4노드×1GPU에선 켜도 no-op |
| EAGLE3.1 드래프터 교체 (~/models/DeepSeek-V4-Flash-EAGLE3.1) | **산수 기각** | 드래프터 1.8GB bf16 별도 스트리밍(rank ~450MB×5 draft ≈ +2.3GB/step = 스텝 +23%) → 손익분기 acc 69% vs 현행 co-trained dspark 52% = 비현실. 부가: max_position 16384(430K 부정합)·draft_vocab 32000. dspark는 본체 내장(graft 5.3MB)이라 비용 비대칭 |
| `VLLM_USE_B12X_SPARSE_INDEXER=1` (구 A/B 큐 ②) | **상한 기각** | 실체=스케줄 메타데이터 빌더 교체(indexer.py:331). 인덱서는 프리필 ~1.5%·디코드 <2% 버킷 → 상한 <1%, #49897 NaN 커널 쌍이 바로 이 경로(리스크만 실재) |
| `fuse_attn_quant=false` (구 A/B 큐 ①) | **상한 기각** | 이득 가설=piecewise 프리필 그래프 회복인데 프리필 GPU busy 실측 99.8% → 상한 0.2% + attn-quant 융합 상실은 확정 손해 |
| #51323 HiSparse nvcc 포팅 | **대상 무관 확정** | HiSparse는 DeepSeek **V3.2/GLM 5.1**용 sparse attention — V4-Flash는 자체 DSA/C4A 스택. nvcc JIT 경로의 마지막 실타깃 소멸 |
| `VLLM_DSPARK_CONFIDENCE_SCHEDULER=threshold` (구 "Deprioritized") | **상한 기각 (08-11 코드 정독, 부팅 없이 종결)** | 이미지 speculator.py 구현 = **방출 draft 토큰 truncation만** ("captured forward shape is never changed" — 고정 draft 블록·verify 그래프 전부 그대로 실행). C 불문 스텝 시간 절약 기전 부재, 수용 기회 상실(하방)만 실재 → C>1 "batched-verify benefit" 가설도 이 구현엔 해당 없음. confidence head 가중치는 로더가 드랍 중(신규 이미지 배선 대기)이나 판정과 무관 |
| 저자 신규 이미지 | 없음 (08-11 확인) | Docker Hub 최신 = production-hybrid-1.6 (07-26) — 감시 유지 |

## 소프트웨어 개선 경로 — 종결 선언 (2026-08-09)

- **업스트림 역포팅**: 적용 가능분 전량 소진 (채택 6건 + 기각/비가용 다수 — 판정 원장 참조).
- **이미지 세대교체**: 경로 없음 — 현행 `production-hybrid-1.6`(07-26)이 저자 최신 발행본이며, 3.75 계열(07-13)은 운영자 과거 실측에서 열세 확인. 신형 발행 시에만 재개.
- **#50424 드래프터 fp8**: 상한 +3~4% < 채택 기준(후반 한자리) → 하드 룰로 부팅 없이 기각.
- 향후 이득 원천: ①저자 신규 이미지 발행 감시(Docker Hub 태그) ②업스트림 머지분의 차기 이미지 유입 ③하드웨어.

## 남은 검증된 레버 (우선순위순, 2026-08-11 전면 갱신)

1. ~~GPU 클럭~~ **결정 완료: 2000 적용** (+2.4% 공정실측). 2200 재도전 보류.
2. ~~컨테이너 내 nvcc 커널 빌드~~ **종결 (08-11)**: #49236은 슬림 포팅으로
   성능 중립 실증 완료, 마지막 실타깃 #51323 HiSparse는 V3.2/GLM 전용 =
   대상 무관 (정찰 일괄 기각 표).
   ※ **수용률 축은 완전 소진** (2026-08-09): SPEC 3/5/7 스윕·greedy·temp 전부
   실측 종결. #47808(적응형 검증)은 34파일 엔진 대수술이라 오버레이 비가용.
3. **호스트/UMA 위생 영구화** (신규, 08-11): compaction_proactiveness=0
   4노드 영구화 + THP defrag 정책 + 플릿 재부팅(고차 페이지 재건) — 실현된
   +1.4~1.6%를 재부팅 후에도 유지하는 유일한 길. 운영자 결정 대기.
4. 잔여 마이크로 후보 — 08-11 "소소한 이익 묶음" 지시로 재평가 후 처분:
   ~~P2b mhc/tf32 스윕~~ **실행 완료** (mhc small-M +0.57% 채택 · tf32 기각,
   위 ★P2b 챕터). ~~quant armada 융합(1.18ms/step)~~ 기각 — 생산자 귀속
   결과 `_fused_q_kv_rmsnorm`(이미지 C++)·`act_and_mul`(이미지)·deep_gemm
   출력 체인(.so)이 대부분 = 오버레이 소유권 밖, 소유 가능분(mhc_pre 에필로그)
   ~0.15ms뿐. ~~AllGather bf16화~~ 기각 — 실체는 [6,4096] hidden 48KB,
   217µs는 대역폭 아닌 동기화 대기(전송 다이어트 무효). topkGating 융합은
   게이트 BN256 재구성 필요(243-cfg 스윕상 BN32 최적과 상충)로 손익 불투명
   보류.
5. 그 외는 하드웨어(GDR — 디코드 comms는 지연 62%가 면역이라 상한
   미미)·모델(mHC 구조, 드래프터 재훈련) 영역 — 소프트웨어 밖.

**2026-08-11 재정찰 총평**: 분류-밖 재조사 2회차의 수확은 노브가 아니라
**호스트 커널이었다** (UMA 컴팩션 스톨). 소프트웨어 노브 축은 이제 정말
전수 소진 — 감시 2종(저자 이미지 태그·업스트림 차기 이미지)과 호스트
위생만 남는다.

~~대기 항목~~ **→ 전량 완결 (08-11 오전, IDXFREQ 스윕 확장 실행)**:

### IDXFREQ 곡선 완성 — 6=봉우리 확정, 8/10/12 기각 (2026-08-11)

운영자 지시("한방에 12로")로 곡선 오른쪽을 한 세션에 채움. 동일시드
(11/12/13) 128K bench-ctx, 셀당 1부트:

| freq | 시드11 | 시드12 | 시드13 | 평균 | 니들 |
|---|---|---|---|---|---|
| 4 (구 기본) | 65.0 | 68.8 | 64.1 | 66.0 | 9/9 |
| **6 (현 기본)** | 69.7–73.0 | 71.7–73.6 | 68.1–76.8 | **70.9–73.4** | 9/9 + **256K 3/3 + 멀티팩트 PASS** |
| 8 | 60.7 | 71.9 | 65.4 | 66.0 | 9/9 |
| 10 | 67.6 | 75.2 | 58.7 | 67.2 (분산↑) | 9/9 |
| 12 | 61.6 | 72.7 | **39.6** | 58.0 (불안정) | 9/9 |

**판정**: 8부터 이득 소멸 + 분산 확대, 12에서 표본 붕괴(39.6, 조기종료
동반) — **6 유지 확정, 8/10/12 기각.**

### one-shot RDMA AllReduce 정찰 — Stage-0 게이트 통과 (2026-08-11)

디코드 comms 6.5ms/step(스텝의 16%, 콜당 65µs — 링 6홉 순차 지연이 62%)의
유일한 공략로인 **커스텀 cross-node one-shot AR**(각 랭크가 3피어에 직접
송신 + 로컬 리듀스) 타당성 검토. **원장의 기존 비관 2건을 정정한다:**

1. ~~"GDR 고장이라 호스트 바운스가 이득을 갉아먹음"~~ — **UMA에서 불성립.**
   GB10은 GPU 메모리=호스트 LPDDR이라 호스트-핀 버퍼(ibv_reg_mr 표준 등록,
   GDR 불요)를 GPU가 풀스피드로 읽고 쓴다. 디스크리트의 바운스 페널티
   (PCIe 왕복)가 이 플랫폼엔 없다.
2. ~~"fan-out 경합이 지연을 키울 것"~~ — **실측 반증**: 24KB RDMA write
   지연 단독 6.36–6.53µs(모델 4.0+0.10/KB 정확 재현) → **3피어 동시
   6.68–6.72µs (+0.3µs)**, p99 14.3µs. NIC 2개가 3플로우 흡수 (perftest
   rdma_cm, 엔진 가동 중 — 선례 준수).

**산정 (실측 기반)**: one-shot 총지연 = GPU 쓰기+플래그(~3µs) + 프록시
busy-poll(~1µs) + 48KB×3피어 병렬 송신(~9µs) + GPU 폴+리듀스(~4µs) ≈
**15–20µs vs 현행 65µs → 콜당 ~45–50µs × 94콜 = 스텝 −4.5ms (−11~12%)
= C=1 디코드 +12~13%** — 잔여 카드 중 최대급 (게이트 융합 +3.5%의 3배+).

**남은 게이트**: Stage-1 (1–2일) — 유니파이드 메모리 E2E 프로토타입 (CUDA
커널→핀버퍼+플래그, 전용코어 프록시 post_send, GPU 폴+리듀스), 게이트
<25µs. Stage-2 (1–2주) — vLLM 통합: FULL cudagraph 안 호스트 액션은
`cudaLaunchHostFunc`(캡처 가능 host node) 또는 mscclpp식 device-FIFO+상주
프록시; 주입 지점은 custom_all_reduce.py(오버레이 가능 경로) + env 게이트
NCCL 폴백. 수치: 리듀스 순서 변경이라 bit-exact 아님 → 품질 게이트 필수.
리스크: 프록시 사망 모드(워치독 필요)·RC QP 풀메시 유지 관리.

**Stage-1 실행 결과 (2026-08-11, 조건부 통과)** — 4노드 실동작 프로토타입
(`probes/oneshot_ar.cu`, rdma_cm 풀메시 RC + 연결별 기본 PD/MR + 코어핀
busy-poll 프록시 + GPU 퍼시스턴트 커널, 5000라운드 × 48KB, numerics 검증):

| 모드 | E2E (wall/round) | 해석 |
|---|---|---|
| **SIGONLY** (시그널+와이어 코어) | **22.06µs (p99 24.1)** | **게이트 <25µs 통과** — NCCL 65µs 대비 −66% |
| FULL (순진 데이터패스 포함) | 46.5µs | 차분 24.5µs = GPU가 비캐시 mapped 호스트메모리를 직접 read하며 reduce한 프로토타입 아티팩트 |

**수정 산정**: 통합 시 데이터 이동은 그래프-내 `cudaMemcpyAsync`(4×48KB
DMA ≈ 3–5µs)로 대체 → **현실 총 ~25–27µs = 콜당 ~40µs 절감 × 94 = 스텝
−3.7ms (−9~10%) ≈ C=1 +10%.** Stage-2 과제에 **데이터패스 설계**(memcpy
노드 또는 GB10 ATS 정합-캐시 경로로 pageable 직접 접근) 1건 추가. 개발
함정 3건 기록: ①rdma_verbs 헬퍼는 교환 버퍼도 MR 안이어야(assert)
②수동 `rdma_create_qp`는 `qp_type=IBV_QPT_RC` 명시 필수(EINVAL)
③이미지에 rdma_cma 헤더/librdmacm 부재 — 호스트 마운트로 빌드·실행.

**Stage-2 코어 실증 (2026-08-11 같은 밤, `probes/oneshot_ar2.cu`)** —
통합 전 3대 미지수를 전부 4노드 실측으로 해소:

| 미지수 | 결과 |
|---|---|
| ①프로덕션 배선 (librdmacm 없는 이미지) | **해소** — 순수 libibverbs 재작성 (수동 RC QP INIT→RTR→RTS, RoCEv2 GID 자동탐지, TCP 부트스트랩→실통합은 torch.distributed 스토어). 컨테이너 자체 빌드·실행 성공 |
| ②FULL cudagraph 호환 | **해소** — per-call 시퀀스(guard→copy-in→signal→wait→H2D→reduce)를 캡처, 2000리플레이×8콜 seq 정확(16,800)·numerics OK. **그래프 오버헤드 +2.5µs뿐** (코어 22.06→24.61µs) |
| ③데이터패스 | 부분 — 커널복사(비캐시 read)는 210µs로 사망, **slot-major 레이아웃 + 단일 144KB H2D + WC 커널 copy-in = 42.8µs** 도달. 잔여 18µs는 소형 DMA 고정비/copy-in — 커널 병합·배치로 30–35µs 경로 가시 |

**핵심 설계 발견**: `CALLS % RING == 0`이면 콜별 슬롯이 리플레이 불변 →
고정 주소 memcpy 노드 캡처 가능 (런타임 슬롯 산술 불요).

### ★★UMA 제로카피 데이터패스 — 스텝 −9% 확정 (2026-08-11)

"개선폭이 작다"(staged 42.8µs = 스텝 −5.2%)의 진원을 규명: 32µs가 전부
데이터패스였고, 그 원인은 **버퍼 할당 방식**이었다. 4-way 게이트
(`probes/uma_datapath.cu`, 48KB reduce 지연):

| 버퍼 | RDMA 등록 | GPU read 48KB |
|---|---|---|
| A `cudaHostAlloc(Mapped)` (구 direct) | OK | **22.5µs** (uncached PCIe식) |
| B/C `cudaMallocManaged` | **불가** | 9.8µs |
| **D `malloc`+`cudaHostRegister`** | **OK** | **9.8µs** |

**mode-1(direct)이 210µs로 죽은 진짜 원인 = `cudaHostAllocMapped`의 device
포인터가 GPU를 uncached 경로로 강제**한 것. managed는 캐시되지만 RDMA
등록 불가. **D만 둘 다 만족** — pageable 버퍼를 host-register하고 GPU가
host VA를 직접 접근하면 GB10 ATS가 native L2 캐시 경로로 처리. 이걸
Ctrl 전체에 적용:

| 데이터패스 | E2E/AR | 코어 대비 |
|---|---|---|
| 시그널+와이어 코어 (데이터 0) | 24.30µs | 물리 바닥 |
| **★direct host-register (채택 경로)** | **27.10µs** | **+2.8µs (데이터패스 소멸)** |
| staged memcpy (구 설계) | 42.72µs | +18.4µs |

**확정 이득: 27.1µs vs NCCL 67.6µs = 콜당 −40.5µs × 94 = 스텝 −3.8ms
(−9.6%) = C=1 디코드 +10.6% (~65→~72 tok/s 예측).** staged 대비 realize
폭 거의 2배. 물리 바닥 24.3µs가 comms 완전제거 −14% 중 −5%를 wire 물리로
남기지만(RC write 완료 대기 = 하드웨어), **−9.6%는 소프트웨어로 realize
가능한 최대**로 확정.

잔여 작업(별도 세션, 순수 배관): 커널 병합(guard+copy+signal /
wait+reduce로 8콜→2커널), vLLM custom-op 패키징(부트시 nvcc 빌드),
custom_all_reduce.py 오버레이 주입 + 사이즈 게이트(≤512KB, 프리필/큰
텐서는 NCCL), torch.dist 스토어 부트스트랩, env 폴백
(VLLM_DSV4_ONESHOT_AR), 품질 게이트(리듀스 순서 변경 bit-exact 아님)+브래킷.

### Stage-3 통합 설계 확정 (2026-08-11, 주입 지점 실코드 확인)

이미지 통신 경로 정독으로 오버레이 훅 확정:
`tensor_model_parallel_all_reduce`(communication_op.py) → `get_tp_group().
all_reduce` → `CudaCommunicator.all_reduce`(**단일 파일 오버레이 지점**,
device_communicators/cuda_communicator.py). 여기 최상단에 size-gate 분기를
끼운다:

```
if _ONESHOT_ENABLED and _oneshot_eligible(input_):   # bf16, M<=32, <=512KB, decode
    if _SHADOW: nccl_out = <기존경로>; osar_out = oneshot(input_)
               _record(divergence, dt); return nccl_out     # 무해 관측
    return oneshot(input_)                                    # 실경로
```

**부품 상태**: 알고리즘/물리 = 완결 (probes/oneshot_ar2.cu 27.1µs, device
in/out·그래프·numerics 검증). 남은 배관 6:
1. **torch custom-op 래퍼** — 프로토타입 로직을 `torch.ops.dsv4.oneshot_ar
   (Tensor)->Tensor`로. host-register Ctrl은 부트 1회 할당, torch 텐서는
   D→HR copy-in 커널(=프로토타입 k_copy_in, device src 이미 실측 포함).
2. **부트타임 nvcc 빌드** — `torch.utils.cpp_extension.load()` (이미지에
   nvcc 13 확인). 첫 부트 수십초, 캐시.
3. **torch.dist 부트스트랩** — TCP 대신 `torch.distributed` 스토어로 QP
   info(qpn/psn/gid/rkey/addr) 교환. rank/fabric-IP 매핑은 기존 launcher
   NODE_RANK.
4. **프록시 스레드** — 엔진 워커 프로세스 내 코어핀 busy-poll (코어 18,
   서빙 스레드와 비충돌 확인 필요). 워치독: ack_seq 정지 감지 시 NCCL
   폴백.
5. **cudagraph 캡처** — custom-op이 FULL 그래프에 들어가야. k_signal/
   k_wait가 그래프 노드로 캡처됨은 프로토타입서 검증(리플레이 2000×8).
6. **게이트/브래킷** — VLLM_DSV4_ONESHOT_AR + SHADOW 기본; 품질 9/9 +
   그리디 발산 + C=1/2/4 브래킷 후 실경로 전환.

**리스크 등급 = 이 세션 최고**: 실경로 전환은 프로덕션 collective 교체라
실패 시 서빙 다운(분산·그래프·RDMA 교락 디버깅). **shadow-first 필수** —
NCCL 실경로 유지하며 numerics/속도만 관측, 검증 후 env로 전환. 착수는
전용 세션(멀티 재기동·디버깅) 권장.

### ★★Stage-3 실경로 채택 완료 (2026-08-11)

착수·검증·채택을 한 세션에 완주. 실행 로그:

- **torch custom-op 빌드**(overlay/dsv4_oneshot_ar.cu): cpp_extension.load
  부트타임 nvcc, 엔진 프로세스 내 로드 성공. standalone 4노드
  (probes/oneshot_ar_disttest.py): torch.dist 부트스트랩 + numerics
  M=6/16/32 **maxerr 0** + graph 29.25µs.
- **shim 첫 시도 함정 (수정됨)**: 매-호출 shadow가 상태ful tx_seq 링을
  프로덕션 AR과 **비대칭 구동** → maxdiv=128 + 요청지연 (rank2 19s 늦은
  connect가 비대칭 방증). **처방**: shadow를 부트 1회 **lockstep
  self-test**(barrier 감싼 단일 osar-vs-NCCL)로 축소 → div=0 격리 검증,
  실사용은 NCCL. real(shadow=0)만 실경로(AR 자리 정확 대체=lockstep 자동).
- **엔진내 검증**: self-test div=0 (4랭크) · 요청 200 OK · spec decode
  정상(acc 5.17) · FULL+dspark 그래프 캡처에 osar 포함 완료 · 품질 9/9.
- **브래킷** (동일 벤치, 재기동 A/B): NCCL 62.8/102.6 → osar 70.7/113.9 =
  **C=1 +12.6% / C=2 +11.0%**. 예측(스텝 −9.6%→디코드 +10.6%) 정합·초과.
- **배선**: 런처 `ONESHOT` 기본 1 + `OSAR_SHADOW` 기본 0(real). 3중 폴백
  (env / self-test 게이트 / 프록시 워치독+예외 → NCCL). 프리필·대형 텐서는
  size-gate(>256KB)로 NCCL 유지.
- **★후속 감시 5건 — 4건 확정 (2026-08-11)**:
  | # | 항목 | 결과 |
  |---|---|---|
  | ① | 2부트 재현 | ✅ C=1 71.2→69.6·C=4 156.3→155.3 (진짜 docker rm -f 재기동, self-test div=0 신규 타임스탬프) |
  | ③ | C=4 고동시성 실경로 | ✅ **155–156** (NCCL ~144 대비 +8%) — 고동시성서도 osar 이득 |
  | ④ | 재부팅 cpp_extension 재빌드 | ✅ 255s 부트 + self-test PASS (컨테이너 재생성=캐시소멸이어도 부트타임 재빌드 정상) |
  | ⑤ | acc @acc50 정규화 | ✅ C=1 raw-acc 회귀 → **@acc50 ~70** (NCCL ~65 대비 **+7~8%**, 롤 제거 후 두자릿수 유지) |
  | ② | 장시간 프록시 안정성 | ✅ **20분 연속 부하 470요청(수만 osar 호출) 실패 0**·헬스 정상·spec-decode 정상(acc 4.0) |
  ★함정: `systemctl restart`는 supervisor만 재시작하고 **기존 스택 adopt**
  (self-test 타임스탬프 불변으로 검출) — 진짜 재기동은 4노드 docker rm -f 필수.
  **판정: 실경로 채택 완전 확정 (감시 5/5 통과)** — one-shot AR은 검증된
  프로덕션 채택. 롤백 = ONESHOT=0 재기동. 코어18 프록시 경합·QP 에러 등
  드문 사건은 워치독+예외 폴백이 커버(NCCL 자동 복귀), 지속 운영 감시만.

### one-shot AR 추가 최적화 2축 — 둘 다 무이득, 27µs=물리 최적 확정 (2026-08-11)

채택 후 wire 24µs를 더 짜는 2개 축을 standalone 4노드로 실측:

| 축 | 결과 | 해석 |
|---|---|---|
| **UC(비신뢰연결) 전송** (`OSAR_QP=uc`) | RC 29.08 → UC **29.18µs** (무이득), numerics 0 | **wire는 RC ACK 왕복이 아니라 순수 전송+폴링 지연 물리** — 신뢰성 오버헤드 없음. RoCE 로컬 ACK가 이미 무시가능 |
| **커널 병합 5→2** (`OSAR_FUSED=1`, 단일블록 guard+copy+signal / wait+reduce) | 29.16 → **56.17µs (2배 악화)** | 단일블록의 copy/reduce 병렬성 손실이 launch 노드 절감(~1µs)을 압도. grid 병렬 5커널이 최적 |

**결론: 27µs(엔진)/29µs(standalone)가 이 아키텍처의 물리 최적.** 데이터패스
+2.8µs(이미 소멸급), wire 24µs(전송+폴링 물리, UC 불변), 커널 grid-병렬
최적(병합 역효과). comms 완전제거 −14% 중 **−9.6%가 소프트웨어 realize 한계**로
재확인. 두 옵션은 실험 인프라로 scratchpad에만(프로덕션 overlay는 원본 5커널
RC 유지 — 무이득 코드 배포 안 함). **one-shot AR 축 종결** — 추가 comms
이득은 하드웨어(NVLink급 인터커넥트)뿐.

### one-shot AR 후 디코드 재귀속 — 소프트웨어 축 완전 종결 (2026-08-11)

채택 후 스텝 예산 재분해 (profile-step decode:600, real osar):

| 버킷 | 구(NCCL) | 신(one-shot) |
|---|---|---|
| comms (NCCL 커널) | 6.5ms/13.5% | **0.4ms/1.0%** (AllGather 잔여만) |
| osar 커널 (k_wait 2.7+reduce 0.3+copy 0.2, "other") | — | 3.2ms/step |
| MoE (W4A16, 대역폭 바닥) | 16.5 | 16.8ms/37.4% |
| GEMM (M=6 극소, 가중치 스트리밍) | 17.0 | 16.4ms/36.6% |
| 스텝 총 | 39.6ms | **37.1ms** |

**NCCL AllReduce가 트레이스에서 소멸, osar 커널이 대체 — comms 커널레벨
6.5→3.2ms 눈으로 확인.** 새 병목 없음: 74%가 MoE+GEMM 대역폭 바닥, k_wait
2.7ms는 wire 물리(UC 불변 확인), 나머지 규명·처리 완료.

**마지막 미규명 `cutlass_80_wmma` 148ms(0.59ms/step) 규명 — 전량 기존
기각 항목**: ①wmma_128x1 ×987 (드래프터 mtp head GEMM, `_save_partial_states`
앞) = **FP8HEAD 기각분**(acc 무이동) ②wmma_128x2 ×754 (인덱서 weights_proj
[64,4096], `_fused_kv_compress_norm_rope_insert_indexer` 앞) = **N=64<128
fp8 구조적 부적격**(동결). 새 이득 아닌 재확인.

**★소프트웨어 개선 축 완전 종결 (재확인)**: 디코드 스텝의 모든 버킷이
물리 바닥(가중치 스트리밍 273GB/s) 또는 규명·기각 완료. one-shot AR이
comms의 소프트웨어 여지를 소진(−9.6%). 추가 두 자릿수 이득은 오직
①모델(DeepSeek 차기 드래프터 = 수용률) ②하드웨어(NVLink급 인터커넥트 =
wire 물리) ③저자 신규 이미지(감시 타이머 가동)뿐. 이 스택은 이제 이
하드웨어·이 이미지의 소프트웨어 상한에 전 축 도달.

### co-ingest 디코드 멎음 — 특성화 + 완화 기각 (2026-08-11, 운영자 결정)

저동시성 실사용의 숨은 지배 항목 정량화 (`bench/streamgap.py`): 디코드
스트림 1개 진행 중 34K 인제스트를 병행하면 —

| 구간 | 디코드 진행 |
|---|---|
| 인제스트 전/후 | 25.2 / 26.1 chunk/s |
| **인제스트 중 (11.3s)** | **1.2 chunk/s (−95%)** |

갭 서명 = **1.28–1.34s × 8회 = 프리필 청크(4096tok ≈ 1.3s)당 1회** —
chunked prefill의 스텝 시간이 청크에 비례하고 동승 디코드는 스텝당
1사이클이라 생기는 구조적 현상. 유일 레버 = `MAX_NUM_BATCHED` 축소
(2048→정지 0.65s, 1024→0.33s), 대가 = 프리필 −1~6% 추정 + **청크 셰이프
재컴파일 ~3.5h**(8192 셀 실증 비용). **운영자 결정: 4096 유지** (동시
인제스트 빈도 대비 재컴파일 비용 기각). 재론 조건: 다중 에이전트 상시
운용으로 co-ingest가 일상화될 때.

### 부트롤 ±1% — NCCL 무혐의 확정, 메모리 배치 로터리로 귀속 (2026-08-11)

프리필이 부트마다 2,860–2,936을 구르는(±1%대) 원인 규명. 가설 "NCCL
링/채널 배치 롤"을 동일 구성 연속 부트 + `NCCL_DEBUG=INFO` 토폴로지
지문(휘발 필드 정규화 후 해시, `probes/nccl_fingerprint.py`)으로 검증:

| 부트 | NCCL 지문 (362 토포라인) | 60K 프리필 정착 |
|---|---|---|
| 1 | `5b3ecb5ea67114e4` | 2,908 |
| 2 | `5b3ecb5ea67114e4` (동일) | 2,934 |
| 3 | `5b3ecb5ea67114e4` (동일) | 2,925 |

**지문 3부트 완전 동일 × 성능 0.9% 롤 = NCCL 채널 배치 무혐의** (가설은
"지문이 다르고 성능과 동행"을 예측 — 정면 반증. 4부트 예정을 운영자
판단으로 3부트 조기 종결, n=3 명시). 채널 맵은 부트 불변(결정적)이며
NET/IB/0·1 두 하프에 교대 배정 — 과거 "NCCL per-call 67.6→96.1µs 부트롤"도
채널 배치가 아니라 다른 원인이라는 뜻. **잔여 용의 = UMA 물리 페이지 배치
로터리**(41GB 가중치 로드 시점의 페이지 할당이 부트마다 다름 — 컴팩션/
단편화 서사와 같은 계열). 페이지 배치는 핀 수단이 없어 **관측 종결** —
실무 함의는 기존 원칙의 물리적 근거 확보: **±1%급 효과는 단일 부트로 판별
불가, 브래킷/동일부트 비교 필수.** 부수: NCCL_DEBUG=INFO 관측 오버헤드는
무시 가능(3부트 전부 대역 상단).

부수 처분: **MARKOV_TOPK 256/1024 곡선 미완 건은 상한-기각** — 512 채택의
이득 기전(W2 GEMV 제거)은 topk 값과 무관하고, 값 변경의 잔여 효과(gather
바이트 ±수백 KB/step)는 위 판별 바닥(±1%) 아래라 셀 소진 부적격.

**기전 (사전 추정의 정정)**: "니들이 깨지는 최대"를 물었을 땐 12–16으로
추정했고 니들 축에서는 실제로 전 셀 9/9 유지 — 그러나 **속도가 수용률
경유로 먼저 무너진다**: 낡은 인덱스가 target 분포를 미세 변질시켜 ①그리디
텍스트 자체가 바뀌고(조기종료 156/189tok 관측) ②드래프터-타깃 정합이
깨져 acc 하락 → tok/s 폭락. 니들(프리필 직후=인덱스 최신 순간 측정)은 이
고장 모드에 구조적으로 둔감하다 — 그래서 `bench/needle-256k.py`에
**멀티팩트 게이트**(한 생성 안에서 25/50/75% 깊이 3사실 순차 회수 = 생성
중 검색 전환 강제)를 추가했고, freq=6은 이것까지 통과했다. 시드13이 신선도
민감 표본(64.1→65.4→58.7→39.6 단조 악화)이라는 것도 부수 확인 — 향후
freq류 실험의 카나리아 시드로 쓸 것.

## 업스트림 비교체크 (2026-08-09, PR·이슈 ~25건 vs 이 스택)

로컬 사본(sai/moe/oproj probe·p474 히스토리)·이미지 파일·부팅 로그·포렌식 대조.

### 판정

| 건 | 판정 | 증거 |
|---|---|---|
| #49897 인덱서 프리필 NaN→IMA(SM12x) | 포크 자체 해결 — 무사 | 문제 커널 쌍이 이미지에 가드 없이 실존 + `VLLM_USE_B12X_SPARSE_INDEXER=False` 기본 확인 = 프로덕션이 그 deepgemm 경로로 상시 통과 중 |
| #51467/#50924 구조화출력+dspark 엔진사망 | **무사 — 런타임 시험 통과** | 유휴 상태 `response_format: json_object` 1발 정상 응답 + 직후 api/chat 건강 (08-09) |
| #49547 그래프 강등 | 변종 확인 — 신규 레버 | `fuse_attn_quant` 비호환 경고로 FULL 강제 → 인덱서 UNIFORM_BATCH 제약으로 **FULL_DECODE_ONLY** (프리필 piecewise 0; 디코드 FULL 1..256 유지). GPU busy 99.6%라 실손실은 작을 것 |
| #50773 fuse_norm/act_quant GB10 오염 보고 | 워치 | 요청 안 한 두 패스가 resolved에서 자동 True 확인 — 품질 9/9라 포크판 정상 추정 |
| #48031 엔진 ready 600s | 채택 (보험) | 이미지 `envs.py:27` 기본 600s + supervisor 이력 'boot grace exceeded' → 런처에 `VLLM_ENGINE_READY_TIMEOUT_S=3600` |
| #49921 라우터 GEMM 게이트 | **기각 — 자체감사로 정정** | 게이트(`gate_linear.py:53 family(100)`)는 실존하나, 이 포크의 생성자는 `force_fp32_compute` 미전달(nvidia/model.py:607) → 현행 폴백이 이미 **bf16 F.linear + [N,256] fp32 캐스트**. (4096,256)은 Tier-1/2 부적격이라 게이트를 열어도 실효는 캐스트 1커널 융합뿐(기대 ~0). 포팅 시도 철회 |
| #49027 TP 웻지(상한 근접) | 이론 잔존 | 포렌식 전건 grep — NCCL 카운트 불일치 시그니처 무발견. 430K 근접 세션 시 위험, 증상 시그니처는 포렌식 로그로 식별 가능 |
| #51489/#50774/#48210/#50365/#51106 | 무관/기커버 | C128A 빌더 zero-가드(p474 L286)·tile_sched 소비 무·bmm_fp8 미검출·atomic 미검출·#49059 가드 확인 |
| #51340/#51009/#50011/#46307 | 워치/기해결 | 워밍업 행 무증상 · 수용률은 bench-dec 열 감시 · sleep 미사용 · UMA는 drop_caches 대응 |

### 구성 해석 드리프트 (요청 vs resolved, 부팅 로그 실증)

- 요청 `FULL_AND_PIECEWISE` → 실효 **FULL_DECODE_ONLY** (`fuse_attn_quant`→splitting_ops=[] → FULL → 인덱서 제약)
- 요청 `fuse_gemm_comms:true` → resolved **False** (조용히 드랍 — 런처 플래그는 장식)
- 미요청 `fuse_norm_quant/fuse_act_quant` → resolved **True** (자동 활성)

### 신규 A/B 큐 (성능 2건은 08-11 상한 기각으로 종결; 인코딩 2건 + 메모리 2건 + 수용률 2건 잔존)

~~1. `fuse_attn_quant=false`~~ / ~~2. `VLLM_USE_B12X_SPARSE_INDEXER=1`~~ —
부팅 없이 기각 (08-11). 근거는 "정찰 일괄 기각" 표 참조 (busy 99.8%에 상한
0.2% / 스케줄 빌더 교체뿐 상한 <1% + #49897 리스크).

3. `EFFORT=high|max` — 레퍼런스 reasoning-effort 프리앰블 (2026-08-11 dsv4
   인코딩 오버레이로 노브가 비로소 실동작; 스톡은 high가 무동작이었음).
   기본은 빈 값(=low=프리앰블 없음, 종전과 바이트 동일). 측정: check-quality
   9/9 + 실워크로드 thinking 길이/지연 — 품질 이득이 thinking 비용을 넘는지.
   **08-11 이지셋 배터리 (per-request 3-arm, 재기동 불요)**: default/high/max
   전부 6/6 정답·thinking 151–201ch·wall ~1s = **이지 태스크에선 무차별**
   (노브 주입은 정상 동작). 기본값 결정은 난문 실워크로드에서만 갈림 —
   기본 빈 값 유지, 운영자 실사용 A/B로 이관.
4. **tools 주입 위치 정렬 품질 브래킷** (dsv4 인코딩 오버레이에 포함, 배포 시
   자동 적용) — 시스템 프롬프트+tools 요청의 프롬프트가 레퍼런스 순서
   (BOS+system+"## Tools")로 바뀜. 에이전트 실워크로드(garble/tool-call 정확도)
   전후 비교로 회귀 없음을 확인할 것. 롤백은 tok_* 2행 마운트 제거.
   **08-11 사후 단측 배터리**: tool-call 4/4 정상 (정확한 fn/args JSON, 템플릿
   마커 가블 0) ✓ — 단, **무관 tools 첨부 + 산문 질문 2/2에서
   SPURIOUS_TOOL_CALL + 빈 content**. 스톡 성향인지 오버레이 귀속인지는
   tok_* 마운트 제거 브래킷으로만 분리 가능 — 에이전트 실사용(무관 tools가
   흔함)에 영향 잠재라 **브래킷 우선순위 상향**.
~~5./6. `HEADFREE=1` / `COMPFREE=1`~~ — **08-11 심야 실측 (안전성 확정, 정량은
   브래킷 잔여)**: 단독+결합 3부트 전부 ①활성 실증 (기동 로그 "bf16 lm_head
   shard released (**252 MiB/rank** returned before KV-pool sizing)" +
   "compressor bf16 originals released"; 러너가 컨테이너 env 의도검증) ②
   fail-closed 가드 통과 ③디코드 무회귀 (C=1 중앙값 HF2 63.2/CF2 64.9/BOTH
   65.5 = 기준대역 64.2–64.3 동급) ④BOTH 9/9. **KV 정량은 야간 부트 드리프트
   (~−100K tokens/부트, 아래 신규 룰)가 기대 신호(+~73K)를 상회해 단부트로
   분리 불가** — 산술 확정치는 로그 실측 기반 **+~0.6GiB/rank ≈ +72K tokens
   (+2.7~2.9%)**. 잔여: 전용 창 즉시-연속 브래킷(base→knob→base)으로 정량
   도장. 켜기 자체는 무해 확인 — 채택은 운영자 결정.
~~7. `REFINE=1` (2-pass 드래프트 자기정제)~~ — **08-11 당일 브래킷 기각**
   (수용률 ×0.55·C=1 −33%, 기각 표 참조). 사전 약정대로 재제안 금지 등재.
8. `MARKOV_SIDELOAD=<refit.pt>` (Markov W1/W2 도메인 재적합, 08-11 구현·미실측)
   — **REFINE 기각으로 우선순위 추가 강등**: 마스크-마지널 학습 계약이
   실측 확인돼 순차 헤드의 사후 보정 여지도 같은 논리로 좁다 (SCALE 평탄성
   + OOD 붕괴 이중 근거). 켜려면 `tools/markov_refit.py` 코퍼스 재적합
   선행. ~~ngram/copy 하이브리드~~는 08-11 상한 시뮬로 기각 (기각 표).

**수용률 축 최종 종결 (08-11)**: 노브 전수(08-10) + 오버레이 구조 반례
2건(2-pass·ngram) 실측/상한 기각 + EAGLE3.1 비용트리 기각으로, 남은 경로는
**드래프터 재훈련 또는 신규 이미지**(confidence head 배선 — arXiv:2607.05147의
동시성 스케줄러 — 포함)뿐. 감시는 `dsv4-image-watch.timer`(srv2, 09:19 KST
일일)가 자동화, 상태 `~/.local/state/stkernel/image-tags.tsv`.

**잔여 큐 (08-11 심야 이후)**:
9. HEADFREE/COMPFREE **KV 정량 브래킷** — 전용 창에서 base→BOTH→base 즉시-연속
   3부트 (드리프트 룰 준수). 기대 +72K tokens 도장용.
10. `MAX_NUM_BATCHED=2048` — **연기** (첫 부팅 신규 셰이프 재컴파일 ~수 시간
   각오, 8192 전례 3.5h). KV +60%대 주장 vs 프리필 −7.7% — 430K 장문 동시성
   수요가 생길 때 전용 창에서.

## ★런북 정비 창 실행 (2026-08-11 심야, 운영자 위임)

RUNBOOK_MAINTENANCE.md 전항목 적용 + 플릿 재부팅. 사후 감사
(`~/refine-ab-results/fleet-audit-post-reboot.txt`) **합격 기준 전부 통과**:

| 항목 | 결과 |
|---|---|
| ① 컴팩션 sysctl 영구화 | 4노드 `persisted: vm.compaction_proactiveness=0` ✓ |
| ② THP defrag | **A안 채택 (현상 유지 + 감시)** — thp_fault_alloc/compact_stall 재발 시 B(never) 재론 |
| ③ RoCE IPv6 정리 | 4노드 양 인터페이스 `ipv6_disabled=1, inet6_addrs=0` → **GID 인덱스 4노드 동일** ✓ (엔진 정지 창에서 적용) |
| ④ srv1 클럭캡 | 설치+enabled, 2405→**1969MHz** (부트 재밍 중엔 직접 적용으로 우회, 재부팅 후 유닛 정상) |
| ⑤ 잔존물 | vllm-tp2* stop+restart=no · bluetoothd disable(srv2/3) · srv4 stash drop ✓ |
| ⑥ 플릿 재부팅 | 200G ×4 유지 · buddy order10>0 재건 · 타 프로젝트 컨테이너(solarflow 일체/paddleocr/grafana 등) 전량 복귀 ✓ |

**KV 신기준**: 재부팅 후 기준 부트 3회 2,918,776–2,948,142 tokens (구 2,656,602
대비 **+10~11%**) — 재부팅+컴팩션 영구화의 실효 용량 회수. HEADFREE/COMPFREE
가 노리던 급의 이득이 재부팅 위생만으로 나온 셈 (두 노브 실측은 별도 진행).

**★KV 드리프트 신규 룰 (08-11 야간 사다리 실측)**: 클린 재부팅 대역(2.92–2.95M)은
호스트 churn(스윕 컨테이너·연쇄 부팅)으로 **부트당 ~−100K tokens씩 침식**돼
2시간 만에 2.54M까지 내려갔다 (drop_caches로도 미복구 — proactiveness=0은
억제일 뿐 치유 아님이 정량 확인됨). 따라서 **KV 비교는 GPU_MEM 명기 + 즉시-연속
브래킷 필수** (기존 룰의 강화). 용량이 중요한 재기동은 플릿 재부팅을 선행할 것.
현행 프로덕션(복원 부트)은 2,536,012 tokens.

**신규 함정 2건 (실사고)**:
- **슈퍼바이저는 재부팅 시 자동 재기동**(enabled user unit + default.target) —
  "정지 후 재부팅" 절차에서 재부팅이 정지를 무효화한다. 06:39/06:50 KST에
  기준 스택을 자동 발사해 A/B 셀 부팅과 경합, 해당 셀(HF/CF 1차) 무효화.
  처방: **재부팅 후 셀 시작 전 supervisor 재정지 + 컨테이너 env 의도검증을
  러너에 내장**(`run-cell3.sh` — API up 직후 docker inspect로 요청 노브
  대조, 불일치 즉시 실패). srv1 plymouth-quit-wait 부트 재밍(멀티유저 타깃
  블록)도 같은 창에서 목격 — 재부팅으로 해소.
- pkill -f 자기매칭 자살 재발 (원장 기존 교훈 재확인) — 원격 명령 문자열
  안의 **모든** 패턴 언급을 브래킷 처리해야 함 (마지막 pgrep 인자까지).

## 인시던트 로그

| 일자 | 증상 | 근본 원인 | 수정 |
|---|---|---|---|
| 08-09 | 워밍업 `c128a_prefill_topk_indices=None` 크래시 | 슬리밍이 `_spec_mult` 하드코딩 — 실환경은 `VLLM_DSPARK_IMPL=upstream`이라 비오버레이 `sparse_mla.py`(threshold 11)와 오버레이(6)가 같은 배치를 다르게 분할 | env-게이트 복원 (51a1a52) + raise에 `[diag]` 자가진단 부착 |
| 08-09 | `/start_profile` 404 ×3 | 라우트는 `--profiler-config` **CLI 인자**로만 attach — env(`VLLM_TORCH_PROFILER_DIR`/`VLLM_SERVER_DEV_MODE`)는 무관 | 런처 serve 인자에 배선 (454f49f) |
| 08-09 | 프로파일 캡처 후 엔진 "웨지" | **OOM이 진범**: `with_stack=true`의 파이썬 스택 이벤트 124만 개 직렬화가 램 폭식 → 커널 OOM 킬러가 37GB 워커 사살 (dmesg 실증; 클라이언트 스크립트도 2회 피살) | `torch_profiler_with_stack=false` — 트레이스 87MB→34.6MB, 커널 이벤트만, 덤프 초 단위. 웨지 클래스 소멸 |

## 방법론 교훈 (체인 운용)

- 컨테이너 env는 런처 `-e` 명시분만 전달 — 신규 변수는 **sed로 런처 변형에 굽고**, 성공 경로에 `printenv` 의도 검증을 넣을 것 (NCCL 헛시험 사례).
- 폴백 분기("실패 시 변형")는 1차가 *의도와 다르게* 성공하면 안 탄다.
- 대기 함수에 teardown을 넣지 말 것 (자기 스택 삭제 사고).
- `pkill -f`는 자기 ssh 명령줄과 매칭되면 자살 — 브래킷 패턴(`[b]ench`) 사용.
- 디코드 비교는 `bench-dec.py`(768tok 생성)로만 — 짧은 벤치는 10% 효과도 못 가른다.
- 구성 A/B에서 한쪽만 추세로 보이면 설명 전에 **콜드 재기동 재현**부터.
- **하드 룰 (08-09, 2회 위반 후 제정): 포팅/최적화 착수 전 귀속 트리로 이론 상한부터 계산.** C4AREUSE(글로벌라이즈 ~1%)와 #49236(idle 0.4–2.6% 안의 할당 오버헤드 → 상한 ~1–2%) 모두 트리가 무효를 예언했는데 착수 후에야 확인함. 상한 < 한자리수 후반이면 부팅할 가치 없음.
- **엔진 열화 사건 (08-09, 기전 수정됨)**: 한 엔진이 2,528→2,152로 40분간 단조
  열화, 재기동으로 전량 회복. 최초 가설(프리픽스 캐시 churn)은 **재현 실패로
  기각** — 빈 캐시에 고유 60K 벤치 10회를 쌓아도 2,573→2,599로 무열화,
  `/reset_prefix_cache`도 무해·무효과. 현행 유력 용의: **웨지된 요청**(슈퍼바이저
  재시작 때 고아가 된 프로브 등)이 continuous batching에 계속 스케줄돼 갉아먹음
  (열화가 슈퍼바이저 재시작 직후 시작·단조 악화·재기동 회복과 정합; 직접 증거는
  엔진 소멸로 미확보). **처방(배포됨, cc65d06)**: ①웨지 워치독 — `running>0`인데
  `generation_tokens` 15분 정지(정상 장시간 생성은 카운터가 증가하므로 판별
  가능)면 포렌식 후 재활용 ②일일 04시 유휴 캐시 리셋(무해 보험). A/B 프로토콜
  (프레시 엔진 + 동일 나이 표본)은 유지 — 원인 불문 장수 엔진 상태는 교란변수다.

## 인접 모델 브링업에서 역수입한 항목 (2026-08-29/30)

출처는 **GLM-5.3-Flash·Qwen3.8-Flash-Next 브링업**이지 dsv4 실측이 아니다. dsv4
스택에 직접 걸리는 것(업스트림 감시·플릿 위생·b12x 미해결 질문)만 추렸다.

### ★★ flashinfer 0.6.18.dev 회귀 — b12x MoE 전면 사망 (업스트림 감시 항목)

0.6.18.dev의 `DenseGemmKernel`에 `_collapse_to_vmk`(MXF4 rank-4 뷰 정규화)가
추가됐는데, **그 파티셔너를 빌려 쓰는 MoE 커널들에는 미이식**이다. SM12x MoE
커널은 자기 인스턴스를 `self`로 넘겨 dense 파티셔너를 재사용한다:

```python
tCrSFA = self._dense_cls._partition_fragment_SFA(self, ...)
```

그 안에서 `self._collapse_to_vmk(...)`가 불리므로 **모든 b12x MoE 실행이**
`AttributeError: 'MoE*Kernel' object has no attribute '_collapse_to_vmk'` 로 죽는다.
차용 클래스는 4개(`MoEDynamicKernel`·`MoEGatedDynamicKernel`·`MoEStaticKernel`·
`MoEMicroKernel`)이고 **어느 것도 헬퍼가 없다** — 런타임이 토큰 수에 따라 커널을
갈아타므로 한 클래스만 고치면 크래시가 이동할 뿐이다. 올바른 수정은 빌려주는
쪽: `_collapse_to_vmk`는 `@staticmethod`라 소유 클래스로 직접 호출하면 `self`가
무엇이든 해결된다(호출부 2곳).

**우리 프로덕션은 0.6.17이라 무사하다.** 이미지 업그레이드 시 이 지뢰를 먼저
확인할 것 — `grep -c 'self._collapse_to_vmk(' dense_blockscaled_gemm_sm120_b12x.py`
가 0이 아니면 해당.

### b12x + EP — **종결(불가)**. 원장의 미해결 질문을 닫는다

flashinfer가 진입점에서 하드 거부한다:

```python
if num_local_experts != num_experts:
    raise NotImplementedError("b12x_fused_moe does not yet support Expert Parallelism ...")
```

워크스페이스 할당이 `state_E=local, weight_E=global`로 둘을 따로 받아 EP를
지원하는 것처럼 보이지만 **경로가 구현돼 있지 않다**(이 오독으로 한 라운드
소모). vLLM의 `_supports_parallel_config → not use_ep`는 배선 누락이 아니라
커널의 거부를 충실히 반영한 것이다. **EP 쓰는 구성에서 b12x는 선택지가 아니다.**

### `swiglu_limit` 클램프 — 재개 조건 ② 구현했으나 W4A4는 안 열린다

원장 "W4A4/W4A8" 항목의 재개 조건 ②(B12X 클램프 구현)를 실제로 구현했다.
FlashInfer `B12xMoEWrapper`는 처음부터 `swiglu_alpha/beta/limit`을 받고
`gated_activation_f32`가 `g=min(g,lim); u=clamp(u,±lim); g*sigmoid(alpha*g)*(u+beta)`
를 계산한다. **alpha=1.0, beta=0.0이면 vLLM의 `_swiglu_limit_torch`와 수학적으로
동일**하므로, SILU+클램프를 `swigluoai_uninterleave`로 매핑하면 된다(vLLM 래퍼가
인자를 전달하지 않았을 뿐).

다만 **이것만으로 A4가 열리지는 않는다** — 실측 결과 b12x는 다른 이유로 막힌다
(위 EP 항목, 아래 패딩 항목). 재개 조건 ①(swiglu_limit 없는 변종)도 이 계열에선
성립하지 않는다: 우리 체크포인트와 RadixArk 판본 모두 `swiglu_limit=10.0`이고
제외 목록(`mtp.*`·`*.mlp.shared_expert.*`)까지 동일하다 = 계열 표준 관행.

### ★★ FP4 중간크기 패딩을 CUTLASS 분기로 확장 — **기각(출력 파괴)**

`align_fp4_moe_weights_for_fi`는 TRTLLM 분기에만 배선돼 있고, CUTLASS/B12X
분기는 같은 상황에서 `NotImplementedError`를 던진다. 랭크당 중간크기가 128
정렬이 아닐 때(q38 TP4 EP-off: 640/4=160 → gate+up 320행, 384로 올림) 걸린다.
**헬퍼를 CUTLASS 분기에도 연결하면 부팅은 통과한다 — 그러나 모델이 망가진다.**

실측: `Padding intermediate size from 160 to 192` + `HEALTH-OK` 이후

```
'안녕하세요'          → '1'
'Hello, how are you?' → ''
'1부터 10까지 더하는 코드' → '10196700000000000000...'
'360과 168의 최대공약수' → '168/360/168/560/360/168/...'   (반복 루프)
```

marlin의 안전성 근거("패딩 영역은 출력에 도달하지 않는다")는 **marlin 레이아웃
전제**다. FlashInfer CUTLASS는 블록 스케일 스위즐이 달라 성립하지 않는다.
**재시도 금지.** 그리고 이 사건의 교훈은 원장의 기존 규율을 다시 확인해준다 —
**부팅 성공은 정확성의 증거가 아니다.** `HEALTH-OK`와 패딩 로그를 성공으로 읽고
한 라운드를 잃었다.

### 플릿 위생 — earlyoom이 vLLM을 우선 사살 (원장 미기재, 순손실)

노드별 설정이 갈려 있다:

| 노드 | `EARLYOOM_ARGS` |
|---|---|
| srv2·srv3 | `-m 2 -s 2 --avoid '(...\|VLLM\|vllm)'` |
| **srv1** | `-m 5 -s 10 --prefer '(sglang\|python3)'` |
| **srv4** | `-m 5 -s 5 --prefer (python3\|vllm\|pt_main_thread\|VLLM)` |

srv4는 vLLM을 **`--prefer`(우선 사살)** 에 넣어두었고 srv1도 `python3`를 지목한다.
브링업 중 워커 사망 6건이 전부 srv1·srv4에서 났고 srv2·srv3에서는 0건 —
`--avoid`가 있는 두 노드다. 통합메모리 박스에서 큰 모델이 상주하면 낮은 free는
**정상**이므로 백분율 임계는 건강한 모델을 쏜다. 4노드 설정 통일이 필요하다
(절대 하한 + `--avoid`).

### 호스트/UMA 위생 — "운영자 결정 대기" 항목이 실행됨 (2026-08-29)

원장 §남은 검증된 레버 3의 **플릿 재부팅(고차 페이지 재건)** 을 4노드 전부
수행했다. 재부팅 직후 실측:

| 노드 | order9 | order10 | compact_stall |
|---|---|---|---|
| srv1 | 411→693 | 0 → **206→1366** | 3,119,115 → **0** |
| srv2 | 12→550 | 0 → **146→782** | 62,446 → **0** |
| srv3 | 635 | **479** | **0** |
| srv4 | 8 | **3086** | **0** |

재부팅 전 4노드 전부 `order9=0 order10=0`(레포 자체 합격 기준 `order10 > 0`
불합격)이었고 srv1은 18일 무재부팅에 compaction stall 311만이었다.
**`drop_caches`·`compact_memory`로는 복구되지 않는다**(원장 기존 기록과 일치).
+1.4~1.6% 유지 여부는 **재측정 필요** — 이 세션은 dsv4 벤치를 돌리지 못했다.

### 반복 비용 — 설정 검증은 `LOAD_FORMAT=dummy`로

브링업 실패의 대부분이 설정 경로(백엔드 거부·패딩 미구현·dynamo 가드·비양자화
MoE)인데, 매번 가중치 로딩(q38 206샤드 ≈ 6분)을 먼저 기다렸다. `--load-format
dummy`는 난수 가중치를 채우므로 로딩이 초 단위가 되지만 **백엔드 선택·MoE 구성·
정렬 경로·컴파일·메모리 프로파일링·그래프 캡처는 전부 그대로 실행**된다 —
실패하던 표면과 정확히 일치한다. 15분 → 3분. 속도·품질 수치는 당연히 무의미하니
"뜨는가"와 "얼마인가"를 분리해서 쓸 것. FlashInfer JIT 캐시(`/root/.cache/
flashinfer`)도 마운트하지 않으면 매 부팅 재컴파일한다.

## GB10 메모리 예산 — 부팅 실패 세 얼굴이 한 원인 (2026-08-30, GLM-5.3 TP=4)

GLM 속도 측정을 하려다 부팅을 여섯 번 태웠다. 실패 메시지는 매번 달랐는데
원인은 하나였다 — **NVRM은 MemAvailable이 아니라 MemFree에서 GPU 컨텍스트를
잡는다**. 통합메모리 박스라 페이지 캐시도 유휴 JVM 힙도 "커널이 내줄 수 있는
메모리"가 아니라 **서빙이 못 쓰는 메모리**다.

| 증상 | 실제 원인 |
|---|---|
| `Free memory on device cuda:0 (90.19/119.69) ... less than desired (0.83, 99.35)` | 측정 후 부팅이 스스로 소모 |
| `NVRM: Out of memory [NV_ERR_NO_MEMORY] ... kgrctxAllocMainCtxBuffer` → 워커가 `Connection closed by peer`로 사망 | gradle 데몬 9.9 GiB |
| `Available KV cache memory: -5.67 GiB` | GMU가 **너무 낮아서** |

### ★★ GMU는 올려야 한다 — 내리는 건 반사적 오답

가중치+활성화가 **랭크당 ~78 GiB**다(GLM-5.3-Flash NVFP4, TP=4). 그래서:

```
GMU 0.60 (71.8 GiB) → KV −5.67
GMU 0.65 (77.8 GiB) → KV −2.06
```

OOM을 보고 GMU를 내리면 KV 적자가 **더 커진다**. 디스크 182 GB를 4로 나눠
45.5 GB/랭크로 어림한 게 틀렸고(실측 78), 이 역산을 세 번째 실패에서야 했다.
CUDA 그래프도 범인이 아니다 — `GRAPH_CAP=16`에서 **1.10 GiB**뿐이다
(런처 기본 256은 `MAX_SEQS`가 6인 설정에 과하다).

### ★★ 측정값 − 11 GiB — 부팅 자신의 소모

부팅 직전에 캐시를 회수하고 재도, 엔진이 볼 땐 **~11 GiB가 없다**. `docker run`이
~31 GB 이미지를 페이지 캐시로 끌어올리기 때문이다. 3회 실측: 101.6→90.19,
102.6→90.49, 113→90.5. 따라서

```
GMU = (노드 최소 MemFree − 11 − 여유) / 119.69
```

### ★★ 유휴 Gradle 데몬 9.9 GiB — `docker stats`에 안 보인다

srv4가 매번 모자랐던 이유. 컨테이너 감사로는 안 잡힌다 — srv4 컨테이너 **전부
합쳐 3.8 GiB**이고 SolarFlow 스택은 ~850 MiB뿐이다(이 둘을 범인으로 지목했던
초기 판단은 오류였다). 단서는 **AnonPages**: srv4 22.6 GiB vs srv3 2.2 GiB.
정체는 안드로이드 빌드가 남긴 JVM 힙 둘(5.74 + 4.13)이고 **죽여도 되살아난다**.

노드 간 `AnonPages` 대조를 첫 실패에서 했으면 여섯 번 중 넷은 없었다.

### 배선

`launchers/memfree-preflight.sh` — 4노드에서 gradle/kotlin 데몬을 죽이고 캐시를
회수한 뒤, 최소 MemFree에서 부팅 소모와 여유를 빼 GMU를 계산한다. AnonPages
격차가 5 GiB를 넘으면 "예상 못 한 입주자"로 경고한다. 노드 하나라도 접근이
안 되면 **거부한다** — 못 본 노드를 기준으로 사이징한 게 오늘 한 번 더 실패한
경로다.

## 한국어 토큰 손상 — 용의자 8종 기각, 서명 확정 (2026-08-30, GLM-5.3 TP=4)

응답의 81%에 U+FFFD가 섞인다(471.5/백만자). 원인은 **아직 미확정**이지만, 무엇이
아닌지가 대부분 확정됐다. 이 절은 다음 세션이 같은 길을 다시 걷지 않기 위한 것이다.

### ★★ 서명: 정규 토큰 분해에서 **단일 바이트 접착 토큰 하나만** 빠진다

한글 음절은 3바이트라 바이트 BPE가 2~3개 토큰으로 쪼갠다. 정규 분해와 실제 출력을
나란히 놓으면 매번 가운데 한 칸만 비어 있다:

| 단어 | 정규 분해 | 모델 출력 | 빠진 것 |
|---|---|---|---|
| 붙여 | `Ġë¶`·**`Ļ`**·`ìĹ¬` | `Ġë¶`·`ìĹ¬` | id 247 |
| 냉각 | `Ġë`·`ĥ`·**`ī`**·`ê°ģ` | `Ġë`·`ĥ`·`ê°ģ` | id 231 |
| 용량 | `Ġìļ`·**`©`**·`ëŁī` | `Ġìļ`·`ëŁī` | id 102 |
| 병렬 | `ë³`·**`ij`**·`ëł`·`¬` | `ë³`·`ëł`·`¬` | 바이트 `91` |

틀린 토큰이 아니다 — 앞뒤가 정규 분해와 정확히 일치한다. 그리고 그 자리에서 모델은
**확률 ≈1.0**(2순위와 Δ12~13)으로 확신한다. 렬처럼 다른 다중토큰 음절은 정상
조립되므로 "모델이 조립을 못 한다"도 아니다. 한국어 특유 현상도 아니다 — 영어에서
토큰 하나가 빠지면 어색한 단어가 되지만 한글에선 **문자 자체가 깨져** 눈에 띌 뿐이다.

### 기각된 용의자와 근거

| 용의자 | 기각 근거 |
|---|---|
| 증분 디토크나이저 | 같은 id를 서버 `/detokenize`로 한 번에 풀어도 동일 손상(스트림 9 vs 오프라인 10) |
| 거절 샘플러 | 매 호출 `torch.full(-1)` 새 버퍼 · greedy 커널은 초안이 아니라 **타깃 argmax**를 쓴다 — 구조적 무손실 |
| 배달 사슬 | `parse_output`→`update_from_output`→`_update_request_with_output`이 전부 append, 정지 시에만 절단 |
| 수용 개수 회계 | 러너 `valid_count−1` = 스케줄러 `len(generated)−num_sampled_tokens_per_step(1)` — **동일 공식** |
| 투기 디코딩 | `SPEC=0`에서도 손상 지속(3/8 응답, 178.6/백만자) |
| async 스케줄링 | `dflash`는 호환 화이트리스트 밖이라 **자동으로 꺼진다**; `SPEC=0`이면 **켜진다**. 정반대인 두 구성이 모두 손상 |
| 반올림 동률 뒤집기 | 깨진 자리 1·2순위 격차 Δ12~13 — fp8/nvfp4 오차로 뒤집을 수 없다 |
| kpool 희소 어텐션 | `index_topk=2048` > 시퀀스 ~1000이라 **미발동**(모든 풀 선택). 깨짐이 `seq_len % 4`에 몰리는지 검정도 기각(2/3/1/2) |

**남은 용의자**: b12x MoE nvfp4 커널 · `--kv-cache-dtype fp8_e4m3` · CUDA 그래프 ·
NVFP4 가중치 자체. 다음 수는 KV를 bf16으로 되돌린 1회 부팅.

### ★★ 계측기부터 검증할 것 — 두 번 오판했다

- **`logprobs.bytes`는 증거가 아니다.** `token.encode("utf-8", errors="replace")`로
  만든다 — 이미 깨진 문자열을 다시 인코딩한 값이라 원본 바이트가 아니다. 진짜 id는
  `return_token_ids: true`로 받는다.
- **접두사를 늘려가며 재디코드하면 가짜 깨짐이 나온다.** 바이트 BPE는 음절을 토큰
  경계로 쪼개므로 끝이 잘린 접두사는 **항상** U+FFFD를 낸다. 실제 손상은 전체
  디코드에서만 세거나, 토큰을 바이트로 되돌려 스트림을 strict 디코드해야 한다.

### 부수 발견: `index_kpool_always_select_tail`이 죽은 플래그다

모델 설정이 True로 선언하는데 **실행 코드가 읽지 않는다**(설치 전체에서 설정
dataclass·대입·주석 한 줄이 전부). 구현은 *미완성* tail만 덧붙이는데 그 구간은
`seq_len % 4 == 0`일 때 비고, 그러면 최신 4토큰이 top-k 경쟁에 맡겨진다. 위 손상의
원인은 아니지만(짧은 컨텍스트에선 미발동) 긴 컨텍스트에선 실재한다 —
`overlay/modules/glm53_kpool_tail_select`로 수리.

## 오버레이가 한 번도 실린 적 없었다 — 베이스 계약이 첫 실행에서 잡은 것 (2026-08-30)

GLM 런처는 파일 **하나**(인덱서 패치)만 바인드했는데 `profiles/glm53.env`는 모듈
**열한 개**를 나열했다. 이미지에도 없다 — `glm53:v13-b12x`에 `fp8_lm_head.py`도
`moe_gate_sm121.py`도 부재. 즉 융합 MoE 게이트·one-shot AllReduce·fp8 lm head
양끝이 조립만 되고 컨테이너에 도달한 적이 없다. **프로필은 한 번도 서빙된 적 없는
구성을 기술하고 있었다.**

### ★★ 배포 게이트가 죽어 있었다 — dsv4 포함

`deploy-overlays.sh`가 게이트로 돌리는 `tests/test_logic.py`가 모듈 분리(#48)
이전 경로(`overlay/attention.py`·`overlay/manifest.tsv`)를 들고 있어
`FileNotFoundError`로 중단됐다. glm53만이 아니라 **모든 프로필**의 배포가 막혀
있었고, 그래서 아무도 오버레이를 실을 수 없었다. 파일명으로 `overlay/modules/*/`를
찾는 리졸버로 교체(고정 경로를 다시 박으면 다음 이름 변경 때 또 멈춘다).

### ★★ 첫 전면 배선 부팅이 즉시 중단됐다 — 그게 옳은 동작

```
ABORT: base preimage mismatch for b12x_moe_experts.py
       (image 86dfe87e…, manifest e613afe5…)
```

핀 추적 결과 `b12x_swiglu_clamp`·`flashinfer_b12x_collapse` 두 모듈의 베이스가
**`glm53:v9`**(b12x 이전 빌드)와 일치했다. v11·v12·v13은 그 파일들이 서로 같고
v9와 다르다 — b12x 수정이 v11부터 이미지에 구워진 것이다.

모듈 소스를 v13과 직접 대조하면 판정이 끝난다:

| 파일 | 모듈 vs v13 |
|---|---|
| `b12x_oracle_nvfp4.py` | **바이트 동일** |
| `dense_blockscaled_gemm_sm120_b12x.py` | **바이트 동일** |
| `b12x_moe_experts.py` | v13이 같은 clamp(주석·필드명까지 동일) **+ 30줄 더** |

**얹으면 얻는 게 없고 그 30줄을 잃는다.** 프로필에서 제외했다. 모듈 자체는 남긴다 —
v9 이미지에 대해서는 올바른 오버레이이고, 지우면 clamp가 왜 있는지가 사라진다.

`flashinfer_b12x_collapse`의 README는 베이스를 `v12-b12x`라고 적었지만 실제 핀은
v9였다. **아무도 이 계약을 검증한 적이 없어서** 잡히지 않았다 — 런처가 매니페스트를
읽지 않았으니 검증할 코드 자체가 돌지 않았다.

제외 후 전수 재검증: **일치 7 · 신규 4 · 불일치 0**.

### ★ 감시 루프가 옛 서버를 새 서버로 오판했다

부팅이 중단되자 이전 서버가 살아남았고, `/v1/models`가 200을 주니 감시 루프가
20초 만에 "준비됨"으로 판정해 **직전 bf16 서버를 다시 쟀다**. "런처가 헤드를 먼저
제거하니 안전하다"고 스스로 기각했던 위험인데, **조기 중단 경로**를 빠뜨렸다.
준비 판정은 응답 여부가 아니라 **컨테이너 ID가 바뀌었는지**로 해야 한다.

소득은 있었다 — 같은 bf16 구성의 반복 측정치를 얻어 **노이즈 바닥**이 잡혔다:

| bf16 실행 | 손상 응답 | 백만자당 |
|---|---|---|
| 1회차 | 5/8 | 1289.7 |
| 2회차 | 4/8 | 985.8 |

실행 간 ~30% 변동. fp8(178.6) 대 bf16(986~1290)의 격차는 노이즈 밖이므로
**fp8 KV 기각 판정은 유효**하다.

## 토큰은 버려지지 않는다 — 계측기가 답한 것과, 계측기 자신의 결함 (2026-08-30)

### ★★ drop-audit 침묵 — 유실 계열 전체 배제

`glm53_drop_audit`을 무장하고 손상이 재현되는 내내 돌렸다. **발화 0건.**
계측기는 네 가지로 검증했다: 컨테이너 env 존재 · 마운트 파일 sha 일치 · 코드
로드 확인 · 투기 ON(`method='dflash'`)이라 `parse_output`이 매 스텝 실행.

그래서 세션 내내 못 가르던 갈림길이 닫혔다:

> `Ļ`는 방출됐다가 사라진 게 아니라 **애초에 안 나온다.** 모델이 조각 토큰을
> argmax로 낸다.

한계는 정직하게: `discard_request_mask` 절반은 짧은 프롬프트·순차 단일 요청에서
애초에 참이 될 일이 없었을 수 있어 약한 증거다. 연속성 절반은 실행이 보장되므로
강하다.

### 속도와 수용률 (전면 마운트 · 노브 OFF · 투기 ON · fp8 KV · 1M)

| C | median tok/s | 수용률(raw) |
|---|---|---|
| 1 | 25.9 | 23.2% |
| 2 | 43.6 | 21.6% |
| 4 | 45.0 | 20.9% |

오버레이를 전부 마운트해도 **속도 비용은 사실상 0**(C=2·C=4는 부팅 간 노이즈 안).
노브가 꺼져 있고 마운트가 동작 중립이라는 것과 일치한다.

**★ 수용률이 이상하다.** 런처 주석의 DFlash2 기대치는 74%인데 실측 raw 21% —
3.5배 미달. verify 포워드의 타깃 로짓이 틀리면 드래프터가 옳은 토큰을 내도 타깃이
동의하지 않아 거절이 는다. **낮은 수용률과 한글 손상이 같은 원인의 두 얼굴**일
가능성이 있고, 그렇다면 원천 하나를 고치면 손상과 잃어버린 속도가 함께 사라진다.

### ★★ 계측기 결함 셋 — 전부 "검증 없이 결과부터 읽음"

하루에 같은 부류를 세 번 밟았다.

1. **`logprobs.bytes`는 증거가 아니다** — `token.encode("utf-8", errors="replace")`로
   만든다. 이미 깨진 문자열을 다시 인코딩한 값.
2. **접두사를 늘려가며 재디코드하면 가짜 깨짐** — 바이트 BPE는 음절을 토큰 경계로
   쪼개므로 끝이 잘린 접두사는 항상 U+FFFD를 낸다.
3. **잘림을 손상으로 계상** — `max_tokens`에서 끊기면 마지막 음절이 중간에 잘려
   U+FFFD가 하나 생긴다. 한글 음절은 3바이트라 대략 2/3 확률로 그렇게 끝난다.
   `bench/korean-corruption.py`가 이걸 세고 있었으므로 **이 도구가 낸 모든 수치에
   응답당 최대 1건의 유령이 섞여 있었다**. `finish_reason == "length"` 이고 끝이
   U+FFFD면 제외하도록 수리.

그리고 원천 프로브도 **틀린 질문**을 하고 있었다: 조각 토큰이 위치 `ti`인데
프리픽스를 `ids[:ti+1]`로 잡아 조각을 포함한 뒤 다음 토큰을 비교했다. 두 경로 다
똑같이 회복하므로 "shape 무관"이라는 무근거 결론이 나왔다. 물어야 할 것은
`ids[:ti]`에서 프리필도 그 조각을 고르는가다.

**교훈은 하나다 — 침묵이든 수치든, 계측기가 돌았고 옳은 것을 묻는지 먼저 증명할 것.**

### 수리 후 실측

16응답에서 진짜 손상 4건, 전부 문장 중간이다: `띄어쓰기 �정`(규정),
`변�기`(변압기), `�어 쓴다`(띄어), `약 3번 정도 �구기`(헹구기).

## ★★GLM-5.3-Flash 2026-09-01 — 프리필 첫 실측, 그리고 축 셋이 닫혔다

부팅 7회. **확정된 이득은 하나(블록 검증 +4.6%), 나머지는 전부 0으로 닫혔다.**
그리고 오늘 잃은 시간의 대부분은 모델이 아니라 **하네스**가 원인이었다.

### 오늘의 전체 측정

**이 표의 `토큰/스텝` 은 레거시(부풀림) 관례다** — `1 + k x acc`, 여기서 acc 는
`bench-dec` 의 substring 합(같은 수용 토큰을 `num_accepted_tokens_total` 과
`per_pos` 합으로 두 번 세고, 분모도 `num_drafts` + `num_draft_tokens`).
정확 관례(`num_accepted_tokens_total / num_draft_tokens_total`)로는 같은 부팅이
약 0.68배로 나온다 — 레거시 3.75 = 정확 2.57 (2026-09-03 실측 대조). 두 계열을
섞어 비교하면 25% 회귀로 오독된다. **드래프터 3종 비교표는 정확 관례다.**

| 부팅 | 커밋 | 구성 | 토큰/스텝 | C=1 | C=2 | C=4 |
|---|---|---|---|---|---|---|
| sel | `aa17a7e` | standard, 웜업 없음 | 3.354 | — | — | — |
| bv | `bc60dce` | block, 웜업 3회 | 3.901 | — | — | — |
| comb | `c4576a7` | block, 웜업 6회 | 3.817 | — | — | — |
| **pair A** | `66c2baf` | **standard** | **3.820** | 30.0 | 42.4 | 59.0 |
| **pair B** | `66c2baf` | **block** | **3.995** | 38.4 | 53.1 | 63.6 |
| dtp | `cfbf28a` | standard + `DRAFT_TP=4` | 3.429 | 38.2 | 61.4 | 74.3 |
| ctl | `cfbf28a` | 노브 전부 OFF(의도치 않게) | — | 37.9 | 62.0 | 74.5 |

step/s 는 **C 로도 나눠야 한다** — `tok/s / (C x (1 + k x raw))`. C=1 만 나눈
값을 쓰면 C>1 에서 스텝이 배치와 함께 *줄어드는* 것처럼 보인다(오늘 한 번
그렇게 잘못 보고했다). 올바른 값: C=1 68 ms · C=2 96 ms · C=4 146 ms —
`0.706 ms x 접촉전문가` 모델과 정합.

### 확정: 블록 검증 (`REJECT_METHOD=block`) — 토큰/스텝 +4.6%

같은 커밋·같은 오버레이·같은 웜업 6회, `rejection_sample_method` 하나만 다른
매치 페어. 3.820 -> **3.995**. 인자 base64 디코드와 엔진 설정 덤프로 축이
움직였음을 이중 확인.

**tok/s 30.0 -> 38.4(+28%)는 채택하지 말 것.** step/s 로 정규화하면 12.59 vs
15.14 인데, 블록 검증이 스텝을 20% 줄일 기전이 없다. 이후 두 독립 부팅이
15.19/15.1 로 모였고 12.59 하나만 이상치다 -- **A 팔이 나쁜 부팅이었다.**
부팅 간 편차가 20% 까지 벌어질 수 있다는 뜻이며, 이 원장의 `+-2 tok/s` 보다
훨씬 크다.

### 프리필 첫 실측 (이 스택에서 처음)

| 요청 | cold tok/s | **warm tok/s** | cold TTFT | warm TTFT | cold 세금 |
|---|---|---|---|---|---|
| 2048 | 1,755.6 | **2,230.0** | 3.29s | 2.59s | **21.3%** |
| 4096 | 1,300.9 | 2,210.3 | 8.85s | 5.21s | **41.1%** |
| 8192 | 1,972.1 | **2,231.5** | 11.68s | 10.32s | 11.6% |

**2048 -> 8192 에서 warm 처리량이 2230 -> 2231 (+0.1%) 로 완전히 평탄하다.**
프리필은 **가중치 읽기 바운드**이지 GEMM 형상 바운드가 아니다. 따라서
`MAX_BATCHED` 를 16384 로 키워 전문가당 M 을 늘리면 이득이 난다는 가설은
**측정으로 사망**했다 -- 그 부팅은 취소했다.

**cold 세금 11~41% 는 JIT 컴파일이고, 사용자가 실제로 체감하는 유일한 프리필
비용이다.** 처리량이 아니라 여기가 남은 표적이다.

### 닫힌 축 셋

| 축 | 결과 | 근거 |
|---|---|---|
| `DRAFT_TP=4` | **0** | step/s 15.19 vs 15.14. osar `wait` 43.7 -> 115.0 us, 콜렉티브 8,181 -> 10,142 -- 쪼개서 아낀 읽기를 대기가 먹는다 |
| `MAX_BATCHED` 16384 | **전제 사망** | 프리필 사다리가 평탄 |
| 웜업 부족 가설 | **반증** | 패스별 37.4/41.7/37.2/40.2 -- 단조 상승 없음. **3,072 토큰이면 충분** |

`DRAFT_TP` 는 이 원장이 이미 답을 갖고 있었다: 2026-08-31 SPEC=0 차분이
**드래프터 비용 = 0** 을 확정했고 "드래프터 최적화 전반"을 닫힌 축으로
적어 두었다. **읽었으면 부팅 하나를 안 썼다.**

### #178 AOT 가드 — 이 하드웨어에서 발화 불가 (되돌림)

`flash_attn.py:445` 가 `self.aot_schedule = get_flash_attn_version() == 3` 이고,
`get_flash_attn_version` 은 `device_capability.major == 9`(Hopper SM90) 에서만
3 을 준다. **GB10 은 major 12** -> FA2 -> `aot_schedule` 항상 False.

계측(#180)이 두 부팅에서 `1 sliding-window draft builder(s), aot_schedule
disabled on 0` 을 찍어 확인했다. upstream vllm#54374 의 "acceptance 1.000 ->
5.542" 는 **우리가 아닌 기종의 숫자**였다. #187 로 오버레이 삭제.

### 하네스가 원인이었던 손실

| 결함 | 증상 | 수리 |
|---|---|---|
| `tests/test_logic.py` 가 torch 를 가드 없이 import | 서빙 호스트 배포 게이트 중단 (#125 이후 재발) | #176 + 정적 계약 |
| 벤치 3종이 `deepseek-v4-flash` 를 기본 모델명으로 물고 있음 | glm53 서버에서 404 -> **빈 섹션**. "측정했는데 변화 없음"과 "안 돌았음"이 구분 안 됨. 프리필 섹션 하나를 통째로 잃음 | #189 (`/v1/models` 조회) |
| `#188` 노브 넷이 무장을 안 알림, `UNION_PREFILL=1` 은 조용히 off | "전부 1로 켜자" 스윕이 기준선을 잼 | #190 (무장 로그 + 1 경고) |
| `EXTRA_ENV` 를 쉼표로 이으면 통째로 한 개의 `-e` 가 됨 | 노브 다섯이 전부 꺼진 채 부팅, 로그는 설정했다고 말함 | #191 (거부) |
| `osar` `MAXEL` 랭크 불일치가 로그뿐 | 한 랭크만 one-shot, 나머지 NCCL -> 교착/오염. 국소로는 안 보임 | #186 (세 번째 투표) |

**#190 의 무장 로그가 없었으면 "노브 다섯 켰는데 효과 0" 으로 보고할 뻔했다.**

### 자기 교정 셋

1. **조건부 vs 주변(marginal)** — 엔진의 `per_pos_total` 은 주변이다. 조건부가
   평평해도 기하급수로 준다. 78.6->6.7% 를 논문의 "모든 위치 86%" 와 나란히
   놓고 붕괴로 읽었으나, 실측 조건부는 **68~89% 로 평평**했다. 붕괴는 없었다.
2. **C>1 step/s** — 위 참조.
3. **원장을 늦게 읽음** — 드래프터 축은 8월 31일에 이미 닫혀 있었다.

### 남은 것

25.0 ms(스텝의 38%)가 "dense 읽기 + 커널 + 글루 + AR" 이다. 커널 축은 천장
12.8%(우리 몫 1.1%), AR 은 종단 0%. 남은 미기각 레버는 **dense 투영
W8A8 -> W4A4** 하나이고, 추정 -3.7 ms(스텝 -5.4%, C=1 38 -> 40)다. `#106`
에서 시도했다 비동기 CUDA 사망으로 중단됐고 **기전으로 기각된 적은 없다.**
품질 검증(temp=0 사실질문 + U+FFFD/lone jamo)이 채택 조건.

목표선 대비: **프리필 2,230 / 2,300 (3% 부족)** · **디코드 38 / 43 (13% 부족)**.

## ★★GLM-5.3-Flash 스텝 재분해 — 62%가 줄일 수 없는 전문가 읽기 (2026-08-31)

C=1 디코드 스텝을 두 축(스펙 on/off, 커널 개수)에서 갈랐다. 오늘 이전까지 이
스텝에 붙은 설명은 전부 **잔차 귀속**이었고 네 번 뒤집혔다.

### 스텝 예산 (C=1, W8A8 적용 후)

| 성분 | ms | 비중 | 근거 |
|---|---|---|---|
| **전문가 가중치 읽기** (8토큰 x top-8, 중복 후 ~58) | **41.0** | **62%** | `0.706 ms x 접촉전문가`, 대역폭 91% — **짤 것 없음** |
| 나머지 (dense 읽기 + 커널 + 글루 + AR) | 25.0 | 38% | |
| **합** | **66.0** | | step/s 15.14 (n=6, CV 1.7%) |

**스텝의 62%가 줄일 수 없는 메모리 읽기다.** k 를 키울수록 검증 토큰이 늘어
전문가 접촉이 늘고 이 항이 커진다 — 그런데 DFlash2 는 `SPEC_K=7` 고정이라
(런처 274행이 다른 값을 ABORT) 그 레버가 없다.

### 드래프터 비용 = 0 (SPEC=0 차분, 부팅 실측)

| | step/s | 스텝 |
|---|---|---|
| `SPEC=0` (드래프터 없음, 1 토큰 검증) | **31.9** (6 rep, 폭 0.1 = **0.3%**) | **31.3 ms** |
| `k=7` (드래프터 있음, 8 토큰 검증) | 15.14 | 66.1 ms |
| 차 | | **34.7 ms** |

차이 = `D + 0.706 x (E8 - 8)`. 특성화의 `E8 = 58.1` 을 넣으면 전문가 증가분만으로
**35.4 ms** 이고, 남는 `D = -0.7 ms` — **드래프터는 측정 한계 안에서 0**이다.
1 패스 격자 드래프터(5 레이어·2.5B)가 45 레이어 MoE 검증에 묻힌다.

부수 확정: `0.706 ms/전문가` 기울기는 **배치 크기**로 적합한 값인데, 완전히 다른
축(스펙 on/off)에서 나온 34.7 ms 를 35.4 로 맞췄다. **독립 검증됨.**

이로써 닫히는 축: 드래프터 최적화 전반 · 드래프터 어휘 shortlist(lm_head
634MB->67MB) · `VLLM_DFLASH2_FP8_DENSE`(#95) — 전부 0 의 일부를 줄이는 일이다.

### 커널 개수의 실측 가격 = 5.4 us/커널

#89 로 209 커널을 제거한 A/B: 스텝 73.80 -> 72.67 ms(Δ 1.13 ms). 앞선 인구조사가
32.1 ms 를 전부 커널 수에 귀속시켜 역산한 **상한 15.4 us 의 1/3**이다.

| | 커널/step | 5.4 us 환산 | 스텝 대비 |
|---|---|---|---|
| 잔여 전체 (#90·#93·#96 이후) | ~1,751 | 9.4 ms | **12.8%** — 축의 천장 |
| 그중 우리 소유 (AR 104 + 게이트 44) | 148 | 0.80 ms | **1.1%** |

**-42 커널짜리 PR 의 기대 효과는 0.35% 로 부팅 내 CV(1.3~1.7%) 아래다** — 단독
A/B 로는 측정 불가. 기전으로 채택하되 측정으로 채택했다고 쓰지 말 것.

### osar 위상 분해 (#99 계측, 커널 안 clock64)

```
[osar] phase us/collective: guard=0.7  copy=6.6  wait=89.0  reduce=5.2 | total=101.5
```

**`wait` 이 AR 커널 시간의 88%.** #100(스핀 백오프 + 도착 플래그 재독 제거)이
그 값을 **89.0 -> 50.0 us (-44%)** 로 줄였다. 그런데:

| | #100 효과 |
|---|---|
| 위상 타이머 | -3.2 ms (환산: 콜렉티브/스텝 73 가정) |
| **종단 step/s (같은 부팅·같은 프로토콜)** | **C=1 +0.1% · C=2 -0.4% · C=4 +1.3%** |

3.2 ms 면 +5% 라 CV(1.7%) 위에서 보였어야 한다. 안 보였다. **종단이 이긴다 —
#100 은 종단 효과 없음.** AR 의 대기를 절반으로 잘라도 스텝이 안 줄었다는 것은
AR 이 이 스텝의 주범이 아니라는 뜻이다.

### 기각된 가설 넷

| 가설 | 반증 |
|---|---|
| 32 ms = 커널 개수 (15.4 us/커널) | 실측 5.4 us. 축 천장 12.8%, 우리 몫 1.1% |
| 호스트 병목 (파이썬이 GPU 를 못 먹임) | 디코드 중 **GPU 사용률 93%** (워커 CPU 114% 는 osar 프록시 스핀) |
| 32 ms = 콜렉티브 지연 | `wait` 을 44% 잘라도 **종단 +0.1%** |
| 드래프터 = 7 회 순차 forward | 코드 반증(1 패스 격자: `DFlashGroupedConv` + `CandidateSelector` + `_score_edges`) · #95 트레이스 171 커널/step · SPEC=0 차분에서 D≈0 |

### 위치별 수용률 — 드래프터 3종 비교 (참조)

| 위치(누적) | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 수용길이 |
|---|---|---|---|---|---|---|---|---|
| **GLM-5.3 DFlash2 (우리 실측)** | .627 | .373 | .228 | .139 | .084 | .050 | .029 | **1.53** |

(정확 관례. 같은 값을 위 부팅표의 레거시 관례로 쓰면 토큰/스텝 3.7~3.8 이다.
2026-09-03 재측정: pos1 .644/.672/.742 @ temp 0.0/0.6/0.95, 수용길이 1.56/1.67/1.86 —
온도가 오르면 초안·타깃 분포가 함께 평평해져 수용률이 **올라간다**.)
| dsv4 dspark (기존 원장) | .785 | .564 | .396 | .286 | .215 | — | — | 2.25 |
| GLM-5.3 DSpark (HF 카드) | .493 | .237 | .115 | .056 | .028 | .015 | .008 | **0.95** |

DFlash2 의 **조건부** 수용률은 위치별로 평평하다(~0.60, pos-7 에서도 57.7%).
dsv4 는 0.72 감쇠, GLM-5.3 DSpark 는 0.48 감쇠. **`Capicua25x/GLM-5.3-Flash-DSpark`
는 전 위치에서 DFlash2 보다 낮고**(1.95 vs 2.53 토큰/스텝), 카드 자신이 "vLLM 에
glm5_next 지원이 없어 공개 서빙 스택에서 사용 불가" 라 적어뒀다 — **대안 아님**.
다만 그쪽은 임베딩/lm_head 를 타깃에서 tie 한다(우리는 `tie_word_embeddings:
False` 로 634M+634M 자기 사본).

### 판정 채널 — C=1 step/s 하나

`tok/s` 는 수락률에 오염돼 못 쓴다. `step/s = tok/s / (1 + k x raw_acc)` 로
정규화하면 **C=1 에서만** 듣는다:

| | tok/s 폭 | step/s CV |
|---|---|---|
| C=1 | 49% | **1.7%** |
| C=2 | 40% | 12.1% |
| C=4 | 35% | 13.5% |

C>1 은 요청마다 수락률이 달라 배치 구성이 흔들려 단일 정규화가 안 먹는다.
**예외: 수락률 자체를 움직이는 변경**(#102 draft_sample_method)은 `step/s` 가
안 움직이므로 `tok/s` 와 토큰/스텝이 정답 채널이다.

### 계측 규율 — 파생량 전에 환산 계수부터

오늘 이 축에서 네 번 틀렸고 전부 같은 부류였다: **계측기를 검증하기 전에 결과를
읽었다.** #99 위상 타이머의 us 는 실측이었지만 스텝당으로 바꾸는 계수(콜렉티브/
스텝)를 재지 않았고, 그 하나 때문에 "-3.2 ms" 를 잘못 주장했다. 종단 step/s 가
아니었으면 그대로 원장에 들어갔다.

- **파생량(스텝당·비율)을 보고하기 전에 그 환산 계수부터 독립적으로 재라.**
- **어떤 계측기도 종단 측정과 어긋나면 종단이 이긴다.**
- 프로파일의 절대 시간은 못 쓴다(CUPTI 가 GPU 바쁜시간을 136 ms 로 부풀림 vs
  실측 47.6). **개수는 오버헤드와 무관하므로 개수를 정본으로.**
- 인구조사의 분모는 추정하지 말고 **세라** — 스텝당 1 회 도는 커널
  (`_get_num_sampled_and_rejected`)이 정확한 분모다.

### #142 사후 — V2 경로 추적: 아이디어는 V2가 이미 네이티브로 구현 중 (2026-08-31)

#142(CAND_SAMPLE)는 머지 4분 전 #141이 glm53_sparse_q를 MODULES에서 뺀 상태로
들어갔으므로 **합성되지 않는 죽은 노브**다. 추적으로 정리한다:

- V2 드래프팅은 `DFlash2Speculator(DFlashSpeculator(DraftModelSpeculator))`
  (worker/gpu/spec_decode) — `llm_base_proposer.py`의
  SpecDecodeBaseProposer 계열과 **상속 관계 없음**. #141 판정 확인.
- V2의 드래프트 law: `_selector_walk_kernel`이 selector_top_k=16 후보 점수 위에서
  확률적 샘플링(SAMPLE_PROBABILISTIC, gumbel/seed 포함) — **후보 한정 샘플링이
  이미 네이티브**. q는 `_cache_draft_logits_kernel`이 [R,K,vocab] fp32에 16개만
  쓰고 이전 후보만 -inf로 증분 유지(외부는 구조적으로 0).
- V2 스텝 경로에서 호스트 동기화 없음(유일한 .item()은 CPU 텐서).
  full softmax/topk(32)/float(leak)는 V1 proposer 전유물이었고 V2에 존재하지
  않는다. #142의 이득 주장은 V1 기준으로만 유효했다 → 리버트.
- 잔여 여지(기록용): V2 rejection이 dense draft_logits([R,K,vocab] ≈4.3MB @C=1)
  를 읽는 것을 16-wide pool로 대체하면 ~수십 µs — CV 아래, 수술 가치 없음.
  이미지 자체 TODO: sampling-param fold-in으로 processed_logits 버퍼 제거(상위
  레인 과제).

교훈은 #141/#138과 동일: **모듈이 합성 목록에 살아있는지 먼저 확인** — 오늘
다섯 번째 죽은 노브였고 이번 건은 리버트로 정리했다.

## ★메가커널 2차 — 벤치 함정 넷, W4 −33%, MHC −22%, PDL, 그리고 deepgemm 과의 진짜 격차 (2026-09-02)

**벤치가 네 번 틀려 있었다.** (1) 48 MB `zero_()` 플러시의 write-back 잔여가 뒤 커널과 겹쳐 mk GEMM 을 15~26% 부풀렸다(#210: 플러시 뒤 ~300 µs 컴퓨트 spacer). (2) 읽기 플러시는 재사용된 라인을 못 쫓아낸다(L2 정책). (3) 단독 발사 열은 발사 지연을 포함해 어느 팔도 공정하지 않다 — 그래프 리플레이 조건은 **연속 2 발(x2)** 열. (4) 그 x2 가 같은 가중치를 두 번 읽어 두 번째 발사가 L2 에 적중했다(n=1024 의 4 MB 는 통째로) — **서로 다른 가중치 둘**로 재야 한다. 활성값 hot(#211)은 유효하나 1~2 µs. DVFS 가설 기각.

| 발사당 µs (srv1·srv2·srv3 교차) | main 단독 | 이 스택 단독 | 이 스택 연속(PDL, 다른 W 둘) | stock 연속(다른 W 둘) |
|---|---|---|---|---|
| gemm n=6416 | 180~186 | 183~191 | 167~171 (PDL off 173) | 146~152 |
| gemm n=4096 | 128~135 | 131~136 | 118~121 (123) | 112~117 |
| gemm n=2048 | 84~88 | 82~87 | 73~75 (78) | 63~65 |
| gemm n=1024 | 57~61 | 57~60 | 48~53 (52) | 38~41 |
| W4 m=8 n=1024 | 66~70 | 45~47 | 39~40 | — |
| mhc T=8 / T=32 | 46~47 / 77~78 | 33~37 / 64.5~66.6 | — | stock 37 / 76 |

**판정.** MK-GEMM(W8) 은 연속 발사에서 stock quant+deepgemm 쌍보다 +13/+5/+15/+25% 느리다(교정 전 표기 15~47%). 서빙에 켜면 손해 — 발사 제거 이득(~5 µs/발사)이 커널 손실(10~30 µs)에 못 미친다. 프로파일러: deepgemm sm120 커널은 TMA 128 + 연산 256 스레드, 9~10 스테이지; 커널 자체 161 vs 190 µs(6416). **루프는 DRAM 을 기다리지 않는다**(wait+sync 누적 0.8 µs/167 µs) — cp.async 발행 자체가 병목이며, 순수 스트림 마이크로벤치는 코드 형태 하나(sync·wait 디스패치·스레드 매핑)에 228 ↔ 145 GB/s 를 오가는 특이 거동을 보였고 TMA 1D bulk 도 145 였다. 이 루프 형태의 실제 한계 ~145~150 GB/s; 스위즐 3종(목적지 XOR·A 타일·팩 시점 선형 memcpy)은 모두 피치 144 보다 5~8% 느려 기각.

**먹힌 것**: W4 확장의 로컬 메모리 바이트 배열 → 레지스터 워드 산술(−20~33%; 타일 우선 팩 + cp.async 3단, 더 깊으면 손해); MHC p2 청크 축약을 24 레인에 + 4×4 sinkhorn 16 레인(bar2 13.5 → 5.4 µs; T=8 46→36, T=32 76→65); PDL 연쇄 발사(gemm 발사당 −3~5%, `VLLM_GLM53_MK_PDL`, 기본 off); ksr 호스트 계산·폴드 나눗셈 제거; 단계 스탬프(`-DMK_PHASE_TS`, gemm/mhc, wait 누적, 상한 프로브 `MK_PROBE_SKIP`).
**안 먹힌 것(기록)**: W 채움 끌어올림(배리어 대기로 자리만 옮김), exact wait + 깊이 4·5, last-arriver 폴드, 동적 유닛 배분(n=6416 +1~2%), x cp.async 스테이징(프롤로그가 채움 전체를 기다림), smem 예산 공유(W8 −4~7% → 별도 인스턴스), mhc p1 fn 1회 읽기·fn 재배열(토큰당 지연이 지배), m≤16 mma 가드(언롤 안 break), 스위즐 3종.

## ★★37차 — "컴파일 엄청 오래 걸리네": 부팅 예산 귀속, 5분 공백은 일과성, 컴파일이 아니다 (2026-09-06, 로그 판독 + 계측 추가, 무재부팅)

**운영자 지시 "컴파일 엄청 오래 걸리네."** 현 프로덕션 부팅(28차 체인 PROD, 로그 UTC 09-05 19:30 = KST 09-06 04:30)의 4 랭크 로그를
`[boot-stamp]` 줄과 각 컴포넌트의 자기 로그로 귀속하고, 같은 소스로 뜬 앞선 두 부팅(다른 세션의 실패 부팅 보관본
`~/glm53-logs/fail-K5-060547/`, `fail-GEMM2-091630/`: UTC 09-04 20:16, 09-05 00:03)과 나란히 놓았다. 재부팅·배포 없음.

### §1 부팅 예산 (rank 3 = srv4, 인터프리터 시작 기준 초)

| 구간 | 09-05 19:30 부팅 (690 s) | 09-04 20:16 부팅 (345 s 급) | 성격 |
|---|---|---|---|
| EngineCore 초기화 → TP 그룹 통신기 | 26 s | 26 s | 고정 |
| **TP 그룹 → EP 그룹(`initialize_model_parallel` 내부)** | **302 s, 4 랭크 모두 무로그** | **1 s** | **일과성** — 원인 불명 |
| 모델 로드 | 118 s (읽기 33 + 적용 21 + fp8-dense 폴드/팩 로드 ~45 + 드래프터 15) | 110 s | 고정 |
| profile-run | 146 s | 25 s | 아래 §2 |
| torch.compile(드래프터 두 그래프만) + 캡처 + 워밍업 | 20 s | 20 s | 고정 |

**본 모델은 torch.compile 대상이 아니다** (`torch.compile is turned on, but the model … does not support it`, 매 부팅 로그). Inductor 가
만드는 것은 `dflash_head`·`dflash2_candidate_selector` 두 그래프뿐이고 콜드 15 s, 캐시 2.5 s. 런처는 오버레이 매니페스트 sha 가
바뀌면 `/cache/vllm/torch_compile_cache` 를 지우므로(`start-glm53-nvfp4-tp4.sh` "overlays changed -> clearing") 배포마다 이 15 s 를
다시 내지만 그뿐이다. **"k 를 바꾸면 컴파일 때문에 오래 걸린다"는 진단은 틀렸다**: `speculative_config` 가 컴파일 해시에 들어가
드래프터 15 s 를 다시 내는 것이 전부이고, K5 부팅이 45 분 멈춘 것은 컴파일이 아니라 헤드 `profile_run → _dummy_sampler_run`
정지였다(체인 10 기록 §12, 그때 `[oneshot] STALL` 은 오탐으로 정정됨).

### §2 profile-run 146 s 의 정체 — 배포당 1회 재빌드 110 s + 고정 35 s

| 항목 | 이번 부팅 | 소스 불변 부팅 | 캐시 |
|---|---|---|---|
| osar 확장 nvcc (`[osar] source md5` → `connected`) | 69 s (md5 e3fa4435 신규) | 1 s | `/cache/osar_build/<md5·flags·torch>` 영속 ✓ |
| 메가커널 확장 nvcc + 자체시험 (`source md5` → `armed=`) | 43 s (md5 39540acf 신규; 자체시험은 ~2 s) | 1~2 s | `/cache/mk_build/<key>` 영속 ✓ |
| b12x CuTe-DSL MoE 커널 (`Invalidating stale … Compiling … Persisted`) | 9 s (소스 해시 바뀜) | 0 s | flashinfer `cached_ops/b12x_moe_sm121a_cute_dsl` 영속 ✓ |
| TileLang(mHC) | 0 s | 0 s | 오버레이 shim 이 `TILELANG_CACHE_DIR=/cache/tilelang` 로 돌림 ✓ (`0.1.12/cuda-binaries` 5 cubin) |
| 나머지 (b12x 래퍼, dflash2 selector, 헤드 shm 대기) | ~25 s | ~22 s | — |

→ 캐시가 안 붙는 컴포넌트는 **없다**. 재빌드 110 s 는 소스가 바뀐 배포의 첫 부팅에만 들고, 같은 소스의 재부팅은 profile-run 25 s.
2026-09-03 "부팅 901 → 345초" 기록과 맞다: 345 + 302(일과성) + 43(재빌드) ≈ 690.

### §3 조치 — 공백은 서서 잡는다 (PR, 부팅 검증 대기)

1. `deneb_boot_stamps`(추가 전용 .pth 모듈)에 **분산 초기화 스탬프**: `init_device`, `init_distributed_environment`(dist-world),
   `init_model_parallel_group`(그룹 이름별 `dist-group tp/pp/dp/ep`), `initialize_model_parallel`. 302 s 가 재발하면 어느 그룹인지 줄로 남는다.
2. **느린 구간 스택 워치독**: 스탬프된 구간이 `DENEB_BOOT_STAMP_SLOW`(기본 60) 초마다 아직 돌고 있으면 그 스레드의 파이썬 스택을
   로그에 찍는다(구간당 최대 10회, 데몬 스레드, `finally` 에서 종료). 컨테이너에 py-spy 도 ptrace 도 없으므로 이것이 유일한 현장 스택이다.
   가짜 vLLM 모듈로 11 구간 래핑·반환값 통과·워치독 발화를 확인했다(`tests/test_logic.py` 핀 추가).
3. `[fp8-dense] … megakernel packs: … in N s` — 모델 로드 118 s 중 귀속 안 되던 ~45 s(팩 캐시 로드·폴드 패스)에 시간을 붙였다.

**운영자 지시 "그렇게해. 그리로 k값조절 jit도 수정하고"** → §4·§5.

### §4 배포 사전 빌드 (런처 `PREBUILD=1` 기본, `PREBUILD=0` 생략)

런처가 **구 컨테이너를 내리기 전에**(memfree preflight 앞) 4 노드 동시에 일회용 CPU 전용 컨테이너(`--cpuset-cpus=19`, `nice -n 19`,
`CUDA_VISIBLE_DEVICES=` 비움 — 두 확장 모두 nvcc 플래그에 아키텍처가 명시돼 torch 가 장치를 묻지 않는다)로 서빙 컨테이너와 같은
오버레이 마운트·`-e` 환경을 주고 `glm53_megakernel._build()` 와 `dsv4_oneshot_shim._build()` 를 부른다. 빌드 디렉터리가 소스 md5·플래그·
torch/CUDA 키라 부팅이 그대로 물려받는다. 300 s 상한, 실패해도 부팅은 예전처럼 안에서 빌드(비치명). 소스 불변이면 ninja 무작업 ~5 s.

| 증명 | 값 |
|---|---|
| srv4 단독 하니스(프로덕션 오버레이·플래그, GPU 없음) | `megakernel ok in 5s`, `osar ok in 0s` — md5 39540acf / e3fa4435 = 프로덕션과 동일 키 |
| 소스 변경 시 실제 nvcc 시간(사전 빌드 창) | **미측정** — fusion 세션 브래킷 중이라 부팅 창으로 미룸(규칙: 디코드 레그 중 무거운 CPU 금지) |
| 플릿 첫 실전(4 노드 ssh 경로) | **대기** — 다음 배포 부팅에서 `== 확장 사전 빌드 ==` 아래 노드별 `[prebuild] … ok` 줄로 확인 |

### §5 k≠7 부팅 — 진단 계획 (플릿 창 대기)

코드로 보이는 것: 메가커널 MHC/MLA/GEMM 은 T·m 을 일반적으로 다룬다(`t >= num_tokens` 탈출, `0 < m <= 32`); TileLang mHC 커널은
`T.dynamic("num_tokens")` 라 형상 특화가 없다; b12x 정적 MoE 는 `static_m{m}` 으로 m 별 CuTe-DSL 커널을 새로 컴파일한다(m=6 지원 여부 미확인);
`tf32_hc_prenorm_gemm` 호스트 휴리스틱은 `_C.so` 안이라 M=6 에서의 거동을 읽을 수 없다. 체인 10·13 의 두 증상(샘플러 Triton 초기화 정지,
프리놈 GEMM CPU 200% 스핀)은 **먼저 GPU 를 교착시킨 커널 뒤의 첫 동기화 지점**으로도 읽힌다 — 워커들의 `[oneshot] STALL` 도 랭크 0 대기와 맞다.
→ 계획: (1) `SPEC_K=5 SPEC_K_FORCE=1` 진단 부팅을 #339 워치독 아래서 12 분 상한으로 띄워 `still running after` 스택을 읽는다;
(2) 필요하면 `VLLM_GLM53_B12X_STATIC_V2=""`(정적 MoE 끔) → `VLLM_GLM53_MEGAKERNEL=0` 순으로 이등분; (3) 지목된 커널/호출부를 고쳐
(후보: 프리놈 호출부 M 을 8 배수로 패딩 — M%8≠0 일 때만이라 k=7 비용 0; 정적 MoE 레인을 m%8≠0 에서 동적 레인으로 폴백) K5 재부팅으로
검증하고 onepass 로 디코드·프리필·한국어를 잰 뒤 k=7 프로덕션 복귀.

**운영자 "진단 없이 코드로는 못 고쳐?"(08:10)** → 진단 없이 고칠 수 있는 것은 고쳤다: 체인 13 이 스핀 위치로 적은 **프리놈 GEMM 의 M<8 패딩**
(`_deneb_hc_prenorm_gemm`, `glm53_kernels/tilelang.py`, 호출부 3곳). 근거: k=7 디코드(M=8/16/24)와 임의 M 의 프리필(예: 27)은 매일 통과하고,
이 GEMM 에 M<8 이 닿은 유일한 경우가 k=5 부팅이므로 **M<8 만** 검증된 M=8 형상으로 보낸다(0 행 덧붙임 → GEMM → 산 행만 복사; 두 출력 모두
행별 연산이라 패딩 행은 읽히지 않음). k=5·C=1 비용은 층당 196 KB 0-복사 + ~30 KB 복사 둘, k=7 은 이 분기를 타지 않는다. 정적 MoE 는 행을
전문가별 `valid_rows` 로 마스킹하므로(m=6 은 새 CuTe-DSL 특수화만) 손대지 않았다. 나머지는 코드로 단정할 수 없어 부팅에서 본다: K5 체인 0단계에
**프리놈 프로브**(`hc_prenorm_probe.py`, M=8 대조 → M=6, 각 240 s 상한, 헤드의 일회용 GPU 컨테이너)를 넣어 "M=6 에서 정말 멈추는가" 를 먼저 적는다.
체인은 `FLEET-free-for-glmfix.done` 과 `K5FIX-merged.done` 둘 다 있어야 출발한다.

**운영자 "딥젬 말고 mkgemm 써서는 못하나" → "1+2"(08:30)**. 디코드에서 deep_gemm 에 닿는 호출은 **첫 층의 단독 pre-mix 하나**뿐임을 확인했다
(`hc_pre` → `mhc_pre_tilelang`: 소형 분기 없음; 나머지 층의 pre 는 융합 post+pre 훅(T≤16)이, 마지막 post 는 GEMM 없이 처리). 그래서 **2 = pre-only 훅**:
융합 커널의 post 단계는 fp32 로 `v = pm[j]·x + Σ_k cm[k][j]·res[k]` 라 **pm=0, cm=I 를 주면 residual_out == residual 이 비트 단위로 같고** pre 부분이
원래 residual 위에서 돌아간다 — 이미 무장된 커널로 단독 pre 를 받는다(커널 수정 0). 정적 계수 버퍼(T_max=32: x 0 [32×4096] bf16 256 KB, pm 0,
cm I)를 자체시험 때 한 번 만들어 호출마다 접두 슬라이스로 넘긴다(그래프 캡처 안 할당 없음). 자체시험 `_selftest_mhc_pre`: T=8 에서 (1) 항등 검사
`torch.equal(rc, res)`, (2) pm/cm/layer_input 을 stock 쌍(`mhc_fused_tilelang` + `mhc_pre_big_fuse_with_norm_tilelang`)과 대조(≤1e-3); 부팅의
`VLLM_GLM53_SPEC_K`≠7 이면 T=k+1 도 검사한다(k=7 프로덕션은 서빙하지 않는 T 를 절대 돌리지 않는다). 무장 키 `mhc_pre`(MHC 세그먼트 무장 아래),
노브 `VLLM_GLM53_MK_MHC_PRE`(기본 1). 서빙 증명: `[megakernel] mhc-pre hook serving (T=…)` 첫 호출 1회 + `_HOOK_SERVED_PRE`. 배선은
`mhc_pre_tilelang` 의 GEMM 앞(T≤16, norm 있음), 미스는 stock 으로 낙하. CPU 스텁 시험(계수·슬라이스·형상·영수증·미스) 통과; GPU 검증은 K5 체인의
부팅 로그(`selftest mhc-pre T=[8, 6] identity=True … -> ARM`)와 k=7 복귀 부팅에서 본다. 속도 이득은 주장하지 않는다(T=8 에서 MK≈stock).

### §6 K5 진단 체인 결과 (09-06 08:49~09:03 KST, fusion 세션이 플릿을 넘긴 뒤 자동 실행)

| 단계 | 결과 |
|---|---|
| 0. 프리놈 프로브(헤드 일회용 GPU 컨테이너) | **M=8: 14 µs/call, M=6: 13 µs/call, rel 2e-4 — M=6 은 단독으로는 멈추지 않는다** |
| main 배포 + PREBUILD(4 노드 병렬, 구 컨테이너 서빙 중) | .cu 가 바뀐 배포: 메가커널 31~34 s + osar 30~32 s = **노드당 62 s**, rc=0 ×4 |
| K5DIAG `SPEC_K=5 SPEC_K_FORCE=1` | **부팅 성공, health 300 s**. `armed={'mhc': True, 'mhc_pre': True, …}`(자체시험 T=[8, 6] 통과), 디코드 1회 SHORT: **44.5 tok/s, raw acc 29.5%, 창 19.4/20.5**; 프리필 warm 2549→2714 tok/s |
| PRODK7 (k=7 복귀) | health **255 s**(PREBUILD 무작업 4~5 s), `selftest mhc-pre T=[8] identity=True rel=1.65e-07 -> ARM`, **`mhc-pre hook serving (T=16)`** — 디코드 스텝에 deep_gemm 호출 없음 |
| 분산 초기화 | 두 부팅 모두 `dist-group ep 1.3 s`, `init-device 8~15 s` — 302 s 공백 재발 없음(원인은 여전히 미확정, 워치독 상시) |

**판독**: k≠7 부팅은 이제 뜬다. 다만 **어느 수정이 살렸는지는 미확정** — 프로브가 M=6 단독 GEMM 의 정지를 재현하지 못했으므로 체인 13 의 스핀은
"M=6 GEMM 자체" 가 아니라 부팅 경로의 다른 조합(당시 커널 상태·캡처 순서)이었을 수 있다. 그래서 K5B 체인이 (a) k=5 3회 브래킷 + onepass,
(b) `VLLM_GLM53_MK_MHC_PRE=0`(패딩 경로) 1회, (c) k=7 복귀 + onepass 로 **k=5 의 이득과 두 수정의 기여**를 나눠 적는다(§7 예정).
K5DIAG 의 44.5 tok/s 는 SHORT 1회라 판정 수치가 아니다. 부수 정정: 러너의 팩 경고가 `cached=180`(캐시에서 읽은 GPTQ 팩)을 RTN 으로 오인 →
`gptq=0 AND cached=0` 일 때만 경고하도록 고침(bench/ab-lever.sh, srv2 ab-lever2.sh).

### §6-b 302 s 공백 재발 — 워치독이 잡았다 (K6 부팅, 09-06 09:25 KST)

`dist-group tp took 302.2s`, `dist-model-parallel took 303.5s`, `init-device took 314.7s`, health 570 s. 60/120/180/240 s 스택 덤프 4회가 모두
같은 자리다: rank 0 `parallel_state.py:2214 in_the_same_node_as → torch.distributed.broadcast_object_list → broadcast`(공유메모리 이름을 보내는 쪽),
rank 3 `parallel_state.py:2224`(받는 쪽) — **TP 그룹 CPU(gloo) 그룹의 첫 집합통신**에서 4 랭크가 함께 ~300 s 를 기다린 뒤 성공한다.
같은 날 K5DIAG(08:50)·PRODK7(08:58) 부팅은 같은 자리에서 2.0 s. 인터페이스 카운터(enp1s0f0np0 RX/TX errors·dropped 0), `tcp_syn_retries=6`(SYN 백오프 합 127 s)
으로는 300 s 가 설명되지 않는다 — gloo 페어 연결/스토어 대기의 고정 타임아웃 한 번을 먹고 넘어가는 모양이다. **다음 실험(운영자 판단)**: gloo 만
관리망으로 옮기기(`GLOO_SOCKET_IFNAME`을 RoCE NIC 가 아닌 인터페이스로; NCCL 은 그대로) — 작은 제어 메시지뿐이라 위험이 낮고, 재발률(오늘 6 부팅 중 2)로 판정 가능.

(b) 302 s 는 다음 재발 때 §3 줄로 판독 — 09-04·09-05 의 세 부팅 중 한 번만 났다. → §6-b 로 판독됨.

**§6-c gloo 포트 이동은 답이 아니었다 (10:31 부팅)**: `GLOO_SOCKET_IFNAME=enP2p1s0f0np0` 로도 `dist-group tp took 302.0s` 재발. 정지 중(10:38) 4 노드에서
`ss` 로 본 결과 SYN-SENT 0, 10.10.11.x 의 gloo 리스너만 열려 있고 페어 연결은 없음 → 소켓 연결 시도조차 없는 **스토어 대기**(gloo 페어 주소
랑데부 `store.wait`)로 좁혀진다. c10d 소켓 경고(`TORCH_CPP_LOG_LEVEL=WARNING`) 0줄. vLLM 은 `VLLM_DISTRIBUTED_USE_SPLIT_GROUP=False` 라 `new_group(…, backend="gloo",
timeout=None)`(30 분 기본) 경로 — 300 s 는 c10d **TCPStore 의 기본 타임아웃(300 s)** 과 같은 값이라, 어느 랭크의 스토어 대기가 타임아웃까지 간 뒤 재시도로
넘어가는 그림이 유력하다. 다음 부팅 진단: 런처 `TORCH_LOGS=+distributed`(스토어 배리어·그룹 생성 로그) + `TORCH_CPP_LOG_LEVEL=INFO`. 포트 변경은 되돌리지 않고
둔다(무해, A/B 표본은 계속 쌓임).

### §7 k 스윕과 프로덕션 k=5 (09-06 09:10~09:55 KST, 운영자 "짧은 조사로 최적 k 찾아 바꿔라")

| k | C=1 레그 tok/s | C=4 합산 tok/s | 비고 |
|---|---|---|---|
| 3 | — | — | k=4 < k=5 라 생략 |
| 4 | 41.1 (1회) | 94.1 | prep-fused 꺼짐(Q=5) |
| **5** | **43.3** (3회: 42.7/43.3/48.0) | 95.4 | prep-fused 꺼짐(Q=6) |
| 6 | 39.4 (1회) | 81.0 | prep-fused 꺼짐(Q=7), 부팅 570 s(302 s 재발) |
| 7 | 39.6 (04:41 PRODV3 3회) | 82.7 · 88.1 (36차) | prep-fused 켜짐 |
| **5 프로덕션**(#346 뒤) | 40.3 (1회) | **101.0** | onepass 9/9, 한국어 0/25, 프리필 불변 — **prep-fused 는 2 s 만에 DISARM**(아래) |

**판정**: k=5 채택(프로필 `SPEC_K=5`, 런처 1..7 무가드). C=1 은 회차 편차 ±5 안에서 k=7 과 같거나 조금 위, C=4 는 **+15~22%**. 라이브 카운터로 본 위치별
채택률(k=5): 70 / 46 / 31 / 21 / 15 % — 4·5번째가 스텝당 0.36 토큰을 보태고, k=7 의 6·7번째는 ~0.17 이었을 것이라 k=5 의 이득은 "안 맞을 검증 토큰을 빼는" 이득이 맞다.
one-spark 프로젝트의 독립 스윕도 k=5 를 기본으로 골랐다(산문·코드 최고, 구조화 출력 −16%).

**prep-fused 는 이 부팅에서 서빙되지 않았다**: `plan built … q=6` 뒤 51 s, C=4 레그 첫 자체점검(fused step 384)에서 `DRIFT: exp_bt(432), idx_bt(108)` → DISARM.
원인은 #346 의 Q_P2 패딩 레인: 확장 블록테이블·인덱서 테이블의 2-D 저장이 열 마스크만 갖고 있어 레인 ≥ Q 가 **다음 요청의 행**(qs+6, qs+7)을 덮어썼다
(C=1 에서는 비교 범위 밖이라 64~320 스텝 점검이 통과했고 C=4 에서 드러났다). 두 저장에 레인 마스크를 붙여 고침(PR 대기); 검증은 k=5 부팅 + C=4 트래픽에서
`checks ok` 가 쌓이고 DISARM 이 없는 것. 따라서 §7 표의 "5 프로덕션" 수치는 융합 꺼진 stock 경로의 값이고, 40.3 vs 43.3 은 같은 경로의 회차 잡음이다.

**운영자 "패딩 말고 다른 해법" → "200줄 쿠다면 10분"(10:20)**: 준비 융합 커널을 CUDA 로 다시 썼다 — 메가커널 확장의 `mk_prep_kernel`/`run_prep`
(요청당 블록 1개 + 채움 블록 1개 + KV 그룹당 패드 블록 1개, Q 토큰은 그냥 루프: 2의 거듭제곱 제약도 마스크도 없음). 포인터 33개·정수 23개를 위치로
넘기므로 양쪽 순서를 이름으로 핀(`tests/test_logic.py`), 버퍼 dtype 은 플랜 빌드 때 검사해 어긋나면 Triton 커널로 낙하(`VLLM_GLM53_PREP_FUSED_KERNEL=triton`
으로 강제 가능). GPU 없는 컨테이너 nvcc 컴파일 32 s 통과, `plan built: … kernel=cuda` 가 서빙 줄. 검증은 k=5 부팅 + C=4 트래픽의 자체점검(§7 위와 동일 기준).

**융합 켠 k=5 재시험 1차(10:31 부팅) — profile-run 정지로 중단(10:47)**: 302 s 공백(포트 1 에서도) → 모델 로드 126 s → 메가커널 `armed={… 'mhc_pre': True …}`
(md5 f5484978, 사전 빌드 .so 를 2 s 에 물려받음 = PREBUILD 증명) → **profile-run 이 240 s+ 멈춤**: rank 3 스택은 4회 덤프 모두 `hc_fused_post_pre → mhc_fused_post_pre_tilelang
→ tilelang jit __call__ → tvm_ffi module.__call__`(**stock TileLang 커널 런치 안**), GPU 사용률 96% — 앞선 커널이 스핀 중이라 런치가 막힌 모양(체인 10 K5 정지의
서명과 같다: 호스트 스택은 "다음 런치"에 멈춰 보인다). osar 는 `connected`·self-test PASS, STALL 줄 0. osar 의 `source md5 → connected` 70 s 는 빌드가 아니라 접속
단계(사전 빌드로도 그대로) — 09-05 의 "osar nvcc 69 s" 귀속은 정정. 같은 구성으로 즉시 재부팅(PRODK5G, 예산 900 s)해 재발 여부를 본다; 재발하면 PREBUILD=0 →
`VLLM_GLM53_PREP_FUSED_KERNEL=triton` → `VLLM_GLM53_MK_MHC_PRE=0` 순으로 이등분.

**융합 켠 k=5 재시험 3차(11:04 부팅, PREBUILD=0 = 노드에서 빌드) — 성공, 이것이 판정 수치**

| 항목 | k=7 (오늘) | k=5 융합 꺼짐 | **k=5 융합(CUDA 준비 커널) 서빙** |
|---|---|---|---|
| C=1 레그 tok/s (3회) | 37.7/39.6/40.9 → 39.6 | 42.7/43.3/48.0 → 43.3 | **47.3/43.0/49.8 → 47.3** |
| C=1 창 step/s | 18.4~19.4 | 19.4~20.2 | **20.4~20.9** |
| C=4 합산 tok/s | 82.7 · 88.1 | 95.4 | **97.1 · 94.9** |
| onepass 사실검색 / 한국어 | 9/9 / 0/25 | 9/9 / 0/25 | 9/9 / 0/25 |
| onepass 디코드 창 step/s (ko/2K/32K/128K) | 18.9/18.4/18.2/17.9 | — | **20.9/20.7/19.4/19.9** |
| tokens/step (정정된 k) | 2.86 | 2.92 | 2.79 (raw acc 35.7%) |
| 융합 증명 | — | DISARM(2 s) | `plan built: mode=on kernel=cuda q=6`, **`fused_steps=4096 checks ok=64 drift=0`** |

**판정**: k=5 + CUDA 준비 융합이 프로덕션. 단일 스트림 +19%(39.6→47.3, 3회 중앙값; 회차 편차 ±3), C=4 +10~17%, 품질·프리필 불변. 스텝이 20.4~20.9 step/s 로
오늘 본 최고치. 302 s 공백은 이 부팅엔 없었다(dist-group tp 1.9 s).

**사전 빌드(PREBUILD) 사고 — 기본값 OFF 로 되돌림**: 10:31·10:47 두 부팅이 profile-run 에서 멈췄다(GPU 96%, 스택은 stock TileLang 런치). 공통점은 GPU 없는 컨테이너가
nvcc 로 만든 메가커널 .so 를 물려받은 것(md5 f5484978, 무장 자체시험은 통과). 캐시를 지우고 노드에서 빌드한 11:04 부팅은 같은 소스로 정상 → CPU 전용 컨테이너의
산출물이 원인. 원인 규명(같은 키로 두 빌드를 만들어 `cmp`/`cuobjdump`)까지 PREBUILD=1 은 실험 노브. 부수 정정: 캐시 삭제 명령의 `$()` 이스케이프 실수로 4 노드의
`/cache/mk_build` 전체를 지웠다(빌드 캐시라 손실은 재빌드 40 s 뿐, 데이터 아님) — 중첩 인용에서 `\$(…)` 는 바깥 셸이 먼저 평가한다.

**사전 빌드 바이너리 대조(11:40)**: 배포 오버레이(md5 f5484978)를 GPU 없는 컨테이너에서 같은 키(ba00777963ef)로 다시 빌드해 노드 빌드와 비교 — `build.ninja` 의
nvcc 명령줄 완전 동일, .so 크기 동일(1,410,536 B), **차이는 `.eh_frame_hdr` 3 바이트뿐**(fatbin/SASS 바이트 동일). 즉 CPU 컨테이너 산출물은 코드가 같고,
"사전 빌드 .so 가 나쁘다"(H1)는 기각. 멈춘 두 부팅과 정상 부팅의 남은 차이는 (a) 302 s 공백의 유무, (b) 무장이 2 s(사전 빌드) vs 40 s(노드 빌드) 인 타이밍.
(a) 단독은 04:30·09:25·09:36 의 공백 부팅이 멈추지 않아 약하고, (b)+(a) 조합 또는 다른 원인이 남는다. PREBUILD 기본 OFF 는 유지(타이밍도 차이), 다음 조치는
메가커널 스핀 루프에 **기한 트랩**(N 회 이상 대기 시 `__trap()`)을 넣어 다음 정지가 메가커널 안인지 밖인지 즉시 갈리게 하는 것.

### §8 밤 라운드 준비 — 드래프터 재학습 (운영자 "새벽에 해볼까" → 승인, 01:30 KST 자동 시작)

- **데이터**: `glm53_draft_dump`(#350) 가 프리필 스텝마다 타깃의 aux 히든 5층·토큰·상위 16 로짓·lse 를 rank 0 NVMe 에 씀(토큰당 ~40 KB). 말뭉치
  `corpus.jsonl` = 한국어 위키 1,341 + 영어 위키 995 프롬프트(≤3,500 토큰, 합 ~4M) + 코드 0.1M(CodeAlpaca; the-stack 은 게이트). 피더는 `max_tokens=1`, C=8.
- **학습기** `train_dflash2.py`: z-lab `model.py` 의 `DFlash2DraftModel` 을 그대로 쓰고, 생성 루프와 같은 형태(블록마다 새 컨텍스트 특징 + `[x_t, mask×7]`
  노이즈 임베딩, KV 캐시를 컨텍스트 길이로 crop)로 2,048 토큰 시퀀스를 배치 8 로 돈다. 손실 = 0.1·CE(타깃 argmax) + 0.9·L1(상위 16 로짓, lse 로 정규화)
  + 1.0·선택기 CE(상위 16 후보 중 정답, 이전 토큰 teacher forcing). 타깃의 임베딩·lm_head 는 체크포인트에서 읽어 고정. 배치당 backward 1회(캐시가 앞
  블록 그래프를 물고 있음). 소형 구성 CPU 시험 통과. 저장 후 `fix_draft_keys.py` 로 코드북 키를 서빙 레이아웃(`.weight` 없음)으로 되돌리고 원본 config 복사.
- **체인** `night/lever-chainNIGHT-glmfix.sh`(srv2): main 배포 → 덤프 모드 부팅 → 4.5M 토큰 피드(~30~40 분) → 플릿 정지, srv2 GPU 단일 노드 학습 2 epoch
  (~1 h 추정) → 새 드래프터로 부팅(`DRAFT_HOST_PATH`, #353) 디코드 3회 + onepass + 위치별 채택률 → 원래 드래프터로 프로덕션 복귀 → 플릿 반환. 교체 판단은
  아침에 수치로.
- **리스크**: 파이프라인 첫 실행(덤프 형식·학습 메모리·저장 키)이 밤에 깨지면 그 밤은 잃고 프로덕션만 복귀한다. 라이선스: DFlash2 는 CC BY-NC-ND — 재학습본은 내부 사용만.

### §9 논리 능력 점검 (운영자 "모델 논리 능력 테스트해봐" → "변별력 없는 건 왜" → "서술형 하나 내서 수준 봐", 11:30~12:00 KST, 프로덕션 k=5 융합)

`bench/reason_eval.py`(온도 0, 서버 기본 thinking, 답 상한 3,072 토큰, C=4):

| 세트 | n | 정답률 | 답당 토큰 | 지연 | 상한 절단 | 읽는 법 |
|---|---|---|---|---|---|---|
| GSM8K (영어 산수 문장제) | 60 | **100%** | 240 | 6.6 s | 0 | 포화 벤치 — 양자화 스택이 산수를 안 망친 증거 이상은 아님 |
| MMLU formal_logic | 60 | 93.3% | 800 | 24 s | 3 | 오답 4 중 3 이 절단 → 끝낸 문항 기준 56/57 |
| KMMLU (Math·Law·한국사) | 60 | 61.7% | 1,656 | 57 s | **19** | 끝낸 41 문항 중 37 정답(90%); 한국어 지식 문항에서 사고가 길어 절단이 지배 |
| 한국어 논리 퍼즐(자작 20) | 20 | 95% | 318 | 9.3 s | 1 | 유일한 오답 = 절단 |

**자작 고난도 문제(기존 벤치 아님)**: 5인 기사/악당/변덕쟁이 + 자기참조 문장 + 개수 제약(243 경우 전수 대입으로 유일해 확인). 모델 답
`A=기사, B=악당, C=변덕쟁이, D=악당, E=악당, 변덕쟁이의 참 문장=(1)` — **정확**, 가정 분기마다 반대편을 닫는 유일성 증명 포함, 사고 ~2만 자.

**서술형(자작, 정량 시스템 설계)**: 위 §7 의 실측(스텝 49 ms, 위치별 채택률 70/46/31/21/15%, C=4 97 tok/s 등)을 주고 "47→80 tok/s 계획을 분해·레버·역효과
레버까지 1,200자로" 요구. 1차(사고 상한 16K 토큰): **사고 5만 자를 쓰고 답을 못 냄(finish=length)** — 사고 내용은 유의미했다(활성 바이트 ~8~10 GB/스텝 →
대역폭 상한 ~8~10 ms vs 실측 49 ms → 이용률 ~20% 를 가장 큰 레버로 지목, k=3·확신도 자르기·트리·증류를 검토)지만 산술 재검산을 반복하며 수렴하지 못했다.
개방형 정량 문제에서 사고 예산을 스스로 못 끊는 것이 약점. 2차(사고 끔 6K / 사고 40K) 결과는 아래.

2차: 사고 끔(`enable_thinking=false`, 6K) — 이 템플릿에선 사고가 본문으로 새어 나와("Let me analyze…") 18.6K 자를 쓰고 답 없음 → 사고 끄기는 무효.
그 본문에서 모델이 **문제의 자기모순을 잡았다**: 20.5 step/s × 2.83 = 58 ≠ 47 tok/s → "드래프터 ~11 ms 가 별도" 로 조정(실제로는 내가 정상상태 창
step/s 와 레그 평균 tok/s 를 섞어 쓴 출제 실수). 사고 40K 는 결과 대기.

사고 40K: **894 s, 4만 토큰(사고 10.1만 자), 답 0 자** — 사고 끝부분에 한국어 최종 답의 초안이 완성돼 있었고("Total ≈ 300+480+430+100 = 1,310")
글자 수를 맞추느라 다듬는 중 상한. 초안의 내용은 수준이 높다: (1) 47/2.83 = 16.6 step/s → 주기 60 ms = 타깃 49 + 드래프터 11(출제 모순을 이렇게 해소),
k 7→5 의 −8% 에서 검증 토큰 1개의 한계 비용 ≈ 2.4 ms 를 역산, 주기를 고정 가중치 읽기 35 ms(58%) + 토큰 비례 14 ms + 드래프트 11 ms 로 분해;
(2) 레버: 드래프트 비용 축소(+10~15%), **실효 대역폭**(스텝당 고유 바이트 수 GB vs 4 노드 1.08 TB/s → 이용률이 낮다, 관건), 채택률(③, 절단됨);
(3) **k=3 역효과**(채택 −13% vs 주기 −8% → 45 tok/s) 를 임계 채택률 p* = N·ΔT/T ≈ 2.83×2.4/60 ≈ 0.11 로 정식화 — 4·5번째(0.21·0.15)는 p* 위라
k=5 가 최적, 6·7번째(+0.03)는 아래라 7→5 가 옳았다; **확신도 절단도 같은 부등식**(절감은 토큰당 2.4 ms 뿐, 고정 읽기는 스텝당 1회) — 우리 §7 의
실측·판단(k=5 최적, 절단 무효)과 일치. 결론 "토큰을 줄이는 게 아니라 고정 비용을 더 많은 토큰으로 나누는 방향(채택·대역폭)".
정성 판단: 분석 자체는 우리가 하루 걸려 도달한 결론을 주어진 숫자만으로 재구성한 수준이고 p* 정식화는 우리보다 깔끔하다. 그러나 **결과물을 내놓지 못한다** —
분량 제약이 있는 개방형 문제에서 사고 안에서 초안을 쓰고 글자 수를 세며 무한히 다듬는다(16K·40K 모두 답 0). 실사용에선 치명적. 완화 후보: 분량 제약 제거,
`reasoning_effort` 제한(아래 실험).

`reasoning_effort` 실험(분량 제약 제거): **medium** — 16K 토큰 사고 5만 자, 답 0(여전히 상한); **low** — 사고 0 자, 86 s, 4,413 토큰, **5,640 자 완결 답**.
low 의 답: 메모리 바운드 진단, 레버(동시 스트림 C, 드래프터 채택률 1.83→3.0 이면 81 tok/s, 전문가 캐싱, 배치 오버헤드), 확신도 절단 기각("메모리
바운드라 검증 토큰 수를 줄여도 가중치 읽기가 그대로"), k=3 기각(3번째 위치 0.10 손실, 최대 61 tok/s) — 방향은 모두 맞다. 흠: 스텝당 39 GB 를
대역폭×시간으로 역산(순환 논법), 위치별 채택률을 조건부로 곱해 2.15 를 얻고 1.83 과의 차이를 얼버무림, C=4 의 2배를 "직렬화 오버헤드"로 오진(실측은 활성
전문가 증가). 40K 사고 초안(p* 임계, 한계비용 2.4 ms)이 더 날카로웠다. **운영 결론**: 이 템플릿에서 개방형 장문은 `reasoning_effort=low`(사고 없이 바로 답,
`enable_thinking=false` 와 달리 사고가 본문으로 새지 않음)가 답을 보장하고, 닫힌 문제는 기본 thinking 이 맞다. API 게이트웨이에서 요청 유형별로 갈라 줄 것.

**논증 분석(자작, 결함 4개 심음: 생존자 편향/역인과·성급한 일반화·사후오류+기저율 교란·인신공격+자기봉쇄, 보너스 거짓딜레마)**: 147 s, 사고 1.1만 자, 답 1,330 자,
정상 종료. 심은 결함 전부를 찾고 각각 이름·이유·보강 증거(2×2 교차표, 무작위 시범 도입, 차이의 차이(DiD), 익명 설문, 크로노타입·수면 추적)를 붙였고
전사 10% vs 3팀 12% 의 기저율과 신규시장 교란을 정확히 결합했다. 흠은 "합성 오류" 라벨(성급한 일반화가 맞음) 정도. 정성 판단: 구조·정확성·간결성 모두
전문가 수준, 한국어 자연스러움, 환각 없음.

**하니스 정정**: `bench/bracket.py`·`bench/onepass.py` 의 tokens/step·step/s 가 k=7 을 가정해(`--num-spec` 기본 7) k=5 에서 tokens/step 3.68 로 부풀렸다
(진값 2.92 = 1 + 5×0.383). 카운터(`num_draft_tokens_total / num_drafts_total`)에서 실제 드래프트 길이를 읽어 쓰도록 고침(`spec_k_eff`). ab-lever 레그 줄의
"raw acc" 는 드래프트당 비율이고 표기에 `(k=…)` 를 붙였다.

**철회**: "선택기 점수로 제안을 자르면 검증 비용이 준다"(경로 1)는 이 러너에서 성립하지 않는다 — uniform decode 의 쿼리 길이는 스텝 전체가 k+1 로 고정되고
그래프도 그 형상으로만 캡처되므로, 요청별로 제안을 -1 로 잘라도 타깃 계산량은 같다. 스텝 단위 동적 k 는 k 별 그래프 캡처 + 러너 변경이 필요해 별도 과제.
DSpark 의 확신도 헤드 이점도 같은 이유로 지금 스택에서는 실현되지 않는다. 남는 레버는 드래프터 품질(밤 재학습).

## ★★★36차 — 다음 레버를 숫자로 고르기: C>1 처리량, 128K 프리필 지도, 하니스 결함 둘 (2026-09-06 07:00~07:15 KST, 프로덕션 부팅 위 무재부팅 측정)

**운영자 지시 "4·5·6 진행."** 모두 현 프로덕션 부팅(v3 정적 MoE + GPTQ 팩)에서 재부팅 없이 쟀다.

### §1 (4) 동시 요청 스케일링 — 스텝이 C 에 따라 가파르게 늘고 C=4 에서 포화

| C | 합산 tok/s (2회) | 창 step/s | 스텝(ms) | raw acc |
|---|---|---|---|---|
| 1 | 39.4 · 41.1 | 19.4 | 52 | 16~17% |
| 2 | 51.5 · 67.4 | 12.9 · 13.5 | 76 | 22~25% |
| 4 | 82.7 · 88.1 | 9.0 | 111 | 23% |
| 8 | 74.9 · 83.5 | 8.7 · 9.0 | 113 | 24% |

가중치 스트림이 스텝을 지배한다면 C=4 의 스텝은 C=1 의 1.2배쯤이어야 하는데 **2.2배**이고 C=8 은 C=4 와 같은 스텝에 tok/s 도 같다(합산 ~85 tok/s 에서 포화). 원인은 MoE 라우팅: 스펙 k=7 로 시퀀스당 8 토큰이 검증되니 C=4 = 32 토큰/스텝 → 활성 전문가 U 가 40 → 150 급으로 늘어 정적 MoE 커널의 전문가 스트림이 ~3배(b12x 표: U=40 727 µs, U=64 861, …) — 스텝 +55 ms 가 그 몫이다. **함의: 동시 요청 트래픽에선 스펙 디코딩이 처리량을 깎는다.** 스펙 없이 C=8 이면 스텝당 8 토큰(U≈50, C=1 스펙 스텝과 같은 급 → ~19 step/s) → ~150 tok/s 로 지금의 1.8배가 산수상 가능. vLLM 상류의 `disable_by_batch_size`(배치 ≥N 이면 드래프트 0) 는 이 포크의 V2 러너/dflash2 스펙큘레이터에 없다(설정 파일에만 흔적) — 구현 시 스펙 0 디코드 그래프 형상 캡처가 필요. **다음 실험 후보 1순위**(트래픽이 동시 요청이라면).

### §2 (5) 프리필 지도 — 32K 와 128K 의 커널 구성 (torch profiler, `bench/profile-step.py`)

| 버킷 | 32K (13.8 s GPU, 4K 스텝 10개: 1,384 ms/스텝) | 128K (56.9 s, 37 스텝: 1,537 ms/스텝) |
|---|---|---|
| MoE dynamic(cute-dsl `MoEGatedDynamicKernel`) | 3,058 ms **22%** | 11,331 **20%** |
| NCCL AllReduce(RING_LL, 455/1,729회) | 2,246 **16%** | 9,322 **16%** |
| MK-MLA(`mk_mla_kernel`) + mqa_logits + topK | 1,734+148+41 **14%** | 7,072+2,294+540 **17%** |
| mHC 계열(`mhc_post` 1,162/4,616 · `mhc_pre` 692/2,755 · tf32 `hc_prenorm` 533/2,108) | 2,387 **17%** | 9,479 **17%** |
| KDA 청크 커널(gla_o·recompute_w_u·conv1d·delta_rule·kkt×2·cumsum·merge) | ~1,150 **8%** | ~4,300 **8%** |
| fp8/fp4 deep_gemm 투영 + cutlass bf16 + nvjet | ~1,600 **12%** | ~5,900 **10%** |
| 양자화·norm(per_token_group_quant 등) | ~1,100 **8%** | ~2,000 **4%** |
| 유휴/런치 글루 | 0.7% | 0.9% |

읽기: 128K 도 "병목 하나"가 아니라 32K 와 같은 비율로 커진다(어텐션만 14 → 17%). AllReduce 는 9-03 채널 스윕으로 패브릭 바닥(142 Gb/s)이고 mHC 는 콜사이트 튜닝 소진(원장 `mhc_post` R2) — 남은 큰 덩어리는 **프리필 MoE dynamic 커널(20~22%)** 로, 35차가 정적(디코드) 커널에 한 DRAM 행 세그먼트 진단을 그대로 적용할 수 있다(b12x 세션의 판정 규칙 "프리필 dynamic 커널도 같은 방법"). 그 다음이 MK-MLA(12~13%).

### §3 (6) 하니스 결함 둘

- **레버 env 미적용(9-05 PRODRTN, `MK_PACK_GPTQ=0` 이 컨테이너에서 1로 읽힘)**: 재현 실패 — `ct_load_profile` 은 호출자 값을 다시 적용하고(`env KEY=0 bash -c '. common-tp4.sh; ct_load_profile …'` → 0 유지), `bash -x` 로 런처를 추적하면 `ENVV` 에 `-e VLLM_GLM53_MK_PACK_GPTQ=0` 이 들어간다. 원인 미상으로 남기되 **증명을 하니스에 넣었다**: `bench/ab-lever.sh`·srv2 `ab-lever2.sh` 가 health 뒤 `docker exec glm53 env` 에서 레버 키의 실제 값을 찍는다(다음 재발 때 판독).
- **팩 줄 감시**: 같은 자리에 `megakernel packs:` 줄을 찍고, 타깃이 `gptq=0` 인데 교정 덤프가 있으면 WARNING(캐시 소실·이름 불일치로 조용히 RTN 이 되는 함정).

## ★★★35차 부록 — b12x v3 정적 MoE 커널 기본값 검증 부팅과 통합 측정(onepass) (2026-09-06 새벽, 브랜치 ostcode/glm-flash-kernel-improvement-5a65a8)

**운영자 지시:** PR #332 머지 뒤 "디폴트화" → 프로필 `VLLM_GLM53_B12X_STATIC_V2=w`(8a2f443). 브래킷 없이 기본값이 먼저 된 셈이라 첫 프로덕션 부팅(체인28, 04:28 KST, 전체 다리 + 한국어 3회)이 서빙 브래킷을 겸한다. 오버레이 = main(b12x v3 + 33차 GPTQ 패커), GPTQ v4 팩 캐시 로드 215.

| 팔 | 디코드 창 (rep) | tok/s · raw acc | 프리필 | 수용률·품질 | 한국어 | 비고 |
|---|---|---|---|---|---|---|
| **PRODV3 (체인28) = 현재 프로덕션** | **19.0·19.4·18.4** (rep 14.8·16.2·14.9) | 37.7·39.6·40.9 · 22.1/20.5/24.9% | 2,598 → 2,697 (불변, dynamic 커널 손 안 댐) | 3.44 tok/step(accepted/draft 2.441), **9/9** | **0/16 · 0/16 · 0/16** | GPTQ 부팅(18.4·18.4·17.9 / 18.0·18.5·17.9) 대비 창 **+3~5%** — 커널 +7~10% 의 스텝 몫(MoE ≈ 스텝의 절반)과 맞음. rep 열의 step/s(14.8~16.2)는 tok/s÷(1+k·acc) 도출값이라 창(직접 카운터)이 판정 채널. 서빙 증명: `[b12x static v2] lane serving: static2_m32_k4096_n512_t8_r640_tm32f2g3a32w (mac=48, m=32, routed=256, smem=98304 B)`(m=24 도). **판정: v3 기본값 확정** — 속도 +3~5%, 프리필 불변, 9/9, 한국어 0/48 |

**통합 측정 `bench/onepass.py` (운영자: "프리필·한국어·디코드·품질을 한큐에", "품질 문서로 프리필 재고 거기서 한국어로 응답"):** 요청 9개(2K/32K/128K 품질 문서 × 3문항)를 스트리밍으로 보내 첫 토큰 시각 = 프리필(문맥당 첫 문항 cold, 나머지 warm), 답은 한국어 두 문단(고유 표기는 원문 인용)으로 받아 정답 검사(한국어 표기 any-of) + 손상 스캔, 생성 중 2 s 창 ~45개 + 스펙 카운터 = 디코드·수용률 → 다리 35분을 ~5분으로. 옛 한국어 8프롬프트(알려진 동률 자리)는 `--korean-extra`. `LEGS=onepass`(bench/ab-lever.sh). **같은 부팅(체인28 PRODV3)에서의 대조:**

| 게이트 | 기존 다리(35분) | onepass 1차(228 s, 9요청) | onepass 2차(400 s, +옛 한국어 16) | 재확인(157 s, 2K + 옛 한국어) |
|---|---|---|---|---|
| 프리필 warm tok/s | 5.8K→23K: 2,598→2,697 | 2K 2,053 · 32K 2,648 · 120K 2,579 | 2,046 · — · 2,321 | 2K 만 |
| 품질 | 9/9 | 9/9 | 9/9 | 3/3 |
| 한국어 | 0/48 (16×3) | 0/9 (검색 답변) | 0/25 (9 + 16) | 0/19 |
| 디코드 창 중앙값 | 19.0·19.4·18.4 (n=8·7·7) | 17.7 (n=26, 전 구간 혼합) | ko 16.0 · 2K 14.5 · 32K 16.0 · 128K 15.0 | **ko 18.5 (n=46)** · 2K 15.5 (n=9) |
| raw acc / tok/step | 22/20/25% · 3.44(프로파일) | 29.0% · 3.03 | 27.2% · 2.91 | 26.3% · 2.84 |

읽기: 프리필(32K ↔ 23K)·품질·한국어는 다리와 일치. 디코드 창은 답변 안쪽 창만 세고 문맥별로 나눠야 다리의 수(짧은 문맥 19.0)와 비교되며, 옛 한국어 프롬프트 묶음(ko)이 그 채널(18.5). 2차의 ko 16.0 은 1차·재확인과 어긋나는데 그 시간대 다른 부하 증거는 없어(srv4 foreign 로그 공백) 미해명 — **디코드 수치가 판정을 가를 땐 onepass 를 2회 돌린다.** 2K 문맥 답변의 창이 낮게 읽힌 건 답변 끝 유휴 꼬리 창(답변당 창 3개) 탓 → 구간 양끝 1 s 여유로 수정(c75f333 이후). tok/step 은 스펙 카운터(토큰당 raw acc ×7)라 프로파일 다리의 accepted/draft(3.44)와 정의가 다르다. 검증 러너(`LEGS=onepass`)는 `--korean-extra` 포함 ≈ 6.5분.

## ★★★35차 — b12x 정적(디코드) MoE 커널 접수: 구조 진단과 v2 레인, 측정 대기 (2026-09-05, 골격)

운영자 "b12x 커널 최적화" → "한차례 더 개선". 표적은 flashinfer 의 SM121 NVFP4 MoE 정적
커널(`flashinfer/fused_moe/cute_dsl/blackwell_sm12x/moe_static_kernel.py`, cute-dsl 파이썬
소스) — 디코드 스텝의 47~58%(21차·보충 분해 5)다. 27차(EXP-14)는 이 커널의 실효 대역폭이
MK 레인과 같다(U=40 720 µs = 197 GB/s; 읽기 참조 245)는 이유로 MK_SEG_MOE 를 닫았지만
**커널 자체는 손대지 않았다**. 이 항목은 그 커널을 접수한 기록이고, **GPU 수치는 아직 없다**
(측정 대기 위치는 §5). 아래 수치 중 27차 것만 실측이고 나머지 구조 항목은 코드 읽기다.

### 1. 구조 진단 (코드 읽기 — 실측 아님)

| 항목 | 스톡 정적 커널 | 왜 대역폭을 잃는가 |
|---|---|---|
| 상주 | grid 48(SM 당 1 CTA), 160 스레드 = MMA 4 워프 + DMA 1 워프, smem 90 KB, 255 reg, 협력 발사 | — |
| 아이템 | (m_tile, 슬라이스 4, 전문가) → U=40 이면 160 아이템 / 48 CTA = 3.33 웨이브 | 마지막 웨이브 16 CTA 만 스트림 |
| 아이템 내부 | gate 패스 → pass_sync 배리어 → up 패스 → 활성화·양자화 → FC2 32 타일 → pass_sync | 파이프라인을 아이템마다 3회 드레인, 경계마다 CTA 전체 배리어 |
| 파이프라인 | `ab_stage = min(…, 2)` 고정 | SM 당 인플라이트 DRAM 바이트 ≈ 18 KB(FC1) / 9 KB(FC2) |
| A 박스 | 128 행 × 8 KB/k-타일(실제 행 ≤ 8) 을 gate·up 두 패스에서 재읽기 | L2→SM 트래픽이 가중치의 64% |
| TMA 박스 | 128 행 × 64 B, 행 간격 2048 B(w13) / 256 B(w2) | 행당 반 라인 접근 — DRAM 페이지 지역성 의심 |

상한 산수: 층당 720 µs → 230 GB/s 면 617 µs, 245 면 578 µs → 스텝 −4.3~−6.0 ms(−7~−10%).
캠페인에 남은 최대 단일 레버.

### 2. v2 레인 (`overlay/modules/glm53_moe/moe_static_kernel_v2.py`, 노브 `VLLM_GLM53_B12X_STATIC_V2`, 프로필 기본 `""` = 스톡)

같은 워크스페이스·가중치 뷰·라우팅 프론트엔드·산술(FC1 누적 순서, 중간값 fp4 양자화, FC2
누적, bf16 원자 scatter). 다른 것: (1) gate+up 을 한 TMA 스테이지(한 mbarrier)로 실어 같은
k 루프에서 동시 누적, (2) FC2 전용 스테이지 버퍼 — DMA 워프가 FC1 로드를 다 내면 즉시 FC2
32 타일, 그 다음 아이템의 FC1 로 이어짐(아이템 경계 배리어 없음; 양자화 중간값은 별도
sA2/sSFA2), (3) A 박스 = tile_m 행(32), tile_m 32 허용(누적기 3개 + B 프래그먼트 2벌이 232
레지스터 안), (4) 파이프라인 상태 리셋 없음(`producer_tail` 로 배수), (5) 스펙 `d` 동적
아이템 스케줄(전역 카운터 claim → smem ring, 첫 스테이지 mbarrier 가 공개, 센티널 스테이지로
종료), (6) 스펙 `s` CTA 별 `%globaltimer` 스탬프. 정확한 GLM TP 기하(E 288, H 4096,
I 2048, top-8, nvfp4, swigluoai limit 10)에서만 잡히고, 첫 빌드/로드에 `[b12x static v2] lane
serving:` 증명 줄을 남긴다(22차·28차 교훈). 기본 스펙 `m32,f2,g4,a32` 의 smem 93,184 B
(상한 101,376; f3g2 96,256 ✓, f3g3 105,472 ✗, m128 ✗).

**GPU 없이 컴파일**: 컨테이너에 `CUTE_DSL_ARCH=sm_121a` 만 주고 `torch.cuda.get_device_capability`
·`get_num_sm`·`get_max_active_clusters` 를 패치하면 `cute.compile` 이 srv4 CPU 에서 2~3 초에
.o 까지 만든다 — 8 구성 컴파일 OK, cubin 스필(STL/LDL) 0. cute-dsl 함정: `while` 본문 안에서만
정의되는 변수는 루프 캐리 타입 불안정(`TYPE_UNSTABLE_JOIN`) → 루프 앞 모든 경로에서 정의.

### 3. 측정 방법 (프레시 컨테이너, `probes/run_mk_probe.sh`)

- `probes/b12x_dram_pattern_bench.py`: 플레인 CUDA cp.async 스트리머로 커널의 접근 순서
  (box64: 128 행 × 64 B) vs box128 vs 선형 8 KB 청크, 인플라이트 깊이 1~11 그룹, 48/96 CTA.
  **판정 규칙**: 선형 ≫ box64 면 가중치 타일 재배치(1-D bulk TMA; 프리필 dynamic 커널도 같은
  레이아웃을 읽어야 하므로 그쪽 이식이 선행)가 레버, box64 가 깊이만으로 선형에 닿으면
  파이프라인 깊이가 레버(= v2), 둘 다 아니면 프론트엔드·꼬리·원자 연산(스탬프가 가른다).
- `probes/b12x_static_probe.py`: 스톡 vs v2 여섯 구성, 1 GB 전문가 세트 + 서로소 전문가
  부분집합 회전으로 DRAM-cold, 그래프 리플레이 µs/호출·GB/s(U 8~64), 수치 게이트
  |v2−stock| ≤ 4×|stock−stock'|(bf16 원자 순서만 다름) at T=8/U=40·T=32/U=16·T=80/U=8(3
  m-타일), v2 스탬프(프론트엔드·아이템별 FC1/양자화/FC2·DMA 리드·CTA 별 유휴 꼬리).

### 4. 판정 규칙

v2 U=40 이 스톡보다 ≥ 5% 빠르고 세 형상 수치 PASS → 프로필 스펙 확정 → 브래킷(base → v2 →
base, 프리필 동반; v2 는 static 백엔드(≤ 640 pairs)만 바꾸므로 프리필 dynamic 은 불변이어야
하고 캡처 로그의 증명 줄이 서빙을 증명한다). 5% 미만이면 스탬프로 병목을 다시 읽고 다음 수
(타일 재배치 / 꼬리)를 고른다.

### 5. 첫 두 GPU 창이 가르쳐 준 것 (2026-09-05 15:14 · 15:20, srv2 유휴 서빙 옆 프레시 컨테이너)

**1차(15:14)는 무효였다 — "v2" 여섯 행이 전부 스톡.** 로그에 `static2_` 컴파일도 증명 줄도
없었다. 원인: `_is_glm53_b12x_tp_geometry` 가 `intermediate_size == 2048` 만 허용하는데
`launch_sm120_static_moe` 는 **랭크당 512**(2048/TP4)를 넘긴다 → 게이트가 런치 시점에 한 번도
참이 된 적이 없다. 같은 게이트를 읽는 #184 의 GLM 튜닝 노브(FORCE_BACKEND·CUTOVER·MAC
래더)도 서빙에서 한 번도 걸린 적 없는 잠복 결함이었다(수정 98ba831: 512·2048 허용, 테스트의
"512 = 불일치" 픽스처를 1024 로). 부수 결함: 프로브의 랜덤 라우팅은 U 개 풀에서 실제
~U(1−e^{−64/U}) 전문가만 건드려 "U=40" 이 ~32 전문가였다(GB/s 열 과대) → 27차 방식(풀 순환)으로
수정. **규칙(22차·28차의 프로브판)**: 프로브는 레인이 탔다는 증거(커널 캐시·증명 줄)를 확인한
뒤에만 행을 찍는다 — 이제 `NOT TAKEN` 으로 거부한다.

**2차(15:20)는 레인이 탔고(증명 줄 8개), 스톡 재측정이 27차와 맞는다:**

| 스톡 (정확히 U 전문가) | U=8 | 16 | 24 | 32 | **40** | 48 | 64 |
|---|---|---|---|---|---|---|---|
| µs/호출 | 149.5 | 320.1 | 449.9 | 583.3 | **723.7** | 848.8 | 1129.6 |
| GB/s | 189 | 177 | 189 | 194 | **196** | 200 | 201 |

v2 기본 구성의 수치 게이트는 T=8/U=40·T=32/U=16 에서 PASS(|diff| 0.75 = 스톡 대 스톡 잡음과
동일)했으나 **T=80/U=8(전문가당 80 행 = 3 m-타일)에서 illegal address** — FC2 scatter 의
`warp_epi_rows = valid_rows − tile_m_base − …` 가 전문가 행 수로 클램프해서 tile_m 32 에서 워프
2·3 이 sC(32 행) 밖을 읽었다(스톡은 전문가가 64 행을 넘으면 tile_m 이 128 이라 잠복). 수정
064a307(`valid_tile_rows` 로 클램프). 장치 오류가 프로세스에 고착돼 v2 시간 행은 하나도 못
얻었다 → 프로브는 640-pair 수치 검사를 모든 스윕 뒤로 옮겼다.

**cp.async 패턴 벤치는 판정 도구가 못 된다**: box64/box128/linear × 깊이 1~11 × 48/96 CTA 가
전부 105~137 GB/s(메가커널 2차의 "cp.async 발행 벽" 재현) — 접근 폭 질문은 torch 리덕션
기반 `probes/b12x_segment_bench.py` 로(큐).

### 6. 3차 창 — 여섯 구성을 한 프로세스에 넣은 static3 은 `d`(동적 스케줄)에서 정지 (15:26~15:38)

증명 줄 순서(stderr 는 즉시 나옴)로 `1`·`m32,f3,g2`·`m64,f2,g2` 의 m8·m32 형상은 지나갔고 `d` 의
첫 발사에서 `torch.cuda.synchronize` 에 멈췄다(py-spy). stdout 이 블록 버퍼라 시간 행은 잃음
→ 구성마다 별도 실행(각 `timeout 240`)으로 큐를 다시 짰다. 교훈: **한 프로세스에 여러 커널
변형을 넣지 말 것** — 장치 오류·교착이 프로세스에 고착된다.

### 7. v2 실측 — 세 형상 수치 PASS, U=40 **+4.2~4.9%**, 스탬프가 남은 15% 의 정체를 가른다 (2026-09-05 15:38~15:41, 구성별 별도 실행, 프레시 컨테이너, 서빙 엔진 유휴)

| µs/호출 (GB/s) | U=8 | 16 | 24 | 32 | **40** | 48 | 64 |
|---|---|---|---|---|---|---|---|
| 스톡 (cfg_1 실행분) | 149.5 | 312.3 | 440.8 | 575.2 | **723.4 (196)** | 846.1 | 1128.1 (201) |
| v2 `m32,f2,g4` | 152.6 (−2.1%) | 289.4 (+7.3%) | 422.9 (+4.1%) | 557.1 (+3.2%) | **692.8 (204, +4.2%)** | 819.9 (+3.1%) | 1090.7 (+3.3%) |
| v2 `m32,f3,g2` | 147.6 (+0.9%) | 291.8 (+7.0%) | 422.8 (+10.7%)* | 554.5 (+4.1%) | **692.7 (204, +4.2%)** | 815.2 (+5.0%) | 1075.9 (+4.6%) |
| v2 `m64,f2,g2` | 156.7 (−3.7%) | 305.4 (+3.1%) | 428.6 (+2.8%) | 566.1 (+2.6%) | 706.3 (200, +3.1%) | 831.9 (+2.4%) | 1103.6 (+2.3%) |
| v2 `m32,f2,g4,s`(스탬프) | 153.4 | 292.1 (+8.8%) | 426.9 | 553.6 | **689.9 (205, +4.9%)** | 831.9 | 1091.8 |

(\* 그 실행의 스톡 U=24 가 473.5 로 튀어 과대; 다른 실행들은 441~446.) 퍼센트는 같은 실행의
스톡 대비. 수치 게이트: 네 구성 모두 T=8/U=40 · T=32/U=16 · T=80/U=8 에서 |v2−stock| =
0.56~1.0 = 스톡 대 스톡 잡음(0.75~1.25)과 같은 급 → **PASS**. `m32,f2,g4,d`(동적 스케줄)는
첫 발사에서 교착(§8).

**스탬프(`m32,f2,g4,s`, U=40, CTA 48 × 아이템 160)** — 커널 스팬 678.5 µs(이벤트 689.9):
프론트엔드 11.2 µs(1.6%); 아이템/CTA 3~4(평균 3.33); **아이템 193.2 µs = FC1 134.8 + 양자화
1.0 + FC2 57.0**; DMA 발행 FC1 130.6 / FC2 60.8, MMA 의 FC1 완료가 DMA 발행보다 9.4 µs 뒤
(파이프라인이 꽉 차 있다 = DRAM 바운드, MMA 바운드 아님); 마지막 아이템 뒤 **CTA 유휴 꼬리
중앙 67.9 / 평균 47.3 µs**.

**해석.** 아이템당 DRAM 바이트 FC1 ≈ 608 KB(gate·up 512 + SF) / 134.8 µs = **4.5 GB/s·CTA(48개 =
216 GB/s)**, FC2 288 KB / 57.0 = **5.05 GB/s·CTA(242 GB/s)**. 같은 커널·같은 파이프라인 깊이인데
FC2 가 12% 빠르다 — 차이는 접근 형태다: w13 은 행 간격 2048 B 에 **64 B 세그먼트**, w2 는
256 B 행을 4 슬라이스 CTA 가 나눠 읽어 DRAM 이 **행 전체**를 본다. 즉 27차가 "발사 안 구조"
라 부른 20% 의 정체는 (1) w13 의 반 라인 접근(~7% 전체), (2) 3.33 웨이브의 꼬리(16 아이템이
~190 GB/s 로 75 µs; 이상 58) ~4%, (3) 프론트엔드 1.6% 다. 245 GB/s 이상값 578 + 11 = 589 대
스팬 678.5.

**판정**: v2 는 서빙 형상에서 +4.2~4.9%(스텝 −1.2~1.4 ms, ~2%) — 규칙의 5% 문턱 바로 아래.
브래킷 후보이지만 먼저 다음 수(§9)로 7~8% 를 더 얻는 쪽이 부팅 값어치가 있다.

### 8. DRAM 은 행 세그먼트 폭을 본다 — 벡터 로드 스트리머 실측 (15:57, `probes/b12x_segment_cuda_bench.py`, 1 GB, 8 회전, 768 CTA × 256 스레드, 스레드당 16 B 로드 4개 인플라이트)

| 패턴 | 행 간격 | 세그먼트 | GB/s |
|---|---|---|---|
| w13 | 2048 B | 32 B | 92 |
| w13 | 2048 B | **64 B (스톡·v2 의 TMA 박스 행)** | **167** |
| w13 | 2048 B | **128 B (v3)** | **202** |
| w13 | 2048 B | 256 B | 222 |
| w13 | 2048 B | 512 B | 231 |
| w13 | 2048 B | 2048 B | 235 |
| w2 단독 | 256 B | 64 B | 208 |
| w2 "4 블록 공유" | 256 B | 64 B | 207 (이 벤치의 블록들은 같은 행을 동시에 안 읽는다) |
| 선형 | — | — | **239** |

접근 폭에 단조 의존: 64 → 128 B 가 +21%, 256 B 부터 90% 이상. 커널의 FC1(216 GB/s)이 이
벤치의 64 B(167)보다 높은 것은 연속 k-타일이 같은 행의 이웃 64 B 를 곧 다시 읽어 DRAM 페이지가
살아 있기 때문으로 보이고, FC2 의 242 는 네 슬라이스 CTA 가 같은 웨이브에서 한 256 B 행을 나눠
읽어 행 전체가 보이는 경우다. 앞선 두 벤치는 도구 결함이었다: cp.async 스트리머(발행 병목
105~137), torch 리덕션/strided copy(int32→int64 리덕션 48 GB/s, 커널 형태 의존).

### 9. 다음 수 = v3 (`w`): FC1 을 64 폭 반쪽 두 번 × 256 폭 K 스테이지로 (w13 행 세그먼트 128 B)

`moe_static_kernel_v3.py`: FC1 타일 (32, 64, 256) — 스테이지 = A 4 + gate 8 + up 8 + 스케일 6 =
26 KB, 2 스테이지 + FC2 3 스테이지(v2 그대로, 타일 (32,128,128)) + 중간값·에필로그 = 98,304 B.
반쪽마다 활성화 → sA2 열 [64h, 64h+64) 양자화(v2 의 SW64 공식 그대로) → FC2 는 v2 와 동일.
누적 순서(k-블록 64 단위)가 같아 v2 와 원자 순서만 다르다. GPU 없이 컴파일 OK. 기대: FC1
216 → ~235 GB/s → 아이템 −8%, 커널 −7%(v2 합쳐 −11~13%). 측정 대기(큐 `cfg_w`·`cfg_ws`).

**`d`(동적 스케줄) 교착의 정체**: hang-diag 스탬프에서 48 CTA 전부가 8 아이템 이상을 "완료"
(전체 160개) — claim 슬롯의 smem 로드가 `has_side_effects=False` 라 컴파일러가 루프 밖으로
끌어올려 같은 아이템을 무한 반복. `ld.volatile.shared` 로 교체(큐 `cfg_d2`). 규칙: 다른
워프가 갱신하는 smem 은 volatile 로 읽는다.

### 10. v3 실측 — **U=40 727.9 → 657.2 µs (+9.7%, 215 GB/s)**, 세 형상 PASS (2026-09-05 17:03, `w,s`, 엔진 유휴 옆 프레시 컨테이너)

| µs/호출 (GB/s) | U=8 | 16 | 24 | 32 | **40** | 48 | 64 |
|---|---|---|---|---|---|---|---|
| 스톡 (같은 실행) | 150.9 | 318.3 | 440.2 | 576.7 | **727.9 (194)** | 860.7 | 1142.9 (198) |
| v3 `w,s` | 139.9 (+7.3%) | 274.0 (+13.9%) | 402.6 (+8.5%) | 529.0 (+8.3%) | **657.2 (215, +9.7%)** | 786.8 (+8.6%) | 1040.2 (218, +9.0%) |

(같은 시각의 `w` 무스탬프 실행(16:44)은 스톡 행 자체가 916 µs = 154 GB/s 로 오염 — 다른 세션의
부팅과 겹친 창 — 이라 버린다; 무스탬프 확인 실행은 큐 `cfg_w2`.) 수치 게이트 세 형상 PASS
(|diff| 0.625~0.875 = 잡음 급). **스탬프**: 스팬 654.7; 아이템 185.3 = FC1 127.1(608 KB →
**4.78 GB/s·CTA = 230 GB/s 총**, v2 216) + FC2 58.6(242); 프론트엔드 11.3; 꼬리 평균 44.2.
세그먼트 폭 예측(64 → 128 B)이 FC1 에서 그대로 나왔다. 남은 것: 꼬리(3.33 웨이브, 마지막 16
아이템이 ~189 GB/s) ~7%, 프론트엔드 1.7%, FC1 잔여 4% — 245 GB/s 이상값 589 대 657.

**판정**: v3 는 서빙 형상에서 +9.7% = 스텝 −3 ms(~4.5%) → 규칙의 5% 문턱을 넘는다 → 프로필
스펙 후보 `VLLM_GLM53_B12X_STATIC_V2=w`, 브래킷 대상(운영자 "해봐"). 프리필 dynamic 커널은
손대지 않았으므로 프리필은 불변이어야 하고, 캡처 로그의 `[b12x static v2] lane serving:
static2_…w` 줄이 서빙 증명이다.

**확인 실행(2026-09-06 02:19~02:20, 스탬프 없음, 각각 별도 프로세스)**: `w`(f2,g3) U=40 727.2
→ **677.7 µs(+6.8%, 209 GB/s)**, U=64 +6.9%; `w,g2`(FC2 2 스테이지) 726.8 → **669.8(+7.8%, 211)**,
U=64 +8.7%. 세 실행(657/678/670)의 스톡은 727~728 로 고정인데 v3 쪽이 ±1.5% 흔들린다 — v3 의
값은 **+7~10%** 대역으로 적는다. 세 형상 수치 PASS 는 세 실행 모두 동일. FC2 스테이지 2 와 3 은
잡음 안(g2 가 1% 앞) → 브래킷은 `w`(f2,g3) 로 간다.

### 11. 브래킷 (자율, 2026-09-06 02:06 무장 → 플릿이 조용해지면 실행)

플릿을 피어 세션 둘(체인 27 GPTQ · 체인 24 fusion)과 나눠 쓰므로 **격리 배포**: srv2 클론
`~/stkernel-b12x`(이 브랜치 + 로컬 커밋 2개 — 프로필 `PROFILE_OVERLAY_DIR=~/overlays/glm53-b12x`,
런처 `CACHE_HOST_PATH=~/glm53-cache-b12x`·`LOG_HOST_DIR=~/glm53-logs-b12x`), 하니스
`~/glm53-logs/ab-lever-b12x.sh`(REPO=클론이라 프로필의 `_vllm_keys` 가 노브를 나른다), 체인
`lever-chain-b12x.sh`: 레버·브래킷·프로브 프로세스가 5 분 조용하면 4 노드 캐시 `cp -a` → 배포 →
스탬프 선기록(런처의 `.overlay-sha` 는 manifest sha 라 배포 커밋이 다르면 torch.compile 캐시를
지운다; 이 변경은 커스텀 op 안이라 AOT 가 유효) → B12XBASE1 → B12XCAND(`…STATIC_V2=w`, 한국어
동반) → B12XBASE2. 공용 배포·캐시·로그를 쓰면 피어의 다음 부팅이 콜드가 되거나 오버레이가
되돌아간다 — 그래서 격리. 결과는 `lever-chain-b12x.out`·`bracket-lever.jsonl`.


## ★★★33차 — W4 팩 정확도 레버 4종: 정확 활성 스케일·행별 shift·GPTQ·저랭크 보정 (2026-09-05 오후, 브랜치 ostcode/glm-flash-kernel-improvement-5a65a8)

**운영자 지시:** "기존 mk_gemm 에서 속도는 손상 없이 정확도는 올릴 방법" → 레버 3개 + 저랭크 오차 보정을 구현. 동기는 §14 의 한국어 동률 뒤집힘: 기본 스택의 수치 편차 본체가 W4 e2m1 GEMM 레인(실가중치 출력 오차 q_proj 0.083 / down_proj 0.079 rel; stock fp8 쌍 0.02 급)이라 편차를 줄이면 동률이 덜 뒤집힌다는 가설.

### §1 레버와 비용

| 레버 | 무엇 | 바이트/커널 | 노브 |
|---|---|---|---|
| 1 정확 활성 스케일 | 프롤로그의 행·128-k 스케일을 pow2 `2^frexp_exp(amax/448)` 에서 `amax*fp32(1/448)` 로. pow2 는 e4m3 가수 3비트 중 최대 1비트를 행마다 버렸다 | 바이트 동일, 프롤로그 rcp 하나 | 무조건 |
| 3 행별 shift | 그룹 지수를 바이트 덧셈 전개 창(11옥타브) 안에 넣는 shift 를 텐서당 → 행당(행의 지수 중앙값), 출력 열에서 되돌림(`MKPack.rgs`) | 바이트 동일, 에필로그 열당 곱 하나 | `VLLM_GLM53_MK_PACK_ROWSHIFT=1` |
| 2 GPTQ | 교정 부팅(`MK_CALIB=1` + `bench/mk-calib-run.py`)이 랭크별 입력 헤시안을 덤프하면 다음 부팅의 팩이 열 단위 오차 피드백 반올림(OBQ/GPTQ, 블록 128, 그룹 스케일은 갱신된 가중치로 재선택) | 바이트·커널 동일, 팩 시간(랭크당 1회, 캐시) | `VLLM_GLM53_MK_PACK_GPTQ=1` |
| 4 저랭크 보정 | `E=W−deq(Q)`, 활성 가중 SVD(`E·S`, S=채널 rms) 의 rank r: `x W_qᵀ + (x Bᵀ) Aᵀ`. v2 커널이 그리드 앞 16개 리듀서 블록으로 `t=xBᵀ` 를 만들고 최종 스토어가 `t[row]·A[col]` 을 더함(플래그 대기, 마지막 스토어가 재무장 → 그래프 리플레이 안전) | 바이트 +r·(n+k)·2 B (r=16: +1~2%), 리듀서 블록 16개 | `VLLM_GLM53_MK_PACK_LORC=r` |

### §2 정확 게이트 사고와 수정

첫 빌드의 정확 게이트가 전 레인 4.22e-3·over-ulp 1526 으로 죽었다. 행별 shift 를 꺼도 같은 수 → 레버 1. 원인: 커널은 `amax / 448.0f`(IEEE 나눗셈), torch 쌍둥이는 `amax / 448.0` 인데 **torch 는 텐서÷스칼라를 스칼라의 fp32 역수 곱으로 계산**해 일부 행에서 스케일이 1 ulp 어긋나고 e4m3 반올림 경계의 원소들이 뒤집혔다. 양쪽을 `amax * fp32(1/448)` 로 맞추자 v1 global/local 3.92e-8, v2 전 슬라이스 ≤1.0e-5, over-ulp 0. **규칙: 커널↔쌍둥이 정확 게이트는 연산의 형태(나눗셈/역수곱, satfinite/clamp, 바닥값)까지 같아야 한다.** 팩 캐시 키도 md5 만으로는 부족했다(같은 시드·원소 수의 [2048×4096]/[4096×2048] 충돌) → 형상·dtype 포함.

### §3 오프라인 정확도 표 (bf16 진실 대비, `probes/mk_pack_accuracy.py`)

벤치의 rel_err 열은 stock fp8 쌍과의 차이(양자화 팔 둘의 불일치)라 레버 순위를 못 매긴다 — 새 프로브가 x_bf16·W_bf16ᵀ(fp32) 대비 각 팔의 오차와 커널↔쌍둥이 간격, v2 런치 시간을 같이 낸다. 실가중치(체크포인트 1층 q/k/v/o·공유 전문가), 활성은 채널별 로그정규 스케일 + 이상치 채널 8개(실 헤시안은 교정 부팅에서).

첫 표(합성 활성, 평가 분포가 교정 분포와 달랐던 판 — 아래 정정)에서 확정된 것 셋:

- **레버 1·3 은 총오차를 안 움직인다.** 실가중치 1층 q/k/v(= KDA in_proj 성분) rtn/ten 8.336e-2 vs rtn/row 8.336e-2, 랜덤 형상 4개도 동일. 출력 오차는 e2m1 16-그룹의 가중치 오차(~8.3%)가 지배하고, 활성 e4m3 의 반 비트와 1.5% 클램프 그룹은 그 밑에 묻힌다. 둘 다 비용 0 이라 두지만 이득도 0 — 정직한 판정.
- **커널↔쌍둥이 간격 1.66e-3 은 bf16 출력 반올림**(쌍둥이 fp32 vs 커널 bf16; 정확 픽스처는 bf16 반올림 뒤 비교라 1e-5). 두 번째 표부터 쌍둥이도 bf16 으로 반올림해 비교.
- **저랭크 보정은 (a) [1024×4096] 형상에서 커널 결과가 쌍둥이와 2~10% 어긋났고(다른 세 형상은 정상) (b) v2 런치 +45~70 µs.** (a) 는 ksr=8 fold 경로에서만 → 별도 진단(`probes/mk_lorc_diag.py`: ksr 강제·m 8/32·연속 런치). (b) 는 리듀서 블록의 smem 행 피치 256 B → 32-way 뱅크 충돌(패딩으로 수정), 여전히 크면 mma 리듀서.
- **GPTQ 가 RTN 보다 12% 나쁘게 읽힌 것은 프로브 설계 오류**: 헤시안은 저랭크 상관을 포함한 생성기로, 평가 x 는 상관 없는 생성기로 뽑아 분포가 달랐다. GPTQ 는 헤시안 분포에서의 출력 오차를 최소화하므로 다른 분포에선 보상이 오차가 된다. 같은 생성기의 held-out 표본(256행)으로 다시 잰다.

**정정된 표(같은 생성기의 held-out 256행, bf16 진실 대비 상대오차; 활성 = 채널별 로그정규 스케일 + 이상치 8채널 + 약한 저랭크 상관, 합성 헤시안 4096토큰; v2 µs 는 m=32, 워커가 GPU 를 쥔 srv4 라 ±5% 잡음):**

| 가중치 | stock fp8 | RTN(=행별) | GPTQ | GPTQ+저랭크16 | GPTQ+저랭크32 | RTN+저랭크32 |
|---|---|---|---|---|---|---|
| 1층 q_proj [실가중치] | 2.66e-2 | 8.33e-2 | **7.27e-2 (−13%)** | 5.28e-2 (−37%) | **4.96e-2 (−40%)** | 8.24e-2 (−1%) |
| 1층 k_proj | 2.66e-2 | 8.30e-2 | 7.22e-2 | 5.25e-2 | 4.94e-2 | 8.23e-2 |
| 1층 v_proj | 2.66e-2 | 8.26e-2 | 7.18e-2 | 5.23e-2 | 4.92e-2 | 8.16e-2 |
| randn 6416×4096 | 3.75e-2 | 8.33e-2 | 7.31e-2 | 5.29e-2 | 4.97e-2 | 8.24e-2 |
| randn 4096×512 (공유 전문가 down 형상) | 3.73e-2 | 8.36e-2 | **6.56e-2 (−22%)** | 3.19e-2 | **2.81e-2 (−66%)** | 7.91e-2 |
| randn 4096×4096 | 3.76e-2 | 8.36e-2 | 7.33e-2 | 5.30e-2 | 4.98e-2 | 8.24e-2 |

읽기: (1) GPTQ 단독 −13%(k=4096) / −22%(k=512), 합성 헤시안 기준 — 실 헤시안이면 더 좋을 수도, 나쁠 수도(교정 부팅이 답). (2) 저랭크 보정은 **GPTQ 위에서만** 크게 먹힌다(−37~−40%; k=512 는 −66%): RTN 의 잔차는 백색이라 rank 32 가 잡는 에너지가 2~3% 뿐이고, GPTQ 의 잔차는 헤시안이 무시하는 방향에 몰려 있어 활성 가중 SVD 가 그 방향을 되찾는다. (3) 레버 1·3 은 0 (표의 RTN 열 = 행별 shift 열, 소수점 4자리까지 같음). (4) 커널↔쌍둥이(bf16 끼리) 간격: 보정 없는 팔 ≤6e-5, 보정 팔 1.8e-3 — 후자는 커널이 보정항을 양자화 전 x 로 계산하기 때문(더 정확한 쪽)이며 쌍둥이 `mk_pack_twin` 이 이를 모델한 뒤 재측정.

비용(정정 전 커널, 같은 표): v2 런치 q_proj 형상 RTN 109 µs → 저랭크16 171 → 저랭크32 222 µs. 보정 없는 팔도 기준선 대비 8~15% 느려져 있었다(리듀서·대기 코드가 같은 커널 안에 살아 레지스터/정적 smem 을 먹음) → v2 커널을 `<RQ, LR>` 로 템플릿 분리(LR=false 는 보정 코드 0).

**저랭크 비용의 정체 (스탬프 타임라인, `mk_lorc_diag.py` + `VLLM_GLM53_MK_PHASE_TS=1`):** 리듀서 블록 16개는 0 µs 에 디스패치·PDL 통과, 9~15 µs 에 t 발행 완료 — 병목이 아니다. GEMM 블록의 루프는 [32×1024×4096] 에서 12/16 µs(중앙/최대)에 끝나는데 종료는 16/**69** µs: 최종 스토어를 하는 블록의 에필로그가 30~50 µs. 원인은 스토어마다 A 행(r 개 bf16)을 L2 에서 종속 로드하는 `lr_term` — 블록당 4096 원소 × 8~16 로드가 지연 시간에 묶였다. 수정: 플래그 대기 뒤 이 타일의 A 128행과 t 를 smem 에 한 번에 스테이징(피치 34 = 뱅크 충돌 0)하고 보정 타일 corr[32][128] 을 협력 계산, 스토어는 smem 읽기 하나. **재측정:** [32×1024×4096] 종료 최대 69 → 32 µs, [32×6416×4096] 스팬 126 → 82 µs; v2 런치(합성 활성, m=32): 6416×4096 RTN 104 → 저랭크16 116 / 32 118 µs(+12/+14), 1024×4096 50 → 61 / 70(+11/+19), 4096×512 45 → 48 / 52(+3/+6). 정확도·정확 게이트 불변(커널↔쌍둥이 ≤7.5e-5). 남은 비용 = 리듀서 블록 16개의 슬롯 점유(10~15 µs 동안)와 에필로그 스테이징 ~5 µs. 랭크당 스텝의 W4 런치 211개에 적용하면 **≈ +1.5 ms/스텝(+2.7%)** — "속도 손상 없이" 를 만족하지 못하므로 저랭크는 브래킷 팔로만 재고, 기본값 후보는 GPTQ(런타임 비용 0).

**GPTQ 솔브 비용(GB10, 프로브 컨테이너):** [6416×4096] 4.4 s, [4096×512] 0.10 s; RTN 은 0.3 s / 0.01 s. 랭크당 180 + 31 선형 ≈ 5~7 분 — 첫 부팅 1회(팩 캐시), 이후 부팅은 캐시 로드.

### §4 서빙 — 체인24(glmfix)

**교정 부팅(16:01, `MK_CALIB=1`, RTN 팩·행별 shift·정확 스케일):** 405 s 에 up, 한국어 1/16(프롬프트 0 병기 2자, 기준 패턴). 교정 트래픽 1차(36 프롬프트 × 4라운드, 64토큰 완성)는 21 분에 프롬프트 토큰 12,164 — **디코드 스텝은 그래프 리플레이라 관찰기가 못 본다**: 교정 토큰 = 프리필 행. 긴 프롬프트 14개(풀을 섞어 이어 붙인 ~2.5K 토큰, `max_tokens=1`)로 2 분 만에 32,948 행 → **4 랭크 모두 180 헤시안 덤프(7.0 GB/랭크, `/cache/mkcalib/rank<r>/`)**. 드래프터의 35 팩은 덤프 없음(불투명 커스텀 op 경로는 팩 튜플을 매 호출 새로 만들어 `id(pack)` 키가 안 맞음) → 드래프터는 RTN 유지(온도 0 에선 타깃 출력에 무관, 수용률만 영향). 사고: 배포 ABORT 가 파이프에 가려져 체인이 옛 오버레이로 한 번 부팅(15:44~15:51, 프로덕션 복귀 뒤 재배포) — 규칙은 [[stkernel-fleet-ops]] 메모리에.

**GPTQ 팩 부팅(체인24 PACKGPTQ, 16:45) 사망 → 원인 둘, 수정:** 팩 줄 `rtn=0 gptq=45`(타깃 180 중 45), 드래프터 `gptq=48 cached=24`, 워커가 드래프터 forward(AOT 그래프)에서 `'NoneType' object is not subscriptable` 로 죽음. (1) **헤시안이 NaN**: 관찰기가 배치의 패딩 행(토큰 수를 그래프 크기로 올린 나머지, 초기화 안 된 메모리)까지 더해 k≥2048 선형 138개의 H 가 전부 NaN → cholesky 실패 → 팩 없음(attach 의 except 가 삼킴) → 스톡 쌍으로 조용히 후퇴; k=512 42개만 유한. 오프라인 재현 `probes/mk_gptq_repro.py`(실 덤프 `in_proj` H: finite=False, diag nan). (2) **드래프터가 타깃의 덤프를 읽음**: 덤프가 맨 모듈 이름(`model.layers.N.mlp.down_proj`)이라 드래프터의 같은 이름이 타깃 H 를 집어 k 가 맞으면 엉뚱한 GPTQ, 안 맞으면 실패 → 일부 팩 None → AOT 그래프가 None 을 첨자. 수정(95fd6c7): 관찰기는 유한 행만, 덤프는 `<모델클래스>/<이름>.pt` 로 이름공간 분리 + k 검사, attach 실패는 로그, GPTQ 실패는 패커 안에서 RTN 후퇴(선형이 팩을 잃지 않음). 체인이 PROD 팔 없이 끝나 플릿이 내려감. **복구 부팅 셋이 더 죽었다(srv2 시계 19:16 PRODRTN, 21:40 PRODRTN2, 21:46 PRODRTN3·4 중단):** 팩 줄 `rtn=0 cached=45` — 교정 부팅이 써 둔 **팩 캐시**(컨테이너가 root 로 쓴 파일)에서 큰 팩 135개의 로드가 실패해 팩 없음 → 드래프터 AOT 그래프 사망(같은 경로). 컨테이너에서 같은 파일은 정상 로드되므로 서빙 프로세스의 로드 시점 조건(메모리 압박 추정)이며 새 오버레이가 사유를 로그로 남긴다. 레버 env(`MK_PACK_GPTQ=0`, `MK_PACK_CACHE=off`)는 컨테이너에 닿지 않았다(팩 줄 `gptq=1`, 캐시 사용) — `ct_load_profile` 은 호출자 값을 다시 적용하므로 원인 조사 중; 체인25 의 후퇴 부팅은 `EXTRA_ENV=` 통과 경로를 쓴다. 조치: 4노드 `mkpacks` 를 docker(root)로 삭제, 고친 오버레이(fed51b0) 배포, 체인25(교정 부팅 → GPTQ 부팅 전체 다리 + 한국어 3회 → 실패 시 RTN 프로덕션, 항상 UP 으로 끝) 21:48 시작. 플릿 다운 시간: 16:56~(체인25 교정 부팅 up 까지) ≈ 5시간 — 대기 스크립트가 '정상'만 기다려 두 번째 사망을 2시간 놓친 탓. 규칙: 대기는 ABORT 도 본다. **체인25 교정 부팅(CALIB2, 21:48)도 사망:** 새 유한 행 필터(`isfinite(...).all(1)` 불리언 인덱싱)가 **디코드 그래프 캡처 중** 디바이스 동기화를 해 `operation not permitted when stream is capturing` — 첫 교정 부팅은 addmm 만 해서 통과했었다. 수정: 캡처 중엔 관찰기 건너뜀(캡처 스텝은 더미 행 리플레이라 볼 것도 없음). 체인은 PACKGPTQ2(덤프 없음 → RTN, 프로덕션 복귀)로 진행 → **21:59 up(225 s), 팩 캐시 로드 정상(`cached=180`; 새 패커가 쓴 파일은 로드된다 — 옛 오버레이가 쓴 파일이 왜 실패했는지는 미해명, 로그가 남는 새 코드로 재발 시 판독)**. 그런데 디코드 다리: **16.7~17.3 tok/s at 17.4 step/s, raw acc 0.0%, accepted/draft 0.000** — 드래프터가 쓰레기를 뽑는다. 원인: 드래프터 선형은 불투명 커스텀 op(`_mk_or_fp8_dense_gemm_op`) 경로인데 그 인자 목록은 옛 3필드 팩(wq4·ws4·wgs)만 나르므로 **행별 shift(rgs)가 떨어져 드래프터 출력이 행마다 2^shift 배 틀렸다**. 타깃 경로(비불투명)는 rgs 를 넘기므로 프리필 2,621 → 2,722 tok/s·품질은 정상. 창 판정(step/s)만 봤다면 통과했을 함정 — **tok/s·raw acc 열을 항상 같이 본다.** 수정(b4908ee): 불투명 경로의 팩은 per-tensor(wgs)로 만든다. 체인26(교정 → GPTQ 부팅 전체 다리 + 한국어 3회) 22:03 시작. **CALIB3(22:04)**: 375 s up, 타깃 팩 캐시 로드 180, 드래프터 per-tensor 35 새로 빌드; 한국어 2/16(둘 다 프롬프트 0 병기 2자·6자); 프리필 전용 긴 프롬프트 16개로 **23 s 에 50,696 토큰 → 4 랭크 215 헤시안(8.8 GB/랭크, NaN 거부 0)**. **PACKGPTQ3(22:14)**: 팩 단계에서 `GPTQ failed (_LinAlgError: leading minor of order 651 not positive-definite)` 9건 이상 → RTN 후퇴(부팅은 산다). 실 헤시안은 fp32 addmm 반올림 수준에서 부정부호 → 대칭화 + float64 분해 + 댐핑 사다리(1→10→100%)로 수정(7d78203), 실 덤프 2개로 재현·통과. 오프라인 정확도는 **평가 입력의 분포에 민감**: 덤프의 대각 스케일만 쓴 입력(상관 없음)에선 GPTQ 가 RTN 보다 2.5~13% 나쁘게 읽히고(GPTQ 가 보상하는 상관을 입력이 갖지 않으면 보상이 오차가 됨), 덤프의 전체 2차 모멘트 x~N(0,H/ntok) 로 뽑은 입력 = GPTQ 의 목적함수 그 자체 — 로 재평가:

| 실 덤프(rank 0, 17,189 행) | 입력 = 덤프의 전체 공분산 x~N(0,H/ntok) | 입력 = 대각만(상관 없음) |
|---|---|---|
| layers.2.mlp.down_proj [4096×3072] | RTN 8.56e-2 → **GPTQ 2.24e-2 (−74%)** | RTN 8.44e-2 → GPTQ 9.44e-2 |
| layers.3.self_attn.fused_qkv_a_proj [2048×4096] | RTN 7.16e-2 → **GPTQ 2.20e-2 (−69%)** | RTN 8.54e-2 → GPTQ 8.76e-2 |

실 활성은 강하게 상관돼 있다(fused_qkv_a 의 H 고유값 4096개 중 736개가 ≈0: 입력이 훨씬 낮은 차원의 부분공간에 산다) — GPTQ 는 그 구조를 쓰므로 실 공분산에선 출력 오차를 3.5배 줄여 **stock fp8 쌍(2.7e-2)의 급**에 닿고, 상관을 뺀 입력에선 보상이 오차가 된다. 서빙 입력은 전자다. 판정은 브래킷(디코드·품질·한국어)이 최종. 또 H 는 fp32 누적 반올림으로 부정부호(음의 고유값 18/3072, 736/4096) → 패커의 대칭화·float64·댐핑 사다리가 필요했던 이유.

| 팔 | 노브 | 디코드 창 (rep) | tok/s · raw acc | 프리필 | 수용률·품질 | 한국어 | 비고 |
|---|---|---|---|---|---|---|---|
| CALIB3 (체인26, 22:04) | `MK_CALIB=1`, RTN | — | — | — | — | 2/16 (프롬프트 0 병기 2자·6자) | 215 헤시안/랭크 |
| **PACKGPTQ3 (체인26, 22:14) = 프로덕션 후보** | 기본값(ROWSHIFT·GPTQ), 덤프 있음: 타깃 gptq 171 + RTN 후퇴 9, 드래프터 gptq 35 | **18.4·18.4·17.9** (rep 17.2·18.1·17.5) | 37.3·38.9·42.2 · 16.8/16.4/20.2% | 2,608 / **2,731** | **3.91 tok/step** (accepted/draft 2.907), **9/9** | **0/16 · 0/16 · 0/16 = 0/48** | 부팅 930 s(팩 솔브 695 s, 1회·캐시). 창·프리필은 기본값 눈금, tok/step 은 기본값 3.33~3.60 보다 위(드래프터 GPTQ; 프로파일 편차 주의). **한국어 48표본 0 은 캠페인 최초** — 기본값 부팅 16회차 중 5회 1/16, 세트 켬 8회차 중 6회 더러움과 대비 |
| **PACKGPTQ4 (체인27, 01:49 KST) = 현재 프로덕션** | 견고한 솔브(v4 팩): 타깃 gptq **180/180**, 드래프터 35/35, 실패 0(일부 댐핑 10%) | **18.0·18.5·17.9** (rep 16.4·18.2·17.6) | 45.4·39.7·45.7 · 25.3/16.9/22.8% | — | — | **0/16 · 0/16 · 0/16 = 0/48** | 부팅 1200 s(팩 솔브 787 s, 1회·캐시). 두 부팅 합쳐 **한국어 96표본 0** |
| 저랭크 16 | `MK_PACK_LORC=16` | (기각) | | | | | 실 헤시안·실 공분산에서 GPTQ 위 이득 0.2%(아래 표) vs +1.5 ms/스텝 |

**판정: GPTQ 팩을 프로덕션 기본값으로 확정(프로필은 이미 `MK_PACK_GPTQ=1`; 서빙 증명 = 팩 줄 `gptq=180 gptq_failed=0`). 속도 동일, tok/step 상승, 품질 9/9, 한국어 96표본 0.**

**읽기(운영자 질문 "오차를 줄였는데 왜 한국어 오류가 느나"에 대한 답):** 2/16 은 오차를 줄이기 전(RTN 교정 부팅)의 표본이었고, 오차를 실제로 줄인 GPTQ 부팅(실 공분산 기준 출력 오차 3~4배 감소, stock fp8 급)에선 같은 판정기·같은 프롬프트로 **48표본 0**. 근접 동률 자리를 뒤집던 섭동이 마진 아래로 내려간 것과 일치한다(확정은 로짓 마진 측정으로).

**저랭크 보정의 실 수치(2026-09-06 04:00, 같은 실 덤프·실 공분산 입력):**

| 실 덤프 | RTN | GPTQ | GPTQ+저랭크16 | GPTQ+저랭크32 | RTN+저랭크16(평범 SVD) |
|---|---|---|---|---|---|
| layers.2.mlp.down_proj | 8.56e-2 | 2.243e-2 | 2.239e-2 | 2.230e-2 (−0.6%) | 8.51e-2 |
| layers.3.self_attn.fused_qkv_a_proj | 7.16e-2 | 2.199e-2 | 2.199e-2 | 2.199e-2 (−0.04%) | 7.13e-2 |

합성 표의 −27~−40% 는 합성 분포의 산물이었다. 실 활성에선 GPTQ 가 입력 부분공간 방향의 오차를 이미 지워 남는 잔차가 입력이 건드리지 않는 방향에 있고, 저랭크 보정은 그 잔차를 담아도 출력에 안 나타난다. **판정: 저랭크 보정 기각** — 이득 <1% 에 +1.5 ms/스텝. 코드(`<RQ, LR>` 인스턴스·리듀서·패커 SVD)는 기본 0 으로 남아 있으며 제거는 운영자 결정.

**GPTQ 가 다시 여는 후보 셋과 판정(2026-09-06 04:30, 운영자):** 타깃 헤드 W4(−0.4 ms/스텝, 서빙 로짓 변경), 드래프트 헤드 W4(−0.4 ms/스텝, 수용률 게이트), nvfp4 프리필 W4A4(프리필 +4%, 활성 4비트 오차는 GPTQ 가 못 잡음) — 합쳐 디코드 −1.5%·프리필 +4% 로 **"이익이 너무 적어 굳이" → 전부 접음.** GPTQ 의 가치는 속도가 아니라 품질(한국어 0/96)이었다.

**운영 주의:** GPTQ 팩은 각 노드 `/cache/mkcalib/rank<r>/<모델클래스>/` 의 헤시안 덤프와 `/cache/mkpacks/rank<r>/` 의 팩 캐시(둘 다 컨테이너가 root 로 씀)에 의존한다. 캐시를 지우거나 새 노드면 팩 줄이 `gptq=0` 으로 돌아오고 조용히 RTN 이 된다 — **부팅 로그의 `megakernel packs:` 줄에서 `gptq=180 gptq_failed=0` 을 확인하는 것이 서빙 증명**. 체크포인트가 바뀌면 교정 부팅(`MK_CALIB=1` + `bench/mk-calib-run.py --long 16`)을 한 번 다시 한다.## ★★★34차 — 메가커널 모듈·노드 융합: 프로덕션 스텝의 첫 노드 인구조사, 남은 자리 넷, 그리고 드래프터 앞의 호스트 갭 (2026-09-05 오후, 무부팅 + 프로파일 캡처 1회)

운영자 "glm 5.3 flash 메가커널 모듈 및 노드 융합". 메가커널 캠페인이 세그먼트(MHC·GEMM·KDA·MLA)를
접수한 뒤 스텝에 무엇이 남았는지를 **노드(그래프 안 커널 발사) 단위**로 읽고, 접을 수 있는 자리를
접었다. 부팅은 하지 않았다 — 체인22 PRODSET 부팅(다른 세션, v2 레인 + prep-fused + MLA + 드래프터
W4 + PDL + KDA·LOCALQ·ARPF)이 유휴일 때 `bench/profile-step.py decode:600` 으로 rank 0·3 트레이스를
한 번 캡처했을 뿐이다(14:14~14:15, srv2 `~/vllm-prof/…1788585310358007760`, srv4 rank 3 사본).

### 1. 도구 — 노드 뷰와 앵커 버그

`tools/trace_step_nodes.py`(신규, 스트리밍): 커널 수가 최빈값인 "깨끗한 스텝"의 중앙값으로 범주
구성·커널별 표(발/스텝, 중앙 µs, 스트림, 소유)·중앙 스텝의 노드 열(`--dump`)·창 안 **전 범주**
이벤트(`--gap`: gpu_memcpy·cuda_runtime·…)를 낸다. RSS 수십 MB 라 부팅 중 노드에서도 안전하다.
버그 둘 고침: (1) `trace_common.cut_steps` 가 "처음 두 번 이상 맞는 앵커"를 골라 prep-fused 부팅
(비균일 스텝에만 스톡 `_gather_block_tables_kernel` 이 남는다)의 245 스텝을 3.6 초짜리 4 "스텝"으로
잘랐다 → 가장 많이 나오는 앵커(동률이면 스텝 시작 앵커). (2) `category()/OURS` 가 v2 레인
`mk_gemm2_kernel`·`mk_smlp_kernel`·원패스·듀얼 게이트를 몰라 "기타/이미지"로 흘렸다.

### 2. 스텝 인구조사 (rank 0, 239 깨끗한 스텝 중앙값, 프로파일러 아래)

| | 09-04 무장 | **09-05 프로덕션(PRODSET)** |
|---|---|---|
| 노드/스텝 | 1,582 | **1,166** |
| 스텝 / 유휴(합집합) | 69.25 / 8.18 ms | **55.8 / 1.20 ms** |
| MoE 전문가 | 42 발 32.5 | 42 발 29.4 |
| AR k_oneshot | 102 발 5.28 | 102 발 6.7 (p50 64) |
| 밀집 GEMM | mk_gemm 185 발 14.13 | **mk_gemm2 147 발 3.52** (KDA 레인이 in/o_proj 68 발 흡수) |
| MK KDA / MHC / MLA | — / 89 발 2.03 / 0 | 34 발 5.77 / 89 발 1.73 / **11 발 0.42** |
| bf16 cutlass·cublas + head gate | 201 발 6.39 | **103 발 3.34** |
| MoE 글루 / elementwise | 136 발 1.20 / 481 발 1.00 | 136 발 1.21 / 318 발 0.68 |

보충 분해 5 의 "다음 캡처 3줄"은 셋 다 확인됐다(mk_mla 11, cutlass 103, mk_gemm2 147). 프로파일러의
왜곡 둘도 확정: 스텝 시작의 타깃 그래프 `cudaGraphLaunch` 가 rank 0 에서 2.8 ms(CUPTI 버퍼 요청
1.6 ms 포함, rank 3 은 0.4) — 스텝 시작 갭은 프로파일러 값이고, 31차의 "그래프당 7~10 µs" 가 실값.

### 3. 남은 자리 — 노드 열로 읽은 것 (STEP_KERNEL_MAP 보충 분해 6)

| 자리 | 노드 | 프로파일러 µs/스텝 | 정체 |
|---|---|---|---|
| **드래프터 앞 호스트 갭** | 0 | **rank 0 420 / rank 3 640**, rank 0 의 드래프터 첫 AR 245 = rank 3 대기 | `precompute_and_store_context_kv` 뒤 `Memcpy DtoH (Device→Pageable)` + `cudaStreamSynchronize` → 파이썬 290~460 µs → 드래프터 `cudaGraphLaunch`. `DFlashSpeculator.propose` 의 `_build_draft_attn_metadata`: FlashInfer 빌더가 비인과 어텐션이라 `seq_lens.cpu()` 를 읽고, FULL 재생 분기(`run_fullgraph`)는 그 dict 를 **읽지 않는다** |
| **MLA 층 bf16 저랭크 사영** | 44 | **2,200** (q_b 67·wq_b 66·q·W_UK 26·W_UV 22 × 11) | 12.6 MB 짜리 둘은 DRAM 속도(190 GB/s) — 바이트가 답이고, W4 레인이면 층당 −100 µs(EXP-4). 인덱서·쿼리 정밀도 강등 = 운영자 결정 |
| **인덱서 head gate `gemmSN`** | 11 | **950** (발당 86) | EXP-9 split-K(offline 89.5 → 12.7) 가 구현돼 있고 판정된 적 없다(PREP3 사망은 플릿 행) |
| 인덱서 top-k 글루 | 121 | ~260 | 층마다 seq_len 2발 + tail 강제 8발 + 복사 1발 — 정수 인덱스 산술 |
| 드래프터 fc 5 청크 + 글루 | 20 | ~390 | EXP-15 early-fc 의 자리 |
| MoE aux 체인 gate_partial 20 → topk 3.8 → MoE | 84 | ~1,000 (임계경로) | gate 가 공유 gate_up 과 겹쳐 20~23(단독 13.6); 게이트+topk 융합은 31차에서 기각 |
| 드래프터 K/V 문맥 사영 bf16 101 µs | 1 | 101 | 레인 밖 "1 of 31" (`_build_fused_kv_buffers`) |
| 마지막 norm 의 aten 9발 + memcpy32 6발 | 15 | ~50 | 사소 |

### 4. 접은 것 (이 PR, 노브 둘 기본 0)

1. **`glm53_drafter_prep`**(신규 모듈, `VLLM_GLM53_DRAFTER_PREP` = time/shadow/1): `DFlashSpeculator.
   _build_draft_attn_metadata` 를 감싸 드래프터의 자체 cudagraph dispatch 가 FULL 이면 형상당 1회 빌드한
   dict 를 되돌려 호스트 빌드(와 그 앞의 DtoH 동기)를 생략한다. `time` 은 스톡 빌드의 호스트 µs 를 찍고
   (프로파일러 없는 상한), `shadow` 는 캐시 dict 를 필드별로 대조(GPU 텐서 동일성·형상·dtype, 호스트
   스칼라 동등; 스텝마다 바뀌는 `max_seq_len` 류는 따로 셈). 이미지 3 파일 preimage 고정, 예외 시 부팅 내내
   스톡. 수치 불변(같은 그래프가 같은 버퍼를 재생).
2. **인덱서 tail-select 융합**(`glm53_kpool_tail_select`, `VLLM_GLM53_INDEXER_DECODE_FUSED=1`):
   `_glm53_indexer_tail_select_kernel` 한 발이 `_decode_topk_seq_lens` + `_force_tail_pool_into_logits`(10 발)를
   대신하고, `expand_pools_and_append_tail_into` 가 확장 결과를 영구 top-k 버퍼에 직접 쓴다(복사 −1). 균일
   레이아웃만; 정수 산술 + 같은 finfo.max 라 비트 동일이 계약 — `probes/indexer_decode_fused_check.py`
   (`run_indexer_fused_check.sh`, 프레시 컨테이너)가 게이트이고 리플레이 µs 를 같이 찍는다. 층당 −11 노드.
   **프로브 실측(srv4, 플릿 유휴 창, 40/40 비트 동일 PASS, 그래프 리플레이 200회 중앙값)**: 층당 스톡 체인
   24.7 µs → 융합 **5.2 µs**(C=1, 8행·1,250 풀), C=4 는 24.6 → 6.2 — **−0.21 ms/스텝**(11 층). 프로파일러
   없는 값이라 §3 의 "~260 µs" 추정과 같은 크기다.
3. RUNBOOK EXP-24: 위 둘 + 대기 중이던 EXP-9·15·20·SMLP2 를 한 팔(FUSION3)로. 상한(프로파일러 값)
   −2.0~2.6 ms/스텝(3.5~4.5%), 절반이 split-K. 수치가 바뀌는 축은 EXP-9 하나.

### 5. 상한 정직성

프로파일러 아래 값은 호스트를 부풀린다 — 드래프터 갭 0.42/0.64 ms 는 상한이고, 실값은 `DRAFTER_PREP=time`
부팅의 `[drafter-prep] stock build host us` 줄이 정한다. 인덱서 글루 0.26 ms 도 CUPTI 의 소형 커널
부풀림(발당 ~2 µs)이 섞인 값이라 실값은 그 절반 근처일 것이다. 앞 회차의 교훈("상한은 목표와 같은
측정 형태로") 그대로: 이 회차의 채택 판정은 브래킷 창 통계다.

### 6. 모듈 접기 (운영자 "디폴트화된 모듈들 4~5개씩을 이득은 유지하면서 1개씩으로 묶어버려")

운영자가 짚은 대로 이 회차의 첫 PR 은 리포의 모듈(27 → 28)·커널 종류·노브를 늘렸다. 줄어드는 것은
노브를 켠 스텝의 발사 수뿐이다. 그래서 glm53 전용 오버레이 모듈 25개를 다섯 묶음으로 접었다 —
`glm53_model`(6) · `glm53_kernels`(5) · `glm53_drafter`(6) · `glm53_moe`(3) · `glm53_runtime`(5);
dsv4 와 공유하는 `tp_oneshot_ar`·`moe_gate_sm121`·`glm53_megakernel` 은 그대로. 프로필 `MODULES` 는
28 → **8** 단어. 접은 것은 디렉터리·`manifest.tsv`·`requires`·README 이고, 매니페스트 행·베이스 계약·소스
파일·노브·기본값은 손대지 않았다: 합성한 `build/glm53/` 은 **파일 바이트 동일**(예외 하나: `glm53_megakernel.cu` 주석의 옛 모듈 경로 한 줄), 매니페스트
행 집합 동일, 행 순서만 바뀜(그래서 다음 배포의 `.overlay-sha` 가 바뀌어 컴파일 캐시가 한 번 비워진다 — 이 PR 은 어차피
서빙 파일을 바꾸므로 그 한 번은 원래 치른다). 묶음 README 는 옛 모듈마다 한 절씩 원문을 보존하고, 프로필의
`MODULES` 위 주석에 옛→새 대응표를 뒀다. 테스트 스위트의 모듈별 검사는 묶음 안의 행·파일을 찾도록 고쳤다
(정확 행 비교 4건은 포함 비교로).

### 7. 운영자 "1~3,6,7" (2026-09-05 오후 2)

- **3번(MoE aux 체인 순서 조정)은 접었다.** 이미지 러너(`fused_moe/runner/shared_experts.py`)를 읽으니 공유 전문가
  쌍은 aux 스트림에서 게이트 앞 동기점에만 의존한다 — 그래프 재생에서 쌍과 게이트가 동시에 준비되고(사실상 "쌍 먼저"),
  MoE 커널은 topk 뒤에 뜬다. 오늘 서빙의 노출은 게이트 경합 7~9 µs + MoE 시작 지연 ~2.5 µs = 층당 ~10 µs 로, 30차
  프로브의 두 순서(MoE 먼저 18.6 / 쌍 먼저 28.6)보다 이미 낫다: 게이트+topk 25 µs 가 쌍 35 µs 를 거의 다 가린다.
  순서를 바꿔 쌍을 MoE 뒤로 보내면 쌍 꼬리가 MoE 끝을 넘어 노출되고(18.6), topk 뒤로 보내면 쌍 블록이 MoE 블록의
  SM 을 잡아 MoE 끝이 늦어진다. 남는 처방은 하나 — MoE 블록(smem 90 KB, 40.8K 레지스터)과 한 SM 에 앉을 수 있는
  **공존형 배경 GEMM**(smem ≤ 10 KB, 블록당 ≤ 24K 레지스터: ksr 8 로 A 슬라이스 4 KB, 128 스레드) — 그러면 쌍이
  MoE 그늘에서 통째로 돌아 층당 ~10 µs(스텝 0.4 ms, 0.7%). 커널 작업이라 이번 묶음에서 뺀다.
- **6번**: 드래프터의 문맥 K/V 사영(`precompute_and_store_context_kv` 의 융합 bf16 GEMM [2560×4096], 21 MB, 101 µs)을
  W4 레인으로 — `VLLM_GLM53_DRAFTER_CTX_KV_W4=1`(기본 0). 드래프터 overlay 가 버퍼 빌드 때 팩을 만들고 디코드 크기
  배치는 `gemm_w4a8`, 그 외는 스톡 `F.linear`. 쿼리 경로의 k/v 행은 이미 같은 종류의 팩으로 서빙되므로 문맥 K/V 수치가
  쿼리 K/V 와 같은 종류가 된다. 상한 −75 µs/스텝(0.13%), 드래프터 수치만 바뀜(수용률 프로파일이 게이트). 층당 글루
  8발(norm·conv prepare/finish·rotary·cache)은 이미 inductor 가 한 발씩으로 접어 둔 것이라 더 접을 것이 거의 없다.
- **1·2번**: 체인24(srv2 `lever-chain24.sh`) = PRODT(기본값 + `DRAFTER_PREP=time`, 호스트 µs 실측) → FUSION3(신규 노브
  셋 + EXP-9·15·20·SMLP2 전부) → ARPF1(`AR_PREFETCH=1` 단독), 팔마다 전체 다리 + 한국어 3회. 플릿이 다른 세션의
  컨테이너로 바쁜 동안은 대기.
- **7번**: 일몰 후보 표는 §8.

### 8. 노브·코드 일몰 후보 (운영자 판정 요청 — 삭제는 승인 뒤)

규칙(28차): 이득이 확인된 개선은 기본값으로 올리고 반대쪽을 지운다; 이득 0 이나 기각은 경로째 지운다. 아래는 원장·런북의
판정이 끝난 것들이다. "범위"는 지울 파일·노브·테스트.

| # | 후보 | 판정 근거 | 범위 | 비고 |
|---|---|---|---|---|
| 1 | `MK_SMLP`(v1 상주 SMLP 커널) | 32차 §7: 서빙 이득 0, 한국어 1~2/16; SMLP2(PDL 체인 v2)가 대체 | `.cu` 의 `mk_smlp_kernel`·`run_smlp`, 드라이버 `smlp_forward` v1 분기, 노브, 벤치 `--segments smlp` v1 열 | SMLP2 는 EXP-24 팔에서 판정 |
| 2 | `MK_KTAIL`(v2 꼬리 유닛) | 30차 §11: 전 셀 검증, 기본값 없음, 두 형상 잡음·나머지 5~17% 손해 | `.cu` 꼬리 유닛 경로, 노브, 벤치 `--ktail2-sweep`, `set_gemm2` 인자 | |
| 3 | `MK_LOCALQ`(29차 로컬 양자화 커널) | 단독 손해, 서빙 이득 0(체인14 LOCALQ), 세트 승격 뒤 되돌림(#322) | `mk_gemm_lq_kernel`·`mk_gemm_phase_t<LQ>`, 플랜 `localq`, 노브, 벤치 `--gemm-sweep` local 열 | v2 레인이 같은 진단의 채택된 처방 |
| 4 | `UNION_PREFILL`·`_SHADOW`·`DENSE_PREFIX_PREFILL` + `glm53_union_prefill.py` | 32차 §5: 프리필 −9%, 검증 커널 컴파일 오류 | 파일 1, 노브 3, 모델 배선의 분기, 테스트 | |
| 5 | `SM121_MLA_PREFILL` + `flash_attn.py` overlay | 기본 off "브래킷 clean 까지"; MK-MLA v5 가 프리필 행을 이미 라우팅(+17%) | 파일 1(행 1), 노브, 테스트 `test_glm53_sm121_mla_prefill_gate` | 겹치는 표적인지 확인 뒤 |
| 6 | `KDA_PREFILL_REGIME` + `kda.py`·`chunk_delta_h.py` overlay | "성능 결과 없음"; 마운트만으로 Triton ABI 가 바뀌어 0 이어도 재컴파일 비용 | 파일 2(행 2), 노브, 테스트 | 지우면 프리필 첫 부팅 JIT 하나 줄어듦 |
| 7 | `FUSED_K_GATE`, `KPOOL_FUSED_TOPK` + `glm53_kpool_topk.cu/.py` | 둘 다 opt-in, 서빙 판정 없음(런북 상태표에 없음) | 파일 2(행 2), 노브 2 | 원장에서 판정 기록을 먼저 찾을 것 |
| 8 | ~~EP 계열~~ — **철회(운영자 "EP 종결 안 됐잖아")**. 08-30 의 "종결(불가)"는 flashinfer 자체 EP(진입점 하드 거부)에 대한 판정이고, 그 뒤 #115·#120~122 가 리포의 로컬 전용 래퍼 EP 경로를 구현해 EXP-1 은 "구현 완료, 부팅 측정만 남음"(런북 상태표)이다 | 후보 아님 | — | 판정은 EXP-1 부팅 뒤 |
| 9 | `MHC_ONEPASS`·`MHC_SMALLM`(디코드 쪽) | mk_mhc 가 쌍을 대체해 전제 소멸(런북 EXP-19 "초월") | 노브 2, `tilelang.py` 의 분기 | `MHC_PASSES`·`MHC_BIGFUSE` 는 프리필 big_fuse 표적이라 유지 |
| 10 | `SKIP_SAMPLER_PROFILE` | K5 탐색용, K5 는 보류 | 노브 1, 러너 패치 1 | K5 재개 전까지 불필요 |
| 11 | 고아 모듈 `glm53_drop_audit`·`glm53_sparse_q` | 어느 프로필도 안 실음(프로필 주석으로 설명됨) | 디렉터리 2 | 설명 절이 "은퇴"면 삭제 |

유지(후보 아님): EP 계열 4 노브 + 마이크로커널 2파일(EXP-1 미측정 — 위 8번 철회), `MK_KDA`·`MK_KDA_SHADOW`(레인은 서빙되고 진단이 남아 있음), `FP8_DENSE_PREFILL_NVFP4`(프리필 레버,
미판정), `DECODABLE_VOCAB`·`VOCAB_MASK_AUDIT`(한국어 품질 도구), `glm53_tail_slot_persistent` 행(잠들어 있지만
prep-fused 가 그 파일의 preimage 를 고정 — 지우면 이미지 원본으로 바뀌어 DISARM).

### 9. 체인24 — EXP-24 브래킷 (srv2 `lever-chain24-fusion.sh`, 배포 = 접기 브랜치; 결과는 아래 표에)

팔 셋, 팔마다 전체 다리(디코드 3회·프리필·수용률·품질·한국어) + 한국어 2회 추가. 첫 부팅은 오버레이 sha 변경으로
컴파일 캐시가 비워진 콜드 부팅. 판정은 창 통계(`judge_windows.py`, 32차 §13 형식)와 `judge_lever.py` 의 게이트 줄.

| 팔 | env | 디코드 창(중앙·q1·q3, n) | rep step/s | 프리필 | 품질 | 한국어 ×3 | 증명 줄 | 판정 |
|---|---|---|---|---|---|---|---|---|
| PRODT | 기본값 + `DRAFTER_PREP=time` | (대기) | | | | | `[drafter-prep] stock build host us` | 기준 + 호스트 µs 실측 |
| FUSION3 | `DRAFTER_PREP=1 INDEXER_DECODE_FUSED=1 INDEXER_GATE_SPLITK=1 DFLASH_EARLY_FC=1 KDA_ONEPASS=1 KDA_DUAL_GEMM=1 KPOOL_UPDATE_DIRECT_POS=1 MK_SMLP2=1 DRAFTER_CTX_KV_W4=1` | (대기) | | | | | `serving: FULL replay`, `tail-select fused`, `-> split-K`, `one-pass KDA serving`, `drafter-ctx-kv] serving`, smlp2 캡처 | |
| ARPF1 | `AR_PREFETCH=1` | (대기) | | | | | prefetch hints learned | 한국어 3회가 판정 |

_이 항목의 측정: 캡처 1회(체인22 PRODSET 부팅 유휴 시, 다른 세션 레그와 겹치지 않음), 분석은 srv4 에서
`nice -n 19 taskset -c 19`(rank 3 규칙; 부팅 창). GPU 프로브·전체 테스트는 플릿이 조용한 창에서._

## ★★★32차 — 레버 2~7 브래킷 체인: EXP-7 이 +7%, 나머지는 0 이거나 죽었고, srv4 가 rank 3 이다 (2026-09-04 밤)

_원장 번호: 이 항목은 29차로 적혀 있었으나 그 번호는 먼저 머지된 메가커널 29차(로컬
양자화 커널, PR #307)가 이미 쓰고 있었다 — 두 계열은 한 수열이다(28차가 "원장 번호 27 은
PR #290 이 쓰고 있어 28 로 적는다"고 적은 그 수열). 그 선례대로 다음 빈 번호인 32차로
옮겼다(30차·31차는 사용 중). 본문의 `32차 item N` 참조도 함께 옮겼고, 측정 자체는
손대지 않았다. **원장 번호는 배정 순서이지 시간 순서가 아니다** — 이 항목(09-04 밤)은
30차(09-05)·31차(09-04~05)보다 번호가 크지만 측정은 더 이르다. 시점은 각 절 제목의
날짜가 정본이다._

운영자 "2~7"(28차 목록의 EXP-7·PDL/AR/early-fc·MK-KDA·AR 융합·union 프리필·nvfp4 프리필). 프로덕션 기본값
(MK 세트 + MLA + W4 드래프터) 위에 팔 하나씩 부팅하는 체인(`ab-lever.sh`, 기록 `bracket-lever.jsonl`)으로
돌렸다. 기준 = 28차 mla 팔 **16.39 step/s** [16.02, 16.64].

### 1. 판정표

| 팔 | 노브 | step/s (6회 중앙값) | 23K 프리필 cold/warm | 게이트 | 판정 |
|---|---|---|---|---|---|
| PREPSHADOW | `PREP_FUSED=shadow` (C=1 짧게) | 14.9 (섀도는 두 경로 실행) | 2,724 / 2,726 | fused 624 스텝 drift=0, preimage 17 ok | 섀도 통과 |
| PDL | (기본값 = PR #290 의 `MK_PDL=1`) | 오염 (§3) | 오염 | — | PDLCLEAN 재측정 |
| **PREP** | `PREP_FUSED=1 SPLITK=1` | **17.59** [16.90, 17.87] | 2,711 / 2,710 | 품질 9/9, pos-1 76.4%, **한국어 레그 HTTP 500** | 디코드 +7.3% 이나 **불합격** — 원인 분리(PREP2/3) |
| ARPF | `AR_PREFETCH=1 EARLY_FC=1` | 16.44 [rep1 9.1 = 첫 요청 JIT] | 2,499 / 2,675 | 9/9 | **0** — 기본 off 유지 |
| KDASHADOW | `MK_KDA=1 KDA_SHADOW=1` | 16.27 (2회) | 2,726 / 2,726 | **레이아웃 게이트가 전 층 거부** (§4) | 서빙 불가 → KDA32 |
| UNIONSHADOW | `UNION_PREFILL=2 +SHADOW` | 16.1 (2회) | 2,608 / 2,536 | 검증 커널 Triton 컴파일 오류 (§5) | 검증 불가 |
| UNION | `UNION_PREFILL=2` | 16.24 | **2,474 / 2,492 (−9%)** | 9/9, 한국어 1/16 | **불합격** |
| NVFP4P | `FP8_DENSE_PREFILL_NVFP4=1` | **부팅 사망** | — | srv3 earlyoom (§8) | JIT 웜 뒤 재부팅 |
| PDLCLEAN(1) | (기본값) | 부팅 사망 | — | one-shot AR watchdog 오탐 (§8) | 재측정 |
| **PDLCLEAN2** | (기본값 = PDL on, 조용한 srv4) | **16.15** [15.8, 16.7] | 2,708 / 2,695 | 9/9, 0/16 | PDL 이득 **0** (기준 16.39) |
| NVFP4P2 | `FP8_DENSE_PREFILL_NVFP4=1` (d1079bc 배포, 데운 JIT) | 디코드 레그 40 s 만에 **정지→엔진 사망** | — | 부팅은 420 s 에 통과, 쌍 161+ 개, 호스트 여유 44 GiB | **불합격** (§9) |
| SMLP | `MK_SMLP=1` (d1079bc) | 15.9 [15.2, 16.5] | 2,614 / 2,690 | 3.61 tok/step, 9/9, **한국어 1/16** | 이득 **0**, 서빙 미증명(§7) → 기본값 0 |
| SMLP2 | `MK_SMLP=1` (a42c9cd, 서빙 증명 줄) | 9.4·9.7·7.8 / 15.1·16.3·16.0 (앞 3회 srv4 오염, §9) | 2,614 / 2,713 | 3.72 tok/step, 9/9, **한국어 2/16** | 서빙 증명됨; 한국어 2회 연속 깨짐(기본값 부팅은 0/16) → **기본값 0 확정** |
| KDA32SHADOW3 | `MAMBA_CACHE_DTYPE=float32 MK_KDA=1 +SHADOW` (0eefdf7: 두 상태 stride 계약) | 16.5 / 17.5 (2회) | 사망 | **게이트 통과(전 층 무장)** → 8K 프리필 중 srv2 earlyoom 이 Worker_TP0 사살 | 섀도 판정기가 층마다 conv(260 MB)+rec(2.2 GB) 풀을 통째로 2벌 복제 → 통합 메모리 고갈. 판정기를 사용 슬롯만 복제하도록 수정 |
| **PREPDEF** | (기본값 = **PREP_FUSED=1** + MK 세트 + MLA, 0eefdf7) | 16.3·17.6 / **17.0·17.3·17.1·17.4** | 2,637 / 2,715 | 3.45 tok/step, 9/9, 한국어 1/16; prep-fused `fused_steps=4096 drift=0` | **+4~6%** (기준 16.39) — 프로덕션 후보, 스톨 없음 |
| KDA32SHADOW4 | `float32 MK_KDA=1 +SHADOW` (05985b4: 압축 판정기 + 시간 섀도) | 16.3 / 17.6 (창 중앙 17.7 / 17.9) | 사망 | 8K 프리필 중 srv2 earlyoom 재발(Worker_TP0 RSS 7 GB) — srv3 10%, srv4 8% 여유 | 판정기 복제는 원인 아님; KDA 부팅이 기본값보다 ~12 GB 더 쓰는 원인 조사(§11). MLA 시간 섀도 첫 출력: T=9 **mk 99.8 µs vs 래퍼 1,707 µs** |
| **PROD(체인7)** | (기본값, c487603: prep-fused on + gemm2/lq 머지) | 17.9·17.8·17.0 (창 중앙 17.9·18.0·17.4) | 2,621 / 2,713 | 3.69 tok/step, 9/9, **한국어 0/16**, 스톨 없음 | 프로덕션 확정치 — 기준 16.39 대비 **+6~9%** |
| KDABF16SHADOW(체인8) | (기본값 + `MK_KDA=1 +SHADOW`, f073d74: bf16 conv 계약, fp32 노브 없음) | 16.7 / 17.5 (창 18.2 / 17.9) | 8K 프리필 **생존** | 게이트 통과, 판정 줄 없음(스텝 적격성 무로그 거부 의심) | 서빙 미증명 → e18751e 증명 줄, 체인9 |
| KDABF16(체인8) | (기본값 + `MK_KDA=1`) | 16.3·17.6·16.8 (창 17.2·17.9·16.9) | 2,624 / 2,720 | 3.63 tok/step, 9/9, 한국어 1/16 | 기본값과 동일 = 서빙 안 됐을 가능성; 체인9 가 판정 |
| PROD(체인8) | (기본값, f073d74) | 16.1·17.6·16.9 (창 17.7·18.2·17.2) | — | 완주 | 프로덕션 |
| KDABF16SHADOW2(체인9) | (기본값 + `MK_KDA=1 +SHADOW`, e18751e 증명 줄) | 15.4 / 17.5 (창 17.5 / 17.9) | 생존 | **`kda lane serving: first eligible step T=32 n_spec=4`(캡처 더미 1회) → `kda lane stock: spec_sequence_masks is None (not a spec-verify batch)`** | **MK-KDA 는 프로덕션 스텝을 한 번도 서빙한 적 없음**(§11) |
| KDABF16(체인9) | (기본값 + `MK_KDA=1`) | 16.9·17.2·16.9 (창 17.9·17.4·17.0) | 2,7xx | 완주 | = 기본값(서빙 안 됨) |
| PROD(체인9, 03:10) | (기본값, e18751e) | 16.6·17.0·16.9 (창 17.9·17.4·16.9) | — | 완주, 스톨 없음 | **현재 프로덕션** |
| KDAPROOF(체인10) | (기본값 + `MK_KDA=1`, c62db9c 집계 줄) | 16.2 / 17.4 (창 18.0 / 17.9) | — | 실제 스텝 집계 없음 | 러너의 list kv_cache 를 tuple 게이트가 거부(§12) |
| K5(체인10) | `SPEC_K=5 SPEC_K_FORCE=1` | **부팅 불가** | — | 프로파일 런 샘플러 Triton 초기화 45분 정지 | K5 레버 미측정(§12) |
| KDAPROOF2(체인11) | (기본값 + `MK_KDA=1`, a2eb3c2 list 허용) | 16.8 / 17.5 (창 17.9 / 17.9) | — | 여전히 집계 없음 → **그래프 재생 맹점** 확인(§12) | 캡처 증명 줄 407cfa3 → 체인12 |
| PROD(체인11, 06:45) | (기본값, 00a3d3c) | 16.4·17.5·16.9 (창 17.5·17.5·17.2) | 2,628 / 2,709 | 3.83 tok/step, 9/9, 한국어 0/16 | 프로덕션 |
| KDAPROOF3(체인12) | (기본값 + `MK_KDA=1`, e453bd7 캡처 줄) | (1회) | — | **`kda lane CAPTURED` + `mla lane CAPTURED`** | KDA 는 서빙 중이었고 이득 0 → 기본값 0 확정(§12) |
| PROD(체인15, 10:11) | 기본값, **srv4 swapoff 뒤 첫 부팅** | 18.0·17.5·16.7 (rep 16.8·16.9·16.5) | 2,712 | 9/9, 한국어 통과(사망 없음) | 스왑 가설 첫 검증 통과(1/1) |
| PROD(체인17, 10:38) | 기본값 | 18.0·17.9·17.0 (rep 16.9·17.6·16.8) | 2,702 | 9/9, 한국어 1/16(통과) | 기본값 부팅 중 상단 눈금 |
| PRODV2(체인18, 10:47) | 프로필 `MK_GEMM2=1` 의도 — **실제 env 0** | 17.5·18.0·17.0 | 2,711 | (끊음) | main 머지가 프로필 아래쪽에 `MK_GEMM2=0` 을 재선언 → 나중 줄이 이겨 기본값 부팅. 캡처 줄이 잡아냄(`gemm2=0`). 중복 제거(f07d9eb) 뒤 체인19 |
| **PRODV2(체인19, 11:10)** | 프로필 `MK_GEMM2=1`(정리됨) — 캡처 줄 **`gemm2=1`** | **17.9·18.2·17.5** (rep 16.4·17.9·17.4) | 2,518 / 2,702 | 3.47 tok/step, 9/9, 한국어 1/16 | **현재 프로덕션(v2 라운드3)**, PR #320 머지 |
| DEVLAB | `DEV_LAB=1` (c487603) | 17.0 (창 18.4) | — | 랩 op 실주행(§10) | 개발 부팅 |
| PREP2 | `PREP_FUSED=1` 만 | 디코드 2~3회차 **행** | — | 워커 무응답 → RPC 타임아웃 | **불합격** (§2) |
| PREP3 | `SPLITK=1` 만 | 16.7 [16.5, 16.8] | 2,687 / 2,686 | 수용률 레그에서 **엔진 사망** | **불합격** |
| KDA32SHADOW | `MAMBA_CACHE_DTYPE=float32 MK_KDA=1 +SHADOW` | 15.7/16.5 (2회) | 2,7xx | dtype 게이트 통과 → **"not contiguous"** 거부 (§4) | KDA32 건너뜀 |

### 2. EXP-7 의 이득은 진짜였다 — 그리고 무장 경로는 행(hang)한다

프리필 사다리는 그대로이고 C=1 스텝만 61.0 → 56.9 ms. 호스트 준비 유휴(9월 1일 트레이스 12%)가
표적이었고 실측 −4.1 ms 가 그 크기다. 한국어 레그(`korean-corruption.py 2 400`, C=2)에서 서버가 500 을
냈고 그 부팅의 로그는 다음 부팅에 덮여 원인이 남지 않았다(레그 스크립트가 실패 시 4노드 로그를 스냅샷
하고 ABORT 하도록 고쳤다). prep-fused 는 "균일 spec-verify + FULL 그래프 + 패딩 없음"에서만 fused 이고
C=2 는 stock 으로 떨어져야 하므로, 용의자는 그 폴백 경계이거나 같은 부팅에 얹은 split-K(M≤16 라우팅,
C=2 = M 16)다. PREP2(fused 만)·PREP3(split-K 만)이 갈랐다: **둘 다 죽는다.** PREP2 는 C=1 디코드
2~3회차에서 워커가 무응답(22:50:51 부터 "shared memory broadcast block 없음" 60 s 반복 → `sample_tokens`
RPC 타임아웃, 워커 로그에 트레이스백 없음 = 커널/집합통신에 갇힘), PREP3 는 수용률 레그(C=1, 온도 0.95)에서
엔진 사망(워커 큐 dequeue 타임아웃). 같은 main 의 기본값 부팅(PDLCLEAN2)은 전 레그를 완주했으므로 두
노브 각각의 무장 경로 문제다. 스냅샷 `fail-PREP2-225350/`, `fail-PREP3-231101/`(4노드 로그). 두 모듈 소유
세션의 수정 항목; 두 노브 모두 0 유지. +7.3% 는 "측정됐지만 미출시".

**정정(00:45, 운영자 "프렙퓨즈드 1해")**: §9 의 포렌식(PREP3·NVFP4P2 가 prep-fused 없이 같은 서명으로 사망)을 보고
운영자가 `VLLM_GLM53_PREP_FUSED=1` 을 기본값으로 올렸다. 0 은 킬 스위치로 남는다. 기본값 부팅 브래킷(체인5 마지막
팔 PREPDEF)이 스톨 리포터 아래서 돈다 — 행이 나면 4 랭크 덤프가 남는다.

### 3. 측정 위생 — srv4 는 rank 3 이다

PDL 팔 디코드 rep 2~5 가 9.1~12.0 step/s 로 떨어졌고 rank 0 의 AR 대기가 53 → 890~950 µs/collective 로
뛰었다. 같은 시각 나는 srv4 에서 테스트 스위트(단일 코어 45 s)를 돌리고 있었다 — srv4 는 TP rank 3 노드다.
워커의 스텝당 호스트 작업이 밀리면 매 AR 마다 ~0.9 ms 늦게 도착하고 네 랭크가 같이 기다린다. 이후
레그 중에는 가벼운 폴링만 했고 무거운 일은 부팅 창에서 `nice -n 19 taskset` 으로 돌렸다. PDL 팔의
프리필(1,395 tok/s)도 같은 시각 srv2 에서 돌린 트레이스 분석이 오염시켰다. → PDLCLEAN 재측정.

### 4. MK-KDA 가 서빙에서 한 번도 안 돈 다섯 번째 이유 — conv 상태 dtype

`armed={'kda': True} shadow_kda=True` 뒤에 `kda layout gate: conv state is torch.bfloat16(1056, 6144, 10),
not float32 (blocks, 6144, W) -- the layer stays stock`. vLLM 의 `mamba_cache_dtype=auto` 는 conv 상태를
모델 dtype(bf16)으로 잡고(순환 상태는 `kda_state_dtype` 이 항상 fp32), 커널은 fp32 를 인덱싱한다. 부팅
자가진단은 fp32 픽스처라 통과("delta variant 1 matches stock") — 22차와 같은 형태의 함정이다. 런처에
`MAMBA_CACHE_DTYPE`(프로필 선언, 기본 auto) 을 넣어 `--mamba-cache-dtype float32`(260 MB/rank)로 띄우면
게이트가 통과할 수 있다(PR #299). KDA32SHADOW 결과: dtype 게이트는 통과했고 다음 검사 **"conv state is
not contiguous"** 에서 전 층 거부 — fp32 (1056, 6144, 10) 텐서가 풀의 strided view 다. 커널이
`slot * 6144 * width` 로 인덱싱하므로 블록 stride 를 인자로 받는 계약이 다음 단계(게이트가 stride 를
로그하도록 보강). 커널을 bf16 conv 상태로 포팅하는 것은 그 다음 선택지.

### 5. union 프리필 — 느리고 검증 불가

섀도의 검증 커널(`glm53_union_prefill.py:126` `tl.range`)이 이미지의 Triton 3.7.1 에서 컴파일 오류를 내
"exact" 주장이 여전히 검증되지 않았고, 무장 팔의 23K 프리필은 2,474 tok/s 로 기본값(2,700)보다 9% 느렸다.
불합격, 기본 0 유지. 모듈 소유 세션의 수정 항목.

### 6. AR 편차 귀속 — 고정 지각 랭크는 없다 (레버 5 대체)

랭크별 phase 리포트(PR #297): 정상 디코드 창에서 네 랭크의 wait 가 48~51 µs 로 대칭(스프레드 2.9 µs ×
102 = 0.3 ms). rank 1 의 22.7 µs 는 부팅 구간 값이었다. 랭크별 커널 시간(같은 캡처 4 랭크): MoE 761~773 µs,
mk_gemm 56.7~59.3 µs — 3% 이내. AR 5.4 ms/스텝은 프로토콜 왕복(p10 22.6 µs)과 무작위 지터이지 노드 불균형이
아니다. 레버 5(융합)와 5′(편차)는 닫는다; 남는 것은 전송 계층(호스트 등록 RDMA 프록시) 뿐이다.

### 8. 죽은 부팅 둘의 원인

- **NVFP4P(22:19)**: nvfp8-dense 패스가 nvfp4 쌍을 만드는 동안 srv3 가용 메모리가 4.2% 까지 떨어져 earlyoom 이
  pipewire/wireplumber 를 죽이고 워커가 종료됐다 — cutlass `mm_fp4` 의 첫 JIT 컴파일(형상당 ~73 s)이 서빙
  프로세스 안에서 돌며 호스트 메모리를 먹은 26차형 절벽. 대응: 서빙 밖에서 JIT 를 데우는
  `probes/nvfp4_prefill_warm.py`(MAX_JOBS=4, 4노드 각자 `/cache` 에 캐시; 4노드 VERDICT PASS, W4A4 by-design
  rel 1.3e-1) + 패스 안의 메모리 감시 로그. 플릿이 빈 뒤 재부팅.
- **PDLCLEAN(1)(22:29)**: 첫 AR 에서 `OneShotFatal: one-shot proxy unhealthy` — watchdog 이 연속 두 호출 사이에
  프록시 스레드의 beat 가 안 움직이면 unhealthy 로 판정하는 구조라 프록시가 CPU 를 잠깐 못 받으면 오탐한다.
  같은 main 의 다음 부팅들(PREP2 이후)은 모두 통과 → 일회성. 워치독을 "N ms 안에 beat 전진" 으로 바꾸는
  것이 옳다(osar 모듈 소유 세션 항목).

### 9. 행(hang) 3건의 공통 서명 — 노브가 아니라 플릿일 수 있다

PREP2(22:49), PREP3(23:06), NVFP4P2(23:57) 는 서로 다른 노브인데 죽는 모양이 같다: 생성 처리량이 한 구간
느려진 뒤(31.8→5.8, 48.7→32, 40.8→24 tok/s) 다음 10 s 창에서 0 — 그리고 5분 뒤 `sample_tokens` RPC 타임아웃으로
엔진 사망. 워커 로그엔 어느 랭크에도 트레이스백이 없고(NCCL 덤프는 철거 후의 것), 스케줄된 토큰은 8(=평범한
C=1 디코드 스텝: NVFP4P2 의 nvfp4 경로(M>32)는 그 스텝에 없었다). 즉 세 번 모두 **디코드 집합통신에서 조용히
멈춘** 것이고, 노브의 수치 오류가 샘플러를 돌게 한 것도 아니다(`_gumbel_sample_kernel` 에 루프 없음).

- 같은 코드의 기본값 부팅(PDLCLEAN2)은 전 레그 완주 → 노브별 문제이거나, 부팅당 ~50% 의 플레이크를 기본값
  부팅이 한 번 피해 간 것이다. 지금 증거로는 가르지 못한다. SMLP 팔(00:02 부팅)이 다음 표본.
- srv4(rank 3)엔 남의 GPU/CPU 상주 작업이 있다: `nemotron-vllm`(임베딩 서버, 3.2 GB GPU, 창마다 24~174 요청 줄)
  + `rerank-server`(2 GB GPU, 10 h) + deneb 메일 스택. 한 창의 요청 수는 통과한 부팅과 죽은 부팅을 가르지
  않았고(PDLCLEAN2 88줄/20분), 호스트 메모리도 모두 ~27 GB 여유로 같다. 다만 osar 대기 시간이 rank 3 만
  20~37 µs(다른 랭크 50~89)로 **rank 3 이 늘 마지막 도착** — srv4 가 공유 노드라는 뜻. NVFP4P2 에선 정지(23:57:18)
  2 분 뒤 srv4 워커 메모리만 먼저 풀렸다(23:59:38; 다른 노드는 00:02 철거 때) = rank 3 워커가 먼저 죽었다
  (감시견 OneShotFatal 로 추정, 로그엔 없음).
- **★결정적 상관(00:35~00:45, SMLP2 디코드 레그)**: 6회 중 앞 3회가 **7.8~9.7 step/s**, 뒤 3회가 15.1~16.3. 같은
  창의 srv4 `nemotron-vllm` 로그: 느린 6분 동안 임베딩 요청 26건, **프롬프트 처리량 13,800~14,400 tok/s**(무거운
  배치) — 빠른 4분 동안은 0건. 즉 srv4 의 남의 GPU 작업이 돌면 플릿 스텝이 **반토막** 난다(rank 3 시분할). 22:00
  이후의 모든 브래킷(PDLCLEAN2 16.15, SMLP 15.9, 각 팔의 사망)은 이 오염 아래서 쟀다. 판정 보류 대상: PDL "이득 0",
  SMLP "이득 0". 처방: 브래킷 중엔 srv4 의 임베딩/리랭크 서버를 내리거나 다른 노드로(운영자 결정) — 그때까지
  srv4 에 `srv4-foreign.log`(10 s 마다 임베딩 요청 수·GPU util)를 남겨 레그마다 오염 여부를 표기한다.
- 필요한 계측(osar 모듈 소유 세션): 정지 시 어느 랭크가 어느 집합통신 번호에서 기다리는지 덤프하는 스톨
  리포터. 없으면 위 세 노브(+7.3% 의 PREP 포함)는 계속 "측정됐지만 미출시" 로 남는다.
- **★첫 진짜 스톨 덤프(01:29:27, PROD 기본값 팔 디코드 초반, 토큰 1,042)**: 4 랭크 모두 드래프터 메타데이터
  빌드의 GPU 동기화(`seq_lens_cpu`)에서 대기, **GPU 96% 스핀** — 커널 안 교착. 정지 직전 10 s 창의 AR 대기가
  rank 0/1/2 모두 36 → **152 µs** 로 늘었고(rank 3 은 48), 그 뒤 영구 정지. 프록시 오류 줄(`[oneshot] WC error`)은
  없음. 4 노드 RoCE f0 포트의 `out_of_sequence` 수천·`packet_seq_err` 수백~수천(누적) = 패브릭 재전송(QP 타임아웃
  14 = 67 ms 단위) — 대기 증가와 맞는 서명. 어느 위상(링 가드/피어 플래그)에서 굳는지는 커널이 말해야 한다.
- **★최종 진단(07:03, 체인12 PROD 팔 한국어 레그, 계측 적중)**: rank 0·1·2 가 모두 `STALL phase=wait(peer-flags)
  seq=284417 slot=1 missing_peer_mask=0x4 rxf[slot]={284417,284417,284413}` — 세 랭크가 같은 한 피어(rank 3 = srv4)의
  플래그를 기다리고, 그 플래그는 4 시퀀스 전(284413)에 멈춤. rank 3 은 스톨 줄이 없음 = **AR 커널에 진입조차 못함**(AR 앞
  커널에서 정지). 직전 10 s 창: rank 0 AR 대기 58 → **693 µs**, rank 3 자체의 copy 3.7 → 10.1 µs·guard 2.8 → 5.0(자기
  GPU 일이 느려짐). 같은 창 srv4 `nemotron-vllm`: **06:58~07:04 임베딩 31건, 프롬프트 15,000 tok/s**(무거운 배치), GPU
  96%. Xid·OOM 없음. → **rank 3 이 외부 임베딩 부하의 GPU 시분할에 굶겨져 AR 에 못 들어오고, 세 피어는 그 플래그를
  기다리며 스핀** — 밤새 행 4건의 기전. 30 s 트랩이 5분 침묵 대신 즉시 엔진을 죽였다(`unspecified launch failure`).
  처방은 운영: srv4 의 외부 GPU 서버를 내리거나 옮긴다. 코드로는 트랩 상한(`-DOSAR_STALL_TRAP_S`)만 남는다 — 굶주림
  자체는 AR 이 고칠 수 없다.
- **계측 오탐 수정(05:35)**: K5 부팅에서 `[oneshot] STALL rank=3 seq=2` 가 20 분간 5 s 마다 찍힘 — 부트스트랩의 정당한 피어 접속 대기가 워드를 쓰고 지워지지 않은 것(tx_seq/rxf 는 진행 중). 스핀 종료 시 워드 초기화 + seq<16 제외(f5f6456).
- **계측 투입(01:45)**: `k_oneshot` 의 두 스핀 루프에 상한 — 3 s 를 넘으면 block 0 이 Ctrl 의 남은 pad 한 칸에
  (seq·위상·안 온 피어 마스크·슬롯)을 쓰고 프록시 스레드가 5 s 마다 `[oneshot] STALL rank=… phase=… seq=…
  missing_peer_mask=… tx_seq/ack_seq/rxf[slot]` 로 출력, 30 s 면 `__trap()` 으로 즉시 사망(5 분 침묵 대신). 다음
  스톨이 원인을 찍는다. 컴파일 확인(이미지 nvcc).


### 11. KDA — fp32 노브 제거(bf16 conv 상태 직접 지원)와 남은 메모리 의문

- 프로덕션 mamba 캐시는 conv 상태 bf16(`auto`), 재귀 상태 fp32. MK-KDA 커널이 fp32 conv 만 받아 `MAMBA_CACHE_DTYPE=float32`
  가 KDA 팔의 전제였다 — 비용은 conv 130→260 MB/랭크로 작다. 커널이 이제 bf16 conv 를 로드 시 fp32 로 올리고 저장 시
  bf16 로 내린다(`cs_bf16` 런치 플래그; 스톡 `causal_conv1d_update` 와 같은 반올림 지점). 게이트는 fp32/bf16 둘 다
  허용, 셀프테스트·벤치에 "bf16" 레이아웃(허용 2e-2). 노브 없이 기본값 부팅에서 KDA 를 켤 수 있다(체인8).
- **bf16 conv 섀도 부팅(체인8 KDABF16SHADOW, 02:04)은 8K 프리필을 살아 넘겼다** — 4 노드 5 s 감시 최소 여유 19~24 GB(fp32 팔은 8~10%). fp32 mamba 캐시가 (conv 260 MB 이상으로) 풀 배치를 바꾸는 쪽이 유력. 단, 이 부팅에서도 `kda shadow #N` 판정 줄이 없다 → 층 게이트는 통과했지만 스텝 적격성이 조용히 거부한 것(§9 SMLP 와 같은 무장≠서빙). 적격성 사유 로그 + "kda lane serving" 증명 줄을 넣어 다음 팔이 판정.
- **판정 보류(체인9, 02:41 → 04:50 정정)**: 증명 줄 `spec_sequence_masks is None` 은 프리필 직후 **첫 디코드 스텝(드래프트 없음)** 에만 해당하고, "first eligible step" 은 캡처 더미에서 먼저 발화해 실제 스텝을 증명하지 못했다. V2 러너(`mamba_hybrid.prepare_attn`)는 정상 스텝(스케줄 8 = 드래프트 7 + 1)에 `num_decode_draft_tokens_cpu` 를 채우므로 spec 마스크는 정상 상태에서 존재한다. 남는 의문은 섀도 판정 줄이 한 번도 안 찍힌 것(`_kda_ensure_packs`/디바이스 게이트의 무로그 거부 가능). 증명 줄을 캡처 제외 + 512 호출마다 served/stock 집계로 고쳐 다음 팔이 확정한다. (아래 문단은 정정 전 가설.)
- (정정 전) 프로덕션 디코드 스텝의 KDA 메타데이터는 `spec_sequence_masks is None`.
  이미지의 `GDNAttentionMetadataBuilder.build` 는 `num_decode_draft_tokens_cpu` 가 넘어올 때만 spec 마스크를 만드는데
  이 V2 러너의 `build_attn_metadata` 경로는 그 인자를 **아무도 넘기지 않는다** → 스톡 KDA 는 항상 비-spec(varlen)
  경로로 q=8 을 처리하고, MK-KDA 레인의 적격성·런치 계약(spec_query_start_loc·spec_state_indices·num_accepted)은
  이 러너가 만들지 않는 형태를 겨냥하고 있었다. 오늘 밤 KDA 팔 7개가 전부 "무장 = 서빙" 오독이었고, 22차의 "트레이스에
  mk_kda_kernel 없음" 도 같은 원인. 다음 단계: 비-spec 메타데이터(query_start_loc·요청별 상태 슬롯·러너의 수락 토큰
  수)를 받는 런치 글루로 계약을 다시 쓰는 것 — 커널이 아니라 글루의 재설계. stride·bf16 계약과 압축 판정기는
  그대로 쓰인다.
- (구) KDA 부팅의 8K 프리필 순간 srv2 20 GB+ 일시 사용(두 번의 earlyoom)은 아직 미해명 — 4 노드 5 s 메모리 감시
  (`memwatch.log`) 아래서 다음 KDA 팔이 답한다. 정상 상태에선 KDA 부팅이 rank 0/1 여유를 오히려 17 GB 더 남긴다.

### 13. mk_gemm 개선 레인의 서빙 판정 (운영자 "mkgemm 개선 테스트", 체인14, 09:02~)

다른 세션의 두 레인을 **서빙 스텝**에서 잰다(단독 벤치 30차: v2 가 대부분 형상에서 −2~7 µs, 로컬 양자화는 단독 손해·
MoE 아래 발사 순서가 전부). 부팅 env 로 고정되는 레인이라 랩 토글 불가 → 탐색 팔 3개(디코드 3회·프리필 8K·품질
2K/32K), 창 중앙값으로 판정. 기준 = 오늘 기본값 PROD 팔들의 창 중앙값 17.2~18.0. 컨테이너 env 로 노브 전달 확인
(`VLLM_GLM53_MK_GEMM2=1`; 프로필에 선언 71a0da8).

| 팔 | env | 디코드 창 중앙 | 프리필 8K | 품질 | 판정 |
|---|---|---|---|---|---|
| GEMM2 | `MK_GEMM2=1` | 부팅 사망 | — | — | 로더(instanttensor) 로드 시점 메모리 검사 `Device memory is not enough`(철거 직후 여유 부족 플레이크, 모델 로드 754 s) — 체인15 에서 재시도 |
| GEMM2R(체인15) | `MK_GEMM2=1` (재시도) | **18.4·17.9·17.9** (rep 17.1·17.8·17.4) | 2,673 / 2,580 | 6/6 | v2 단독도 상단 창 |
| **GEMM2P(체인16, 승격 팔)** | `MK_GEMM2=1` 전체 레그 | **18.2·17.5·17.5** (rep 17.1·17.5·17.2) | 2,528 / 2,730 | 3.51 tok/step, 9/9, 한국어 1/16(기본값 팔들도 1/16 두 번) | **승격** — 아래 통계 |
| LOCALQ | `MK_LOCALQ=1` | 18.0·18.0·17.0 (rep 16.6·17.5·16.7) | 완주 | 6/6 (2K/32K) | **이득 0**(기본값 17.2~18.0 과 동일; srv4 외부 부하 0건 = 깨끗한 측정) |
| GEMM2LQ | 둘 다 | **18.4·18.0·17.7** (rep 17.5·17.8·17.4) | 2,701 / 2,704 | 6/6 | 오늘 최고 창(기본값 17.2~18.0 상단); +2~3% 후보, 해상도 안쪽 — GEMM2 단독(체인15)과 함께 판정 |
| PROD(체인14) | 기본값 | 17.4·17.4·16.9 (rep 15.8·17.1·16.8) | 2,703 | 9/9 → 한국어 레그 **사망 09:46** | 스톨 2번째(§9): rank 0·1·2 `wait seq=202406 missing=0x4`, rank 3 침묵, 트랩. 이번엔 srv4 임베딩 4건뿐·직전 AR 대기 정상 — 남는 rank 3 특이점은 **srv4 만 스왑 2.7 GB 사용**(통합 메모리 페이지 폴트 가설) → 09:50 `swapoff -a`(되돌림: `swapon -a`), 리포터 임계 20 s 로 트랩 전 덤프 확보 |

**창 표본 통계(2 s 창, `bracket-lever.jsonl`, C=1)** — 오늘 부팅 전부:

| 팔 | 창 n | 중앙 | q1 | q3 | 평균 |
|---|---|---|---|---|---|
| 기본값(9 부팅 합) | 219 | 17.45 | 16.95 | 17.95 | 17.39 |
| LOCALQ | 25 | 17.45 | 16.95 | 17.95 | 17.43 |
| **GEMM2R(v2)** | 25 | **17.94** | 17.44 | 18.44 | 17.80 |
| **GEMM2LQ(v2+lq)** | 26 | **17.95** | 17.95 | 18.45 | 17.95 |
| KDABF16 | 52 | 17.43 | 16.94 | 17.93 | 17.29 |

판정(10:40, 승격 팔 포함): 창 표본 기본값 264(13 부팅) vs v2 74(3 부팅) — **중앙 17.45 → 17.95(+2.9%), 평균 17.35 → 17.80
(+2.6%)**. 부팅 단위: v2 3/3 이 상단 눈금(17.94~17.95), 기본값은 13 중 3 만(우연이면 ≈1%). 품질 9/9, 수용률 정상, 한국어
1/16(기본값 팔 PREPDEF·KDABF16 도 1/16). 로컬 양자화는 0(v2 위에 얹어도 분포만 조여짐). → **`VLLM_GLM53_MK_GEMM2=1`
기본값 승격**(규칙 §8), 체인18 PROD 부팅이 새 기본값의 첫 프로덕션.

### 14. "이득 0" 레버를 모아 켜면 0인가 (운영자, 체인17 ALL0)

개별 브래킷에서 해상도 아래(±3%)였던 넷 — SMLP(공유 전문가 융합), KDA(bf16 계약, 캡처 확인), LOCALQ(로컬 양자화),
AR 프리페치(ARPF) — 를 한 부팅에 전부 켠다(PDL 은 이미 기본 on). 가설: 층당 수십 µs 급 이득들이 합쳐지면 한 눈금
(+0.5 step/s) 위로 올라온다. 탐색 레그 + 한국어(SMLP 단독이 1~2/16 깨뜨렸던 축). 판정은 창 통계(§13 표).

| 팔 | env | 디코드 창 중앙 | 프리필 8K | 품질 | 한국어 | 판정 |
|---|---|---|---|---|---|---|
| ALL0 | `MK_SMLP=1 MK_KDA=1 MK_LOCALQ=1 AR_PREFETCH=1` | 17.9·17.9·16.9 (rep 16.5·17.7·16.7); 창 27개 중앙 17.93·**평균 17.47(+0.7%)** | 완주 | 6/6 | **2/16** | **모아도 0**(평균 +0.7%, 잡음 안); 캡처 줄로 kda·smlp·mla 전부 서빙 확인; 한국어는 SMLP 패턴(단독 1·2/16, 합쳐 2/16) → 넷 모두 off 유지, SMLP 는 품질 축에서 기각 |
| ALL0S(체인20, v2 기본값 위) = 이득 0 세트에서 SMLP 제외 | `MK_KDA=1 MK_LOCALQ=1 AR_PREFETCH=1` | **18.7·18.5·17.9** (rep 17.2·**18.2**·17.5) — 캠페인 최고 | 2,634 / 2,715 | 3.60 tok/step, 9/9 | **1/16** | 캡처 줄로 kda·gemm(v2)·mla 서빙 확인. 운영자 규칙(한국어만 안 깨지면 도입): 1/16 은 기본값 팔 13개 중 4개의 수준이라 확인 부팅(체인21: 한국어 ×3 = 48표본)으로 판정 |
| ALL0S2(체인21, 확인 부팅, 같은 3개 켬) | `MK_KDA=1 MK_LOCALQ=1 AR_PREFETCH=1` | **18.7·18.4·17.5** (rep 17.5·18.2·17.5) | — (decode·korean 만) | — | **1/16 · 0/16 · ≥1/16** (한 부팅에서 한국어 3회 = 48표본) | 깨진 것으로 잡힌 응답은 세 번 모두 같은 프롬프트(조력 발전)의 **한자 병기** — `(조력發電)`, `(潮力發電)`: 판정기의 gloss 규칙(한국어 단어 뒤 괄호)이 붙여쓰기·영어 뒤 병기를 못 알아봐 cjk_mixed 로 센 것이지 토큰 깨짐(replacement·lone_jamo·control 0)이 아니다. 3회차 표본 수는 체인 스크립트의 `tail -3` 이 요약 줄을 잘라 ≥1 만 확인(내 실수) → 새 기본값 PROD 부팅에서 전체 출력으로 다시 3회 잰다 |
| PROD(체인21, 기본값 = v2) | (없음) | 18.7·18.4·17.5 | 2,622 / 2,682 | 3.34 tok/step, 9/9 | 0/16 | ALL0S2 와 같은 창(18.7·18.4·17.5) — 이 부팅에선 셋의 이득이 창에 안 보임 |
| PRODSET(체인22, **셋을 프로필 기본값으로 올린 뒤** 프로덕션 부팅, 한국어 3회 전체 출력) | (프로필: KDA=1 LOCALQ=1 AR_PREFETCH=1) | 17.9·**18.9**·17.9 (rep 16.5·17.9·17.5) | 2,622 / 2,706 | 3.33 tok/step, 9/9 | **0/16 · 1/16 · 1/16** | 캡처 줄 kda(T=32 n_spec=4)·gemm(gemm2=1)·mla, `[osar] prefetch hints learned (target): 90 collectives, 752.6 MB`. 깨짐 2건은 둘 다 프롬프트 5(MPPT)의 **중국어 새어듦** `-扰动 관찰법/섭동 관찰법`, `-扰动观察法 / 퍼터브 앤 옵저브` — 병기가 아니다 |

### 12. 후속 세 건(운영자 "그릐라", 04:5x~)

**판정(운영자 규칙 '한국어만 안 깨지면 +0.7% 도 도입') — 승격했다가 되돌림:** 13:45 에 셋을 기본값으로 올리고(6f804a5) 새 기본값 프로덕션(체인22)에 한국어 3회를 붙였더니 0·1·1/16 이 나왔고, 깨진 둘이 병기 아닌 **중국어 새어듦**이었다. 기본값 부팅에서도 같은 종류가 한 번 있었지만(체인19 PRODV2 프롬프트 4 `are原则上 spaced`) 비율이 다르다:

| 프로필 | 한국어 회차(16표본) | 더러운 회차 | 응답 기준 |
|---|---|---|---|
| 셋 켬(ALL0·ALL0S·ALL0S2·PRODSET, 4 부팅) | 2·1·1·0·≥1·0·1·1 | **6/8** | 8/128 = 6% |
| 기본값(v2 시대: PRODV2·PROD20·PROD21·GEMM2P) | 1·0·0·1 | 2/4 | 2/64 = 3% |
| 기본값(캠페인 전체 16회차) | 1 이 다섯 번 | 5/16 | 5/256 = 2% |

Fisher 단측 p≈0.055(6/8 vs 5/16). 새는 자리가 정해져 있다: 프롬프트 5 의 'Perturb and Observe' 번역 자리가 셋 켬에서 세 번(ALL0·PRODSET×2) 중국어(`扰动`)로 기울었다. **정정(14:40, 전체 로그 타임라인 재조사):** 같은 자리가 기본값에서도 두 번 새었다 — 09-04 18:07 드래프터 브래킷 base1(`MEGAKERNEL=1 MHC=1 GEMM=1 MLA=0`, fp8 드래프터, W4·prep-fused·v2·KDA·LOCALQ 전부 이전)과 09-05 02:32 체인8 PROD. 즉 이 새어듦은 셋이 만든 것이 아니라 **MK 세트(특히 W4 e2m1 GEMM 레인) 위 기본 스택이 원래 갖고 있던 근접 동률**이고, 셋은 그 동률을 더 자주 중국어 쪽으로 밀 뿐이다(온도 0, 수치 편향; AR 프리페치는 수치를 안 건드림). 이 문단의 이전 판 '기본값에선 한 번도 없었다' 는 v2 시대 PROD 로그만 본 오류였다. 창 이득은 0~+2%(체인22 17.9·18.9·17.9 vs 체인20 PROD 17.9·18.2·17.5, 체인21 은 동일). **한국어 혼입률 2~3% → 6% 를 +0.7% 와 바꾸지 않는다 → 세 줄을 0 으로 되돌림**(같은 PR 안에서; 프로필 순변화 0). 되돌린 기본값으로 체인23(디코드 3회 + 한국어 3회 전체 출력)을 돌려 같은 날 48표본 기준선을 남긴다. 운영자가 셋을 원하면 KDA·LOCALQ 를 따로 한국어 3회씩 돌려 새는 쪽만 빼는 것이 다음 수(부팅 2개, ~50분).

1. **MK-KDA 서빙 증명 재판정** — 체인10 KDAPROOF(05:07): "first eligible step T=24" 는 메모리 프로파일 더미, 실제 트래픽에선
   47k 호출 동안 **집계 줄이 한 번도 없음** = 적격성 검사 앞에서 잘림. 원인: `_kda_layout_reason` 이 `layer.kv_cache` 를
   tuple 로만 인정하는데 러너는 **list** 로 바인딩 → "not the pair yet" 문구가 매 스텝 반복되며 1회 로그 뒤 침묵(무장≠서빙
   5건째의 진짜 기전). tuple/list 허용 + 같은 사유 2,000회 반복 시 경고(a2eb3c2) → 체인11 KDAPROOF2 가 판정.
   → 체인11 KDAPROOF2: **여전히 집계 없음, 반복 경고도 없음.** 진짜 답: 서빙 디코드 스텝은 **FULL CUDA 그래프 재생**이라
   파이썬 훅이 아예 돌지 않는다. 훅은 캡처 시점(과 eager 인 프로파일·프리필 스텝)에만 실행되므로 레인이 서빙되는지는
   "캡처 때 그래프에 박혔는가"로만 알 수 있고, 섀도 판정기는 캡처를 건너뛰도록 설계돼 디코드를 한 번도 판정할 수 없었다
   (오늘 밤 KDA 팔 9개의 침묵 전부 이것). `kda_block` 에 캡처 시점 "kda lane CAPTURED" 줄 추가(407cfa3) → 체인12
   KDAPROOF3 이 최종 판정. 교훈: 그래프 아래 레인의 증명은 캡처 로그다 — 이후 모든 레인(SMLP·MLA 포함)에 같은 줄을 둔다.
   → **체인12 KDAPROOF3(06:51) 최종**: `kda lane CAPTURED into the decode graph: T=32 n_spec=4` + `mla lane CAPTURED: T=32`.
   **MK-KDA 는 서빙되고 있었다** — 그리고 디코드는 기본값과 같다(KDABF16 16.9·17.2·16.9 vs PROD 16.4~17.9). 판정: **KDA
   레인 서빙 이득 ≈ 0**, 기본값 0 유지(SMLP 와 같은 결론). 밤새의 "서빙 안 됨" 가설은 철회. 남는 사실: stride·bf16 계약,
   압축 판정기, 캡처 증명 줄은 유효; 34 층 KDA 블록의 스톡 체인이 이미 충분히 짧아 융합 이득이 브래킷 해상도 아래.
   (원래 항목:) 증명 줄을 캡처 제외·512 호출 집계로 고쳐(c62db9c) 체인10 KDAPROOF 팔(기본값 + `MK_KDA=1`,
   디코드 2회)이 `kda lane tally: served=… stock=…` 로 확정한다. 서빙되면 KDA 레인은 살아 있는 것이고 이득 0 이 판정;
   안 되면 `_kda_ensure_packs`/디바이스 게이트의 무로그 거부(이번에 로그 추가)가 답.
2. **K5 팔**(체인10 두 번째) — **부팅 불가**: 05:15 부팅이 45분간 초기화에서 멈춤. 헤드 Worker_TP0 가 `profile_run → _dummy_sampler_run → gumbel_sample` 의 Triton 커널 초기화(`_init_handles`)에 CPU 300%·GPU 96% 로 갇힘, 워커 3개는 유휴. K=5 에서 더미 샘플러 형상이 달라지며 생기는 초기화 병목(컴파일 루프 또는 거대 더미 배치)으로 보이며, K5 레버는 이 초기화를 따로 풀어야 잴 수 있다. 06:00 팔 중단 → PROD 진행. (원래 계획:)
   → **2차 시도(체인13 K5TRY, 07:16, 더미 샘플러 건너뛰기 스위치 `VLLM_GLM53_SKIP_SAMPLER_PROFILE=1`)**: 프로파일의
   샘플러 자리는 넘어갔으나(`SKIPPED` 확인) 바로 다음 자리 — 그래프 메모리 프로파일 첫 배치의 `tf32_hc_prenorm_gemm`
   (deep_gemm 계열 JIT, MHC 프리놈) — 에서 rank 0 이 CPU 200% 로 19분+, 캐시 신규 파일 2개뿐 = 컴파일이 아니라
   **설정 탐색 스핀**. K=5 의 M(요청당 6 토큰, 8 의 배수 아님)을 이 JIT 들이 처리 못하는 형상 문제로 판단. 07:41 중단.
   K5 는 "한 줄"이 아니라 M 정렬(패딩) 수준의 수정이 필요 — 보류.
   → **부수 사고(07:51)**: K5 부팅이 남긴 드래프터 torch.compile/AOT 아티팩트를 다음 K7 프로덕션 부팅이 로드해
   `expected size 7==5 … incorrect fake kernel` 로 사망 — 캐시 키에 num_speculative_tokens 가 없었다(28차 드래프터
   W4 와 같은 부류). root 소유 항목을 이미지 컨테이너의 `rm` 으로 제거(`purge-restart.sh`)하고 재부팅;
   `VLLM_GLM53_SPEC_K` 를 컴파일 팩터로 등록(6297b43). 규칙: 형상을 바꾸는 노브는 컴파일 팩터 등록이 먼저. `SPEC_K=5 SPEC_K_FORCE=1`(런처의 실험용 탈출구), 탐색 레그(디코드 3회·프리필 8K·수용률).
   가설: 검증 배치 (k+1) 토큰이 줄면 스텝당 서로 다른 전문가 수가 줄어 스텝이 빨라지고, tok/step 은 조금 준다 —
   one-spark 는 같은 드래프터를 K5 로 서빙(구조화 4.94 tok/step). 판정은 tok/s(step/s × tok/step).
3. ~~**2.05 bpw 품질 기준점**(one-spark, 1× Spark)~~ — **취소(06:25, 운영자 "원스파크 빌드 평가는 하지마")**. 체크포인트(80 GB)와 저장소는 srv4 에 보관만, 빌드·서빙·평가 없음. (원래 계획:) srv4 `~/one-spark`(2bd465d) + `~/models/GLM-5.3-Flash-exl3-2.05bpw`
   (turboderp exl3 mul1, 80 GB) + DFlash2. 플릿을 내린 창에서 이미지 빌드(base `vllm/vllm-openai:glm53-flash-arm64-cu130`
   + ExLlamaV3 e648f1a, MAX_JOBS 8, ~1 h) → `eval.sh`: 우리 게이트(품질 2K/32K·한국어 2×400·디코드 C=1·수용률)를
   그들 서버(TP1, K5, fp8 KV)에 그대로. 답할 것: 2 bpw 가 우리 품질 게이트를 통과하는가(통과하면 전문가 2~3 bpw 가
   27 ms 전문가 바닥을 절반으로 만드는 진짜 레버가 된다).

### 10. 부팅·벤치 병목 대책(운영자 "1,2,5,6 진행", 01:0x~)

| # | 무엇 | 어디 | 효과 |
|---|---|---|---|
| 1 | 탐색/승격 레그 행렬: `LEGS=decode,prefill8k QUALITY_CTX=2000,32000`(탐색) vs 전부(승격) | `bench/ab-lever.sh`(srv2 `ab-lever2.sh`), `bench/check-quality.py` | 팔당 ~10 분 절감 |
| 2 | 디코드 3회 기본 + 스텝 창 표본: 엔진 `iteration_tokens_total_count` 를 2 s 마다 샘플 → 창마다 step/s, 판정기는 창 단위 | `bench/bracket.py` (`_StepWindows`, `--reps` 3) | 6회→3회, 표본 수는 증가 |
| 5 | **개발 랩**(부팅 없는 커널 루프): `POST /glm53/lab` → 4 랭크 collective_rpc — `replay`(서빙 디코드 그래프 n 회 재생, us/step), `reload`(새 .cu 로 확장 재빌드 + 셀프테스트), `recapture`(그래프 재캡처) | `overlay/modules/glm53_dev_lab`, 드라이버 `rebuild()`, 런처 `--middleware`, 프로필 `VLLM_GLM53_DEV_LAB=0` | 커널 반복 25 분 → 1~2 분. **검증(01:48, DEVLAB 부팅)**: `info` 가 서빙 FULL 그래프(8 토큰)를 기억, `replay` 50회 **53.5 ms/step**·200회 57.1 ms(서빙 스텝 58 ms 중 타깃 그래프 몫), 4 랭크 동시(±1 µs); `reload` 38~60 s 재빌드·재무장; `recapture` 는 드래프터 매니저를 안 비워 단언 → 수정(9a98f45) |
| 6 | 시간 측정 섀도: KDA 판정기(CUDA 이벤트, `kda shadow timing … ms/step`), MLA 1회 판정(`mla shadow timing`), prep-fused(`[prep-fused] on timing` 호스트 시간) | 각 모듈 | 프로덕션 로그에서 첫 판정, 브래킷 전 |

### 7. MK 세부 커널 — 남은 표적은 소형 GEMM 의 고정비

디코드 트레이스의 `mk_gemm` 30~45 µs 급이 스텝당 86.7개·3.47 ms, 그중 **2.37 ms 가 다른 스트림에 덮이지
않는 임계경로**(스트림 17/210 반반). 형상은 공유 전문가 gate_up [1024×4096]·down [4096×512](각 1 MB 팩)
— 1 MB 를 230 GB/s 로 읽으면 4.5 µs 인데 발사가 40 µs 니 **발사당 ~25 µs 가 고정비**다. 비용 모델은
k-블록 ≤ 8 형상의 split 을 거부하고(`mk_choose_ksr`, "8 k-블록 미만 슬라이스는 파이프라인이 안 뜬다"는
k=2048 실측 규칙) 32 타일을 48 블록에 놓아 16개가 논다. 벤치에 그 형상을 넣고 split 강제 노브
(`VLLM_GLM53_MK_KSR`)를 붙였다(PR #298); 부팅 핑거프린트가 [N×K]×count 를 찍는다. 벤치는 플릿이 빌 때.
후보 순서: split r=2 강제 → 2 CTA/SM → 공유 전문가 3연산(gate_up·act·down) 단일 발사.

**MK_SEG_SMLP 첫 GPU 검증(09-04 23:40~23:44, 플릿 비운 srv4, `probes/megakernel_glm53_bench.py --segments smlp`)**:
활성화를 gate_up 에필로그로 접은 3번안(위상 3→2, 배리어 2→1). e2m1 격자 가중치의 exact 게이트와 3발 체인
(W4 gate_up → vLLM `SiluAndMulWithClamp` → W4 down) 대조를 세 형상 모두 통과.

| 형상(랭크당) | exact rel | 체인 3발 µs | 융합 1발 µs | 연속(x2) 체인→융합 |
|---|---|---|---|---|
| T=8, int=512, k=4096 (공유 전문가) | 3.6e-6 | 77.8 | **45.1 → 43.0**(L2 웜) | 74.6 → 39.0 |
| T=8, int=3072, k=4096 (dense 층) | 3.1e-6 | 160.8 | **130.1 → 127.0** | 158.1 → 121.9 |
| T=32, int=512, k=4096 | 3.4e-5 | 90.1 | **59.3 → 57.2** | 86.9 → 52.2 |

- 노브: `KSR_GU=6` 은 기본값과 같고(46.1), `KSR_D=2` 는 손해(51.2/134.1/67.6) → 기본값 유지. down 팩 L2 웜은
  세 형상 모두 −2 µs 로 일관 → **무조건 켜고 노브 삭제**(규칙 §8).
- 해석: 융합 45 µs ≈ 단독 GEMM 두 발의 합(26.6 + 18.6). 활성화 런치와 런치 간격은 사라졌지만 GEMM 위상은 단독
  런치와 같은 속도 — 남은 병목은 위상의 대역폭(92 GB/s = DRAM 의 1/3, 24차 "mk GEMM 정체")이지 융합 구조가 아니다.
- 스텝 환산 상한: 공유 전문가 42층 × 32~35 µs ≈ **−1.4 ms/스텝(≈ +2%)** — 단, 이 비교 대상은 벤치의 3발 체인
  (런치 간격 + torch 활성화 커널 포함)이다. 서빙은 CUDA 그래프 아래라 간격이 없고 체인이 ~50 µs 이므로 정직한
  기대치는 ~7 µs × 42 ≈ 0.3 ms(+0.5%), 브래킷 해상도 아래다.
- **브래킷(00:02~00:15, d1079bc)**: 디코드 15.9 [15.2, 16.5] (기본값 16.15/16.39) = 이득 0, 프리필 2,690 동일,
  9/9, 한국어 **1/16**. 4 랭크 모두 `armed smlp=True` 였지만 훅이 서빙 포워드에서 융합 경로를 탔다는 로그가 없다
  (타깃 모델은 eager 라 캡처 때 파이썬 훅이 돈다 — 그래도 증명 없이는 28차의 "무장≠서빙" 과 같은 자리).
  훅에 "smlp lane serving: first fused call / smlp lane stock: <이유>" 줄을 넣었고(1회 출력), SMLP2(00:31 부팅)가
  판정: 4 랭크 모두 "smlp lane serving: first fused call T=32 k=4096 n_int=3072 n_out=4096" 이고 거부 사유 줄은
  없다 → **훅은 서빙됐고 이득 0 은 진짜다.** 기본값 `VLLM_GLM53_MK_SMLP=0` 유지.

## ★스텝 커널 지도 최신화 — 무장 부팅의 인구조사, 그리고 도구가 세고 있던 남의 커널 (2026-09-04 밤, 무부팅)

운영자 "스텝 커널 지도 최신화". 부팅 없이 기존 캡처로 `STEP_KERNEL_MAP.md` 를 현재 기본값 쪽으로 끌어왔다.
쓴 트레이스는 srv2 `~/vllm-prof/` 의 rank 0 셋 — 08-31 스톡, 09-01 스톡, **09-04 18:42 무장**(28차 §3 이
프로파일한 18:25 cand 부팅: MK MHC+GEMM 무장, MK-MLA off, 드래프터는 fc 만 W4, 서빙 PDL 없음).

| | 스톡 08-31 | 무장 09-04 |
|---|---|---|
| 커널/스텝 | 1,886 | **1,582** (−304) |
| 이 리포에서 컴파일되는 커널 | 144 (7.6%) | **402 (25.4%)** |
| 밀집 GEMM 구간 | deep_gemm 197 발 **14.06 ms**(09-01) | mk_gemm 185 발 **14.13 ms** |
| MHC | 185 발 **2.54 ms**(09-01) | mk_mhc 89 + 잔여 7 발 **2.07 ms** |
| `per_token_group_quant` | 179 발 | **2 발** (MK GEMM 이 커널 안에서 양자화) |
| MoE 글루 | 331 발 **2.71 ms**(09-01) | 136 발 **1.20 ms** |
| GPU 유휴(스트림 합집합) | 9.24 ms(09-01) | 8.18 ms |

**판정**: 개수 축은 크게 움직였고(−304) 시간은 두 자리에서만 움직였다 — 양자화가 GEMM 안으로 들어간
자리(−1.5 ms)와 MHC 두 발이 한 발이 된 자리(−0.5 ms). 밀집 GEMM 은 발 수가 197 → 185 인데 시간은
제자리다. 두 트레이스는 **다른 부팅**이라 브래킷이 아니고 종단 판정은 28차 §8 의 기본값 부팅이 갖는다.
지도의 "커널 하나 5.4 µs" 상한은 이 규모에서 다시 낙관으로 확인됐다 — 무장 스텝의 elementwise 481 발
총점유는 **1.00 ms(1.4%)**, 발당 ~2 µs.

**꼬리**(신규 도구 `tools/trace_step_tail.py`: 준비 커널로 스텝을 자르고 마지막 MoE 전문가 커널로 다시
자름): 스텝 69.25 = forward 61.88 + 꼬리 **7.37 ms**, 꼬리의 93% 가 바쁘다. 드래프터 층 bf16 cutlass
2.88 ms/34 발 + lm_head 둘 1.66 ms/2 발 + AllGather 0.63 + 드래프터 AR 0.54/12 발 + fc(이미 MK W4)
0.50/6 발 + `kernel_mha` 0.11/5 발. 25차의 꼬리 그림이 무장 트레이스에서 그대로 재현된다.

**도구 결함 셋을 고쳤다.**

1. **`census.py` 가 남의 커널을 우리 것으로 세고 있었다** — osar 그룹 패턴의 `k_reduce` 가 앵커 없이
   deep_gemm `sm120_split_k_reduce_impl` **42 발/스텝**을 잡았다. 지도의 "우리 소유 186 개(9.9%)" 는
   **144 개(7.6%)** 로 정정. 우리 osar 모듈의 `__global__` 은 `k_oneshot` 하나(102 발/스텝)이고, 문서
   안 구조표(102)와 그룹표(144)가 어긋나 있던 것이 신호였다. 위 "커널 개수의 실측 가격" 표는 같은 부팅을
   **148(AR 104 + 게이트 44)** 로 적고 있었다 — 원장이 맞았고 지도만 186 이었다. **정규식 그룹은 계약이
   아니다 — 앵커 없이 쓰면 남의 커널이 우리 칸에 들어온다.**
2. **분류기가 코드보다 늦어 있었다** — `tools/trace_common.py::category()` 가 메가커널을 몰라
   mk_gemm+mk_mhc **274 발 14.5 ms** 를 `other` 로 흘렸다(첫 실행의 1 위 범주가 "기타"였다). MK
   GEMM/MHC/MLA/KDA·prep_fused 범주와, 리포에서 컴파일되는 커널을 판정하는 `owner()` 를 추가했다.
   새 커널을 만들면 `OURS` 에 심볼을 넣어야 지도가 그것을 우리 것으로 센다.
3. **`census.py` 가 부팅 중인 노드에서 위험했다** — `json.load` 로 트레이스를 통째로 올려 40 MB 캡처에
   수 GB 를 썼다. 이번에 다른 세션의 콜드 부팅(23:11)과 겹쳐 내가 중단시켰다(그쪽 부팅은 무사; 26차의
   메모리 절벽 구간에서 MemAvailable 26 GiB). 이벤트 하나씩 흘려보내는 파서로 교체 — 최대 RSS
   **17 MB**, 08-31 트레이스 출력은 이전 판과 **비트 동일**, 실행 시간은 오히려 짧다.

**미실측 — 다음 캡처에서 볼 세 줄**: 오늘의 기본값은 이 트레이스보다 앞서 있다(드래프터 W4 전량 19:38,
`MK_SEG_MLA=1` 19:55, 서빙 PDL PR #290). (1) `mk_mla_kernel` 11 발/스텝이 있는가, (2) cutlass bf16
201 → ~171 인가, (3) mk_gemm 185 → ~215 인가. 셋이 맞아야 "오늘의 기본값이 서빙된다" 가 트레이스로
증명된다 — 22차의 "무장 ≠ 서빙" 은 문서에도 적용된다.

**테스트**: `tests/test_logic.py` 에 소유 축·스트리밍 파서·꼬리 절단 계약 3 건 추가 (44,611 checks OK).

## ★★★메가커널 30차 — mk_gemm 이 deep_gemm 과 같은 14 ms 인 이유: 상주 커널은 MoE 옆에서 진다 (2026-09-05, 09-04 18:42 무장 트레이스 vs 09-01 스톡 트레이스)

운영자 지시: "deep_gemm 197발 14.06 ms(09-01) / mk_gemm 185발 14.13 ms — mk gemm 이
deep gemm 보다 훨씬 빠르게". 오프라인 벤치(lq1.main, srv2)는 W4 레인이 형상마다
stock 의 0.55~0.85배인데(84/62.5/41.9/30.8/26.6/18.4/59.4 vs 133/98/55/38/34/21.5/96)
서빙 트레이스 합은 제자리다. 트레이스를 **발사 위치별**(스텝 안 185개 위치, 22 스텝
중앙값; 스크래치 `an_gemm.py`)로 읽으니 답은 형상이 아니라 스트림이었다.

### 1. 무장 트레이스의 185발, 위치별

| 클래스 | 발/스텝 | 트레이스 µs (중앙, 층별 범위) | 오프라인 단독 µs | 비고 |
|---|---|---|---|---|
| KDA in_proj [6416×4096] | 34 | 88 (83~99) | 84 | prev = mk_mhc (PDL 체인) |
| KDA o_proj [4096×2048] | 34 | 41 (38~45) | ~35 | |
| MLA qkv_a [2048×4096] | 11 | 36 | (m=32: 42) | |
| MLA o_proj [4096×4096] | 11 | 64 | 59 | prev = cutlass, 부풀림 없음 |
| dense gate_up [6144×4096] | 3 | **112~119** | 바닥 62 (14.2 MB) | 48 타일 = grid, 분할·균형 없음 |
| dense down [4096×3072] | 3 | 55~59 | 바닥 31 | |
| 공유 gate_up [1024×4096] (aux 210) | 42 | 41 (35~45) | 26.6 | MoE 글루와 26 µs 겹침 |
| **공유 down [4096×512] (aux 210)** | 42 | **135 (110~172; p10 40~110, p90 180~220)** | 18.4 | MoE 커널과 100~150 µs 겹침, 시작 gap 650~880 |
| 드래프터 fc 청크 | 5 | 60~84 | 59 | 꼬리 |

위치별 중앙값 합 13.86 ms(인구조사 14.13). **공유 전문가 down 하나가 5.7 ms(41%)** 이고,
메인 스트림 99발은 6.4 ms(오프라인 합 5.7 대비 +12%, dense gate_up +80%).

### 2. 기전 — SM 을 나눌 수 없는 세 커널

트레이스의 발사 속성(`an_aux.py`): MoE b12x static 커널 smem **90,112 B**, 255 reg × 160,
grid 48(SM 당 1블록, 768 µs/발); `mk_gemm_kernel` smem 69,648 B, 80 reg × 256, grid 48;
deep_gemm `sm120_fp8_fp4_gemm_1d1d_impl<0,6528,4096>` smem 92,672 B, 168 reg × 384, grid
48. SM 의 shared memory 는 102,400 B — 셋 중 어느 둘도 한 SM 에 같이 못 앉는다.

그래서 aux 스트림의 공유 전문가 쌍은 MoE 와 **직렬**이다. 스톡의 deep_gemm 도 같은
처지지만 블록이 독립이라 MoE 블록이 하나씩 물러나는 꼬리(~150 µs)에 들어가 그 안에서
끝난다(09-01 트레이스: down 36 µs, 전부 MoE 커널 구간 안 = 노출 0). 상주 mk_gemm 은 먼저
자리를 얻은 블록들이 **publish 배리어**에서 마지막 블록까지 기다리고, 마지막 블록은
MoE 가 완전히 끝나야 앉는다 → 실제 GEMM 은 MoE 뒤에서 돌고 CUPTI duration 은 꼬리 전체를
품는다(135 µs). gate_up 쪽은 반대로 MoE 의 시작을 막는다: MoE 커널의 선행 커널 뒤 gap 이
p50 **14.7 µs**(다른 모든 커널은 0.6) — 48블록이 SM 을 비워 줄 때까지.

임계경로로 환산(메인 스트림 커널이 하나도 안 도는 aux 구간): mk_gemm 노출 **1,177 µs/스텝
= 층당 28 µs**(MoE 시작 지연 15 + MoE 뒤 down 13). 쌍의 DRAM 바닥은 3.4 MB = 15 µs 라
여기서 회수 가능한 것은 ~0.5~1.0 ms/스텝. 트레이스의 14.13 중 임계경로 기여는 메인 6.4 +
aux 1.2 ≈ **7.6 ms**; deep_gemm 의 14.06 은 헤드/fc 두 발(2 × ~820 µs)을 포함한 값이라
같은 자로 재면 스톡 ~12.4 vs 무장 ~7.6 — 이미 빠르지만 "훨씬" 은 아니다.

메인 스트림의 나머지 +0.7 ms 는 (a) 발사당 고정비 15~20 µs(x 로드·양자화 6~10, 배리어
1.4, 첫 채움 1.9, 발사·꼬리 4~5; 4차 스탬프)와 (b) 정적 첫 유닛 배분의 꼬리 — nblk 가
grid 의 배수(dense gate_up 48 타일)거나 full > 0 이면 분할이 없어 가장 느린 블록이 벽시계를
정한다(DRAM 중재는 공평하지 않다, 4차의 25% 편차).

같은 진단이 SMLP(32차, 다른 세션의 공유 전문가 융합 커널)에도 적용된다: 상주+배리어인
한 MoE 직렬화를 그대로 물려받는다 — SMLP 팔 step/s 15.2~16.5 는 기본값 ~16.4 와 무차이.

### 3. 처방 — 비상주 v2 커널 (`mk_gemm2_kernel`, `VLLM_GLM53_MK_GEMM2`, 기본 0)

deep_gemm 처럼 독립 블록이되 바이트는 W4 그대로: grid = (n/128) × ksr 블록, **배리어
없음**(블록이 자기 k-슬라이스의 A 를 x(L2)에서 직접 양자화 — amax·pow2·SATFINITE 변환이
`quant_store` 와 같아 mma 가 보는 바이트는 동일), **공유 A 스테이징 없음**, W4 전개를 smem
타일 대신 **레지스터에서 B 프래그먼트로**(`mk_w4x4`: 같은 두 LUT 즉치값, 같은 sign prmt —
32 KB 의 전개 타일이 사라진다), smem **37,120 B → SM 당 2블록**(`__launch_bounds__(256, 2)`,
레지스터 ≤ 128), k 루프는 k-블록당 `__syncthreads` 하나(A 더블버퍼), ksr > 1 슬라이스는
fp32 partial 을 *배정*하고 마지막 도착 슬라이스가 고정 순서로 폴드(결정적, zero pass 없음).
ksr 규칙(`mk_choose_ksr2`): 유닛 ≥ 2 × 상주 슬롯(2 × 48 × 2 = 192), 슬라이스 ≥ 4 k-블록,
partial 은 m × n × ksr ≤ 32 × 6528 × 4. 벤치 `--gemm2 both`(v1 열 옆에 v2 열, 출력 diff:
비분할 형상은 비트 동일, 분할 형상은 슬라이스 합 순서만 다름) + `--ksr2-sweep`, exact 게이트는
두 레인 × ksr 1/2/3/5/8. `probes/mk_gemm_moe_overlap_probe.py` 가 공유 전문가 쌍의 MoE
아래 노출을 두 레인으로 잰다.

기대(측정 전): 메인 99발 6.4 → ~4.9 ms(고정비 ~3 µs, 균형 꼬리 제거), aux 쌍 노출 28 →
~5 µs/층(down 이 MoE 꼬리 안에서 끝남) → 스텝 −2.5 ms, 트레이스 mk_gemm 합 14.1 → ~6.5~7.

### 4. 측정 1차 — 단독 벤치 (2026-09-05 00:48, srv2, chain4 와 chain5 사이의 3분 창, `g2.bench.out`, iters 10)

수치 게이트: exact grid v1 3.9e-8 / v2 ksr 0·1·2·3·5·8 전부 ≤ 3.9e-8, over-ulp 0, replay 안정 —
**PASS**. v2 출력은 비분할 형상(k=512, 4096², k=2048, k=3072, m=16 4096²)에서 v1 과 **비트
동일**, 분할 형상은 슬라이스 합 순서 차이 1e-10~5e-6.

단독(콜드 L2)/연속 2발(x2) µs, m=8 이 C=1:

| 형상 | stock | v1 | v1 x2 | v2(규칙) | v2 x2 | 규칙 ksr/유닛 | 스윕 최선 (ksr: 단독/x2) |
|---|---|---|---|---|---|---|---|
| [6416×4096] in_proj | 133 | 84.0 | 80.9 | 93.2 | 86.0 | 4 / 204 | 8: 86.0/79.4 |
| [4096×4096] MLA o | 100 | 61.4 | 58.4 | 60.4 | 54.8 | 6 / 192 | 2: 55.3/55.3 |
| [4096×2048] KDA o | 49 | 34.8 | 31.7 | 41.0 | 34.8 | 4 / 128 | 2: 34.8/32.8 |
| [2048×4096] MLA qkv | 49 | 36.9 | 32.8 | 39.9 | 34.8 | 8 / 128 | 6: 35.8/29.7 |
| [6144×4096] dense gu | 142 | 78.7 | 74.8 | 80.9 | 73.6 | 4 / 192 | 2: 74.8/70.7 |
| [4096×3072] dense dn | 73 | 50.2 | 46.6 | 50.2 | 44.0 | 6 / 192 | 2: 45.1/44.0 |
| [1024×4096] 공유 gu | 34 | 26.6 | 22.5 | **24.6** | **21.5** | 8 / 64 | 8 |
| [4096×512] 공유 dn | 20.5 | 18.4 | 15.4 | **16.4** | **14.8** | 1 / 32 | 1: 16.4/13.9 |
| m=16 [4096×4096] | 97 | 63.5 | 60.4 | 82.9 | 67.9 | 6 / 192 | 2: 63.5/62.5 |
| m=32 [2048×4096] | 54 | 41.9 | 37.9 | 53.2 | 49.6 | 8 / 128 | 6: 45.0/40.9 |
| m=32 [1024×4096] | 35 | 30.7 | 28.6 | 33.8 | 31.2 | 8 / 64 | 8: 33.0/30.7 |

읽기: (1) 공유 전문가 두 형상은 단독으로도 v1 보다 빠르다(−8~11%) — 서빙에서 보는 것은
이 값이 아니라 MoE 옆의 노출(§2)이고 그건 노출 프로브가 잰다. (2) 큰 형상은 **ksr 규칙이
틀렸다**: "2 웨이브 + 슬라이스 ≥ 4" 가 고른 값(4·6·8)보다 스윕의 최선은 유닛 수가 상주 슬롯
96 에 딱 맞거나(6144 × 2 = 96, 2048 × 6 = 96) 그 아래로 긴 슬라이스(4096: ksr 2 = 64 유닛)
인 쪽이고, 51 타일(in_proj)만 잘게(ksr 8 = 408) 썰어야 꼬리가 줄었다(ksr 2 = 102 유닛은
6개짜리 두 번째 웨이브가 +30 µs). 최선을 쓰면 v2 는 m=8 전 형상에서 v1 과 같거나 빠르다
(6416 86/79 vs 84/81, 4096² 55 vs 61, 6144 75/71 vs 79/75, 3072 45/44 vs 50/47).
(3) **m ≥ 16 은 v2 가 느리다**(m=16 4096² 83 vs 63.5, m=32 2048 53 vs 42): 블록마다 k-블록당
x 를 다시 읽고 양자화하는 비용이 m 에 비례하고(warp 당 4행), 그 L2 적재 지연이 mma 뒤에
드러난다 — v1 은 발사당 한 번이었다. C=1(m=8)에는 없는 비용이지만 C≥2 브래킷 전에 고쳐야
한다(후보: m ≤ 16 특화 인스턴스 + x 2단 선적재, 또는 k-블록별 first-arriver 양자화 공유).

### 5. 측정 2차 — ksr 3 스윕과 규칙 확정 (2026-09-05 01:11, srv2, chain5 뒤 창, `g2.ksr3.out`)

| 형상 (m=8) | ksr 2 | ksr 3 | ksr 4 | ksr 8 | v1 x2 |
|---|---|---|---|---|---|
| [6416×4096] 51타일 | 112.6/98.8 | 87.0/82.4 | 93.2/86.6 | **86.0/79.4** | 80.4 |
| [4096×4096] 32 | 56.3/55.3 | **55.3/49.7** | 66.6/61.9 | 61.4/56.3 | 56.3 |
| [4096×2048] 32 | 34.8/32.8 | **35.8/29.7** | 40.9/34.8 | 37.9/34.8 | 32.8 |
| [2048×4096] 16 | 43.0/41.0 | 34.8/33.8 | **34.8/32.8** | 41.0/34.8 | 33.2 |
| [6144×4096] 48 | **76.0/70.7** | 85.0/82.6 | 79.0/73.2 | 81.9/75.8 | 72.8 |
| [4096×3072] 32 | 45.1/44.5 | **44.1/39.9** | 55.3/48.1 | 51.2/45.6 | 46.1 |
| m=16 [4096×4096] | 62.5/62.0 | **56.3/51.7** | 70.7/65.1 | 68.6/62.0 | 59.4 |
| m=32 [2048×4096] | 61.4/58.4 | **47.1/45.1** | 47.1/45.6 | 53.3/48.6 | 37.9 |

(단독/x2 µs.) **유닛 수 = 상주 슬롯 96 딱 한 웨이브**(48×2, 32×3, 16×6)가 k 와 무관하게
최선이고, 51 타일은 짧은 두 번째 웨이브(102)가 최악이라 잘게(8) 썰어야 하며, 8 타일은
슬라이스 ≥ 4 k-블록 안에서 최대(8 = 64 유닛). 이 규칙을 `mk_choose_ksr2` 에 넣었다(SM 수는
장치에서). 규칙 적용 시 v2 x2 vs v1 x2: in_proj 79.4/80.4, 4096² 49.7/56.3, k2048 29.7/32.8,
n2048 32.8/33.2, 6144 70.7/72.8, k3072 39.9/46.1, 1024 21.5/22.5, k512 14.8/15.4, m=16 4096²
51.7/59.4 — **m ≤ 16 전 형상에서 v2 ≥ v1**. m=32 는 45.1 vs 37.9 로 여전히 뒤진다(§4 (3)).

노출 프로브 1차는 스톡 flashinfer 래퍼의 `run()` 에 `out=` 이 없어 실패(벤치 러너는
메가커널 파일만 마운트하고, `out=` 은 배포 b12x 오버레이의 확장) — 반환값을 쓰도록 고쳐
재대기. 유닛 스탬프도 다음 창.

**리뷰(PR #305 머지 뒤, 13건)에서 나온 커널 쪽 사실**(SASS 판독): k-블록당 warp 명령 ~670
중 LUT 쌍 재구성(프래그먼트마다 16쌍) 216, 스테이지 복사 루프 미언롤 80, 행당 양자화 70
(`1.0f/sc` 가 IEEE 나눗셈 슬로패스 호출), 에필로그의 런타임 바운드 때문에 acc 가 로컬
메모리(64 B 스택). 후속 PR 에서 복사 언롤·`__frcp_rn`(2의 거듭제곱이라 정확)·정적 인덱스를
넣었고, LUT 쌍 공유(k 순열로 프래그먼트 두 개를 한 그룹에)는 v1 과의 비트 동일을 깨므로
스탬프로 발행 한계가 확인되면 다음 라운드.


## ★★메가커널 29차 — 공유 전문가 GEMM 쌍의 고정비: 배리어 없는 로컬 양자화 커널은 단독으로 손해, MoE 아래에서는 발사 순서가 전부 (2026-09-04 밤, srv2)

**표적**: 28차 트레이스 읽기의 30~45 µs 클래스 — 공유 전문가의 gate_up `[1024 × 4096]` 과 down `[4096 × 512]`,
스텝당 86발, 3.5 ms, 그중 2.4 ms 가 다른 스트림에 덮이지 않는 임계경로. 바이트는 1~2 MB(레인 속도로 5~12 µs).
가설: 고정비 = grid-wide A 양자화 + 공표 배리어 + `g_mk_aq` 왕복 + 유휴 16 블록의 대기 → 블록당 유닛이 하나뿐인
발사(32 유닛/48 블록)는 각 블록이 자기 유닛의 A 를 x(L2)에서 직접 양자화하면 배리어가 필요 없다.

**구현**: `mk_gemm_phase_t<LQ>` + 별도 커널 `mk_gemm_lq_kernel`(한 커널에 두 경로를 넣으면 ptxas 가 합집합 128 레지스터를
잡아 `mk_gemm_kernel` 80 이 깨진다 → 분리, 전역 커널은 바이너리 그대로). 산술은 `quant_store` 와 동일 → 같은 split 에서
출력 **비트 동일**(스윕 `same` 열 전부 yes, exact 게이트 3.9e-8/over-ulp 0, 리플레이 동일). KDA 인라인 phase 는 `<false>`.

**1. 단독(srv2 빈 창 23:36~42, 같은 프로세스·같은 split, 첫 판 = 48블록 발사·양자화가 sync 구간 안)**:

| shape | lq0 µs (x2) | lq1 µs (x2) | Δ |
|---|---|---|---|
| m=8 n=1024 k=4096 (gate_up, ksr 4, 32 유닛) | 24.7 (22.5) | 28.7 (26.1) | **+4.0** |
| m=8 n=4096 k=512 (down, 32 유닛) | 18.4 (15.4) | 18.4 (15.4) | 0 |
| m=8 n=2048 k=4096 (ksr 3, 48 유닛) | 36.9 (32.8) | 38.9 (35.8) | +2.0 |
| m=16 n=1024 | 28.7 (24.6) | 34.7 (30.7) | +6.0 |
| m=32 n=1024 | 32.6 (28.7) | 45.0 (41.0) | **+12.4** |
| m=32 n=4096 k=512 | 24.6 (21.5) | 28.7 (25.6) | +4.1 |

전역 커널 대조(n=6416/4096, 96 유닛): main 84.0/62.5 vs 후보 84.0/63.5 — 변화 없음. ksr 스윕은 로컬에서도 모델의
선택(4 / 3)이 최선이었고(n=1024 ksr 6 = 48 유닛 26.6 은 동률), k=512 는 어떤 split 도 손해.

**스탬프(m=8 n=1024, 블록 중앙값, 진입 기준 µs)**: 전역 — 양자화 완료 4.2, 배리어 통과 5.5, 첫 유닛 완료 19.5, 스팬 21.8;
로컬 — 프롤로그 1.3, 첫 k-블록 양자화 4.0, 유닛 완료 22.1, 스팬 24.6. 즉 **전역 프롤로그(x 로드+양자화+배리어)는
5.5 µs 뿐**이고(4차의 "8~10 µs" 는 콜드 첫 발사 값), 로컬은 유닛 루프에 k-블록당 ~0.85 µs 를 더한다 — sync 구간 안의
양자화 + 행별 `break` 가 셔플 체인을 직렬화(m=32 에서 4행 × 5단). mma·expand·wait 누적은 두 경로가 같다(2.4/3.2/0.2).
**상한 정정**: 이 클래스의 발사당 제거 가능한 고정비는 ~5 µs 이지 15 가 아니다 → 단독 이득 상한 ≤ 0.4 ms/스텝, 실측 0 이하.

**2. MoE 아래 동시 실행**(`probes/mk_gemm_concurrent_probe.py`: 그래프 하나에 fork, 스트림 A = 서빙 b12x 래퍼 U=40
740 µs, 스트림 B = 쌍; exposed = span(moe‖pair) − span(moe)):

| 경로 | 쌍 단독 | moe-first 노출 | pair-first 노출 |
|---|---|---|---|
| 전역(lq0, 48 블록+배리어) | 40.5 | 43.8 | 44.9 |
| 로컬 v1(lq1, 48 블록) | 42.4 | **17.3** | 41.8 |

전역 경로는 두 순서 모두 **쌍 전체가 노출**된다(28차의 "2.4 ms 노출" 과 같은 그림): 블록이 MoE 블록이 물러나는 대로
들어오고 배리어가 마지막 블록까지 전부를 잡아둔다. 로컬 v1 은 MoE 가 먼저 발사되면 27 µs 를 숨기지만(×43 = −1.15
ms/스텝), 쌍이 먼저면(서빙의 aux 스트림 순서: 공유 전문가가 라우팅 MoE 앞에 enqueue 된다) 48 블록이 SM 을 다 잡아
MoE 가 기다린다. **이득의 정체는 양자화가 아니라 "배리어 없음 + SM 점유"** 다.

**v2 실측(00:18~20, srv2 빈 창, 동시 실행 프로브 v2 = 리플레이 5회 묶음으로 발사 간극 상각; 같은 방법 안에서만 비교)**:

| 경로 | 쌍 단독 | moe-first 노출 | pair-first 노출 | ×43 ms/스텝 |
|---|---|---|---|---|
| 전역(lq0, 48 블록+배리어) | 35.2 | 47.4 | 47.4 | 2.04 |
| 로컬 v2, 32 블록(=유닛) | 43.0 | 36.2 | **31.8** | 1.56 / 1.37 |
| 로컬 v2, 16 블록 캡 | 74.1 | 33.4 | 36.7 | 1.44 / 1.58 |
| 로컬 v2, 8 블록 캡 | 139.6 | 87.2 | 88.6 | 3.75 |

pair-first(서빙 순서)에서 47.4 → 31.8 µs(**−16 µs/층**). 층 수는 모델의 MoE 층 42(STEP_KERNEL_MAP: 층 3~44;
28차 읽기의 "86발" 은 84 + 2) → **×42 ≈ −0.66 ms/스텝(≈ 1%)의 투영**이지 스텝 실측이 아니다(프로브의 메인
스트림엔 MoE 커널만 있고 서빙은 그 앞에 라우터 GEMM·topk 를 돈다; 활성화는 복사로 대체; 한 층 반복이
42 층을 대신). 위에 적은 채택 기준 "15 안팎" 은 v1 moe-first 17.3 에서 나온 희망치이고 실측 31.8 은 거기
못 미친다 — 브래킷 해상도(CV 1.7% ≈ 1.1 ms) 안쪽이라 단독 판정은 어렵고 EXP-6 세트로만 잰다. **v1 → v2 의
moe-first 17.3 → 36.2 는 설명되지 않았다**(측정 방법이 다르다: v1 은 유휴 스트림 위 단발, v2 는 5회 묶음; 전역
대조는 43.8/44.9 → 47.4 로 +3 뿐) — 48블록 로컬 행을 v2 방법으로 다시 재야 한다(v2 프로브에 행 추가).
8 블록은 쌍이 140 µs 로 늘어 MoE 꼬리 밖으로 삐져나온다. 단독 쌍은 여전히 +8 µs(35.2 → 43.0): v2 가 옮긴 것은
양자화의 *위치*이고 *양*이 아니다 — 첫 v2 는 프롤로그처럼 4행을 클램프해 전부 축약해 m=8 에서 4배 일을
했다 → 워프의 실제 행 수(`nrows`)로 술어화(커밋 뒤, 미실측). 남은 후보: 유닛의 A 를 프롤로그에서 한 번에
양자화해 smem 에 두기(m ≤ 16 이면 ≤ 22 KB 로 예산 안).

**2차 코드 리뷰(2026-09-05 00:3x~01:0x)에서 바뀐 것**: (1) lq 커널이 전역 경로를 통째로 품고 있었고 "드리프트면
전역으로 강등" 은 32블록 발사에서 48 도착을 기다리는 배리어 정지였다 → `if constexpr` 분리 + 드리프트 `__trap()`.
(2) v2 가 첫 k-블록 A 저장을 expand 뒤로 옮긴 것이 전역 커널에도 적용돼 "바이너리 불변" 이 깨졌다 → 전역 경로는
main 순서로 원복. (3) 양자화 셋(프롤로그·로컬·KDA p4)이 헬퍼 하나(`mk_warp_amax` = redux.max, `mk_pow2_scale`
= 지수 산술, `mk_pow2_rcp`, `mk_pack4` = 페어 cvt + prmt)를 쓴다 — 결과는 구성상 동일, **전역 커널의 SASS 는
바뀐다**(3,360 → 3,240 줄, 80 레지스터 그대로; exact 게이트와 n=6416/4096 대조 행이 확인 대상). (4) 로컬 경로: x 를
두 k-블록 앞서 레지스터 링에(런타임 인덱스는 로컬 메모리로 갔다, 스택 64 → 128 B — 이름 둘로), 양자화를 mma 앞에.
(5) 자가진단·probe_exact 가 plan 이 정말 로컬 커널을 골랐는지 확인(안 그러면 전역 둘의 비교 = 공허), 픽스처는
드라이버 하나(`exact_fixture`). (6) 발사 그리드 노브는 벤치 전용(`set_probe`; env 표면 삭제), 동시 실행 프로브에
**전역 커널 32블록 대조군**(자기 티켓 카운터)과 48블록 로컬 행 추가 — 미실측. (7) 문서의 상한·"hidden"·"wash"
표현을 실측으로 교체.

**v2**(코드 리뷰 반영과 함께 랜딩): (1) lq 커널을 **유닛 수(32)만큼만 발사**하고(`VLLM_GLM53_MK_LQ_GRID` 로
16/8 캡 스윕) 블록이 발사 grid 로 유닛을 순회 — 남는 SM 은 MoE 몫; (2) `lq_quant` 를 파이프라인 wait 앞으로, 행 전부를
무조건 축약(체인 인터리브; 클램프 행은 프롤로그처럼 버림), smem 저장만 배리어 뒤; (3) 호출 측 게이트 — fp8-dense 훅이
`mlp.shared_experts.*` 를 `_mk_bg` 로 표시하고 `VLLM_GLM53_MK_LOCALQ` 는 0(기본, 프로필 선언)/1(bg 발사만)/2(유닛 ≤
grid 전부, 벤치); (4) 자가진단이 두 커널을 exact 게이트 + 비트 동일로 검사, 부팅 로그에 plan 을 찍는다. 채택 기준 =
pair-first 노출이 전역의 44 에서 15 안팎으로 내려가는 것(×43 ≈ −1.2 ms/스텝, 1.8%); 그 뒤 EXP-6 브래킷 cand 팔.

**3. 코드 리뷰(10 관점 + 검증, 같은 밤)에서 고친 것**: 스탬프 리더의 활성 블록 필터(전역 유휴 블록이 슬롯 4 를 찍는다 →
슬롯 6), `set_probe` 가 env 를 복원 못 하던 것(−1 = env 재읽기), plan 과 발사의 결정 분리(단일 `mk_gemm_plan_for`, 패딩 n),
기본값 on 이 프로필·핑거프린트·bracket 스냅샷 밖이던 것(런처는 프로필이 선언한 키만 나른다 → 기본 0 + 선언), exact
게이트가 lq 커널만 타던 것, RUNBOOK 순서 목록 삽입 위치, 스윕 중복 행, `MK_SPLIT_ELEMS` 주석(786,432 인데 393,216).

**교훈**: (1) 상한은 **정상 상태 스탬프**로 세운다 — 콜드 첫 발사 스탬프(8~10 µs)로 세운 −0.4~1.3 ms 는 프로브 하나에
0 이 됐다(27차 교훈 4 의 재범). (2) 동시 실행 이득은 **발사 순서와 SM 점유**를 먼저 잰다 — persistent 48블록 커널은 aux
스트림에서 MoE 와 SM 을 다툰다. (3) 유휴 가드 없는 원격 잡은 남의 부팅 위에서 돈다(23:55:47~56:02 NVFP4P2 캡처 단계와
14초 겹침, KV 예산 줄보다 뒤라 무해로 판단) — 어떤 컨테이너든 있으면 대기.


### 6. 측정 3차 — 노출 프로브·규칙 검증·유닛 스탬프 (2026-09-05 01:37~01:39, srv2, chain6 뒤 창, `g2.{overlap2,rule,stamps}.out`)

**노출 프로브**(`mk_gemm_moe_overlap_probe.py`: 공유 전문가 쌍을 MoE b12x U=40 옆 스트림에서, 그래프 리플레이 20회 중앙값):

| | µs | 노출 µs/층 |
|---|---|---|
| MoE 단독 (U=40) | 736.1 | |
| 쌍 단독 v1 / v2 | 40.4 / 32.9 | |
| MoE ‖ 쌍 v1, MoE 먼저 / 쌍 먼저 | 783.7 / 783.4 | **47.7 / 47.3** |
| MoE ‖ 쌍 v2, MoE 먼저 / 쌍 먼저 | 754.7 / 764.6 | **18.6 / 28.6** |

v1 은 어느 순서든 47 µs 가 그대로 얹힌다(§2 의 기전: 상주 48블록은 MoE 와 SM 을 못 나눈다). v2 는
독립 블록이라 MoE 꼬리 안에 들어가 18.6~28.6 — 서빙의 발사 순서(aux 의 gate_up 이 MoE 보다 먼저
= "쌍 먼저")로 47.3 → 28.6, **층당 −19 µs, ×42 = −0.79 ms/스텝**(MoE 먼저면 −1.2).

**규칙 검증**(`--gemm2 both`, 규칙 적용판, x2 = 연속 발사 정상 상태):

| 형상 | v1 x2 | v2 x2 | 규칙 ksr/유닛 |
|---|---|---|---|
| [6416×4096] m=8 | 79.9 | **75.8** | 8 / 408 |
| [4096×4096] m=8 | 57.3 | **47.6** | 3 / 96 |
| [4096×2048] m=8 | 32.8 | **27.6** | 3 / 96 |
| [2048×4096] m=8 | 32.8 | **28.7** | 6 / 96 |
| [6144×4096] m=8 | 73.7 | **69.1** | 2 / 96 |
| [4096×3072] m=8 | 46.1 | **37.9** | 3 / 96 |
| [1024×4096] m=8 | 22.5 | **21.0** | 8 / 64 |
| [4096×512] m=8 | 15.4 | **12.9** | 1 / 32 |
| [4096×4096] m=16 | 58.9 | **50.2** | 3 / 96 |
| [2048×4096] m=32 | 37.9 | 38.9 | 6 / 96 |
| [1024×4096] m=32 | 28.6 | 29.7 | 8 / 64 |

exact 게이트 두 레인 × ksr 5종 PASS, 비분할 형상 비트 동일. **m ≤ 16 전 형상에서 v2 가 v1 보다 빠르고
(−5~−18%), m = 32 는 ±1 µs 동률** — §4 (3) 의 m=32 열세는 규칙으로 해소됐다.

**유닛 스탬프**(`--stamps2`, `-DMK_PHASE_TS`, 5회 중앙값; skew = 유닛 진입 편차, first = 첫 레코드
도착, loop = 유닛의 k 루프, tail = 마지막 퇴장 − 중앙 퇴장, span = 마지막 퇴장, ev = 이벤트):

| 형상 | 유닛 | skew | first | loop | tail | span | ev |
|---|---|---|---|---|---|---|---|
| [6416×4096] | 408 | 64.9 | 3.3 | 10.9 | 29.7 | 77.9 | 82.9 |
| [4096×4096] | 96 | 0.1 | 7.6 | 34.1 | 5.9 | 48.5 | 53.2 |
| [4096×2048] | 96 | 0.1 | 7.4 | 15.2 | 4.9 | 28.6 | 32.8 |
| [2048×4096] | 96 | 0.1 | 7.3 | 15.3 | 6.2 | 29.6 | 33.8 |
| [6144×4096] | 96 | 0.1 | 7.6 | 54.0 | 7.1 | 69.5 | 74.8 |
| [1024×4096] | 64 | 0.1 | 4.8 | 8.4 | 4.7 | 19.1 | 23.6 |
| [4096×512] | 32 | 0.0 | 2.8 | 7.1 | 0.4 | 10.6 | 14.5 |

96유닛 형상은 전부 동시에 앉고(skew 0.1), 첫 레코드가 7.5 µs 뒤에 온다 — 96 블록이 t=0 에 2.6 MB
를 한꺼번에 요청하는 초기 버스트의 큐잉(230 GB/s 로 11 µs 분량)이라 단독 발사의 고정비이고, PDL
체인에서는 앞 커널 꼬리에 숨는다(x2 열이 단독보다 4~9 µs 작은 이유). 꼬리 5~9 µs = DRAM 중재
불공평의 마지막 블록. ev − span ≈ 4.5 µs = 발사 + 드레인. 408유닛(in_proj)은 4+ 웨이브라 skew
65·tail 30 이 구조적이지만 ev 82.9 로 v1(84)과 같고 x2 는 75.8 로 앞선다.

**결산 — 서빙 기대치(스텝, C=1)**: 메인 스트림 99발 x2 차이 합 ≈ −0.55 ms(in_proj −4.1×34,
o_proj −5.2×34, MLA −4.1×11·−9.7×11, dense −4.6×3·−8.2×3, fc −9.7×5) + 공유 전문가 쌍 노출
−0.79~−1.2 ms → **−1.3~−1.8 ms/스텝(≈2~3%)**, 트레이스의 mk_gemm 합은 down 의 135 µs 가
사라져 14.1 → ~7~8 ms. 오프라인 게이트는 전부 통과 — 남은 것은 EXP-21 브래킷(base 기본값 →
cand `VLLM_GLM53_MK_GEMM2=1`, 운영자 승인).

### 7. 3라운드 커널과 MK_SEG_SMLP2 (2026-09-05 07:1x~, 브랜치 `claude/mk-gemm-v2-round3`; GPU 숫자는 §8)

§5 의 리뷰 사실(SASS: k-블록당 warp 명령 ~670 중 LUT 쌍 재구성 216)과 §6 의 스탬프(작은 형상은
k-블록당 ~1.5 µs 로 발행 바운드: k512 의 loop 7.1 µs 가 2 k-블록)에서 나온 두 변경:

(1) **레인 k 순열로 LUT 쌍 공유**: mma 의 k 축을 k-블록 안의 순열로 쓴다 — 쿼드의 레인 q 가 자연
원소 [32q, 32q+32)(W raw 청크 q, e2m1 그룹 2q·2q+1)를 소유하고 스텝 ks 에 그중 워드 (ks+q)&3
을 b0/b1 로 먹인다(A 도 같은 원소). 레인당 행당 LUT 쌍 8 → 2, raw 로드는 (row, ks)당 4 B 하나
(회전 워드 덕에 쿼드 4 로드가 4 뱅크), `sel0` prmt 제거. 같은 바이트가 mma 에 들어가지만 mma 내부
합 순서가 바뀌어 **v1 과의 비트 동일은 깨진다**(exact 게이트 ≤ 1 bf16 ulp 가 계약; 벤치의 v2−v1
열은 ~1e-7 을 보인다).
(2) **m 별 인스턴스 `mk_gemm2_kernel<RQ>`**(RQ = warp 당 양자화 행 1/2/4 ↔ m ≤ 8/16/32): m-타일
수와 x lane 매핑이 컴파일 타임. m=32 의 양자화가 32레인 체인 4개 → 8레인 체인 1개(행당 16원소).
ptxas: 96/96/117 레지스터(이전 124), 스필·스택 0.

**MK_SEG_SMLP2**(`VLLM_GLM53_MK_SMLP2`, 기본 0): 공유 전문가 MLP 를 **PDL 체인된 v2 발사 둘**로 —
gate_up 발사는 `pair_act`(타일 쌍의 두 번째 최종 저장을 한 블록이 그 128열의 clamp·silu·mul 을
bf16 반올림으로 계산해 fp8 그룹 + 행 스케일을 `g_mk2_aq/axs` 에 방출; 슬라이스 폴드와 같은
release/acquire, 쌍 카운터 자가 재무장), down 발사는 `a_ready`(x 양자화 대신 그 그룹을 스테이징).
그리드 배리어 없음 → 상주+배리어인 29차 SMLP 가 물려받는 MoE 직렬화(§2)가 없고, act_and_mul
42발/스텝과 down 의 양자화·첫 x 지연이 사라지며 down 의 링 채움이 gate_up 꼬리에 겹친다. 자가진단은
29차 SMLP 와 같은 exact 픽스처 3종 + 리플레이. ptxas(a_ready·pair 경로 포함): 110/112/114.

### 8. 측정 4차 — 3라운드 커널 (2026-09-05 07:52~08:02, srv2, chain13 뒤 창, `g2.r3.out`/`g2.r3stamps.out`)

같은 런의 v1(상주) x2 대비, 3라운드 v2 x2(연속 발사 정상 상태) / 단독:

| 형상 | v1 x2 | v2 2R x2 (§6) | **v2 3R x2** | 3R 단독 | 3R 대비 v1 | GB/s(단독) |
|---|---|---|---|---|---|---|
| [6416×4096] m=8 | 85.5 | 75.8 | **70.1** | 74.8 | −18% | 203 |
| [4096×4096] m=8 | 55.3 | 47.6 | **45.1** | 50.2 | −18% | 190 |
| [4096×2048] m=8 | 32.8 | 27.6 | **25.1** | 29.7 | −23% | 162 |
| [2048×4096] m=8 | 31.7 | 28.7 | **25.6** | 29.7 | −19% | 162 |
| [6144×4096] m=8 | 85.0 | 69.1 | **69.1** | 71.7 | −19% | 200 |
| [4096×3072] m=8 | 45.1 | 37.9 | **34.3** | 38.9 | −24% | 185 |
| [1024×4096] m=8 공유 gu | 21.5 | 21.0 | **17.9** | 21.5 | −17% | 114 |
| [4096×512] m=8 공유 dn | 15.4 | 12.9 | **11.8** | 14.3 | −23% | 87 |
| [4096×4096] m=16 | 57.4 | 50.2 | **45.6** | 50.2 | −21% | 193 |
| [2048×4096] m=32 | 38.4 | 38.9 | **33.2** | 37.9 | −14% | 135 |
| [1024×4096] m=32 | 27.6 | 29.7 | **25.6** | 29.7 | −7% | 90 |

(이 런의 stock/v1 열은 체인 직후라 §6 보다 2~5% 높게 나왔다 — 비교는 같은 런 안에서.) exact 게이트
두 레인 × ksr 5종 PASS(v2 는 k 순열로 합 순서가 바뀌어 8.0e-5, over-ulp 0; ksr 1/2/3/5 는 0.0). 2R → 3R
의 이득은 −5~−8%(m ≤ 16), m=32 는 −14%(양자화 체인 4 → 1) — 발행 바운드였던 작은 형상과 m=32 에서
컸다. **v2 3R 는 모든 형상에서 v1 을 이긴다.** 큰 형상의 단독 GB/s 는 200±5 = 실측 상한(230~245)의
83~87%; 남은 것은 첫 레코드 7 µs(초기 버스트 큐잉, PDL 체인에서 숨음)와 꼬리 4~9 µs.

스탬프(3R, 96유닛 형상): loop 가 2R 대비 −4~−12%(4096² 34.1 → 31.1, k512 7.1 → 5.9, n1024 8.4 → 6.8),
skew·first·tail 은 같다 — 명령 감소가 곧 시간인 곳(loop)만 줄었다.

**SMLP2 는 이 창에서 재지 못했다**: 벤치가 `arm()` 없이 확장을 직접 몰아 `_ARMED["smlp2"]` 가 False
→ smlp 프로브의 smlp2 열이 빠짐(조건을 바인딩 존재로 고침, 다음 창). 29차 SMLP(상주 융합) 참고값:
T=8 int=512 fused x2 38.9 vs v1 체인 73.6; T=32 51.2 vs 84.9.

### 9. EXP-21 브래킷 1차 — 서빙에서 v2 레인 +2.9~+4.6% (2026-09-05 09:48~10:11, 다른 세션 chain15, 배포 418b01b = 2라운드 v2)

다른 세션의 lever-chain15 가 탐색 팔 `GEMM2R`(`VLLM_GLM53_MK_GEMM2=1`, decode 3 reps + prefill 8k +
quality 2K/32K)과 짝지은 `PROD`(기본값)를 같은 배포(418b01b: 그 세션 브랜치 + origin/main #313 =
v2 2라운드 규칙판)로 연달아 돌렸다. 헤드 로그 `selftest gemm ... -> ARM (lane v2, in_proj plan
on/ksr/units/bps=[1, 8, 408, 2])` 로 v2 서빙을 확인.

| | 창(2 s) step/s 중앙값 [p25, p75] | rep 별 acc 정규화 step/s | 평균 |
|---|---|---|---|
| PROD 같은 배포 2 부팅 (09:40, 10:04) | 17.43 [16.46, 17.93], n=49 | 15.8 / 17.15 / 16.82 · 16.82 / 16.89 / 16.51 | 16.67 |
| **GEMM2R** (09:52) | **17.94 [17.44, 18.44]**, n=25 | 17.09 / 17.77 / 17.44 | **17.43** |

창 중앙값 **+2.9%**, rep 평균 **+4.6%** — 30차 §6 의 기대(스텝 −1.3~−1.8 ms ≈ +2~3%)와 같은 급.
프리필 8k: GEMM2R cold 2,673 / warm 2,580 tok/s(TTFT 8.62 / 8.93 s) vs PROD 2,690 / 2,712(8.56 / 8.49) —
warm −4.9% 는 단일 실행 편차 안(오늘 PROD 팔들의 warm 은 2,614~2,712; v2 는 M ≤ 32 만 타므로 프리필
GEMM 은 stock 그대로), 승격 팔의 프리필 사다리로 확정. quality 2K/32K 3/3·3/3.

주의: 이 팔은 **2라운드** 커널이다(3라운드 −5~−8%, §8 은 아직 미배포). chain16 이 `GEMM2P`(승격 레그
전부)를 돌리는 중 — 그 결과가 기본값 판정(EXP-21 통과 → 프로필 `VLLM_GLM53_MK_GEMM2=1`)의 근거.

### 10. SMLP2 측정, 승격 팔, 꼬리 스윕 미완 (2026-09-05 10:16~11:14)

**MK_SEG_SMLP2**(`g2.smlp2.out`, srv2 11:12, iters 10; exact 게이트 = `_smlp_gate`, 단독 / x2 µs):

| 형상 | v1 체인 | 29차 SMLP(상주 융합) | v2 체인 x2 | **smlp2** | **smlp2 x2** |
|---|---|---|---|---|---|
| T=8 int=512 k=4096 (공유 전문가) | 77.8 / 74.6 | 43.0 / 38.9 | 65.9 | **31.8** | **28.2** |
| T=8 int=3072 (dense MLP) | 166.9 / 169.9 | 134.1 / 133.7 | 140.7 | **103.5** | **101.3** |
| T=32 int=512 | 88.1 / 85.9 | 55.3 / 52.1 | 77.2 | **46.1** | **41.4** |

exact2 4.1e-6 / 2.4e-5 / 3.4e-5(게이트 2e-3), VERDICT PASS. 공유 전문가 쌍이 x2 기준 상주 융합 38.9 →
**28.2**(−28%), v2 두 발 + act 65.9 → 28.2(−57%) — 배리어 없이 act_and_mul 발사와 down 의 양자화가
사라진 값. 바이트 바닥(3.4 MB) 15 µs 대비 1.9배. MoE 옆 노출은 §6 의 v2 값(18.6~28.6)보다 작을
것으로 기대(독립 블록 + 발사 하나 감소); 노출 프로브에 smlp2 열을 추가해 다음 창에 확인.

**EXP-21 승격 팔 `GEMM2P`**(chain16, 배포 418b01b = 2라운드 v2, 전 레그): 창 중앙값 **17.95** vs 같은
배포 PROD 3 부팅 17.45(+2.9%), rep 평균 17.29 vs 16.81(+2.9%); 프리필 사다리 warm 2,528 → 2,730 tok/s
(+8.0%; PROD 2,614 → 2,690); 수용률 pos-0 76.1% vs PROD 75.9 / 78.7(±2 pct 안); quality **9/9**; 한국어
**1/16** — 오늘 다른 팔들도 ALL0 2/16·PREPDEF·SMLP·UNION·PRODV2·KDABF16 1/16, PROD 0/16 ×2 라 팔 편차
수준이며 다른 세션이 chain22 에서 PROD 한국어 반복 측정 중(그 결과로 게이트 해석 확정). GEMM2R(§9)
까지 두 부팅 모두 +2.9%(창 중앙값) — **EXP-21 통과 근거**, 기본값 승격은 한국어 반복 뒤.

**꼬리 유닛 스윕**: 벤치 버그(`kept` 에 `got` 를 계산 전 저장 → UnboundLocalError)로 미실행 — 고쳐
재큐잉. 3라운드 최종 바이너리(a_ready·pair·tail 경로 포함, 114 reg)의 plain v2 재측정도 같은 창.

### 11. 꼬리 유닛 스윕 — 기본 off 유지 (2026-09-05 14:49, srv2, `g2.ktail.out`, iters 10, 모든 셀 검증 PASS)

최종 바이너리(a_ready·pair·tail 경로 포함, 114 reg)의 tail=0 이 §8 의 3라운드 값과 같다(in_proj x2 69.1,
4096² 45.1, 6144 67.1, k3072 33.8, 공유 gu 17.9 / dn 12.3) — 머지된 코드의 §8 수치 유효.

| 형상 (m=8 외 표기) | tail=0 | tail=1 | tail=2 | tail=4 |
|---|---|---|---|---|
| [6416×4096] (408유닛) | 73.7/69.1 | 78.9/73.2 | 76.8/71.1 | – |
| [4096×4096] m=16 | 51.2/46.6 | 50.2/46.1 | 50.2/46.1 | 52.3/47.6 |
| [2048×4096] m=32 | 37.9/33.3 | 44.3/39.4 | 44.1/38.9 | – |
| [1024×4096] m=32 | 27.7/26.0 | 34.8/29.7 | 35.9/32.3 | – |
| [1024×4096] | 21.5/17.9 | **20.6/16.9** | 23.6/20.0 | – |
| [4096×512] | 14.3/12.3 | 14.3/12.3 | 15.4/11.8 | – |
| [4096×4096] | 50.2/45.1 | 49.2/45.1 | 49.2/44.5 | 51.2/45.6 |
| [4096×2048] | 29.8/25.2 | 30.7/26.1 | 31.7/27.1 | – |
| [2048×4096] | 30.8/26.1 | 29.7/26.2 | 30.7/27.1 | – |
| [6144×4096] | 70.7/67.1 | **66.6/65.6** | 68.6/65.5 | 70.7/66.0 |
| [4096×3072] | 37.9/33.8 | 37.9/34.3 | 39.9/34.8 | 42.0/36.4 |

(단독/x2 µs; – = 규칙이 거부한 조합.) 이득은 [1024×4096] tail=1 −5.6%(x2)와 [6144×4096] tail=1 −2%
뿐이고 m=32·k2048·in_proj 는 5~17% 손해 — 꼬리 유닛이 흡수하는 4~9 µs 보다 두 번째 partial 과 짧은
유닛의 링 채움이 더 든다. **`VLLM_GLM53_MK_KTAIL` 기본 0 유지**(형상별 규칙을 짜기엔 이득이 노이즈
급). 남은 발사당 고정비는 첫 레코드 큐잉(PDL 체인에서 숨음)과 DRAM 중재 꼬리 그 자체.

**30차 결산**: v2 3라운드가 프로덕션 기본값(다른 세션 PR #320, 체인19 PRODV2 창 17.9·18.2·17.5; 브래킷
+2.9%), SMLP2 는 측정(공유 전문가 x2 28.2 µs)만 되고 기본 off — 다음은 SMLP2 탐색 팔(decode + prefill8k)
과 노출 프로브의 smlp2 열.

### 12. 서빙 트레이스 재캡처 — 같은 185발 13.86 → 7.78 ms, 쌍 노출 1.16 → 0.15 ms; 다음 병목은 GEMM 레인 밖 (2026-09-05 15:07, srv2 `bench/profile-step.py decode:600`, 246 스텝)

트레이스 srv2 `~/vllm-prof/dp0_pp0_tp0_dcp0_ep0_rank0.1788588396786633088.pt.trace.json.gz`. 캡처 조건: 라이브
컨테이너(다른 세션 chain23 의 PRODBASE 팔) env `MK_GEMM2=1 MK_KDA=0 MK_LOCALQ=0 AR_PREFETCH=0 PREP_FUSED=1
MK_MHC=1 MK_MLA=1 MK_PDL=1 MK_SMLP2=0 MK_KTAIL=0` — §1 의 09-04 무장 트레이스와 같은 GEMM 고객 집합(KDA
in/out_proj 가 별도 발사)에 드래프터 층 GEMM 30발(W4 드래프터)이 더해진 **215발/스텝**. 도구는 §1 과 같은
`tools/trace_launch_positions.py <trace> mk_gemm2_kernel`(스텝 절단 = 가장 많이 맞은 앵커
`_get_num_sampled_and_rejected`; 첫 앵커 규칙이었다면 `_gather_block_tables_kernel` 4발이 4스텝으로 잘랐다 — 34차가 같은
수정을 먼저 넣었다) + `tools/trace_step_composition.py`(241 스텝: 스텝 중앙값 **55.30 ms**, 유휴 0.68 ms, 스트림 간
겹침 2.51 ms). 09-04 쪽은 같은 도구로 다시 잘라(184발 인식, 합 13.86) 역할별로 맞췄다.

**(1) 역할별**(위치별 중앙값의 합 µs/스텝 / 발당 평균):

| 역할 | 발 | 09-04 v1 | 09-05 v2 | Δ |
|---|---|---|---|---|
| 공유 전문가 gate_up [4096×1024] (aux) | 42 | ~1,706 / 40.6 | 1,291 / 30.7 | −24% |
| 공유 전문가 down [4096×512] (aux) | 42 | 5,714 / **136.0** | 582 / **13.9** | **−90%** |
| KDA in_proj [4096×6528] | 34 | 3,064 / 90.1 | 2,896 / 85.2 | −5% |
| KDA out_proj | 34 | 1,409 / 41.4 | 1,292 / 38.0 | −8% |
| dense gate_up (3층) | 3 | 348 / 116.1 | 312 / 104.0 | −10% |
| dense down | 3 | 170 / 56.8 | 144 / 48.0 | −15% |
| MLA q_proj | 11 | 394 / 35.8 | 377 / 34.2 | −4% |
| MLA o_proj | 11 | 712 / 64.7 | 582 / 52.9 | −18% |
| 드래프터 fc | 5 | 343 / 68.6 | 302 / 60.4 | −12% |
| **소계(같은 185발)** | 185 | **13,860** | **7,777** | **−6.1 ms (−44%)** |
| 드래프터 층 GEMM(새 고객) | 30 | — | 1,161 / 38.7 | |
| 합계 | 215 | | 8,938 | |

aux 쌍의 노출(메인 스트림 커널과 겹치지 않은 시간) **1,163 → 154 µs/스텝**. 쌍은 이제 MoE 와 겹치지 않고
(ov_moe 6 µs) 다음 층의 mhc·in_proj 옆에서 돈다(ov_oth 1,712 µs) — 그 대가로 메인 in_proj 는 오프라인 70 µs(§8)가
서빙 85 µs: 쌍과 SM 슬롯·DRAM 대역을 나눈다. 메인 역할의 서빙 이득(−5~−18%)이 오프라인(−15~−24%)보다 작은 이유.
"트레이스 mk 합 14.1 → ~7"(§3, EXP-21 상한)은 **7.8 로 확인**. 커널 시간 −6.1 ms 인데 브래킷이 −2.9%(≈1.6 ms)인 것은
09-04 의 down 136 µs 중 5.2 ms 가 MoE *뒤에서 기다린* 시간(§1 ov_moe 5,177 µs)이라 스텝에 그만큼 노출되지 않았기
때문 — 노출 감소 1.0 ms + 메인 0.5 ms ≈ 1.5 ms 가 브래킷과 맞는다.

**(2) 스텝 재귀속**(55.30 ms):

| 범주 | ms/스텝 | 발/스텝 | 몫 |
|---|---|---|---|
| MoE 전문가 커널(b12x, 층당 683 µs) | 28.62 | 42 | 51.8% |
| MK GEMM v2 | 8.95 | 215 | 16.2% |
| AR k_oneshot (p50 47.5 µs) | 5.56 | 102 | 10.1% |
| bf16/cuBLAS 소형 GEMM + 헤드 게이트 (cutlass 16×16 92발, 32×32 22발, gemmSN 12발) | 3.70 | 171 | 6.7% |
| MK MHC | 2.34 | 89 | 4.2% |
| LM 헤드 deep_gemm fp8×fp8 W8A8 (m=8·7, k=4096, 835 µs/발; §13 정정) | 1.67 | 2 | 3.0% |
| MoE 글루 | 1.49 | 136 | 2.7% |
| KDA 스톡(recurrent·conv·norm) | 1.35 | 102 | 2.4% |
| elementwise 글루 | 0.99 | 454 | 1.8% |
| nccl · MK MLA · indexer · 기타 | 1.8 | | 3.3% |
| 유휴(span − 합집합) | 0.68 | | 1.2% |

**(3) 다음 병목** — GEMM 레인은 1순위가 아니다(16%). 레인 안에 남은 것: (a) 메인 GEMM 은 DRAM 대역 78%
(in_proj 13.4 MB / 70 µs) 근처 — 이론 상한 −1.7 ms, 현실적으론 링 깊이 스윕(`VLLM_GLM53_MK_NBUF2=4`) 정도.
(b) **LM 헤드 2발 1.67 ms**: deep_gemm 이 835 µs/발. ~~vocab/TP × 4096 × fp4 ≈ 78 MB 는 바닥 ~320 µs, 대역 39%~~
**정정(§13)**: 헤드 가중치는 fp8(W8A8) 158 MB/rank 라 190 GB/s = 이미 fp8 바이트의 대역 바닥(78%). 이득은 W4 팩으로
바이트를 반으로 줄일 때만(33차 v1 W4 418 µs) — 헤드 정밀도 변경이므로 드래프트 헤드 브래킷 → 타깃은 운영자 결정.
`_gate_splitk` 는 인덱서 head-gate 라 헤드와 무관. (c) 드래프터 35발 1.46 ms: fc 5발 60 µs(긴 K 단일 발사 −40 µs
그대로). (d) **SMLP2 탐색 팔 기대치 하향**: 쌍 노출이 154 µs 뿐이라 융합(쌍 44.6 → 28.2 µs)의 −0.7 ms 커널 시간 중
스텝에 닿는 건 노출분 ≤ 0.2 ms + SM 경합 완화 — 스텝 ≤ 0.3 ms(0.5%), 브래킷 해상도(창 0.5 ms) 아래라 부팅 2회
값어치가 없다(보류). 레인 밖: MoE 28.6 ms, AR 5.6 ms(102발 × 47.5 µs, 레이턴시 바운드), bf16 소형 GEMM 3.7 ms
(171발 — census 로 소유자 귀속 필요).

도구: `census.py` 의 메가커널 그룹 정규식과 `tests/test_logic.py` 앵커에 `mk_gemm2_kernel` 추가 — v2 발사가
census 에서 "cutlass/cublas GEMM" 으로 새고 있었다.

### 13. W4 헤드 레인 — 구현, GPU 게이트 대기 (2026-09-06 새벽, 브랜치 `claude/mk-gemm-v2-lmhead`, EXP-22)

**§12 (3)(b) 의 정정**: 서빙 헤드 2발은 fp8 가중치다. 트레이스 커널 이름
`deep_gemm::sm120_fp8_fp4_gemm_1d1d_impl<0u, 8u/7u, 4096u, …, false, false, false, …>` 의 뒤쪽 세 플래그
(kIsFP4, kBIsFP4, kAIsFP4)가 전부 false — deep_gemm 의 sm120 통합 커널 이름일 뿐이고, 09-04 트레이스의 dense
잔여 발사 `<0u, 6528u, …>` 도 같은 플래그다. 경로는 `fp8_lm_head.Fp8HeadLogitsProcessor._apply_head` →
`fp8_gemm_nt`(W8A8 블록 스케일): `VLLM_TARGET_LM_HEAD_FP8=1`(타깃 verify, m=8)과 `VLLM_SPEC_FP8_LM_HEAD=1`
(드래프트 후보, m=7). 158 MB/rank ÷ 835 µs = **190 GB/s = 대역 78%**: fp8 바이트로는 바닥이고, 33차가 같은
형상에서 잰 참고치 bf16 1306 / fp8 918 / MK W4(v1) 418 µs 가 바이트 절반의 값이다. 운영자의 33차 결정("타깃
헤드는 서빙 로짓")은 그대로 — 드래프트 헤드부터.

**구현**: (1) 커널: `MK2_TILES_MAX` 64 → 320(헤드 38,720 → 303 타일), ksr 규칙에 "타일이 두 웨이브 이상이면
ksr 1"(분할마다 fp32 partial 2.5 MB + 폴드; 스윕이 확정), partial 상한은 분할일 때만(ksr 1·tail 0 은 누산기에서
bf16 직접 저장이라 m=32 헤드도 통과). (2) `glm53_megakernel.head_pack/head_logits`: 헤드 객체당 팩 한 번(타깃·
드래프터가 한 헤드를 공유하면 팩도 공유, 게이트는 endpoint 별; 실패는 fp8 복사처럼 래치), 첫 적격 호출(이거
워밍업, 캡처 전)에 첫 8타일·끝 8타일(패딩 꼬리)을 twin 과 exact 게이트(≤1e-3, over-ulp 0; 634 MB 대신 16 MB
역양자화) → 실패면 그 endpoint 를 부팅 동안 해제(로그), 통과면 서빙 증명 줄. (3) `Fp8HeadLogitsProcessor(mk_env=…)`:
타깃 `VLLM_GLM53_MK_HEAD_TARGET`, 드래프터 `VLLM_GLM53_MK_HEAD_DRAFT`(둘 다 기본 0) — fp8 복사보다 먼저 묻고
None 이면 기존 경로 그대로; 첫 서빙 호출에서 같은 행을 fp8(없으면 bf16) 헤드로도 계산해 argmax 일치 수와
|Δ|/행 로짓 std 를 한 줄 남긴다. GEMM2=1 필요(v1 상주 커널은 303 타일 미검증 → 해제).

**기대**: 303 유닛 × 288 KB(W4 + 스케일) = 87 MB → 245 GB/s 바닥 356 µs, 레인 실효 ~200 GB/s 면 ~420 µs; 두 발
−0.8 ms/스텝(1.5%), 드래프트만이면 −0.4 ms(0.75%, 브래킷 해상도 경계 — 창 판정은 타깃까지 켠 뒤가 선명하다).
**게이트**: 벤치 `--gemm-shapes 8:38720:4096,7:38720:4096 --gemm2 both --ksr2-sweep 1,2`(stock 열 = 서빙 fp8
헤드) → 드래프트 헤드 브래킷(acc 정규화 step/s, pos-1 ±2 pct, quality, 한국어) → 타깃은 운영자 결정. 팩 빌드는
158 M 파라미터 RTN(교정 Hessian 없음) — 첫 부팅에 수십 초, 이후 팩 캐시.

**품질·속도 교환비(운영자 질문, 09-06)** — 속도: 두 헤드 W4 −0.8 ms/스텝 = +1.5%(55.3 ms 스텝), 한쪽만 +0.75%;
브래킷 CV 1.7% 라 한쪽만은 판정 불가, 둘 다는 경계. 품질: 실제 체크포인트 `lm_head.weight`(bf16 [154880×4096])의
rank-0 샤드 2048행(128행 블록 16개, `probes/head_quant_noise.py`, CPU)에서 서빙 fp8 와 W4 팩을 그대로 재구성:

| | fp8 (서빙, 128² 블록 e4m3 + ue8m0) | W4 팩 (e2m1 × 16그룹 e4m3 스케일) | 배 |
|---|---|---|---|
| 가중치 상대 RMS 오차 | 2.66% (행 중앙 2.66, 최대 2.78) | 8.37% (행 중앙 8.37, p90 8.48, 최대 9.92) | 3.15 |
| 로짓 잡음 / 로짓 분산 (등방 x) | 2.7% | 8.5% | 3.2 |

즉 W4 헤드는 지금 서빙 중인 fp8 헤드가 이미 더한 로짓 잡음의 **3배**. 실제 토큰에서 greedy 뒤집힘·수용률로
얼마가 되는지는 은닉 상태의 마진 분포에 달려 미측정(가우시안 x 의 뒤집힘 15% 는 마진 없는 인공 수치라 기록하지
않음). **드래프트 헤드의 교환비는 구조적으로 정해진다**: 스텝당 토큰 ≈ Σ_{i=0..5} a^i (a = 위치별 수용률 0.63 →
2.44, 벤치 실측 600/246 과 일치) 이므로 da = 1 pt ↔ +0.052 tok/스텝 = **+2.1%**; 속도 이득 +0.75% 의 손익분기는
**수용률 −0.36 pt**. fp8 드래프트 헤드는 수용률을 안 움직였고(33차 rowwise 변형 메모), W4(잡음 3배)는 미지수 —
acc 정규화 step/s 브래킷이 자동으로 정산하되 0.75% 는 CV 아래라 단독 판정은 안 된다. **타깃 헤드**: +0.75% 를 얻고
서빙 로짓 잡음 3배를 지불 — 판정 불가능한 이득에 측정 가능한 정밀도 손실이라 권하지 않음. 잡음을 줄일 레버는
33차 팩 레버 그대로: 교정 부팅(`VLLM_GLM53_MK_CALIB=1`, 헤드 훅이 켜져 있으면 `lm_head` Hessian 이 자동 기록)의
GPTQ 팩 + 저랭크 보정(r=32)이 dense 층에서 오차를 크게 줄였으니 헤드에서도 3배 → ~2배 이하가 기대치.

### 14. "이득이 너무 적네" — 남은 레버를 크기로 다시 세다: 소형 GEMM 인구조사, EXP-4+EXP-9 묶음 팔 큐 (2026-09-06)

헤드 레인은 보류(§13 교환비: +1.5% 최대 vs 로짓 잡음 3배). v2 레인 자체는 대역 78% 라 남은 것이 ~1 ms. 그래서 §12 의
"bf16/cuBLAS 소형 GEMM 3.70 ms, 171발" 을 `tools/trace_vendor_gemm_census.py`(발사 위치의 앞 커널로 소속을 잡는다)로
세었다 — 같은 15:07 트레이스, 245 스텝, 3,761 µs/스텝, 183발:

| µs/스텝 | 발/스텝 | 발당 | grid | 커널 | 앞 커널 → 소속 |
|---|---|---|---|---|---|
| 1,475 | 22 | 67.0 | (8,32,1) | cutlass bf16 16×16 128x1 | `_fused_q_kv_rmsnorm` ×11, cutlass ×11 → **MLA q_b_proj [1536×4096] + 인덱서 wq_b [1536×4096], bf16 12.6 MB 씩** |
| 961 | 11 | 87.4 | (1,2,1) | cuBLAS gemmSN **fp32** | elementwise ×11 → **인덱서 head-gate** `weights_proj` [4096×32] fp32, cuBLAS 가 2블록 커널을 고른다(EXP-9 의 표적 그대로) |
| 343 | 68 | 5.0 | (8,16,1) | cutlass bf16 16×16 | mk_gemm2 ×34, cutlass ×34 → KDA 층당 2발(이 캡처는 KDA=0; KDA=1 프로덕션에선 mk_kda 안) |
| 283 + 249 | 11 + 11 | 25.7 / 22.7 | (8,2,16) / (8,1,16) | cutlass bf16 32×32 split-K 16 | `concat_and_cache_mla` / `mk_mla_kernel` → 흡수 MLA 의 q·W_UK, attn·W_UV 쌍 |
| 144 + 107 + 94 + 54 | 12 + 1 + 11 + 11 | | | | 소형 잔여(인덱서 mqa logits 54 포함) |

**레버 크기표**(스텝 55.3 ms): (1) `SPEC_K` 7→5/3 — MoE 가 DRAM 포화(m=8 행이 만지는 전문가 ≤48 × 3.5 MB 가 683 µs
= 대역 ~100%)라 행 수가 곧 MoE 시간; 수용 프로파일(3.83 tok/스텝 ↔ a≈0.77 등비)로 k=5 tokens 3.44(−10%)·k=3 2.82(−26%),
스텝 −7~−16 ms → tok/s 콘텐츠에 따라 +3%(높은 수용)~+29%(2.44 tok/스텝 류). **다른 세션이 오늘 K4/K5/K6 팔을 돌리는 중**
(`bracket-lever.jsonl` 09:15 K5 44.69 tok/s·step 15.54 vs 07:55 PRODT 44.84·17.99, 11:13 PRODK5H-dec 46.67·20.60) — 그쪽 레버.
(2) **EXP-4** `VLLM_GLM53_FP8_DENSE_BPROJ=1`: q_b + wq_b 22발 1.48 ms → fp8 dense → MK W4 팩 → v2 레인 → ~0.35 ms, **−1.1 ms
(2.0%)**, 로짓 무관(어텐션·인덱서 q 투영; 인덱서 wq_b 의 W4 는 index 점수 순위에 닿을 수 있어 quality 레그가 게이트). (3) **EXP-9**
`VLLM_GLM53_INDEXER_GATE_SPLITK=1`: 88 → 12.7 µs × 11 = **−0.83 ms (1.5%)**, 합산 순서만 변경(README 검증 완료). (4) MLA split-K
쌍 0.53 → mk_mla 흡수(다른 세션 커널). (5) AR 5.6 ms(osar), MoE 커널(대역 포화 — 행 수 레버가 정답).

**실행(운영자 "제안대로")**: 프로필에 `VLLM_GLM53_FP8_DENSE_BPROJ=0` 선언(PR #357 — 미선언 키는 런처가 컨테이너로 안 보낸다,
32차 사고 부류) → srv2 `~/glm53-logs/lever-chain-mkg3.sh`(12:21 큐; 다른 세션 체인·피어의 캘리브레이션/GPTQ 부팅 뒤 10분
유휴에 시작 → origin/main 배포 → `MKG3BASE ""` decode+prefill8k → `MKG3 "VLLM_GLM53_FP8_DENSE_BPROJ=1
VLLM_GLM53_INDEXER_GATE_SPLITK=1"` 전 레그). 기대 −1.9 ms(3.5%, CV 1.7% 위). 판정 = step/s + quality 9/9 + 한국어 0/16 + pos-1 ±2 pct;
증명 줄 `[fp8-dense] … linears`(+33: q_b·kv_b·wq_b × 11) 와 트레이스의 gemmSN 11발 소멸. 결과 `lever-chain-mkg3.out`, 행 이름 MKG3BASE/MKG3.

## ★★★28차 — 드래프터 W4 는 서빙된 적이 없었다(컴파일 캐시), MK-MLA 서빙 사망의 원인은 스크래치 재할당, 그리고 드래프터 W4 판정 (2026-09-04)

운영자 "glm flash 커널개선작업" → 25차 승인분 드래프터 W4 브래킷을 26차 수정 위에서 완주시키는 날이었다.
부팅 8회. **확정 셋**: (1) MK-MLA 를 켠 부팅은 서빙 중 2/2 죽고 끈 부팅은 3/3 완주 — 원인은 커널이
아니라 캡처된 그래프 아래서 재할당되는 fp32 스크래치. (2) 드래프터 W4 첫 브래킷의 "이득 0" 은
후보가 서빙되지 않은 측정이었다 — vLLM 컴파일 캐시가 로드 후 quant_method 스왑을 키로 안 삼는다.
(3) 실제로 서빙된 두 번째 브래킷: **C=1 step/s 15.95 → 16.235(+1.8%)**, 수용률·품질·한국어·프리필
게이트 통과 → 기본값(운영자 규칙: 이득 확인 → 기본값, 반대쪽 삭제). 서빙 오버레이 커밋: c7540e7(캐시
키·증명), bdeae3c(MLA), cf2e16c(기본값). 원장 번호 27 은 PR #290 이 쓰고 있어 28 로 적는다.

### 1. 부팅 여덟 번 — 무엇이 죽고 무엇이 완주했나

| 시각 | 팔 | MK-MLA | 결과 |
|---|---|---|---|
| 16:45 | base(bf16 드래프터) | on | bench-dec 4번째 요청에서 rank 1·2 `vectorized_gather_kernel index out of bounds`(`hidden_states[logits_indices]`) |
| 17:26 | base | on | rank 2 사망 → srv1 earlyoom(연쇄) |
| 17:38 | cand(W4, 캐시 문제로 fc 만 서빙) | on | 디코드 6회·프리필 사다리 완주 뒤 **23K 프리필 직후 첫 요청**에서 4랭크 `illegal memory access`(`propose → _build_draft_attn_metadata → seq_lens.to("cpu")` 동기화점) |
| 18:07 | base1 | **off** | 완주 |
| 18:25 | cand(fc 만 서빙) | off | 완주, step/s 15.945 = base |
| 19:01 | cand(전부 서빙, c7540e7) | off | 완주, **16.235** |
| 19:33 | mla(cand + MLA on, bdeae3c) | on | §6 |

공통 부품 중 실 트래픽 검증이 없던 유일한 것이 MK-MLA v5(09-03 오후, 프리필까지 라우팅; 섀도 게이트 공허
통과 — [[stkernel-armed-vs-serving]] 3번)라 두 팔 모두 `VLLM_GLM53_MK_MLA=0` 으로 고정해 변수 하나만
바꿨고, 그 뒤로 죽지 않았다. 프리픽스 캐시는 프로파일에서 꺼져 있어(PREFIX_CACHE=0) "재사용 프리픽스"
가설은 처음부터 제외.

### 2. MK-MLA 사망의 원인 — 캡처된 그래프 아래서 스크래치가 옮겨 다녔다

`_mla_workspace(device, T, splits)` 는 "grown once and then strongly held" 라고 주석했지만 코드는
`need = T*splits > cap` 이면 `_MLA_WS` 딕셔너리를 새로 만들어 **옛 텐서를 놓아 버렸다**(cap =
max(need, 2·MAX_TOK) = 64 행에서 시작). 디코드 FULL 그래프는 캡처 시점의 `part`/`pml` 주소를 굽는다.
그리고 v5 는 프리필 행도 같은 입구(`mla_decode`)로 보낸다: `mla_splits(T)` 는 "T·s 가 그리드(48)의
배수인 최소 s" 라, 37 토큰 프롬프트(bench-dec·수용률 프로파일의 첫 요청)는 **s=48, 1,776 행** →
재할당 → 캡처된 디코드 그래프들이 해제된 메모리에 부분합을 쓴다. 할당자가 그 자리를 다음에 무엇에
주느냐에 따라 증상이 갈렸다: base 는 4번째 요청에서 인덱스 텐서가 깨져 gather assert, cand 는 23K
프리필의 대량 임시 할당 뒤 첫 디코드 리플레이에서 illegal access. v5 이전(T ≤ 32 만 라우팅)에는
need ≤ 96 이라 09-03 서빙이 살아남았던 것.

**수정(bdeae3c)**: 스크래치를 `MLA_WS_ROWS = 192` 행(6.3 MB)으로 **한 번만** 할당하고 절대 키우지
않는다(초과 계획은 재할당 대신 raise); `mla_splits` 는 `T·s ≤ 192` 로 상한 — 디코드 형상(8→6, 16→3,
24→2, 32→3, 64→3)은 그대로이고, 규칙이 48 분할하던 33~63 행은 직접 저장 경로(s=1, 프리필이 원래
타는 길)로 간다. 자가진단에 직접 경로(40·100 행) 추가. 섀도 게이트는 실 KV 행(rows ≥ 16, |ref| ≥
1e-3, T ≤ 4096)에서만 판정하고 실패하면 `mla SHADOW FAIL ... this boot is invalid` 로 시끄럽게 —
단 부팅 때 캡처된 디코드 그래프는 되돌릴 수 없으므로 실패 부팅은 폐기가 규칙이다.

### 3. 드래프터 W4 첫 브래킷의 0 — 후보가 서빙되지 않았다

MLA 를 끈 두 팔이 15.95 vs 15.945 로 같길래 살아 있는 cand 서버를 프로파일했다(18 스텝, 69.25 ms,
`tools/trace_step_composition.py`). 스텝 꼬리에서 드래프터 fc 만 `mk_gemm_kernel` 5발(K-chunk, 합
346 µs; base 의 bf16 792 µs)이고, 레이어 안 선형 30개는 base 와 **같은 bf16 cutlass 커널·같은 시간**
(qkv 65, conv 66, o 45, conv 54, gate_up 228, down 124 µs)이었다. 핑거프린트는 "31 linears w8a8,
31 MK W4 packs" 라 했다.

원인: vLLM 은 torch.compile 캐시 — `VLLM_USE_AOT_COMPILE=1` 이라 아티팩트 전체 — 를
`envs.compile_factors()`(vllm.envs 에 **등록된** env 전부) + `vllm_config.compute_hash()` + forward
소스 해시로 키하고, `evaluate_guards` 가 꺼져 있어 로드한 아티팩트의 가드를 검사하지 않는다
(`decorators.py` "Directly load AOT compilation"). 로드 후 바꿔 끼운 `quant_method` 는 키가 아니다.
드래프터 아티팩트(srv4 `~/glm53-cache/vllm/torch_compile_cache/torch_aot_compile/696fd…`)는 **09-03
12:00** 생성이고 인덕터 코드에 `extern_kernels.mm` 30회, 우리 op 0회 — 그 뒤 모든 부팅이 이 bf16
그래프를 로드했고, eager 로 도는 fc 만 새 경로를 탔다. 드래프터 노브가 없던 09-03 의 그래프를
`VLLM_DFLASH2_FP8_DENSE=1` 부팅이 그대로 서빙한 것이다("무장 ≠ 서빙" 네 번째 사례).

**수정(c7540e7)**: 노브를 `vllm.envs.environment_variables` 에 등록해 값마다 자기 아티팩트를 갖게
했고(꺼진 철자는 "0" 으로 접음), opaque op 호출을 세어 드래프터 forward 첫 호출들에서 판정하는 증명
줄을 부팅 로그에 남긴다 — `[fp8-dense] drafter lane serving: 30 of 31 opaque GEMM calls per forward`
(fc 는 forward 밖). 리플레이는 파이썬이 없어 판정 제외, 캡처 중 forward 는 결정적. 19:01 부팅에서
4랭크 모두 30 of 31.

### 4. 드래프터 W4 브래킷 — 서빙된 후보 (base1 18:07 vs cand 19:01, 둘 다 MK-MLA off)

| 항목 | base1 (bf16 드래프터) | cand (W4, c7540e7) |
|---|---|---|
| C=1 step/s (6회, 중앙값 [min,max]) | **15.95** [15.64, 16.17] | **16.235** [15.19, 16.75] |
| tok/s 중앙값 | 37.8 | 39.1 |
| 수용률 프로파일 pos-1 (드래프트 n) | 61.6% (183) | 64.5% (354) |
| 토큰/스텝 정확 | 2.45 | 2.65 |
| 프리필 2048(5,769 tok) cold / warm | 2,236 / 2,245 | 2,194 / 2,289 |
| 프리필 8192(23,033 tok) cold / warm | 2,300 / 2,304 (TTFT 10.0 s) | 2,308 / 2,302 (10.0 s) |
| 품질 | 9/9 | 9/9 |
| 한국어 | **1/16 깨짐** | 0/16 |

step −1.1 ms/스텝(−1.8%; 25차 오프라인 추정 −2.0 ms 의 절반 남짓, rep1 워밍업 15.19 포함). 수용률은
게이트(±2 pct) 안쪽에서 오히려 위(샘플링 잡음, n≈200~350 의 표준오차 ~3 pct). **PASS** → 기본값
`VLLM_DFLASH2_FP8_DENSE=1`, `w8` 스킴 삭제(cf2e16c). 18:25 의 "fc 만 서빙" 팔(15.945)이 통제군 노릇을
한다: 환경변수·부팅 순서까지 같은데 그래프만 옛것이면 base 와 같다. 한국어 1/16 은 **base 팔**에서
나왔다 — n=16 게이트의 잡음 수준을 원장에 남긴다(cand 판정에 쓰지 않음).

### 5. MK-MLA 의 이득은 얼마인가 — 디코드 0, 프리필 +17%

| 근거 | 수치 |
|---|---|
| 커널 단독(srv4, 층당 T=8, W=2048) | v4 67.7 µs vs FlashInfer 124.3 → 11층 × ~57 µs ≈ 0.6 ms = **~1%** 천장 |
| 서빙 09-03 (네 세그먼트) | 15.89 vs 15.87/16.02 — 변화 없음 |
| 서빙 오늘 (17:42 on vs 18:16 off) | 15.78 vs 15.95 — 잡음 |
| **프리필 18:03 사다리(on, 죽기 직전 회수) vs off 두 팔** | 2048: 2,531/2,525 vs 2,194~2,236/2,245~2,289 (**+13%**); 8192: 2,687/2,721 vs 2,300~2,308/2,302~2,307 (**+17~18%**, TTFT 10.0 → 8.5 s) |

top-k 슬롯이 2048 고정이라 장문맥에서도 디코드 이득은 ~1% 천장이고, 프리필은 어텐션 비중(32K 에서
25%)만큼 커진다. 지울 게 아니라 고칠 대상이었던 이유.

### 6. MLA-on 팔(수정판, bdeae3c) — 살아남았고, 디코드 +1%, 프리필 +17%

부팅 19:33(cand 구성 + `VLLM_GLM53_MK_MLA=1`). 게이트 줄: `selftest mla rel=2.70e-03 -> ARM`(직접 경로
40·100 행 포함), **`mla shadow vs wrapper rel=1.40e-03 (T=9, rows<=18, real KV) -> ARMED`**(워밍업의
실요청 — 처음으로 공허하지 않은 섀도), `drafter lane serving: 30 of 31`. 첫 패스에서 디코드 레그는
브래킷 러너의 `--tag {base,cand}` 제한에 걸려 안 돌았고(스크립트가 `mla` 를 넘김), 사다리·수용률·
품질·한국어는 완주; 같은 서버에서 `DRAFTW4MLA/cand` 이름으로 레그 전부를 다시 돌렸다(두 번째 패스).
23K 프리필 뒤 첫 요청 — 18:04 에 죽던 바로 그 자리 — 를 두 번 지났다.

| 항목 | cand (MLA off, 19:01) | **mla (MLA on, 19:33)** |
|---|---|---|
| C=1 step/s (6회, 중앙값 [min,max]) | 16.235 [15.19, 16.75] | **16.4** [16.0, 16.6] |
| tok/s 중앙값 / bench-dec raw acc | 39.1 / 14~31% | 42.7 / 19~29% |
| 프리필 2048 cold / warm | 2,194 / 2,289 | 1,880(첫 요청 JIT, 세금 28.7%) / **2,638** |
| 프리필 8192(23K) cold / warm, TTFT | 2,308 / 2,302, 10.0 s | **2,679 / 2,726, 8.60 / 8.45 s** |
| 수용률 프로파일 pos-1 (드래프트 n) | 64.5% (354) | 75.6% (234) — §6 주의 |
| 품질 / 한국어 | 9/9 / 0/16 | 9/9 / 0/16 |

디코드 +1.0% 는 §5 의 천장(~1%)과 맞고, 프리필 +15~18% 는 18:03 의 죽기 직전 사다리와 재현된다.
**수용률 프로파일의 이동은 표본 편차였다.** 첫 패스는 같은 시드·온도(0.95)에서 raw 38.8%/pos-1
75.6% 로 off 팔(20.7~23.6%/61.6~64.5%)보다 훨씬 높았는데, 같은 서버의 두 번째 패스는 raw 26.6%/pos-1
**67.0%**(301 드래프트)였다 — 같은 시드로도 텍스트가 달라지는(배치·리플레이 비결정) 프로파일이라 4개
프롬프트의 편차가 ±7 pct 급이고, bench-dec 의 raw acc(19~29%)는 off 팔과 같다. 두 번째 패스의
사다리도 재현(2,592/2,520, 2,701/2,721, TTFT 8.53/8.46 s), 품질 9/9·한국어 0/16 두 번. 게이트 전부
통과 + 이득 확인 → 운영자 규칙대로 **프로필 기본값 `VLLM_GLM53_MK_MLA=1`**(cf 뒤 커밋). 단 이 노브는
마스터 `VLLM_GLM53_MEGAKERNEL` 아래에 있고 프로필의 마스터는 아직 0 이다 — 메가커널 세트 자체의
프로덕션 기본값은 다른 세션의 EXP-6/12 브래킷이 결정한다(PR #290). 그리디 텍스트 대조용으로 MLA on
서버의 온도 0 응답을 `~/glm53-logs/greedy-mla-on.json` 에 저장해 두었다(off 부팅과 diff 는 후속).

### 7. 교훈

- **컴파일되는 모델의 런타임 스왑은 캐시 키에 넣고, 서빙은 증명 줄로 남긴다.** 핑거프린트의 "N linears
  armed" 는 로드 시점 이야기다. 트레이스 없이 "무장했으니 돈다" 고 쓰지 말 것은 22차의 규칙이었고,
  이번엔 증명 줄이 그 트레이스를 대신한다.
- **캡처된 그래프 아래의 버퍼는 고정 예산으로 한 번만 할당한다.** "grown once and strongly held" 는
  주석이지 코드가 아니었다. 예산을 초과하는 계획은 재할당이 아니라 raise, 계획 함수가 예산을 안다.
- **공허한 게이트**: 비교는 실 데이터에서, 참조 노름에 바닥을 두고. rel=0.00e+00 이 4랭크에 똑같이
  찍히면 그건 통과가 아니라 무측정이다.
- 하네스: srv4 `~/stkernel` 이 19:23 에 통째로 사라졌다(원인 미상; 같은 시각 deneb 자율 상태 갱신).
  브랜치가 origin 에 있어 `~/stkernel-glmfix` 에 재클론해 이어갔다 — 작업 전 push 가 보험이다.

### 8. 운영자 지시(20:10) — 드래프터 bf16 원본 해제, 메가커널 세트를 프로덕션 기본값으로

"드래프터 bf16 원본 해제. 해체하고 마스터 메가커널 기본으로." 두 가지를 한 PR 로 올린다.

- **드래프터 bf16 해제**: `load_dflash_model` 이 패스·증명 설치 뒤 `maybe_free_fp8_dense_bf16(dflash_model,
  label="drafter")` 를 부른다(타깃과 같은 노브 `VLLM_GLM53_FP8_DENSE_FREE_BF16=1`). 안전 근거: 체크포인트
  걷기는 끝났고(`get_model` 반환 뒤), `_build_fused_kv_buffers` 는 `qkv_proj.weight` 의 k/v 반쪽을
  `torch.cat` 으로 이미 복사했으며, 컴파일된 forward 는 opaque op 로 fp8 사본·W4 팩만 읽고, 바이어스
  선형이 없다. 회수분은 KV 산정 전에 돌아온다(핑거프린트 `[fp8-dense] drafter: ... released 0.73 GB`).
- **메가커널 세트 기본값**: `VLLM_GLM53_MEGAKERNEL=1`, `MK_MHC=1`, `MK_GEMM=1`(MLA 는 이미 1), KDA 0 유지.
  브래킷된 "cand" 구성 = 프로덕션 기본. `ab-glm53.sh` 의 base 팔은 `MEGAKERNEL=0` 을 명시해 stock 을
  뜻하게 했다(안 그러면 base 가 조용히 MK 세트가 된다).
- **적용 부팅(20:36, 환경변수 없음, main 55eae48)**: 헬스 345 s. 핑거프린트 — `drafter: FREE_BF16=1:
  released 0.73 GB`(타깃 3.67 GB 보다 먼저), `selftest mla rel=2.70e-03 -> ARM`, `armed={'mhc': True,
  'gemm': True, 'kda': False, 'mla': True}`, `drafter lane serving: 30 of 31`, `mla shadow vs wrapper
  rel=1.34e-03 (T=9, rows<=18, real KV) -> ARMED`. Available KV 24.02 GiB(17:39 cand 부팅 23.48 → +0.54
  GiB = 해제분; KV 는 토큰 수로 고정이라 여유로 남는다). 스모크: C=1 step/s 15.0(워밍업)/16.6/16.3,
  raw acc 18~25%; 프리필 2048 2,523/2,646, 8192(23K) 2,696/2,645 tok/s, TTFT 8.55/8.71 s — mla 팔과 같다.
- **부수 발견 — 그리디 텍스트는 부팅 간 재현되지 않는다.** 같은 구성(MK 세트 + MLA + W4 드래프터)의 두
  부팅(19:33 mla 팔, 20:36 프로덕션)에서 온도 0 응답 6개 중 5개가 16~409 자 뒤에 갈렸다(p0 는 123 vs
  355 토큰). 아키텍처가 같아도 커널 선택·리덕션 순서가 부팅마다 달라 근접 동률 argmax 가 뒤집힌다.
  따라서 **그리디 diff 로는 서빙 수치 변경을 판정할 수 없다** — 게이트는 품질 9/9·한국어 0/16·수용률
  프로파일이고, §6 에 남긴 "off 부팅과 diff" 후속은 폐기한다.
## ★★메가커널 27차 — 서빙 PDL 은 꺼져 있었다, MoE 커널은 우리 레인과 같은 속도, L2-warm 은 W4 GEMM 에 7% (2026-09-04, srv2)

전략 문서(2026-09-04 아티팩트 "메가커널 대대적 개선 방안")의 후보 중 운영자가 고른 셋 — 서빙
PDL, AR 대기 중 다음 커널 L2 프리페치, MK_SEG_MOE go/no-go — 를 `probes/run_mk_probe.sh`(b12x
파일까지 마운트하는 일반 러너)로 srv2 빈 창(다른 세션의 DRAFTW4 브래킷이 끝난 뒤)에서 쟀다.

**1. 서빙 PDL (EXP-12).** 발견: `mk_pdl_enabled()` 는 env `VLLM_GLM53_MK_PDL` 을 읽는데 glm53.env
에도 `ab-glm53.sh` 의 cand 팔에도 없었고 벤치 프로브만 켰다 → 09-03 무장 부팅(21차 트레이스)의
173발은 전부 PDL 없이 돌았다. `probes/mk_pdl_graph_check.py`(그래프 캡처 안 체인): gemm→gemm→gemm 과
mhc→gemm 의 리플레이가 eager 와 비트 동일, 재리플레이 동일 — PDL on/off 모두. 24발 그래프
(n=k=4096, 8.4 MB 팩 24개 회전, DRAM-cold): **off 58.0 → on 53.6 µs/발사(−7.6%)**. 173발/스텝이면
−0.76 ms(1.1%). 프로필 기본 1, cand 팔에 명시. 수치 불변이라 EXP-6 브래킷의 cand 팔에 얹는다.

**2. MK_SEG_MOE go/no-go (EXP-14).** 서빙이 만드는 b12x 래퍼(같은 기하·같은 디스패치 오버레이;
C=1 은 64 pairs 라 static 백엔드)를 디코드 형상(8토큰·top-8)에서 고유 전문가 U 별로, 가중치
DRAM-cold(8 세트 순환, 세트당 1 GB), 그래프 리플레이로:

| U | µs/호출 | MB | GB/s |
|---|---|---|---|
| 8 / 16 / 24 | 148 / 311 / 451 | 28 / 57 / 85 | 191 / 182 / 188 |
| 32 / **40** / 48 | 573 / **720** / 859 | 113 / 142 / 170 | 198 / **197** / 198 |
| 56 / 64 | 985 / 1135 | 198 / 227 | 201 / 200 |
| MK W4 레인 n=6416 (12팩 그래프, PDL) | 76.7 | 15.0 | **196** |
| torch sum (읽기만, 180 MB) | 736 | 180 | 245 |

U=40 의 720 µs 는 21차의 층당 738 µs 와 맞는다 — 서빙의 고유 전문가 수가 ~40 이라는 것과
실효 ~197 GB/s 가 둘 다 확인됐다. **판정: b12x = 레인의 103% → 90% 규칙으로 축을 닫는다.**
남은 20%(196~200 vs 읽기 245)는 두 커널에 공통인 "발사 안 구조" 몫이다: 레인은 루프만 244
GB/s 이고 발사당 ~15 µs 의 고정비(4차)가 있으며, MoE 커널은 층당 한 발사라 고정비가 아니라
타일 구조다. persistent 스트림이 240 에 닿는다는 증거는 아직 없다(레인의 발사당 196~222). 그러니
MK_SEG_MOE 의 상한 −4~5 ms 는 그 가정 위에 있고, 착수하려면 먼저 2~3일짜리 "persistent 전문가
타일 스트림" 마이크로커널로 240 도달 여부를 재야 한다. 지금은 보류.

**3. AR 프리페치의 소비자 이득 (EXP-13 게이트 3).** n=6416 W4 GEMM 한 발: DRAM-cold 86.0 µs →
팩 15 MB 를 L2 에 미리 읽어 둔 뒤 79.9 µs(**−7%**). W4 GEMM 은 L2-warm 이어도 80 µs 다 — DRAM 이
아니라 발행(cp.async/LSU, 2차)에 묶여 있어서, 프리페치가 사는 것은 DRAM 지연분뿐이다. 회당
~10 MB 를 데워도 −4 µs/AR, 스텝당 100회면 **−0.4~0.6 ms(0.6~0.9%)**. 전략 문서의 −1.5~2.5 ms 는
"L2 는 DRAM 의 4배" 가정이었고 그 가정이 틀렸다. 구현은 랜딩했다(기본 off, 힌트 없으면 커널
byte-identical): 4랭크 disttest(`t_wait` 힌트/무힌트)와 브래킷은 부팅이고, 단독으로는 CV 1.7%
아래라 EXP-6+12 위에 얹어서만 판정한다.

**4. AR 커널 빌드 (EXP-13 게이트 1).** `probes/osar_build_check.py`: `prefetch.global.L2` 가 sm_121a
ptxas 를 통과하고 `oneshot_ar_hint`·`phase_counters` 가 묶였다. PASS.

**5. 드래프터 꼬리 인구조사 (09-03 트레이스, 깨끗한 디코드 스텝 5·6).** 샘플러 앵커에서 다음 스텝의
준비 커널까지 136 커널, 커널 합 6.28 ms(스팬 6.1~7.1): bf16 cutlass GEMM 33 × 122 = 4.0 ms(EXP-10 이
서빙되면 ~1.25), 타깃 헤드 fp8 814, `k_oneshot` 11 × 72 = 788(forward 안의 45 보다 느리다 — 꼬리
구간의 랭크 편차), `kernel_mha` 5 × 29 = 145, AllGather 2 × 54 = 108, 글루 ~50개 0.33 ms. 드래프터
메가커널의 상한 = 글루 0.33 + 발사 간극 ~0.35 + MK 발사 고정비 30 × ~10 µs ≈ **−0.8~1.0 ms
(1.2~1.5%)**; AR 0.79 ms 는 융합해도 남는다.

**6. 전략 문서의 상한 대 프로브 (정정).** 운영자 물음 "실질 이득이 적다면 왜 처음엔 크다고
봤나" 에 대한 답:

| 항목 | 전략 상한 | 프로브 뒤 | 틀린 가정 |
|---|---|---|---|
| AR 프리페치 | −1.5~2.5 ms | −0.4~0.6 | 소비자가 DRAM 바운드라는 가정. 2차의 "발행 병목" 을 L2-warm 에 적용하지 않았다 |
| MK_SEG_MOE | −4~8 ms | 닫힘 | MoE 190 을 레인의 루프 전용 244 와 비교. 같은 형태(그래프 안 발사당)면 레인 196 = MoE 197 |
| 샤딩 샘플링 | −0.4 ms | −0.1 | 25차의 AllGather 409~567 µs 는 프리필 문맥 스텝의 값. 깨끗한 디코드는 2 × 54 µs → **후보에서 내림** |
| 드래프터 메가커널 | −1~1.5 ms | −0.8~1.0 | 대체로 맞음(위 5) |
| 서빙 PDL | −0.9~1.7 ms | −0.76 | 범위 안 |

공통 원인: 상한을 목표와 같은 측정 형태(그래프 안 발사당, 깨끗한 디코드 스텝, 확인된 병목)로
만들지 않았다. 남은 큰 항목 — k 축소(SPEC=0 대 k=7 실측에 앵커), W4 팔(발사당 실측), EXP-7(호스트
유휴 실측) — 은 그 형태의 측정 위에 있다.

**교훈**: (1) 노브에 독자가 있어도 서빙이 그 값을 나른다는 보장은 없다 — 프로브가 켜는 env 는
프로필에도 있어야 하고, 무장 부팅의 fingerprint 에 찍혀야 한다. (2) "대역폭 바닥" 판정은 같은
바이트를 우리 커널이 얼마나 빨리 흘리는지와 견줘야 한다 — 역산값끼리 맞는 것은 검증이 아니다.
(3) L2-warm 이 이득이 되려면 소비자가 DRAM 바운드여야 한다 — W4 GEMM 은 아니었다.
(4) 상한은 목표와 같은 측정 형태로 — 루프 전용 스탬프·프로파일러 꼬리·미확인 병목 가정으로 만든
상한은 프로브 하나에 절반 이하로 준다.

## ★★★26차 — 부팅의 메모리 절벽: fp8-dense 패스가 로드 중에 여러 번 돌았고, W4 팩 빌더가 예약 메모리를 흘렸다 (2026-09-04, 4노드 계측)

드래프터 W4 브래킷(25차 승인)을 띄우자 base 팔 첫 부팅이 로드 시작 13초 뒤 죽었다. srv1 은
커널 OOM 킬러(avail 33 MiB), 나머지 셋은 earlyoom SIGKILL(3.7%). 워커 RSS 는 7 GiB 뿐이라 `ps`
로는 아무것도 안 보였다. 노드마다 1초 `/proc/meminfo` 샘플러를 심고 세 번을 더 죽였다.

**궤적(srv2, 안전 로더 부팅, MemAvailable GiB)**: 100 → 52(파라미터 46 GiB, 4초) → 43(첫 샤드
읽는 25초: page cache +12, anon +6) → **26 → 5(4초)** → 사망. 네 노드가 동시에, 같은 모양으로.
`LOAD_FORMAT=auto` 도 `instanttensor` 도 같다 — 로더가 아니다. `VLLM_GLM53_FP8_DENSE=0` 이면
둘째 계단이 없다(바닥 43 GiB) — **fp8-dense 패스다.**

**패스 안 단계별 계측**(`torch.cuda.memory_reserved / max_memory_allocated`, 오버레이 사본을
4노드에 직접 심음):

| 선형 | 단계 | cuda free | torch reserved | peak alloc |
|---|---|---|---|---|
| in_proj [6416×4096] | 양자화 | 41.4 | 45.3 | 45.3 |
| | 복사 검사 | 41.1 | 45.3 | 44.9 |
| | **W4 팩 attach** | **26.0** | **59.8 (+14.5)** | 45.1 |
| o_proj [4096×1024] | W4 팩 attach | 21.1 | 64.6 (+4.7) | 45.1 |
| gate_up [6144×4096] | W4 팩 attach | 13.0 | 72.5 (+7.6) | 45.2 |

**범인 1 — `build_mk_weight_w4`**: 16후보 스케일 탐색이 청크(4M 원소)마다 후보당 임시 텐서
십여 개를 만들고, 통합 메모리 GB10 의 캐싱 할당자(expandable segments)가 그 churn 에 해제
블록을 재사용하는 대신 **새 페이지를 매핑**한다. 살아 있는 텐서는 +0.2 GiB 인데 예약이
+14.5 GiB. 선형 180개면 노드 하나다. 예약된-빈 블록은 이 기계에서 호스트가 잃은 메모리다.

**범인 2 — 훅이 한 번이 아니었다**: `AutoWeightsLoader` 는 스트리밍 반복자를
`itertools.groupby` 로 프리픽스 연속 구간마다 자식 `load_weights` 를 호출한다. "로더가 돌아온
뒤" 라고 믿고 `Glm5NextForCausalLM.load_weights` 에 둔 훅은 실제로는 로드 25초 지점(첫 샤드
경계)에 **미로드 가중치 위에서** 돌고, 구간마다 다시 돈다 — 매번 fp8 사본 180 + W4 팩 180 +
검증 GEMM 360. 설계는 수치엔 옳았고(나중 호출이 재빌드) 메모리엔 눈이 없었다.

**이게 그동안의 사고다.** 09-03 srv3 OOM("덴스 패스 안에서 죽었다"), 09-04 새벽 srv1·srv2
wedge — 전부 이 절벽에서 몇백 MiB 차이로 살거나 죽었다. earlyoom 이 들어온 뒤(11:00) 결정적
SIGKILL 이 되면서 비로소 측정됐다.

**수정(46431f7)**: (1) 후처리 훅 4개를 `run_post_load` 로 묶어 멀티모달 래퍼
`Glm5NextForConditionalGeneration.load_weights` 가 자기 로더가 돌아온 뒤 **1회** 호출(자식은
소유자가 없을 때만 스스로 실행); (2) 패스가 선형마다 `finally: torch.cuda.empty_cache()`;
(3) 팩 빌더 청크 4M→1M 원소 + 끝에 `empty_cache()`(KDA 팩은 첫 forward 안에서 만들어지므로
남는 예약은 프로파일러가 KV 예산에서 뺀다).

**수정판 부팅 실측(16:45, instanttensor, 같은 계측)**: 로드 중 조기 패스 **0회**(로더 51% 지점
바닥 45.4 GiB — 수정 전엔 이 지점에서 죽었다), 100% 로드 뒤 패스 **1회**: 선형 180개를 지나는
동안 cuda free 54.4 → 51.8 GiB(= fp8 사본 + W4 팩의 상주분 2.6 GiB, 그 외 0), torch reserved
46.0 → 47.8, peak 48.0 — 예약 증식 없음. 부팅 전체 바닥 srv2 34.5 / srv4 40.3 GiB(수정 전
1.6~8). 핑거프린트 "180 linears w8a8, 180 MK W4 packs".

**부수 관찰**: (a) 죽은 부팅 다음 부팅은 NCCL `tp:0` 초기화에 4~5분이 걸린다(정상 1초; 잔여
RDMA 상태 추정, 결국 진행됨). (b) `pgrep -f "VLLM::Worker"` 는 earlyoom 의 `--prefer` 인자도
잡는다 — `^VLLM::Worker`. (c) 원격 `pkill -f <script>` 는 그 문자열을 담은 자기 ssh 를 죽인다.

## ★★★메가커널 25차 — 24차 온디바이스 마감, 그리고 디코드 스텝의 꼬리: 드래프터가 임계경로에 있다 (2026-09-04, srv2)

운영자 "glm flash 커널개선작업". 착수 순서는 원장의 미실측부터 — 24차가 "GPU 가 필요한 셋"을
남겨 두었다.

**1. 24차 온디바이스 마감.** main(054a864) 과 24차 직전(0e6e4a9) 을 srv2 에서 교차(main→pre→main).

| W4 (mk_us, x2) | n=6416 | n=4096 | n=2048 | n=1024 | by-design |
|---|---|---|---|---|---|
| 24차 직전 | 84.0 / 79.4 | 59.4 / 56.3 | 41.0 / 37.8 | 30.7 / 27.7 | 0.125 |
| main (e4m3 스케일) | 84.0~85.0 / 79.4~79.9 | 60.5~61.4 / 56.4~57.3 | 41.0 / 37.8~37.9 | 30.7 / 28.5~28.7 | **0.086** |

전개 +16 SASS 의 비용은 노이즈 안(≤1 µs), 벤치의 무작위 가중치에서도 by-design 오차 −31%.
자가진단은 "selftest gemm exact=3.92e-08 -> ARM". 그런데 **벤치의 exact 게이트가 FAIL** 이었다 —
프로브 `probe_exact` 가 여전히 sexp∈[-5,6] 를 뽑는데, 새 팩은 중앙값 정규화 뒤 e∈[-5,5] 라 2^6
그룹(8.3%)이 클램프돼 roundtrip 6.63e-2. 팩의 표현 범위가 바뀌면 그 픽스처도 같이 움직여야
한다(자가진단은 24차에서 [-12,-2] 로 옮겼고 프로브는 아니었다). 프로브를 같은 분포로 고쳤다:
roundtrip 0.00e+00, exact grid 3.92e-08, over-ulp 0, **VERDICT PASS**. 셋 중 남은 하나
"품질 이동" 은 브래킷 몫.

**2. 스텝의 꼬리.** 21차는 `execute_context_0(0)_generation_1(8)` 주석 안만 봤고, 그 주석은
타깃 forward 다(스트림 17·203 에 하나씩 → 6개 주석 = 3 스텝; 21차 표의 ms/step 은 절반 값,
비중은 유효). 준비 커널(`_gather_block_tables_kernel`)에서 자르면 스텝 66~71 ms = **forward
59~62 + 꼬리 7.5~8.5 ms**, 꼬리 동안 GPU 95% 바쁨, 다음 스텝의 준비가 꼬리 끝에서 시작.

| 꼬리(µs, 스텝 6) | 무엇 |
|---|---|
| 836 + 409 | 타깃 헤드 fp8(W8A16, 158 MB, ~190 GB/s) + 로짓 AllGather |
| **792** | **드래프터 fc, bf16 cutlass** [7×20480]×[20480×4096], 168 MB, 212 GB/s — 9/1 메모의 "deep_gemm fp4 809 us" 는 오귀속 |
| 5 × ~660 | 층당 GEMM 51+66+43+52+236+118 ≈ 567 us bf16(200~235 GB/s = bf16 DRAM 하한) + AR 2회 + 글루 |
| 812 | 드래프트 헤드 fp8(W8A16) |

즉 **드래프터의 bf16 바이트 3.2~3.6 ms/스텝(5%)이 디코드에 남은 가장 큰 커널 쪽 레버**다.
"D≈0" 은 9/1 프로파일러 부팅에서 호스트 유휴 뒤에 숨었을 때의 값이었다. 부수: 드래프터는
이 부팅에서 TP=4 로 돈다(층마다 o/down 뒤 `k_oneshot`, qkv 12.6 MB 스트림) — 프로필의
`DRAFT_TP=1` 은 서빙에 닿지 않았고, 대역폭으로는 TP=4 가 맞다(랭크 0 단독이면 층당 400 MB).

**3. 프로브 — 이번에 처음 돌았다.** `probes/drafter_fc_check.py` 는 첫 팔에서 즉사하는 코드였다
(참조 matmul 의 가중치 전치, 그래프 헬퍼가 `fn(i)` 를 부르는데 팔이 인자를 안 받음). 실형상
(config.json: hidden 4096, 5층, 32/8 헤드, inter 12288, conv proj [1024,4096]), TP=4 샤딩,
가중치 순환(가장 작은 팔의 바이트가 L2 의 4배를 넘게), 그래프 리플레이:

| 랭크당 형상 | bf16 | fp8 pair | MK W4 |
|---|---|---|---|
| fc [4096×20480] | 682 | 489 | **301** (K-chunk 5) |
| qkv [1536×4096] ×5 | 60.5 | 33.5 | 22.2 |
| o [4096×1024] ×5 | 39.4 | 25.8 | 16.7 |
| gate_up [6144×4096] ×5 | 212 | 175 (144 GB/s) | 73.6 |
| down [4096×3072] ×5 | 104 | 83 | 40.5 |
| kernel_projection [1024×4096] ×10 | 46.8 | 26.1 | 18.7 |
| **스텝 합 (M=7)** | **3.23 ms** | 2.34 (−1.3%) | **1.25 ms (−2.0 ms, −3.0%)** |

deep_gemm fp8×fp4 팔은 이 형상들에서 assertion(SKIP). 헤드 형상 [38720×4096] 참고: 1306 / 918 /
**418** — 서빙 헤드 둘(812·836)을 W4 로 하면 각 −400 µs 지만 타깃 헤드는 서빙 로짓(운영자
결정), 드래프트 헤드는 수용률 게이트(다음 후보).

**4. 배선(랜딩, 부팅 없음).** `VLLM_DFLASH2_FP8_DENSE=1` 이 드래프터 dense 선형 31개 전부를
덮는다 — 전엔 base 패턴이 o/gate_up/down 만 맞아(병합 `qkv_proj`·`fc`·`kernel_projection` 은
목록에 없었다) 바이트의 43% 가 bf16 으로 남았다. 드래프터 forward 는 torch.compile 되므로
(`@support_torch_compile`) GEMM 마다 불투명 커스텀 op 하나(`gemm_mk_or_fp8`)가 런타임에 MK-or-fp8
을 고른다 — dynamo 가 레인의 자격 검사(토큰 수 가드)와 pybind 호출을 추적하게 두면 안 된다.
fc 는 K=20480 이라 레인의 K 계약(4096)을 K-chunk 팩 5개로 넘고 `gemm_w4a8` 이 fp32 로 합산
(커널·smem 예산은 그대로 — 173발/스텝을 섬기는 커널을 건드리지 않는다). `w8` = fp8 pair 만.
**오프라인 게이트 `probes/drafter_dense_path_check.py`(srv2) PASS**: 31 선형 팩 부착(fc 는
5-chunk 리스트), M=7/8/32 에서 apply == 레인 비트 동일(rel 8.5~8.8e-2, e2m1 급), M=40 은 fp8
pair 로 비트 동일 폴백, `torch.compile(fullgraph=True)`·`dynamic=True` 모두 eager 와 비트 동일,
CUDA-graph 리플레이 비트 동일, `w8` 은 팩 0개.

**미측정 — 게이트는 부팅이다**: 수용률(pos-1 ±2 pct), C=1 step/s, 같은 부팅의 프리필. 기대값
(정직) W4 −2.0~2.4 ms/스텝 = −3~3.5%(수용률이 버틸 때), 대안 `w8` −0.9~1.2 ms. 드래프터의 bf16
원본은 이번엔 안 놓는다(+~0.57 GB/rank; `_build_fused_kv_buffers` 가 k/v 반쪽을 로드 시 복사하니
놓을 수 있어 보이지만, 그건 제 부팅을 가진 별도 변경).

**교훈**: (1) 주석 하나로 자른 스텝은 그 주석 밖을 못 본다 — 준비 커널에서 자르고 주석은
스텝 안에 완전히 들어오는 것만 forward 로 본다. (2) 프로브는 쓸 때가 아니라 **돌 때** 측정된다
(이 프로브는 이틀간 "숫자가 나온 뒤 커널" 의 관문이었는데 한 번도 실행되지 않았다). (3) 팩의
표현 범위를 바꾸면 그것을 재는 모든 픽스처(자가진단·프로브·테스트)가 같이 움직여야 한다.

## ★★메가커널 24차 — W4 팩이 프로덕션 그룹을 전부 클램프하고 있었다 (2026-09-04)

운영자 "nvfp4와 메가커널을 배타 아니게 못하나" → "2번(MK-GEMM에 nvfp4 레인)" → "그게 최고야?" → "8lut이랑 비교해봐" → "진행해". 그 과정에서 **살아 있는 결함**이 나왔다.

**1. sm_121a 에는 fp4 텐서코어가 없다.** ptxas 직접 확인:

```
mma.sync ... kind::mxf4nvf4.block_scale   not supported on .target 'sm_121'
mma.sync ... kind::mxf4.block_scale       not supported on .target 'sm_121'
mma.sync.aligned.m16n8k64 ... e2m1.e2m1   not supported on .target 'sm_121'
mma.sync.aligned.m16n8k32 ... e4m3.e4m3   OK   ← MK-GEMM 현재
```

따라서 nvfp4 는 이 기계에서 **산술 이득이 없고**(어차피 e4m3/bf16 으로 풀어서 곱한다), 팩 크기도 우리와 같은 0.5625 B/param 이다. 차이는 스케일 정밀도 하나뿐이었다. (23차의 "상호 배타" 서술은 22차·23차 PR에서 MK-GEMM 한정으로 이미 좁혔다.)

**2. W4 팩이 프로덕션에서 3배 나쁘게 양자화되고 있었다.** 전개가 바이트 덧셈이라 그룹 스케일이 크기 1 기준 2^-5 까지밖에 못 내려가는데, GLM-5.3 덴스 투영은 중앙값 2^-7, p1 2^-16 이다.

```
self_attn.q_proj  needed exponent  min=-18  p1=-16  median=-7  p99=-6
                  main 하한(-5)에 걸린 그룹: 100.0%
```

| 팩 구성 (layers.1 실가중치) | q_proj | down_proj |
|---|---|---|
| main 현재 (pow2 + amax 커버, 클램프) | 0.2259 | 0.2394 |
| + 텐서당 pow2 정규화 | 0.1176 | 0.1138 |
| + e4m3 그룹 스케일 & 오차최소 선택 | **0.0816** | **0.0767** |
| (정확 산술 하한, 도달 불가) | 0.0812 | — |

**−64~68%.** nvfp4 스케일과 동등한 지점이다.

**왜 안 보였나**: exact 게이트가 `sexp ∈ [-5,6)`, 즉 O(1) 크기 가중치만 뽑는다 — 클램프가 유일하게 안 물리는 구간. by-design 게이트는 GEMM **출력**을 보는데 K=4096 평균이 3배 가중치 오차를 0.15 톨러런스 아래로 감춘다. 게이트를 프로덕션 크기로 바꿨다.

**3. 세 변경은 함께여야 값이 난다.** 커버 규칙으로는 스케일 격자를 촘촘히 해도 **더 나빠진다**(그룹을 e2m1 격자의 성긴 위쪽으로 민다). 오차최소 선택은 2 옥타브 × 8 가수 = 16후보로 전체 72후보 최적과 정확히 일치한다.

**바이트 산술**: 스케일 가수 3비트를 e4m3 바이트에 더하는 것이 곱셈이다(e2m1 크기가 가수 1비트라 자리올림이 최대 1회, 지수 필드로 정확히 넘어간다). 보정은 크기 가수 1.5인 코드 3·5·7 에 `1≤k≤5` 일 때 +1 뿐이고 **k 에 무관**하므로 상수 2개 + 1비트 select 로 끝난다. SASS 4그룹당 64 → 80.

**검증(GPU 없이)**: 바이트 공식이 모든 (code, k, e) 조합에서 `e4m3(mag × scale)` 재현(테스트가 전수 스윕 + NaN 인코딩 부재 확인), 온그리드 왕복 비트 완전 일치(0.000e+00), sm_121a 컴파일 통과, 44,342 checks OK.

**미측정**: 온디바이스 자가진단, 전체 커널 안에서의 전개 비용, 그리고 **낮아진 가중치 오차가 서빙 품질을 움직이는지**. 셋 다 GPU 가 필요하고, 여기 숫자가 오르기 전까지 품질 이득은 미실측이다.

**교훈**: 22·23차와 같은 계열의 세 번째다. 22차 "무장했는데 안 돈다", 23차 "안 도는데 값은 치른다", 24차 **"돌긴 도는데 자가진단이 안 보는 구간에서만 나쁘다"**. 셋 다 게이트가 프로덕션 조건을 대변하지 못해서 생겼다.

## ★★메가커널 23차 — srv3 호스트 OOM: nvfp4 첫 부팅이 덴스 패스 안에서 죽었다 (2026-09-03)

09-03 15:10 스택 4노드 전부 다운. 원점은 srv3 이고, 나머지는 헤드의 TCPStore 가 닫히며 따라 죽었다(srv4 rank3 은 `armed=` 까지 정상 도달 후 `recvValue failed ... Did the remote server shutdown or crash?`).

커널 덤프(srv3, 15:10:31):

```
Node 0 Normal  managed 119 GiB   free 4.00 GiB   min 4.00 GiB
               active_anon 55.4 GiB   inactive_anon 2.4   shmem 2.8
               all_unreclaimable? yes
oom-kill: constraint=CONSTRAINT_NONE, global_oom
Killed process 362418 (VLLM::Worker_TP) total-vm:664 GiB anon-rss:3.5 GiB
```

free 가 min 워터마크와 **같은 값**이고 회수 가능분이 0이다. 스왑 16 GiB 는 GPU 매핑 페이지라 쓰이지 못한다.

**직전 정상 부팅과의 차이는 노브 하나**: `VLLM_GLM53_FP8_DENSE` 가 `w8a8` → `nvfp4`. 그리고 이번 부팅은 **그 패스 안에서** 죽었다 — 패스 끝에 찍히는 `[fp8-dense] ... fingerprint` 줄이 **네 랭크 어디에도 없다**(메가커널 확장은 이미 빌드된 뒤인데도).

GB10 은 CPU/GPU 가 한 풀이다. `--gpu-memory-utilization 0.7529` 가 GPU 쪽에 ~90 GiB 를 예약하므로 같은 램에서 호스트 몫은 **~30 GiB** 뿐이고, 그 안에서 로더와 양자화 사본이 전부 돌아야 한다.

**그 피크에 죽은 사본 둘(#263 에서 제거)**:

| 항목 | 크기/rank | 왜 죽은 사본인가 |
|---|---|---|
| MK W4 팩 | ~1.0 GB | 카피 체크·스킴 분기보다 **앞**에서 전 레이어에 빌드. `NvFp4DenseMethod.apply` 는 nvfp4 커널로 직행하고 팩을 한 번도 읽지 않는다 |
| nvfp4 트리플 2중 | ~1.0 GB | `_quantize_nvfp4` 는 `alpha_scale` 을 읽지 않는데 재시도 루프 안에 있었다. 우변이 재바인딩보다 먼저 만들어져 동일 트리플 2개를 동시 보유 |

**bf16 수명은 레버가 아니다.** 빌드 패스 안에서 놓는 변경은 이미 부팅 하나를 죽였다(`AutoWeightsLoader` 가 체크포인트를 다 훑기 전 자식 `load_weights` 로 들어와 빈 텐서를 만난다 → `IndexError: tuple index out of range`). 그래서 해제가 별도 패스이고, 이제 그 계약은 테스트로 못박혔다.

**부수 발견 — nvfp4 와 메가커널은 상호 배타다.** `NvFp4DenseMethod` / `W4A8DenseMethod` 의 `apply` 는 MK 경로를 타지 않으므로, 그 팔이 가져간 레이어에서는 **MK-GEMM 이 돌지 않는다**. `_kda_ensure_packs` 가 `isinstance(in_m, Fp8DenseMethod)` 를 요구하므로 **MK-KDA 의 in_proj 팩도 함께 사라진다**. 21차 트레이스의 `mk_gemm 7.00 ms/step` 은 `w8a8` 부팅의 값이고, nvfp4 부팅에서는 그 항목이 통째로 없어진다. 이제 핑거프린트가 팩 개수를 세고, 0개면 그렇다고 말한다.

**미측정**: 절감은 추정 ~1~2 GB/rank 이고 이것만으로 OOM 이 걷히는지는 다음 부팅이 답한다. GMU 레버(0.7529)는 그대로 남아 있다.

**교훈**: 22차와 같은 계열이다. 22차는 "무장했는데 안 돈다", 23차는 "안 도는데 값은 치른다". 둘 다 **어떤 코드가 실제로 서빙하는지를 아무도 말해주지 않아서** 생겼다.

## ★★메가커널 22차 — "무장" 이 "서빙" 을 뜻하지 않았다: KDA 레이아웃 게이트의 침묵 (2026-09-03)

운영자 "해봐"(KDA 무장). 무장 노브를 켜러 갔더니 **이미 켜져 있었고, 그런데도 한 번도 나간 적이 없었다**.

라이브 컨테이너 env: `VLLM_GLM53_MK_KDA=1`, 부팅 로그: `armed={'mhc': True, 'gemm': True, 'kda': True, 'mla': True}`, 자가진단: `kda delta variant 1 matches stock (out=5.95e-04, conv=0, rec=1.14e-07)`. 그런데 트레이스에 `mk_kda_kernel` 이 없고, 그림자 로그(`kda shadow #`, 64회마다 1줄)도 **0줄**이다.

라이브에서 직접 확인한 값:

```
layout= SD   dim_first= False   env VLLM_SSM_CONV_STATE_LAYOUT= None
```

기전은 이렇다.

1. `_selftest_kda` 는 **자기 DS 픽스처**를 만들어 돌린다 → 통과 → `_ARMED["kda"]=True`.
2. 핫패스 `_kda_layout_ok` 는 `layer._conv_state_dim_first` 에서 **모든 프로덕션 레이어를 부팅 내내 거부**한다. vLLM 기본은 `SD (state_len, dim)`, 이 커널은 `DS (dim, state_len)` 을 인덱싱한다(`mamba_utils.get_conv_state_layout` 기본값 `"SD"`).
3. 거부가 맨 불리언이라 **아무 말도 하지 않는다**. 그림자도 같은 게이트 뒤에 있어 함께 막힌다 — 그래서 0줄.

게이트 자체는 옳다(SD 상태를 DS 로 읽었으면 OOB). 결함은 게이트가 아니라 **침묵**이고, 그 침묵이 "armed" 를 "serving" 으로 읽히게 만들어 캠페인 하나를 통째로 오도했다. 21차의 원인 진술도 그래서 틀렸다(정정 완료).

**고친 것(#260)**: `_kda_layout_reason` 이 실패한 술어를 돌려주고 이유마다 한 번씩 로그(SD 이유는 고치는 env 를 이름으로 말한다) / 무장 시점에 프로세스 레이아웃과 대조해 경고 / 런처가 `VLLM_GLM53_MK_KDA` 에서 `VLLM_SSM_CONV_STATE_LAYOUT=DS` 를 파생하고 충돌 값이면 부팅 중단.

**검증**: 합성 모듈 CPU 실행 — SD 는 env 를 이름으로 말하고, DS 는 통과, 반복 거부에 경고 1회, 과도기 사유(상태 미부착)는 별도 줄. 런처 4경우(off/on+unset/on+DS/on+SD) 의도대로. 41,483 checks OK.

**미측정**: 다음 재기동부터 MK-KDA 가 실제로 디코드에 나간다. 기대 이득은 21차 트레이스 기준 스텝당 ~0.4 ms 이며, 수치가 바뀌므로 브래킷 대상 — 여기 숫자가 오르기 전까지는 미실측이다.

**교훈**: 자가진단이 만든 픽스처는 프로덕션 텐서의 **레이아웃**을 대변하지 못한다. 자가진단이 통과했다는 사실과 커널이 실제로 나간다는 사실은 다른 명제이고, 둘을 잇는 유일한 증거는 트레이스이거나 — 이제는 — 거부가 스스로 말하는 로그다.

## ★★메가커널 21차 — 무장 상태의 디코드 스텝 실측: 대역폭 포화이고 커널 여지는 끝났다 (2026-09-03)

운영자 "메가커널 디코딩 개선". 추정 대신 **실제 서빙 트레이스**(09-03 05:15 캡처, MK-GEMM/MHC/MLA 무장)에서 순수 디코드 스텝(`execute_context_0(0)_generation_1(8)`, 6개, 평균 56.3 ms)만 뽑아 귀속했다.

| 커널 | ms/step | 호출/step | 비중 |
|---|---|---|---|
| MoE (flashinfer b12x cutlass) | **15.5** | 21 | 47.7% |
| **mk_gemm_kernel** | **7.00** | 90 | 21.5% |
| osar `k_oneshot` (AR) | 4.70 | 45.5 | 14.5% |
| cutlass bf16 wmma | 1.26 | 67 | 3.9% |
| **mk_mhc_kernel** | 1.06 | 44.5 | 3.3% |
| **mk_mla_kernel** | 0.53 | 5.5 | 1.6% |
| stock `fused_recurrent_gated_delta_rule` | 0.48 | 17 | 1.5% |
| 나머지 | < 0.5 each | | |

**1. GPU 는 놀지 않는다.** 스텝별 점유율(스트림 병합 후) 95 / 99 / 95%. 50 µs 넘는 틈은 스텝당 6개, 합쳐 1.8 ms 뿐이고 그나마 인덱싱 elementwise 뒤다. 캠페인의 출발점이던 "발사 간극"은 이미 닫혀 있다.

**2. mk_gemm 의 긴 꼬리는 결함이 아니라 MoE 와의 대역폭 경합이다.** 호출 분포가 p50 59.5 / p95 177.8 / max 263.8 µs 로 벤치 최악(84 µs)을 크게 넘어 의심했으나, 110 µs 이상 호출(41개, 평균 158 µs)은 **자기 시간의 85%를 MoE 커널과 겹쳐** 돌고 110 µs 미만(125개, 평균 57 µs)은 4%만 겹친다. 레이어마다 4회 패턴에서 첫 호출만 느린 것도 이것으로 설명된다.

**3. 스텝은 전문가 가중치 대역폭에 묶여 있다.** MoE 가 52.6 ms 스텝의 30.7 ms(58%)를 차지한다. 검산: 레이어당 고유 전문가 ~40개 × 랭크당 3.1 MB(nvfp4, TP=4) × 42 MoE 레이어 ≈ 5 GB/step, 190 GB/s 면 ~27 ms — 실측 30.7 과 맞는다.

**결론: 메가커널의 커널 단계는 끝났다.** MK 세 커널 합계는 8.6 ms/step 이고, 그중 GEMM 은 이미 W4 대역폭 바닥에서 돌며 느린 구간은 MoE 와 겹쳐 있다. 남은 것은 커널 밖이다:

- **MoE 바이트**(47.7%): 우리 모듈이 아니고 이미 nvfp4.
- **AR 고정비**(14.5% 중 회당 22.7 µs): 20차 참조, GPU-initiated RDMA 가 본 수술.
- **MK-KDA 는 서빙에서 아예 안 돈다**(트레이스에 `mk_kda_kernel` 부재). ~~`KDA_SHADOW=1` 이라 그림자만 돌아서~~ — **정정(22차)**: 원인은 그림자가 아니라 conv-state 레이아웃 게이트다. #220 으로 state-index 계약을 고친 뒤라 무장 자체는 가능하지만, 이득은 트레이스 기준 **스텝당 ~0.4 ms**(stock recurrent 0.48 + conv 0.13 + gate 0.48 을 융합 커널이 흡수) — 픽스처의 2배 격차는 stock fp8 GEMM 을 포함한 비교였기 때문이고, 서빙에서는 그 투영이 이미 mk_gemm 으로 빠르게 돈다.

**교훈**: 커널 벤치의 우열은 서빙 스텝의 우열이 아니다. mk_gemm 은 벤치에서 84 µs 인 형상이 서빙에서 264 µs 로 보이는데, 그건 느려진 게 아니라 MoE 와 대역폭을 나눠 쓰며 **겹쳐서** 도는 것이다.

## 메가커널 20차 — two-shot all-reduce 기각: 병목은 바이트가 아니라 고정비 22.7 µs (2026-09-03)

19차에서 "전송 지배면 two-shot 으로 바이트를 절반" 이라고 적었다. 짓기 전에 두 데이터점으로 모델을 세우니 **two-shot 은 여기서 25% 느리다.**

**두 점**: 부팅 자가검증은 `(6, 4096)` bf16 = 48 KiB 를 쓰고 그때 회당 ~27 µs(장부 08-11), 실부하는 상한 256 KiB 에서 45.5 µs(n=6922). one-shot 은 회당 3×페이로드를 수신하므로 수신량은 144 KiB 와 768 KiB.

```
유효 수신 대역폭 = (768-144) KiB / (45.5-27.0) µs = 34.5 GB/s   ← dual HCA 가설 확인
고정비          = 27.0 - 144 KiB/34.5 GB/s      = 22.7 µs
검산            = 22.7 + 768 KiB/34.5 GB/s      = 45.5 µs (실측 45.5)
```

즉 **회당 45.5 µs 는 고정비 22.7 + 전송 22.8** 이다. 반반이다.

| 방식 | 256 KiB 기준 |
|---|---|
| one-shot | 22.7 + 22.8 = **45.5 µs** |
| two-shot(RS+AG) | 2 × 22.7 + 11.4 = **56.8 µs** |
| one-shot (48 KiB) | 22.7 + 4.3 = 27.0 µs |
| two-shot (48 KiB) | 2 × 22.7 + 2.1 = 47.6 µs |

two-shot 은 바이트를 절반으로 줄이는 대신 **동기 지점을 두 번** 지난다. 고정비가 전송과 맞먹는 이 패브릭에서는 어느 크기에서도 진다(작은 메시지일수록 더 크게 진다). 메시지 상한이 256 KiB(그 이상 NCCL)라 "충분히 커지면 이긴다"는 구간도 없다.

**따라서 지렛대는 고정비 22.7 µs 다.** 구성 요소는 커널이 `tx_seq` 를 올림 → CPU 프록시 스레드가 그걸 보고 ibverbs 로 3개 write 게시 → NIC 전송 → 피어 NIC 가 플래그 기록 → 커널이 플래그 폴링. CPU 가 임계 경로에 있다. 후속 후보는 (a) 고정비 분해 계측(게시/관측/완료/관측 4점), (b) GPU-initiated RDMA(IBGDA류)로 프록시를 임계 경로에서 제거. (b) 는 패브릭 모듈의 큰 수술이라 (a) 다음이다.

**이 모델은 반증 가능하다**: 19차에서 심은 크기별 wait 카운터가 다음 부팅에 정확한 2점 적합을 준다. 자가검증 27 µs 는 장부의 "27µs급" 이라는 근사 표기라 기울기가 달라질 수 있고, 그러면 결론도 다시 본다.

## 메가커널 19차 — all-reduce 의 wait 38.7 µs 는 무엇인가 (2026-09-03, 확인 중)

운영자 "확인해봐". 서빙 로그의 osar 위상 분해(n=6922, 실부하):

| 위상 | µs/collective |
|---|---|
| guard | 0.6 |
| copy | 3.5 |
| **wait** | **38.7 (85%)** |
| reduce | 2.6 |
| 합계 | 45.5 |

레이어당 2회 × 45층 ≈ 스텝당 99회이므로 **wait 만 스텝당 약 3.8 ms**(66 ms 스텝의 6%). 부팅과 달리 매 스텝 반복된다.

**wait 의 정의를 코드에서 확인**(`dsv4_oneshot_ar.cu`): 피어 3개의 `rxf` 플래그가 올라올 때까지의 스핀이다. 즉 **도착 편차뿐 아니라 RDMA 전송 시간이 포함**된다. one-shot 은 랭크마다 자기 버퍼를 피어 3개에 통째로 쓰므로, 받는 쪽은 회당 **3 × 페이로드**를 수신한다.

**크기 검산**: MAXEL 131,072개 bf16 = 256 KiB 가 상한(그보다 크면 NCCL 폴백). 상한에서 랭크당 수신 768 KiB.

| 가정 | 예상 wait |
|---|---|
| 단일 200G 실측 17.8 GB/s | ~43 µs |
| dual HCA 가 함께 받을 때 | ~22 µs |
| **실측** | **38.7 µs** |

전송 지배로 보이지만 **확정은 아니다**. dual HCA 쪽 가정이면 16 µs 가 편차 몫으로 남는다. 둘은 구분 가능하다 — **전송은 메시지 크기에 비례하고 편차는 아니다.**

**심은 계측**: 같은 `t3 - t2` 를 128 KiB 경계로 갈라 누적한다(`t_wait_sm`, `t_calls_sm`). 디코드 페이로드는 hidden 4096 × 토큰 × 2 B 라, C=1 형태(8토큰 = 64 KiB)와 4-시퀀스 스펙 배치(32토큰 = 256 KiB)가 경계 양쪽에 놓인다. 두 필드는 기존 `pad[3]` 에서 꺼냈다 — tx/rx 오프셋이 움직이면 피어들이 등록한 주소가 전부 무효가 되기 때문이다. 프록시가 구간마다 양쪽을 함께 찍는다.

**다음 부팅이 답하면**: 큰 쪽 wait 이 작은 쪽의 ~4배면 전송 지배 → 지렛대는 **선 위의 바이트**이지 스케줄링이 아니다. 그 경우 two-shot(reduce-scatter + all-gather)은 수신량을 3×P → 1.5×P 로 반으로 줄인다(동기 지점 한 번 추가). 상한 크기에서 회당 ~16 µs, 스텝당 ~1.6 ms 이고 작은 메시지에서는 상쇄되므로 **크기 적응형**이어야 한다. +12.6% 를 만든 모듈이라 플래그 뒤에서 브래킷으로만 건드린다.

## ★메가커널 18차 — 가중치 로드 296초는 우리 벤치가 만든 숫자였다 (2026-09-03)

운영자 "1번 해결"(= 부팅 최대 항목인 가중치 로드 296초). **해결할 296초가 애초에 없었다.**

| 부팅 | Loading weights | 그때 이 세션이 한 일 |
|---|---|---|
| 09-02 23:22 | **296.4 s** | srv1~4 전체에서 메가커널 벤치·컴파일 연속 실행 |
| 09-03 00:15 | **294.0 s** | 부팅 캐시·스탬프 작업(빌드/컨테이너 반복) |
| 09-03 04:06 | **63.5 s** | 한산 |

같은 코드, 4.7배 차이. 스위치 팬 때와 같은 함정을 내가 다시 밟았다 — 오프라인 벤치가 서빙과 겹치면 안 된다는 규칙을 [[stkernel-megakernel-bench-ops]] 에 적어놓고, **부팅 로그를 읽을 때는 적용하지 않았다.**

**한산한 부팅의 실제 분해** (04:06):

| 항목 | 값 |
|---|---|
| InstantTensor 읽기 | 184 GB / 58 s = **3.38 GB/s** |
| Loading weights 전체 | 63.5 s (= 읽기 58 + 적용 ~5) |
| Model loading 전체 | 72.6 s (+ process_weights_after_loading, W4 팩 포함 ~9 s) |
| 같은 NVMe 의 O_DIRECT dd | **8.3 GB/s** |

즉 로드는 **읽기 지배**이고 적용은 5초다(우리 W4 팩 빌드는 그 뒤 9초 구간에 있다 — 무관). TP=4 는 EP 없이 모든 랭크가 184 GB 전체를 읽고 자기 슬라이스를 취하므로 읽는 바이트 수는 구조적이다.

**남은 여지**: 3.38 vs 8.3 GB/s. vLLM 은 `instanttensor.safe_open()` 을 기본값으로 호출하는데, 이 라이브러리는 `INSTANTTENSOR_CONCURRENCY / IO_DEPTH / CHUNK_SIZE / BUFFER_SIZE / BACKEND` 를 환경변수로 받는다(런처 env 한 줄이면 되고 오버레이가 필요 없다). 기본값이 병목이면 58 s → 25~30 s. 서빙이 내려가는 창을 기다려 스윕을 걸어뒀다(`it_bench.py`, 셰어드 프리픽스 12 GiB 씩 default/conc8/conc16/io32/conc16+io32).

**이번에 심은 것**: boot-stamps 가 `get_all_weights` 제너레이터를 양쪽에서 재서 `weights: N tensors, X GiB | read Ys (Z GiB/s) | apply Ws` 를 남긴다. 다음부터 "로드가 느리다"는 말은 읽기인지 적용인지, 그리고 그때 디스크가 몇 GB/s 였는지와 함께만 존재한다.

**교훈**: 남의 측정을 의심하는 규칙은 내 측정에도 적용된다. 부팅 로그는 "그 부팅이 어떤 부하 위에서 났는지"를 함께 읽어야 한다.

## 메가커널 17차 — KV 캐시를 토큰 수로 고정 (2026-09-03, 운영자 지시)

운영자: "kv를 너무 많이 두지 말고 2million 고정으로 램을 잡으면 안되나". 라이브 엔진의 `/metrics` 가 근거를 준다:

| 항목 | 값 |
|---|---|
| `num_gpu_blocks` | 2175 |
| `kv_cache_size_tokens` | **4,579,624** |
| `kv_cache_max_concurrency` | **4.37** |
| `block_size` | 2304 |
| `max_num_seqs` / `max_model_len` | 4 / 1,048,576 |

즉 스케줄러가 최대 4개 시퀀스를 도는데 KV 는 최대 길이의 4.37배를 들고 있었다. 2M 으로 고정하면 그 여유가 자유 메모리로 돌아온다(GB10 은 통합 메모리라 시스템 RAM 이 그만큼 빈다).

**토큰 → 블록은 `tokens/block_size` 가 아니다.** 45층 중 11층만 어텐션(MLA, `kv_lora_rank=512`, `qk_rope_head_dim=0`, fp8 → 토큰당 11×512 = 5,632 B)이고 34층은 KDA 다. KDA 의 순환 상태는 **토큰이 아니라 시퀀스당**이라 블록 몇 개를 통째로 차지한다. 실측으로 분해하면 `(2175 − 187) × 2304 = 4,580,352 ≈ 4,579,624` — 하이브리드 예비가 약 187 블록. 그래서 `blocks = ceil(tokens/2304) + 187`, 2M → **1056 블록**.

**메커니즘**: `--num-gpu-blocks-override`. `kv_cache_utils` 가 이 값을 받으면 `available_memory = override × bytes_per_block` 으로 다시 잡으므로, 청구되지 않은 블록은 애초에 할당되지 않는다(프로파일 결과를 덮어쓰는 게 아니라 용량 자체가 그 값이 된다). 부팅 로그가 `Overriding num_gpu_blocks=X with num_gpu_blocks_override=1056` 을 찍어 매핑을 검증해 준다.

**예상 효과**: 블록 2175 → 1056(−51%). 페이지당 2304×5,632 B ≈ 12.98 MB 기준 KV 약 28 GB → 13.7 GB, **랭크당 약 14 GB 반환**. 동시성 4.37 → 약 1.9(최대 길이 기준) — `max_num_seqs=4` 면 시퀀스당 50만 토큰. 실제 트래픽이 그보다 길면 스케줄러가 대기/선점을 하므로, 장문 동시 요청이 일상이면 `KV_TOKENS` 를 올리면 된다.

**부수 수정**: 런처의 `--kv-cache-memory` 는 실제 플래그 `--kv-cache-memory-bytes` 의 argparse 축약형이라 지금은 우연히 통한다(같은 접두를 쓰는 플래그가 하나 더 생기면 모호해져 부팅이 깨진다). 정식 이름으로 바꿨다. `KV_TOKENS` 와 `KV_BYTES` 를 동시에 주면 같은 캐시를 두 번 재는 것이라 abort.

**검증 필요**: 187 이라는 예비 블록 수는 현재 `MAX_SEQS`/스펙 설정에서의 실측이다. 다음 부팅의 `Overriding num_gpu_blocks=...` 와 `/metrics` 의 `kv_cache_size_tokens` 로 확인하고, 어긋나면 `KV_HYBRID_BLOCKS` 로 보정한다. 노브: `KV_TOKENS=auto` 로 종전 동작 복귀.

## 메가커널 16차 — 부팅: cudagraph 메모리 프로파일러 끄기 (2026-09-03, 운영자 승인)

15차에서 남겨둔 12초 항목. 운영자 승인("그정도는 괜찮아") 뒤 **적용 전에 vLLM 소스를 읽고 숫자를 바로잡았다.**

`gpu_worker.py` 의 회계는 이렇다:

```
available_kv = requested_memory - non_kv_cache_memory - cudagraph_estimate_applied
```

`VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=0` 은 마지막 항을 0으로 만든다. 즉 **끄면 KV 가 그 0.85 GiB 만큼 늘어난다**. 제가 앞서 보고한 "같은 KV 를 유지하려면 0.7671 로 올려야" 는 **틀렸다** — 그 값은 vLLM 이 `suggested_util = current_util + cg_delta` 로 계산해 로그에 찍는 것이고, **프로파일러를 켜 둔 채** v0.21 이전의 KV 크기를 되찾으라는 안내다. 끄면서 그 값까지 올리면 같은 0.85 GiB 를 두 번 돌려주는 셈이라 오늘보다 1.7 GiB 더 쓰게 된다.

**적용**: 프로파일러를 끄고, 같은 몫을 GMU 에서 뺀다(`GMU - 0.0071`). GMU 는 고정값이 아니라 memfree-preflight 가 매 부팅 실측해 덮어쓰므로 고정 util 이 아니라 **델타**여야 한다. 드라이런 확인: `GMU 0.76 -> 0.7529` — vLLM 이 같은 부팅에서 찍은 "equivalent to 0.7529" 와 정확히 일치한다(회계가 맞다는 증거).

| | KV 크기 | 부팅 |
|---|---|---|
| 종전(프로파일러 on, GMU 0.76) | 기준 | +12 s |
| **적용(off, GMU 0.7529)** | **동일** | **-12 s** |
| (하지 않음) off + 0.7671 | +1.7 GiB | -12 s, OOM 여유 잠식 |

노브: `CG_MEM_PROFILE=1` 로 종전 동작 복귀, `CG_UTIL_DELTA` 로 델타 조정. 델타 0.0071 = 0.85 GiB / 119.7 GiB 는 현재 캡처 세트(4 사이즈, 최대 32) 기준이라 `cudagraph_capture_sizes` 가 바뀌면 재측정해야 한다 — 부팅 로그가 "Estimated CUDA graph memory" 와 util 동등식을 둘 다 찍는다.

**교훈**: 도구가 로그로 권하는 숫자에도 전제가 붙는다. 이번 것은 "프로파일러를 켠 채"라는 전제였고, 그 전제를 확인하지 않았으면 KV 를 1.7 GiB 늘려 OOM 여유를 깎을 뻔했다.

## 메가커널 15차 — 부팅: `init engine` 192초를 쪼갤 스탬프 (2026-09-03)

운영자 "엔진 초기화·컴파일·그래프 캡처, 이건 더 빨라지게 못하나". 먼저 그 192.52 s 가 무엇인지 확인했다. 가중치 로드 직후(23:28:26)부터 서버 기동 직전(23:31:38)까지 전부이고, **그 안에 13·14차에서 캐시로 없앤 것들이 들어 있다**:

| 구간 | 09-02 실측 | 캐시 적용 후 |
|---|---|---|
| osar 빌드+연결 | 60 s | 0.3 s |
| TileLang MHC 1차 | 5 s | 0.1 s |
| 메가커널 컴파일+무장 | 43 s | ~9 s (자가검증 ~3 포함) |
| **마커 없음** | **42 s** | 그대로 |
| torch.compile 2개 범위 | 10 s | 배포 없는 재시작이면 재사용 |
| cudagraph 메모리 프로파일링 | 12 s | 그대로(끌 수 있음) |
| **마커 없음** | **15 s** | 그대로 |
| cudagraph 캡처 | 2 s | 그대로 |
| **마커 없음(꼬리)** | **6 s** | 그대로 |

즉 배포 없는 재시작에서 이 창은 **192 → 약 90 s** 가 된다(자체 빌드 108 s → ~10 s). 남은 90 s 중 **63 s 는 로그 마커가 아예 없는 세 구간**이고, 이것이 지금 리포가 귀속하지 못한 가장 큰 부팅 비용이다.

**확실한 나머지 두 가지**: (a) cudagraph 메모리 프로파일링 12 s — vLLM 이 로그로 직접 안내하는 `VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=0`, 다만 `--gpu-memory-utilization` 의 실효 의미가 0.76→0.7529 로 바뀌어 같은 KV 크기를 유지하려면 0.7671 로 올려야 하므로 운영자 결정. (b) torch.compile 10 s — 캐시는 `/cache/vllm/torch_compile_cache` 에 있고, 부팅 스크립트가 오버레이 매니페스트 SHA 가 바뀔 때만 지운다(스테일 엔트리가 로드 실패를 내던 정당한 이유). 배포할 때마다 10 s, 재시작만이면 0.

**조치(이번)**: 추측 대신 스탬프. `glm53_boot_stamps` 모듈 신설 — 이미지에 이미 있는 `sitecustomize.py` 를 덮지 않도록 `.pth` 로 진입해서, meta-path post-import 후크로 워커의 여섯 단계(`load_model`, `determine_available_memory`, `profile_run`, `initialize_from_config`, `compile_or_warm_up_model`, `capture_model`)를 감싸 `[boot-stamp] <phase> took X.Xs (at Y.Ys ...)` 를 stderr 로 남긴다. 측정만 하고, 원래 반환값을 그대로 돌려주며, 설치 실패는 조용히 무측정으로 넘어간다(`DENEB_BOOT_STAMPS=0` 로 끔).

시행착오: audit 후크(`import` 이벤트)는 **이미 sys.modules 에 있는 모듈에는 이벤트가 오지 않아** 패치가 "다음 새 import" 에 얹히는 비결정적 동작이었다(이미지 컨테이너에서 6개 중 2개만 걸린 것을 확인). meta-path 후크는 대상 모듈 실행 직후에 확정적으로 건다 — 검증에서 6/6.

**다음 부팅 한 번이면** 42/15/6 s 의 정체가 드러난다. 그 전까지 이 세 구간에 대한 추정은 기록하지 않는다.

## ★메가커널 14차 — 부팅: 형제 자체 커널 두 개도 영속 캐시로 (2026-09-03)

13차에서 메가커널 nvcc 를 영속 캐시로 옮긴 뒤 운영자 "둘 다 해줘". 남겨뒀던 두 개를 같은 방식으로 처리했다.

| 자체 빌드 | 콜드 | 웜(재시작 상당) | 키 |
|---|---|---|---|
| 메가커널 nvcc (13차) | 59.4 s | 0.3 s | 소스 md5 + 플래그 + torch/CUDA |
| **osar nvcc** (`dsv4_oneshot_shim`) | **49.0 s** | **0.3 s** | 〃 (MAXEL 은 플래그에 실려 스트라이드 격리 유지) |
| **TileLang MHC 쌍** | **9.8 s** | **0.1 s** | TileLang 자체 키(버전/아키/커널 해시) |

측정: srv4, 같은 캐시 디렉터리를 마운트한 별도 컨테이너 2회. 합계 **약 118 s → 0.7 s**. 2026-09-02 부팅 로그의 해당 구간(메가커널 34.4 + osar 60 + TileLang ~10)으로 환산하면 재시작 부팅에서 **약 100초**가 빠진다.

**구현**: osar 는 `_build_dir(_src_md5, cuda_flags)`(노브 `VLLM_DSV4_OSAR_BUILD_ROOT`), 종전의 `/root/.osar_build_maxel{N}` 경로 격리는 MAXEL 이 플래그로 키에 들어가 그대로 유지된다. TileLang 은 MHC 오버레이가 import 시점에 `TILELANG_CACHE_DIR` 을 마운트로 지정한다 — tilelang 의 `EnvVar` 는 접근 때마다 `os.environ` 을 읽으므로 tilelang 이 먼저 import 됐어도 첫 컴파일 전에 먹고, 이미 지정돼 있으면 건드리지 않는다.

**계약**: `test_self_built_kernels_persist_their_caches` 신설(세 캐시 + 정리 로직). 기존 OSAR 스트라이드 격리 계약은 경로 리터럴 대신 키에 실린 `-DMAXEL` 을 확인하도록 갱신(취지 동일).

**남은 부팅 비용**(측정만, 손대지 않음): 가중치 로드 296 s, 엔진 초기화·컴파일·그래프 캡처 192 s. 자체 커널이 부팅에서 차지하는 몫은 이제 자가검증 ~3 s 뿐이다.

## ★메가커널 13차 — 부팅: 자체 커널 컴파일을 영속 캐시로 (2026-09-03)

운영자 "자체커널 부팅시간 개선 가능한가". 2026-09-02 23:22 부팅 로그(srv2, `glm53-logs/glm53.log`)로 구간을 떼어냈다. 전체 부팅 9분 7초(23:22:36 → 23:31:43) 중:

| 구간 | 시간 | 메가커널 몫인가 |
|---|---|---|
| 가중치 로드 48.79 GiB | 296 s | 아니오 |
| osar(`dsv4_oneshot_shim`) 빌드+연결 | 60 s | 아니오(형제 모듈, 같은 문제) |
| **메가커널: md5 → 무장 완료** | **43 s** | 예 |
| ├ nvcc 컴파일 | 34.4 s | ninja 로그 실측 |
| ├ TileLang 스톡 커널 JIT(자가검증 기준) | ~6 s | 부팅당 두 번 컴파일됨 |
| └ 자가검증 4종(mhc/gemm/mla/kda) | ~3 s | 유지(안전 게이트) |
| 엔진 초기화·컴파일·그래프 캡처 | 192 s | 아니오 |

**원인**: 컨테이너는 `TRITON_CACHE_DIR=/cache/triton`, `VLLM_CACHE_ROOT=/cache/vllm` 로 캐시를 호스트 마운트에 두는데, 자체 커널 빌드만 `/root/.mk_build`(컨테이너 내부)라 재시작마다 34초를 다시 냈다. `.cu` 는 배포 때 바뀌지, 재시작 때 바뀌지 않는다.

**조치**: 빌드 디렉터리를 영속 마운트(`/cache/mk_build`, 없으면 `/root/.cache`, 그것도 없으면 종전 경로)로 옮기고, 이름을 **소스 md5 + nvcc 플래그 + torch/CUDA 버전** 해시로 키잉했다. 배포로 `.cu` 가 바뀌거나 프로브가 플래그를 바꾸거나 이미지가 올라가면 새 디렉터리 = 정직한 재컴파일이고, 오래된 디렉터리는 7일 뒤 정리한다. 노브 `VLLM_GLM53_MK_BUILD_ROOT`.

**검증(srv4, 같은 캐시를 공유하는 별도 컨테이너 2회)**: 콜드 **59.4 s** → 웜 **0.3 s**. `tests/test_logic.py` 통과(아키텍처 핀 계약은 플래그 변수도 따라가도록 일반화).

**남은 부팅 후보(미적용, 측정만)**: (a) osar 도 `/root/.osar_build` 로 같은 문제 — 같은 한 줄짜리 고침으로 ~30~60 s; (b) TileLang 캐시가 `/root/.tilelang`(컨테이너 내부)이고 `mhc_pre_big_fuse_with_norm` 이 한 부팅에 두 번 컴파일된다 — ~10 s. 둘 다 다른 모듈 소관이라 손대지 않았다.

## 메가커널 12차 — MHC 발행 배치 채택, KDA delta 3-sync 는 기각 (2026-09-03, srv2)

배경: 인접 커널 협력 두 건이 접힌 뒤 커널 내부 두 곳을 더 봤다.

**MHC p1 발행 배치 (PR #239, 채택)**: 토큰마다의 fence+sync+atomic(~1 µs)을 두 토큰에 한 번(마지막 토큰 예외)으로. 꼬리 시작이 한 토큰 늦어질 뿐 손해 없음(T=32 는 어차피 p1 과 꼬리가 겹치고, T=8 은 꼬리가 p1 뒤에나 시작). 검증(서빙 인스턴스가 비는 창에 자동 실행): **T=8 27.6→25.6, T=32 42.0→39.9**, 수치 동일(1.25e-7/3.86e-7), VERDICT PASS.

**KDA delta rule 5-sync → 3-sync (브랜치 feat/delta-3sync, 기각)**: sync 를 2개 줄이려고, 게이트·q·k 를 스레드마다 자기 k-쿼터 32차원 패킹 레지스터로 직접 전역에서 읽게 바꿨다(이중 버퍼로 다음 토큰 선로드까지). 결과: **delta 34→40.7 µs, 스팬 176→182 — 역효과**. 원인: 기존 5-sync 판은 게이트·q·k 를 **128개 d-스레드가 한 번씩만** 전역에서 읽어 smem(y_s/q_s/k_s)으로 브로드캐스트하고 256개 스레드 전부가 그 smem 을 읽었다. 3-sync 판은 q/k/게이트 값이 k-쿼터(`kq`, 4가지)에만 의존하고 행(`vl`, 64가지)엔 무관한데도 스레드마다 직접 읽게 해서, **같은 쿼터의 64개 행 스레드가 같은 32개 값을 64번 중복해서 읽는** 꼴이 됐다. 토큰당 레지스터 로드 96개(현재+다음 토큰)의 명령 오버헤드가 sync 두 개 절감분(~1~1.5 µs)보다 컸다.

**교훈**: sync 개수만 줄이는 최적화는 그 sync 가 없애던 "여럿이 하나를 나눠 읽고 broadcast" 구조까지 함께 없애면 손해가 난다 — 브로드캐스트의 값이 몇 안 되는 인덱스(여기선 kq 4가지)에만 의존한다면 그 적은 인덱스의 스레드만 읽고 나머지는 smem 으로 받는 편이 항상 이득이다. 레지스터 절감/오프로드 실험은 자원 덤프(STACK/REG)만으로는 이런 중복 로드를 못 잡는다 — 실측이 필수.

## 메가커널 11차 — 인접 커널 협력: 둘 다 접음 (2026-09-03, 브랜치 feat/adjacent-fusion 에 기록)

운영자 "인접 커널 간 협력 해봐" → 두 가지 구현·측정 → 운영자 "그거 두개 말고 다른걸 개선 해보던가".

1. **fp8 A 전달 (상쇄)**: MHC 꼬리가 `layer_input` 을 다음 GEMM/KDA p0 의 fp8 A 타일·pow2 스케일로 직접 쓰고, 소비자는 입력 텐서가 그 출력과 동일할 때만 `a_ready` 로 프롤로그를 건너뜀. 체인 프로브(srv2, 서빙 전, T=8): 비트 동일성 True(GEMM·KDA), mhc→gemm 109.6→111.5, mhc→kda 302.1→301.8 — **이득 없음**. 꼬리의 그룹 amax 축약(MHC 27.6→30.4)이 소비자 프롤로그 절감(~3 µs)과 상쇄. 걷어냄(KDA 안 p4→p5 는 유지).
2. **L2 예열 (측정 불가로 보류)**: MHC 가 다음 소비자의 W4 팩(호출 순서로 학습한 `_NEXT_PACK[fn ptr]`)을 `prefetch.global.L2` 로 데움 — p1 토큰 반복마다 스레드 1/4 이 한 줄(~110 GB/s), 타일 접두 균일 걷기. 첫 선형 걷기(1.2 MB)는 T=8 노이즈 수준; 개선판 두 번의 측정은 모두 **네 호스트의 TP=4 서빙 부트와 겹쳐 무효**(stock MHC 63~70, KDA stock 680~750). 퇴장 구간 예열은 소비자 PDL 시작을 절감분만큼 늦춰 제거. 기대치는 T=8 −4, T=32 −15 µs/레이어였으나 확정 못 함.

**교훈**: 인접 커널 협력의 이득은 "소비자 프롤로그 ~3 µs" 급이라 방출 비용과 같은 자릿수 — 상쇄되기 쉽다. L2 예열은 균일 분산이 필수(가장 느린 블록이 커널 시간). 오프라인 벤치는 서빙과 절대 겹치면 안 된다(`docker ps` 확인).

## ★메가커널 10차 — KDA 2라운드: 스팬 199 → 176, 벤치 320 → 280 (2026-09-02 밤, srv2)

8차 뒤 스팬 199 µs 에서 phase 단위로 다시: (1) p1(gates)·p2(conv)는 둘 다 p0 의 qkv 만 읽으므로 한 phase 로 합쳐 배리어 하나 제거(배리어 2 대기 3.4→0; 스팬 199→196). (2) o_proj 의 split-K 스윕(`VLLM_GLM53_MK_KSR_OUT` 프로브 노브): 모델 선택 r=3 40.8 µs, r=1 35.5, **r=2 33.4**, r=4 43.8 → 슬라이스가 8 k-블록보다 짧으면 cp.async 파이프라인이 채워지기 전에 끝나 "라운드 수"의 이득이 벽시계로는 손해. 비용 모델에 슬라이스 ≥ 8 k-블록 상한. (3) conv: 런타임 인덱스 람다(`st[]`/`kept[]` 로컬 메모리) → 토큰 루프를 스펙 창(8)으로 언롤하고 선택식으로: 6.6→4.7. (4) gates: 워프당 내적(행 재사용, 5단 셔플) → cp.async 32행 타일 + `mma.sync m16n8k16 bf16` GEMM: **14.5→6.2**(하한 4.5). (5) **기각** — delta 동안 노는 16 블록이 o_proj 팩 4.5 MB 를 `prefetch.global.L2` 로 예열: p5 35.1→29.5 로 예열은 먹히지만 delta 가 33.8→45.9(토큰 사슬의 로드가 예열 트래픽 뒤에 줄을 섬), 스팬 176→182, 벤치 280→309. 페이싱(~100 GB/s)하면 남는 이득이 2~3 µs 라 보류.

수치는 매 단계 동일 급(out 5~6e-4, rec_state 1.6e-6, VERDICT PASS). 스팬: 199 → 196(merge) → 194(conv) → 184(gates) → **176**(ksr 상한: p5 39.7→35.1). 최종 176 = in_proj 76 + gates 6 + conv 4 + delta 34 + norm 0.5 + o_proj 35 + 배리어 ~17; in_proj·o_proj 는 W4 스트림의 DRAM 속도(~190 GB/s 유효) 그 자체다. 벤치 MK-KDA: 320 → 312~314(gates) → **279~281**(ksr 상한; GEMM 형상은 불변, n=1024 첫 발사 32.7→30.7).

## ★★★메가커널 결산 — "최대한 더 개선해" 라운드 뒤 (2026-09-02 저녁, main 0995825, srv2 한 번의 전체 벤치)

| 세그먼트 | MK | stock | 비고 |
|---|---|---|---|
| GEMM W4 8×6416 / 16×4096 / 32×2048 / 32×1024 (x2) | 79.9 / 59.9 / 38.4 / 29.1 | 132 / 95 / 53 / 34 | by-design 0.125, exact grid 0.0 |
| MHC T=8 / T=32 | 25.8 / 42.0 | 32.8 / 69.6 | 1.25e-7 / 3.86e-7 |
| KDA acc=1/3/8 (복제 포함 호출 시간) | 296 / 300 / 299 | 654 / 651 / 636 | out ~5e-4, rec_state 1.6e-6; 커널 스팬 199 |

VERDICT PASS. 라운드 시작점(#219 직후): GEMM 80/59/38/29(변화 없음, 상한), MHC 30.7/59.4, KDA 478. 이번 라운드의 머지: #222(KDA 단계 귀속·재작성), #224(MHC 재구성).

**남은 것은 커널 밖이다.** 단독 커널은 각자의 하한 근처(GEMM 지속 상한의 ~90%, KDA in_proj = W4 GEMM 상한·delta = 상태 8 MB DRAM 쓰기 하한, MHC p1 토큰당 사슬 3.3 µs). 다음 지렛대: (1) 서빙 무장 — MK-GEMM(W4) 브래킷, MK-KDA shadow → 브래킷; (2) 체인 융합 — 앞 커널이 다음 GEMM 의 fp8 A 를 내는 `a_ready`(KDA 안 p4→p5 에서 검증됨)를 MHC 꼬리 → in_proj/qkv 로 확장(서빙 glue 의 텐서 동일성 추적 필요).

## ★메가커널 9차 — MHC 재구성: T=8 30.7→27.4, T=32 59.4→42.0 µs (2026-09-02, srv2)

MHC 스탬프(main, T=8 / T=32): p1 13.4 / 37.8, bar1 3.1 / 3.0, p2 0.1(최대 4.0~4.4: 토큰당 워프 하나라 블록 0 에 몰림) + bar2 5.3 / 5.3, p3 0.8 / 2.6, bar3 1.4 / 1.5, p4 1.1 / 1.9 → 스팬 24.9 / 52.3, 이벤트 32.8 / 61.2.

| 실험 | 결과 (T=8 / T=32, 이벤트 µs) | 배운 것 |
|---|---|---|
| p1 의 청크별 fn 96 KB smem 상주, 48 블록 | 43.0 / 75.7 (악화) | 96-로드 채움이 지연 지배, 토큰마다 smem 96 읽기, 동시성 1/3 |
| 꼬리(p2·p3·p4)를 토큰의 last-arriver 가 처리, 배리어 3개 제거 | 34.6 / 86.0 (악화) | 쌍 16t+15 를 맡는 블록이 t, t+9, t+18, t+27 의 꼬리를 직렬로 |
| 꼬리 티켓 큐(유휴 블록이 도착 카운터 대기 후 처리, 마지막 퇴장 블록이 재무장) | 34.8 / 59.4 | T=32 동률, T=8 은 꼬리 ~13 µs 가 그대로 더해짐 |
| 꼬리 분리 스탬프 | p2 10.7, p34 10.0 (T=8) | 3 블록/SM 이면 컴파일러가 레지스터를 85 로 묶어 꼬리가 스필 → L1 이 거의 없는 SM 에서 로컬 메모리 왕복 |
| p34 로드를 p2 밑에서 선발행 | 32.8 / 67.6; 96 블록(2/SM) | 레지스터가 늘어 2 블록/SM 이 되자 꼬리는 p2 4.7 / p34 0.7 로 정상, 대신 p1 동시성 손실(p1 end 11.7→17.5) |
| **fn 을 레지스터(96개)에, 블록당 토큰 3개, 큐 꼬리** | **30.7 / 51.2** (stock 32.8 / 71.7) | T=8 의 p1 11.7 µs 는 128쌍 동시 × fn 96 KB = 12 MB 의 L2→SM 트래픽(L2 속도 한계); 레지스터 상주로 1/3. 48 유닛만 있어 96 블록의 절반이 놀았다 |

| 꼬리를 레지스터 절약형으로(레인별 셔플, p34 선로드는 워프 1~7만) | 34.8 / 45.1 | 꼬리 p2 12→8.8 |
| 적응형 그룹(grid/16) + 다음 토큰 x/residual 선발행 | 36.9 / — | T=8 p1 end 15.0→14.8 무변화: 토큰당 사슬 ~5 µs 가 병목 |
| **mix 계수 선로드 + 8레인 그룹 축약(32 로드 + 3 셔플)** | **27.4 / 41.8** | 레지스터가 늘어 48 블록(1/SM); 토큰당 사슬 ~3.3 µs |
| hc 상수 선로드, sinkhorn 근사 내장함수, 단일 레인 4×4 sinkhorn | 27.4 / 42.0 (동일) | 프로브 창(4.5 µs)은 줄었지만 벤치 불변 — `%globaltimer` 프로브 자체가 워프 0 의 사슬을 창마다 ~0.5 µs 늘린다. 실제 꼬리는 ~5 µs |

**최종(c69f6d8)**: MHC **T=8 27.4 µs, T=32 42.0 µs**(stock 32.8 / 71.6 → −16% / −41%; 이번 라운드 시작 30.7 / 59.4). 수치 1.25e-7 / 3.86e-7(게이트 1e-3). 구조: 격자 배리어 0개, (청크, 토큰 그룹) 블록 + fn 레지스터 상주, 전치 smem 축약, 꼬리 티켓 큐(마지막 퇴장 블록이 재무장), p3+p4 레지스터 융합.

**교훈**: (1) 축약·발행 사슬이 있는 커널은 SM당 동시 블록 수가 성능이다 — 큰 smem/레지스터로 동시성을 줄이면 같은 코드도 느려진다; (2) "마지막 도착자가 꼬리를 처리"는 매핑에 따라 한 블록에 직렬로 쌓인다 — 티켓 큐가 안전; (3) 3 블록/SM 목표는 컴파일러의 레지스터 상한(85)을 뜻하므로 꼬리 같은 희소 경로의 배열이 스필된다; (4) 프로브는 측정 대상을 바꾼다 — 벤치(이벤트)로 확정할 것.

## ★★메가커널 8차 — MK-KDA 단계 귀속과 재작성: 커널 스팬 402 → 199 µs (2026-09-02, srv2)

KDA 커널에 단계 스탬프(`-DMK_PHASE_TS`, `read_kda_ts`, 스크래치 `diag_kda.py`)를 넣고 6 phase 를 귀속시켰다. 시작점(acc=3, 스팬 402 µs): in_proj 104, **gates 68**, conv 4(+배리어 9.7), **delta 160**(헤드당 블록 하나, 나머지 32블록은 배리어 4 에서 158 대기), norm 2.6, o_proj 41.

| 단계 | 원인 | 조치 | µs |
|---|---|---|---|
| p1 gates | 스레드마다 256 B 행을 2 B 씩 읽어 섹터 증폭; 그 다음엔 워프당 반복마다 DRAM 왕복 대기 | 워프당 가중치 행 하나(8 B/레인 병합 로드)를 모든 토큰에 재사용, 행 2개 동시(4개는 18.2 로 역효과) | 68 → 14.4 |
| p3 delta | **`S[64]` 가 `#pragma unroll 8` 때문에 로컬 메모리**(STACK 336 B) + 스레드 0 직렬 l2norm + 토큰마다 의존 글로벌 로드 + 토큰마다 64 KB 상태 저장 | 완전 언롤 레지스터 상주(REG 135, STACK 96), 셔플 norm, 다음 토큰 입력 선로드, sync 8→5, **헤드당 두 블록(행 분할)** | 160 → 143 → 68 → 34 |
| conv 뒤 배리어 | 채널 6144 를 블록당 256 → 24 블록만 일함 | 블록당 128 채널 | 9.7 → 2.9 |
| p4 norm + p5 프롤로그 | p4 가 bf16 attn 을 쓰고 p5 프롤로그가 다시 읽어 양자화·배리어 | p4 가 워프당 (토큰, 헤드)로 정규화하면서 o_proj 의 fp8 A 타일·pow2 스케일을 직접 씀(헤드 128차원 = 128-k 그룹), p5 는 `a_ready` 로 프롤로그 생략, 유닛 카운터 리셋은 p4→p5 배리어 밑 | 2.7+44 → 0.5+41 |
| p0 in_proj | 단독 84 인데 104: PDL 무관(on/off 동일). **픽스처가 발사 직전 상태 10.5 MB 를 복제 → dirty write-back 이 in_proj 스트림 밑에서** | 진단 전용 `mk_run(drain=True)`(64 MB 읽기로 배수) — 서빙 체인에는 없는 오염 | 105 → 77 |

**최종 스팬(배수 진단, acc=3)**: in_proj 77, gates 18, conv 8, delta 35, norm 0.5, o_proj 39, 배리어 합 ~18 → **199 µs**. 벤치 MK-KDA(복제 포함 호출 시간): 478 → 465 → 369 → 353 → 352 → **318~325 µs**(stock 640~1760, 실행 간 편차). 수치는 모든 단계에서 동일(out 4.5e-4/5.0e-4, conv_state 0, rec_state 1.6e-6), VERDICT PASS.

**교훈**: 레지스터 배열은 반드시 완전 언롤(부분 언롤 = 로컬 메모리, 자원 덤프의 STACK 으로 확인); 워프당 반복마다 DRAM 왕복을 기다리는 루프는 배치로; 행-국소적 재귀(delta rule)는 행으로 블록을 나눌 수 있다; 발사 직전의 큰 복제는 첫 phase 를 오염시킨다.

## ★메가커널 7차 — MK-KDA 의 state-index 계약 확정과 수정 (2026-09-02, srv2)

README 의 "MK-KDA 서빙 전 미결 항목"을 stock 소스로 닫았다. `spec_state_indices_tensor` 는 `[n_spec, max_query_len]` = **질의 위치마다 상태 슬롯 하나**이고, `num_accepted_tokens` 는 *이전* 스텝의 수용 수다:
- 재귀(`fused_recurrent.py`, IS_SPEC_DECODING): 초기 상태는 슬롯 `[r, acc−1]`, **매 토큰 j 뒤** 상태를 슬롯 `[r, j]` 에 저장(슬롯 ≤ 0 = NULL_BLOCK_ID, 출력 없이 반환).
- conv(`causal_conv1d.py` spec 커널): 이력은 `prior_tokens = base + (acc−1)` 의 세 항목 `[acc−1, acc, acc+1]`, 되쓰는 창은 `old[acc], old[acc+1]` + 새 토큰(이미 MK 와 일치), conv 슬롯 = `[r, 0]`(서빙 오버레이가 `spec_state_indices_tensor[:, 0]` 을 넘김).

MK 는 `[r, 0]` 에서 재개하고 `j == acc−1` 에서만 되감아 쓰며 conv 이력을 버퍼 끝 3개에서 읽었다 — 모든 초안이 수용된 경우(acc == nq)에만 맞는 창. 그래서 픽스처의 acc=8 만 통과하고 acc=1/3 은 out 1.3(conv 이력), rec_state 0.05~0.12(되감기 위치)로 어긋났다. 픽스처는 슬롯 하나를 모든 위치에 매핑해 위치별 계약을 숨기고 있었다 → `SLOT .. SLOT+7` 로 확장(rec 슬롯 10개).

**검증 (srv2, iters 10, cec6610)**: KDA acc=1/3/8 = out 4.5e-4 / 5.0e-4 / 5.0e-4 (게이트 2e-2), conv_state 0.0, rec_state 1.6e-6 — 위치별 슬롯 전부 일치. GEMM/MHC/정확 게이트 변화 없음. VERDICT PASS.

**비용**: 토큰마다 상태(64 KB/헤드)를 쓰므로 8 MB/레이어 — stock 도 같다. 첫 구현은 레지스터에서 바로 써서(스레드 v 의 64개 float 이 512 B 간격, 4 B 저장마다 32 B 섹터) MK-KDA 445→978 µs; dynamic smem 으로 전치해 128 B 행으로 병합 저장(024dcf0)하면 **478~481 µs** — 계약의 대가 ~33 µs(7%), stock 642~659 대비 −26%. 수치는 동일(out 4.5e-4/5.0e-4, rec_state 1.6e-6), VERDICT PASS.

## 메가커널 6차 — W8 팔 제거, W4 단일 레인 (2026-09-02, 운영자 결정)

운영자: "W8 있으면 끄는 설정해야하고 유지보수 부담해야하니 귀찮지않나". W8 팔은 정상 상태에서 stock 보다 10~18% 빨랐지만(4차) W4 가 22~42% 빠른 마당에 선택·자가검증·브래킷·유지의 두 번째 팔이었다. **레인은 W4 아니면 stock.**

**지운 것**: `mk_run_gemm`(fp8 진입점)·`build_mk_weight`(fp8 팩)·`mk_gemm_kernel<false>`·`GEMM_SMEM`(W8 예산 58,368)·`g_mk_gemm4_bar`·`stage_w`·`MK_W_NBUF`·`VLLM_GLM53_MK_W4`(1/all 노브)·`MK_PROBE_SKIP`·`_selftest_w4`·벤치 `probe_w4`·hook 의 in_proj 예외. **남은 것**: `run_gemm`(=W4), `build_mk_weight_w4`, 예산 69,632 B, 노브 `VLLM_GLM53_MK_GEMM` 과 깊이 `VLLM_GLM53_MK_NBUF`.

**따라온 변화**
1. KDA 커널의 내장 in/out GEMM 두 단계도 W4 팩(`mk_gemm_phase` 단일). 서빙 KDA in_proj 도 W4 — 체인 전체가 한 수치 축, 브래킷이 전체를 판정.
2. 정확 게이트의 기준이 W8 커널 → torch 쌍둥이(`_mk_quant_x_ref`(프롤로그의 128-k 그룹 pow2 양자화) @ `mk_w4_dequant`(타일-메이저 팩 역양자화)). 커널 출력이 bf16 이라 fp32 기준과는 2^-9 급 차이가 보이므로 게이트는 "bf16 반올림 기준 대비 1 ulp 초과 원소 0개 + rel ≤ 1e-3"(`_exact_gate`). 첫 실행 1.67e-3 은 그 반올림이었다.
3. KDA 픽스처는 가중치를 e2m1 그리드에 스냅(`mk_w4_dequant(*build_mk_weight_w4(...))`)해 두 팔이 같은 값을 보게 하고 2e-2 게이트를 유지; 서빙 shadow 는 실제 가중치의 W4 팩 vs stock fp8 이라 by-design 급 `_TOL_KDA_SHADOW = 0.15`.
4. **잠복 버그 발견**: `gemm_w8a8` 의 eligibility 가 타일-메이저 팩(#208)의 `shape[0]`(타일 수)을 n_pad 로 넘겨 `n_pad % 128` 에서 항상 실패 → MK-GEMM 레인이 서빙에서 조용히 stock 으로 남았을 것. 이제 `shape[0] * 128`, 계약 추가.

**검증 (srv2, PDL 1, iters 20, 7d14414)**

| m×n (k=4096) | by-design | mk 1st | mk x2 | stock 1st | stock x2 |
|---|---|---|---|---|---|
| 8×6416 | 1.25e-1 | 84.0 | 79.9 | 133.1 | 132.6 |
| 16×4096 | 1.26e-1 | 62.5 | 58.9 | 95.4 | 94.2 |
| 32×2048 | 1.25e-1 | 41.9 | 37.9 | 52.1 | 51.6 |
| 32×1024 | 1.25e-1 | 31.7 | 28.1 | 35.8 | 33.7 |

게이트: pack roundtrip 0, exact grid 0.00e+00(1 ulp 초과 0개 — 비트 정확), MHC 1.15e-7/3.47e-7, KDA acc=8 out 0.0(스냅 덕분에 정확; main 은 3.9e-3). **KDA acc=1/3 은 main(1ed1127)에서도 1.32/1.42 로 동일하게 실패** — 기존 state-index 미결, 이번 변경과 무관. MK-KDA 커널 477→447 µs(W4 팩). 관찰: stock KDA 열은 같은 코드에서도 실행마다 637~1797 µs 로 흔들린다(main@iters10 637, main@iters20 1739, W4단일@iters10 1700, @iters20 1739~1797) — 반복 수도 스냅도 아닌 stock 경로(Triton 5커널)의 실행 간 편차; MK 열은 안정(main 477~500, W4 단일 445~447 = −7~10%). 한 실행에서 GEMM `mk_spread` 가 54/85% 로 튄 적이 있으나(876a429) 재실행에서 3.5~6%.

## ★★★메가커널 5차 — W4 확장을 prmt 테이블 조회로 (2026-09-02, srv2)

e2m1 크기는 8 코드 = `__byte_perm` 의 3비트 선택자. 원시 워드의 하위 16비트(부호 마스크)가 원소 0..3 의 선택자, 상위 16비트가 4..7 → e4m3 4바이트당 prmt 하나. 그룹 지수는 16원소마다 표에 접고(`MK_E2M1_LUT64` 두 반쪽에 `__vadd4` 로 (s<<3); 일반 덧셈은 음수 지수의 바이트 랩이 옆 바이트로 넘쳐 게이트 FAIL — 첫 시도의 교훈), 부호는 니블 비트 3 위의 두 번째 prmt. 원시 워드당 ~22 → ~13 연산.

| m×n (k=4096) | w4 1st | w4 2nd | (직전 4차) 1st / 2nd | mk 2nd | stock 2nd |
|---|---|---|---|---|---|
| 8×6416 | 84.0 / 84.0 | 76.0 / 75.8 | 87.0 / 79.3 | 117.9 | 131.1 |
| 16×4096 | 63.5 / 63.5 | 55.3 / 54.3 | 67.6 / 59.4 | 76.3 | 93.2 |
| 32×2048 | 42.9 / 41.6 | 33.7 / 35.2 | 44.5 / 35.8 | 43.9 | 51.1 |
| 32×1024 | 32.7 / 31.6 | 23.5 / 24.6 | 34.8 / 26.6 | 27.2 | 30.8 |

(두 반복 / , 1ed1127, PDL 1, iters 20; exact grid 0.00e+00, VERDICT PASS ×2)

스탬프(6416): 블록당 expand 합 30.5 → 13.0 µs, mma_fold 9.5, W4 루프 71.1 → 68.4 (대역폭 하한 ~61 = 13.2 MB 니블 + 1.6 MB 지수 @230 GB/s 의 90%). W4 는 이제 stock 대비 정상 상태 −42 / −41 / −33 / −22%, 첫 발사 −37 / −34 / −21 / −8%.

## ★★★메가커널 4차 — W8 팔도 stock 을 넘었다: smem 기준 정렬 + 밀집 W 행 + 벤치 dirty 배수 (2026-09-02, srv2)

**측정 규약(이번에 확정)**: 플러시 = `zero_`(48 MB) → matmul spacer → 64 MB fp32 읽기 ×2(dirty 라인 배수) → hot 터치. 종전 플러시는 dirty 24 MB 의 write-back 이 측정 커널 밑에서 빠져 **양팔 첫 발사가 ~35% 부풀었다**(mk 195→133, stock 170→134). `2nd` = 2·x2 − single(쌍의 두 번째 발사, 정상 상태).

| m×n (k=4096) | mk 1st | mk 2nd | stock 1st | stock 2nd | w4 1st | w4 2nd |
|---|---|---|---|---|---|---|
| 8×6416 | 133.1 / 134.1 | 125.9 / 128.1 | 134.1 / 142.6 | 130.1 / 122.6 | 86.2 / 86.0 | 79.8 / 78.8 |
| 16×4096 | 89.1 / 88.1 | 78.9 / 80.9 | 96.3 / 96.3 | 92.1 / 93.1 | 68.6 / 67.6 | 59.4 / 59.4 |
| 32×2048 | 54.3 / 54.3 | 44.9 / 46.1 | 54.2 / 54.3 | 50.2 / 49.9 | 45.0 / 44.0 | 36.8 / 36.8 |
| 32×1024 | 37.8 / 37.8 | 27.8 / 27.6 | 34.8 / 34.8 | 31.8 / 31.8 | 34.8 / 34.7 | 26.6 / 26.7 |

(두 반복 / , b6bdb42, PDL 1, iters 20)

**무엇이 바뀌었나** (직전 정상 상태 mk 148/101/57/35 → 126/80/45/28):
1. **동적 smem 기준 정렬 (결정타)**: GEMM 단계의 정적 `__shared__ int s_last, s_unit`(8 B) 때문에 동적 smem 이 +16 에서 시작 → A·W 타일의 모든 128 B 행이 뱅크라인 경계를 걸쳤다. 피치 144 나 밀집 128 이나 똑같이 걸치므로 앞선 스위즐 실험 3종이 "효과 없음/역효과"로 나온 것. 런타임에 1 KB 정렬(상수에 1 KB 슬랙).
2. **W 타일 밀집 128 B 행**: 팩 시점에 타일 안 행 r 의 16 B 청크 c 를 c^(r&7) 에 두고, stage_w 는 선형 memcpy, mma B 로드·W4 확장 저장만 mk_swz. 깨끗한 마이크로(srv2, 쌍의 두 번째 발사, 24 MB): 피치 128 110 µs(230 GB/s) vs 144 130 µs(194). 정렬 없이 밀집만 적용하면 커널은 154→158 (skip=4) 로 오히려 악화 — 1 의 증거.
3. **벤치 플러시**: 위.

4. **정상 상태 복사에 스레드 0 참여** (796c82e): 배리어 앞의 호이스트된 첫 채움만 스레드 0 을 빼고(배리어의 fence 가 그 스레드의 cp.async 를 배수), 그 뒤의 모든 stage_w 는 256 스레드가 정확히 4 청크씩.

| m×n (k=4096) | mk 1st | mk 2nd | stock 1st | stock 2nd | w4 1st | w4 2nd |
|---|---|---|---|---|---|---|
| 8×6416 | 127.0 / 126.0 | 118.4 / 118.8 | 133.1 / 132.1 | 131.1 / 132.1 | 87.0 / 87.0 | 79.8 / 78.8 |
| 16×4096 | 85.0 / 86.0 | 76.8 / 76.8 | 96.3 / 96.3 | 93.1 / 94.1 | 67.6 / 67.6 | 59.4 / 59.4 |
| 32×2048 | 53.1 / 53.2 | 43.9 / 44.0 | 54.1 / 54.3 | 49.1 / 50.1 | 45.0 / 44.0 | 34.8 / 36.8 |
| 32×1024 | 36.7 / 37.7 | 27.7 / 26.9 | 35.8 / 34.7 | 30.6 / 31.7 | 34.8 / 34.8 | 26.6 / 26.6 |

**상한 대비 (796c82e)**: n=6416 26.3 MB → 정상 상태 118.5 µs = **222 GB/s, 지속 상한 ~230 GB/s(114 µs)의 96%**; 첫 발사 126.5(208 GB/s) vs stock 132.6. 작은 형상은 지연 지배(n=1024 정상 27 µs = 155 GB/s; 프롤로그 양자화+배리어 ~6 µs, 유닛 전환 재채움 2×~3 µs 가 남은 몫). 첫 발사에서 n=1024 만 stock 보다 +2 µs(발사·프롤로그 고정비).

**기각 — 프롤로그 x-먼저 순서** (feat/gemm-prologue-x-first 6971dfa, 머지 안 함): PDL wait → x 로드 → 첫 채움 → amax 로 바꾸면 스탬프의 x 로드 완료가 8.2→3.0 µs 로 줄지만 배리어가 1.4→5.8 로 늘어(원자 왕복이 이번엔 채움 뒤에 줄을 섬) 첫 발사는 −1 µs 뿐이고, **정상 상태(쌍의 2번째)는 118.5→125 / 77→81 / 44→48 / 27→32 로 악화** — 채움이 PDL wait 뒤로 밀려 이전 커널 꼬리와의 겹침을 잃는다. 서빙 체인은 전부 PDL 연쇄이므로 채움-먼저(main)가 맞다.

**기각 — 유닛 간 prefetch** (feat/gemm-cross-unit-prefetch 8e64d3d, 머지 안 함): W8 링을 전역 스테이지 카운터로 바꿔 유닛 꼬리의 DIST 반복에서 다음 유닛의 첫 k-블록을 빈 슬롯에 미리 스테이징. 스탬프의 슬라이스 재채움 `rest` 6.6→4.1 로 예측대로 줄었지만 루프가 107.5→109.1 로 늘어 상쇄(6416 2nd 119.8 vs 118.5, 4096 77.3 vs 77, 2048 42.4 vs 44, 1024 27.1 vs 27.3 — 전부 노이즈 안). DRAM 포화 상태에서는 한 블록의 파이프라인 거품을 다른 블록의 트래픽이 이미 메운다.

**W8 마감**: 루프는 이론 대역폭(273 GB/s)의 89~95%, 커널은 지속 상한(230)의 96%. 남은 ~10 µs 고정비(프롤로그 x 로드+양자화+배리어 ≈ 5, 발사·꼬리 ≈ 4)는 커널 안에서는 못 없애고, 앞 커널 에필로그가 출력 타일(m×128 = 다음 GEMM 의 128-k 그룹)을 fp8+스케일로 내보내는 체인 융합만이 없앤다. 다음은 W4 확장 경로(6416 루프 71 µs 중 expand 30.5, 대역폭 하한 ~61).

**기각/교훈**: "첫 kernel 배치가 빠르다"·DVFS·TMA 1D/2D 무효 판정은 모두 오염된 벤치 위의 결론이었다. 정렬 문제는 smem 상수 계산으로는 보이지 않고 `__cvta_generic_to_shared(smem) & 127` 로만 드러난다.

## ★★메가커널 3차 — W4 팔이 stock 을 넘었다 (2026-09-02 오전 후반)

W4 루프의 누적 스탬프(srv2): n=6416 루프 127 µs = mma_fold 41 + expand 53 + wait 0.7 — **DRAM 대기 없는 compute-bound**. 넷을 고쳐 W4 가 네 shape 전부에서 stock quant+deepgemm 쌍을 이겼다(srv2 2회, 서로 다른 가중치 두 발 연속 발사, PDL on; 플릿 교차 진행).

| 발사당 µs | W4 (이 배치) | stock W8 쌍 | W8 mk | W4 단독 / stock 단독 |
|---|---|---|---|---|
| n=6416 | **112** | 151 | 167 | 128 / 170 |
| n=4096 | **82** | 115 | 115 | 91 / 130 |
| n=2048 | **52** | 65 | 68 | 59 / 69 |
| n=1024 | **35** | 40 | 43 | 41 / 44 |

**먹힌 것(순서대로, W4 x2 n=6416)**: A 타일 밀집 128 B + XOR 스위즐(4-way 충돌 제거) 145→141; 확장의 바이트-레인 SIMD(LUT 닫힌 식, `__vcmpeq4/__vadd4/__byte_perm`, 비트 동일) →128; mma k 스텝 완전 언롤 →122; stage_a L2 로드 선발행 →118; m ≤ 16 에서 두 번째 m 타일 제거(`mma_fold<MT>`, 제네릭 람다) →112. W8 도 같은 절감으로 6416 +10%·4096 동률·2048 +4%·1024 +8% 까지.
**지속 스트림 한계**: srv2 마이크로벤치에서 cp.async·TMA 1D·TMA 2D(텐서맵 128 B 스위즐)·레이아웃(타일 우선/행 우선 128~2 KB 런)·깊이 3~8 모두 ~135~150 GB/s. 프로세스의 **첫 커널 배치만 225 GB/s**(부스트) — 마이크로벤치는 워밍업 뒤 값만 믿을 것. W8 이 deepgemm 에 지는 남은 10% 는 스트림이 아니라 고정비.
**W4 채택은 운영자 결정**(서빙 수치 변경, by-design 1.24e-01): README 의 품질 브래킷(9/9 + 한국어 0/16 + pos-1 수용률 2%p) 뒤에만.

## ★★준비 커널 통합 (`glm53_prep_fused`) — 스텝의 12% 가 GPU 유휴, 그 자리에 발사 하나 (2026-09-02, srv4 오프라인)

**발견 (9월 1일 트레이스 재분석, rank 3, 229 스텝, 스트림 합집합)**: 프로파일된 72.1 ms
스텝에서 GPU 가 비는 시간 8.9 ms(12%)가 한 곳에 몰려 있다 — 드래프터 그래프와
타깃 그래프 사이의 eager 입력 준비 5.7 ms(aten 1,028 호출·memcpy 45·1~3 us 커널
~100개, 커널 간 공백 50~430 us = 호스트가 발사를 못 따라감), 타깃 그래프 제출
`cudaGraphLaunch` 1.43 ms(1,640 노드, 50스텝 중앙, p10 1.34 p90 1.59), 드래프터 앞
DtoH 대기 0.6, 스텝 전환 0.8. 프로파일러가 호스트를 부풀리므로 프로파일러 없는
앵커는 기존 원장의 "디코드 중 GPU 93%" (같은 정의) → 실제 유휴 4~5 ms/66 ms. dflash
가 async scheduling 을 끈다는 기록(위 표)과 일치한다. 준비 구간이 그 대부분이다.
**천장 = 준비 구간 자체, 스텝의 4~6%**; 읽는 바이트 0.

같은 트레이스에서 그래프 안 elementwise 글루 605개의 정체도 잡았다(STEP_KERNEL_MAP
보충 분해 2): 풀어텐션 인덱서 275(우리 kpool 파일), KDA 의 split-뷰 복사 136(MK-KDA
가 흡수), MoE shared 덧셈 42, 나머지 ~150 — 커널당 CUPTI 2.0 us + 간격 0.2 us,
전부 합쳐 2.7 ms(부풀린 값)라 호스트 유휴가 그래프 안 글루 전체보다 크다.

**구현**: `overlay/modules/glm53_prep_fused` — 균일 spec-verify 스텝(전 요청 드래프트
7개, FULL 그래프, 요청 패딩 없음)에서 `prepare_inputs` + `prepare_attn` + KV 그룹 7개
빌더가 쓰는 **모든 persistent 버퍼**를 pinned H2D 1회 + Triton 발사 1회 + deep_gemm
스케줄 1회로 쓴다. 메타데이터 dict 는 형상별 캐시(FULL 리플레이는 버퍼만 읽고,
dict 는 드래프터에 넘어가 무시된다). 설치는 러너/빌더 파일 17개의 preimage 를
고정(드리프트 → DISARM), plan 은 캡처 직후 live 러너에서 만들고 커널을 미리
컴파일한다. 러너 파일은 덮지 않는다(메서드 패치).

**오프라인 게이트 (srv4 새 컨테이너 `glm53:sm121-fi618`, 마운트 오버레이 30개,
`probes/run_prep_fused_check.sh`)**: stock 빌딩블록(input_batch Triton 3종·
BlockTables gather/slot·GDN FULL 복사·MLA req_id·indexer uniform-decode +
compressed slot + deep_gemm 스케줄) vs fused 커널, 프로덕션 기하(그룹 7, MLA 폭
2052, indexer 256토큰 페이지/factor 4/storage 64, kpool 4, mamba 폭 8), 무작위
배치 C=1~4 **60/60 회 46개 텐서 bit-exact**.

| C=1 스텝당 | stock 빌딩블록 | fused |
|---|---|---|
| 발사 | 커널 ~30 + memcpy ~8 | 커널 3 + memcpy 1 |
| 벽시계(호스트+GPU) | 2,556 us | 201 us |
| 이벤트 루프 (당초 "GPU" 로 적었던 값) | (호스트 대기 포함 2,562) | 200 = 커널 88.5 + deep_gemm 스케줄 64.4 + 복사 |
| **실제 GPU 시간** (CUDA 그래프 리플레이·CUPTI, 리뷰 검증) | — | 커널 ~23, deep_gemm 스케줄 ~3.4, 발사 전체 ~27~40 |

stock 열은 빌딩블록만이라 러너의 aten ~1,000 호출은 빠져 있다 — 실제 절감은 이보다
크고, 상한은 트레이스의 준비 구간(프로파일러 없이 3~5 ms). **정정(2026-09-02 리뷰)**:
파이썬 발사 루프를 이벤트로 감싼 "88/64/200 us GPU" 는 발사 비용보다 짧은 커널에서
호스트 발사 시간(Triton 디스패처 ~45, deep_gemm 래퍼 ~75 us, Grace CPU)을 잰 것이다.
fused 가 임계경로에 새로 올리는 GPU 비용은 ~30 us 이고 호스트 ~200 us 가 남는 몫이다.

**미측정 (서빙)**: 섀도 부팅(`VLLM_GLM53_PREP_FUSED=shadow`, drift=0 확인) → EXP-7
브래킷(C=1 step/s). 물리 기전 확인 = 켠 부팅 트레이스에서 준비 구간의 memcpy·
`at::native::*` 가 사라지고 `_glm53_prep_fused_kernel` 하나가 남는 것. 기본값 0.

**코드 리뷰 (2026-09-02, 10 관점 × 검증 14건)** — 첫 판(PR #221)의 결함과 수정:
(1) plan 이 `prefill_len.gpu`·`num_blocks.gpu` 를 캐시했는데 둘은 `UvaBackedTensor` 라
`copy_to_uva()` 마다 라운드로빈 풀의 다른 버퍼로 재바인딩된다(깊이 ≥2, `num_blocks` 는
매 스텝 `apply_staged_writes` 로 회전) → 격 스텝마다 한 스텝 stale: 새 블록으로 넘어가는
스텝(요청당 ~16 스텝에 1회)에 gather 가 한 블록 짧아 어텐션이 잘못된 블록을 읽고, 슬롯
재사용 시 `input_ids` 를 안 쓴다. 이미지 코드로 재현. 수정 = 발사 시점에 소유 객체에서
읽기. (2) 드래프터 KV 그룹(7번째)에 타깃 쪽 FlashInfer 빌더가 붙어 있어 `build_plan` 이
모든 실제 부팅에서 실패 → 모듈 inert(오프라인 하네스는 그 그룹을 빌더 없음으로 가정해
못 잡음). 수정 = 레이어 소속으로 드래프터 그룹 식별. (3) pinned 스테이징 버퍼 1개를 매
스텝 덮어써 async scheduling 아래에서 이전 스텝의 DMA 와 경합(GB10 재현) → `UvaBufferPool`.
(4) Q 가 2의 거듭제곱이 아니면 `tl.arange` 컴파일 실패가 첫 적격 스텝에서 엔진을 죽임 →
plan 가드 + warmup 실패 DISARM + 발사 예외 시 stock 폴백. (5) shadow 가 stock 배치를
반환해 armed 분기를 한 번도 실행 안 함 → 검증 뒤 fused 배치를 흘리고 armed 도 64 스텝마다
self-check. (6) kpool tail 원형 버퍼 휴면을 가정만 함 → plan·검증마다 assert.
(7) 메타데이터 캐시는 dflash 가 dict 를 무시할 때만 안전 → speculator 타입 게이트.
(8) 노브 오타가 arm 으로 떨어짐 → 미지 값 DISARM. (9) 런북·README 의 `EXTRA_ENV=` 명령이
프로필 선언 키라 런처에서 ABORT → caller env 형식(EXP-6 도 같은 결함). (10) 프로브 래퍼가
preimage 검증 없이 전 행을 마운트해 fi618 위에 v13 오버레이 5개를 섞어 돌림 → 래퍼가
런처처럼 검증. (11) "88 us GPU" 는 호스트 발사 시간(위 정정). (12) warmup 이 돌려주는
컴파일 커널로 직접 발사해 Triton 디스패처 호스트 비용 ~12 us/스텝 절감(검증에서 bit-exact).
기록되지 않은 부수 사실: PR #221 은 09:46Z 머지됐으나 PR #224(e76452f) 가 언급 없이
전부 되돌려 main 에서 사라졌다 — 이 판(v2)이 복원본이다.

**v2 오프라인 게이트 재실행 (srv2, 프로필 이미지 `glm53:v13-b12x`, 래퍼가 31개 행의
base preimage 를 검증)**: 무작위 배치 60/60 이 46개 텐서 bit-exact — 이번엔 stock 팔과
fused 팔 사이에 `num_blocks`·`prefill_len` 의 UVA 핸들을 한 번 더 회전시켜 첫 판의 결함
(1)을 재현하는 조건에서. C=1 스텝당 stock 빌딩블록 2,469~2,535 us vs fused 184 us
(JIT 디스패치) → **152 us**(warmup 의 컴파일 커널로 직접 발사, 두 번째 실행), 호스트+GPU;
**실제 GPU 시간 24.4~24.5 us/스텝**(발사 20개를 CUDA 그래프로 캡처해 리플레이, deep_gemm
스케줄·복사 포함), deep_gemm 스케줄 호출 호스트+GPU 11~65 us(실행 간 호스트 편차).

**부수 발견(미조치)**: (1) kpool tail 의 원형 슬롯 매핑은 러너가 빌더에 `positions`
를 안 넘겨 이 이미지에서 잠들어 있다 — `glm53_tail_slot_persistent` 의 고정 버퍼도
그래서 무효이고, tail 그룹은 generic 매핑(문서 자체가 "pos >= kpool 이면 블록 0 으로
붕괴"라 적은 것)을 쓴다. C>=2 수치 축이라 별도 브래킷. (2) 인덱서 fp32 head-gate
`torch.mm` 이 cuBLAS gemmSN 2블록 커널로 층당 86 us × 11(CUPTI) — 우리
`glm53_prefill_fastpath.py:402` 소유. (3) 드래프터 커널 합 ~3 ms(CUPTI; fc 투영
814 us eager bf16 168 MB 포함) — 원장 D≈0 과 긴장, 직접 측정 전 판단 보류.

## ★★★dflash 는 이름 때문에 async scheduling 을 잃고 있다 (`glm53_async_dflash`, 2026-09-02, 코드 읽기)

`config/vllm.py` 의 async 자동 판정은 method 이름 허용 목록(eagle 계열·ngram GPU·
`draft_model`·`dspark`)이고 `dflash` 가 없다 → 모든 dflash 부팅이 동기 스케줄러
(로그: "Async scheduling not supported with dflash-based speculative decoding and
will be disabled"). 업스트림 main 도 동일(2026-09-02). 그런데 `DSparkSpeculator` 는
`DFlashSpeculator` 의 서브클래스로 `propose()` 를 상속하며 dsv4 는 dspark 를 async 로
서빙 중이고, V2 러너에는 method 별 async 분기가 없다(스케줄러는 `[-1]` placeholder,
워커가 실제 id 를 커널로 채움). 즉 dflash 의 동기 실행은 기전이 아니라 이름의 결과다.

**의미**: 9월 1일 트레이스의 GPU 유휴(입력 준비 ~5.7 + 그래프 제출 1.43 + 전환,
스텝의 12%, 프로파일러 없이 ~7%)가 전부 동기 스케줄러가 임계경로에 올린 호스트
시간이다. async 는 그것을 이전 스텝의 GPU 실행 뒤로 숨긴다 — **천장 7~12%**,
이 캠페인 최대 단일 레버(EXP-1 EP 와 같은 급).

**구현**: `overlay/modules/glm53_async_dflash` — 플릿 이미지의 `config/vllm.py`
(preimage 2469…) 에 두 허용 조건 각각 `and not _deneb_dflash_async_ok(method)` 한 줄,
헬퍼는 `VLLM_GLM53_ASYNC_DFLASH=1` 이고 method 가 dflash 일 때만 True 이며 부팅
로그에 자기 선언. 기본 0 = stock 판정 그대로. 파일 전체를 마운트하는 이유: 판정이
프런트엔드·엔진코어 프로세스에서 모델 모듈 import 전에 돌아 우리 훅이 닿지 않는다.

**미측정**: EXP-8(부팅 1회 + 브래킷). 수용률은 움직이면 안 된다(실행 시점만 바뀜).
V2+async 는 `max_concurrent_batches`=2 라 KV in-flight 예약이 두 배 — KV 라인 확인.

## ★인덱서 fp32 head-gate: cuBLAS 2블록 47 us → split-K 10 us (`glm53_indexer_gate_splitk`, 2026-09-02, srv4 오프라인)

**대상**: `attention.py` `Indexer.forward` 의 `torch.mm(hidden_states.float(), self._wp_fp32)`
— [M,4096]×[4096,16] fp32, 층당 1회 × 11층. fp32 인 이유는 코드 주석대로 bf16 게이트
(~1e-2)가 근소 차 풀 순위를 뒤집기 때문. cuBLAS 는 이 형상에 `gemmSN` 2블록 커널을
고른다(48 SM 중 2개).

**실측**(GB10, CUDA 그래프 리플레이, `probes/indexer_gate_check.py`; 9월 1일 트레이스의
86 us 는 CUPTI + 공유 경합):

| M | stock `torch.mm` | split-K(8) | 경로 |
|---|---|---|---|
| 1 | 15.5 us | 8.5 us | split-K |
| 8 (C=1) | 50.0 us | 9.9 us | split-K |
| 16 (C=2) | 50.1 us | 11.7 us | split-K |
| 32 (C=4) | 15.9 us | 17.4 us | `torch.mm` 유지 |

M=32 부터 cuBLAS 가 다른 커널을 고르며 빨라지므로 M<=16 만 라우팅. 대안 비교(M=8):
`F.linear` NT 레이아웃 32 us, mul+sum 20.7 us, split-K(16) 6.2 us — 8 분할이 원자 경합과
프로그램 수의 균형점이라 채택.

**수치**: 양쪽 fp32 누적, 합산 순서만 다름. 300회/2,480행 max|diff| 2.4e-6 절대,
6.7e-7/행 최대, top-1 뒤집힘 0, top-4 집합 변화 0. bit-exact 아님 → 품질 브래킷 대상.

**천장**: 유휴 GPU 기준 11 × 40 us ≈ 0.44 ms/스텝 = C=1 스텝의 ~0.65%; 9월 1일 트레이스
in-situ 로는 gemmSN 이 88 us 라 11 × 78 us = 0.86~0.96 ms = **~1.35%** (경합 포함). 그래도
단독 부팅은 안 하고 EXP-7/8 부팅에 얹어 잰다. `VLLM_GLM53_FUSED_K_GATE=1` 팔의 융합 인덱서도 같은 헬퍼를 타므로
두 팔이 어긋나지 않는다. 기본 0 = stock 과 같은 `torch.mm` 호출. RUNBOOK EXP-9.

## ★오프라인 레버 소진 판정 — 9월 1일 트레이스의 스텝 구성 (2026-09-02 밤, 무부팅)

rank 0, 223 스텝, 중앙 71.0 ms(프로파일러 하). 범주별 커널 시간: MoE 전문가 30.18 ms
(42.5%, 대역폭 바닥) · deep_gemm 밀집 GEMM 14.06 (19.8%) · GPU 유휴 5.65 (8.0%) ·
`k_oneshot` AR 5.76 (8.1%; p10 30.6 / p50 43.5 / p90 80.4 us → ~2.5 ms 가 랭크 간 대기) ·
bf16 GEMM 5.17 (7.3%) · MoE 글루 2.71 (3.8%, 331개) · MHC 2.54 · KDA 1.27 · elementwise
0.95 (466개) · MLA 0.66 · 인덱서 0.39. 표와 시간 구조는 STEP_KERNEL_MAP 보충 분해 3.

**확인한 비-레버 셋**: (1) MoE 게이트 `_deneb_gate_partial_kernel` — 그래프 안 37.5 us 는
stream 17 공유 전문가 경로와 겹친 시간; GLM 형상 단독 콜드 13.6 us 이고 타일 스윕(비트
동일 60여 구성)에 더 빠른 것 없음. (2) 인덱서 디코드 글루 — 층당 top-k 24 us + 소형
10개, 스텝 0.4%. (3) 드래프터 ~4.3 ms(소형 3.5 + fc 809 us) — 다음 스텝의 호스트 준비
유휴 뒤에 숨어 D≈0 성립; **EXP-7/8 뒤 재측정 조건부**(천장 ~6%).

**EXP-9 정정**: in-situ gemmSN 88 us × 11 → 아래 09-03 항목에서 N=32 로 재실측(~1.2%).

**판정**: 무부팅으로 닫을 >1% 레버 없음. 부팅 순서 EXP-8 → EXP-7 → EXP-6 → (EXP-9·EXP-4 얹기).

## ★★리뷰 정정 — split-K head gate 는 플릿 형상(N=32)에서 한 번도 돌지 않았다; v2 결정적 커널 (2026-09-03, srv4 오프라인)

PR #228 의 `glm53_indexer_gate_splitk` 리뷰(15건) 결과. **치명 1**: 플릿 체크포인트
`glm53-redhat-nvfp4` 의 `index_n_heads=32` → `_wp_fp32` 는 [4096,32] 인데 헬퍼는 N<=16 만
받고 커널도 BN=16 고정 — 노브를 켜도 11층 전부 stock `torch.mm`. README·원장·프로브의
수치 전부가 합성 N=16 형상이었다. **치명 2**: fp32 `tl.atomic_add` 합산이 리플레이마다
비트가 달랐고(49/49), 인덱서는 랭크마다 복제 실행이라 근소 차 풀 순위가 한 랭크에서만
뒤집힐 수 있었다. **치명 3**: x 의 inner stride·K 일치 미검사(열-stride 뷰에서 max|diff|
4.65). 나머지: 심볼 검사기가 상대 임포트·고아 모듈을 건너뜀, 항진 테스트, 트레이스
도구의 단일 스트림 유휴 계산, 앵커 소실, 문서 수치 불일치, 수용률 게이트 누락 등.

**v2 커널**: K 를 32개 프로그램으로 나눠 가중치 슬라이스를 한 번씩 읽고 bf16 을 레지스터에서
fp32 로 캐스트, [16,N] 부분합을 쓴 뒤 두 번째 커널이 **고정 순서**로 합산 — atomic·memset
없음, 발사 2개(stock 도 cast+gemm 2개). N<=32, x 2-D·unit stride·K 일치를 헬퍼가 검사.

**실측 (N=32, K=4096, 콜드 L2 46 가중치 순환, `probes/indexer_gate_check.py --config`)**:
M=1 20.8→9.5 us · M=2 51.0→10.1 · M=4 86.4→11.1 · **M=8 89.5→12.7** · M=12 93.9→14.7 ·
M=16 87.7→16.8; M=24 17.3 / M=32 17.7 은 stock 유지. cuBLAS 의 N=32 콜드 88 us 는 트레이스
in-situ 88 us 와 일치(첫 판의 47 us 는 N=16 값). 수치 200회/1,571행 max|diff| 2.4e-6,
7.2e-7/행 최대, 순위 뒤집힘 0; 50회 비트 동일; stride·K 불일치·M=0·M=17 라우팅 검사 통과.

**천장**: 11 × 76.8 us = 0.84 ms/스텝 = C=1 의 **~1.2%**(C=2 0.78 ms). EXP-7 부팅에만 얹는다
(EXP-8 은 수용률 게이트 귀속 문제). 게이트에 pos-1 수용률 ±2pct 추가.

**유휴 정정**: `tools/trace_step_composition.py` 를 스트림 합집합 기준으로 고치니 9월 1일
트레이스의 GPU 유휴는 5.65 ms 가 아니라 **9.24 ms/스텝(13.0%)**, 두 스트림 동시 실행분
중앙 3.58 ms — 보충 분해 2 의 합집합 8.9 ms 와 일치. 도구는 EXP-7 트레이스(`_gather_block_tables`
가 사라짐)를 위해 앵커 폴백(`_glm53_prep_fused_kernel` → `_get_num_sampled_and_rejected`)을 갖는다.

**MoE 게이트 스윕 재확인**: 배포 커널을 직접 import 해 스윕(BM32 는 stages 3 사다리);
E=288 결론(더 빠른 비트 동일 타일 없음)은 유지.

## ★★★EXP-8 기각 — dflash async scheduling 은 이미 켜져 있었다 (2026-09-03, 전 팔 부팅)

`glm53_async_dflash`(#223/#226 계열)의 전제가 틀렸다. 이미지의 `config/speculative.py` 에서
`EagleModelTypes` 는 `DFlashModelTypes = Literal["dflash"]` 를 **중첩 포함**하고 `get_args` 가
이를 평탄화한다 — `get_args(EagleModelTypes)[-1] == 'dflash'`. 그래서 `config/vllm.py` 의
async 비활성화 elif 는 첫 항에서 단락되고, 우리 헬퍼(`_deneb_dflash_async_ok`)는 **두 분기
어디에서도 호출되지 않는다**. 체인의 나머지 분기도 안 걸려 `else: async_scheduling = True`.

**부팅 증거**: 전 팔 부팅(2026-09-03)의 헤드·워커 로그에 async 관련 경고가 **하나도 없다**.
비활성화 분기는 pooling(디버그)만 빼고 전부 warning_once 를 남기고, 이 모델은 pooling 이
아니다. 컨테이너 안에서 헬퍼를 직접 부르면 정상 동작하고 로그도 남는다(즉 마운트·환경변수
문제가 아니라 도달 불가). 모듈과 노브는 제거했다.

**천장 7~12% 철회**: 9월 1일 트레이스의 호스트 준비 유휴(스트림 합집합 9.24 ms/스텝)는
**async 가 켜진 상태에서** 찍힌 값이다. async 는 스케줄러 파이썬 작업을 겹치게 할 뿐,
워커 `execute_model` 안의 입력 준비는 그래프 리플레이 사이에 직렬로 남는다. 그 구간의
레버는 EXP-7(`glm53_prep_fused`) 하나다.

**교훈**: 허용 목록을 이름 문자열로 읽을 때 `Literal` 중첩·`get_args` 평탄화를 확인할 것.
소스 grep 으로 "dflash 가 목록에 없다" 를 결론냈는데, 목록은 다른 Literal 을 통해 그것을
포함하고 있었다. 부팅 로그에 **켜졌다는 줄도 꺼졌다는 줄도 없으면 코드가 아니라 도달성을
의심할 것.**

**부수 수정**: `glm53_prep_fused` 의 화자 가드가 플릿의 `DFlash2Speculator` 를 이름으로
거부해 이 부팅에서 DISARM 됐다. 이미지 소스 확인 결과 `_generate_draft` 호출 두 곳 모두
타깃 메타데이터를 넘기지 않는다(`attn_metadata=None` 또는 드래프터 자신의
`draft_attn_metadata`), `DFlash2Speculator` 는 `propose()` 를 재정의하지 않는다 → 가드에 추가.

## ★★MK_SEG_MLA — sm_121a 수제 sparse MLA 디코드, 3판에서 FlashInfer 를 넘었다 (2026-09-03, srv4)

운영자 지시("천장 재지 말고 그냥", "무조건 더 빠르게")로 착수. 토큰·헤드마다 인덱서 top-k
슬롯에 대해 `s = sm_scale·q·c·ckv_scale`, softmax, `out = Σ p·c` — k 와 v 가 같은 압축
잠재(D=512)라 슬롯 512 B 를 한 번 읽어 16 헤드가 공유하는 것이 설계 축.

**세 판의 궤적 (층당, T=8, W=2048, 30만 슬롯으로 L2 무효; FlashInfer run 124 us)**:

| 판 | 구조 | T=8 | 루프라인 |
|---|---|---|---|
| v1 | fp32 FMA, 레인=D, 내적을 5단 셔플로 리덕션 | 205 us | 스트림 89 + 내적 68 + softmax/출력 52, **가산** |
| v2 | fp32 FMA, 레인=슬롯(셔플 0), XOR 스위즐 | 220 us | 스트림 84 + 점수 80 + 9.5 + 44, 가산 |
| **v3** | **bf16 mma m16n8k16**, fp32 acc, P bf16 | **93.7 us** | 스트림 바닥(68~84) 근처 |

v1·v2 가 진 이유는 명령 수였다: fp8→fp32 소프트웨어 변환이 16값당 48 명령, q 언패킹
까지 더하면 슬롯·레인당 4천 명령 — FMA 피크 계산(19.7 TFLOP/s)은 맞았지만 **변환과
언패킹이 산술의 4배**였다. 텐서코어는 [16 헤드 × 8 슬롯] 을 명령 하나로 낸다. 정밀도는
FA2 경로와 동일 등급(bf16 q·C·P, fp32 acc; e4m3→bf16 은 무손실): "q 를 양자화해야 한다"
던 앞선 판단은 e4m3 mma 만 생각한 오류였다.

**최종 (분할 정책: T·s 가 그리드 48 의 배수인 최소 s)**:

| T | MK | FlashInfer | 배수 |
|---|---|---|---|
| 8 (C=1) | **93.7 us** | 124.8 | 1.33 |
| 16 | 162.5 | 196.1 | 1.21 |
| 32 | 335.8 | 551.9 | 1.64 |

수치: 자가검증 rel 2.7e-3(torch fp32 트윈), FlashInfer 대비 3.4e-3 — 서빙 수치 변경
(품질 9/9 + 한국어 0/16 + pos-1 ±2pct 브래킷). 천장: 11층 × 31 us ≈ 0.34 ms/스텝(C=1 의
~0.5%); C=4 는 11 × 216 = 2.4 ms(스텝의 ~2%).

**자가검증이 잡은 버그 2건**: cp.async 그룹 수가 wait_group 과 어긋나 타일 하나짜리
짧은 행이 도착 전 smem 을 읽던 것(빈 커밋으로 NSTAGE-1 고정), 96블록 그리드가 48블록용
공유 배리어 카운터를 쓰던 것(전용 카운터).

**정정**: 이 커널의 애초 명분이던 호스트 `plan()` 은 스텝당 ~38 us 이지 2.4 ms 가 아니다
(앞선 값은 예약 버퍼 없는 합성 래퍼). 진짜 이득은 커널 시간이다. `glm53_mk_mla_wiring`
이 T<=32 를 라우팅하고, 프리필은 래퍼를 그대로 쓰므로 빌더의 plan 도 그대로 둔다.
**정정**: 이 커널의 명분이던 호스트 `plan()` 비용은 **스텝당 ~38 us** 이지 2.4 ms 가
아니다. 앞서 잰 2.4 ms 는 예약 버퍼 없이 만든 합성 래퍼의 값이었다. 따라서 이 축의
천장은 스텝의 ~2%(FI run 124 us × 11층 = 1.36 ms)이고, 커널이 래퍼를 실제로 이겨야
의미가 있다. **현 상태로는 채택 불가**, 셔플 제거 후 재측정이 조건.
## ★★GLM-5.3 프리필 첫 프로파일 — 32K 에서 시간의 정체와 레버 (2026-09-03, MK-MLA 부팅, rank 0)

프리필 개선 여지를 묻는 질문에 원장에 GLM 프리필 분해가 없었다(있던 것은 DSV4 32K).
MK-MLA 부팅에서 신선한 36,852 토큰 프롬프트 하나를 프로파일했다(벽시계 17.1 s, 커널
합 16.9 s → **GPU 99% 바쁨, 프리필은 GPU 바운드**). `census.py` 그룹 합계 × 8 스텝:

| 그룹 | 초 | 비중 | 무엇 | 레버 |
|---|---|---|---|---|
| **어텐션 (sparse MLA 프리필)** | **4.19** | **24.8%** | 11층 × 5청크, 층-청크당 76 ms — 쿼리마다 top-k 2048 × 512 B 를 gather (층-청크당 8.6 GB) → gather 대역폭 바운드 | `glm53_union_prefill`(인접 쿼리 union, exact, opt-in, **미측정**) |
| MoE b12x | 3.22 | 19.0% | 42층 × 5청크, 층-청크당 14.6 ms = 3.3 TFLOP → **226 TFLOP/s** | b12x 커널 몫, 우리 레버 아님 |
| NCCL AllReduce (ring LL bf16) | 2.72 | 16.1% | 455회 × 6.0 ms, 64 MB 페이로드 → 링크의 64% | 겹치기(SP/async TP 패스는 torch.compile 필요 — 이 모델은 미지원), fp8 AR(수치) |
| cutlass/cublas GEMM | 2.35 | 13.9% | M=8192 밀집 투영 | — |
| mHC (TileLang) | 1.92 | 11.4% | post 1.19 + pre 0.73 | BIGFUSE 는 dsv4 에서 in-graph +0.1% — 아님 |
| KDA/FLA 청크 | 0.91 | 5.4% | 34층 청크 스캔 | EXP-5 청크 스윕(1~2%) |
| elementwise·기타·norm/quant | 1.51 | 8.9% | | |

**판정**: 프리필은 MoE·GEMM(33%, 물리)과 어텐션 gather(25%)·통신(16%)이 지배한다.
오버레이가 닿는 레버는 어텐션 gather 하나가 크다 — 인접 쿼리의 top-k 가 대부분 겹치므로
union 으로 한 번 읽으면 gather 가 절반 이하: **천장 ~12%**. KDA 1~2%, 통신은 이 이미지의
겹치기 패스가 torch.compile 전제라 막혀 있어 0~5%(연구 항목). 합쳐 **15~20%**, 32K TTFT
17.1 → ~14 s(2,150 → ~2,600 tok/s). 그 이상은 MoE/GEMM 커널 몫.

**사용자가 체감하는 첫 요청 비용은 따로 있다**: 콜드 JIT 세금 11~41%(9/1 실측). 런처의
`PREFILL_WARMUP` 이 호출자 env 복원 목록에 없어 켜지지 않던 버그를 #243 으로 고쳤다.
다음 부팅부터 `PREFILL_WARMUP=1` 이 실제로 웜업한다.

**재현**: `~/glm53-logs/prefill_profile.sh`(헤드; start_profile → 32K 요청 → stop_profile →
census). 트레이스 `dp0_pp0_tp0_dcp0_ep0_rank0.1788380125392907694`.

## NCCL 채널 스윕 — 프리필 AllReduce 129 → 142 Gb/s (2026-09-03, 엔진 다운, 채택)

프리필의 16% 가 NCCL AllReduce(455회 × 6.0 ms, 64 MB bf16)라 패브릭 축을 부팅 없이
쟀다. 4노드 torch all-reduce, 런처와 동일한 컨테이너 플래그(`--device /dev/infiniband`,
`memlock=-1`, `IPC_LOCK` — 빠뜨리면 "Failed to initialize any NET plugin").

| 구성 | 64 MB AR | ring 유효 | 256 KB |
|---|---|---|---|
| 기준(런처 현행) | 6.16~6.23 ms | 129~131 Gb/s | 0.272 ms |
| **ch16 + peer4** | **5.657 ms** | **142.3 Gb/s** | **0.157 ms** |
| ch16 | 5.68~5.71 | 141~142 | 0.265~0.314 |
| ch8 | 5.835 | 138.0 | 0.138 |
| ch24 / ch32 | 6.05 / 6.12 | 133 / 132 | — |
| Simple only | 6.19 | 130.2 | 0.270 |
| **LL128 only** | **21.54** | 37.4 | — (3.5배 느림) |
| qps4 / qps8+ch16 / buff16M | 6.49 / 5.97 / 6.20 | 124~135 | — |

**채널 수가 유일하게 먹힌 축이다.** 16 채널이 최적이고 그 위(24·32)는 다시 나빠진다 —
링크 두 개에 채널이 과분할되면 메시지당 몫이 작아져 RoCE 왕복이 드러난다. `peer4` 는
64 MB 에서 미미하고 작은 메시지에서 크다(0.272 → 0.157). 프리필 AR 이 프리필의 16% 이므로
**프리필의 ~1.3%**, env 한 줄이고 수치 변경 없음. 런처에 채택.

기각: LL128 단독(3.5배 느림 — RoCE 에서 LL128 의 flag 트래픽이 손해), 추가 QP,
16 MB 버퍼, Simple 단독.

## ★★sm_121a 텐서코어 ISA 실측 — fp4 는 fp8 보다 빠르지 않다, 2배는 2:4 희소뿐 (2026-09-03)

"프리필은 연산 바운드니 하드웨어에 맞춘 커널이 가장 잘 먹힐 곳" 이라는 물음에 답하려고
이 부품의 텐서코어가 실제로 무엇을 갖고 어떤 속도를 내는지 쟀다(레지스터 피연산자
back-to-back mma, 48 SM × 4 블록 × 256 스레드, **SM 클럭 1.59 GHz — 냉각 상한 때문에
최대 3.0 GHz 의 53%**. 절대값은 클럭에 비례하지만 아래 비율은 불변).

| mma 형태 | TFLOP/s @1.59 GHz | bf16 대비 |
|---|---|---|
| dense bf16 `m16n8k16` | 77.5 | 1.0× |
| dense e4m3 `m16n8k32` | 155.1 | 2.0× |
| **dense e2m1 `m16n8k32 kind::f8f6f4`** | **155.1** | **2.0× — fp8 과 동일** |
| **sparse e4m3 `m16n8k64 mma.sp::ordered_metadata`** | **308.9** | **4.0×** |

**결론 1 — fp4 는 연산 이득이 0이다.** 네이티브 e2m1 mma 가 있지만 K=32 로 fp8 과 같아
명령당 FLOP 이 동일하다. fp4 의 이득은 순수하게 메모리(바이트 절반)다. **이는 MK-GEMM 의
설계를 검증한다**: e2m1 을 e4m3 으로 펼쳐 k32 mma 로 돌리는 현재 방식은 연산에서 아무것도
잃지 않는다. 네이티브 fp4 명령으로 바꿔도 같은 속도다.

**결론 2 — 이 부품의 유일한 2배 레버는 2:4 구조적 희소다.** `mma.sp` e4m3 k64 가 정확히
2배다. GB10 의 "1 PFLOP NVFP4" 도 2:4 희소 기준이다. 다만 가중치가 2:4 로 가지치기돼
있어야 하고, 학습된 체크포인트에 적용하려면 품질 손실을 동반한 재훈련이 필요하다 —
운영자 결정 사항이며 이 리포의 오버레이 범위 밖이다.

**미지원 확정**: `.block_scale` 계열(`kind::mxf4`, `kind::mxf8f6f4`)은 sm_121 에서 거부된다
(sm_100a 전용). 스케일은 커널에서 따로 적용해야 한다 — 우리 W4 팩이 이미 그렇게 한다.

**빌드 함정(수정)**: `-arch=sm_121a` 는 `-cubin`·`-ptx` 에서는 통하지만 **`-c` 에서는 평범한
`sm_121` 타깃을 낸다**(cpp_extension 이 쓰는 모드). 그러면 위 두 아키텍처 전용 명령이 전부
"not supported on .target 'sm_121'" 로 거부된다. 오늘 쓰는 e4m3/bf16 mma 는 공통이라 생성
코드가 바뀌지 않지만, 전용 명령을 쓰려면 `-gencode arch=compute_121a,code=sm_121a` 여야
한다. 메가커널과 kpool top-k 빌드를 고치고 테스트로 고정했다.

**프리필 함의**: 프리필의 연산 항목 중 MoE(19%)는 nvfp4 로 226 TFLOP/s 인데, 이 부품의
밀집 fp4 천장이 fp8 과 같은 155(@1.59 GHz, 클럭 상한 해제 시 ~293)이므로 **MoE 는 이미
천장 근처**다. 프리필의 최대 항목인 어텐션 25% 는 연산이 아니라 **gather 대역폭**
(층-청크당 8.6 GB, 유효 113 GB/s)이라 커널 정밀도가 아니라 읽는 양이 레버다 → union.

## MK_SEG_MLA 전용 명령 검토 — fp8 mma 는 5% 를 위해 순위 30% 를 흔든다, 기각 (2026-09-03)

"sm_121a 전용 명령을 실제로 쓰라" 는 지시에 따라 MK-MLA 를 e4m3 k32 mma 로 바꾸는 안을
검토했다. 잠재 캐시는 이미 e4m3 이므로 q 만 양자화하면 텐서코어 발행률이 2배(77.5 →
155 TFLOP/s)가 된다.

**속도 상한 (실측)**: v3 커널 T=8 전체 93.3 us, 메모리 파이프라인만 79.0 us →
**연산 몫은 14.3 us(15%)**. 그 안에 softmax·e4m3→bf16 변환·smem 트래픽이 함께 있어
mma 자체는 절반쯤이다. 발행률을 2배로 올려도 회수는 **4~5 us, 전체의 ~5%**.

**품질 비용 (실측, `probes/mla_q_precision_check.py`, 8 시행 × 8행 × 16헤드)**:

| q 정밀도 | 출력 rel | top-1 슬롯 바뀜 | top-8 집합 바뀜 |
|---|---|---|---|
| bf16 (현행) | 5.3e-4 | 0.39% | 2.05% |
| e4m3 (fp8 mma) | 8.5e-3 | **4.59%** | **30.37%** |

출력 오차 16배, top-1 교란 12배. 이 점수가 sparse attention 이 실제로 보는 토큰을
고르므로 5% 와 맞바꿀 수 없다. **기각** — FlashInfer 백엔드가 "queries stay BF16" 라고
적어둔 이유가 이것으로 확인됐다.

**남은 레버는 메모리다**: 바닥 79.0 us 에서 106 GB/s 인데 이 박스는 135~175 GB/s 를 낸다.
거기까지 가면 전체 93 → 74 us. 막는 것은 점유율이다 — smem 82 KB(ring 32 + bf16 타일 33
+ q 17)라 SM 당 블록이 하나다. bf16 타일을 없애고 조각을 레지스터에서 변환하면 33 KB 가
풀리지만 출력 단계가 그 타일을 `ldmatrix.trans` 로 읽으므로 조각 조립을 다시 써야 한다
(타일 16 으로 줄이는 것만으로는 구조가 32 전제라 깨진다 — rel 28 로 확인).

## ★★★MK_SEG_MLA v4/v5 — 점유율이 병목이었다, 그리고 프리필까지 (2026-09-03)

**v4 — smem 82 → 46 KB, SM 당 2블록.** 방향은 gather 마이크로벤치가 정했다
(`probes/gb10_gather_roof.cu`, 1 GiB 버퍼): 이 부품은 **512 B 흩어진 청크를 225 GB/s**
로 읽는다(순차 230, 256 B 219, 4 KB 227). 즉 무작위 패턴은 문제가 아니었고, 커널이
106 GB/s 였던 것은 **점유율** 탓이다 — v3 는 잠재의 bf16 사본을 smem 에 만들어(33 KB)
총 82 KB 를 써서 SM 당 블록이 하나였다. v4 는 그 사본을 없애고 두 mma 단계의 B 조각을
e4m3 링에서 레지스터로 변환한다(점수: 인접 2바이트, 출력: 스트라이드 2바이트). 타일
32→16, k 분할 2→4 로 8워프를 (n그룹 2 × k사분면 4)로 재배치. **45.8 KB → 2블록/SM.**

**v5 — splits==1 직접 저장으로 프리필 확장.** 한 split 이 행 전체를 가지면 결합할 것이
없으므로 phase 0 에서 정규화해 bf16 으로 바로 쓴다. 이게 프리필을 가능하게 한다:
T=8192 에서 `[T][splits][H][D]` fp32 스크래치는 268 MB 다.

| 형상 | MK v5 | FlashInfer | 달성 대역폭 |
|---|---|---|---|
| T=8 (C=1) | **67.7 us** | 124.3 | 124 GB/s |
| T=16 | **96.7** | 199.8 | 174 |
| T=32 | **212.8** | 547.2 | 158 |
| **T=8192 (프리필)** | **31.6 ms** | 층-청크당 ~76 ms | **204 GB/s = 천장의 91%** |

수치: 자가검증 ARM, FlashInfer 대비 rel 3.4e-3, torch fp32 트윈 대비 2.5e-3 —
프리필·디코드 동일 등급.

**v4 는 연산을 완전히 숨겼다**: 루프라인 프로브에서 전체와 메모리-only 가 같다
(T=8 66.2 vs 66.4 us). 그래서 **전용 fp8 mma 로 바꿔도 이득이 0**이다(그 전 v3 에서는
5% 였고 품질 비용 때문에 이미 기각했다).

**프리필 함의**: 어텐션은 32K 프리필의 25%(4.19 s / 16.9 s)이고 113 GB/s 로 돈다.
같은 gather 를 204 GB/s 로 하면 4.19 → 2.3 s, **프리필의 ~11%**. 서빙 훅
(`glm53_mk_mla_wiring`)의 라우팅 상한을 올려 프리필도 이 커널을 타게 했다.
재현: `probes/mk_mla_prefill_check.py`, `probes/mk_mla_bench.py`.

## 기각: 프리필 밀집 GEMM 의 스케일 입도 — deepgemm 은 이미 천장의 67% 였다 (2026-09-03)

"프리필 GEMM 이 천장의 23~28% 라 3배가 남아 있다"는 **오염된 측정이었다.** 그 값은
플릿 부팅이 같은 GPU 에서 가중치를 로딩하는 동안 쟀고, 그것을 유휴 상태의 천장과
비교했다. 원장이 이미 적어둔 함정을 그대로 밟았다.

유휴 GPU 실측 (`probes/fp8_scale_granularity.py`, M=8192 K=4096, 천장 155 TFLOP/s):

| N | deepgemm (블록와이즈) | 행/열 | 텐서당 | bf16 |
|---|---|---|---|---|
| 4096 | 104.5 | 127.5 | 134.7 | 69.4 |
| 3072 | 98.6 | 123.8 | 129.0 | 68.9 |
| 2048 | 88.3 | 121.3 | 123.0 | 67.1 |
| 512 | **43.6** | 109.4 | 108.0 | 67.8 |

**deepgemm 은 나쁜 커널이 아니다** — 넓은 N 에서 천장의 63~67% 다. bf16 도 자기 천장
77.5 의 89% 다.

정확도(실제 체크포인트 가중치, 양쪽 피연산자 양자화, 활성 이상치 40배 포함):
블록와이즈 2.86e-2 vs 행/열 3.55e-2 — **이상치가 있어도 1.2배뿐**이다. 블록와이즈가
사주는 정확도는 통념보다 훨씬 작다. (e4m3 은 지수부가 있어 블록 스케일은 블록 간
동적범위가 넓을 때만 값을 한다.)

**그래도 기각한다.** 밀집 GEMM 은 프리필의 13.9%(2.30 s)이고, 산수가 이렇다:

| | 도달 | 2.30 s → | 프리필 |
|---|---|---|---|
| 행/열로 교체 | 127 | 1.88 s | **1.8%** |
| + 자작 커널이 천장에 닿음 | 155 | 1.54 s | +1.5% |
| + 2:4 희소 | 309 | 0.77 s | +3.2% |

1.8% 를 위해 60개 층의 수치를 1.2배 나쁘게 만드는 거래는 하지 않는다. 자작 GEMM 도
그 위에 1.5% 뿐이라 이 리포에서 가장 어려운 커널 부류를 쓸 값이 없다.

**남는 실마리 하나**: N=512 에서 deepgemm 43.6 vs 행/열 109 — 2.5배다. 좁은 N 에서
타일이 SM 을 못 채운다. 형상별로 좁게 고칠 수 있지만 N=512 투영은 FLOP 비중이 작다.

**되짚을 조건**: 밀집 GEMM 이 프리필의 25% 를 넘거나, 2:4 희소를 (재학습 없이 얼마나
잃는지 재고 나서) 도입할 때. 2:4 는 양자화와 성격이 다르다 — 남는 가중치는 정밀도를
그대로 두고 절반을 0 으로 만드는 **용량** 축이라, 엔비디아 레시피처럼 마스크 고정
재학습이 있으면 회수되지만 우리에겐 그 파이프라인이 없다.

**측정 규율**: 부팅·서빙이 도는 동안 잰 GPU 수치는 천장과 비교하지 않는다.
같은 프로브가 오염 시 43.5, 유휴 시 104.5 를 냈다 — 결론을 뒤집는 크기다.

## ★정정: fp4 는 fp8 의 2배다 — "fp4 연산 이득 0" 기록은 틀렸다 (2026-09-03)

원장은 "e2m1 `kind::f8f6f4` m16n8k32 = 155.1 TFLOP/s, e4m3 과 동일 → fp4 는 연산 이득이
없다"고 적어 두었다. **틀렸다.** 그 명령은 fp4 를 **fp8 데이터패스로 흘리는** 경로다
(nibble 하나가 byte 레인 하나를 쓴다). 네이티브 NVFP4 는 그 2배다.

세 갈래가 같은 곳을 가리킨다:

1. **서빙 트레이스**: b12x NVFP4 MoE 가 226 TFLOP/s. FLOP 계산 검산 — 토큰당 층당 9회
   전문가 통과 × 50.3 MFLOP × 7,370 토큰 = 3.34 TFLOP, 트레이스의 3.3 과 일치. 즉
   226 은 옳고 "천장 155" 와 모순이다.
2. **벤더 스펙 · 제3자 실측**: GB10 은 밀집 NVFP4 500~511 TFLOPS, 2:4 희소
   1014~1022 TFLOPS.
3. **클럭으로 환산하면 정확히 닫힌다**: 칩앤치즈 실측 부스트 **2.55 GHz**. 우리 bf16
   실측 77.5 @ 1592 MHz → 2.55 GHz 에서 124 → fp8 248 → **fp4 496 ≈ 스펙 500** →
   2:4 희소 992 ≈ 1 PFLOP.

**우리 클럭(1592 MHz)에서의 천장표 (정정본)**

| 형식 | TFLOP/s @1.59 GHz | @2.55 GHz |
|---|---|---|
| bf16 m16n8k16 | 77.5 | 124 |
| e4m3 m16n8k32 | 155 | 248 |
| e2m1 `kind::f8f6f4` (느린 경로) | 155 | 248 |
| **네이티브 NVFP4** | **310** | **496** |
| 2:4 희소 NVFP4 | 620 | 992 |

`mma...kind::mxf4` m16n8k64 는 ptxas 가 sm_121a 에서 거부한다(실측). 즉 빠른 경로는
직접 쓰는 mma 가 아니라 flashinfer 가 싣고 있는 `Sm120B12xBlockScaledDenseGemmKernel` /
`mm_fp4` 쪽 철자다. 226/310 = 73% 가 그 커널의 실효율이다.

**되돌려야 할 결정**: 이 155 위에 "fp4 는 바이트만 줄이고 연산은 그대로"라고 판단한
곳들 — 특히 프리필 밀집 GEMM 기각(바로 앞 항목)은 fp8 천장 155 를 전제했다. 네이티브
NVFP4 라면 밀집 GEMM 은 104 → 최대 310 이고, 커널 실효율 73% 를 잡아도 226 이라
**2.2배**다. 2.30 s → 1.06 s, 프리필의 7.3%. 게다가 fp4 가중치는 fp8 팩의 절반
(0.74 GB/rank). 재는 순서: `mm_fp4` 를 백엔드별로(`b12x`/`cute-dsl`/`cutlass`) 밀집
형상에서 실측 → 넘으면 수치 브래킷.

**교훈**: ISA 발행률은 명령 하나를 어셈블해 재는 것으로 끝내면 안 된다. 같은 형식에
빠른 경로와 느린 호환 경로가 둘 다 있으면 느린 쪽만 재고 "이득 0"이라 적게 된다.
벤더 스펙과 클럭 환산으로 교차 검증할 것.

## nvfp4 밀집 GEMM — 2.3배 · 메모리 절반 · 오차 3.7배 (2026-09-03)

fp4 천장 정정(위) 뒤 프리필 밀집 GEMM 을 다시 쟀다. 유휴 GPU, `mm_fp4`.

| M=8192 K=4096 | fp8 deepgemm | nvfp4 b12x | nvfp4 cutlass | 비 |
|---|---|---|---|---|
| N=4096 | 104.5 | 221.2 | **236.6** | 2.26× |
| N=2048 | 88.3 | 206.2 | 216.2 (auto 217.0) | 2.46× |
| M=4096 N=4096 | 103.6 | **255.3** | 220.2 | 2.46× |

`cute-dsl` 과 `trtllm` 백엔드는 이 형상을 안 받는다. 최고값은 310 천장의 76~82% 다.

**품질 (실제 체크포인트 가중치, 양쪽 피연산자 양자화, 활성 이상치 40배 포함)**

| | 이상치 없음 | 이상치 있음 |
|---|---|---|
| fp8 블록 128 (현 서빙) | 3.65e-2 | 2.86e-2 |
| nvfp4 (블록 16 + e4m3 스케일) | 1.34e-1 | 1.21e-1 |
| 비 | 3.7배 | 4.2배 |

**거래**: 밀집 GEMM 2.30 → 1.00 s (프리필의 7.7%), 팩 1.47 → 0.74 GB/rank,
오차 3.7배. 중간 지점은 없다 — 2.3배는 가중치와 활성이 **둘 다** fp4 여야 나오고,
가중치만 fp4 인 W4A8 arm 은 GEMM 이 fp8 속도로 돈다.

**문맥 둘**: (1) 이 체크포인트의 전문가 가중치는 **이미 이 오차 수준**으로 돌고
GPQA Diamond 90.57% 를 낸다. (2) 그럼에도 recipe.yaml 은 fp4 대상을
`mlp.experts.*` 로 한정하고 밀집 투영·인덱서·lm_head 는 뺐다 — 제작자의 보수적
선택이지 하드웨어 제약이 아니다.

**판정**: 오프라인 노름으로는 못 가른다. 노브로 만들어 기본 끄고 브래킷
(9/9 검색 · 0/16 한국어 · pos-1 수용률 ±2% · C=1/2/4)을 돌린다. 하나라도 움직이면
기각하고 여기에 적는다. 재현: `probes/fp8_scale_granularity.py`(속도),
`probes/nvfp4_dense_accuracy.py`(품질).

## 부팅 로더: instanttensor 는 내내 있었다, 프로필이 안 가리켰을 뿐 (2026-09-03)

`glm53:v13-b12x-it` = 베이스 + `pip install instanttensor` 는 **이틀 전부터 네 노드에
전부** 있었다. `profiles/glm53.env` 의 `PROFILE_IMAGE` 가 베이스 태그를 가리켜서 쓰이지
않았고, 그래서 매 부팅이 평범한 safetensors 로더로 340초를 썼다.

같은 날 `LOAD_FORMAT` 기본만 instanttensor 로 바꿨다가 부팅 하나를 날렸다 — case 문은
문자열을 통과시키고, 이미지에 패키지가 없어 75초 뒤 전 랭크가
`No module named 'instanttensor'` 로 죽었다. **문자열 검증은 검증이 아니다.** 런처가
이제 실제 이미지에 `import instanttensor` 를 시켜 보고 안 되면 부팅 전에 멈춘다.

전환 전 확인:
- `-it` 이미지에서 import 통과, `torch.cuda.nccl.version()` = (2, 29, 7)
- pip 이 nvidia-nccl-cu13 을 2.30.7 → 2.29.7 로 내린다고 표시하지만 **실질 무해**:
  현재 서빙 컨테이너도 이미 torch nccl (2,29,7) 로 돌고 있다(pip 메타데이터만 2.30.7).
- 오버레이 preimage **20/20 일치, 불일치 0** — 추가 패키지는 `instanttensor/` 와
  `nvidia/nccl/` 만 건드리고 오버레이는 그 경로를 대체하지 않는다.

굽는 비용(재확인용): `pip install` 53초 + 레이어 커밋. 네 노드가 31.2 GB 베이스를 이미
갖고 있으므로 전송은 없고 각자 로컬 빌드라 병렬 3~5분.

되돌리는 길: `PROFILE_IMAGE="glm53:v13-b12x"` + `LOAD_FORMAT=auto`.

**남은 위험**: 배포 보고서가 말하는 다중 노드 조용한 랭크 사망은 부팅을 죽이지 않고
출력을 망가뜨린다. 운영자 확인으로는 그동안 문제가 없었다. 그래도 런처가 매 부팅마다
load-format 을 무조건 찍게 두어(전에는 auto 가 아닐 때만 찍었다) 나중에 이상한 숫자가
어느 로더의 것인지 귀속할 수 있게 한다.

## bf16 원본 해제 — 랭크당 3.67 GB, KV 토큰 +39.5% (2026-09-03)

`VLLM_GLM53_FP8_DENSE_FREE_BF16=1`. `quant_method` 가 교체되면 apply() 는 fp8 복사본만
읽으므로 bf16 원본은 죽은 무게다. 같은 arm 구성에서:

| | KV 메모리 | KV 토큰 | GMU |
|---|---|---|---|
| 해제 전 | 26.9 GiB | 3,497,358 | 0.77 |
| **해제 후** | **29.82 GiB** | **4,880,721** | 0.78 |

`[fp8-dense] ...FREE_BF16=1: released 3.67 GB of bf16 sources (0 linears kept theirs
for a bias)`. GMU 차이 1.3% 를 빼도 증가분의 대부분이 해제분이다.

**여기까지 오는 데 부팅 세 번을 날렸고, 세 번 다 원인이 달랐다.**

1. **빌드 안에서 해제** → `maybe_build_fp8_dense` 는 이른 호출을 견디도록 쓰였는데
   (AutoWeightsLoader 가 체크포인트를 다 훑기 전에 자식 load_weights 로 들어온다)
   해제는 되돌릴 수 없다. `parameter.py:221` 에서 로더가 1차원 빈 텐서를 만나
   IndexError. → 별도 패스로 분리.
2. **`GPUModelRunner.load_model` 에서 호출** → `glm53_drop_audit` 은 **어떤 조합에도
   없는 고아 모듈**이라 죽은 코드였다. 로그에 해제 줄도 예외 줄도 없이 조용히 통과.
3. **`Glm5NextForCausalLM.forward` 에서 호출** → **그 forward 는 불리지 않는다.**
   `Glm4vForConditionalGeneration.forward` 가 건너뛰고
   `self.language_model.model(...)` 을 직접 부른다.

정답은 `Glm5NextForConditionalGeneration.forward` 다 — vLLM 이 실제로 부르고,
컴파일 영역 위라 일회성 free 가 트레이스 그래프에 안 들어가며, 프로파일 forward 안이라
그 뒤 KV 산정이 줄어든 용량을 본다. 셋 다 계약 테스트로 고정했다.

**대가**: bias 와 예외 폴백이 사라진다. 이 모듈이 받아들이는 선형 중 bias 를 가진 것은
없다(로그의 "0 linears kept theirs for a bias" 가 그 확인이다).

## 부팅 시간 901 → 345초 (2026-09-03)

`glm53:v13-b12x-it` + `LOAD_FORMAT=instanttensor`: 가중치 **184 GB 를 58초**에 읽는다
(평범한 safetensors 로더는 340초, 그 구간만 5.9배). 여기에 다른 세션의 nvcc·osar·
TileLang 영속 캐시(#248/#249)가 더해져 전체 901 → **345초**.

## ★osar 에필로그 융합(AR+residual+norm+quant) — 인접성 실측으로 기각 (2026-09-04, 무부팅)

제안: k_oneshot 각각 뒤에 residual add → norm → quant 체인이 붙는다는 전제로,
이를 AR 커널 에필로그에 접어 콜렉티브당 2-3개씩 200-300개(≈1.7-2.4%)를 없애는
축. 벤더 `fuse_allreduce_rms`의 sm_121 런타임 게이트와 무관하게 osar는 우리
커널이라 게이트를 직접 만들 수 있다는 것이 근거였다.

**기각 — 전제인 "AR 직후 체인"이 이 모델에 존재하지 않는다.**

소스(정본): MHC 45층 전부에서 attn AR(o_proj)의 소비자는 norm 이 내장된
`hc_fused_post_pre` 단일 TileLang 커널이고(`glm5next_model.py`), MoE AR 의
소비자는 다음 층의 같은 커널이다. 비-MHC(MTP) 경로의 add+norm 도 이미
인듀서가 한 커널로 융합한다. 트레이스 교차확인(08-31 스톡 체인 트레이스,
`census.py --after k_oneshot`): +1번째 커널 = mhc_fused 84.0(82.4%) ·
인듀서 fused_add_rms_norm 8.0 · mhc_post 6.0 · 단발 4.0 — 흡수 가능 천장
≈ 10개/스텝 ≈ 0.08%. +2/+3의 quant·게이트는 mhc 커널 뒤라 osar 에필로그가
아니며, quant 축은 기존 판정(소유 가능분 mhc_pre 에필로그 ~0.15ms)이 닫았다.

스코핑 주의: 위 수치는 08-31 **스톡 체인** 트레이스 기준이다. 메가커널
세그먼트가 AR 소비자인 부팅(EXP-6 계열)에서는 인접 성분이 다를 수 있으니
MK 팔에서 이 결론을 재인용하기 전에 `census.py --after k_oneshot` 으로 다시
셀 것. 도구(신규): `census.py <trace> --after REGEX [--depth N]`.

교훈: 일반 vLLM 레이어 구조에서 이식한 추정이 glm5-next 의 MHC 융합 구조와
충돌한 사례 — "그룹 합계는 소스를 읽기 전까지 의문점"과 같은 부류다.

## ★PDL은 vllm-mhc 쪽에서 이미지가 봉인 — pdl_sync/trigger는 dead code (2026-09-04, 신규 컨테이너 실측)

mhc TileLang 커널의 `T.pdl_sync`/`T.pdl_trigger`는
`ENABLE_PDL = current_platform.is_arch_support_pdl()` 조건부인데, 신규
컨테이너 실측으로 이 값이 **False**다(GB10). 이미지 구현이 사유를 명시한다
(`vllm/platforms/cuda.py`): "PDL lowering is unvalidated on SM12x (GB10) and
races on KDA state kernels there; keep it to Hopper/Blackwell-datacenter."
(`major in (9,10)`). 즉 **vllm mhc 체인의 PDL 재활성은 저자가 경합 사유로
봉인한 축** — 이 게이트를 우회하는 실험 금지.

**스코프**: 이 판정은 vllm/TileLang mhc 커널의 플랫폼 게이트 이야기다.
메가커널 자체 발사의 PDL(EXP-12, `VLLM_GLM53_MK_PDL`)은 우리 커널의 런치
속성으로 직접 켜는 별개 축이며 이 판정과 무관하다.

재현 (srv4, 서빙 컨테이너 미접촉):

```bash
docker run --rm --gpus all --entrypoint python3 glm53:v13-b12x -c \
  "from vllm.platforms import current_platform; print(current_platform.is_arch_support_pdl())"
```

## ★31차 — 자체 소유 미세 융합 묶음 2 (EXP-20): 오프라인 게이트 결과 (2026-09-04~05, srv4)

_원장 번호: 이 작업은 29차로 적혀 있었으나 그 사이 29차(메가커널 로컬 양자화, PR #307)와
30차(비상주 v2 레인, PR #305)가 먼저 머지돼 31차로 옮겼다 — 28차의 선례("원장 번호 27 은
PR #290 이 쓰고 있어 28 로 적는다")와 같은 처리다. 아래는 PR #304 리뷰(15건) 반영 뒤 09-05 에
다시 잰 최종 수치다(프로브 11 = 조용한 GPU, 12 = KDAPROOF3 부팅 창); 첫 판과 무엇이 왜
달라졌는지는 "방법 정정"에 적었다._

"소소한 이익 묶음"(R2 선례) 방식으로 네 축을 한 캠페인 창에 묶는다. 각 축은 킬스위치
노브(프로필 기본 0)이고, 판정은 오프라인 수치 게이트 + 인그래프 물리확인 + e2e 무회귀.
프로브 `probes/run_micro_fusion_check.sh`(fresh container, 그래프 리플레이, 층마다 다른
가중치/상태). 09-04 18:42 rank3 트레이스(무장 세트, 스텝 64.8 ms, 1,548 커널) 기준.

| 축 | 노브 | 런치 −/스텝 | 수치 | 그래프 리플레이(층당 us, 조용한 GPU, 붙여 돌린 두 리플레이의 둘째) | 스텝 Δ |
|---|---|---|---|---|---|
| 게이트 top-k epilogue (`moe_gate_sm121`, 마지막 도착 블록) | (기각, 트리에 없음) | 42 | **bit-exact**(logits·ids·weights 568행, 동률 포함; `FusedTopKBiasRouter` 훅 서빙 경로 포함) | M=8 28.2 → **31.3**, M=16 28.1 → 45.6, M=32 34.3 → 86.8 | **+0.13 / +0.74 / +2.2 ms — 기각** |
| f_b+g_b 듀얼 GEMM (`glm53_kda_onepass`, BLOCK_N=32) | `VLLM_GLM53_KDA_DUAL_GEMM` | 34 | M≤16 **bit-exact**(cuBLAS 16x16 wmma 와 동일, N 타일 폭에 무관), M=32 는 cuBLAS 가 다른 커널을 골라 ≤10/131,072 원소 1 ulp | 10.0 → 6.4 (M=8), 10.1 → 6.2 (M=16), 8.8 → 6.7 (M=32) | −0.12 / −0.13 / −0.07 ms |
| KDA 원패스 conv+복사4+recurrent+norm (`glm53_kda_onepass`) | `VLLM_GLM53_KDA_ONEPASS` | 204 (층당 7→1) | conv 상태·recurrent 상태·출력 **bit-exact**(acc 1/3/5/8, SD/DS 레이아웃, bf16/fp32 conv 상태, varlen T=[8,3,8,1], 같은 카운터로 두 번째 발사, 2스텝 사슬 8 케이스; 부팅 자가진단 3 케이스 PASS) | C=1: 68.8 → 67.6 (BV=8; BV=16/32 는 72.0/73.3 으로 손해); C=4: 228.4 → 221.5 | −0.04 (C=1) / −0.23 (C=4) ms |
| kpool 갱신 int64 positions 직접 (`glm53_kpool_tail_select`) | `VLLM_GLM53_KPOOL_UPDATE_DIRECT_POS` | 11 | 캐시 쓰기 **bit-exact**(47행) | 6.9 → 5.6 | −0.014 ms |

**방법 정정 — 첫 판(원패스 −0.14, 듀얼 −0.09)과 다른 이유.** (1) 첫 판의 stock KDA 체인은
타이밍 그래프 *안에서* 입력을 clone 했다(stock conv 가 in-place 라서). clone 노드가 stock
쪽에만 얹혀 stock 74.1 us 가 부풀었고, clone 을 그래프 밖에서 미리 해 두니 stock 68.8 /
원패스 67.6 — 이득은 −0.14 가 아니라 **−0.04 ms** 다. (2) 시간은 붙여 돌린 두 리플레이의
둘째("gpu")와 유휴에서 한 번 돌린 것("+launch")을 따로 적는데 둘이 거의 같다(제출이 실행과
겹친다): 이 묶음의 스텝 이득은 런치 간극이 아니라 커널 시간 자체에서 온다. (3) 듀얼 GEMM 은
BLOCK_N 을 128/64/32 로 쓸어 32 를 택했다(M=8: 7.6 → 6.7 → 6.4 us; 수치 등급은 N 타일에
무관). (4) 원패스는 fp32 conv 상태(`MAMBA_CACHE_DTYPE=float32`, MK-KDA 부팅)를 stock 커널처럼
받고, stock 세 커널의 스킵 조건을 따로 둔다(conv 는 spec 슬롯 0 이 널 블록이 아닐 때만,
recurrent 는 재개 슬롯이 유효할 때만, 둘 다 아니면 즉시 반환). (5) 교차 프로그램 의존
둘(헤드의 conv 상태 갱신, norm 꼬리)은 단조 카운터의 마지막 도착자(NV 의 배수, NV 는 2의
거듭제곱)로 풀어 리셋이 없고 int32 랩이 무해하며, 카운터 버퍼(256 요청 × 16 헤드)를 넘는
배치는 raise 가 아니라 stock 으로 내린다. (6) 부팅의 첫 eager forward(프로필 런)에서
자가진단(stock 체인 vs 원패스, 플릿 형상 3 케이스 bit 비교)이 돌고 불일치면 그 부팅은
`[kda-onepass] self-test FAIL ... DISARMED` — 아래 레이아웃 함정의 회귀 감시다. (7) 배선
(`glm5next_kda.py`)은 모듈의 `resolve()/gate_gemms()/spec_onepass()` 만 부르고, 노브·가드·
로그·카운터는 전부 모듈 안에 있다; `_forward` 가 o_norm 을 스스로 적용한다(플래그 없음).
(8) 2차 리뷰(Codex): 카운터는 **블록 폭(NV)별 버퍼**다 — 마지막 도착 판정이 `count % NV` 라 한 버퍼가
두 NV 를 보면(프로브의 BV=8/16/32 스윕) 이른 프로그램이 마지막으로 뽑혀 conv 상태 갱신이 읽기와
경주한다; `resolve()` 가 서빙 폭(BV=8, NV=16)을 캡처 전에 준비하고 캡처 중 없는 폭은 stock 으로
내린다. 프로브의 출력 게이트는 rel 이 아니라 **비트 동일**(8 케이스 + 자가진단 3, 프로브 13 PASS).
`cudaGraphLaunch` 의 호스트 시간은 `host submit` 로 따로 재니 stock 0.2 / 원패스 0.3 us/층
(그래프당 ≈ 7~10 us, 노드 수 238 vs 34 에 거의 무관) — 이 묶음의 이득이 호스트 제출이 아니라
장치 시간에서 온다는 (2) 의 판독과 같다.

**게이트 top-k 의 비트 일치는 소스가 아니라 프로브가 정했다.** 커널 소스
(`single_group_topk_block_kernel`)는 `sigmoid_accurate = 0.5f*tanhf(0.5f*x)+0.5f` 와
`cg::reduce` 합을 쓰지만 이미지 빌드의 비트는 (1) sigmoid = `1/(1+exp(-x))`(Triton exp +
IEEE 나눗셈; 0/2048 — libdevice tanh 형태는 절반이 1~2 ulp 어긋남, fast-math 빌드 추정),
(2) 재정규화 합 = 순위순 순차 합 + `div_rn(scale, s+1e-20)`(0/4096; 헤더가 시사하는
xor-butterfly 순서는 1/3 행에서 1~3 ulp)이었다. 선택은 편향 점수 내림차순·동률 낮은
인덱스(packed 64-bit key 로 한 랭크당 축약 1회).

**KDA 원패스의 bit-exact 는 루프 모양에 달렸다.** stock 커널 세 개를 op 순서 그대로 옮기고
conv 출력을 bf16 으로 반올림하면 상태·출력이 비트 일치한다. 그러나 토큰 루프의 [K] 피연산자를
prefetch/hoist 하거나 norm 꼬리를 [8,128] 2-D 타일로 바꾸면 Triton 의 레이아웃 배정이
커널 전체에서 바뀌어 l2norm 축약 트리가 달라지고 recurrent 상태가 ~3% 원소에서 1 ulp
어긋난다(세 변형 모두 같은 74,353 원소). 그 변형들은 3~5 us/층 빨랐지만(64.4 vs 69.8)
비트 일치를 택했다. 남은 후보: `tl.static_range` 1-D 꼬리(레이아웃 불변 가정)는 미검증.

**게이트 epilogue 는 기각이다 — 수치가 아니라 병렬성 때문에.** 1판([8,512] fp32 타일 6개를
동시에 든 채 랭크당 축약 3회)은 레지스터 스필로 커널 전체가 4배 느려졌고(111 vs 27.5 us/층,
경합 GPU), 2판(packed 64-bit key = 점수 비트<<32 | 65535−expert 로 랭크당 `tl.max` 1회 +
`tl.gather`, 4행 청크)도 조용한 GPU 에서 M=8 28.2 → 31.3, M=16 → 45.6, M=32 → 86.8 us/층.
stock 의 `single_group_topk_block_kernel` 은 토큰마다 CTA 하나(256 스레드, 워프 bitonic)를
쓰는데 epilogue 는 마지막 도착 CTA 하나가 전 행을 직렬로 고른다. 그리드 배리어(스핀)로
18 블록에 나누면 되지만 같은 스트림에서 MK 의 48블록 배리어 커널과 SM 을 나눠 잡다 교착할
수 있어 쓰지 않는다. 코드는 트리에 두지 않고(운영자 규칙) 비트 일치 요령만 남긴다 —
MoE 를 persistent 세그먼트로 접는 날 top-k 는 이 요령대로 그 안에 들어가면 된다.

**정직한 합계(C=1, 조용한 GPU 그래프 리플레이 기준)**: 듀얼 −0.12 + 원패스 −0.04 + kpool
−0.014 ≈ **−0.18 ms/스텝(0.3%)**, 런치 −249/1,548(16%); C=4 는 −0.07 −0.23 −0.014 ≈ −0.3 ms.

_재정정(2026-09-05): 09-05 07:04~07:11 의 정정("원패스는 `use_spec` 에서만 도는데 이 V2 러너는
`spec_sequence_masks` 를 만들지 않으므로 프로덕션에서는 −204 런치가 발생하지 않는다")은 32차 §11 의
**정정 전 가설**에 기댔다. §11 의 04:50 정정(`mamba_hybrid.prepare_attn` 이 정상 스텝에
`num_decode_draft_tokens_cpu` 를 채운다)과 체인12 KDAPROOF3(`kda lane CAPTURED into the decode
graph: T=32 n_spec=4` — 같은 spec-verify 전제의 MK-KDA 레인이 캡처됐다)이 그 가설을 철회했고,
이미지 코드(`build_for_cudagraph_capture` 가 `query_start_loc` 차분에서 spec 행을 만든다)로
유니폼 디코드 그래프의 캡처 형상이 곧 순수 spec-verify 배치임을 확인했다. 따라서 오프라인 합계는
프로덕션에서 도달 가능한 몫이다(섞인 스텝은 스텝 단위로 stock). 단, 증명은 캡처 시점의
`one-pass KDA serving` 줄이며, 그 줄이 없는 부팅은 원패스 축만 보류하고 듀얼 + kpool(런치 −45,
≈ −0.13 ms)로 판정한다._
브래킷 해상도(CV 1.7%) 아래 — R2 규칙(bit-exact + 트레이스 소멸 + 무회귀)으로 채택 판정한다.
원패스의 GPU 시간 이득이 작은 이유: recurrent 는 토큰 8개의 직렬 지연 사슬(상태 슬롯 8 MB
쓰기 포함)이라 conv·복사·norm 을 그 사슬에 접어 넣어도 사라지는 것은 작은 커널 시간과
런치 간극뿐이고, 마지막 도착 블록의 norm 꼬리(토큰 8개 직렬)가 되돌려 준다. C=4 에서
커지는 것은 그 꼬리가 헤드 병렬로 겹치기 때문이다.

**경합 주의**: srv4 는 rank 3 노드다. 다른 세션 체인의 decode 레그 중 프로브(00:00)를 돌리다
죽였고, 이후 프로브는 스크래치 `gpu_window.sh` 로 boot/idle 창에서만 돌렸다. 경합 창의
수치(첫 판 stock 체인 139 us/층; 프로브 12 의 KDAPROOF3 부팅 창 stock 105.5/520.7 us/층)는
수치 게이트(전부 PASS)에만 쓰고 시간은 원장에 쓰지 않았다.

- 다음: 리뷰 반영 PR(#316) 머지 → 운영자 승인 뒤 묶음 부팅(base → cand 세 노브 → base, 프리필
  2048/8192 동반) → cand 부팅 로그 `[kda-onepass] self-test PASS ... ARMED` 와 캡처 시점의
  `one-pass KDA serving` + 트레이스에서 `_causal_conv1d_update_kernel` 0 /
  `layer_norm_gated_fwd_kernel` 0 / `_kda_onepass_spec_kernel` 34 / `_dual_gate_gemm_kernel` 34 /
  인덱서 `direct_copy` −11 확인 → 채택 시 프로필 기본값. 캡처 줄이 없으면 원패스만 보류(위 재정정).

## ★★★33차 — 프리필 축 탐구, 첫 표적 완결: 콜드 JIT 세금을 부팅으로 (PREFILL_WARMUP=1, 2026-09-05)

운영자 "프리필 개선 방안 탐구" → "진행해". 원장 재조사로 축의 지도를 확정하고, 그중
**구현은 끝나 있고 스위치만 꺼져 있던** 유일한 레버를 승격·검증했다.

### 프리필 축의 지도 (탐구 결론 — 신규 측정 없음, 근거는 각 절)

| 축 | 판정 | 근거 |
|---|---|---|
| 웜 처리량 | 물리 바닥권 | GPU busy 99% · 사다리 평탄(2,638→2,726) · 어텐션 gather는 MK-MLA v5가 204 GB/s=천장 91%로 회수 완료 · MoE 226 TFLOP/s=fp4 천장의 73% |
| **콜드 세금(길이별 첫 요청)** | **남은 표적** | 09-01 실측 11~41% · "사용자가 체감하는 유일한 프리필 비용" |
| NVFP4 프리필 밀집 GEMM | **재시도 후보** | 두 사망 모두 비(非)코드 — ① JIT earlyoom(→ `probes/nvfp4_prefill_warm.py` 4노드 PASS로 해결) ② §9 플릿 스톨 서명(srv4 외부부하/스왑, 이후 swapoff·외부부하 로깅·스톨 리포터 완비). 오프라인 2.3배+가중치 절반, 프리필의 ~7.7% |
| 프리픽스 캐시 | 잠김 | `PREFIX_CACHE=0` — 캐시 히트 토큰의 드래프터 KV 미기입(#47926 계열, 다중 파일 수정). 에이전트 장문 트래픽 기준 잠재 최대 TTFT 레버 — 포팅이 다음 대형 항목 |
| UNION_PREFILL · MAXBATCH 16384 · MLA q fp8 mma · LL128 | 재론 금지 | §5 · 09-01 사다리 · 전용명령 검토 · NCCL 스윕 |

### 채택: `PREFILL_WARMUP=1` 프로필 기본값 (런처 post-boot 웜업)

`launchers/prefill-warmup.py`(PR #192, #243 수리) — 서버 기동 후 길이별(2048/4096/8192)
**정확히 그 토큰 수만큼의** 실제 프리필을 1회씩(`max_tokens=1`, 내용 매번 상이, 출력 폐기).
세금을 첫 사용자 요청이 아니라 부팅이 지불한다. 브래킷 하네스(`bench/ab-lever.sh` ·
`launchers/ab-glm53.sh`, srv2 사본 `ab-lever2.sh` 포함)는 **자기 부팅을 `PREFILL_WARMUP=0`으로
핀** — 콜드/웜 채널은 브래킷의 재산이고 웜업 6건이 첫 디코드 레그의 2s 창 안에 끼어들기
때문. 0은 호출자 env 킬스위치로 잔존.

**하네스 결함(수리, c938323)**: 첫 무장 부팅에서 웜업은 **아무것도 데우지 못하고 요청 1에서
사망** — 이 포크의 `/v1/completions`은 최상위 `prompt_token_ids`를 무시(400 "Either prompt or
prompt_embeds must be provided")하고 `{"prompt": {"prompt_token_ids": ...}}` 객체 형식도 검증
거부. **정수 리스트가 이 포크의 토큰 ID 형식**이다(`usage.prompt_tokens == len(ids)` 검증).
#189(빈 섹션)과 같은 부류 — rep1-vs-rep2 영수증 줄이 없었으면 "무장=웜업"으로 오독할 뻔했다.
계약 테스트 추가.

### 측정 (부팅 c938323 — ALL0S 기본값 위, 같은 코드라 컴파일 캐시 웜 · 헬스 210s)

수동 실행 영수증(무장 사망 직후 같은 부팅에서 수정본 수동 실행 — 그 사이 트래픽은 3토큰
테스트 요청뿐):

| 길이 | rep1 | rep2 | 세금 |
|---|---|---|---|
| 2048 | 0.86s (2,387 tok/s) | 0.84s (2,424) | ~1.5% |
| 4096 | 1.57s (2,604) | 1.56s (2,632) | ~1% |
| **8192** | **6.04s (1,357)** | **3.12s (2,628)** | **48%** |

2048/4096의 세금이 작은 것은 같은 코드의 전 부팅들이 컴파일 캐시를 데워둔 탓 — 이 부팅의
프로세스 내 오톊닝(FLA 청크 등)은 8192 형상에 몰려 있었다. 웜업 후 **래더 프로브의 첫 요청(=사용자
첫 요청)이 웜 숫자에 랜딩**:

| 요청 | 실제 tok | cold | warm | 콜드 세금 |
|---|---|---|---|---|
| 2048 | 5,769 | 2,606.8 | 2,619.7 | **0.5%** |
| 4096 | 11,515 | 2,653.3 | 2,653.1 | −0.0% |
| 8192 | 23,033 | 2,593.4 | 2,721.4 | **4.7%** |

(승격 전 기준: 09-01 실측 11~41% · 체인19 콜드/warm 2,518/2,702 ≈ 7%.) 웜 2,721@23K는
프로덕션 대역(2,702–2,726) 정상, 디코드 스모크 step/s 17.7 · 창 중앙 17.9 [17.5, 18.9] =
캠페인 최상단 밴드 — ALL0S 기본값 위 무회귀. **이 부팅이 프로덕션**.

### 잔여

- 8192(23K=3청크+꼬리)의 잔여 4.7%: 웜업은 정확히 8192 단일 청크만 데운다 — 다중 청크 경계·
  꼬리 형상은 미웜. `PREFILL_WARMUP_LENS`에 장문 엔트리(예: 24576)를 추가하면 닫히지만
  그만큼 부팅이 늘어난다(운영자 결정 사항).
- 다음 표적: ① 클린 플릿에서 NVFP4 프리필 재판(웜 프로브 선행 — 두 사망 원인 모두 해소됨)
  ② #47926 포팅으로 프리픽스 캐시 해제(에이전트 장문 트래픽 TTFT 최대 레버) ③ 프로브 먼저:
  EXP-5 KDA 청크 스윕 · GLM53 프리필 형상 MHC 스윕 · b12x MAC 래더.

### 플릿 협업 기록

- 검증 부팅은 다른 세션의 ALL0S 확인 부팅(13:46, 70요청 완주 · 한국어 3패스 포함) **후에**
  실시. 그 세션의 미푸시 프론티어(6f804a5)를 srv2에서 fetch해 머지 후 푸시(5bd7240) —
  부팅 구성은 ALL0S 채택 + 웜업. srv2 갭 러너(gap_bench8, ktail 스윕 대기)는 헤드
  컨테이너 공백이 ~1분<120s 임계라 발사하지 못하고 계속 대기 중.
- srv4 외부 테넌트(nemotron 등)가 10.8 GiB anon을 잡고 있어 memfree preflight가 GMU
  0.73→0.66으로 자동 조정 — KV는 토큰 고정(2,000,000)이라 불변.

### 정정 (같은 날 15:4x) — 33차 검증 부팅은 셋 ON 구성이었다; PR #322 머지 후 교체

14:21 부팅은 당시 srv2 프론티어(6f804a5)를 그대로 물려받아 **ALL0S 셋(KDA+LOCALQ+ARPF)이 켜진
구성**이었다 — 위 디코드 스모크 17.9 도 그 구성의 값이다. 같은 세션이 13:56 에 연 PR #322 가
셋 승격을 **되돌림**으로 판정(체인22 한국어 3회 0·1·1/16, 병기 아닌 중국어 새어듦; 셋 켬 6/8 vs
기본값 캠페인 5/16, Fisher p≈0.055 — "+0.7% 를 위해 혼용률 3배는 운영자 규칙 위반"). #322 머지
(a661f94) 직후 **병합 기본값(셋 OFF + 웜업 ON)으로 재부팅**:

| 게이트 | 값 |
|---|---|
| 무장 웜업(자동, 이번엔 손수행 없음) | 2048 rep1 **986** → rep2 2,417 tok/s(세금 2.45×, 이 부팅의 콜드 자리는 2048) · 4096 2,553→2,648 · 8192 2,593→2,620(직전 부팅의 수동 웜업이 디스크 캐시를 데워 세금 ~1%) — "done, slowest 3.16s" |
| 한국어 1패스 | **1/16** — 프롬프트5 `扰动` 누출 = PR #322 정정이 문서화한 기본값 기준선 패턴(셋 이전 부팅에도 동일) |
| 디코드 스모크 | step/s 18.3 · 창 중앙 **18.7 [18.4, 18.9]** = 캠페인 최상단(#323 라운드3 커널 병합 효과로 추정) |
| 부팅 노브 | KDA=0 · LOCALQ=0 · ARPF=0 · GEMM2=1 · MLA=1 · PREP_FUSED=1 — 병합 프로필과 일치 |

**이 부팅이 현재 프로덕션.** 33차의 웜업 승격 판정(영수증·첫 요청 랜딩)은 두 부팅 모두에서
성립 — 웜업은 런처 쪽 변경이라 서빙 구성과 직교한다.

## 메가커널 정확성 리뷰 수정 (2026-09-05, 기준 3b2f180, 미배포)

정확성 수정: MHC의 deferred-publication 분기에 shared tile 재사용 동기화,
LOCALQ의 홀수 시작 split에 상대 버퍼 인덱스, KDA 짧은 verify 요청의 retained
conv window 전체 이동. MLA 실KV shadow 실패는 Python DISARM에 그치지 않고
worker 오류로 전파하며, KDA shadow는 takeover보다 우선한다.

검증 수정: 비유한 오차·분모 norm overflow를 실패 처리하고, GEMM 부팅 exact 게이트가
실제 v1 global/local 및 v2 M=8/16/32를 선택·검사한 뒤 원래 probe 설정을 복원한다.
KDA 부팅 게이트는 nq=1/4/6/8과 모든 쓰인 recurrent 슬롯을 검사한다.
`tests/test_megakernel_*regressions.py`는 실제 Python 함수 및 CUDA에서 추출한
제어문을 CPU에서 실행한다. 최초 세 CUDA 결함을 되살린 변형이 각 검사에서 실패했다.
이 회귀 검사들은 `tests/test_logic.py`의 일반 배포 검사에 포함했다.

새 `probes/mk_smlp2_concurrent_probe.py`는 v2/native-activation 체인과 SMLP2를
단독 및 MoE 양쪽 발행 순서의 CUDA graph로 비교한다. 출력·유한성·반복 replay를
통과한 뒤 정/역순으로 시간을 수집한다. 기존 벤치의 조용한 Torch activation
fallback도 제거했다. **새 성능 수치와 서빙 품질 결과는 아직 없다.**

검증 상태:

- 로컬 `tests/test_logic.py` **6,236 checks + 새 회귀 검사 38개 통과**.
  Python 문법, glm53/dsv4 합성 및 소스 일치, `git diff --check`도 통과했다.
- srv2 `glm53`, srv4 `glm53-worker` 및 외부 테넌트가 가동 중이어서 GPU 실행과
  racecheck는 수행하지 않았다. 서비스 재기동·배포·기본값 변경은 하지 않았다.
- CUDA compile-only 시도: srv2의 별도 runc 컨테이너, GPU 장치 없음, CPU 1개,
  RAM/swap 3 GiB, network=none, 단독 `/tmp/stkernel-mk-review-inGtyiFg` 마운트.
  이미지 `a3dd4c0f6cbb`, torch 2.13.0+cu130 / CUDA 13.0, sm_121a 플래그.
  약 29초 후 메모리 한도로 OOMKilled=true / exit 137. **컴파일 성공이 아니다.**
  실패 소스 SHA256 `5b2ca90381fe85fca0249465e7c800343563a2be86ed55e08df9e33f32fc8ff3`;
  이후 호스트 probe-state 음수 ksr2 복원 범위만 수정했고 재시도하지 않았다.
  로그는 srv2의 위 임시 디렉터리에 있으며 SHA256은
  `ca83fbeed0f7fb3aca25781265168e8a49a273d8cfa9b2706eaacd71418bd961`.

다음 GPU 검증: idle window에서 위 README의 `--boot-gates` 벤치,
MHC racecheck, SMLP2 동시 실행 프로브를 수행한 뒤
서빙 성능·한국어·수용률을 별도 브래킷으로 판단한다. 기존 SMLP2 65.9→28.2 µs
마이크로벤치 값을 이번 수정의 실측 이득으로 재사용하지 않는다.
