# 실측 원장 (measurement ledger)

이 스택에서 내린 모든 성능 판정과 그 수치. **여기 없는 주장은 미실측이다.**
방법론·함정은 [README의 bench 표](README.md#bench--측정-도구와-함정) 참조.
기준 환경: 4× GB10(SM121) · TP=4 · CRS812.

**이 파일은 45차(ST 엔진)부터다.** 그 앞의 항목 113개(2026-08-10~09-11, 물러난 vLLM 스택)는
[MEASUREMENTS_ARCHIVE.md](MEASUREMENTS_ARCHIVE.md) 에 **고치지 않고 그대로** 있고, 아래 "45차 이전" 절이
제목 한 줄씩으로 요약한다. `28차 §8` 처럼 번호로 부르는 참조는 그 파일에서 찾는다.
어느 노브가 지금 켜져 있는지는 [profiles/README.md](profiles/README.md), 디코드
스텝이 무슨 커널로 이루어지는지는 [STEP_KERNEL_MAP.md](STEP_KERNEL_MAP.md),
부팅이 필요한 다음 실험은 [RUNBOOK_KERNEL_CAMPAIGN2.md](RUNBOOK_KERNEL_CAMPAIGN2.md).

## 이 원장을 쓰는 규칙

아홉 줄이 이 파일의 판정 규율 전부다. 항목마다 되풀이하지 않는다.

1. **여기 없는 주장은 미실측이다.** 프로브·모델·추정은 원장 항목이 아니라 그 항목의 재료다.
2. **판정 채널은 C=1 step/s**(`bench/bench-dec.py`), 부팅 CV **1.7%**. 그보다 작은 기대값은
   단독 부팅으로 판정하지 않고 다른 팔에 얹는다.
3. **브래킷은 base→cand→base**, 노브 하나. **base 팔의 값은 명시**한다 — 프로필 기본값이
   움직인 뒤로는 "노브 없음"이 곧 stock 이 아니다(28차 §8).
4. **채택 게이트**: 품질 9/9 · 한국어 0/16 · pos-1 수용률 ±2 pct · 프리필 사다리 무회귀.
   수용률을 움직이는 변경은 tok/s 와 tokens/step 을 함께 적는다.
5. **무장 ≠ 서빙.** 노브가 1 이라는 사실은 커널이 돈다는 뜻이 아니다(22차·28차). 증거는
   셋 중 하나 — 부팅 로그의 증명 줄, 트레이스, 또는 거부가 스스로 말하는 로그.
6. **오프라인 프로브 숫자는 상한이거나 형상이 다르다.** 27차에서 상한 하나가 프로브 하나에
   절반 이하로 줄었다. 상한은 목표와 **같은 측정 형태**로 만든다.
7. **CUPTI 시간은 트레이스 사이에서 비교하지 않는다.** 개수가 정본이고 시간은 같은 트레이스
   안에서의 상대 비교로만 쓴다([STEP_KERNEL_MAP.md](STEP_KERNEL_MAP.md)).
8. **그리디 텍스트 diff 로는 판정하지 않는다** — 같은 구성의 두 부팅에서도 온도 0 응답이
   갈린다(28차 §8). 판정은 게이트와 브래킷이다.
9. **항목 번호는 PR 번호다**(2026-09-12). 절 번호를 파일 끝을 읽어서 정하는 규약은 세션이
   하나일 때만 성립한다 — 그날 하루에 세 줄의 작업이 몇 분 간격으로 머지되면서 **§30·§31·§32 가
   각각 두 번씩** 쓰였고, 그때부터 "§31 참조" 가 어느 쪽인지 알 수 없게 됐다. PR 번호는 GitHub 이
   유일하게 발급하니 조율이 필요 없고 상호 참조가 정확하다. 새 항목은
   `### <차> — <제목> (<날짜>, <노드>, PR #NNN)`, 참조는 `PR #NNN`. 이미 쓰인 §2~§40 은 그대로 두되,
   **겹친 여섯 개는 제목 끝에 PR 번호를 달아** 어느 쪽인지 읽을 수 있게 했다.

## 45차 이전 — 보관된 항목 (요약)

2026-08-10~09-11 의 항목 113개, 8685 줄은 [MEASUREMENTS_ARCHIVE.md](MEASUREMENTS_ARCHIVE.md) 로 옮겼다.
물러난 vLLM 스택의 기록이고 더 이상 바뀌지 않는다. **하나도 고치지 않았다** — 이 파일이 길어져 읽히지 않는 것이
기록을 잃는 것보다 나쁘기에 옮겼을 뿐이다. 제목이 곧 판정이라 아래 한 줄씩이 요약이다; 수치와 근거는 저 파일에 있다.

- GLM53 EP + 입력 준비: 67 tok/s 절대 목표 통과, 기본값 채택 (2026-09-10, PR #511)
- GLM53 EP K3: engine 회복, 출력 67 tok/s 미달 (2026-09-10, PR #511)
- GLM53 EP BF16 scatter: 품질 통과, 67 tok/s 목표 미달 (2026-09-10, PR #511)
- GLM53 EP tiled + SF6 원패스: 기본값 보류 (2026-09-09, PR #511)
- GLM53 TP 프리필 Q0 기본값 선택 (2026-09-09, PR #478)
- GLM53 AR–MHC consumer 결합 기본값 채택 (2026-09-08, PR #473)
- 찾아보기
- 기준선 — `dsv4` (슬리밍+인시던트픽스, 노브 전부 OFF)
- 기준선 — `glm53` (메가커널 세트 기본값, 2026-09-04 20:36 부팅)
- 커널 귀속 — 프리필의 완전 분해 (2026-08-09)
- 디코드의 실체 — 콘텐츠 바운드 (2026-08-09)
- 디코드 스텝 예산 — 커널 귀속 (2026-08-09, with_stack=false 캡처)
- 디코드 수용률 배터리 (2026-08-09, 동일 엔진·연속 5창 — 상대 비교 유효)
- 판정 원장
- ★★패브릭 200G 복구 — 프리필 +9.1% (2026-08-10, 채택)
- ★★UMA 페이지 마이그레이션 스톨 — 프리필 간헐 −15% 요동 근절 (2026-08-11, 채택)
- ★REFINE_PASS A/B — 수용률 레버 실측, 기각 (−38%, 수용률 반토막) (2026-08-12)
- ★MBU 효율 분석 — 포화≠효율, MoE 56%가 speculative 대역폭 (2026-08-12)
- 정밀 관측 — idle 뒤에 여지 없음, 4스트림 포화 확증 (2026-08-12)
- 미세 이익 라운드 — 실측 종결 (2026-08-11~12, "미세라도 여러개 모으면")
- 전 축 재확인 — 프리필·장문·attn/indexer 전수 점검 (2026-08-11, "다른 걸 더")
- 단일 스트림 극한 — 작은 GEMM 융합 여지 조사·이미 소진 판정 (2026-08-11)
- ★P2b — mhc small-M 타일 채택·tf32 prenorm 기각 (2026-08-11, "소소한 이익 묶음" 1호)
- 정찰 일괄 기각 — 잔여 후보 전수 소진 2회차 (2026-08-11)
- 소프트웨어 개선 경로 — 종결 선언 (2026-08-09)
- 남은 검증된 레버 (우선순위순, 2026-08-11 전면 갱신)
- 업스트림 비교체크 (2026-08-09, PR·이슈 ~25건 vs 이 스택)
- ★런북 정비 창 실행 (2026-08-11 심야, 운영자 위임)
- 인시던트 로그
- 방법론 교훈 (체인 운용)
- 인접 모델 브링업에서 역수입한 항목 (2026-08-29/30)
- GB10 메모리 예산 — 부팅 실패 세 얼굴이 한 원인 (2026-08-30, GLM-5.3 TP=4)
- 한국어 토큰 손상 — 용의자 8종 기각, 서명 확정 (2026-08-30, GLM-5.3 TP=4)
- 오버레이가 한 번도 실린 적 없었다 — 베이스 계약이 첫 실행에서 잡은 것 (2026-08-30)
- 토큰은 버려지지 않는다 — 계측기가 답한 것과, 계측기 자신의 결함 (2026-08-30)
- ★★GLM-5.3-Flash 2026-09-01 — 프리필 첫 실측, 그리고 축 셋이 닫혔다
- ★★GLM-5.3-Flash 스텝 재분해 — 62%가 줄일 수 없는 전문가 읽기 (2026-08-31)
- ★메가커널 2차 — 벤치 함정 넷, W4 −33%, MHC −22%, PDL, 그리고 deepgemm 과의 진짜 격차 (2026-09-02)
- ★★★39차 — 드래프터 축: DFlash 학습 레시피·DSpark 비교·HF 드래프터 15곳 조사, cfontes TR3-v3 브래킷 기각, 하니스 onepass 단일화 (2026-09-06 13:40~14:40 KST)
- ★★★38차 — b12x 정적 MoE 커널 2라운드: 남은 레버 셋의 실측 — 짝수 웨이브 기각, 분할 −1~2%, v4(256 B 세그먼트) −3.5% (2026-09-06 08:00~10:30 KST, srv2 프로브, 무부팅)
- ★★★38차 부록 — v4 `u` 원샷 벤치: 부팅 1회, 디코드 창 +2%, 프리필·품질·한국어 불변 (2026-09-06 12:20~12:48 KST, 운영자 지시 "원샷 벤치")
- ★★38차 §8 — 3라운드: A 링 상쇄, L2 프리페치 기각(요청률 병목의 증거), 프로덕션 `u` 검증 (2026-09-06 14:39~14:50 KST, 묶음 프로브 + 프로덕션 부팅)
- ★★38차 §9 — `w` 원패스 재검증(같은 프로덕션 구성)과 `b`(SF bulk 복사) 첫 실측 (2026-09-06 14:54~15:03 KST)
- ★★38차 §10 — 4라운드 교차 묶음: `b`(SF bulk 복사) 상쇄, "요청률 병목" 가설 기각 — v4 `u` 가 이 설계 계열의 끝 (2026-09-06 15:35~15:38 KST)
- ★★38차 §11 — 5라운드: 꼬리 교환 `t`(마지막 웨이브 FC1 을 48 CTA 로) — 수치 PASS, 시간 손해, 기각 (2026-09-06 15:5x~17:00 KST)
- ★★37차 — "컴파일 엄청 오래 걸리네": 부팅 예산 귀속, 5분 공백은 일과성, 컴파일이 아니다 (2026-09-06, 로그 판독 + 계측 추가, 무재부팅)
- ★★★36차 — 다음 레버를 숫자로 고르기: C>1 처리량, 128K 프리필 지도, 하니스 결함 둘 (2026-09-06 07:00~07:15 KST, 프로덕션 부팅 위 무재부팅 측정)
- ★★★35차 부록 — b12x v3 정적 MoE 커널 기본값 검증 부팅과 통합 측정(onepass) (2026-09-06 새벽, 브랜치 ostcode/glm-flash-kernel-improvement-5a65a8)
- ★★★35차 — b12x 정적(디코드) MoE 커널 접수: 구조 진단과 v2 레인, 측정 대기 (2026-09-05, 골격)
- ★★★33차 — W4 팩 정확도 레버 4종: 정확 활성 스케일·행별 shift·GPTQ·저랭크 보정 (2026-09-05 오후, 브랜치 ostcode/glm-flash-kernel-improvement-5a65a8)
- ★★★34차 — 메가커널 모듈·노드 융합: 프로덕션 스텝의 첫 노드 인구조사, 남은 자리 넷, 그리고 드래프터 앞의 호스트 갭 (2026-09-05 오후, 무부팅 + 프로파일 캡처 1회)
- ★★★32차 — 레버 2~7 브래킷 체인: EXP-7 이 +7%, 나머지는 0 이거나 죽었고, srv4 가 rank 3 이다 (2026-09-04 밤)
- ★스텝 커널 지도 최신화 — 무장 부팅의 인구조사, 그리고 도구가 세고 있던 남의 커널 (2026-09-04 밤, 무부팅)
- ★★★메가커널 30차 — mk_gemm 이 deep_gemm 과 같은 14 ms 인 이유: 상주 커널은 MoE 옆에서 진다 (2026-09-05, 09-04 18:42 무장 트레이스 vs 09-01 스톡 트레이스)
- ★★메가커널 29차 — 공유 전문가 GEMM 쌍의 고정비: 배리어 없는 로컬 양자화 커널은 단독으로 손해, MoE 아래에서는 발사 순서가 전부 (2026-09-04 밤, srv2)
- ★★★28차 — 드래프터 W4 는 서빙된 적이 없었다(컴파일 캐시), MK-MLA 서빙 사망의 원인은 스크래치 재할당, 그리고 드래프터 W4 판정 (2026-09-04)
- ★★메가커널 27차 — 서빙 PDL 은 꺼져 있었다, MoE 커널은 우리 레인과 같은 속도, L2-warm 은 W4 GEMM 에 7% (2026-09-04, srv2)
- ★★★26차 — 부팅의 메모리 절벽: fp8-dense 패스가 로드 중에 여러 번 돌았고, W4 팩 빌더가 예약 메모리를 흘렸다 (2026-09-04, 4노드 계측)
- ★★★메가커널 25차 — 24차 온디바이스 마감, 그리고 디코드 스텝의 꼬리: 드래프터가 임계경로에 있다 (2026-09-04, srv2)
- ★★메가커널 24차 — W4 팩이 프로덕션 그룹을 전부 클램프하고 있었다 (2026-09-04)
- ★★메가커널 23차 — srv3 호스트 OOM: nvfp4 첫 부팅이 덴스 패스 안에서 죽었다 (2026-09-03)
- ★★메가커널 22차 — "무장" 이 "서빙" 을 뜻하지 않았다: KDA 레이아웃 게이트의 침묵 (2026-09-03)
- ★★메가커널 21차 — 무장 상태의 디코드 스텝 실측: 대역폭 포화이고 커널 여지는 끝났다 (2026-09-03)
- 메가커널 20차 — two-shot all-reduce 기각: 병목은 바이트가 아니라 고정비 22.7 µs (2026-09-03)
- 메가커널 19차 — all-reduce 의 wait 38.7 µs 는 무엇인가 (2026-09-03, 확인 중)
- ★메가커널 18차 — 가중치 로드 296초는 우리 벤치가 만든 숫자였다 (2026-09-03)
- 메가커널 17차 — KV 캐시를 토큰 수로 고정 (2026-09-03, 운영자 지시)
- 메가커널 16차 — 부팅: cudagraph 메모리 프로파일러 끄기 (2026-09-03, 운영자 승인)
- 메가커널 15차 — 부팅: `init engine` 192초를 쪼갤 스탬프 (2026-09-03)
- ★메가커널 14차 — 부팅: 형제 자체 커널 두 개도 영속 캐시로 (2026-09-03)
- ★메가커널 13차 — 부팅: 자체 커널 컴파일을 영속 캐시로 (2026-09-03)
- 메가커널 12차 — MHC 발행 배치 채택, KDA delta 3-sync 는 기각 (2026-09-03, srv2)
- 메가커널 11차 — 인접 커널 협력: 둘 다 접음 (2026-09-03, 브랜치 feat/adjacent-fusion 에 기록)
- ★메가커널 10차 — KDA 2라운드: 스팬 199 → 176, 벤치 320 → 280 (2026-09-02 밤, srv2)
- ★★★메가커널 결산 — "최대한 더 개선해" 라운드 뒤 (2026-09-02 저녁, main 0995825, srv2 한 번의 전체 벤치)
- ★메가커널 9차 — MHC 재구성: T=8 30.7→27.4, T=32 59.4→42.0 µs (2026-09-02, srv2)
- ★★메가커널 8차 — MK-KDA 단계 귀속과 재작성: 커널 스팬 402 → 199 µs (2026-09-02, srv2)
- ★메가커널 7차 — MK-KDA 의 state-index 계약 확정과 수정 (2026-09-02, srv2)
- 메가커널 6차 — W8 팔 제거, W4 단일 레인 (2026-09-02, 운영자 결정)
- ★★★메가커널 5차 — W4 확장을 prmt 테이블 조회로 (2026-09-02, srv2)
- ★★★메가커널 4차 — W8 팔도 stock 을 넘었다: smem 기준 정렬 + 밀집 W 행 + 벤치 dirty 배수 (2026-09-02, srv2)
- ★★메가커널 3차 — W4 팔이 stock 을 넘었다 (2026-09-02 오전 후반)
- ★★준비 커널 통합 (`glm53_prep_fused`) — 스텝의 12% 가 GPU 유휴, 그 자리에 발사 하나 (2026-09-02, srv4 오프라인)
- ★★★dflash 는 이름 때문에 async scheduling 을 잃고 있다 (`glm53_async_dflash`, 2026-09-02, 코드 읽기)
- ★인덱서 fp32 head-gate: cuBLAS 2블록 47 us → split-K 10 us (`glm53_indexer_gate_splitk`, 2026-09-02, srv4 오프라인)
- ★오프라인 레버 소진 판정 — 9월 1일 트레이스의 스텝 구성 (2026-09-02 밤, 무부팅)
- ★★리뷰 정정 — split-K head gate 는 플릿 형상(N=32)에서 한 번도 돌지 않았다; v2 결정적 커널 (2026-09-03, srv4 오프라인)
- ★★★EXP-8 기각 — dflash async scheduling 은 이미 켜져 있었다 (2026-09-03, 전 팔 부팅)
- ★★MK_SEG_MLA — sm_121a 수제 sparse MLA 디코드, 3판에서 FlashInfer 를 넘었다 (2026-09-03, srv4)
- ★★GLM-5.3 프리필 첫 프로파일 — 32K 에서 시간의 정체와 레버 (2026-09-03, MK-MLA 부팅, rank 0)
- NCCL 채널 스윕 — 프리필 AllReduce 129 → 142 Gb/s (2026-09-03, 엔진 다운, 채택)
- ★★sm_121a 텐서코어 ISA 실측 — fp4 는 fp8 보다 빠르지 않다, 2배는 2:4 희소뿐 (2026-09-03)
- MK_SEG_MLA 전용 명령 검토 — fp8 mma 는 5% 를 위해 순위 30% 를 흔든다, 기각 (2026-09-03)
- ★★★MK_SEG_MLA v4/v5 — 점유율이 병목이었다, 그리고 프리필까지 (2026-09-03)
- 기각: 프리필 밀집 GEMM 의 스케일 입도 — deepgemm 은 이미 천장의 67% 였다 (2026-09-03)
- ★정정: fp4 는 fp8 의 2배다 — "fp4 연산 이득 0" 기록은 틀렸다 (2026-09-03)
- nvfp4 밀집 GEMM — 2.3배 · 메모리 절반 · 오차 3.7배 (2026-09-03)
- 부팅 로더: instanttensor 는 내내 있었다, 프로필이 안 가리켰을 뿐 (2026-09-03)
- bf16 원본 해제 — 랭크당 3.67 GB, KV 토큰 +39.5% (2026-09-03)
- 부팅 시간 901 → 345초 (2026-09-03)
- ★osar 에필로그 융합(AR+residual+norm+quant) — 인접성 실측으로 기각 (2026-09-04, 무부팅)
- ★PDL은 vllm-mhc 쪽에서 이미지가 봉인 — pdl_sync/trigger는 dead code (2026-09-04, 신규 컨테이너 실측)
- ★31차 — 자체 소유 미세 융합 묶음 2 (EXP-20): 오프라인 게이트 결과 (2026-09-04~05, srv4)
- ★★★33차 — 프리필 축 탐구, 첫 표적 완결: 콜드 JIT 세금을 부팅으로 (PREFILL_WARMUP=1, 2026-09-05)
- 메가커널 정확성 리뷰 수정 (2026-09-05, 기준 3b2f180, 미배포)
- ★★★39차 — b12x 정적 MoE 커널 3라운드: 커널 내부 — 타일 우선 가중치 배치(v5, 셀 `t`) (2026-09-06 14:0x~ KST, 무부팅, 프로브 대기)
- C=1 MK-GEMM: FP8 묶음 변환과 8행 전용 타일 (2026-09-07)
- GLM53 W4 키 SHA256 — warm 부팅 234 → 226.5초 (2026-09-07)
- GLM53 C=1 MoE 타일 묶음과 FP4 주소 수정 (2026-09-08, PR #461)
- ★★40차 — 프리필 스텝 고정비, 측정으로 확정: `a = 211 ms/step` (2026-09-08, pstep0908v1)
- ★42차 — 지금 사전샤딩된 DSv4.1 은 GB10 에 **안 들어간다**(TP 를 안 해서다): 랭크 84.70 → TP 면 73.30 (2026-09-11, srv4 로컬, 부팅 없음)
- ★43차 — 계획 변경: 자체 엔진 첫 대상 DSv4.1 → **Qwen3.8-Flash-Next** (2026-09-11, srv4 로컬, 부팅 없음)
- ★44차 — 방향 전환: GLM-5.3 이 공통 부분의 기준 모델 (운영자, 2026-09-11) + `profiles/glm53/plan.py` 현실 대조

## ★45차 — 엔진 네이티브 GLM-5.3: 서빙 4파일 재호스팅 대신 우리 계약으로 다시 씀 (2026-09-11)

44차 끝에서 "vLLM 없이 임포트되나"를 파일별 이정표로 삼았는데, 자동 치환된 4파일이 여전히 요구한 이름이 44개
(`VllmConfig`·`get_forward_context`·`AutoWeightsLoader`·`FusedMoEFactory`·`MambaStateShapeCalculator`…)였다. 그 44개를
`base/`에 채우면 vLLM 의 추상을 엔진 안에 다시 짓는 것이고, 운영자 지시("특정 하드웨어·NVFP4만, 레거시 없이 처음부터")와
D7(전방 컨텍스트 없음, 플랫 메타)·D11(사실 + 만료 노브)·D16(아레나만이 할당)에 정면으로 어긋난다. 그래서 4파일을 지우고
(`git rm`, 3adc8d47 의 산물), GLM-5.3 을 엔진의 계약으로 **다시 썼다**. 서빙 파일은 대수(algebra)의 출처로만 읽었다.

### 무엇이 생겼나

- `profiles/glm53/facts.py` — 상수 전부를 config.json 에서 읽고 **코드가 기대는 가정을 로드 시 단언**(nope·noaux_tc·
  sigmoid·fp32 라우터·NVFP4 대상 = 3~44층 전문가뿐·kpool 4·hc 4…). vLLM `Glm5NextTextConfig` 가 파생하던 값
  (post_mult 2.0, sinkhorn 20, mla_nope)은 판정 이미지에서 읽어 박았다. **런처 사실**: ENABLE_EP=0 → 전문가는 TP
  (중간차원 분할). `plan.py` 의 "EP" 라벨은 종류가 틀렸었다(바이트는 같음) — 고침. MTP(45층, fp8 전문가)는 **서빙되지
  않는다**(DFlash2 드래프터) → 프로필 범위 밖.
- `base/params.py` — `Spec(name, shape, dtype, sources, build)` 와 `bind(specs, views)`: 모델은 가중치를 선언만 하고
  로더가 아레나에서 깎은 뷰에 묶인다. 캐스트·리셰이프 없이, 이름·모양·dtype 불일치는 예외.
- `base/preshard.py` — safetensors 를 **스트리밍**으로 쓰는 `RankWriter`(헤더 선계산, 텐서는 제 오프셋에 pwrite)
  + `write_ranks`(층 단위로 소스를 한 번 읽어 4랭크에 동시에 씀). `RankLoader` 와 safetensors 둘 다로 읽어 확인.
- `profiles/glm53/specs.py` — 랭크가 쥐는 가중치 지도. 서빙 로더가 로드 때 하던 병합을 여기서 한 번(q|k|v|b|f_a|g_a
  → `in_proj`, q_a|kv_a → `qkv_a`, 세 conv → `[3HD,K]`, gate|up, 전문가별 gate|up → `w13`), dtype 승격도(A_log·dt_bias·
  mHC·인덱서 head-gate/k_norm → fp32), 전역 스케일은 **곱셈자로 저장**(`*_mult`, 압축텐서의 제수를 뒤집음). 자가검증:
  랭크당 1,288 텐서 **44.50 GiB = plan 인구조사 44.48**(MTP 전문가 제외, 나머지 0.02 는 승격), 체크포인트의 텍스트 텐서
  전부가 정확히 한 spec 의 소스.
- `profiles/glm53/lanes.py` — 커널 레인 여덟(conv·kda_chunk·mhc_pre·mhc_post·indexer_logits·kpool·mla_sparse·expert)을
  `reference()`(modules 의 판정된 참조) 또는 `served()`(오버레이 커널, 이미지 안 마운트 경로로 임포트, 전부 아니면 예외)
  로 묶는 표. 활성화는 `swiglu_clamped` 하나: 서빙 dense 는 SiluAndMulWithClamp, 서빙 b12x 는 "swigluoai α1 β0 limit
  10" — **같은 식**이다(flashinfer_b12x_moe 1380: "whose math reduces to clamped SiLU").
- `profiles/glm53/net.py` — 조합. 임베드 → 45층 [mHC pre(+in_norm) → KDA | DSA → mHC post/pre(+post_norm) → dense |
  MoE] → contract(mean) → norm → 헤드. 뷰 위의 평범한 함수, 상태는 `Caches` 프로토콜(층별 플랫 latent/pool-key/
  pool-scale, 슬롯별 KDA conv·recurrent·tail 링, 호출자의 블록표가 슬롯을 준다)로 받는다. KDA: in_proj → conv(상태
  carry) → q,k,v / raw gate / beta=sigmoid / 출력 게이트 → 레인 → o_norm(rmsnorm·sigmoid) → o_proj → all_reduce.
  DSA: qkv_a → q_a_norm → q_b; kv_a_norm → **latent fp8 쓰기(스케일 1, 체크포인트에 kv 스케일 없음)** → 인덱서(wq_b·
  wk+LayerNorm·fp32 head-gate·gate score → FWHT-fp8 q, 완성 풀 압축·쓰기, 꼬리 링, 후보 풀 = seq_len//4 미만, top-512
  풀 → 꼬리 포함 토큰 → 슬롯) → W_UK 흡수 MQA(레인) → W_UV → o_proj. MoE: noaux_tc 라우터(fp32 sigmoid, 선택은 +bias,
  가중치는 재정규화×2.5) → 전문가별 레인(W4A4, gate/up 전역 스케일 따로) → 공유 전문가 → all_reduce 한 번.
- `profiles/glm53/preshard.py --world 1 --layers 0-4` → dev 랭크파일 12.17 GiB, **14 s**. `--world 4` 전체는 백그라운드로.
- `profiles/glm53/check.py` — 진짜 가중치로 0~4층(KDA 4, DSA 1, MoE 2)을 한 아레나(12.21 GiB, 25 영역)에서 돌림.
  로드 **2.5 s**(13 런, 5.2 GiB/s), 512 토큰 프리필 8.0 s(참조 레인, KDA 참조가 파이썬 루프). 유한, |h| mean 1.10.

### 판정 설계에서 배운 것 — "두 청크 = 한 프리필"은 MoE 를 지나면 토큰 단위로 성립하지 않는다

처음 판정(최종 hidden 의 rel)이 2.4e-1 로 실패했고, **첫 청크**(캐시를 읽지 않는)도 똑같이 틀렸다. 블록별 추적:
M=512 와 M=256 의 bf16 GEMM 은 누적 순서가 달라 1 ulp(p50 4.6e-3)씩 다르고, 3층을 지나 1e-2 가 되며, 3층 라우터가
그 차이로 **256 토큰 중 30 토큰의 top-8 을 바꾼다**(인덱서의 슬롯 집합은 256/256 동일). 뒤집힌 토큰은 4.6e-1 까지
벌어진다. 어느 엔진이든 배치 크기가 다르면 MoE 뒤의 토큰은 다르다 — 서빙 경로도 같다. 그래서 판정은 **어텐션 블록의
둘째 청크 출력(KDA 상태·latent·풀·꼬리를 읽는 유일한 곳)이 첫 청크의 잡음 바닥을 넘지 않는가**로 바꿨다(블록별
p50·max ≤ 1.5×, 상류 MoE 의 뒤집힘은 물려받는다): L3 dsa 첫 7.4e-3/1.6e-2 vs 둘째 6.7e-3/1.3e-2, L4 kda 3.7e-2/1.4e-1
vs 3.4e-2/1.4e-1 — **캐시 경로는 아무것도 더하지 않는다**. PASS. 이 판정은 조합이 GLM 의 대수인지는 말하지 않는다;
그것은 서빙 층을 옆에 놓는 다음 판정의 몫이다.

### 계기

`hosting.py` 를 엔진 네이티브 지도로 다시 씀(서빙 3파일; mtp 는 서빙 안 됨): **114 심볼 = 89 ours(78%) + 25 drop, real 0**.
"ours" 의 뜻이 바뀌었다 — 이름을 우리 모듈로 바꾼 것이 아니라 **그 자리에 엔진이 무엇을 두었는가**(대개 `net.py` 의
한 줄이거나 `specs.py` 의 분할 규칙, 혹은 "사실이라 필요 없음": rope·yarn·fp8 dense·kv 스케일).

### 남은 것(순서대로)

1. `check.py --lanes served` 를 판정 이미지 안에서 — 같은 조합 위에서 참조 레인 vs 서빙 커널(레인 어댑터 판정).
2. 서빙 `Glm5NextDecoderLayer` 를 옆에 놓는 층 판정(조합 판정, D4).  3. 디코드 경로(recurrent KDA·conv update·
   드래프트 슬롯 K=5 의 희소 MLA·꼬리 링 갱신).  4. 러너 결합(`cache_spec` 으로 아레나에서 캐시를 깎고 블록표로 슬롯을
   주는 `Caches`).  5. b12x 전문가 레인 바인딩, DFlash2 드래프터, 4노드.

### 45차 — 절 색인 (§ 번호로 찾는다)

이 캠페인은 절 92개다(§2~§95, 결번 §35 · §47). 파일 순서는 절 번호 순이 아니다 — 세 절이 뒤에 다시 번호를 받았고(§76~§78 은 옛 §30~§32, 그 사연은 그 절들이 적고 있다) 새 절은 앞에 끼워 넣기도 했다. **찾을 때는 `45차 §N` 으로 검색한다.** 절을 새로 쓰면 이 표에 한 줄 더한다.

- **§2** — 네 기본값을 형태로: TP=4·GB10·NVFP4 기본형·레거시 없음
- **§3** — 한 스텝 함수, 위치 링 상태: 디코드·검증 스텝과 롤백이 프리필과 같은 코드
- **§4** — 서빙 커널은 메인 스레드에서만; 캐시는 프로필의 실물로
- **§5** — 러너가 GLM 을 돈다: 엔진 어댑터 + boot --local
- **§6** — 서브 루프와 문, 플릿 부팅 경로, 런처
- **§7** — DFlash2 드래프터가 엔진 안에서 돈다; 서빙 recurrent KDA 레인
- **§8** — b12x 전문가 레인: 서빙이 로드 때 하던 팩을 사전샤딩이 한 번
- **§9** — 간판(D16)을 실물 캐시에: 다중 영역 티어, 대화가 턴을 넘어 산다
- **§10** — 간판이 돈다: park → 아레나 회수 → resume → 이어서 디코드 = 한 번에 돌린 것과 같다
- **§11** — 정렬: 서빙 mHC 레인의 CUDA_ERROR_INVALID_VALUE 는 스레드가 아니라 12 바이트였다
- **§12** — 정렬 뒤: 서빙 mHC·KDA 레인 통과, 파킹된 대화 HTTP 로 이어짐
- **§13** — PR #535(codex) 평가와 병합; 서빙 레인 전 구간 판정에서 b12x 만 참조와 다르다
- **§14** — 병합본 검증
- **§15** — b12x 수치 판정: 커널은 w13 의 **둘째 반**을 게이트한다 ([up; gate])
- **§16** — 랭크 파일 v4([up; gate]) 판정: 서빙 b12x 레인이 참조와 청크 잡음 바닥 안에서 같다
- **§17** — 커널 패키징: vLLM 이름공간을 떠난 엔진, ST 이미지 안에서 판정
- **§18** — 병합본(main #541 + 이 브랜치) 판정: 표식 랭크 파일로, ST 이미지 안
- **§19** — 첫 45층 4노드 부팅: 돌았으나 다른 세션의 플릿과 충돌해 죽었다; 문(chat)·잠금
- **§20** — 운영자 지시 넷: prefix 재사용, 취소·타임아웃, 문의 나머지, 슈퍼바이저
- **§21** — 커널 패키지 노브 43개의 D11 정리: 채택값은 코드로, 후보 둘은 선언된 노브로, 나머지는 폐기
- **§22** — 쿠다 그래프 경로 검토와 개선 넷: 캡처 안전 통신, 서빙 천장 하나, 풀 분리 단언, 재생 경로 고정 메모리
- **§23** — 프로덕션 전환 시도: 창·45층 4노드 부팅·문 사다리, 그리고 깨진 글의 원인 = KDA `o_norm` epsilon
- **§24** — ST 엔진 관측 메트릭: 세 질문에 답하도록
- **§25** — vLLM 이 못 내는 관측: 어느 그래프가 돌았나, 스텝이 얼마나 걸렸나, 수용은 어떤 모양인가, 무엇이 묶였나
- **§26** — 플릿 예약을 하나로: 큐가 ST 를 받고, ST 가 큐를 본다
- **§27** — 예약을 엔진 것으로: 리스는 엔진, 큐는 벤치, 그리고 유령 큐
- **§28** — 엔진에 결합해야 가능한 것 넷, 그리고 컨트롤러
- **§29** — "완벽하게 스무스하게 됐어?" 로 §28 을 감사: 거친 곳 여섯, 그중 실제 버그 하나
- **§30** — 문의 스트리밍 디토크나이저: 러스트 디코드 스트림과 프로덕션에서 겪고 고친 복구 둘
- **§31** — 구조화 출력: 스텝 하나의 비트마스크, 전용 커널, 그리고 죽은 드래프트
- **§32** — 접두사 캐시 블록 수명: 핀을 버리고 등급 있는 자유 목록으로, 스냅샷만 읽는 복원, 테넌트 솔트
- **§33** — SGLang 대조: 구조화 출력에서 우리가 틀린 것 셋 (사고·크래시·컴파일 자리)
- **§34** — 샘플러 커널: 정렬을 빼고 임계값을 찾는다, 배치 한 벌로
- **§36** — 같은 부분의 SGLang 구현 조사, 그리고 그 조사가 찾아낸 회귀: 한국어 답이 잘려 나가고 있었다
- **§37** — 부팅 뒤 첫 요청이 `add` 에서 죽었다: 지연 생성된 페널티 히스토리에 가드가 둘 빠져 있었다
- **§38** — 한국어 최적화 셋: 답 길이 기본값을 글자로, `text_offset` 을 글로, `bytes` 를 진짜 바이트로
- **§39** — SGLang 전면 대조(남은 영역): 가져올 것은 **하나 반**이었다, 그리고 내가 넷이라고 적은 방식
- **§40** — 한국어, 계속: 줄 위의 바이트와 거절의 숫자와 글자 계량기; 그리고 아니었던 것 둘
- **§41** — 구조화 출력 × 한국어: `[가-힣]` 이 한글의 6분의 1을 조용히 버리고 있었다
- **§42** — 같은 한국어를 두 번 토큰화하고 있었다: 조합형/분해형, 그리고 U+FFFD 로 겹치는 top_logprobs
- **§43** — llama.cpp·Ollama 에서 배워올 것 조사: 하나 있고, 이미 한 것이 넷, 못 하는 것이 둘
- **§44** — 도구 호출 인자를 조각으로 흘린다: 닫힐 때까지 기다리던 3.9 초가 0 이 됐다
- **§45** — 도구 호출에 지연 문법: 선언 안 한 도구도, 없는 인자도 이제 못 쓴다
- **§46** — 사고 예산: 생각이 답을 다 먹어 치우지 못하게
- **§48** — 엔진 스위트가 오랫동안 빨갰다: 셋이 썩어 있었고 여덟이 안 돌고 있었다
- **§49** — 부팅 창의 호스트 메모리: 스테이징 버퍼가 필요보다 862 MiB 넓었고, 좁히니 더 빨랐다
- **§50** — 메모리를 vLLM 과 줄 맞춰 보니: 부팅은 저쪽이 재고 우리는 선언하고, 서빙은 둘 다 눈이 없다
- **§51** — 그 "장비 필요" 숙제는 이미 원장에 있었다, 그리고 서빙이 끝나면 아레나를 내놓는다
- **§52** — "확실히 깔끔하게": 아레나만으론 안 끝난다, 그리고 끝났는지 말하는 숫자
- **§53** — "종료하면 찌꺼기 안 남아?" 물어서 디스크를 봤더니: 루트 99%, 그중 103 GB 가 티어였다
- **§54** — 구현 범위 안에서 vLLM 과 맞대 보니: 앞선 것 여섯, 뒤처진 것 다섯, 나머지는 범위 밖
- **§55** — 내가 §54 에서 틀린 것 하나, 그리고 진짜였던 것 하나: `POST /v1/prefix/reset`
- **§56** — 데네브·웜홀과의 연계: 엔진이 자기가 뭘 할 수 있는지 말한 적이 없었다, 그리고 웜홀은 눈을 가리고 있었다
- **§57** — 웜홀이 우리 눈을 가리던 키를 껐다, 그리고 `max_model_len` 이 비어 있는 걸 실물에서 봤다
- **§58** — 데네브까지의 사슬을 끝까지 따라가 보니, 빠진 고리는 우리 한 칸뿐이었다
- **§59** — `medium` 은 표준인데 우리 문이 400 을 냈다, 그리고 템플릿 kwargs 는 검사를 통째로 건너뛰고 있었다
- **§60** — 양방향으로 보니 한쪽이 끊겨 있었다: 통제면이 우리 엔진을 모른다
- **§61** — 양쪽 다 우리 것이니 알 수 있는 것: 에이전트는 매 턴 대화 전체를 다시 토크나이즈시키고 있었다
- **§62** — 파킹된 대화 280개가 호스트 램 1.04 GiB 를 잡고 있었다, 스캔이 물어보는 건 숫자 셋인데
- **§63** — 남은 셋을 코드에 대조했더니 둘은 이미 있고 꺼져 있었다, 그리고 그 이유를 GPU 없이 지웠다
- **§64** — `__restrict__` 손실의 값을 재 보니 0 이었다
- **§65** — 프로덕션이 스스로 말했다: 드래프트 폭 5 가 걸리는 곳이다
- **§66** — 컴파일된 문법 캐시에 천장이 없었다; 이미지 안에서 재 보니 스키마 하나가 173 KiB
- **§67** — PDL 의 이득을 실제로 쟀다: 공짜지만 프로덕션 청크에선 거의 0
- **§68** — 디코드 체인의 eager 가운데를 쟀다: 융합 문제가 아니라 **디스패치** 문제였다
- **§69** — 블록 검증을 런치 하나로 접었다: 2,451 → 459 µs, 토큰은 한 개도 안 바뀌고
- **§70** — 두 시점을 하나로 보고 있었다: 런 안에서는 D16 이 켜지고, 런 사이에서는 접두사 캐시가 이미 최대다
- **§71** — 샘플러는 한 행에 505 µs, 배치가 1이든 48이든 같다: 점유가 벽이다
- **§72** — 검증 커널 형상 조정(+13%), 그리고 §69 의 5.8배가 무엇의 함수였는지
- **§73** — 데네브 레포 쪽에서 우리를 보니: 문이 모르는 모델 이름을 그대로 돌려주고 있었다
- **§74** — 디코드 가운데를 CUDA 그래프로 캡처했다. 되고, 값은 1.0~1.1배다
- **§75** — 스냅샷 미터를 트래픽 실린 플릿에서 처음 읽었다: 96 은 과하지 않고 **포화**다
- **§76** — 오늘 것 재검토: 인계가 플릿에서 아예 안 돌고 있었다
- **§77** — 오늘 것 말고: 플릿 큐 자체를 조사
- **§78** — §31 의 넷을 해결: 플릿 스위트가 393/393 이 됐다
- **§79** — "최근 원패스가 최악" 의 답: 같은 커밋인데 다른 엔진을 쟀다
- **§80** — 겹침은 된다: 4배의 일이 1.21배의 시간에. 문제는 실리콘이 아니라 **독립적인 일이 없다는 것**
- **§81** — 디코드 스텝의 구성을 프로덕션이 답했다: 드래프터가 36.4% 다
- **§82** — observe 의 16 ms 는 투영이 아니었다: 로터리가 매번 자기 상수표를 다시 만들고 있었다
- **§83** — 드래프터의 나머지 절반: 탭 컨볼루션이 스텝당 1,500 디스패치였다
- **§84** — 드래프터를 **그래프 재생으로** 재니 답이 달라졌다: 14.97 → 6.12 ms
- **§85** — 대화 이어가기가 왜 한 번도 안 탔나: 추론을 켜는 쪽과 되돌려주는 쪽이 다른 계층이다
- **§86** — 프로브가 틀렸었다: 한 스텝에 블록은 **한 번** 돈다, 그리고 드래프터의 최대 항목은 어휘 헤드다
- **§87** — 선택기를 접었다, 그리고 **박아둔 계약 하나를 깰 뻔했다**
- **§88** — W4 GEMM 은 바닥의 **1.25배**다: 드래프터에서 접을 것이 끝났다
- **§89** — 다음 파트로 넘어가니 **자가검사 셋이 썩어 있었다**, 그리고 KDA 링은 이미 바닥이다
- **§90** — 창을 예약했고, 그동안 **창에서 쓸 자를 만들었다**
- **§91** — 대기예약을 걸었다, 그리고 **큐가 하지 않은 부탁을 했다고 적고 있었다**
- **§92** — 테스트 부팅 모드: 플릿을 안 잡고 **프로덕션 레인**을 돌린다
- **§93** — D17 을 처음 집행했다: 오늘 성능 PR 넷에서 잰 이득이 없고, 디코드는 낮게 나왔다
- **§94** — 테스트 부팅은 **경로가 프로덕션과 같아야** 한다, 그리고 본체는 계측창이다
- **§95** — 큐가 허용한 검사 여섯 중 셋은 **큐가 시작은 하되 돌릴 수 없었다**

### 45차 §2 — 네 기본값을 형태로: TP=4·GB10·NVFP4 기본형·레거시 없음 (운영자, 2026-09-11 오후)

운영자: "TP=4, dgx spark, nvfp4, 레거시 없음의 4가지 요소를 디폴트로 해서 아주 잘 써봐" — 그리고 정정: NVFP4 는 **유일형이
아니라 기본형**(가장 잘 지원하는 형태). 그래서 dense 투영의 강제 W4A16 양자화는 하지 않는다; 체크포인트의 형태(전문가
W4A4 캘리브레이션 있음, 나머지 bf16)를 그대로 쥐고 NVFP4 를 1급으로 지원한다(packed 바이트 그대로 상주, packed 위에서 TP,
전역 스케일은 곱셈자, 서빙 커널이 레인).

- **TP=4 가 코드의 형상**: `facts.TP = 4`, 랭크-로컬 크기가 사실(`heads_local 16, kda_heads_local 16, moe_inter_local 512,
  dense_inter_local 3072, vocab_local 38,720`). `specs`/`preshard`/`net` 에서 `W` 인자와 `// W` 제거; `Glm53Net` 은 world≠4 를
  거부. `preshard.py` 도 `--world` 없음(출력 기본 `facts.RANKS`).
- **GB10 단언**: `facts.check_box()` — 장치 1개, SM121, `mem_get_info` 총량 == `/proc/meminfo`(통합 메모리)를 부팅·검증 때
  단언. 오늘 박스: "SM121, unified 122 GiB".
- **한 노드에서 TP=4 를 진짜로**: `base/comm.LocalTP(4)` — 네 랭크를 네 스레드로, all_reduce/all_gather 는 배리어에서
  만나 fp32 로 랭크 순서대로 합산(네 사본이 **동일**, NCCL 이 보장하는 것과 같은 성질). 죽은 랭크는 원인 랭크로 표면화.
  world-1 항등은 base 자가검증에만 남는다(프로필 경로에는 없음).
- **검증 입력은 실제 랭크 파일**: 사전샤딩 전체 완료 — **176.09 GiB 읽고 4 × 44.50 GiB 씀, 343 s**(0.51 GiB/s, 층당 6~9 s).
  `check.py` 는 dev 슬라이스 대신 `RANKS/rank{r}of4` 에서 0~4층 키만 골라 랭크별 아레나(3.10 GiB)에 적재: **로드 2.9 s
  (4 런, 1.1 GiB/s — 4 스레드가 한 NVMe 를 나눔)**, 512 토큰 프리필 12.9 s(참조 레인, 스레드 4개가 GIL 을 나눔).
- 판정 둘: **네 랭크의 hidden·logits 바이트 동일(max |diff| 0.0)** — 행/열 분할 규칙과 리덕션 배치가 맞다는 뜻(world-1
  검증은 이걸 절대 못 본다); 캐시 경로는 첫 청크 잡음 바닥 안(L3 dsa 둘째 9.4e-3/1.8e-2 vs 첫 1.1e-2/2.0e-2). **PASS**.
- 아까 한 번 본 device-side assert 는 재현되지 않았다(같은 코드로 4회 통과). 원인을 못 봤으니 "고쳤다"가 아니라 "재발
  감시"로 적는다.

### 45차 §3 — 한 스텝 함수, 위치 링 상태: 디코드·검증 스텝과 롤백이 프리필과 같은 코드 (2026-09-11)

`net.py` 를 **스텝 = 세그먼트 목록**(seq, slot, ctx, length)으로 다시 썼다. 프리필 청크는 긴 세그먼트 하나, 디코드는
짧은(1+K) 세그먼트 n개 — 조합은 둘을 구분하지 않고 레인만 구분한다(청크 KDA vs 토큰별 상태를 남기는 recurrent).
시퀀스 상태는 전부 **위치로 주소**를 매긴다: conv 입력 링 `[C, K-1+spec_k]` 와 recurrent 상태 링 `[spec_k+1, H, D, D]`
는 `pos % width`, latent/풀/꼬리는 pos. 그래서 거부된 드래프트는 되돌리지 않는다 — 다음 스텝이 같은 위치에 덮어쓰고,
시작 상태는 그냥 "ctx-1 뒤의 상태"다. 서빙 `kda_state_shape(num_spec)` 의 K+1 상태 열과 같은 것이며 `plan.state_bytes`
도 여기에 맞췄다(**시퀀스당 207 MiB**, 34.8 은 K=0 수치였다). 인덱서는 ctx 가 풀 경계가 아니어도 꼬리 링의 앞 토큰과
새 토큰을 합쳐 풀을 완성한다.

`check.py` 판정 넷(TP=4 스레드, 실랭크 파일, 참조 레인): 네 랭크 동일(0.0) · 두 청크 · **verify**(T-6 프리필 + 6토큰 검증
스텝) · **rollback**(T-12 프리필 → 진짜 2 + 가비지 4 검증 → "2 수락" → 진짜 6 검증). 어텐션 블록별 첫 청크 잡음 바닥
대비: verify L3 dsa 7.5e-3/1.1e-2, L4 kda 2.3e-2/3.7e-2; rollback L3 dsa 8.0e-3/1.0e-2, L4 kda 3.9e-2/4.9e-2 — 가비지가
어느 캐시(conv 링·recurrent 링·latent·풀·꼬리)에도 새지 않는다. **PASS**.

서빙 레인 첫 실행은 triton autotuner 의 스레드 비안전(`self.nargs` 공유)으로 랭크 1 이 죽었다 — 서빙 레인 어댑터를 잠금
하나로 직렬화(GPU 가 어차피 직렬). 재실행은 백그라운드.

이름: 운영자가 자체 엔진을 **ST 엔진**으로 명명(헌장 머리말, `engine/README.md`).

### ★인시던트 — q38 rank 3 이 남의 방에 96 GiB 로 들어가 srv4 를 세웠다: 09-04 웻지의 세 번째 (2026-09-11 15:40~16:16 KST)

운영자 "srv4 의 세션들이 왜 원격 해체됐지". 증상은 ssh 로 나타났다 — **핑은 깨끗한데 ssh 만
안 됐다**(tailscale 10~21 ms, 내부 패브릭 0.2 ms, 손실 0%).

### 진단 — 핑과 ssh 를 가르는 것은 유저스페이스다

ICMP 는 커널이 softirq 에서 답한다. sshd 로그인은 fork → `~/.ssh/authorized_keys` 디스크 읽기 →
PAM/NSS 다. 그래서 **어느 단계에서 멈추는지가 곧 진단이다**:

| 단계 | 관측 | 뜻 |
|---|---|---|
| TCP 22 connect | OK | 커널·리스너 살아 있음 |
| 배너(ConnectTimeout 8) | `timed out during banner exchange` | 자식이 fork 뒤 스톨 |
| KEX(ConnectTimeout 60) | 통과 | CPU·상주 메모리는 돈다 |
| publickey 응답 | 무한 정지 | 홈 디렉터리 읽기/PAM 경로가 회수에 갇힘 |

랩탑(tailscale)과 srv1(0.2 ms 패브릭)에서 **동일하게** 막혔다 — 경로가 아니라 박스다. 네 랭크
전부 torch master :29501 에 붙어 있었으므로(10.10.10.4 포함) 프로세스는 살아 있었다. 즉
**이미 도는 것은 돌고, 새로 시작하는 것만 안 된다** = 메모리 압박의 교과서적 형태.

### 우회로는 없었다

srv4 안에서 명령을 실행시킬 경로를 전부 찔렀다: 8080 은 TCP 만 열리고 HTTP 응답 000(그 서비스도
스톨), docker API 2375/2376·node_exporter 9100·dcgm 9400 닫힘, tailscale ssh 미활성(피어 JSON 에
SSH 호스트키 없음), srv1 의 prometheus 는 localhost 타깃만 스크레이프. **유저스페이스가 안 도는
박스에는 다른 문도 없다** — 밖에서 메모리를 빼주는 것이 유일한 레버였다.

### 원인 — 박스 하나, 모델 서버 둘

srv4 는 GPU 워커 전용이 아니다. `nemotron-vllm`, `solarflow-{engine,web-ssr,backend}`,
`sfpoc-*`, `vaultwarden`, `deneb-{mailserver,mailarchive}`, `cloudflared-deneb` 가 상주한다.
15:38 경 `nemotron-vllm` 이 떠 있는 상태에서 15:40 에 q38 TEP4 rank 3 이 들어왔다. GB10 은
통합메모리라 GPU 몫이 곧 호스트 몫이다(42차: box 121.63 GiB, `mem_get_info` total == MemTotal).

| | 16:15 (직전) | 16:16 (q38 워커 제거 직후) |
|---|---:|---:|
| MemAvailable | 9 GiB | **104 GiB** |
| PSI memory `full avg10` | **95.41** | 0.10 |
| load average | **217.33** | 69 (하강) |
| ssh | 배너 정지 | 즉답 |

rank 3 하나가 **96 GiB**(used 111 → 12)를 쥐고 있었다. 그래서 ① sshd 가 로그인을 못 끝내고
② rank 3 이 진도를 못 빼 head 가 50분간 `No available shared memory broadcast block` 만 찍었다.

### 산수 구멍 — 고정 KV 는 창(window) 밖에 있다

부팅을 띄운 사본을 찾았다: head 의 `~/q38-stack`(브랜치 `ostcode/upgrade-glm-dsv4-flash-54ec28`,
HEAD 043ca6d), **mtime 15:40:34 / atime 15:40:37** — 쓰여지고 3 초 뒤에 실행됐고 컨테이너 생성이
15:40:43 이다. main 이 아니다(`~/stkernel` 은 #511 에 머물러 있고 이 런처 파일 자체가 없다).

그 브랜치의 오늘 커밋이 원인을 말한다:

    043ca6d launcher(qwen38): KV_CACHE_MEMORY knob -- fixed KV size instead of the utilization remainder

`tp4-mem.sh gmu` 는 **TOTAL 의 분수**를 고른다(오늘 0.60). 고정 KV 는 그 분수 **밖에** 얹힌다 —
vLLM 이 부팅 로그에서 그대로 말한다: "reserved 16.0 GiB memory for KV Cache as specified by
`kv_cache_memory_bytes` config and skipped memory profiling. **This does not respect the
gpu_memory_utilization config.**" 0.60 × 121.63 ≈ 73 + KV 16 + 오버헤드 ≈ **96 GiB** = 실측 해제량.
창은 맞았고, 창이 전부가 아니었다. 그래서 새 가드는 창이 아니라 **실효 상주**를 비교한다
(`_gmu_eff = GPU_MEM + KV_CACHE_MEMORY/TOTAL`; `KV_CACHE_MEMORY` 가 걸려 있으면 초과 시 ABORT).

### 세션이 "원격 해체"된 경위 — 두 겹

- **설계된 쪽**: 런처가 head 에서 각 워커로 ssh 해 컨테이너를 갈아끼운다
  (`start-qwen38-nvfp4-tep4.sh` 의 `docker rm -f q38-worker`), 유휴 컨트롤러는 300 초 정적이면
  스스로 복구 부팅을 한다(`fleet.sh` 머리말, `fleet_idle.py` `IDLE_SECONDS=300`). 오늘 08:15 의
  복구 시도는 `ABORT: ... fleet/holder 없음` 으로 실패했고 glm53 서빙 로그는 08:42 에 끊겼다.
- **사고 쪽**: 박스가 스톨하자 세션이 응답을 멈췄고, `~/.ssh/config` 의
  `ServerAliveInterval 30 × ServerAliveCountMax 6` = **180 초**에 클라이언트가 먼저 끊는다.
  이 리포의 원격 작업은 tmux 없이 ssh 페이로드로 도니까 그 순간 SIGHUP 이 프로세스 그룹째 간다.

### 조치

운영자 승인("깨버려")으로 멈춘 q38 부팅을 head 에서 해체 — `q38`(srv2), `q38-worker`(srv1·srv3·
srv4). srv4 워커를 지우는 순간 96 GiB 가 풀리고 ssh 가 즉시 돌아왔다. 다른 테넌트는 건드리지
않았다.

### 고친 것 — 이름이 아니라 자리로 막는다

**q38 레인만 `memfree-preflight.sh` 를 안 돌았다.** glm53 은 `GMU_SAFE` 로 핀된 값을 ABORT 하고
hy4 도 같은 스크립트를 부르는데, q38 은 `GPU_MEM=auto` → `tp4-mem.sh gmu` 만 믿었다. 그런데
gmu 는 **TOTAL 의 분수**지 "지금 그만큼 비어 있나" 가 아니다(오늘 0.60 을 골랐고, 그래도 96 GiB 였다).
그래서 같은 가드를 이 레인에 붙였다(모델·이미지 확인 뒤 → 자기 레인의 이전 컨테이너만 회수 →
`memfree-preflight.sh 10` → 핀된 값이면 ABORT, auto 면 실측 채택).

**이름 기반 denylist 는 확장하지 않았다**(운영자 "nemotron vllm 은 막지마"). `ct_refuse_foreign_stacks`
의 정규식을 넓히면 합법적인 동거 테넌트를 이름으로 쫓아내게 된다. 동거는 잘못이 아니다 —
자리를 묻지 않고 부팅한 것이 잘못이다. 잴 수 있는 것으로만 막는다.

### 남은 것

- 가드는 main 의 런처에 들어갔고 부팅은 `~/q38-stack`(`ostcode/upgrade-glm-dsv4-flash-54ec28`)에서
  나온다 — **그 브랜치에 닿기 전까지 효력이 없다.** 그리고 부팅이 어느 체크아웃에서 나왔는지
  남는 기록이 없어 오늘도 파일 atime 으로 역추적했다. `[boot-stamp]` 에 소스 경로 + 리비전을
  넣는 것이 다음 항목.
- `nemotron-vllm` 이 16:15:27 에 **새로 떴다**(`RestartPolicy=no`, `RestartCount=0` — 재시작이
  아니라 새 `docker run`). 이 상태에서 q38 을 다시 띄우면 같은 충돌이 재발한다. 새 가드가
  그걸 ABORT 로 잡아야 한다 — 첫 재부팅이 이 패치의 판정이다.
### 45차 §4 — 서빙 커널은 메인 스레드에서만; 캐시는 프로필의 실물로 (2026-09-11)

서빙 레인 판정 두 번째 실행은 랭크 0 의 mHC pre 레인(우리 tilelang 포크 → DeepGEMM `tf32_hc_prenorm_gemm`)이
`CUDA_ERROR_INVALID_VALUE`. `probes/mhc_lane_isolate.py`(이미지 안, 한 번에 여섯 형태): 메인 스레드 — norm_weight 유무·
아레나 뷰(256 B/3840 B 오프셋)·T=6/64/256/512/2304 **전부 OK**; **워커 스레드에서만 FAIL**. 원인은 우리 조합이 아니라
DeepGEMM JIT 런타임의 스레드 가정이다. 조치: `base/comm.LocalTP.on_main` — 랭크 스레드가 잡을 큐에 넣고 `run()` 을 부른
메인 스레드가 그 잡을 실행(플릿에선 프로세스당 한 랭크라 직접 호출). `lanes.served()` 의 어댑터 아홉이 이 길로 간다
(triton autotuner 의 스레드 비안전도 같이 해결; 잠금은 뺐다).

`profiles/glm53/caches.py` — 프로필의 실물 캐시: 층별 플랫 영역(latent·풀 키·풀 스케일; 서빙 커널이 먹는 전역 슬롯 레이아웃)
+ 슬롯별 링(conv·recurrent·꼬리), `base/kv` 의 BlockPool/SlotPool 위에서 **블록표 주소 변환은 여기 한 곳**(토큰 `row[p//2304]*2304
+ p%2304`, 풀 `row[j//576]*576 + j%576`). 블록 = 11 층 × 1,255,680 B = 13.17 MiB, 슬롯 = 207.2 MiB(plan 과 바이트 일치 —
plan 이 빠뜨렸던 꼬리 링 11×2 KiB 를 채움). `check.py` 는 이제 ChainCaches(항등 표) 대신 이것을 쓴다: 블록은 free 스택에서
받고(ids 2,1,…), 슬롯 1, 랭크별 아레나 3.14 GiB. 판정 그대로 **PASS**(네 랭크 동일, verify L3 dsa 7.5e-3/1.1e-2, rollback
8.0e-3/1.0e-2).

### 45차 §5 — 러너가 GLM 을 돈다: 엔진 어댑터 + boot --local (2026-09-11)

운영자: "버그 해결보다 우선 전체 구현을 틀을 잘 잡고 빠르게" — 서빙 레인의 DeepGEMM 스레드 문제는 **열린 항목**으로 두고
(플릿은 프로세스당 한 랭크라 애초에 없음) 틀을 세운다.

- `base/runner.py` 프로토콜 정정: `open(seq, slot)`·`prefill(..., slot)`, 디코드 예약은 `model.horizon(seq)`(= ctx+1+K)까지만 —
  거부된 드래프트 위치는 다음 스텝이 덮어쓰므로 **다시 예약하지 않는다**(전엔 매 스텝 1+K 를 무조건 예약해 블록이 샜을 것).
- `profiles/glm53/engine.py` — 러너의 네 메서드 뒤의 GLM: 토큰 버퍼(프롬프트+생성), 스텝 구성, `base/sampler`(시드 → 리플레이),
  **위치 기반 검증**([마지막 토큰]+K 드래프트를 ctx..ctx+K 에 먹이고 모든 위치에서 샘플, 일치하는 동안 수락, accepted+1 개 추가).
  `Drafter` 인터페이스(`propose(seq, tokens) -> K ids`): `NullDrafter`(K=0) 지금, DFlash2 는 같은 두 호출로 꽂는다.
- `profiles/glm53/boot.py` — 사실 → comm → 아레나(가중치 + KV_GIB 8.73 을 블록/슬롯으로) → 로더 → 캐시 → 레인 → 러너, 한 함수.
  `--local`: 네 랭크 스레드·참조 레인·층 부분집합으로 **러너 경로 전체**. 0~4층, 300토큰 프롬프트 2개, max_new 8: **16 스텝
  (프리필 2, 디코드 14 — D10 순차: 0번이 끝까지 디코드한 뒤 1번 프리필), 27.3 s, 링 16 레코드, 블록 753/슬롯 4 반환, 네 랭크
  토큰 동일**. 디코드 스텝 0.6 s(참조 KDA 파이썬 루프 34→3 층분 + 스레드 4개 GIL). 토큰은 언어가 아니다(5층).
- 간헐: 같은 명령이 한 번 "rank 1 failed"(추적 못 잡음) 뒤 두 번 PASS. 앞서 device-side assert 한 번과 같은 부류 —
  LocalTP 4 스레드 아래의 비결정 실패로 기록, 추적은 보류(운영자 지시).

### 45차 §6 — 서브 루프와 문, 플릿 부팅 경로, 런처 (2026-09-11)

- `base/serve.py` — `Server(engine, runner, comm, port)`: 랭크 0 만 문을 연다(HTTP 스레드, `POST /v1/completions` {prompt|ids,
  max_tokens, temperature}, `GET /` 상태). 매 반복: 랭크 0 이 도착 큐를 비워 **한 번의 `comm.broadcast_object`** 로 모두에게
  → submit → 스케줄러 한 스텝. 토큰은 랭크끼리 주고받지 않는다 — 로짓이 all-gather 로 동일하고 샘플러가 시드 고정이라
  **같은 토큰을 각자 계산**. 시퀀스가 러너에서 빠지면 완료 → 랭크 0 이 기다리던 HTTP 호출에 답.
- `base/comm` 에 `broadcast_object`(NCCL: `broadcast_object_list`, LocalTP: 슬롯 0 + 배리어 둘).
- `boot.py`: `--local --serve` 는 네 스레드 위에서 **서브 루프 자체**를 돌린다(클라이언트 스레드가 HTTP 로 프롬프트를 보냄):
  0~4층, 300토큰 2개, max_new 6 → **12 스텝, 2건 응답, 38.7 s, PASS**(문을 루프 전에 열지 않아 첫 시도는 연결 거부가 데몬
  스레드에 삼켜져 300 s 정지 — 이제 클라이언트 오류는 표면화). 플릿 모드(`boot.py` 인자 없음): `Comm.init`(NCCL) → `lanes.served()`
  (전부 아니면 죽음) → 45층 전부 → `DeathDump` → 토크나이저(`tokenizers`, 체크포인트 tokenizer.json, eos = generation_config)
  → `Server.loop`. KV 8.73 GiB(40차 실측)를 블록/슬롯으로.
- `launchers/start-st-glm53.sh` — 노드당 컨테이너 하나(판정 이미지, 엔진 트리 rsync → /repo, 랭크 파일, 오버레이 8종을 manifest
  경로에, /cache, 프로덕션 런처의 NCCL/RoCE env 그대로), `stop`/`logs r`. **glm53*/q38*/vllm* 컨테이너가 떠 있으면 거부**(플릿 무단
  점유 금지). `launchers/fanout-st-ranks.sh` — 랭크 r 파일을 노드 r 로(srv3 은 srv2 경유). srv2·srv3 fan-out 진행 중; **srv1 은
  디스크 95%(52 GB 남음)** — 44.5 GiB 를 넣으면 98% 라 운영자 판단 뒤에.
- 열린 항목: 서빙 레인 판정(한 상자, DeepGEMM 워커 스레드) — 플릿에선 문제 아님. DFlash2 드래프터 포팅이 다음(인터페이스는
  `Drafter.propose` 하나; 대상 층 5·14·24·33·42 의 hidden 을 net 이 내줘야 함).

### 45차 §7 — DFlash2 드래프터가 엔진 안에서 돈다; 서빙 recurrent KDA 레인 (2026-09-11 저녁)

- `profiles/glm53/drafter.py` — DFlash2 를 같은 규율로: 사실 단언(qwen3 5층·32/8 헤드·창 2048·비인과 블록·mask 154856·selector
  16/256·대상 층 5,14,24,33,42 → 출력은 4,13,23,32,41 층 뒤), 81 텐서를 체크포인트 이름 그대로 뷰로 바인딩(2.18 GiB **복제**,
  DRAFT_TP=1 그대로 — 임베드·헤드는 대상 모델 것을 빌림), 슬롯별 컨텍스트 K/V 링 `[5, 2, 2048, 8, 128]`(40 MiB),
  `observe(ring, positions, aux)`(fc → hidden_norm → 층별 k/v → k_norm → rope → 링)와 `propose(anchor, position, ring)`
  (앵커+마스크 K 블록 → 그룹 conv(2탭·16그룹, 블록 내 위치로 탭 마스크) 감싼 5층 → 마스크 위치의 로짓 top-16 → 코드북
  에지 점수 → 앵커부터 탐욕 walk). 서빙 `_selector_walk_kernel` 의 온도 0 가지와 같다; Gumbel 가지는 안 옮김.
- `net.forward(..., aux_layers)` 가 해당 층 뒤의 contract(hc_post) 를 이어 붙여 돌려준다(서빙의 aux_hidden_states 규칙).
  `adapter`: 프리필 뒤 프롬프트 전 토큰, 디코드 뒤 **수락된 위치까지** observe; 다음 스텝은 마지막 토큰을 앵커로 propose.
  토크나이저가 디코드 못 하는 행은 대상·드래프터 로짓 모두 마스크(서빙의 vocab-mask).
- `boot.py --local --drafter`(0~4층, aux 층은 체인 끝으로 잘라 배관만): **16 스텝, K=5, 0/70 수락**(5층짜리 체인이니 당연),
  네 랭크 토큰 동일, PASS. 플릿 모드는 드래프터 기본 on.
- 레인: beta 는 **raw 로짓**으로 다닌다(참조는 sigmoid, 서빙 chunk 는 fp32 sigmoid, 서빙 recurrent 는 커널 안). 서빙
  `fused_recurrent_kda` 를 dense·non-inplace 형(`final_state [T,H,D,D]` = 토큰마다의 상태)으로 `kda_recurrent` 에 바인딩.
  두 서빙 커널의 상태 레이아웃([k,v] vs [v,k])은 **부팅 때 작은 입력으로 참조와 대조해 측정**(`_state_layout`, 무장≠서빙).
  `served(reference_for=("expert",))` — 남은 참조 레인은 b12x 전문가뿐이라고 표 이름이 말한다.
- 이름 충돌 함정: `profiles/glm53/engine.py` 가 스크립트 실행 시 `engine` 패키지를 가렸다 → `adapter.py`.
- 16:14 이 박스에서 다른 세션의 q38 vLLM 이 nvidia 드라이버 rwsem 에서 D 상태로 죽고(journal 정지·재시작, 서비스 KILL),
  내 스모크와 자가검증이 137 로 죽었다. 재실행 정상. 기록만.

### 45차 §8 — b12x 전문가 레인: 서빙이 로드 때 하던 팩을 사전샤딩이 한 번 (2026-09-11 저녁)

서빙 `flashinfer_b12x_moe.process_weights_after_loading` 이 하는 일 셋: (1) 블록 스케일에 전문가별 전역 스케일을 접는다
(`block *= scale_2 = 1/w_gs`, 이후 α = 1), (2) fc2 입력 스케일을 1 로 강제하고 fc1 입력 전역 스케일은 아예 안 넘긴다 —
**SM12x 커널은 활성화를 블록마다 동적으로 양자화**하므로 체크포인트의 `input_global_scale` 은 읽지 않는다, (3) 스케일을
`flashinfer_convert_sf_to_mma_layout`(= `flashinfer.fp4_quantization.block_scale_interleave`, 128×4 타일 인터리브)로 바꾼다 —
랭크당 42층 × 288 전문가 = **4.75 GiB 의 스케일을 부팅마다 재배치**. ST 엔진은 이 셋을 `specs.py` 의 build 로 옮겨 사전샤딩이
한 번 한다(D1: 로더는 재패킹하지 않는다): `moe.w13_sf [E, 2I·H/16]`, `moe.w2_sf [E, H·I/16]` = 접고 인터리브한 e4m3;
`*_mult`·`a*_mult` 는 사라짐(spec 1,288 → 1,120 텐서, 바이트 동일 44.50 GiB).

인터리브 식은 `modules/nvfp4_sf.py` 에 torch 로: `(m, s) → ((m//128)·(Sp//4) + s//4)·512 + (m%32)·16 + ((m%128)//32)·4 + s%4`.
`probes/sf_swizzle_check.py`(이미지 안): 네 형상([1024,4096]·[4096,512]·[256,2048]·[200,1024], E=1~3)에서 flashinfer 와
**바이트 동일**, 역변환 정확. (`flashinfer.fused_moe.utils.swizzle_sf` 가 부르는 `torch.ops.trtllm.block_scale_interleave` 는
이 이미지에 없다 — 서빙 층이 진짜 쓰는 것은 `fp4_quantization.block_scale_interleave` 다.)

레인: `expert`(전문가 하나) 대신 **`moe`(이 랭크의 라우팅된 전문가 전체)** 하나 — 참조는 언스위즐 + 전문가 루프 + **동적
활성화 양자화**(전역 1, 서빙과 같은 형), 서빙은 `b12x_fused_moe(x, w13, w13_sf, w2, w2_sf, sel, w, E, top_k, α=1, α2=1,
fc2_input_scale=1, input_global_scale=None, "swigluoai_uninterleave" α1 β0 limit 10, nvfp4)`. `served()` 에 참조 레인이 더 이상
없다 — 전부 바인딩되거나 죽는다(D3). 랭크 파일 재절단(백그라운드) 뒤 fan-out 을 다시 한다.

### 45차 §9 — 간판(D16)을 실물 캐시에: 다중 영역 티어, 대화가 턴을 넘어 산다 (2026-09-11 밤)

- `base/kv_tier.NvmeTier.demote/promote` 가 **영역 목록** `[(storage, block_bytes), …]` 을 받는다: 시퀀스 파일 하나에 영역별 세그먼트
  (세그먼트 안에서 블록 연속), 매니페스트에 영역 크기 기록·promote 때 대조. GLM 블록은 11 층 × (latent 1,179,648 + 풀 키 73,728
  + 풀 스케일) 의 33 영역. 풀 스케일 블록(576 × 4 B = 2,304 B)은 O_DIRECT 가 안 되므로 **섹터(4,096 B)로 패딩**(`caches.ps_block`,
  블록 13.17 → 13.19 MiB, 0.1%); 스케일은 `caches.read/write_scales(layer, slots)` 로만 만진다(레이아웃은 캐시 안에). 자가검증:
  1 GiB 단일 영역 demote 1.05 GiB/s·promote 4.0 GiB/s, 3 영역(288·18·1 섹터) 블록 64개 왕복 바이트 동일, 영역 불일치 거부.
- `base/tiered_kv.TieredKV(pool, tier, regions=caches.regions(), on_resume=caches.sync_row)`.
- `base/runner`: **대화가 턴을 넘어 산다** — `keep_idle` 이면 끝난 시퀀스는 블록·슬롯을 쥔 채 `idle` 로 남고, `park`(idle 만)·
  `resume`·`wake`(다음 토큰은 어댑터에 대기 중이라 바로 디코드)·`extend`(새 턴: 쥔 토큰 위에 프롬프트 추가, computed 부터 프리필)·
  `evict`(진짜 끝: 블록·슬롯·버퍼 반환). 자가검증에 다섯 동사 전부.
- `boot.py --local --park`: 대화 0 을 파킹(아레나 블록 회수) → 재개 → wake → 4 토큰 더 → **같은 프롬프트를 max_new+4 로 한 번에
  돌린 꼬리와 비교**(캐시가 바이트 동일해야 같다). 결과는 §10.

### 45차 §10 — 간판이 돈다: park → 아레나 회수 → resume → 이어서 디코드 = 한 번에 돌린 것과 같다 (2026-09-11 밤)

`boot.py --local --drafter --park`(0~4층, TP=4 스레드, 재절단 랭크 파일): 대화 0 이 첫 턴을 마치고 idle 로 블록을 쥔 채 남음 →
`park` **12 ms**, 1.2 MiB(5층 체인의 DSA 한 층분 블록 하나), 아레나 free 583 → 584 → `resume` → `wake` → 4 토큰 더
`[55908, 20172, 60100, 30502]` == 같은 프롬프트를 max_new+4 로 **한 번에** 돌린 꼬리 — 캐시가 바이트 동일하다는 뜻. 네 랭크
동일. 재절단 파일로 `check.py` 도 그대로 PASS(랭크 동일, 캐시 판정 잡음 바닥). 드래프터 K=5: 0/145(5층 체인).
서브 루프: `POST {conversation: id, ids|prompt, max_tokens}` 로 파킹된 대화를 이어간다(루프가 resume + extend; idle 턴은 티어가
있으면 **즉시 파킹** — 아레나는 늘 살아 있는 일만 쥔다). 부팅에 D11 `Config`(사실 7개, 노브 0: `STK_*` 미선언 env 는 부팅 사망).
같은 시각 이미지 안 서빙 레인 판정은 세 GPU 작업 동시 실행으로 컨텍스트 생성에서 OOM — 순차로 다시 돈다(§11).

### 45차 §11 — 정렬: 서빙 mHC 레인의 CUDA_ERROR_INVALID_VALUE 는 스레드가 아니라 12 바이트였다 (2026-09-11 밤)

메인 스레드로 옮겨도(`on_main`) 이미지 안 판정이 같은 자리에서 죽었다 — 그런데 **층 0 의 attn 믹스는 지나가고 ffn 믹스에서**.
랭크 파일은 spec 순서대로 텐서를 붙여 쓰는데 `hc.attn_base`(96 B) + `hc.attn_scale`(12 B) 뒤의 `hc.ffn_fn` 이 16 B 경계에서
12 B 어긋난다; DeepGEMM 의 TMA 디스크립터가 그 주소를 거부한다(`runtime_utils.hpp:145` invalid argument). 격리 프로브는
256 B·3840 B 오프셋(둘 다 16 정렬)만 시험해 "워커 스레드"를 원인으로 잘못 잡았었다 — 워커 스레드 실패도 진짜였으나
(DeepGEMM JIT 스레드 가정), 그것만이 아니었다. 조치: `base/preshard.RankWriter` 가 **크기가 256 의 배수인 텐서를 먼저, 홀수
꼬리(hc base/scale·A_log·라우터 bias — 평범한 torch 읽기)를 맨 뒤에** 쓴다 → 커널이 보는 뷰는 전부 256 B 정렬. 랭크 파일 세
번째 재절단(v3), fan-out 은 `--whole-file`(재절단 파일에 델타 전송은 손해). 결과는 §12.

HTTP 두 번째 턴(파킹된 대화 이어가기) 첫 시도는 **0 토큰**: `runner.extend` 가 `kv.tokens`(예약 = 마지막 horizon 만큼 초과)를
컨텍스트로 써서 프리필 시작 위치가 틀렸고, 서브 루프가 턴 시작점을 잘못 빼서 답이 비었다. 조치: 프로토콜에 `model.context(seq)`,
`adapter.extend` 가 "대기 중인 마지막 토큰 + 새 프롬프트" 수를 돌려주고 turn 경계는 prompt_len 이동으로만 표현. 재실행은 §12.

### 45차 §12 — 정렬 뒤: 서빙 mHC·KDA 레인 통과, 파킹된 대화 HTTP 로 이어짐 (2026-09-11 밤)

- 정렬 랭크 파일(v3)로 이미지 안 판정: 층 0~2(mHC pre/post·서빙 KDA chunk·conv)를 **지나** 층 3 의 MLA 레인에서 "megakernel MLA
  lane did not arm" — 원인은 env: `VLLM_GLM53_MK_MLA=1` 만 주고 마스터 `VLLM_GLM53_MEGAKERNEL=1` 을 안 줬다(모듈 기본 0). 판정
  러너와 플릿 런처 둘 다에 마스터 플래그 추가, 재실행.
- `boot.py --local --serve --park --drafter`: 첫 턴 둘(6 토큰씩) → 대화 0 은 즉시 파킹 → `POST {conversation: 0, ids: 5개, max_tokens: 4}`
  → 루프가 resume + extend(대기 중이던 마지막 토큰 + 새 5 토큰을 프리필) → **4 토큰, 7.05 s**(참조 레인, 5층). 네 랭크 락스텝,
  16 스텝, 3건 응답. **PASS**.

### 45차 §13 — PR #535(codex) 평가와 병합; 서빙 레인 전 구간 판정에서 b12x 만 참조와 다르다 (2026-09-12 새벽)

**PR #535**(`codex/st-engine-runtime-hardening`, main 머지됨)을 diff 로 평가했다. 맞게 잡은 것 셋: (1) **인덱서 꼬리 링** — 내
`[kpool=4]` 링은 K=5 드래프트가 4 칸 떨어진 위치끼리 충돌해 거부된 드래프트가 수락 위치의 raw key 를 덮어썼다; `kpool-1+K=8` 칸
+ 셀당 한 번 쓰기로 고침. 내 롤백 판정이 못 본 이유: ≤2048 토큰이면 풀이 전부 선택돼 키 내용이 어텐션에 안 보인다(그들은 18/20
→ 20/20 으로 정량화). (2) **rank 0 = srv2** — `env://` 초기화는 rank 0 이 MASTER_ADDR 에 TCPStore 를 연다; 내 순서(srv1=0)는
NCCL 초기화가 불가능한 배치였다. GID 도 컨테이너 안 `CT_GID_PRELUDE`. (3) **256 B 정렬**을 나와 독립적으로 같은 12 B 원인으로
찾아 패딩 텐서로 해결(spec 순서 보존, 내 "홀수 꼬리 뒤로"와 동치). 그 외: 참조 MLA 의 latent 전체 fp32 변환(호출마다 O(S))을
gather 뒤 변환으로, `reserve_to`·트랜잭션 submit·디코드 폭 초과 사망(D3)·티어 Future/잠금. 증거: 네 노드 각 한 프로세스 NCCL/RoCE
로 층 0·3 참조·서빙 레인 PASS(격리 체크아웃, 프로덕션 미접촉) — **첫 ST 엔진 플릿 실행**. 마찰: `runtime.py` 가 `adapter.py` 와
중복(접을 것), 원장 대신 로그 3천 줄 커밋, b12x 수치는 paired 오라클(자기 자신과 비교, 반복 오차 2.7e-2)이라 미판정.

**병합**: #535 의 블록-메이저 페이지드 캐시(한 물리 블록에 모든 층 → 블록 = NVMe 단위, 내 33-영역 티어 확장 불필요), 강화된
러너/스케줄러/kv, 정렬 라이터, KDA 고정 전치, `mma_sf_view` 를 채택; 내 것 중 대화 동사(park/resume/wake/extend/evict, extend 는
`model.context` 와 `reserve_to` 로), 서브 루프 이어가기, `--ckpt-meta`·메타 배포, 서빙-대-참조 레인 판정을 다시 얹음. 자가검증 9종 OK.

**서빙 레인 전 구간 판정**(내 정렬 랭크 파일, 0~4층, b12x 포함, 한 상자): 네 랭크 동일, 청크/verify 잡음 바닥 안(롤백 L0 kda
p50 4.4e-3 vs 바닥 2.7e-3 는 1.5× 기준의 경계 — 서빙 KDA 의 바닥이 참조보다 낮아서), **서빙-대-참조**: mHC·KDA(chunk/recurrent)·
conv·인덱서·kpool·MLA 가 층 0~3 어텐션 블록에서 p50 ≤ 1.1e-2 로 일치, **L3 MoE 만 p50 0.49** — 서빙 b12x 와 참조 MoE 중 하나가
틀렸다(후보: 활성화 동적 양자화 세부, gate/up 반 순서, α 의미). #535 의 오라클은 이걸 볼 수 없다. 다음 판정 대상.

### 45차 §14 — 병합본 검증 (2026-09-12 새벽)

#535 병합본(페이지드 캐시 + 강화 러너 + 내 대화 동사·서브·메타 부팅·레인 판정): 자가검증 9종 OK. `check.py --layers 0-4`(참조):
네 랭크 동일, **페이지드 == 연속 오라클(정확 일치)**, 러너 생성 [3, 1], 청크/verify/롤백 PASS. `boot --local --drafter --park`:
32 스텝, park 1.3 MiB(블록-메이저 블록 하나) → 아레나 542 → 543 → resume → 이어서 4 토큰 == 한 번에 돌린 꼬리, 네 랭크 동일, PASS.
첫 시도는 이전 스모크가 같은 티어 디렉터리에 파킹한 seq 0 을 #535 의 트랜잭션 submit 이 거부해 죽었다 — 옳은 거부. 조치: 파킹
파일은 **쓴 블록 레이아웃에서만 재개**(매니페스트에 block_bytes, 다르면 stale 로 남기고 부팅 때 개수만 보고), 로컬 스모크는
실행별 티어 디렉터리. srv2 에 rank0(새 순서) 복사 완료.

### 45차 §15 — b12x 수치 판정: 커널은 w13 의 **둘째 반**을 게이트한다 ([up; gate]) (2026-09-12 새벽)

`probes/b12x_lane_semantics.py`(이미지 안, 층 3 실제 전문가, 우리 랭크 파일의 [gate; up] 순서): 서빙 b12x vs 참조 rel **0.75**;
참조의 gate/up 반을 바꾸면 **0.106**, 서빙에 바꾼 가중치를 주면 0.106 — 원인은 반 순서. vLLM 은 자기 [gate; up] 을 flashinfer
CuTe-DSL 커널의 [up; gate] 로 로드 때 바꾼다(`reorder_w13_to_w31_for_flashinfer_cutedsl` / `swap_w13_to_w31`); 우리 b12x 백엔드
(`flashinfer_b12x_moe.py`)는 그 위에서 돈다. 서빙 반복 오차(원자적 합) 1.9e-2.
남은 0.1 의 정체(`b12x_lane_semantics2.py`, fc4 표현 가능 활성화로 동적 스케일 = 1, 전문가 하나, 가중치 1): 서빙 vs 참조 max
**5.5e-2**, 토큰별 [0.015, 0.055, 0.012, 0.003], corr 0.99959, 크기비 1.003 — 남는 것은 fc2 입력의 e2m1 양자화(가수 1비트) 반올림
차이와 bf16/원자합. 반대 순서(gate first)는 0.92. 즉 **의미 불일치는 반 순서 하나였고, 나머지는 W4A4 의 고유 잡음**이다.
조치: `specs.py` 가 w13 을 [up; gate] 로 쓴다(패킹·스케일 모두), 참조 레인은 둘째 반을 게이트. 랭크 파일 4차 재절단 + fan-out,
서빙 판정과 참조 체크 재실행은 §16.

### 45차 §16 — 랭크 파일 v4([up; gate]) 판정: 서빙 b12x 레인이 참조와 청크 잡음 바닥 안에서 같다 (2026-09-11 저녁)

재절단(`preshard.py`, srv4): 176.09 GiB 읽고 4 × 44.50 GiB 를 483 s 에 씀(1,120 텐서/랭크, 256 B 정렬). fan-out: rank0 → srv2, rank2 → srv3
(srv1 은 디스크 29 GB 라 그대로 비어 있음). 서빙 판정(`probes/run_engine_check.sh --layers 0-4`, 이미지 안, 512 토큰, TP=4 스레드, 82.0 s):
네 랭크 동일(max |diff| 0), 페이지드 == 연속 오라클(정확 일치), 러너 생성 [3, 1], 서빙 전문가 레인 반복 오차(원자합) 2.5e-2.
서빙 레인 vs 참조 레인(같은 구성·같은 입력, 블록별 토큰 rel p50/max; 괄호는 그 블록의 청크 잡음 바닥 p50):

| 블록 | 서빙 vs 참조 p50 | max | 바닥 p50 | 비 |
|---|---|---|---|---|
| L0 kda / dense | 4.7e-3 / 8.0e-3 | 1.0e-2 / 2.0e-2 | 2.7e-3 / 5.3e-3 | ≤1.7× |
| L1 kda / dense | 6.1e-3 / 1.0e-2 | 2.5e-2 / 2.6e-2 | 4.5e-3 / 6.8e-3 | ≤1.5× |
| L2 kda / dense | 1.1e-2 / 1.2e-2 | 2.2e-2 / 2.4e-2 | 8.2e-3 / 7.9e-3 | ≤1.5× |
| L3 dsa | 9.1e-3 | 2.0e-2 | 8.5e-3 | 1.07× |
| **L3 moe (b12x)** | **9.5e-2** | 5.4e-1 | 8.1e-2 | **1.17×** (§13: 0.75) |
| L4 kda | 4.2e-2 | 1.5e-1 | 3.5e-2 | 1.2× |
| L4 moe (b12x) | 1.4e-1 | 8.3e-1 | 1.3e-1 | 1.08× |
| 최종 hidden | 7.7e-2 | 4.9e-1 | — | — |

참조 체크(v4 파일, 참조 레인, 109.3 s): 네 랭크 동일, 오라클 정확 일치, 청크/verify/롤백 전 블록 바닥 안(L3 moe verify step 1 p50 6.4e-2, 바닥 7.3e-2;
롤백 step 2 7.4e-2, 바닥 7.3e-2). **판정: b12x 전문가 레인 바인딩 끝** — §13 의 0.75 는 반 순서 하나였고(§15), 남는 것은 W4A4 고유 잡음이다.
**정정(§17 에서 발견)**: 서빙 판정 스크립트의 종합 줄은 **FAIL** 이다 — 판정 대상 25 행(kda·dsa 의 청크/verify/롤백) 중 한 행,
`rollback step 2 L0 kda p50 4.4e-3`(바닥 2.7e-3 의 1.63×, 규칙 1.5×; max 7.2e-3 는 통과). 참조 레인의 같은 행은 정확히 0(토치 경로는 형상 무관)이고,
서빙의 층 0 바닥 2.7e-3 은 청크 커널의 형상 잡음, 4.4e-3 은 그 위에 얹힌 **재귀 커널 vs 청크 커널** 산술 차이다(코덱스 측정: 재귀 출력 rel 6.2e-4, 청크 6.5e-3).
캐시 불일치라면 1e-1 대로 나온다. 서빙 verify/롤백 행의 바닥은 재귀 경로로 따로 재야 한다 — 열린 항목, 게이트를 늘리지는 않았다.
MoE 뒤 블록의 max 0.5~0.8 은 라우터 top-8 뒤집힘(§5)이라 어느 엔진에서도 같다. 층 0-4 판정에 든 시간: 사전샤딩 483 s + fan-out + 판정 82 s + 참조 109 s.

### 45차 §17 — 커널 패키징: vLLM 이름공간을 떠난 엔진, ST 이미지 안에서 판정 (2026-09-11 저녁)

**발견**: 운영자의 "진행" 뒤 시작하려던 A단계(engine/kernels)가 **codex 세션의 손에서 이미 이미지까지 되어 있었다** — `st-engine:9391`(17:25,
빌드 컨텍스트 `/tmp/st-engine-9391`, git 아님, 어느 브랜치에도 없음): `engine/kernels/{kda,causal_conv,kpool,mhc,mla,b12x,deep_gemm}`
(49 파일, 출처·이식 전 sha256 은 `SOURCES.json`, repository 25 / image 24) + `engine/runtime/{Dockerfile,build.sh,promote_deep_gemm.py,
dependencies.json}` + `probes/run_engine_probe.sh` + `tests/test_engine_kernels.py`. 이미지의 나머지 엔진 파일은 #537 이전 스냅샷(내 대화
동사·메타 부팅·레인 판정이 없음). 다시 만들지 않고 **들여와 판정**했다(중복 구현 금지). 판정 중 codex 는 더 새 트리(`/tmp/st-engine-completion`,
18:44: LICENSE·THIRD_PARTY_NOTICES·kernels/README·dockerignore·`weights.py` 레이아웃 표식·자기 런처)로 넘어갔고 45층 전체를 [up; gate] 로
`~/models/st-glm53-9391-up-gate-full`(4 × 44.5 GiB, srv4 디스크 505 GB 여유)에 재절단 중이다. 그쪽 저위험 파일(라이선스 셋, dockerignore,
`moe_static_ep_tiled.py` 의 `_SF_STAGE_BYTES` NameError 수정, `_CUTE_DSL_MODULE = "st_b12x_moe"`, 전역 검사 테스트)은 접어 넣었고, 레이아웃
표식(랭크 파일 재절단 필요)과 런처 재작성은 그쪽 PR 에 맡긴다.

**형태**: 서빙 레인은 `engine.kernels.*` 만 임포트(`tests/test_engine_kernels.py` 가 `vllm`·`flashinfer.fused_moe` 임포트를 AST 로 막고, symtable 로
이식된 함수의 전역 누락을 잡는다). 진짜 의존은 Triton 3.7.1·TileLang 0.1.12·CUTLASS DSL 4.6.2·FlashInfer 0.6.18 유틸/JIT·DeepGEMM 2.6.1.
DeepGEMM 은 시드의 `vllm/third_party/deep_gemm`(확장 .so + JIT 헤더 879 파일)을 `deep_gemm` 로 승격(sha256 검증), 그 뒤 `pip uninstall vllm`.
mHC 는 `torch.ops.vllm` 등록 없이 pre/post 함수 직접 호출, MLA 는 메가커널 .py 3,041 줄 중 MLA 드라이버 367 줄만(.cu 4,066 줄은 그대로,
빌드 루트 `ST_MLA_BUILD_ROOT=/cache/mla`), 마스터 플래그 없이 **필수 레인**(부팅 자가검증 실패 = 부팅 실패). 이미지 `st-engine:glm53`:
`bash engine/runtime/build.sh`(시드 ID 핀, `--network none`) — 시드 위 층 둘(34.7 MB + 4 MB), `docker system df` UNIQUE 4 MB.
**31.5 GB 는 시드의 것**(dist-packages 16 GB: flashinfer_cubin 6.8·nvidia wheels 3.1·torch 0.9·vllm 0.7·triton 0.65; cuda-13.0 2.6; /root 2.2);
`pip uninstall` 은 층 위의 whiteout 이라 크기를 못 줄인다. 자체 베이스로 다시 쌓으면(B단계 본편) flashinfer_cubin·vllm·cupy 를 빼 10 GB 안팎.

**첫 실행에서 잡은 것**: `kernels/mla/__init__.py` 의 `_rel_err` 가 `math` 없이 `math.isfinite` — 자가검증 경로가 한 번도 GPU 를 안 탄 채
이미지가 구워졌다(NameError → "rank 0 failed"). `import math` 한 줄; codex 의 새 트리도 같은 수정을 했다.

**ST 이미지 안 판정**(오버레이 마운트 0, vLLM 없음, `ST_CACHE=~/glm53-cache`):
- 참조 레인 `check.py --layers 0-4`: **PASS** — 네 랭크 동일, 페이지드 == 연속 오라클, 러너 [3, 1], 청크/verify/롤백 전부 바닥 안, wall 85.1 s.
- `boot.py --local --layers 0-4 --drafter --park`: **PASS** — 32 스텝 65.9 s, 네 랭크 토큰 동일, DFlash2 K=5 0/145 수용(5층 사슬이라 품질 아님),
  park 1.3 MiB(아레나 542 → 543) → resume → 이어서 4 토큰 == 한 번에 돌린 꼬리, 541/1 자원 반납.
- 서빙 레인 `run_engine_check.sh --layers 0-4`: 옛 판정 이미지의 §16 수치와 **소수점 그대로 같다**(서빙 vs 참조 L3 moe p50 9.5e-2, L0 kda
  4.7e-3 …, 반복 오차 2.2e-2, wall 54.3 s — JIT 캐시 웜 뒤 82 → 54 s). 종합 줄은 두 이미지 모두 **FAIL 한 행**(§16 정정 참조): 패키징의
  회귀가 아니라 서빙 레인의 성질이다.

**잔여(헌장 D11/레거시)**: 커널 소스 안 환경 노브 35 개(b12x 21 — 정적 v2 사다리·EP tiled·W4A16 강제·prefill reuse; fla 8; mla 5; mhc 2;
이름 `ST_GLM53_*`/`FLA_*`/`DENEB_*`) — 기본값이 서빙 경로, 정리는 다음 단계. .cu 는 MLA 만 바인딩하지만 통째. 런처는 노드마다 rsync →
`build.sh` → run 으로 바꿨다(플릿 창 미확보라 미실행, `bash -n` 만). srv1 디스크는 그대로 열린 문제.

**추기(19:10)**: 위를 쓰는 사이 codex 의 **PR #541**(`codex/st-engine-native-kernels`, +67,544)이 18:35 에 main 에 머지되어 있었다 — 같은
패키지 + `weights.py` 표식(`weight_layout=st-glm53-b12x-up-gate-v1`, 없는 랭크 파일은 부팅·판정이 거부) + 자기 런처 + `measurements/
st_engine_native_kernels_20260911`. 이 브랜치는 main 을 병합하며 커널·런타임·런처·프로브 러너·테스트·lanes·README 는 **main 을 택했고**,
런처엔 노드마다 `build.sh` 한 줄(main 은 이미지가 없으면 중단만 한다), 프로브엔 §15 판정 주석만 되살렸다. 이 브랜치의 몫은 원장 §15~§17,
`b12x_lane_semantics*` 판정, ST 이미지 안 판정 기록이다. **랭크 파일**: 표식이 있는 것은 codex 의 재절단본 `~/models/st-glm53-9391-up-gate-full`
(19:04 완성, 4 × 44.5 GiB) 뿐이고, `facts.RANKS`(`glm53-redhat-nvfp4-tp4`, 내 v4)와 srv2·srv3 의 사본에는 없다 — 표식본을 제자리로 옮기고
다시 fan-out 해야 main 의 엔진이 그 파일을 연다(운영자 판단: 178 GB 이동). 병합본 판정은 §18.

### 45차 §18 — 병합본(main #541 + 이 브랜치) 판정: 표식 랭크 파일로, ST 이미지 안 (2026-09-11 저녁)

`engine/runtime/build.sh` 로 병합 트리에서 `st-engine:glm53` 재빌드(캐시 히트, 층 둘) 뒤 `--ranks ~/models/st-glm53-9391-up-gate-full`(codex 재절단,
`weight_layout` 표식 있음):

| 검사 | 결과 | wall |
|---|---|---|
| `check.py --lanes served --layers 0-4` | 네 랭크 동일, 오라클 정확 일치, 러너 [3, 1]; 서빙 vs 참조 L3 moe p50 **9.3e-2**(§16 9.5e-2), 최종 hidden 7.7e-2, 반복 2.5e-2; 종합 **FAIL 한 행**(`rollback step 2 L0 kda` 4.4e-3, §16 정정과 같은 행·같은 값) | 49.3 s |
| `check.py --lanes reference --layers 0-4` | **PASS** | 90.2 s |
| `boot.py --local --layers 0-4 --drafter --park` | **PASS** — 32 스텝, 네 랭크 토큰 동일, park 1.3 MiB → resume → 이어서 4 토큰 == 한 번에 돌린 꼬리, 541/1 반납 | 71.3 s |

즉 #541 의 커널 패키지·표식 계약 위에서도 엔진의 판정 결과는 §16~§17 과 같다(패키징 전후, 옛 판정 이미지/ST 이미지, 내 v4/codex 재절단본 —
네 조합 모두 같은 수치, 같은 한 행). 같은 GPU 에서 codex 의 `engine_decode_graph_check.py --distributed`(st-completion-9391)가 동시에 돌았다.

### 45차 §19 — 첫 45층 4노드 부팅: 돌았으나 다른 세션의 플릿과 충돌해 죽었다; 문(chat)·잠금 (2026-09-11 밤)

**부팅**(19:38, `RANKS_DIR=~/models/st-glm53-9391-up-gate-full`, codex 재절단 표식본; 노드마다 rsync → `build.sh` → 컨테이너): 네 컨테이너
19:38:56~19:39:04 시작. 로그엔 NCCL 경고 한 줄뿐 — `python3` 이 `-u` 없이 돌아 stdout 이 블록 버퍼링됐고, 죽을 때 버퍼가 사라졌다(런처 수정:
`python3 -u`). 그래도 **rank 0 의 사망 줄이 부팅 성공을 증언한다**: `Watchdog caught collective operation timeout: WorkNCCL(SeqNum=91482,
OpType=BROADCAST, NumelIn=17)` — 서브 루프의 `broadcast_object` 가 **91,482 번** 돌았다(유휴 2 ms 루프 ≈ 3 분). 즉 45층 로드·JIT·자가검증·
NCCL 초기화를 지나 서빙 루프에 들어가 있었다(19:41 께). 요청은 못 보냈다(문 대기 태스크가 세션과 함께 끊김).
**충돌**: 19:42:01 codex 세션의 `st-full-completion-9391` 컨테이너가 srv1·srv4 에 떴고(그쪽 첫 45층 부팅), 19:42:25 내 rank 2(srv3)가 137 로
죽고(cgroup OOM 아님) 19:44:08 rank 0 의 브로드캐스트가 120 s 타임아웃 → 19:45:13 rank 0 종료. codex 쪽은 "55.4 GiB 아레나 할당 실패,
srv1/srv4 SSH 응답 없음"(PR #545 본문) — 같은 노드에서 내 플릿이 55 GiB 를 쥔 뒤였다. 20:19~20:22 남은 컨테이너가 모두 255 로 정리되고
20:31 프로덕션 vLLM 이 다시 떴다(플릿 창 닫힘). **두 세션 모두 결과 없음.** 조치: 런처가 `st-*` 컨테이너도 거부하고 rank 0 노드의
`~/st-fleet.lock` 을 잡는다(`stop` 이 푼다) — 플릿을 쓰는 모든 세션이 지켜야 한다(README).
**main 병합**(#543~#546, codex: 인덱서 슬롯 융합, 실행 소유권 분리, 디코드 그래프·TP4 MoE fp32 누적, 풀 슬롯 커널; 13 커밋, 충돌 없음).
`facts.RANKS` 가 `glm53-redhat-nvfp4-tp4-up-gate-v1` 로 바뀌었는데 **그 디렉터리는 어느 노드에도 없다** — 표식본은 네 노드 모두
`st-glm53-9391-up-gate-full` 에 있다(RANKS_DIR 로 넘기거나 심링크).
**문**: `base/serve.py` 에 OpenAI chat 방언 — `POST /v1/chat/completions`(토큰 단위 SSE 스트리밍: 스텝 루프가 요청별 큐에 새 토큰을 넘기고
HTTP 스레드가 텍스트 델타로; 불완전 멀티바이트는 다음 토큰까지 보류; `</think>` 앞 `reasoning_content` 뒤 `content`; `stream_options.
include_usage`), `GET /v1/models`, `GET /metrics`(벤치가 읽는 `vllm:` 이름의 카운터: success/running/waiting/prompt/generation/spec_decode
accepted·draft), `/health`. 프로필은 프로덕션과 같은 `chat_template_mm_v2.jinja`(transformers 렌더, `thinking` kwarg 통과)와 `</think>`=154842 를
넘긴다(이미지 안 검증: 렌더 결과를 `tokenizers` 로 인코딩한 id 가 transformers 와 동일, 22 토큰). `tests/test_engine_serve.py` 20 tests OK
(문 4종 추가: 목록·카운터·health, 통째 답, 토큰 스트림 + 추론 분리, 엔진 사망 시 스트림 클라이언트 해제). `boot --local --serve` 는 스트림
chat 한 턴을 더 판정한다(GPU 창 없어 미실행). **다음**: 플릿 창(프로덕션 정지, 한 세션만) → 부팅 → chat 한 요청 → `bench/onepass.py`.

### 45차 §20 — 운영자 지시 넷: prefix 재사용, 취소·타임아웃, 문의 나머지, 슈퍼바이저 (2026-09-11 밤)

플릿 창이 없어(프로덕션 vLLM 20:31 복귀) 모두 CPU 판정(가짜 엔진·실물 풀·러너)까지, GPU 판정은 프로브로 준비했다.

**취소·타임아웃**(`base/serve.py`): 취소는 rank 0 이 도착과 같은 브로드캐스트에 실어 네 랭크가 같은 반복에서 같은 행을 버린다(FIFO 에 있으면
빼고, 행이면 `runner.cancel` → 블록·슬롯·모델 상태 반납 → 클라이언트에 499/504). 근거 셋: 클라이언트 소켓 EOF·broken pipe(토큰 묶음마다
`select`+`MSG_PEEK`), `REQUEST_TIMEOUT_S`=3600 초과, `stop` 문자열(내용 채널에서 잘라 조기 종료). chat 요청은 스트림이 아니어도 토큰 큐로
흐른다(끊김·stop 이 양쪽에서 같이 먹힌다).
**문의 나머지**: `min_tokens` 는 어댑터에서 뽑힌 뒤 고침(끝 토큰이 최소 전에 뽑히면 그 자리의 차선 토큰; 캡처된 샘플링 그래프 무변경),
`tools` 는 템플릿 렌더 + `<tool_call>{name}<arg_key>k</arg_key><arg_value>v</arg_value>…</tool_call>` 파서(`profiles/glm53/tools.py`) →
`tool_calls`, `finish_reason` `tool_calls`; `n`≠1·`logprobs` 는 400; `usage.completion_tokens_details.reasoning_tokens`(게이트웨이가 읽음).
게이트웨이(`deneb/gateway-go`)는 비스트리밍 chat + tools 를 쓴다.
**슈퍼바이저**(`launchers/st-glm53-supervisor.sh` + `st-glm53.service`, dsv4-tp4-supervisor 의 형): 30 s 마다 4 토큰 chat(문이 살아도 링은
죽을 수 있다), 3 회 실패 → 포렌식(네 랭크 로그·free·nvidia-smi·metrics) → stop → start, 백오프 60 s ×2 ≤ 30 min, 5 회 뒤 정지. 프로덕션·
q38 컨테이너가 보이면 절대 안 띄움. 한 사이클 판정(`ST_SUPERVISOR_ONCE=1`): 지금은 "fleet taken: 10.10.10.2:glm53".
**prefix 재사용**(`base/prefix.py`, `base/kv.py` 소유 개수, `runner.submit(ids)`/`_checkpoint`, `scheduler.arrive(computed)`, GLM
`caches.snapshot_layout/checkpoint/restore`, `adapter.checkpoint/restore`, boot `PREFIX_SNAPSHOTS`=8): 단위는 프리필 청크(6,912 = 블록 3개)
— 선형 어텐션의 상태는 프리필이 멈춘 자리에만 있으므로 청크 경계마다 링 상태를 스냅샷(KDA conv 탭 3 + 재귀 1 × 34층 = 35.4 MiB,
드래프터 링 41.9 MB; 꼬리 링은 경계에서 비어 제외)하고 앞 블록들을 핀. 새 프롬프트는 자기 길이보다 짧은 가장 긴 경계를 입양(공유 블록은
완결·읽기 전용, 상태는 슬롯에 복원)해 거기서 프리필 — 토큰 하나는 늘 계산. 회수는 LRU, 풀이 부족할 때 콜백으로. vLLM 의 APC 와 다른 점:
블록 단위가 아니라 청크 단위(2K 프롬프트는 이득 없음; 6,912 이상의 공유 문서·시스템 프롬프트가 대상), 세션 간 대화 이어가기는 D16 이 맡는다.
**판정**: `tests/test_engine_prefix.py` 8(소유 개수·입양·핀·회수·해시 사슬·경계 캐시/입양·LRU·실패 반납), serve 25(취소 5 종 추가),
tools 3, glm53 32, runtime 28, bootpaths 2, tier 16 — 전부 OK. GPU: `probes/engine_prefix_check.py`(공유 두 청크 + 다른 꼬리 두 프롬프트의
토큰 동일·프리필 시간 비) — srv4 는 프로덕션이 GPU 100 GB 를 쥐고 있어(가용 21 GiB) 돌리지 않았다. 남은 것: 창에서 프로브 → 45층.

### 45차 §21 — 커널 패키지 노브 43개의 D11 정리: 채택값은 코드로, 후보 둘은 선언된 노브로, 나머지는 폐기 (2026-09-12 새벽, srv4, GPU 없음)

**출발점**(§17 잔여 "노브 35개"의 실측; main 의 §20 과 나란히 붙는 절이라 §21): `engine/kernels` 가 `os.environ`/`getenv` 로 읽던 이름은 **43개**(b12x 20, kda 10, mla 11 = 파이썬 6 + `.cu` 5, mhc 2)였고,
어느 것도 `base/config` 의 `STK_` 게이트를 지나지 않았다(프로필은 `knobs=[]`). 그중 넷은 프로덕션 `profiles/glm53.env` 가 켜 둔 채택값인데 ST 는 하나도 안
넘겨 **스톡·off 로 돌고 있었다**: `B12X_STATIC_V2=t,r,sf6`(09-09 채택, 38차 v4 +9.7~11.6%, sf6 스케일 4.43→3.36 GiB/랭크), `MK_PDL=1`(27차 발사당 58.0→53.6 µs),
`KDA_PREFILL_QK_NORM=1`(09-06), `EP_TILED=1`(E=72 EP 기하라 TP4 ST 엔 무관). 운영자(09-12 새벽): "기존 채택 노브 중 살릴 수 있는 건 최대한 살리고, 어떤 건
노브로 하고 어떤 건 엔진이나 커널에 그대로 박자".

**세 갈래**(환경 읽기 43 → 0; `tests/test_engine_kernels.py` 가 AST 로 강제, 예외는 `ST_MLA_BUILD_ROOT` 캐시 경로 하나):
- **박기(코드 기본값 = 프로덕션 값)**: `.cu` `mk_pdl_enabled()` → true, `MK_INPUT_CTA` 4, `MK_INPUT_REUSE` 1(GEMM 전용, ST 미사용이나 충실히); KDA strided Q/K norm on;
  `_MICRO_SHARE_INPUT` on, 컷오버 640(런처가 안 넘기던 네 별칭), 경계 검사 on; FLA 라이브러리 8개는 기본값 고정(ieee tril·정확 exp/log·TMA/그래프 off·kernel2 norm).
- **선언된 만료 노브**(`boot.declared`, 만료 09-30, `STK_*` 미선언·만료 = 사망; 런처·프로브 러너가 `STK_*` 를 통과시킨다):
  `STK_moe_static` 기본 `stock`(§15~18 이 판정한 구성), 후보 `t,r,sf6[,q0]`(프로덕션 채택값 + TP 레시피 `TP_SF6_Q0=1`);
  `STK_mla_prefill` 기본 `stock`, 후보 `tile32|pair|pair4`(프로덕션 off, "numerics·TTFT 브래킷 대기").
- **폐기**: EP 타일 계열 5파일 4,248줄(`glm53_ep_*`, `moe_static_ep_tiled`; 임포트 그래프 도달 불가, TP=4 형태가 아님)과 그 노브 3, 강제 W4A16, 백엔드·컷오버·MAC 사다리
  강제 5, `DENEB_B12X_TILE_M`, KDA regime, mHC pass/big-fuse, MLA `SPLITS`/`PAIR_STATS`, `.cu` `MK_KSR2`/`MK_MLA_PROBE`(프로브는 `mk_set_gemm2` 등 pybind 로), `base/instruments`
  의 미선언 `STK_INSTRUMENT_MEMORY`.

**`t,r,sf6` 를 ST 에서 살리는 배관**(이번에 코드로, GPU 판정은 미실행): `lanes.served(moe_static=…)` 가 `moe_dispatch.configure_static_v2()` 를 한 번 적용(뷰가 생긴
뒤엔 거부 — 타일/행 우선 선택은 프로세스당 하나), 새 `Lanes.moe_prepare` 를 `Glm53Net.bind()` 가 층마다 캡처 전에 부른다: 셀 `t` 는 `tile_expert_weights_inplace` 로
**아레나 바이트를 제자리 타일 우선으로**(층당 임시 사본 하나, 45층 두 벌 없음), `sf6` 는 `_get_weight_views(packed_only=True)` 로 packed 스케일 소유자만(스케일 메모리:
지금 스톡 경로 raw 4.43 + 변환 사본 4.43 → sf6 raw 4.43(아레나, 못 지움) + packed 3.36 GiB/랭크, 즉 −1.1 GiB). 호출은 `b12x_fused_moe(_weight_views=)` 로 뷰를 넘긴다.
판정기는 `ref_net.p = net.p` 로 같은 텐서를 읽으므로 참조 레인은 `engine/modules/expert_layout.row_major_expert` 로 타일 바이트를 전문가별 행 우선 사본으로
되읽는다(CPU 검증: 제자리 재배치 → 되읽기 바이트 동일, 멱등). 바인딩 없는 프로브 호출은 사본 경로(호출자의 행 우선 텐서 보존).
**끝 모양**은 사전샤딩이 타일 우선·packed 스케일을 직접 쓰는 것(랭크 파일 재절단 = 운영자 판단).

**검증**: 이미지 안 CPU 스위트 `test_engine_*.py` 178 tests OK(46 skip = GPU), 새 `tests/test_engine_knobs.py`(선언·파싱·재설정 거부·타일 되읽기·prefill 모드) 포함;
`engine_kernel_check.py --imports-only` 48 모듈 임포트·served 테이블 바인딩 OK. `.cu` 편집(getenv 5→0) 은 JIT 키를 바꾸므로 다음 부팅에 노드마다 nvcc 41~63 s(§17 ninja 로그).
**미실행**: GPU 판정(`run_engine_check.sh --layers 0-4 --moe-static t,r,sf6` 와 stock, `--mla-prefill`) — srv4 는 프로덕션 vLLM 서빙 중 가용 20 GB 라 창 없음.
따라서 `STK_moe_static` 기본값은 판정된 `stock` 이고, 후보가 판정을 통과하면 기본값을 `t,r,sf6` 로 바꾸고 노브를 지운다(D11 승격 = 노브 제거).

**§21 보충 — 폐기 재검토(운영자 "폐기가 되게 많네. 더 살릴 수 있는 건 없어?")**: 폐기 목록을 원장 판정으로 다시 갈랐다.
**측정돼서 진 것(그대로 폐기, D11 "진 쪽을 지운다")**: prefill reuse(39차 MOER NEUTRAL(−), −1.9~−2.0%), FC1 N128(기각, −71%), KDA regime(KREG NEUTRAL, 승격 안 함),
mHC big-fuse(GLM in-graph +0.1%, 9/1 트레이스 "아님"), 강제 W4A16(운영자 "억지 W4A16 금지"; API 인자 경로는 그대로). EP 타일 계열은 프로덕션이 09-10 절대 목표
67 tok/s 로 채택했으나 **디코드는 TP 보다 9.8% 느리고**(72.6 vs 80.5 tok/s, PR #511) 프리필 입력 tok/s 만 +4~15% — TP=4 형태의 ST 엔 잃은 승자가 아니며, EP 레인은
사전샤딩(E=72)·토큰 교환까지 포함한 형태 결정이라 별건이다(복구는 `git checkout` 한 줄).
**한 번도 안 잰 것·프로브 도구(env 없는 훅으로 되살림)**: MLA 분할 강제 `mla_decode(splits=)`, MLA 루프라인 모드 `mla_decode(probe=)`(`.cu` `run_mla` 넷째 int; 옛
`mk_mla_bench.py` 의 "streams only / + the dot"), 쌍 프리필 겹침 통계 `mla.PAIR_STATS`(39차에 env 함정으로 미기록이던 통계, `STK_mla_prefill=pair` 브래킷의 계기),
동적 tile_m 고정 `moe_dispatch._DYNAMIC_TILE_M_OVERRIDE`(Qwen3.8 40행/전문가 셀 측정용), 백엔드·컷오버·MAC 사다리(모듈 속성 직접 대입), mHC TileLang 패스(TMA·WS)
`engine.kernels.configure_mhc_passes()`(mhc 임포트 전; 순환 임포트 때문에 패키지 밖에 둠; mHC 는 프리필 스텝의 11.4% 인데 GLM 에선 미측정). 검증: 이미지 CPU 스위트
181 tests OK(46 skip), 임포트 프로브 48 모듈 OK. 노브 수는 그대로 둘(`STK_moe_static`, `STK_mla_prefill`).

**§21 보충 2 — JIT 캐시 지도 정정(운영자 "CUTE_DSL_CACHE_DIR 진행" 에 대한 답)**: 그 env 는 ST 에 무효라 넣지 않았다. 실측: (1) b12x 다섯 가족 중 넷(static·micro·dynamic·sf6 계열)은
flashinfer 래퍼 `build_and_load_cute_dsl_kernel` 로 컴파일되고 `.o` 를 `/cache/.cache/flashinfer/0.6.18.dev20260819/121a/cached_ops/` 아래(숨김 디렉터리, 앞선 `ls` 가 놓침)에
내보낸다 — 프로덕션 캐시에 CuTe 계열 `.o` 75개 80 MB, ST 모듈 `st_b12x_moe_sm121a_cute_dsl` 10개; 적중이면 `cute.runtime.load_module` 로 로드하고 DSL 컴파일은 없다.
(2) CuTe DSL 자체 파일 캐시는 `cute.compile` 경로에서 꺼진다(`dsl.py`: `if not no_cache and compile_only: no_cache = True`) — 토이 커널 두 번 컴파일에도 `CUTE_DSL_CACHE_DIR`
에 파일 0개, 프로덕션 컨테이너 `/tmp/root/cutlass_python_cache` 도 36분 서빙 뒤 0개인 이유. (3) 디스크에 안 남는 것은 direct micro 커널 하나(`cute.compile` + 프로세스 안 dict,
실제 스트림·자체 block-dim 프로브라 래퍼의 TVM-FFI 규약과 다름) — 래퍼로 옮기는 것은 GPU 검증이 필요한 후속; `STK_moe_static=t,r,sf6` 이면 이 경로는 안 탄다.
(4) #555 의 `moe_dispatch.py` 편집은 `_kernel_source_files()` 해시에 들어가 다음 부팅에 ST 모듈 `.o` 10개가 한 번 재컴파일된다.

**§21 보충 3 — direct micro 를 같은 디스크 캐시로(운영자 지시, 2026-09-12)**: 마지막 프로세스 안 컴파일이던 direct micro 커널을 flashinfer 래퍼
`build_and_load_cute_dsl_kernel` 과 같은 경로(`JitSpecCuteDsl`, 모듈 `st_b12x_direct_micro`, 키 = DSL 스택 + dispatch·kernel·activation·fp4_common·utils 소스 해시)로 옮겼다.
규약 변경: 컴파일은 `make_fake_stream(use_tvm_ffi_env_stream=True)` + `--enable-tvm-ffi`, 발사는 포인터 13개를 정수 주소로, 배리어 텐서 둘은 torch 텐서로, m·grid_x 는 int 로,
스트림 인자 없음(정적·동적 가족과 동일; `_tvm_ffi_args_spec_converter` 가 `Pointer` → `DataPointer`, `_FakeStream(env)` → 서명에서 제거). 프로브·테스트용 옛 형태는 `tvm_ffi=False` 로 남김.
block-dim 프로브(`compiled_direct_micro_accepts_block_dim`)는 프로세스 안 객체의 `kernel_info`·CUDA 라이브러리 핸들이 필요해 디스크 로드본으로는 못 돌린다 → 빌드 때 판정을
`.o` 옆 `<커널>.blockdim.json` 사이드카에 쓰고 적중 때 읽는다; 사이드카 없음·낡음(block_dim 불일치)·읽기 실패는 재빌드(추측 금지). CPU 검증(이미지 안): 새 `tests/test_engine_direct_micro_cache.py`
5 tests OK(미스→빌드·사이드카, 적중→로드, 사이드카 없음/낡음→재빌드, 거부 판정 보존, 캐시 비활성, FFI 발사 인자 17개 = 주소 13 + 텐서 2 + int 2, 스트림 없음).
**GPU 미검증**: 실제 `.o` 내보내기·재로드 후 발사와 수치 동일성은 다음 창의 `run_engine_check.sh --layers 0-4`(direct micro 는 8토큰 이하 디코드 검사 구간)로 판정한다.

### 45차 §22 — 쿠다 그래프 경로 검토와 개선 넷: 캡처 안전 통신, 서빙 천장 하나, 풀 분리 단언, 재생 경로 고정 메모리 (2026-09-12 새벽, srv4, GPU 없음)

운영자 "쿠다그래프 관련 엔진 구현 검토" → 정적 검토 + CPU 계산. **반례를 찾으려다 막힌 것(건전함 확인)**: 혼합 온도(캡처된 확률 샘플러가 디바이스 온도 버퍼에서 행별 greedy 를 읽고
`clamp_min(1e-5)` 가 0 나눗셈을 막아 이거 경로와 같은 답), 분산 argmax(음수 XOR·−0.0 정규화·NaN 페이로드 정규화·하위 32비트 인덱스 타이브레이크, 최대 비트 패턴 `<<32` 도 int64 안),
용량 버킷 패딩(`ctx//kp + count − 1 ≤ candidate_capacity − 1` 이라 `clamp_max` 는 패딩 행만; 미예약 페이지는 블록테이블 0 클램프로 인바운드 후 유효 개수 마스킹), RNG 상태 저장·복원,
MLA 무장이 사이드 스트림 워밍업에서 끝나 캡처 금지 가드를 안 건드림.

**개선 넷**:
1. **캡처 안전 통신을 형태로**(`comm.graph_capture_safe`): `LocalTP` 의 집합 통신은 호스트 `threading.Barrier` 와 파이썬 슬롯 대입으로 랭크를 건넌다 — 캡처하면 배리어는 캡처 시점에 한 번 돌고
   그래프에 노드를 안 남기며 피어 텐서는 캡처 당시 주소로 읽힌다(재생 때 순서 보장 0). 오늘은 `fleet()` 만 캡처해 도달 불가였으나 막는 코드가 없었다 → `Glm53DecodeGraphs` 가 디바이스를 건드리기 전에 거부(D3).
2. **서빙 천장 하나로 합침**: 문(`serve.max_context`)과 그래프 사다리가 각자 `F.max_position` 에서 따로 유도하던 것을 한 값으로 묶고(`Glm53Engine(context_ceiling=)` → `capacity_ladder()`),
   `STK_context_ceiling` 로 선언(만료 09-30, 기본 0 = 학습 위치 그대로라 **동작 불변**). 지금 사다리는 9버킷 = 타깃 그래프 36개(전체 51). 상위 셋(262K/524K/1M)은 한 시퀀스가 595블록 중 114/228/455개를
   혼자 쥐어야 도달한다. 자를 근거는 이미 부팅 메모리 행 `target/<shape>/{before,warmup,captured}` 의 seconds 에 있다(45층 부팅 캡처 28.2 s / 그때 115 그래프, 샘플러 통합 뒤 51).
3. **풀 분리를 단언으로**: 디코드 루프가 세그먼트마다 타깃 그래프 풀의 보조 은닉 상태를 읽는 사이 드래프터 관측 그래프가 재생된다. 안전한 이유는 오직 `DecodeGraphs` 인스턴스마다 풀이 다르기 때문인데
   아무도 그렇게 쓰지 않았다(공유하면 유한한 값으로 조용히 오염 — 어떤 수치 테스트도 못 봄). `graphs.pool` 공개 + `_check_graph_pools()` 가 캡처 직후 거부, 모듈 docstring 에 출력 수명 규칙 둘 명시.
4. **재생 경로에서 페이지블 복사 제거**: 스텝마다 CPU 텐서 3개를 만들어 페이지블 H2D 하던 것을 고정(pinned) 블록 하나 + numpy 뷰 기록 + `non_blocking` 복사로(샘플러 온도도 동일). 스텝당 모양 계산도 2회→1회.
   안전 근거: 다음 fill 전에 샘플러 readback 이 스트림을 동기화한다. 캡처 실패 그래프를 `close()` 가 reset 하던 경로도 고침(성공한 캡처만 등록).

**검증**: 이미지 CPU 스위트 244 tests OK(55 skip), 새 `tests/test_engine_graph_contracts.py` 10 tests(캡처 거부·사다리 천장·풀 분리·고정 스테이징), 임포트 프로브 48 모듈 OK.
**GPU 미검증**: `probes/engine_decode_graph_check.py`(재생 vs 이거 + 롤백)와 실제 부팅 캡처 시간. 프로덕션 서빙 중이라 창 없음.


### 45차 §23 — 프로덕션 전환 시도: 창·45층 4노드 부팅·문 사다리, 그리고 깨진 글의 원인 = KDA `o_norm` epsilon (2026-09-11 밤 ~ 09-12 새벽)

**창**: 22:21:56 srv2 `fleet-idle-recovery.timer` 정지(이 컨트롤러만 프로덕션을 되살린다), 22:22:01 네 노드 `glm53`/`glm53-worker` 제거. 직전 프로덕션 카운터
prompt 198,017 · generation 1,947 · success stop 10 + length 6. wormhole 의 `glm-5.3-flash-local` 은 `fallback: glm-5.3-flash`(z.ai) 라 창 동안 상류 API 가 받았다(로그 확인).
운영자 지시로 창은 진단이 끝날 때까지 열어 둔다(23:58 "프로덕션 복귀는 시키지마").

**부팅**(`start-st-glm53.sh`, RANKS_DIR=`~/models/st-glm53-9391-up-gate-full`, 네 노드 표식본): arena 2.26 s(+57.17 GiB 한 덩어리 — 부팅 OOM 가설 기각), load 41.2 s(44.5 GiB, 54 블록),
drafter 2.8 s, **capture decode 238.9 s**(콜드; 두 번째부터 63.7 s), 총 5분 20초(프로덕션 vLLM 웜 부팅 345 s 급). `ST engine: GLM-5.3, TP=4, lanes=served, KV 8.73 GiB, serving on :8000`,
`/v1/models` = `glm-5.3-flash`. 예산 표(rank 0): 상자 119.69 − OS 11.97 − 런타임 바닥 5.54 − 가중치 44.50 − 드래프터 2.18 − 슬롯 1.21 − prefix 스냅샷 0.59 − 워크스페이스 12.00 − 티어 0.12
= **KV 41.58 GiB**, 선언 7.52(580 블록) → 미할당 34.06 GiB; 동시 4 에서 1,048,576 tok/요청.

**부팅 넷의 판정**:
| 부팅 | 티어 | 디코드 | 결과 |
|---|---|---|---|
| 22:22 | srv4 에 16:52 로컬 테스트 잔재(rank0~3, seq-0/1) | 캡처 그래프 | 첫 요청 안 끝남, 네 랭크 96% GPU 스핀, `adapter.decode` 안 |
| 22:36 | 같음 | 이거(`STK_decode_eager=1`) | 64 토큰 나왔으나 글이 깨짐; rank 3 이 retire 에서 `conversation 0 is already parked` 로 사망 → 셋이 다음 all_reduce 에서 스핀 |
| 22:39 | 청소 | 캡처 그래프 | 3분 20초 진전 0(카운터 전부 0), 네 랭크 살아서 96% — 재현 2/2 |
| 22:45 | 청소 | 이거 | 요청 정상 종료(10.7 tok/s, 수용률 25%), **글은 여전히 깨짐** |

**뿌리 셋** (3 은 codex 세션이 같은 시각에 독립적으로 찾아 **PR #565** 로 먼저 머지했다 — 그쪽은 한 상자 LocalTP 45층 추적으로, 여기는 vLLM 층별 훅 대조와 플릿 4노드 서빙으로 같은 상수에 닿았다; 이 절의 몫은 판정 사슬·도구·플릿 검증이고 수정 커밋은 #565 의 것이다):
1. **티어 잔재 = 랭크 발산**(고침). 티어는 랭크·노드마다라 한 노드의 옛 파일이 그 랭크만 "이미 파킹된 대화"로 만들어 `_admit` 이 프리필 대신 복원을 골랐다.
   조치: 네 노드 `st-tier` 청소 + `Server._agree_on_parked`(개수·마지막 키·체크섬을 all_reduce_max 로 맞추고 다르면 네 랭크가 같은 메시지로 죽는다, D3; 캡처 앞에서도 한 번).
2. **NCCL all_reduce 가 든 캡처 디코드 그래프는 첫 replay 에서 안 돌아온다**(청소 뒤에도 2/2). 같은 레인을 이거로 돌리면 돈다 — 커널·NCCL 무죄, 그래프 replay 의 문제. 열린 항목(§23 보충에서 재확인).
3. **깨진 글 = `net.py` `O_NORM_EPS`**. 판정 사슬: (a) `max_tokens=1` 에서 이미 첫 토큰이 틀림("Twinkle, twinkle, little" → `,`, "import numpy as" → `s`, "One, two, three, four," → `chr`) → 프리필 경로;
   (b) 참조 레인표(`STK_lanes=reference`)로 45층을 돌려도 깨짐(`, and the, and, and`) → 커널 레인 무죄, 두 표가 공유하는 조합·가중치·캐시·문;
   (c) 랭크 파일 == 현재 `specs.py` 빌더(rank 0·3, top/L0/L3/L44 + L3 전문가, `probes/glm53_rankfile_audit.py`), 아레나(O_DIRECT·H2D) 경로 == CPU 경로(`probes/glm53_arena_bytes_check.py`) → 가중치 무죄;
   (d) **외부 참조**: 프로덕션 vLLM 모델 코드(오버레이)를 srv4 한 대·TP=1·앞 3층으로 띄워(로더에 층 범위 가드 한 줄) embed·각 층 attn/mlp·L0 내부(mhc_pre·in_proj·f_b·g_b·o_norm·o_proj) 를 훅으로 덤프, ST 프로브(`probes/glm53_first_token.py`, 참조 레인, LocalTP, 0~2층)와 대조 —
   **embed 완전 일치, L0 KDA 블록부터 per-token rel 1.9**(ST rms 0.0188 vs vLLM 0.0064);
   (e) CPU 재현(HF 텐서, ST 모듈 함수): x·투영·f_a·f_b·g_b 출력이 vLLM 과 4자리 일치 → 코어(conv·recurrence·o_norm) 안. KDA 코어 출력은 rms ~2e-4, **mean(x²) ~7e-10** 이라 `rmsnorm` 이 eps 에 지배된다:
   vLLM 은 `FusedRMSNormGated(head_dim, activation="sigmoid")` **클래스 기본값 eps=1e-5**(`third_party/flash_linear_attention/ops/kda.py:483`; 1e-6 은 맨 함수 `rms_norm_gated` 의 기본값), ST 는 1e-6 → 모든 KDA 블록 출력이 √10 = 3.16배(0층 실측 2.9배).
   `O_NORM_EPS=1e-5` 로 L0 블록 rel **1.94 → 0.008**. 플릿(서빙 레인·이거) 재부팅: 첫 토큰 " Paris"·" star"·" np"·" Thursday"·" five", 본문 `' Paris. In French, Paris is spelled "Paris", but…'`,
   `' 도쿄입니다. 대한민국의 수도는 서울이고, …'`, `' 7, 8, 9, 10, 11, 12, 13, 14,'`, chat(thinking off) `'대한민국의 수도는 서울입니다.'`. 자기일관성 판정이 전부 통과한 이유: 상수 하나를 두 레인표가 공유한다.
   **교훈(D14)**: 참조 레인은 커널의 참조이지 알고리즘의 참조가 아니다 — 알고리즘의 참조는 vLLM 스택이고, 층별 훅 대조(`scratch vllm_ref_hooks.py`, 이번에 만든 절단 로더 가드)가 그 판정기다.

**문**: thinking off 이면 템플릿이 `<think></think>` 로 블록을 닫으므로 생성 전부가 content — 문이 `</think>` 를 기다리며 답을 `reasoning_content` 에 넣던 것을 고침(렌더된 프롬프트가 reasoning-end 로 끝나면 content 모드).
**리베이스 뒤(main #561~#565: KDA 상태·후보 교환·그래프 검토·KDA 출력 노름 융합 커널·eps)**: 융합 노름 경로로 첫 토큰 " Paris"·" star" 정상, 그러나 3토큰 프롬프트("import numpy as")에서 네 랭크가 `moe_dispatch._get_direct_micro_kernel` 의 `kernel.__cache_key__()` TypeError 로 사망 — #558 의 direct-micro 디스크 캐시 래퍼가 GPU 에서 처음 도는 순간이었고 `__cache_key__` 는 프로퍼티다(`moe_w4a16_kernel` 은 맨 속성으로 읽음). 고침(호출 가능하면 호출, 아니면 속성).
**부수 수정**: 런처·슈퍼바이저의 자기 ssh(헤드 srv2 는 자기 키를 거부 → 로컬 셸), `facts.RANKS`·런처 기본값을 실재하는 표식본으로, `STK_*` 노브 컨테이너 전달, `qualify_eager_decode(warmup=)`.
**뿌리 2 의 답(06:28, main #567 병합 뒤 재부팅)**: codex 가 원인을 찾았다 — 캡처 워밍업·첫 프리필이 MoE 워크스페이스 캐시를 키우며 **그래프에 기록된 작은 할당을 해제**해 replay 가 해제된 주소를 읽었다; #567 이 소유자를 그래프 수명 동안 붙든다. 이 브랜치 + #567 로 그래프 부팅(capture 92.9 s): `The capital of France is` → ` Paris. In French, …`, 3토큰·7토큰 첫 토큰 정상, **256 토큰 스트림 29.7 tok/s(TTFT 4.78 s, 수용 173/525)** — 이거 10.7 tok/s 의 2.8배, 프로덕션 C=1(16.6 step/s × ~2.2) 의 ~80%. 표본 하나, 띠 아님(D14).
**그래프 경로 첫 onepass(ST-GRAPH-45, 06:31, 123 s)**: 2K 2/3 · 32K 0/3 · 128K 0/3 = **2/9**, 한국어 깨진 응답 1/5, 디코드 창 없음 — 그런데 표의 프롬프트 토큰이 세 문맥 모두 **2048**, 32K 프리필이 1.18 s. 원인: `tokenizer.json` 이 `truncation {max_length 2048, direction Right}` 를 싣고 있고 문의 `tokenizers.Tokenizer.from_file` 이 그 규칙을 그대로 적용해 **모든 프롬프트를 앞 2048 토큰으로 자른다**(질문이 끝에 있으니 채움글 얘기만: 32K 답이 사원수·해밀턴). transformers 의 AutoTokenizer(vLLM 의 토크나이즈 경로)는 이 설정을 무시한다. 직접 확인: 2,684자·10,583자·30,085자 프롬프트 모두 `prompt_tokens` 2048, 끝에 심은 코드 못 찾음. 조치: `boot.tokenizer()` 가 `no_truncation()`·`no_padding()`; `tests/test_engine_tokenizer.py`.
**잘림 수정 뒤(06:37 부팅, 그래프 경로)**: 바늘 찾기 2,684자/10,583자/30,085자 → prompt_tokens 2,220/8,545/24,251, 코드 셋 다 정답(BLUE-7742·RED-3319·GREEN-5561; 1·1.2·3 청크). **onepass ST-GRAPH-45b(173 s): 9/9 정답 · 한국어 깨짐 0/5 · 프리필 2K 2,028 tok/s(웜, TTFT 1.05 s) · 32K 2,117 tok/s(TTFT 15.4 s) · 128K 1,990 tok/s(TTFT 64.6 s)**; 디코드 창은 안 잡힘(이 워크로드의 답이 짧아 2 s 창 없음 — 고정 길이 다리 `--fixed-decode-tokens` 로 따로). 프로덕션 기준선(23K 2,696 tok/s·TTFT 8.55 s)의 프리필 ~79%. 한 부팅 한 표본 — 띠가 아니다(D14).
**디코드(고정 길이 다리, ST-GRAPH-45c, 297 s, 같은 부팅)**: `--fixed-decode-tokens 2048 --fixed-decode-reps 3` → C=1 **32.8 / 32.3 / 35.4 tok/s**(2048/2048 토큰씩), 품질 18/18, 한국어 0/8. 프로덕션 C=1 기준선 16.6 step/s × raw acc 18~25%(≈2.0~2.2 tok/step) ≈ 33~36 tok/s 와 같은 급. onepass 의 디코드 창(step/s)은 ST 문에 `vllm:iteration_tokens_total_count` 가 없어 0개였다("INVALID: too few fixed decode windows") — 문에 그 행(= runner.steps)을 추가, 다음 부팅부터 창이 잡힌다.
**남은 것**: (1) 부팅 반복으로 띠(프리필·디코드 창) 세우기; (2) 이거 디코드 10.7 tok/s 는 프로덕션(16.6 step/s × ~2.2)의 1/7 — 그래프가 돌아야 전환 논의 가능; (3) onepass 품질 게이트(9/9·한국어) 미측정; (4) KV·max_seqs 결정(미할당 34 GiB).

**A/B 전체 구현(09:05, PR #575)**: vLLM 대비 미구현 조사(A 계약·B 엔진)를 문(`base/serve.py`)·어댑터·샘플러에 다 넣었다 — A1 샘플링 옵션(top_k/top_p/min_p/반복·빈도·존재 페널티/logit_bias/seed/stop ids/min_tokens/n≤4/best_of/echo; 옵션 있는 행만 base/sampler 의 리치 경로, 드래프터는 `propose_sampled` + 기각 샘플링으로 유지), A2 `response_format`(xgrammar, 드래프트 K=5 를 롤백으로 걷는 마스크), A3 logprobs/top_logprobs, A4 tools/tool_choice + `<tool_call>` 스트리밍(부분 태그 보류), A5 reasoning_effort/enable_thinking, A6 레거시 `/v1/completions`·`/tokenize`·`/detokenize`(엔진 방언은 `/v1/engine/completions` 로), B1 이어가기(히스토리가 진접두면 `extend`), B2 generation_config 기본값·KV 24 GiB, B4 웜업 사다리(64…4096 × 폭 1…4), B6 `vllm:iteration_tokens_total_count`. B3(비동기 스케줄링)·B5(선점) 는 보류·설계대로. CPU 스위트 305 OK; 플릿 검증은 #570 슈퍼바이저가 수동 부팅을 죽여 창 대기.
**A7 그림(09:25, 같은 PR)**: 운영자 정정 — 프로덕션 vLLM 은 이미지 4·영상 1 을 서빙한다(PR #431); "텍스트 전용" 은 틀린 옛 요약. 프로덕션 이미지에서 `glm5next/nvidia/multimodal.py`(타워)와 `transformers_utils/processors/glm5next.py`(프로세서)를 읽어 vLLM 없이 다시 썼다(`profiles/glm53/vision.py`, 700줄): 24블록 ViT(qkv 편향, q/k RMSNorm eps 1e-5, 2-D neox rope 32/64, 클램프 SwiGLU 10) → post norm → 2×2 conv 다운샘플 → 머저(proj·LayerNorm·GELU·SwiGLU 10240) 4096폭; 프로세서는 토큰 예산 → 픽셀 예산(이미지 16~8000 토큰, 영상 16~30000 = 프로덕션의 `_MAX_VIDEO_TOKENS` 캡), 28 정렬 위로 올림·예산 초과면 이진 탐색, pad 모드(비율 유지·우하 0 패딩·축소만), bicubic antialias, CLIP 평균/표준편차를 rescale 과 융합, 시간 패치 2(정지 이미지는 같은 프레임 둘), Qwen-VL 패치 순서; 영상은 프로덕션 로더처럼 32 프레임 균등 → 전부 실렸으면 GLM 샘플러(`fps_interval` 2, 2048 프레임 상한, 짝수화) → 프레임 쌍마다 `<|begin_of_image|>` + 토큰 + `<|end_of_image|>` + "N.N seconds"; EXIF 회전 정규화·투명은 흰 바탕 합성, 178,956,970 픽셀 초과 거부. **판정**: 합성 이미지(407×613)·합성 영상(8 프레임)의 `pixel_values` 가 프로덕션 프로세서와 sha256 비트 일치, 캔버스 크기 9종·영상 5종·프레임 인덱스 7종 일치(`tests/fixtures/glm53_vision_reference.json`, 프로덕션 이미지에서 뽑음). 배관: 문이 `image_url`/`video_url` 파트를 받아(data:·http(s), 이미지 5 s·영상 30 s, 64/512 MiB 상한) 캔버스(uint8)를 만들고 자리표시자를 늘려 `media` 로 넘긴다 → 도착 브로드캐스트에 실려 네 랭크 → 어댑터가 그 위치에 닿는 첫 프리필 청크에서 타워를 돌려(`net.Step.patches` 로 임베딩 행 교체) 청크 창으로 자르고 지난 뒤 버린다; 네 랭크의 행 합을 max-reduce 로 대조(D3). 가중치는 `vision.safetensors`(347 텐서 1.05 GiB, `preshard.py --vision`, 1.4 s; RankLoader 1.0 s) — 4노드 배포 완료(체크섬 609aaf3d…), 플릿 부팅은 없으면 죽는다; 부팅이 최대 이미지(32,000 패치)·최대 영상(16쌍)을 한 번 인코딩해 SDPA 백엔드·워크스페이스를 증명한다(수학 백엔드 배제: 최대 이미지에서 32 GiB). 같은 자리표시자에 다른 그림은 다른 프롬프트: 이어가기 판정과 prefix 캐시 체인에 그림 digest 를 섞었다. 실제 가중치로 CPU 인코딩 448×448 → 256×4096 유한(rms 0.019). CPU 스위트 326 OK; **vLLM 타워와의 수치 대조와 플릿 검증은 운영자 지시로 생략**(09:30).
**블록 단위 prefix + 비동기 스케줄링(10:20, 같은 PR; 운영자 "블록단위 프리픽스, 비동기 스케줄링 구현")**: (1) prefix 캐시의 단위를 프리필 청크 6,912 에서 풀의 블록 2,304 로(프로덕션 APC 와 같음). 청크 안 경계의 상태는 링에 남지 않으므로 러너가 스텝 전에 `marks={위치: 스냅샷}` 로 이름 붙이고 스텝이 받는다: `net._kda` 가 KDA 되풀이를 경계에서 끊어(조각마다 `kda_chunk`, 상태 이어 넘김; 경계가 청크 시작에서 64 정렬이라 커널 청크가 거기서 다시 시작 → 자르지 않은 계산과 같은 순서) 조각 끝 상태·conv 입력 3개를 `caches.mark_kda` 로 스냅샷에, 드래프터 링은 스텝 전 링 복사 + 경계 전 위치 관측(`mark_draft`); 스텝 끝 경계는 종전대로 `checkpoint`. 스냅샷 8 → 24(경계당 77 MiB, 1.8 GiB/랭크). CPU 판정: 참조 레인 tiny facts 로 자른 것과 안 자른 것의 출력·끝 상태 일치(atol 2e-2 bf16), 경계 상태 = 접두만 돌린 상태, conv 탭 = 경계 전 입력(`tests/test_engine_kda_marks.py`); 러너: 19 토큰 프롬프트가 4·8·12·16 에 경계, 13 토큰 공유 프롬프트가 12 를 재사용(전엔 8). (2) 디코드가 호스트보다 앞서 돈다(`pipeline.py`): 장치에 타깃 그래프 → 샘플러 → `commit_batch`(수용·한도·끝 토큰 클립) → 마스크 관측(`observe_masked`, 유효 개수 장치 스칼라) → 제안(`propose_from`/`propose_sampled_tensor`) → 다음 ids 를 남기고 결과만 핀 버퍼 + CUDA 이벤트로; 러너는 `inflight` 깊이 2 로 띄우고 `resolve_oldest` 로 한 스텝 늦게 적용, 프리필·동기 디코드·취소·정지 전엔 비운다(`drain`). 끝난 행은 장치가 `alive=0` 으로 슬롯을 null 로 돌려 유령 스텝이 링·꼬리에 아무것도 못 쓰고 KV 는 ctx 뒤 자리라 무해; 예약은 `horizon = ctx + 6 × (1 + 앞선 스텝 수)`. 온도/top_p 행은 장치 기각 샘플링 `speculative_pick_batch`(그리디 행은 원-핫 드래프트 분포로 같은 식), 나머지 옵션 행은 동기. 루프의 객체 브로드캐스트·투표는 gloo 제어 그룹으로(NCCL 그룹의 `broadcast_object_list` 는 스텝 커널 뒤에 줄 서고 읽기에 장치 동기화). CPU 판정: 러너 파이프라인(가짜 모델: 깊이 2, 유령 무시, 프리필/동기/취소 전 비움, 지평선), 기각 샘플러·커밋 클립 성질(같은 분포면 전부 수용, 분리 지지면 첫 기각·복구, 시드 결정성, 한도·끝 토큰), 분포 배치·readback 부기 — 스위트 354 OK. **GPU 경로(그래프 device fill·마스크 관측 그래프·이벤트 readback)와 실측은 미검증**(운영자 지시로 생략) — 첫 플릿 창의 할 일: 부팅 qualify + 원패스 디코드 창(동기 대비 스텝 간 거품 제거분).
**prefix 캐시를 더 관용적으로(11:10, PR #585; 운영자 "네개 전부")**: 토큰이 다르면 못 쓴다는 한계(vLLM 도 같음) 안에서 넷을 바꿨다. (1) 단위 2,304 → **768**(`facts.BLOCK`; 청크 정렬 2,304 는 `facts.CHUNK_ALIGN` 로 분리, 6,912 법칙 그대로 — 768 = 12×64 로 KDA 커널 청크 정렬, kpool 4 의 배수, 청크당 9 블록): 3,000 토큰 공유가 2,304 → 2,304 지만 3,100 이면 3,072. 페이지 레이아웃·인덱서 커널은 블록 크기를 인자로 받으므로 커널 변경 없음; 블록 테이블 3배(수십 KB). (2) 스냅샷 24 → **96**(7.2 GiB/랭크, 예산 여유 ~40 GiB 안) + 퇴거 정책: 채택된 적 없는 경계부터(`_victim`), 그 다음 LRU — 128K 프롬프트 하나(170 경계)가 공유 시스템 프롬프트를 밀어내지 못한다. (3) **생성 중 넘은 경계**: 동기 경로는 스텝 직후 링에서 `checkpoint`(rec 6칸·conv 8칸이 아직 그 자리를 들고 있다), 앞서 도는 경로는 다음 스텝이 링을 덮으므로 장치 스테이지(`caches.stage_boundaries`, Triton `_stage_rec`/`_stage_conv`: 넘은 행만 KDA 상태 1개 + conv 탭 복사 — 행당 스텝당 ≤36 MB, 46 ms 스텝의 ~0.3%; 슬롯당 스테이지 35.7 MB 는 아레나에 선언)에 놓고 `resolve` 때 `checkpoint_from_stage`; 러너는 `history()`·`media_marks()` 로 체인을 다시 계산해 이어간 턴의 경계까지 마크한다. 이득: 티어에서 잊힌 대화의 재전송이 답변 블록까지 재사용. (4) 이어가기 관용: 히스토리 끝이 끝 토큰이고 그 앞까지 일치하면 이어간다(`extend(drop_unfed=True)`: 그 토큰은 샘플됐지만 먹인 적이 없어 상태가 정확히 그 앞) — 템플릿 확인(이미지 안): thinking off 재전송은 답 뒤에 바로 `<|user|>` 라 모델이 `<|user|>` 로 끝냈으면 원래 맞고 `<|endoftext|>` 로 끝냈으면 이 규칙으로 맞는다; **thinking on 은 이전 턴 추론을 템플릿이 되그리지 않아(`<think></think>`) 이어가기 불가 — 그 앞 블록까지는 prefix 캐시가 산다**(reasoning_content 를 되보내면 일치). CPU 판정: 스테이지 = 링 `checkpoint` 와 비트 일치·안 넘은 스텝은 손 안 댐(`test_engine_boundary_stage.py`), 생성 경계 8·12 채취와 재전송 대화의 8 재사용, 채택된 경계 생존, 끝 토큰 없는 재전송 이어가기 — 스위트 **390 OK**. GPU(스테이지 커널·768 블록 부팅) 미검증.
**prefix 캐시 더(11:50, 운영자 "abcf")**: (A) **NVMe prefix 티어** — 러너가 스텝마다 `maintain_prefix`: 빈 스냅샷이 8개 아래면 퇴거 순서의 잎 경계(다른 경계가 잇지 않는 것; 프리필 중인 프롬프트의 경계는 다 들어올 때까지 제외)를 대화 티어 옆 `prefix/` 디렉터리에 미리 쓴다(블록 + 스냅샷 바이트 + `{hash, tokens}` 기록; `TieredKV`/`NvmeTier` 그대로, 키 = 해시 56비트, 스테이징 32 MiB 별도). 쓰인 경계는 `_victim` 에서 같은 등급 안 먼저 나가고(잃는 게 없다) `tier_keys` 에 남는다; 새 프롬프트가 메모리엔 없고 티어에 있는 경계를 지나면 rank 0 이 도착에 후보를 실어 보내고 `_admit` 이 네 랭크 투표(`have`) 뒤 요청 행에 `restore_begin`(블록 예약 + 빈 스냅샷 + 티어 스레드 읽기) → `_settle` 투표 → `submit(prepared=(tokens, snap))` 로 그 뒤부터 프리필; 한 랭크라도 못 읽으면 되돌리고 보통 입장. 부팅 때 티어 키를 읽어 `_agree_on_parked` 로 네 랭크 일치 확인. (B) **동시 중복** — `runner.shared_ahead`: 프리필 중인 행의 체인이 같은 경계를 앞으로 만들면 둘째 요청은 뒤 요청을 먼저 보내고 기다렸다가(`dedup_waits`) 경계가 캐시되면 맨 앞으로(`_reorder_waiting`) 채택. (C) `POST /v1/prefix/warm`(messages/prompt/ids, 1 토큰 생성 뒤 버림, `pin`)·`/v1/prefix/unpin`; 핀·해제는 rank 0 의 제어 큐가 도착 브로드캐스트에 실려 네 랭크에 같은 반복에 적용(캐시 상태 동일 유지); `probes/st_prefix_warm.py`. (F) `/metrics` 7 계열. CPU 판정: 잎 스필 → 퇴거 → 티어 복원(스냅샷 바이트 왕복 일치)·`submit(prepared)` → 접두 12 재사용; `shared_ahead` 가 진행 중 프리필의 앞 경계를 보고 캐시되면 None; 문: 같은 프롬프트 둘 동시 → 둘째 대기 1회·16 토큰 재사용, warm/pin/unpin, 티어 복원 1회 — 스위트 통과(아래). GPU·플릿 미검증(지시).
**호스트 경로 최적화(12:50, 리뷰 후속 "비슷하게 더", PR #598)**: 스텝마다·요청마다 반복되던 것 일곱. (1) 비동기 스텝 안 `stage_boundaries` 가 오프셋 표 4개를 매 스텝 `torch.tensor(..., device=cuda)` 로 만들었다 — 페이지 가능 H2D 복사는 스트림을 기다리므로 **앞서 띄운 스텝을 매번 기다리게 해 겹침을 없애던** 결함; 한 번 만들어 둔다. 파이프라인 `_shrink` 의 인덱스도 핀 + non_blocking, 호스트 스텝의 자리표시자 ids 는 n 별 캐시. (2) `caches.prepare` 의 블록 테이블 업로드가 페이지 가능 복사(행이 블록을 넘을 때마다 스트림 대기) → 핀 링 16칸 + 이벤트. (3) 문의 스트리밍이 매 스텝 활성 행마다 생성 전체를 슬라이스 복사(`generated()`) — 같은 것을 #597 이 먼저 고쳐(`generated_count`/`generated_since`) 리베이스에서 그쪽을 취함. (4) 이어가기 스캔이 파킹된 대화마다 티어의 JSON 기록을 매 요청 읽고 O(m) 리스트 비교 → 기록 메모이즈 + 마지막 토큰 한 개 필터. (5) sha1 체인을 요청당 4~5회, 대기 중 중복 요청은 스텝마다 재계산 → rank 0 이 한 번 계산해 도착에 실어 보내고(128K 프롬프트 3.4 KB) 입장·중복 대기·티어 후보·`submit` 이 그것을 읽음(`peek_chain`/`lookup_chain`/`tier_lookup_chain`). (6) 생성 경계마다 히스토리 전체 복사 + 체인 재계산 → `extend_chain` 이 마지막 경계 뒤 블록만 해시(`history_from`). (7) `maintain_prefix` 의 후보 정렬·growing 집합을 캐시 `version` 이 바뀌었을 때만. 스위트 443 OK. 발견 방식은 같다: 스텝 루프·입장 루프 안에서 크기가 프롬프트·생성·파킹 수에 비례하는 것을 찾는다.
**호스트 경로 2차(13:20, PR #601)**: (1) 동기 디코드(옵션 있는 행·이거 레인)의 `Step` ids 가 `torch.tensor(flat, device=cuda)` — 그래프가 어차피 정적 입력으로 복사하므로 핀 호스트 버퍼에 두면 그 복사가 비동기가 된다(페이지 가능 텐서는 만들 때 스트림 전체를 기다림). (2) 이어가기 스캔이 유휴 행마다 `history()` 로 128K 토큰을 복사한 뒤 한 토큰 필터를 적용 → `history_ref`(복사 없음, 비교만). (3) `adapter.validate` 는 입장 때 네 랭크가 루프 안에서 도는데 Python 루프가 120K 토큰에 3.8 ms; `array('q')`+min/max 는 오히려 4.7 ms(요소마다 PyLong 생성), numpy 변환+min/max 1.3 ms — dtype 을 강제하지 않아 float·문자열은 정수 배열이 안 돼 거부(잘림 없음). **측정하고 골랐다** — 첫 시도(array)는 더 느렸다. 스위트 450 OK. 남은 것은 GPU 쪽이라 창이 필요: (a) 프리필 마크가 KDA 되풀이를 청크당 9조각으로 끊어 KDA 커널 런치 ~9배(+2.2k 런치/스텝 ≈ 5~8% 추정) — 커널의 64-청크 상태 `h` 가 bf16 이라 스냅샷엔 못 쓰고, 요청한 청크의 fp32 상태를 옆 출력으로 내는 Triton 수정이 답; (b) 비동기 파이프라인의 행별 드래프터 제안 그래프(n 회 순차 재생) → n 행 배치 제안 그래프; (c) 파이프라인 꼬리(기각 샘플링·커밋·마스크 관측·제안, ~40 런치)의 그래프 캡처; (d) `distribution_batch` 의 정렬을 top_p<1 행에만.

**GPU 판정 3차(14:30, 운영자 "필요하면 측정해", PR #615)**: 프로덕션이 네 GPU 를 96% 쓰는 옆에서 작은 판정기만(직전 MemAvailable 확인·한 번에 하나·`--memory 10g`·timeout; 절대값은 잡음이라 **상대값만**). (1) **KDA 컷 대신 커널 옆출력**: 마크가 프리필 청크의 KDA 되풀이를 끊는 비용을 서빙 커널로 쟀다 — 층당 T=6,912 uncut 8.64 ms, 3×2,304 9.86 ms(+14%), 9×768 17.60 ms(**+104%**; 34층이면 스텝당 ~300 ms). 컷은 정확하다(출력·상태 max|d| 0 — 커널은 64-청크 경계에서 조각 합성이 비트 동일) 그러나 비싸므로 컷을 버리고 Triton `chunk_gated_delta_rule_fwd_h` 에 **요청한 청크 앞 상태를 fp32 로 내는 옆출력**(`states_at`/`N_AT`, 청크 상태 `h` 는 bf16 그대로, 본 계산 무변경)을 달았다: 옆출력 8개 있는 층 6.98 ms vs 없는 층 6.99 ms(**공짜**; 부하가 다른 두 번째 실행은 20.61 vs 19.99 — 프로덕션 옆 잡음 폭이 이만큼이라 차이는 잡음 안), 옆출력 == 잘라 돌린 prefix 의 최종 상태 **비트 동일**(8 마크 전부, 유한), 옆출력 유무에 출력·최종 상태 비트 동일, 같은 입력 두 번 비트 동일. 커널 vs torch 되풀이(fp32 토큰별)는 상태 max|d| 3.2e-3(상태 최대 0.66; `h` 가 bf16 인 커널의 원래 오차, 옆출력도 prefix 실행도 같은 값). 마크는 이제 64 토큰 커널 청크 위에만(`lanes.kda_chunk_tokens`; 768·2,304 는 배수) — 아니면 ValueError. 기준 레인은 조각을 이어 돌려 같은 계약을 낸다. **판정기 함정 하나(정직하게)**: 첫 판정은 "옆출력이 잘라 돌린 상태와 0.32 다르다, 컷이 상태를 0.3 옮긴다" 였는데 커널 오류가 아니라 **판정기 오류** — `dt_bias` 를 [D]=128 로 만들었고 커널은 [H·D] 로 읽으니 나머지 15 헤드 몫이 범위 밖 읽기(이웃 텐서 내용 → 실행마다 다른 게이트)였다. 형상을 고치니 전부 0. 교훈: 커널 판정기는 입력 형상을 프로필 계약(`lanes.py` 주석)에서 베껴 올 것. (2) `distribution_batch` 는 **nucleus(top_p<1) 행만 정렬**: 배치 분포가 n=4(×6 위치) 에서 정렬 있으면 4.58 ms, 없으면 0.70 ms(정렬 [24, 154,880] 만 2.91 ms; n=1 은 1.30 vs 0.23) — 그리디·온도만 행은 정렬 없음, 행 위치는 배치를 짤 때 장치 텐서로 한 번(스텝마다 호스트 리스트를 복사하면 페이지 가능 H2D 가 진행 중 스텝을 기다림). top-k 2,048 뒤 정렬(0.69 ms) 은 전체 정렬과 1.8e-2 차이라 채택 안 함. 기각 샘플링 pick 1.1 ms + commit 0.6 ms 는 스텝당 그대로. (3) 같은 창에서 판정한 것: 경계 스테이지 Triton 커널 == 링 체크포인트 비트 동일, 마스크 관측 의미(관측 안 한 행의 드래프터 셀 불변), 비전 타워 SM121 qualify 통과(fused SDPA 만; 최대 이미지 3.7 s·영상 9.1 s, 최고 4.24 GiB — 프로덕션 옆이라 절대값은 참고만). GPU 후보로 남은 것: 배치 제안 그래프, 파이프라인 꼬리 캡처, 비동기·768 블록 부팅의 플릿 검증(창 필요).

**GPU 판정 4차 — 드래프터를 행 배치로(15:40, 운영자 "추가 조사", PR #627)**: 디코드 스텝의 호스트·장치 경로를 다시 훑다가 가장 큰 것을 찾았다. **파이프라인이 드래프터(DFlash2, 5층 bf16 2.18 GiB, `DRAFT_TP=1` 이라 랭크마다 전부)를 행마다 따로 돌렸다** — 제안 그래프 n 회 재생, 관측 그래프 n 회 재생, 그리고 그래프마다 그 행의 40 MiB 링을 `index_select` 로 복사해 들어가고 `index_copy_` 로 돌려놓았다(행당 스텝당 링 복사 셋 = 120 MiB). 실측(프로덕션 96% GPU 옆이라 절대값은 잡음, 상대값만; 실물 가중치, 가짜 타깃 embed/head): 행 하나의 제안 그래프 재생 **40.5 ms**, 관측 그래프 4.1 ms + 링 복사 2.8/3.1 ms; 드래프터 GEMM 47개(2.03 GiB)를 B=6 행으로 한 번 읽으면 20.8 ms, **B=48(8행)이면 26.5 ms** — 가중치 읽기가 바닥이라 행을 묶으면 거의 공짜. 어텐션은 층당 einsum 경로 4.3 ms(2048 창의 K/V 를 fp32 로 펼치고 `repeat_interleave`) vs 융합 SDPA 3.2 ms. 프로덕션 vLLM 의 DFlash 는 행을 묶어 한 번(꼬리 7.5~8.5 ms/스텝, W4 GEMM) 돌았으니 이건 vLLM 대비 미흡 경로였다. **고친 것**: (1) `drafter.observe_rows / block_rows / propose_rows` — 모든 행을 한 번에: GEMM 은 [n·t] 행으로, 관측은 `index_put` 으로 필드의 셀에 **제자리 쓰기**(링 복사 없음), 어텐션은 **필드 전체를 한 번의 융합 SDPA**(cuDNN/efficient, D3 라 math 폴백 없음)로 — 행의 질의를 슬롯 자리에 흩고, 링은 **저장 순서 그대로** 읽고(키가 자기 위치로 rope 돼 있어 순서가 무의미), 창보다 짧은 컨텍스트는 키 마스크로, kv 헤드를 나눠 쓰는 헤드들은 그 kv 헤드에 대한 질의 행으로 펼쳐 헤드 복제도 없음. 블록 자신의 키는 링 뒤의 **여유 셀**(`ring_cells = window + block`, 링 +0.4%)에 두어 창+블록이 한 연속 구간이 된다. (2) `DrafterDecodeGraphs` 에 폭 1..max_seqs 의 `rows_masked / rows_propose / rows_sampled` 그래프(표본 걷기는 엔진 생성기를 그래프에 등록, 캡처 뒤 상태 복원); 파이프라인은 스텝마다 관측 한 번·제안 한 번. 온도 0 행은 표본 배치 안에서도 argmax(원-핫 분포). **판정(같은 창, 상대값)**: 제안 n=1 **17.1 vs 23.3 ms**, n=2 **17.1 vs 46.9**, n=4 **18.8 vs 101.2**(행별 ×4); 관측 1.9/1.9/4.1 vs 3.2/6.2/11.9; 표본 걷기 n=4 그래프 18.9 ms. n=4 스텝의 드래프터 몫 ~113 → ~23 ms. **정밀도(정직하게)**: 배치 경로는 행별 경로와 비트 동일하지 않다 — 층당 어텐션 차이는 bf16 1 ulp(efficient vs fp32 참조 max|d| 1.2e-4/3.5e-2) 인데 블록 5층을 지나면 어느 두 bf16 실현 사이에서도(행별 SDPA vs 행별 einsum 조차) hidden 의 중앙 상대차 4~7%, 최대 |d|/max|ref| 2~8% 가 되고, 임의 가중치·임의 링 판정에서 4 행 중 2 행의 드래프트가 바뀌었다. 같은 입력 두 번은 비트 동일(결정적)이고 네 랭크가 같은 그래프를 돌리니 랭크 간 동일. 드래프트는 검증이 지키므로 수용률 말고는 영향이 없고, 프로덕션 vLLM 의 DFlash(플래시 어텐션·W4)는 이보다 더 먼 실현이었다. 관측 쓰기의 차이(값 64~128 에서 0.25~0.5)도 M 이 다른 GEMM 의 bf16 1 ulp. **남은 레버**: 이제 스텝당 드래프터 바닥은 가중치 2.03 GiB 읽기(비경합 추정 ~12 ms) — W4/NVFP4 로 4배 줄이는 것이 다음이며 M=6..24 용 밀집 W4 GEMM 커널이 엔진에 없어 창·커널 작업이 필요. 티어 주의: 스냅샷 바이트가 +0.16 MiB 라 이전 부팅의 prefix/대화 티어 기록은 `promote` 의 크기 검사에서 거부돼 한 번 실패하고 잊힌다(조용한 오염 없음). **플릿 미검증**(창 필요): 새 그래프 3세트 × 폭 4 의 캡처, 웜업 사다리, null 슬롯의 NaN 격리.

**GPU 판정 6차 — GPTQ: 커널·패커는 철회, 자가 교정을 기본으로(19:30~21:00, 운영자 "gptq 패킹 진행" → "노브가 아니고 아예 기본으로 해버려, w4 랑 gptq 전부"; 노브 판은 PR #644 로 머지, 기본화는 PR #650)**: GPTQ 를 붙이려 main 을 다시 보니 **codex 의 PR #591(13:48 머지, "네이티브 TP4 채택")이 W4 레인을 통째로 먼저 가져와 있었다** — `engine/kernels/dense/`: 33차의 플릿 검증된 MK W4A8 디코드 커널(e2m1 × e4m3 스케일, CUDA 확장)·`pack_w4(hessian=)`(같은 캠페인의 `_w4_gptq_codes`, fp64 대칭화·댐핑 사다리)·`PackStore`(`/cache/mkcalib/rank<r>/<이름>.pt` 의 {H, ntok, name} 을 읽어 GPTQ, 없으면 RTN 으로 세어 둠)·`DenseLinear`(≤32 행 W4A8, FP8 프리필, NVFP4); 드래프터는 TP4 로 **샤딩**(qkv·gate_up 융합, o/down 은 K 분할 + all_reduce, 랭크당 링에 kv 헤드 둘)돼 그 레인으로 서빙되며 부팅 증명이 `drafter_w4 == 31` 을 요구한다. 그러니 **W4 는 이미 기본**이다. 내가 5차에 쓴 Triton W4A16 커널(int4 g32)과 이번에 쓴 `pack_w4_gptq`·`preshard --drafter-w4` 는 **같은 자리의 두 번째 구현**이라 철회했다(PR #636 닫음; D3·"서빙 형태는 하나"). 실수의 원인은 [[stkernel-parallel-sessions]] 규칙 그대로 — 커널을 쓰기 전에 origin/main 을 다시 보지 않았다(#591 은 내 분기점 29 분 뒤에 머지됐다). **남은 빈자리는 교정 데이터였다**: 스토어가 읽는 mkcalib 블롭은 물러난 vLLM 스택의 `MK_CALIB` 덤프인데, 이 노드(srv4)의 /cache 에는 없고(→ 지금 네이티브 부팅은 드래프터를 RTN 으로 팩; #591 의 README 는 자기 환경의 옛 캐시로 GPTQ 였다), 있어도 샤딩된 o_proj/down 의 K 와 맞지 않아 `incompatible calibration` 으로 부팅이 죽는다. 첫 판은 노브(`STK_drafter_calib`)로 켜는 교정 부팅이었는데 운영자가 "노브가 아니고 아예 기본으로" 라 해서 **자가 교정을 기본 동작**으로 다시 썼다(`engine/kernels/dense/calibration.py`): (1) 부팅이 아레나를 잡기 전에 `PackStore.missing_calibration(name, cols)` 로 **스토어에 교정이 없는 팩 타일**(K>4096 은 4,096 열 타일)을 드래프터(`drafter.dense_shapes`)·타깃(`net.dense_weight_names` + spec 형상) 순으로 모으고, 예산 2 GiB/랭크 안에서 아레나에 XᵀX 버퍼를 깎는다(넘치는 층은 다음 부팅으로 미룸 — 부팅마다 조금씩 채워져 수렴). 맞지 않는 옛 블롭은 파일명을 대며 죽인다(D3: 조용한 재교정 없음). (2) `DenseLinear` 에 `observer` 와 `rows_ok` 를 달아 **모든 호출자**(타깃·드래프터)의 입력이 한 자리에서 합산된다 — 캡처된 그래프 안 `addmm_`, 기여마다 장치 스칼라 `armed`(웜업·캡처가 쓰레기를 흘리므로 문이 열릴 때 1)와 행 마스크(파이프라인 유령 행 `alive` → 제안 그래프 입력, 관측의 `valid` 너머)를 곱하고, 마스크 없이 ≤32 행이 올 수 있는 층(타깃의 디코드: null 슬롯 유령 행)은 **프리필 행만**(33차 덤프도 프리필 행) 합산. 모든 랭크가 자기 것(샤딩된 투영은 랭크마다 다른 열). (3) 파일링은 **스스로**: 스텝 루프의 `housekeeping` 훅(base/serve 가 256 스텝마다 부름, 장치 읽기 한 번)이 모든 블롭이 32,768 행을 넘으면 `<루트>/mkcalib/rank<r>/…pt` 로 쓰고, 종료 때도(4,096 행 미만이면 굶은 헤시안이라 안 씀), `POST /v1/engine/calibration` 으로도. 다음 부팅의 스토어가 그대로 GPTQ 팩을 짓는다 — 노브도 프리샤드도 없다. `/metrics` 의 `st:lane_info{packs="gptq N rtn M", calibration="collecting|filed|complete …"}` 가 무장≠서빙을 증명. **판정(GPU, 프로덕션 옆)**: 관측기의 블롭을 실제 `PackStore` 가 읽어 `{'built': 1, 'gptq': 1}`(K=4096, 5.0 s), 저장 뒤 `missing_calibration` 은 빈 목록; 상관 입력(랭크 64 혼합, N=1024)에서 같은 분포의 새 입력 출력 상대 오차 **RTN 0.082 → GPTQ 0.009**(main 의 e2m1 패커; 저랭크 합성 입력이라 상한, 실 활성은 33차 기준 −69~−74% 가 기대치); 64 스텝 × 24 행의 합산 94 ms(스텝당 1.5 ms, K=4096 한 타일 — 랭크당 ~35 타일이면 교정 부팅의 스텝은 수 ms 느리고, 파일링 뒤 재부팅하면 사라진다). CPU 판정: arm 전엔 0, 마스크된 행만, 작은 호출은 허용된 층만, 넓은 입력은 타일별, 예산 초과 층은 유예, 블롭 형식·스토어 왕복, 맞지 않는 블롭 거부, 드래프터 배치 경로가 넘기는 마스크, 노브 목록은 원래대로 둘. **절차(기본)**: 재부팅 두 번이면 끝 — 첫 부팅이 트래픽 30K 토큰 안팎에서 스스로 파일링(로그 "calibration: rank r filed …"), 두 번째 부팅의 `store.stats` 가 gptq 로 채워진다; 그 뒤 tok/s·raw acc 브래킷.

**GPU 판정 7차 — GPTQ 를 더 넓게: act-order·전체 K·NVFP4 프리필 레인(21:30~23:00, 운영자 "gptq 더 여러곳에 적용 가능한가" → "최선의 길로 해", PR #659)**: 양자화되는 가중치를 전부 놓고 보니 GPTQ 가 닿는 자리는 (1) 밀집 투영의 디코드 팩(W4A8, 이미 GPTQ), (2) 같은 가중치의 프리필 레인 FP8·NVFP4(RTN), (3) MoE 전문가(체크포인트가 RTN NVFP4, bf16 원본 없음), (4) 헤드(FP8). 최선은 **패커 개선(모든 팩에 동시에) + NVFP4 레인 승계**; FP8 레인은 GPTQ 해(e2m1 격자)를 fp8 로 재양자화하면 보정된 8% 위에 보정 안 된 3% 가 얹혀 bf16→fp8 RTN 보다 나빠지므로 **제외**(같은 논리로 GPTQ 해는 fp4 격자에서만 승계 가능), 전문가는 원본이 없어 제외. **(A) act-order + 정적 그룹**(`_w4_gptq_codes(act_order=)`, 기본 켬): 열을 헤시안 대각 내림차순으로(가장 중요한 입력부터 반올림, 그 오차를 나머지가 흡수), 그룹 스케일은 RTN 패커의 것을 걷기 전에 고정(커널의 16-그룹은 그대로). 실물 드래프터 가중치 셋 × 합성 헤시안 둘(랭크 64 상관; + 33차의 "이상치 채널 20배"): 이상치 없으면 plain 과 같고(0.010 vs 0.010, RTN 대비 목적함수), **이상치 있으면 4~9배 낫다**(q_proj 0.012 → 0.002, down 0.009 → 0.001, gate 0.004 → 0.002; 출력 상대오차 0.0093 → 0.0039) — 실 헤시안이 그 모양이다. 게다가 **더 빠르다**(3.2 vs 5.2 s: 루프 안 그룹 탐색이 없다). **(B) 전체 K GPTQ**(`pack_w4_wide`, `PackStore.pack_wide`): 커널 타일 4,096 보다 넓은 가중치(드래프터 fc 20,480 = 다섯 타일; 입력이 타깃 다섯 층의 hidden 이라 타일 사이 상관이 큼)를 **전체 헤시안**으로 한 번에 — 교정도 타일이 아니라 가중치당 블롭 하나(20,480², 1.6 GiB; 랭크당 예산 2 GiB 안). 인수분해는 fp64 로 CPU 에서, **fp64 행렬 둘까지만**(재시도는 원본을 다시 읽음; 첫 판은 셋+순열 사본으로 20 GiB 컨테이너를 죽였다): 합성(타일 가로지르는 상관)에서 타일별 0.001 → 전체 0.000(목적함수/RTN), 출력 상대오차 0.0029 → **0.0023**(−21%), 77 s vs 16 s(부팅 한 번, 캐시). **(C) NVFP4 프리필 레인이 GPTQ 해를 승계**(`nvfp4_from_w4`): W4 팩의 니블은 e2m1 그 자체이고 그룹 스케일 e4m3 × 행 시프트 × 2 의 거듭제곱 전역 스케일은 e4m3 로 정확히 표현되므로(1,048,576 그룹 중 **부정확 0**) `mm_fp4` 가 **디코드 커널과 같은 GPTQ 해**를 곱한다 — bf16 을 새로 RTN 하던 것을 대체. 남는 차이는 W4 커널이 곱(코드×스케일)을 e4m3 로 반올림하는 것뿐(rms 1.9%). 실 q_proj + fp4 활성(flashinfer 양자화)에서 출력 상대오차 **RTN-NVFP4 0.112 → 팩 승계 0.064**, 활성의 fp4 오차만으로 0.061 — 가중치 쪽 오차가 활성 오차 밑으로 거의 사라졌다. **(D) 배관**: `W4Pack.calibrated`, `DenseLinear.calibrated/nvfp4_inexact`, 스토어 `calibrated()`·팩당 블롭 하나, 부팅 게이지 `dense_calibrated / dense_nvfp4_from_packs / dense_nvfp4_inexact_groups`, `lane_info{nvfp4_from_packs}`. 스토어의 알고리즘 해시가 바뀌므로 다음 부팅은 팩을 전부 다시 짓는다(교정 있는 곳은 GPTQ ~3 s/팩: 옛 덤프가 있는 노드라면 ~11 분 한 번). **정직하게**: 셋 다 합성 헤시안 판정이고 실 수용률·프리필 품질은 플릿 창의 브래킷 몫; NVFP4 승계는 e4m3 곱 반올림 차이(1.9%)를 남기며, 이상치 없는 입력에서 act-order 의 이득은 0 이다. CPU 스위트 682 OK(교정 테스트는 가중치당 블롭 하나로).

**GPU 판정 8차 — FP8 레인은 NVFP4 로 가지 않고, 자기 격자에서 GPTQ 한다(23:30, 운영자 "정확도가 올라갔으니 fp8 레인도 nvfp4 화 시키면 어때", PR #661)**: 같은 입력(상관 + 이상치 채널, 33..1023 행 레인의 512 행)에서 세 가지를 쟀다 — 실물 드래프터 q_proj 와 타깃 L3.mla.o_proj(랭크 0 샤드), bf16 진실 대비 출력 상대오차. **FP8 W8A8(지금, RTN 가중치) 0.0375 / 0.0364** = 가중치 몫 0.027 + 활성 몫 0.026; **NVFP4 W4A4(7차의 GPTQ 팩) 0.064 / 0.075**, 그중 활성 fp4 몫이 0.061 / 0.071 — 가중치가 완벽해도 fp4 활성 하나가 fp8 레인 전체보다 크다. 그러니 FP8 → NVFP4 는 짧은 프롬프트(1,023 토큰 이하: 대화의 대부분)의 프리필 오차를 **두 배**로 만든다. 답은 반대쪽: **fp8 격자 위의 GPTQ**(`packing.fp8_gptq`): 서빙 레인의 128×128 UE8M0 블록 스케일은 그대로(정적), 열마다 그 블록 스케일 아래 e4m3 로 반올림하되 헤시안 대각 내림차순(act-order)으로 오차를 되먹인다 → 가중치 몫 **0.027 → 0.0009**, 합계 **0.0375 → 0.0264**(활성 몫만 남음), 실제 DeepGEMM fp8 GEMM(`FP8Linear` 에 준비된 가중치를 물림)으로 0.0266. 엔진의 fp8 RTN(`fp8_block_scales/fp8_rtn`)은 DeepGEMM 의 `per_block_cast_to_fp8(use_ue8m0=True)` 와 **코드·스케일 바이트 동일**(세 가중치 전부 0 바이트 차이) — 패커가 라이브러리 형식을 재현한다. **헤드도**: `FP8Linear` 로 서빙되는 어휘 헤드(운영자가 W4 는 접었음)를 같은 방법으로, 랭크 0 샤드 [38,720, 4,096] 에서 0.0377 → **0.0269**, 3.0 s — 헤드는 `Glm5NextForCausalLM/lm_head` 로 자가 교정에 든다(행은 배치 자체라 마스크 없이 합산). **배관**: `PackStore.pack_fp8`(가중치·헤시안·패커 신원으로 캐시; 3.1 s 생성, 0.06 s 재적재), `DenseLinear` 의 FP8 레인과 `net.prepare_dense` 의 헤드가 교정이 있으면 그것을 씀, `FP8Linear(quantized=, name=)` + observer, 게이지 `dense_fp8_calibrated`, `lane_info{fp8_gptq}`. 넓은 K(fc 20,480)는 CPU fp64 인수분해. 이로써 **밀집 가중치의 세 레인(W4A8 디코드·FP8 프리필·NVFP4 큰 M)이 전부 같은 교정에서 나온 GPTQ 해**를 쓴다. 정직하게: 합성 헤시안 판정이고, fp8 GPTQ 의 이득은 활성 fp8 오차(0.026)에 막혀 합계 −30% 가 상한이다.

**조사 9차 — 같은 방식으로 더 줄일 곳(00:10, 운영자 "같은 방식으로 오차를 더 줄일수 있는 곳 없어?", 판정만 — 원장만이던 PR #664 는 닫고 PR #669 에 실었다)**: 세 레인의 가중치 몫이 0.001 수준이 된 뒤 남은 오차는 활성 양자화다. 교정 통계로 채널 이상치를 가중치로 옮기는 SmoothQuant(α=0.5: s_k = amax_x^α / amax_w^(1−α), W·diag(s) 를 GPTQ, X/diag(s) 를 양자화)를 같은 입력(상관 + 이상치 채널 20배, 실물 q_proj·o_proj)에서 재 보니: **FP8 W8A8 0.0268 → 0.0262(효과 없음)**, **W4A8 디코드(행별 활성 스케일) 0.0207 → 0.0211(없음)** — 둘 다 e4m3 의 가수 3비트가 바닥이라 이상치를 옮겨도 남는 게 없다; **NVFP4 W4A4(큰 M 프리필) 0.064 / 0.072 → 0.046 / 0.050(−28%)** — fp4 는 가수 1비트라 16-그룹 안의 이상치가 그룹 전체를 망치고, 그것은 옮길 수 있다. α=0.75 는 더 못하다. **MoE 전문가 체제**(체크포인트의 RTN NVFP4 가중치 + fp4 활성, 실물 sh_gate_up 으로 흉내): 0.113 → 채널별 평활 **0.059(−48%)** 이지만 bf16 전문가가 있어야 접을 수 있고, e4m3 블록 스케일에 정확히 접히는 16-그룹 2의 거듭제곱 평활은 **0.114(효과 없음)** — 그룹 단위론 이상치 채널을 고립시키지 못한다. **결론**: 같은 방식으로 남은 자리는 하나, NVFP4 큰 M 레인의 채널 평활(관측기에 채널별 amax, prepare 때 s 를 앞 norm 가중치에 접어 런타임 비용 0 — norm 이 앞에 없는 o_proj·down·fc 는 명시 곱이 필요, 팩 신원에 s 포함); 가장 큰 오차 덩어리(전문가 0.11)는 bf16 원본 + 전문가별 라우팅 토큰 교정(평활 + GPTQ → 추정 ~0.04)이라는 다른 규모의 일이다. 판정기 주석: 평활한 행의 "weights only" 열은 평활 전 입력으로 곱해 무의미(합계·활성 열만 유효).

**GPU 판정 10차 — NVFP4 큰 M 레인의 채널 평활, 앞 norm 에 접어서(01:30, 운영자 "진행", PR #669)**: 9차의 남은 자리 하나. `engine/kernels/dense/smoothing.py`: 교정의 채널별 amax(관측기가 헤시안 옆에 합산; 블롭에 `amax`, 없는 옛 블롭은 다시 합산하되 H 는 그대로 팩)로 SmoothQuant 계수 s_k = amax_x^0.5 / amax_w^0.5 를 만들고, **앞 RMSNorm 의 가중치를 s 로 나눠 두고 그 norm 출력을 읽는 모든 가중치의 열에 s 를 곱한다** — 런타임 비용 0, x·Wᵀ 는 그대로. **읽는 이를 하나라도 빠뜨리면 그 가중치는 준비 안 된 입력을 보므로** 층 종류마다 소비자 집합을 박았다(`Glm53Net.smoothing_groups`): KDA 층 in_norm → in_proj; MLA 층 in_norm → qkv_a + 인덱서의 wk·gate(bf16, 제자리 재스케일), q_a_norm → q_b + 인덱서 wq_b; 밀집 MLP 층 post_norm → gate_up; **MoE 층의 post_norm 은 라우터와 전문가도 읽으므로 안 한다**. 드래프터(`Drafter.smoothing_plan`): input_layernorm → attention_conv 커널 투영 + (그룹 conv 를 지나) q/k/v, post_attention_layernorm → mlp_conv 커널 투영 + gate/up — 그룹 conv 는 채널별 선형이라 계수가 통과하고, **컨텍스트 투영(context_kv)은 평활 안 된 k/v 원본으로**(그 입력 c 는 나눠지지 않으므로). 스토어는 `smooth` 를 받아 헤시안을 diag(1/s)H diag(1/s) 로 바꾸고 신원에 s 의 해시를 넣으며, 관측기는 이번 부팅의 합을 **평활 전 도메인으로 되돌려 파일링**(H → sHs, amax → amax·s)해 부팅마다 s 를 처음부터 다시 구한다(이중 평활 방지). **계수는 2 의 거듭제곱으로 반올림**: 연속 계수의 첫 판은 실물 드래프터 블록 출력을 rms 16% 움직였는데 — 접기 자체의 bf16 반올림(조각당 0.3~0.5%, 작은 드래프터 1%)이 5 층을 지나며 증폭된 것(4차의 민감도 그대로) — 2 의 거듭제곱이면 norm 나눗셈도 열 곱도 bf16 에서 정확해 곱이 하나도 안 바뀌고 **블록 출력이 비트 동일**(작은 드래프터·실물 드래프터 모두 0.0). **판정**: 실물 드래프터 융합 qkv 의 NVFP4 W4A4 레인, 상관 + 이상치 채널 입력에서 **0.0740 → 0.0566(−23%)**, 활성 fp4 몫 0.0718 → 0.0522(연속 계수는 0.0556: 거듭제곱의 손해는 1% p); FP8·W4A8 레인은 9차대로 ±0. 게이지 `dense_smoothed`, `lane_info{smoothed}`. CPU 판정: 접기의 비트 동일(norm→독자, 타깃 MLA 그룹의 세 독자, 드래프터 블록 전체), 그룹 집합(층 종류별, MoE 제외), 관측기의 amax 와 되돌린 파일링, 옛 블롭 재합산. **정직하게**: 합성 헤시안·합성 amax 판정이고 실 입력의 이상치 구조가 이득을 정한다; o_proj·down·fc·sh_gate_up 은 앞에 norm 이 없거나 공유돼 평활하지 않는다(명시 곱은 청크당 수십 ms 라 보류).

**GPU 판정 11차 — GPTQ 라운드 전부를 실제로 기본으로: 교정 계획이 한 부팅에 들어오게(운영자 "gptq 라운드 전부 디폴트화", PR #673)**: 6~10차는 코드에 노브가 없어 이미 기본이지만 **플릿에서 실제로 켜지는 것은 그중 일부**였다. 네 노드의 `/cache/mkcalib/rank<r>/` 에는 33차 vLLM 스택의 덤프가 랭크별로 남아 있고, 형상을 하나씩 대 보니 **전부 맞는다** — 드래프터도 그때 이미 TP4 로 샤딩돼 덤프됐다(o_proj K=1024 = heads/4·D, down K=3072 = I/4). 6차에 "샤딩된 o_proj/down 의 K 와 맞지 않아 `incompatible calibration` 으로 죽는다" 고 적은 것은 **틀렸다**(부적합 0건). 그래서 228 개 팩 타일 중 **204 개는 헤시안을 이미 갖고 있다**. 문제는 그 블롭에 10차가 읽는 **채널 피크(`amax`)가 없다**는 것: `missing_calibration` 이 그것들을 통째로 "없음" 으로 돌려주므로 부팅은 [K] 짜리 피크를 얻으려고 [K,K] 헤시안 버퍼를 새로 잡는다 — 계획 **9.96 GiB** 대 랭크당 예산 2 GiB, 한 부팅에 **20/228** 만 들어가고 나머지는 다음 부팅으로. 평활은 그동안 **어느 가중치에도 안 걸린다**. **고침 (A)**: `store.Need(key, start, width, hessian)` — 스토어에 맞는 헤시안이 있는 타일은 **피크만** 합산한다(버퍼 [K] floats = 20 KB, 헤시안 67 MiB 대신). 계획 **1.82 GiB**, 예산 2 GiB 안에서 **228/228 이 한 부팅에**(남은 24 개는 애초에 헤시안이 없는 것들 — 드래프터 fc 20480² 1.56 GiB, 헤드, MLA q_b 와 인덱서 wq_b). 파일링은 디스크의 헤시안과 그 토큰 수를 **그대로 두고** 피크만 얹으며(테스트가 H 바이트 동일을 건다), 임시 파일 + `os.replace` 로 쓴다 — 쓰다 죽은 부팅이 잘린 블롭을 남기면 다음 부팅이 D3 로 죽기 때문. **고침 (B) 팩 시간**: 한 가중치의 W4 레인과 FP8 레인은 **같은 H 의 같은 열을 act-order 로 걷는데 fp64 인수분해를 각자** 했고, W4 쪽은 블롭이 호스트에 로드돼 있다는 이유만으로 **CPU 에서** 했다. `packing.gptq_factor` 가 (순열, U, dead) 를 한 번 만들어 둘에 넘긴다(`store._factor`: 항목 하나, 다음 가중치가 갈아치움; 256 MiB 넘는 인수분해는 쓰고 버린다 — 드래프터 fc 의 1.6 GiB 를 가득 찬 아레나 옆에서 들고 있지 않는다), 타일 폭 K 는 가중치의 장치에서. **판정(실물 랭크3 가중치 넷, 프로덕션 옆, 후보 교차 반복 5회 최솟값)**: 가중치당 **−10~29%** (mla.o_proj 2.44 → 1.73 s, kda.in_proj 2.69 → 2.14, moe.sh_gate_up 4.00 → 3.14, moe.sh_down 0.47 → 0.43), W4 출력 상대오차는 그대로(0.0031 → 0.0031), fp8 코드는 지금 것과 **바이트 동일**. 스토어 알고리즘 해시가 바뀌어 첫 부팅이 랭크당 204 개를 다시 굽는데(10 분대), 그 값이 그만큼 준다. 게이지 `calibration_blobs`(전체 타일)와 `calibration_hessians`(그중 헤시안을 새로 만드는 것), 스토어 통계 `factor_built/factor_reused`. CPU 스위트 **758 OK**. **끝에서 끝까지(실물 블롭, 프로덕션 옆, PR #677)**: 이 노드 랭크3 의 진짜 33차 덤프(`…layers.3.self_attn.o_proj`, H 4096², ntok 17,189, amax 없음)를 임시 스토어에 놓고 기본 경로를 그대로 돌렸다 — 스토어가 `Need(hessian=False)` 를 돌려주고, 교정이 Gram 버퍼 0 개에 **20.0 KiB**(헤시안이었다면 64 MiB)를 잡고, 32,768 행을 합산해 파일링하니 **H 가 덤프와 바이트 동일이고 ntok 도 그대로**, 피크 [4096] 만 새로. 그 블롭으로 다음 부팅을 흉내내면 `missing []`, 그리고 한 가중치에서 **세 레인이 모두** 나온다: W4 GPTQ·NVFP4 승계(부정확 그룹 0)·FP8 GPTQ·평활 적용, 스토어 통계 `factor_built 1, factor_reused 1`(두 레인이 인수분해를 **하나만**). NVFP4 프리필 출력 상대오차 0.0590. 인수분해를 공유할 때 순열과 `act_order` 가 어긋나면 죽도록 막았다(내가 남긴 함정: 어긋난 채로 부르면 `sc_static` 미정의로 NameError 였다). **정직하게**: "한 부팅" 은 계획이 예산에 다 들어온다는 뜻이고 32,768 행을 채우는 속도는 트래픽이 정한다; 팩 시간은 프로덕션이 GPU 를 쓰는 옆에서 잰 상대값이다; 피크는 블롭마다 한 번 모이고 다시 갱신되지 않는다(재교정은 블롭을 지우는 것).

**조사 12차 — 평활이 더 갈 곳과, 실제 트래픽의 채널 구조(운영자 "추가 개선해", 판정만 — PR #686 에 함께 실렸다)**: 10차가 남긴 자리는 "앞에 norm 이 없는" o_proj·down·fc·sh_gate_up 이었다. 코드를 보니 o_proj 둘은 **선형 생산자**가 있어 명시 곱 없이 정확히 접힌다: MLA 는 `o = einsum(ctx_lat, w_uv)` 이므로 `kv_b` 의 v 행(bf16, 양자화 안 됨)을 1/s 로 나누면 **채널마다** 정확히 접히고, KDA 는 `kda_output_norm` 의 가중치가 [D] 라 **머리마다 공유**되므로 head-dim 마다만 접힌다. down·sh_down 은 **불가** — `swiglu_clamped` 가 u 를 ±10 으로 자른 뒤 곱하므로 u 행을 나누면 잘리는 지점이 바뀌어 서빙 함수가 달라진다. 그래서 두 o_proj 만 실측했는데, 입력을 **실물 생산자 가중치로** 만들어 보니(KDA: 실제 o_norm 으로 게이트된 RMS 출력, MLA: 실제 w_uv 를 지난 latent) **±0 이었다**: KDA 0.1280 → 0.1280(head-dim) / 0.1297(채널), MLA 0.0765 → 0.0770. 이유는 입력의 채널 피크 퍼짐이 각각 **1.4 배·1.9 배**뿐이라는 것 — 옮길 이상치가 없다. 그래서 합성이 아니라 **실제 트래픽**을 보았다: 이 노드의 33차 덤프 215 개에서 sqrt(diag(H)) = 채널별 rms 의 max/median. **드래프터가 이상치의 집이다** — qkv 25~61, mlp_conv/attention_conv 커널 투영 10~61, mlp.down 14~44, gate_up 11~32, fc.k2~k4 13~19. **타깃은 앞쪽 몇 층만** 크다(L0~L3: o_proj 31·6.5, down 58.7·19.1·15.9, gate_up 14.6·11.2) 그리고 **나머지는 평평하다**(깊은 층의 in_proj·sh_gate_up 1.8~3.5, sh_down 1.0~1.7). 즉 **10차의 −23% 는 합성 이상치(×20) 위의 수치이고 실서빙의 타깃에서는 그만큼 나오지 않는다**; 반면 10차가 같이 넣은 드래프터 평활은 이상치가 실제로 있는 자리다. 결론: 평활은 여기서 끝. o_proj 두 접기는 **가능하지만 가져갈 것이 없어** 안 한다(기록만).

**GPU 판정 13차 — NVFP4 밀집 프리필 레인을 걷어낸다: 사는 것이 없고 6~11차가 산 정확도를 쓴다(운영자 "추가 개선해", PR #686)**: 남은 가장 큰 밀집 오차는 가중치가 아니라 **NVFP4 레인의 fp4 활성**이었다(8차 이후 고정된 사실). 그 레인은 39차 §4g P3C2 가 **vLLM 스택의 처리량만 보고**(프리필 +3.5~5.1%) 기본으로 올린 것이다. 이 엔진의 FP8(DeepGEMM) 레인과 **이 랭크가 실제로 서빙하는 여덟 형상 전부**에서 다시 쟀다(프로덕션 옆, 후보를 repetition 마다 교차, 최솟값 8회): 형상별로 `moe.sh_gate_up`[1024,4096]×42 **0.64~0.87배**, `moe.sh_down`[4096,512]×42 **0.66~0.90배**(NVFP4 가 더 느리다), `kda.in_proj`[6416,4096]×34 1.20~1.32배, `mla.o_proj` 1.02~1.25배, 나머지는 0.80~1.21배. 인구를 가중해 합치면 오늘의 전-NVFP4 대비 전-FP8 이 M=1024/2048/4096/8192 에서 **+3.7% / −0.1% / +0.4% / −2.3%** — **무승부**다(형상마다 빠른 쪽을 고르면 −9.4~9.9% 지만 그건 레인 둘을 다 들고 가는 값이다). 정확도는 무승부가 아니다: 실물 가중치 + **실물 교정 헤시안의 공분산**으로 뽑은 입력에서 kda.in_proj 0.0382 대 0.0104, mla.o_proj 0.0761 대 0.0207, moe.sh_gate_up 0.0701 대 0.0193 — **FP8 이 3.65배 정확하다**(NVFP4 쪽은 7차의 팩 승계를 쓴 뒤의 값이다). 사는 것이 0 이고 쓰는 것이 3.65배면 레인이 아니다. **걷어냈다**: `DenseLinear` 의 세 번째 가지와 `nvfp4=` 인자(드래프터는 이미 False 였다), `packing` 의 `nvfp4_from_w4`·`w4_rowmajor`·`_fp4_encode_nibbles`(58 줄), 게이지 `dense_nvfp4_from_packs`·`dense_nvfp4_inexact_groups`, `lane_info{nvfp4_from_packs}`, 부팅 증명의 `target_nvfp4`(이제 가중치마다 W4 + FP8 둘). 그 레인의 PDL 의존성 해저드를 박아 두던 `tests/test_engine_nvfp4_order.py` 도 레인과 함께 사라진다. **결과**: 실물 L3.mla.o_proj 의 프리필 행이 0.0761 → **0.0207**, 디스패치는 "≤32 행 W4A8, 그 위 FP8" 둘뿐(D3: 서빙 형태는 하나), 그리고 이 랭크에서 레인이 들고 있던 **fp4 가중치 사본 0.98 GiB** 가 없어진다. CPU 스위트 **782 OK**. **정직하게**: 첫 측정(비교차)은 전-FP8 이 10~34% 빠르다고 했는데 그건 프로덕션 경합 편향이었다 — 교차 반복의 답이 무승부다; 밀집 GEMM 은 프리필 스텝의 10% 안팎이라 종단 처리량 변화는 그보다 작다; 라우팅 전문가의 NVFP4(b12x)는 체크포인트가 fp4 라 그대로다.

**조사 14차 — 프리필 구성표와, 라우팅 전문가 레인이 실제로 무엇에 묶여 있나(운영자 "개선해"→"전문가 레인 꼬리", 판정만 — PR #692)**: ST 프리필은 옛 vLLM 기준선의 **79%**(2,028~2,117 vs 2,696 tok/s)다. 그 21% 가 어디인지 보려고 서빙 청크(6,912 토큰 = 블록 3개, 3.27 s)를 구성요소별로 쟀다(프로덕션 옆, 실물 랭크3 가중치, 최솟값): **라우팅 전문가 42층 0.82 s(25%)**, **MLA 희소 어텐션 11층 0.42 s(13%)**, **KDA 청크+conv+o_norm 34층 0.28 s(9%)**, **밀집 GEMM 202개 0.27 s(8%)**, kv_b 아인슈타인합 0.04 s, **미측정 1.44 s(44%: mHC·집합통신·인덱서·라우터·호스트)**. 2026-09-03 vLLM 프로파일(32K, 층-청크당)과 대면 **ST 의 MLA 어텐션은 76 ms → 38 ms 로 절반**(39차의 prefill32 register-Q 타일이 ST 에 있다), KDA 는 5.4 → 7.0 ms. **전문가 레인의 정체**: 같은 층·같은 T 에서 라우팅만 바꿔 쟀더니 시간이 일의 양을 안 따라간다 — 전문가마다 **정확히 192 행**으로 같고 타일 1,728개·패딩 행 55,296 으로 **완전히 동일한데**, 토큰이 규칙적으로 성기면(`sel[t] = (8t+j) mod 288`) **12.78 ms**, 같은 배정을 무작위로 섞으면 **19.98 ms**(+56%). 균일 난수 20.65, 교정 공분산에서 뽑은 실물 라우터 19.61(max 2,296·median 40·활성 216) — **실 라우팅은 나쁜 쪽 끝에 있고, 행 수 쏠림(max/median 57배)은 시간을 거의 안 움직인다**(균일 난수와 실물 라우터가 같은 자리). 즉 이 레인은 **일의 양이나 전문가 불균형이 아니라 토큰 배정 무늬**에 묶여 있다. **고쳐 보려다 둘 다 실패**: (1) 전문가마다 연속 토큰 블록 **25.96 ms**, (2) 실물 카운트 그대로 두고 토큰을 전문가 정렬로 재배치 **24.91 ms** — 둘 다 더 나쁘다. 이유는 그 구성들이 전문가의 192 행을 **소수의 서로 다른 토큰**으로 접어(각 토큰이 8번씩) 에필로그 scatter-add 가 같은 출력 행에 몰리기 때문이다. 게다가 디스패처는 **이미 A 를 전문가 major 로 팩한다**(`packed_input [state_E, max_rows, k/2]`), 그래서 "A 를 미리 정렬해 두자" 는 빠진 조각이 아니다. (3) 같은 라우팅·같은 수학에서 **활성값의 행 순서만** 바꿔도(top-1 전문가로 정렬, 전문가 집합으로 정렬; 출력은 역치환해 값이 같음을 확인) **0%·−1%** 로 꿈쩍하지 않는다 — 즉 메모리 순서가 아니다. strided 가 빠른 것은 그 구성에서 **한 토큰의 8 전문가가 연속된 id** 라는 점뿐이고, 그것은 실 라우팅이 가질 수 없는 성질이다. 커널 스케줄도 범인이 아니다: 항목은 (전문가, m-타일, n-블록) 단위로 균등하고(`row % tile_m == 0` 마다 `next_item += output_tile_count_n`), CTA 는 `bidz` 에서 `gdim_z` 씩 건너뛰며 라운드로빈으로 가져간다. **결론**: 층당 ~7 ms(청크의 0.29 s, 프리필의 9%)가 게이트·양자화 프리패스 안의 무늬 의존 비용으로 남아 있다 — 호스트 재정렬로는 못 꺼내고 커널 프로파일이 필요하다. **그리고 이것은 21% 격차의 원인이 아니다**: 옛 vLLM 스택도 같은 커널에 같은 실 라우팅을 물렸다. 21% 는 아직 안 잰 44% 안에 있다(vLLM 표에서 그 자리는 mHC 11.4% · NCCL AllReduce 16.1% · elementwise 8.9% 였다). **정직하게**: 절대 시간은 부팅마다 흔들린다(같은 strided 구성이 플릿이 붙어 있을 때 18.01, 비었을 때 12.78) — 유효한 것은 한 실행 안의 비교뿐이고, 위의 배수는 전부 한 실행 안에서 잰 것이다.

**GPU 판정 15차 — 구성표의 나머지(mHC 7%·인덱서 2%)와, 풀 선택이 필요 없는 정렬을 사던 것(운영자 "다른 곳 개선", PR #693)**: 14차의 미측정 44% 를 둘 더 깎았다. **mHC**(`lanes.mhc_pre/post`, tilelang): 청크에서 pre 2.77 ms + post 2.38 ms, 모듈 89개 → **0.229 s(청크의 7%)** — vLLM 표의 11.4%(청크당 384 ms)보다 **40% 낫다**. 형상을 여기 박아 둔다: `hc.attn_fn` 은 `[(2+hc)·hc, hc·hidden] = [24, 16384]` 이므로 **hc 는 마지막 차원이 아니라 그 식에서 나온다(hc=4, hidden=4096)** — 이걸 16384 로 읽어 `[6912, 16384, 4096]`(927 TB)을 요청했다가 earlyoom 이 프로덕션 워커를 쏴 플릿이 내려갔다([[gb10-docker-memory-does-not-bound-cuda]]: `--memory` 는 CUDA 통합메모리를 못 막는다). **DSA 인덱서** 11층: quant 0.45 + kpool_compress 0.10 + logits 1.10 + **top-k 선택 4.85** = 6.25 ms/층 → 0.069 s(2%). 즉 **인덱서 시간의 74% 가 점수 계산이 아니라 고르기**다. 누적 구성표(청크 3.27 s): 전문가 0.82(25%) · MLA 0.42(13%) · KDA 0.28(9%) · 밀집 GEMM 0.27(8%) · mHC 0.23(7%) · 인덱서 0.07(2%) · kv_b 0.04 → **2.13 s(65%) 설명됨**, 남은 1.14 s(35%)는 집합통신(랭크 넷이 필요해 단독 측정 불가)·라우터·elementwise·호스트. **여기까지에서 ST 가 vLLM 보다 나쁜 곳은 KDA 하나뿐이다**(7.0 vs 5.4 ms/층-청크). **고친 것**: `sparse_indexer.topk_positions` 는 정렬된 top-k 를 사고 있었는데 **뒤의 `pool_slots` 가 어차피 토큰 위치 내림차순으로 다시 쓴다** — 필요한 것은 집합뿐이다. `sorted=False` 로 바꾸고, 패딩 표시도 정렬이 돌려준 fp32 값의 `isinf` 대신 **int32 승자에 대한 위치 비교**로 바꿔 [rows, k] 를 두 번 덜 지난다. 실물 형상 판정(프로덕션 옆, 최솟값 8회): 청크 [6912, 1728]×512 **4.85 → 3.86 ms(−20%)**, 32K 문맥 [6912, 8192] 14.49 → 13.54(−7%), 디코드 형상 −5~6%; **고른 집합은 전부 동일**(랜덤 valid 포함, CPU 테스트가 건다). 청크 기준 11층 × 1 ms = 11 ms(프리필의 0.3%)로 작지만 공짜다. **다음 레버는 그 선택 자체다**: [6912, 8192] fp32 216 MB 를 14.5 ms 에 고르는 것은 **15 GB/s**, 대역폭 바닥의 30분의 1이다 — 정렬 없이 행마다 k 번째 값을 히스토그램으로 찾고 그 위를 모으면 두 번 지나면 되고(≈1 ms) 문맥이 길수록 커진다(128K 에서 n_cand 32,768 → 층당 ~58 ms × 11). 샘플러의 `_search`(비트 임계 이분 탐색)는 **그 배치가 L2 에 든다는 전제**라 216 MB 에는 그대로 못 쓴다; 라딕스 두 패스가 맞는 모양이다. **정직하게**: 구성요소 시간은 격리 측정이라 실제 스텝의 겹침·런치 간격을 포함하지 않는다(합이 3.27 s 를 밑도는 이유의 일부); 절대값은 플릿이 붙어 있느냐에 따라 흔들려 유효한 것은 한 실행 안의 비교다.

**조사 16차 + 집행 — 희소 MLA 프리필의 유니온 후보(`pair`·`pair4`)를 재고 걷어냈다(운영자 "다른 레인", PR #698)**: 39차가 남긴 유일한 미판정 노브였다(`STK_mla_prefill`, "프로덕션 off, numerics·TTFT 브래킷 대기" — 원장에 수치가 없다). 두 후보는 **인접 쿼리의 선택이 크게 겹친다**는 예보(“75% 공통이면 트래픽 0.4375”)에 기대 그룹의 선택 **합집합을 한 번만 읽는다**. 그 예보를 변수로 놓고 쟀다(T=6,912·topk 2,048, 겹침 0/50/75/90/100%, 세 변종을 각각의 컨테이너에서, 최솟값 6회). **latent 16 MiB(32K 문맥, L2 초과)**: tile32 는 겹침과 **무관하게 18.3 ms 로 평평**하고, pair 는 42~61 ms, pair4 는 47~111 ms — **모든 겹침에서 tile32 가 2.3~6배 빠르다**. 선택이 **100% 공통**이어서 유니온이 최선일 때조차 tile32 37/18.3 < pair 41.6/42.1 < pair4 47.2/47.7 이다. 세 변종의 출력 체크섬은 같다(같은 것을 계산한다). 처음엔 latent 를 T 와 같은 6,912(3.5 MiB)로 잡았는데 그건 **전부 L2 에 들어** 게더가 DRAM 을 안 치므로 유니온의 전제 자체가 시험되지 않았고 시간도 비단조였다(50% 에서 18.2, 양 끝에서 36~37) — 문맥 크기를 실제로 키운 뒤에야 답이 나왔다. **집행**: `PREFILL_MODES` 는 `("stock", "tile32")`, `_mla_prefill_pair`·`PAIR_STATS`·`ENABLE_MLA_PREFILL_PAIR`·`MLA_PREFILL_GROUP` 과 그 부팅 자가시험 분기를 지웠다(파이썬 −110 줄); 노브 설명도 고쳤다. **남은 것**: `glm53_megakernel.cu` 의 `run_mla_prefill_pair`·`run_mla_prefill_group4` 와 그 `mk_mla_group4_*` 커널(≈1,200 줄)은 이제 아무도 안 부른다 — 그 파일을 다음에 건드리는 사람이 같이 걷으면 된다(지금 지우면 네 노드의 MLA nvcc 캐시가 통째로 무효화되고 그 파일을 만지는 다른 세션과 충돌한다). **정직하게**: 겹침은 합성으로 만들었다(블록마다 공통 선택 + 나머지 무작위); 실 서빙의 인접 쿼리 자카드는 이 레인이 부팅 때 스스로 찍게 돼 있었는데(`mla pair stats`) 그 진단도 후보와 함께 사라진다 — 없어진 후보의 전제였으므로 남길 이유가 없다.

**조사 17차 — 프리필의 고정비: 라우팅 전문가 레인은 토큰이 아니라 청크마다 뱅크를 읽고, 그 읽기가 메모리 바닥의 3배다(운영자 "프리필 개선 방법이나 찾아" → "고정비를 낮추는게 답인것 같은데", PR #711)**: 14차의 구성표에서 가장 큰 조각은 라우팅 전문가 0.82 s(청크의 25%)였다. 그 비용이 무엇에 비례하는지 청크 크기로 쓸어 보니 **토큰이 아니다**: 같은 층에서 T=1,728 → 14.18 ms, 3,456 → 18.96, 6,912 → 21.97, **13,824 → 22.17** — 토큰을 두 배로 실어도 **0.9%** 만 는다. 두 점으로 분해하면 **층당 고정 13.0 ms + 토큰당 0.66 µs** 이고, 고정분이 하는 일은 이 랭크의 전문가 뱅크(w13 576 + w13_sf 72 + w2 288 + w2_sf 36 = **972 MiB**, 42층이면 청크당 39.9 GiB)를 한 번 읽는 것이다. **그 읽기의 바닥을 이 상자에서 직접 쟀다**: 1 GiB fp32 읽기 235 GB/s, `copy_` 221 GB/s, 그리고 **실물 w13 텐서를 그대로 읽으면 576 MiB 를 2.57 ms = 235 GB/s** — 레이아웃 탓이 아니다(처음 `sum()` 으로 11 GB/s 가 나온 것은 uint8 리덕션이 벡터화되지 않는 torch 쪽 함정이었다). 뱅크 972 MiB 의 순수 읽기는 **4.3 ms**. 즉 **고정비 13.0 ms 는 메모리 바닥의 3.0 배**이고, 초과분은 층당 8.7 ms · 청크당 **0.37 s · 프리필의 11%** 다. **청크를 키우는 것은 이 낭비를 없애는 게 아니라 토큰으로 나누는 것**이다(6,912 → 13,824 이면 토큰당 12% 싸지지만 낭비는 그대로). 청크를 정하는 것은 `boot.TOKEN_BUDGET = 8192` 한 줄이고(`floor((8192−5)/2304)×2304 = 6,912`), vLLM 에서는 APC align 이 3 블록에 못박아 예산을 올려도 안 움직였지만 **ST 는 예산에서 직접 계산한다** — 다만 프리필 활성 피크가 비례해 커지므로 메모리 관문이 판정할 값이고, `decode_token_budget` 이 혼합 스텝을 이미 2,304 로 따로 막고 있어 큰 청크는 순수 프리필 스텝에만 걸린다. **막힌 곳**: 그 13.0 ms 가 큰 가중치 스트림인지 작은 스케일·활성 박스의 요청률인지 가르는 계측이 **커널 안에 이미 있는데**(`xs` = FC1 SFB 박스 생략, `xa` = A+SFA 박스 생략, 수치는 쓰레기·시간은 유효) **프로덕션 조합과 배타다**: `sf6` 는 `t,r` 을 요구하고 `r`(decode_reform)은 `a_ring·sf_pack·skip_sf·skip_a` 중 하나라도 있으면 거부한다(`_parse_glm53_static_v2`). 그래서 서빙 레시피(`t,r,sf6,q0`)에서는 이 질문을 물을 수 없다 — 계측을 `r` 과 같이 쓸 수 있게 푸는 것이 이 길의 첫 걸음이다. **참고**: 38차 피어 3라운드가 같은 자리를 쳐서 "메모리 경로는 바이트가 아니라 **L2 요청률**에 묶인다"고 결론했고 연속 16 KB TMA 박스 셀 `t` 는 `u` 대비 무이동이었다 — 그 판정은 vLLM 오버레이의 형상에서 나온 것이라 ST 청크에서 다시 유효한지는 열려 있다. **정직하게**: 절대 시간은 플릿이 붙어 있느냐에 따라 크게 흔들린다(같은 T=1,728 기준이 9.3~14.2 ms) — 위의 분해는 한 실행 안의 네 점에서 얻은 것이고, 바닥 4.3 ms 와의 비교는 같은 조건의 대역폭 측정과 나란히 잰 것이다.

**GPU 판정 18차 — 계측을 서빙 레시피에 겨눌 수 있게 풀고, 고정비가 무엇인지 물었다(운영자 "풀어", PR #714)**: 17차가 막힌 곳은 계측이었다 — 층당 13.0 ms 의 고정비가 **큰 가중치 스트림**인지 **작은 스케일·활성 박스**인지 가르는 셀 `xs`(FC1 SFB 박스 생략)·`xa`(A+SFA 박스 생략)가 서빙 레시피 `t,r,sf6` 에서 안 켜졌다. 관문이 둘이었다: 파서의 `r` 은 `a_ring·sf_pack·skip_sf·skip_a` 중 하나라도 있으면 거부했고, 커널의 `reform_sf_pack`(sf6)은 그 넷에 `split` 을 더해 거부했다. **둘 다 진짜 충돌이 아니었다**: 스킵은 레이아웃이 아니라 `const_expr(not self.skip_*)` 로 **TMA 발행 자체를 컴파일 타임에 빼는 것**이라 어떤 스케일 기하와도 합성된다. 두 관문을 좁혀 `skip_sf`·`skip_a` 만 통과시켰다(스케일 경로를 실제로 바꾸는 `a_ring`·`sf_pack`·`split` 은 그대로 배타, 그리고 **서빙 파스는 여전히 `probe-only` 로 거부**한다 — 프로덕션이 실수로 집을 수 없다). **판정(서빙 레시피 `t,r,sf6`, T=1,728, 네 변종을 한 프로세스에서 교차, 최솟값 8회)**: served **9.43 ms** · xs 9.56(+1.4%) · xa 9.32(−1.2%) · **xs+xa 9.31(−1.3%)**. **작은 박스를 둘 다 꺼도 1.3% 다** — 고정비는 스케일도 활성도 아니고 **전문가 가중치 스트림 그 자체**다(커널 주석의 "xa 진단이 그 로드를 ~3% 로 매겼다" 와 같은 자리). 그러니 17차의 3.0 배 간극(13.0 ms vs 바닥 4.3 ms)은 w13/w2 의 B-박스 발행 패턴에 있고, 38차 피어의 "L2 요청률에 묶임" 도 작은 박스가 아니라 **가중치 박스** 이야기로 읽어야 한다. **측정 규약**: 변종마다 컨테이너를 따로 쓴 첫 판은 쓸 수 없었다 — 같은 기준이 9.89 와 15.69 로 59% 흔들렸다(플릿이 그 사이 떴다). `md._WEIGHT_CACHE`·`_REFORM_SF_CACHE` 를 비우고 `lanes` 를 다시 만들면 **한 프로세스에서 네 변종을 다 세울 수 있다** — 그 교차가 답을 냈다. **그래서 셀 공간을 쓸었다(서빙 청크 T=6,912, 기준과 후보를 한 프로세스에 둘씩 교차, 최솟값 8회)**: 서빙 `t,r,sf6,q0` 대비 **평범한 `t` −0.3%**, `t,f4,g2`(FC1 스테이지 4) **−1.1%**, `t,f2,g4`(FC2 스테이지 4) **+3.3%** — **전부 ±3% 안이다**. 스펙 언어가 여는 손잡이(스테이지 수, 타일 주요·재배열, 스케일 패킹)로는 가중치 스트림이 안 움직인다. 38차 피어의 "t 는 u 대비 무이동" 과 같은 결론에 ST 의 청크에서 따로 도달한 셈이다. **그러니 17차의 3.0 배 간극은 셀이 아니라 커널 안의 B-박스 발행 방식**(동시 진행 수·스위즐·생산자/소비자 분할)에 있고 그건 CuTe-DSL 개조다 — 값은 이미 알려져 있다(청크당 0.37 s, 프리필의 11%). 한 가지 더: `t,r,sf6` 를 쓰지 않는 후보는 `q0`(SF6 메타데이터 캐시)도 같이 잃으므로 위 `t` 행은 기하 하나가 아니라 **레시피 전체**의 비교다. CPU 스위트 845 OK(이미지 안 `test_engine_knobs` 12 OK). **정정(운영자 "너가 커널 본문 공략해" 뒤)**: 셀을 쓸어도 평평하길래(서빙 `t,r,sf6,q0` 대비 `t` −0.3%, `t,f4,g2` −1.1%, `t,f2,g4` +3.3%) 커널 본문으로 들어가려고 `s`(스탬프) 셀을 켰더니 **스탬프가 한 줄도 안 찍혔다**. 이유는 단순했다 — **프리필은 정적 커널을 쓰지 않는다**. 토큰 수로 백엔드를 확인하면 T=8(routed 64) → `static2_m8`, T=64(512) → `static2_m64`, **T=1,728(13,824)·T=6,912(55,296) → `[tp-sf6-q0]`** 이고, 프리필 호출 동안 정적 커널의 스탬프 텐서는 하나도 늘지 않는다. `xs`·`xa`·`f`·`g`·`t`·`r` 은 전부 `moe_static_kernel_v4` 를 설정하는 셀이고 그것은 **디코드** 레인이다. 그래서 (a) 18차의 "고정비는 작은 박스가 아니라 가중치 스트림" 은 **프리필에 대해 근거가 없고**(프로브가 도는 커널에 닿지 않았다; ±1.4% 는 잡음), (b) 셀 스윕이 평평한 것도 같은 이유다 — 안 도는 커널의 손잡이를 돌렸다. **살아남는 것**: PR #714 의 관문 완화는 그대로 옳다(디코드 레시피에 계측을 겨눌 수 있게 했다); 17차의 고정비 자체(층당 13.0 ms, 뱅크 순수 읽기 4.3 ms 의 3.0 배, 청크당 0.37 s = 프리필의 11%)는 `lanes.moe` 를 통째로 잰 것이라 유효하다. **다음 사람에게**: 프리필 전문가 레인을 공략하려면 `engine/kernels/b12x/moe_dynamic_gated_sf6_q0.py` 를 보라 — 정적 커널의 셀 스윕과 스탬프는 디코드 질문에만 답한다. **그리고 17차의 귀속도 틀렸다(운영자 "거기부터 다시 봐")**: 프리필 레인에서 청크와 라우팅 행 수를 고정한 채 "몇 개의 서로 다른 전문가에 떨어지나" 만 바꾸면 8개(그 가중치 27 MiB) 14.73 ms · 96개(324 MiB) **10.44** · 288개(972 MiB) 12.86 이다 — **읽는 가중치를 36배 늘려도 시간이 오히려 줄거나 비슷하다**. 디스패처의 `tile_m` 도 평평하다(표 128 대비 명시 64 −0.4%, 명시 128 −1.0% = 잡음 바닥). 그러니 17차의 "고정비 13.0 ms 는 뱅크를 한 번 읽는 것이고 메모리 바닥의 3.0 배" 는 **추론이었지 측정이 아니었고**, 그 위의 "청크당 0.37 s 낭비" 도 함께 철회한다 — 진짜 답은 19차다(점유율). **정직하게**: T=1,728 한 점이고(고정비가 지배하는 지점으로 골랐다), 절대값은 여전히 플릿에 따라 흔들린다 — 유효한 것은 한 실행 안의 네 값뿐이다.

**조사 19차 — 프리필 전문가 레인의 "고정비" 는 위상이 아니라 점유율이었다: 서빙 청크가 정확히 무릎에 앉아 있다(운영자 "계측해봐", PR #744 — #731 이 원장 충돌로 닫히고 §23 정리에 통합)**: 17·18차의 귀속이 둘 다 틀린 뒤, 이 레인을 **밖에서** 계측했다. `torch.profiler` 로 `lanes.moe` 한 콜을 보니 **CUDA 커널이 둘뿐**이다 — `kernel_cutlass_kernel_...b12x moe_dynamic_gated...` **18.741 ms(96.3%)** 와 elementwise 0.723 ms(3.7%). 라우팅 히스토그램도, 활성 양자화도, 스캐터도 **따로 없다**: 전부 그 한 융합 커널 안이다. 그러니 "고정비를 위상으로 가른다" 는 물음 자체가 틀렸고, 남는 물음은 **그 커널이 얼마나 차 있나**다. 행 수로 쓸었다(실 라우터, 최솟값 6회, 커널은 미리 구워 둠 — 첫 판의 T=13,824 는 중간에 CuTe-DSL 컴파일이 끼어 49.04 ms 로 오염됐다): **T=6,912(서빙) 19.38 ms · 35.9 TFLOP/s · 1K 토큰당 2.803 ms**, T=13,824 21.69 · **64.2** · **1.569**, T=20,736 30.69 · 68.0 · 1.480, T=27,648 39.90 · 69.8 · 1.443. **서빙 청크는 무릎 바로 아래에 있다** — 한 번 두 배로 하면 커널 효율이 **1.79 배**(35.9 → 64.2 TFLOP/s), 토큰당 값이 **−44%** 이고, 그 위로는 평평하다(68~70). 그래서 앞선 관찰들이 전부 설명된다: "토큰에 평평하다"(반쯤 빈 커널이 채워지는 구간), "가중치 36배를 읽어도 공짜"(계산에 묶여 있지 대역폭이 아니다), "셀·타일이 안 움직인다"(모양 손잡이가 점유율을 못 바꾼다). **값**: 전문가 레인은 청크의 25%(0.82 s)이고 토큰당 −44% 면 종단 프리필 **약 11%**. **바꿀 것은 한 줄**: `boot.TOKEN_BUDGET = 8192` → 16,384 이면 `floor((16384−5)/2304)×2304 = 13,824`, 정확히 무릎이다. **대가**: 프리필 활성 피크가 대략 두 배(부팅의 메모리 관문이 판정한다 — 안 들어가면 D3 로 죽으므로 창에서 확인해야 한다); 혼합 스텝은 `decode_token_budget` 이 이미 2,304+k 로 막아 두어 영향이 없다; 짧은 프롬프트의 TTFT 는 청크가 상한이라 그대로다. **정직하게**: 한 층·합성 hidden 의 라우팅이고 절대 시간은 플릿에 따라 흔들린다 — 유효한 것은 한 실행 안의 곡선이다. 그리고 1K 토큰당 값은 전문가 레인만의 것이지 스텝 전체가 아니다(나머지 레인은 토큰에 선형이라 안 싸진다).

**GPU 판정 20차 — 프리필 청크를 세 블록에서 네 블록으로(운영자 "메모리가 얼마나 튈것 같은데? 해봐", PR #734)**: 19차가 잰 무릎을 실제로 넘는다. 청크 후보를 한 프로세스에서 교차해 재면(실 라우터, 10회, 워크스페이스는 미리 다 만들어 둠) **6,912(3블록) 1K 토큰당 2.827 ms · 9,216(4블록) 1.728(−39%) · 11,520(5블록) 1.637(−42%) · 13,824(6블록) 1.560(−45%)** 이다 — **4블록이 이득의 87% 를 가져간다**. 메모리가 그 선택을 정했다: 프리필 활성 피크는 토큰에 선형이고(레인 자체의 할당자 피크도 6,912 에서 54 MiB, 13,824 에서 108 MiB 로 정확히 2배) 원장의 자격 검증된 피크가 6,912 에서 **5.72 GiB** 인데 `budget.WORKSPACE_GIB = 12.0` 이 아레나 밖 전부의 상한이다 — 6블록이면 ~11.4 GiB 로 **상한선에 붙고**, 4블록이면 ~7.6 GiB 로 4.4 GiB 가 남는다. 그래서 `boot.TOKEN_BUDGET` 을 8,192 → **10,240** 으로 올렸다(`floor((10240−6)/2304)×2304 = 9,216`; `shapes._selfcheck` 에 그 줄을 더했다). **전문가 레인은 청크의 25% 이므로 종단 프리필 기대치는 −10% 안팎**이고, 혼합 스텝은 `decode_token_budget = chunk_align + spec_k` 가 이미 2,304+6 으로 따로 막아 영향이 없으며, 짧은 프롬프트의 TTFT 는 청크가 상한이라 그대로다. **부팅이 판정한다**: 메모리 관문이 가장 큰 legal 프리필을 실제로 돌려 `peak_workspace_bytes` 를 재고, 12 GiB 를 넘으면 D3 로 죽는다 — 그러니 이 값은 다음 릴리스 부팅의 `prefill/9216/...` 행이 확인해 준다. **측정 함정 하나**: 청크 크기를 프로세스 안에서 바꿔 잴 때 `torch.cuda.empty_cache()` 를 사이에 부르면 MoE 워크스페이스가 반납돼 다음 호출이 21.6 대신 **48.9 ms** 로 나온다(두 번 속았다) — 후보의 워크스페이스를 **먼저 다 만들고** 그 뒤에 교차할 것. CPU 스위트 883 중 882 OK; 실패 하나는 main 자체의 것(`SOURCES.json` 의 provenance 핀이 `kda/`·`_moe_dynamic/gated.py` 중 하나에 대해 낡았다 — 내 변경을 빼도 실패한다).

**GPU 판정 21차 — 20차의 숨은 대가를 걷어냈다: 청크가 8,192 을 넘으면 MLA 가 prefill32 에서 떨어져 나간다(운영자 "무조건 더 빠르게해 어떻게든", PR #737)**: 20차로 청크를 9,216 으로 올린 뒤 **다른 레인들도 이득을 보는지** 확인했다(세 청크, 한 프로세스 교차, 버퍼는 미리 다 만듦). KDA 는 토큰당 −4%, 밀집 FP8 은 ±0 인데 **MLA 희소 어텐션이 +19% 악화**했다(2,648 → 3,162 µs/1K, 그리고 13,824 에서도 같은 값 — 계단이지 기울기가 아니다). 계단의 정체는 `mla/__init__.py` 의 문 `4096 <= T <= 8192` 였다: **청크가 그 상한을 넘으면 prefill32 register-Q 타일 커널이 안 물리고 `mla_decode` 로 떨어진다**(39차가 76 → 38 ms 로 반으로 줄인 바로 그 커널이다). 8,192 는 구조가 아니라 **옛 청크의 천장**이었다 — prefill32 는 행마다 독립 CTA 고(`grid = T`) **스크래치도, prepare 패스도, membership 맵도 없으며**(커널 주석이 그것들을 피했다고 적어 둔 268 MB 는 안 쓰는 쪽이다) 본문의 T 의존은 `blockIdx.x` 하나, 오프셋은 전부 `size_t` 다. 그래서 CUDA 쪽 `TORCH_CHECK(ints[0] <= 8192)` 와 파이썬 문을 **16,384** 로 올렸다. **판정**: T=6,912 1,449.5 µs/1K · **9,216 1,440.9** · 13,824 1,442.4 로 **완전히 평평**하고, 참조 대비 상대오차는 1.91e-03 / **1.82e-03** / 1.85e-03 으로 6,912 의 값과 같다(무장 관문의 2e-2 한참 아래). 즉 20차의 −39% 는 그대로 두고 MLA 의 +19% 만 사라진다 — 구성 가중치로 **종단 프리필 −7.6% → −10% 안팎**으로 복구된다. `glm53_megakernel.cu` 는 provenance 핀 대상이라 `SOURCES.json` 의 `local_sha256` 도 갱신했다(`local_modifications` 는 원래대로 참, 벤더 `sha256` 과 다름). **대가**: 그 파일이 바뀌면 네 노드의 MLA nvcc 캐시가 무효화돼 다음 부팅이 한 번 다시 굽는다. CPU 스위트 **904 OK**. **정직하게**: 절대 µs/1K 값은 이 실행 안에서만 비교된다(앞선 레인 스윕의 2,648 은 `lens` 가 꽉 찬 경우였고 여기는 ragged 다); 바뀐 것은 계단이 사라졌다는 사실이다.

### 45차 §24 — ST 엔진 관측 메트릭: 세 질문에 답하도록 (2026-09-12, srv4, GPU 없음)

운영자 "리베이스 후 st엔진 관측 메트릭 개선". 브랜치를 main(#575 까지)으로 패스트포워드한 뒤 `/metrics` 를 다시 썼다.
**전**: 9줄, 전부 맨 카운터/게이지, HELP·TYPE 없음 — 지연 0, 용량 0, 캐시 0, 스텝 종류 0. 서빙 엔진인데 "요청이 얼마나 기다렸나 / 캐시가 얼마나 찼나 / 만든 재사용이 먹히나" 를 못 물었다.
**후**: 벤치 방언 9줄은 이름·의미 그대로(게이트 불변, `bench/window_metrics.traffic_state` 재확인), 그 위에

- **지연 히스토그램 셋**(vLLM 이름·vLLM 버킷 경계): `time_to_first_token_seconds`, `time_per_output_token_seconds`, `e2e_request_latency_seconds`.
  **도착 시각 기준**이다(요청을 서빙한 스텝이 아니라 `submit` 의 `clock()`). 한 스텝이 토큰 여러 개를 내면(드래프터 수용 구간) 그 스텝의 경과를 토큰 수로 나눠 각각 관측 — vLLM 의 spec-decode 규약.
  버킷은 이 엔진 범위를 가른다: 프리필 스텝 214.7 ms 는 TTFT 의 0.25 근처, 토큰 간격 46 ms 는 0.025~0.05 사이.
- **포화도**: `vllm:gpu_cache_usage_perc`(원시 점유 — `kv.available()` 이 아니다; 그건 프리픽스 캐시가 내놓을 블록까지 세므로 별 계열로 뺐다), `st:kv_blocks_{total,used,free}`, `st:kv_rows_in_use`, `st:state_slots_{total,free}`.
- **재사용**: `vllm:prefix_cache_{queries,hits}_total`, `st:prefix_cache_evictions_total`, `st:prefix_cache_reclaimable_blocks`.
- **스텝 종류**(D9): `st:steps_{prefill,decode}_total` — 합이 `iteration_tokens_total_count` 와 같다(테스트가 강제).
- **티어·실패**: `st:conversations_parked`, `st:tier_bytes_{written,read}_total`(NvmeTier 에만 있으므로 `getattr` 로 보호 — 계측이 문을 내리지 않는다), `st:requests_{cancelled,timed_out}_total`(타임아웃을 취소에서 분리).
- 모든 계열에 **HELP·TYPE**.

**구현 함정 둘(둘 다 테스트가 잡음)**: (1) 취소 경로에서 대기열 분기는 `row` 를 바인딩하지 않는데 거기에 `_token_at.pop(row)` 를 넣어 `NameError` — 문 테스트 44개가 전부 죽었다(행 아는 자리로 옮김).
(2) `BlockPool.available` 은 메서드, `SlotPool.available` 은 프로퍼티 — 비대칭. 그리고 `available()` 은 "지금 빈 것 + 캐시가 내놓을 것"이라 점유율에 쓰면 과소평가다.

**검증**(이미지 안, GPU 없음): 엔진 CPU 스위트 358 tests OK(104 skip), 새 `tests/test_engine_metrics.py` 10 tests(벤치 방언 유지·도착 기준 지연·스텝 합·원시 점유·타임아웃 분리·티어 조건부·노출 형식·히스토그램 누적/포함 경계/불량 표본/찢긴 읽기 단조성).
한 요청 4토큰을 돌린 실측: TTFT 1회 0.05 s, 토큰 간격 3회 합 0.15 s, e2e 0.2 s, 프리필 1 + 디코드 3 = 스텝 4. **GPU·플릿 미검증**(프로덕션 서빙 중).

### 45차 §25 — vLLM 이 못 내는 관측: 어느 그래프가 돌았나, 스텝이 얼마나 걸렸나, 수용은 어떤 모양인가, 무엇이 묶였나 (2026-09-12, srv4, GPU 없음)

운영자 "성능에 악영향이 없을만 하면서 vllm 에서 지원 안 하지만 커널이나 서빙 개선에 도움이 될만한 메트릭". 기준 셋: **디바이스 읽기·동기화 0**, **이미 손에 있는 수로만**, **열린 결정 하나를 닫을 것**.

| 계열 | 닫는 결정 | 왜 vLLM 엔 없나 |
|---|---|---|
| `st:decode_steps_by_sequences_total{sequences}` | 스케줄러가 실제로 채운 배치. 4행이 있는데 n=1 스텝만 쌓이면 처리량 손실이 스케줄링에 있다 | 캡처 그래프 모양을 노출하지 않는다 |
| `st:decode_capacity_bucket_total{capacity}` | **§22 가 연 채로 둔 사다리 천장**. 262K/524K/1M 버킷이 한 주 서빙에도 0 이면 `STK_context_ceiling` 을 잘라 타깃 그래프 36→24 | 용량 버킷이라는 개념 자체가 없다 |
| `st:step_seconds{kind}`(히스토그램) | 호스트 관측 스텝 시간. 두 종류 모두 샘플 읽기로 끝나 종단이다. CUPTI 없이 회귀를 본다(오프라인 46 ms 디코드·214.7 ms 프리필 사이를 버킷이 가른다) | 스텝 시간을 종류로 가른 히스토그램이 없다 |
| `st:spec_accepted_per_step_total{accepted}` | `spec_k`. 0 과 k 에 몰린 분포와 완만히 꼬리 무는 분포는 같은 평균에 **다른 k** 를 부른다 — 총합만으론 구분 불가 | 총합(·일부 빌드의 위치별)만 낸다 |
| `st:lane_info{lanes,moe_static,mla_prefill,spec_k,context_ceiling}` | **"무장 ≠ 서빙"**(§17). 어떤 커널 셀이 이 프로세스에 묶였는지를 부팅 로그가 아니라 스크레이프가 답한다 | 커널 셀이라는 축이 없다 |

**생산 지점**(전부 이미 계산된 값 옆): 그래프 모양은 어댑터가 샘플러에 넘기려고 이미 구한 `shape` 에서 `(shape[0], shape[2])` 한 줄; 수용 분포는 `_commit` 이 이미 구한 `committed` 에서 한 줄;
스텝 시간은 `once()` 의 `clock()` 한 번 추가; lane_info 는 부팅 상수. 딕셔너리는 유계다(모양 = 최대 시퀀스 × 버킷 = 4×9 = 36, 수용 = k+2).

**비용 실측**(이미지 안, 플릿 규모 상태로 채운 뒤): 렌더 **0.096 ms / 11,250 B / 190 줄**(스크레이프당 1회, HTTP 스레드), 관측 **0.96 µs**/회 — 디코드 스텝 최악(4 시퀀스 × 6 토큰 = 24회)이 **23 µs**,
46 ms 스텝의 **0.05%**. 디바이스 읽기·동기화 없음.

**한계**: 그래프 모양 계열은 캡처 경로에서만 는다(이거 폴백은 모양이 없다 — 정직하게 0). `st:lane_info` 는 `fleet()` 부팅만 채운다.
**검증**: 엔진 CPU 스위트 368 tests OK(104 skip), `tests/test_engine_metrics.py` 16 tests(새 6: 그래프 모양 계열, 생산 지점, 수용 분포, 스텝 시간 종류 분할·합=스텝 카운터, lane_info, 부팅이 lane_info 를 채움).
**GPU·플릿 미검증**(프로덕션 서빙 중).

### 45차 §26 — 플릿 예약을 하나로: 큐가 ST 를 받고, ST 가 큐를 본다 (2026-09-12, srv4)

운영자 "플릿 예약 기능이 원래 잘되어 있었잖아. 그걸 왜 안 써? 안 옮겨왔나". **안 옮겼다.** 확인한 사실:

1. `bench/fleet.sh` + 21 모듈 5,146줄의 예약 큐는 살아 있다(상태 `/home/choiceoh/glm53-logs/fleet`).
2. GPU 승인이 **onepass 전용**이라 ST 검사는 줄을 못 선다 — 실측 거부: `GPU work is onepass-only; ...; custom GPU scripts and standalone checks are disabled`.
3. 그래서 §19 가 런처에 `~/st-fleet.lock` 이라는 **두 번째 기구**를 달았다(대기·노화·우선순위·증거·하트비트·복구 없음).
4. **둘이 서로를 못 본다.** 09-12 10:20 실측: 네 노드 전부 `st-glm53`(다른 세션 prefill-diag, 파일 잠금은 잡음) 인데 `fleet.sh status` 는 `fleet: FREE`, `holder: none`. `FREE` 는 holder 파일이 비었는지만 보고,
   점유 감지는 `glm53` 이라는 **이름의 컨테이너만** 찾는다. 이 상태에서 누가 브래킷을 큐에 넣으면 큐가 허가하고 09-11 19:42 충돌이 한 층 위에서 재현된다.
5. 덤: `fleet.sh` 가 `REPO=/home/choiceoh/stkernel` 로 고정 — 그때 거기는 다른 세션 브랜치라 `bench/fleet_*.py` 가 0개였고, 워크트리에서 부른 하위 명령이 전부 파일 없음으로 깨졌다.
6. **내 잘못**: 그동안 "창이 없다"고 미룬 판단은 `docker ps` 만 본 것이다. 큐가 있었으면 미룰 게 아니라 예약해 두고 열릴 때 돌게 했어야 한다(밀린 GPU 검증 셋: 커널 노브 §21, 그래프 재생 §22, direct micro 디스크 캐시 §보충 3).

**해결 넷**:
- **큐가 ST 를 본다**: `st_engine_up()`(로컬 `st-*` 컨테이너) 이 `_try_hold`·`_adopt` 에서 허가를 거부하고, `status` 가 `TAKEN by the ST engine, outside this queue` 라고 답한다.
- **런처가 큐를 본다**: `start-st-glm53.sh` 가 rank 0 노드의 `$FLEET_DIR/holder` 를 읽고 holder 가 있으면 거부. 양방향이 됐다.
- **ST 검사가 큐에 선다**: `fleet_onepass.SHELL_ENTRIES` 에 `probes/run_engine_{probe,check}.sh` 추가. 러너는 `docker run --gpus all <probe>` 라 **러너를 승인하면 임의 프로브가 승인되므로**,
  프로브 이름을 `ST_PROBES` 에 못박고 러너와 함께 바이트 고정했다. 인자는 리터럴 플래그 화이트리스트(`ST_FLAGS`/`ST_SWITCHES`)만. 실측: 정식 다섯 조합 ADMIT, 지어낸 프로브·`--sanitizer`·`; rm -rf /` 전부 거부.
- **`REPO` 를 스크립트 위치에서 해석** → 워크트리에서도 하위 명령이 돈다.

**검증**: 플릿 스위트 385 tests, 실패 4 — **origin/main 에서도 같은 4개가 실패**(coalescing 2, feedback 1, source 1; 임시 워크트리로 대조)라 회귀 0. 새 테스트 8개(ST 승인 4 + 상호 배제 4).
`fleet.sh preflight ... probes/engine_decode_graph_check.py` → PASS, `classify` → gpu.

### 45차 §27 — 예약을 엔진 것으로: 리스는 엔진, 큐는 벤치, 그리고 유령 큐 (2026-09-12, srv4)

운영자 "그냥 저 기능을 엔진에 내장해도 되지 않아?". **가르는 답**: 예약의 두 층이 다르다.
- **리스**(누가 쥐었나·언제부터·아직 살아 있나)는 **엔진 것**이다. 엔진이 플릿을 가져가는 주체이고, 지금까지 그게 런처의 `echo > ~/st-fleet.lock` 한 줄이었다 — 소유자도 만료도 없어서 죽은 부팅과 도는 부팅을 구분할 방법이 사람이 파일을 지우는 것뿐이었다.
- **캠페인 큐**(노화·우선순위·일시정지·핸드오프·증거·러너 핀)는 벤치 것으로 둔다. 5,000줄의 이미 푼 문제이고, 엔진에 다시 만들면 §26 이 진단한 실수를 거울상으로 반복한다.

**§26 이후 드러난 사실 셋**(전부 실측):
1. **예약 셋이 실행 직전에 멈췄다**: 큐는 러너 스냅샷(`fleet_pin`)을 `bench/` 만 떠서 핀하는데 실행 때 그 스냅샷으로 재검증한다 → `cannot verify probes/run_engine_probe.sh`. 훅은 이미 있었다(`run_ar_consumer_campaign.sh` 선례) — `ST_ENTRIES`/`ST_PROBES` 를 같이 핀하도록 고침.
2. **홈은 노드끼리 공유가 아니다**. srv2 에 진짜 큐가 있고(원장·캠페인 전체 하트비트·러너), 내가 srv4 에서 `fleet.sh` 를 돌려 **유령 큐**를 만들었다 — 그래서 `status` 가 FREE 라고 답했고 예약 셋이 아무도 안 보는 디렉터리에 들어갔다. 취소하고 지웠다.
3. 그래서 `fleet.sh` 는 이제 **컨트롤러(srv2)가 아니면 상태 명령을 거부**한다(`require_controller`; `classify|preflight|version|nodes|busy` 는 호스트 무관이라 유지, `FLEET_ALLOW_LOCAL=1` 로만 예외).

**엔진 리스**(`engine/base/fleet_lease.py`, stdlib 전용): 소유자·컨테이너·이유·추정 시간·하트비트를 가진 JSON 레코드, `O_EXCL` 로 원자적 획득. 살아 있음은 **타이머가 아니라 증거** 순서다 —
(1) 머리 노드에 그 컨테이너가 떠 있으면 살아 있음, (2) 아니면 이 호스트의 pid, (3) 아니면 마지막 하트비트로부터 `GRACE_S`(15분). **판정 불가는 점유로 친다**: docker 가 답을 못 하면(`None`) 리스는 유지되고, 옛 평문 잠금은 증거도 하트비트도 없으므로 **영원히 유효**로 읽어 다른 세션의 잠금을 밟지 않는다(D3).
런처는 이 모듈을 **머리 노드로 파이프해서** 돌린다(`python3 - ... < engine/base/fleet_lease.py`) — 리스를 잡는 일이 살아 있는 세션이 마운트한 `$ENGINE_DIR` 을 rsync 로 건드리면 안 되기 때문.

**검증**: 리스 자가검증 통과, 새 `tests/test_engine_fleet_lease.py` 10 tests(한 소유자·증거 우선·판정 불가는 점유·유효기간 지난 것은 한 번만 회수·소유자만 해제·평문 잠금 존중·양쪽 배관), 플릿 정책 22 tests OK. srv2 로의 실제 ssh 왕복으로 `read` → `free` 확인.

### 45차 §28 — 엔진에 결합해야 가능한 것 넷, 그리고 컨트롤러 (2026-09-12, srv4/srv2)

운영자 "엔진과 결합되어서 구현 가능한 신기능" → "전부 구현해" + "컨트롤러도 풀어".

**1. 대화를 잃지 않는 플릿 인계(drain-park-yield)** — 큐의 `yield` 는 프로브에게만, 서빙이 유휴일 때만 양보한다. 부팅 보유자는 양보 자체가 불가능하고 양보하면 진행 중 대화가 사라진다(vLLM 은 보존할 방법이 없다).
ST 는 부품이 전부 있었다: 대화 키 파킹 티어, 보유자 목록, 리스. 이제 `fleet_lease.request_yield()` 로 **요청**하면 엔진이 (a) 신규 승인을 503 으로 거부하고, (b) 받던 요청을 끝내고, (c) 남은 대화를 대화 키로 파킹하고, (d) 리스를 놓고 루프를 끝낸다.
다음 보유자는 그 대화를 그대로 이어받는다(재개는 요청이 대화를 지명할 때 지연 수행 — 이미 있던 경로). 결정은 rank 0 이 폴링해 **스텝의 다른 결정과 같은 브로드캐스트로** 실려 모든 랭크가 같은 스텝에 배수를 시작한다.
**구현 중 잡은 실제 누수**: 거부를 승인 중간에 두었더니 거부된 요청이 `pending` 항목을 남겨 **엔진이 영원히 조용해지지 않았다**(따라서 영원히 못 놓는다). 상태가 생기기 전으로 옮김.

**2. 리스가 엔진 상태를 말한다** — 보유자가 스텝 루프에서 `publish(running, waiting, served, steps)` 로 하트비트와 함께 상태를 쓴다. `held` 한 줄이 "쥐고 있음" 이 아니라 "3 요청 서빙 중" 이 된다. 큐의 ETA 는 제출자가 타이핑한 추정치였는데 이제 실측이 곁에 있다.
동시 쓰기(보유자의 publish vs 대기자의 yield 요청)는 `.mut` 원자 잠금으로 직렬화 — 서로의 필드를 지우지 않는 것을 테스트가 강제한다.

**3. 프로브도 리스를 잡는다** — `run_engine_probe.sh` 가 이름 있는 컨테이너(리스의 증거)로 짧은 리스를 잡고 `trap` 으로 어떻게 끝나든 놓는다. 마지막으로 남아 있던 "런처도 큐도 못 보는 맨 `docker run`" 구멍이 닫혔다. 실측: 첫 프로브 정상, 리스 잡힌 상태의 둘째 프로브는 `ABORT: other on srv4 since ...`.

**4. 빌드 교차 재개** — 3번까지 오면 따라온다: 파킹 키가 대화이고 재개가 지연 수행이므로, 인계로 파킹한 대화는 **다음 보유자가 다른 빌드여도** 레이아웃만 같으면 이어진다(`tier.stale()` 이 다른 레이아웃을 걸러낸다).

**컨트롤러 해제** — srv2 의 `~/stkernel` 은 다른 세션 브랜치(`codex/fleet-onepass-live-20260909`)라 ST 승인 코드가 없었다. 그쪽을 건드리지 않고 **전용 워크트리 `~/fleet-controller`**(origin/main, detached)를 만들었다. 확인: ST 진입점 둘 인식, 러너 핀에 non-bench 8개 포함, `bench/fleet.sh status` 가 **진짜 큐**(마커 파일이 있는 그것)를 답한다.

**검증**: 이미지 안 엔진 스위트 **466 tests OK**(105 skip), 새 `tests/test_engine_fleet_lease.py` 24 tests(리스 7 + 양보 프로토콜 6 + 엔진 인계 6 + 프로브·런처·부팅 배관 3 + 양쪽 배관 2). 리스 자가검증 통과. 프로브 왕복 실측.
**GPU·플릿 미검증**: 실제 두 세션 인계. 컨트롤러가 풀렸으니 다음 창에 큐로 예약해 판정한다.

### 45차 §29 — "완벽하게 스무스하게 됐어?" 로 §28 을 감사: 거친 곳 여섯, 그중 실제 버그 하나 (2026-09-12, srv4)

운영자 질문에 답하려고 §28 을 스스로 감사했다. 답은 **아니오였다**. 찾은 것과 고친 것:

1. **실제 버그 — 프로브의 리스가 다른 파일이었다.** 프로브는 `$HOME/st-fleet.lock` 을 **자기가 도는 노드**에 잡는데 부팅은 머리 노드(srv2)에 잡는다. 홈은 공유가 아니므로 **둘은 영원히 안 만난다** — 프로브 리스가 아무것도 안 막고 있었다.
   → `launchers/lib/fleet-lease.sh` 하나로 합쳐 모든 호출자가 머리 노드의 **한 파일**을 쓴다.
2. **큐가 요청을 안 했다.** `request_yield` 를 만들어 놓고 `fleet.sh` 는 여전히 거부만 했다 → 큐에 선 작업이 **사람이 가서 양보를 부탁할 때까지** 기다린다. → `_try_hold` 가 거부할 때 대기자 이름으로 양보를 요청한다(옛 평문 보유자면 조용히 무시).
3. **`yield` 가 안 기다렸다.** 묻고 끝나서 호출자가 폴링하고 경쟁해야 했다 → 이제 인계가 끝날 때까지 기다리고(`YIELD_WAIT_MINUTES`, 기본 30) 진행 상황을 줄마다 찍는다.
4. **인계가 파킹 실패를 삼켰다**(`except: pass`) — 약속한 바로 그것이 조용히 사라진다 → `parked`/`lost` 를 세고, 잃었으면 찍고, 리스에 `phase="handed over"` 로 남기고, 메트릭 둘로 낸다.
5. **CPU 전용 프로브가 플릿 넷을 잡았다**(`ST_PROBE_NO_GPU=1` 인데도) → GPU 를 안 쓰면 아무것도 예약하지 않는다.
6. **bash 함정 둘**: `VAR=x . file` 의 대입은 **임시**라 헬퍼가 자기 기본값을 못 지킨다(`FLEET_LEASE_SSH: unbound`), 그리고 `LOCK` 이 그것을 쓰는 서브명령보다 **뒤에** 정의돼 있었다 → 설정하고 나서 source, 정의를 앞으로.

**남는 노출(정직하게)**: SIGKILL 당한 프로브는 trap 이 안 돌아 리스가 샌다. 사람 손은 필요 없다 — 증거가 다른 노드라 유예(15분) 뒤 스스로 낡고 `read` 가 그동안 `free (stale: ...)` 로 답한다. 그리고 옛 평문 잠금을 쓰는 보유자에게는 여전히 양보를 요청할 수 없다(그쪽에 듣는 사람이 없다).

**검증**: 이미지 안 엔진 스위트 **478 tests OK**(105 skip), 플릿 스위트 393 tests — 실패 4는 origin/main 과 동일(회귀 0). 리스 테스트 34개(새 8: 한 파일·하트비트·대기·큐가 요청·source 함정·정의 순서·손실 집계·리스 보고).
실측: CPU 프로브 뒤 리스 `free`, GPU 프로브가 머리 노드 리스를 잡고 `held` 가 소유자·이유를 답함.


### 45차 §30 — 문의 스트리밍 디토크나이저: 러스트 디코드 스트림과 프로덕션에서 겪고 고친 복구 둘 (2026-09-12, srv4, GPU 없음, PR #606)

`VLLM_COMPARISON_20260912.md` 가 샘플러·스케줄러·캐시를 훑고 남긴 마지막 영역이 **문이 토큰을 글자로 바꾸는 곳**이다.
vLLM 은 여기를 두 번 고쳐서 지금 모양이 됐다: `tokenizers` 의 러스트 `DecodeStream` 으로 옮기고, 그 뒤 **프로덕션에서 두 번 터져서** 복구를 달았다.
우리는 파이썬 창(두 번 decode 하고 이미 센 몫을 뺀다)을 쓰고 있었고 **복구는 하나도 없었다.**

**가져온 것**(`engine/base/serve._Stream`):

1. **러스트 디코드 스트림.** 스텝의 토큰을 **한 번에** 넘긴다(`step(tok, [id, ...])`) — 스텝당 러스트 호출 하나. `skip_special_tokens` 는 반드시 써야 한다:
   `DecodeStream` 기본값은 **False** 인데 `Tokenizer.decode` 기본값은 **True** 라, 빼면 답변에 `<|assistant|>` 가 조용히 찍히기 시작한다.
2. **복구 A — 토큰 id 가 아닌 id**(vllm#21951). `Tokenizer.decode` 는 음수·초과 id 에 `OverflowError` 를 던진다. 우리 코드에선 그게 **HTTP 스레드에서 올라와 SSE 를 중간에 끊는다**.
   실측: 배치 `step` 은 **인자 변환 단계에서** 거부되므로 스트림 상태가 안 건드려진다 → 한 개씩 재생해서 나쁜 id 하나만 버린다(vLLM 과 같은 정밀도, 배치 비용으로).
   파이썬 창 경로에도 같은 보호를 달았다(`_decode`).
3. **복구 B — Invalid prefix**(vllm#17448). 디코더가 이미 낸 글자를 고쳐 쓰면 스트림의 prefix 가 자기가 든 것을 못 가리키고, **이후 모든 step 이 같은 자리에서 터진다**.
   vLLM 은 빈 스트림을 새로 만들고 이번 토큰만 다시 넣는다 → 들고 있던 토큰의 글자를 **잃는다**. 우리는 **미정산 id 전부**를 그대로 말하고 그것으로 새 스트림을 **프리필**(`DecodeStream(ids=...)`, tokenizers ≥ 0.22)한다 → **잃는 토큰이 없다.**
   이 체크포인트 토크나이저로는 **재현이 안 된다**(id 2~4개 무작위 60,000 회, 0건). 테스트는 `Sequence([Fuse, Replace])` 디코더로 **강제**한다.
4. **vLLM 에 없는 셋째 가드.** 붙잡아 둔 꼬리는 "글자가 나머지를 기다리는 것"이고 그 기다림엔 상한이 있다(UTF-8 은 4바이트). 그보다 길면 기다리는 게 아니라 **막힌 것**이다 —
   외톨이 선두 바이트가 이어지면 **답 끝까지 클라이언트에게 아무것도 안 보이고**, 반복되는 decode 가 그 길이만큼 커진다(실측: 1,600 토큰에서 창 16.4 ms / 스트림 30.6 ms, 둘 다 0 글자).
   `_STALL_TOKENS = 8` 을 넘으면 꼬리를 있는 그대로 말한다. 두 경로 모두.

**그런데 여기가 문의 시간이 아니었다.** 러스트 스트림만으로는 스텝당 −8% 였다. 프로파일이 가리킨 진짜 둘:

- `partial_suffix` 가 **flush 마다 needle 마다 길이마다** 접두사를 새로 만들어 비교했다 → `text` 가 `needle[:cut]` 로 끝나면 `text[-1]` 은 반드시 `needle[:-1]` 안에 있다는 사실로 **한 번의 멤버십 검사**로 끝낸다(동치 증명은 테스트가 무작위 400+전수 조합으로 대조).
- 정지 문자열 스캔이 **매 스텝 답 전체**를 `find` 했다 → 답 길이에 비례해 스텝이 비싸지는 모양. `_scanned` 커서를 두고 **직전 flush 가 통째로 못 봤을 꼬리만** 본다(`floor = max(_stop_from, _scanned - _stop_span)`; 아래로 빠지는 자리는 이전 flush 가 더 낮거나 같은 바닥으로 이미 봤다).

**수치**(srv4, GPU 없음, 이 체크포인트의 실제 tokenizer.json). 상자를 다른 세션과 같이 쓰고 있어서 **전/후를 5회 번갈아 돌리고 각자의 최소**를 쓴다 —
한 번씩만 재면 부하가 그대로 결과가 된다(같은 코드가 라운드에 따라 2배까지 흔들렸다).

스텝당, 스텝당 수락 토큰 수와 정지 문자열 유무별 (µs):

| 수락/스텝 | 정지 없음 전 | 후 | 정지 2개 전 | 후 |
|---:|---:|---:|---:|---:|
| 1 | 1.68 | 1.62 | 4.17 | **2.42** |
| 2 | 2.11 | 1.95 | 4.61 | **2.80** |
| 4 | 2.91 | 2.56 | 5.44 | **3.44** |
| 6 | 3.72 | 3.12 | 6.22 | **3.97** |

4,096 토큰 답 하나, 행 4, k=6, 정지 2개 = 호스트 CPU **17.07 → 10.82 ms**.

**모양이 더 중요하다** — 스텝당 µs, 답 길이별(k=6, 정지 2개):

| 답 길이 | 전 | 후 | 답 하나 전체 |
|---:|---:|---:|---|
| 1,024 | 5.20 | 4.06 | 0.89 → 0.69 ms |
| 4,096 | 5.95 | 4.04 | 4.06 → 2.76 ms |
| 16,384 | **21.51** | **4.29** | **58.73 → 11.72 ms** |

전에는 답이 길수록 스텝이 비쌌다. 이제 평평하다.

(§28·§29 가 먼저 들어와 main 과 병합한 뒤 다시 쟀다 — 그때는 상자가 훨씬 바빠서(load 7.3, 9 users) 양쪽 다 2배 가까이 느렸지만 비율은 그대로였다: 10.14→7.99, 11.47→8.01, 33.56→8.93 µs/스텝. main 의 serve.py 변경(리스 인계)은 디토크나이저 경로를 안 건드린다.)

**계측**: `st:detokenizer_rust_stream`(어느 경로가 서빙했나 — D3 의 규칙은 커널 것이지만 조용히 타는 경로는 아무도 안 보는 경로다),
`st:detokenizer_repairs_total{reason=invalid_token_id|invalid_prefix|stalled}`(건강한 런에는 **시리즈 자체가 없다**).

**검증**: `tests/test_engine_serve.py` 135 tests — 실패 목록이 HEAD 와 **완전히 같다**(사전 존재 12건: 전부 `ModuleNotFoundError: test_engine_tier`, 러너가 `tests/` 를 경로에 안 넣어서).
새 테스트 11개: 두 경로 같은 글자(스텝당 1·3·7), 러스트 토크나이저는 러스트 경로로 간다, 복구 A(네 가지 나쁜 id), 복구 B(강제 재현 + 프리필로 이어짐), 막힌 꼬리(두 경로),
끝의 미완성 글자, 스캔이 답과 함께 안 자란다, 긴 런 뒤의 정지 문자열, `partial_suffix` 동치, 메트릭 둘.
`tests/test_engine_{metrics,glm53,tokenizer}.py` 도 HEAD 와 같은 실패 목록.

**안 한 것**: vLLM 의 네이티브 프리필(`DecodeStream(ids=프롬프트)`)로 채널을 시작하지 않는다. 우리 채널은 프롬프트가 아니라 **reasoning/content 경계**에서 시작하고,
프롬프트로 프리필하면 첫 delta 의 모양이 바뀐다(Metaspace 계열에서 선두 공백). 복구 B 안에서만 프리필을 쓴다 — 거기선 그게 정확히 맞는 도구다.
### 45차 §31 — 구조화 출력: 스텝 하나의 비트마스크, 전용 커널, 그리고 죽은 드래프트 (2026-09-12, srv4, GPU 없음, PR #608)

`VLLM_COMPARISON_20260912.md` §1 의 넷째 항목. 전송(핀 스테이징 + 행마다 한 번)은 이미 고쳐져 있었고, vLLM 이
더 가진 것은 **배치 전체용 비트마스크 버퍼 · 전용 적용 커널 · 배치 128+ 병렬 채우기 · 스펙 오프셋 행 재배치 ·
문법이 거부할 드래프트를 -1 로 죽이기** 다섯이었다. **셋은 가져왔고, 하나는 실측이 반대라 안 가져왔고, 하나는
우리 쪽이 한 단계 앞에서 이미 한다.**

**1. 스텝 하나의 비트마스크** (`base/grammar.Grammars.prepare`). 행마다 `[positions, words]` 스테이징·랜딩을
따로 들던 것을 엔진이 하나 들고, 이 스텝의 문법 행들을 **살아 있는 위치만 촘촘히** 채운 뒤 한 번 건넌다.
행마다 하나씩 있던 블로킹 전송 4 → 1 (302 KiB).

**2. 전용 적용 커널.** `xgrammar.apply_token_bitmask_inplace` 가 패킹된 워드에서 곧장 -inf 를 쓴다. 전에는
비트마스크를 `[positions, words, 32]` 로 펼쳐 bool 어휘를 만들고(`~mask` 한 번 더) `masked_fill` 했다 —
위치마다 어휘를 다섯 번 훑는다. 부팅에서 한 번 워밍한다(`Grammars.warm`): CUDA 백엔드가 Triton 이라 JIT 이
첫 구조화 요청 안에 떨어지면 안 된다(45차 §23 B2 와 같은 규칙).

**3. 포워드 앞으로.** 마스크의 걸음은 **매처의 커밋 상태와 이 스텝의 드래프트만** 필요하고 타깃이 내는 것은
하나도 안 쓴다. 그래서 프리필/디코드의 forward 를 띄우기 **전에** 채우고 전송을 큐에 넣는다 — 호스트의 일과
그 복사가 디바이스 일 밑으로 들어간다. 스테이징 재사용은 이벤트로 막는다(`crossed`): 다음 스텝의 채우기는
지난 전송이 스테이징을 다 읽은 뒤에만 시작한다(버퍼가 커져 **해제**될 때도 같은 순서라 안전).

**4. 병렬 채우기는 안 가져온다 — 실측이 반대다.** GB10 호스트, xgrammar 0.2.6, 어휘 154,880 (스레드 수만
바꿔 같은 걸음을 잰 것이라, 아래 A/B 표와는 걸음이 달라 절대값이 다르다):

| | 직렬 | 2 스레드 | 4 스레드 | 8 스레드 |
|---|---:|---:|---:|---:|
| 4 행 × 6 위치 | **0.023 ms** | 0.114 | 0.146 | — |
| 32 행 × 6 | **0.100** | 0.650 | 1.021 | 1.015 |
| 128 행 × 6 (vLLM 의 문턱) | **0.872** | 4.755 | 4.055 | 3.922 |
| 256 행 × 6 | **1.078** | 6.540 | 7.986 | 7.706 |

이유 둘: (a) `fill_next_token_bitmask` 하나가 **1.3 µs**(웜), 무거운 스키마라도 2.4 µs 라 디스패치가 일보다
비싸다. (b) **GIL 을 안 놓는다** — 한 스레드가 20,000 fill 에 13.4 ms, 네 스레드가 각각 20,000 에 **274 ms**
(fill 당 0.67 → 3.4 µs). vLLM 의 128 문턱은 배치 128 짜리 이야기고 `max_seqs` 는 4 다. **공짜 병렬은 3 번**이다.

**5. 스펙 오프셋 행 재배치는 애초에 없다.** vLLM 은 촘촘히 채운 다음 **배치의 로짓 순서로 되돌려 정렬**한다 —
커널 한 번이 배치 전체를 덮어야 하고 드래프트가 행마다 위치를 밀어놨기 때문이다. 우리는 촘촘히 채우되
**행에게 자기 슬라이스를 준다**(행마다 런치 하나, 배치 ≤ 4). 되돌릴 순서가 없고, 한 행의 마스크가 다른 행의
로짓에 앉을 방법도 없다(테스트가 드래프트 수가 다른 두 행으로 이걸 건다).

**6. "-1 로 죽이기" 는 우리 쪽이 한 스텝 앞이다.** vLLM 은 문법이 거부할 드래프트를 -1 로 덮어 **디바이스
기각 샘플러가** 떨구게 한다. 우리는 걸음에서 그 자리를 알고(`live`), **그 뒤 위치는 게더도 안 한다** —
`_gather` 가 `local[start : start+live]` 만 all_gather 하므로 죽은 위치 하나마다 `[1, V]` 한 줄
(모델 dtype, bf16 이면 302 KiB)의 수집이 통째로 안 일어나고, 페널티·분포·픽도 안 돈다. 커밋은 `block_verify` 가 `drafts[:live-1]` 만 검증하고 마지막 자리를
**마스크된 타깃에서 새로** 뽑으므로, 거부된 드래프트는 제안조차 안 된다(출력 분포는 그대로).

**측정**(한 디코드 스텝 = 4 행 × 6 위치, 어휘 154,880, 마스크당 4,840 워드; 교대 A/B, 최솟값·중앙값):

| | 전 | 후 |
|---|---:|---:|
| 걸음(호스트, 양쪽 다 호스트) | 0.266 / 0.319 ms | **0.055 / 0.059 ms** |
| 걸음 + 로짓에 마스크(호스트 CPU 커널) | 23.8 / 26.0 ms | **4.1 / 6.5 ms** |
| 호스트→디바이스 전송 | 4 | **1** (302 KiB) |
| 마스크가 내는 텐서 연산 | 44 | **4** |

프로덕션에서 전송은 DMA, 적용은 Triton 커널이라 **절대값은 이 표가 아니다** — 옮겨 가는 것은 비율과 개수다.
걸음의 0.055 ms 조차 이제 포워드 밑에 숨는다.

**곁가지 셋**:
- `reset_token_bitmask` 는 **필요 없다**. `fill_next_token_bitmask` 가 받은 행을 통째로 덮는다(더러운 행으로
  확인). 위치마다 19 KB memset 하나(1.6 µs, fill 1.3 µs 보다 비싸다)를 통째로 뺐다.
- fill 의 반환값 `need_apply` 가 False 면 그 행은 **커널을 아예 안 부른다**(문법이 아무것도 안 막는 상태).
- `process_logits(out=...)`: 위치마다 어휘 하나씩 새로 잡던 것을 **한 버퍼의 연속 행**에 쓴다(행 하나 = 커널
  런치 하나의 전제). 쓰이지 않던 `mask=` 인자는 같이 일몰 — 문법 마스크는 이제 커널의 일이다.

**측정 함정 하나**(다음 사람 주의): 호스트에서 `[16, 4840] int32` 한 번 `copy_` 가 **네 번 나눠 하는 것보다
느리다**. ATen 이 원소 수 문턱을 넘으면 스레드를 깨우기 때문이고, 실제 경로(고정 메모리 → 디바이스)는
`cudaMemcpyAsync` 라 반대다. 처음엔 이 인공물 때문에 "스텝 하나 전송이 손해" 로 읽었다.

**검증**: 엔진 CPU 스위트 OK(머지 후 재실행), `tests/test_engine_grammar.py` **14 tests** — 그중 진짜
xgrammar 0.2.6 으로 도는 4 개(스크래치 설치로 호스트에서 CPU 실행: json_object 마스크·롤백, 거부된 드래프트가
행을 끝내는 것, json_schema 강제)와 가짜 모듈로 도는 10 개(행별 슬라이스 교차 금지, 버퍼 유지·상향 성장,
죽은 위치는 안 채움, 문법 없는 행, 안 막는 마스크는 커널 생략, min_tokens 금지 위에 얹히는 것, 소스 모양).
GPU 는 안 건드렸다(프로덕션 서빙 중).
**§29 보충 — 실제로 큐에 태워 보니 셋 다 2초 만에 죽었다**: 컨트롤러(srv2)에 예약 셋을 넣었더니 큐가 **GO 를 주고** 각각 2초 만에 실패해 티켓까지 사라졌다(`held=0m boots=0 records=0`).
로그가 원인을 그대로 말한다 — `hold refused: ST engine occupies the fleet` 세 번 뒤 곧바로 `GO st-graph`, 실행 로그엔 `LeaseHeld: ... (opaque lease record) ... pid=3232025`.

1. **컨테이너와 리스가 어긋났다.** `st_engine_up` 은 컨테이너만 봤는데 앞 세션의 컨테이너는 내려갔고 **평문 잠금은 남아 있었다** → 이제 컨테이너 **와** 리스를 함께 본다.
2. **내 "판정 불가는 점유" 규칙이 죽은 손이 됐다.** 그 잠금의 `pid=3232025` 는 이미 죽어 있었고(확인), 규칙상 opaque 는 영원히 살아 있으므로 사람이 `stop` 을 돌릴 때까지 플릿이 막힌다.
   → 평문에서 `pid=`·`@host` 를 기회적으로 파싱해, **이 호스트의 그 pid 가 없고 `st-*` 컨테이너도 없을 때만** 회수 가능으로 판정한다. 못 물으면(`container_up` 이 None) 여전히 점유다.
3. **프로브가 즉시 포기했다.** 큐가 방금 들여보낸 작업이 앞 세션의 30초 정리 때문에 차례를 잃는다 → `ST_PROBE_WAIT_MINUTES`(기본 10) 동안 기다리며 누가 쥐고 있는지 줄마다 찍는다.

**검증**: 엔진 481 tests OK(105 skip), 플릿 393 중 실패 4 = origin/main 동일(회귀 0), 리스 37 tests(새 3). 평문 잠금 판정은 죽은 pid·살아 있는 pid·다른 호스트·pid 없음 네 경우를 모두 고정한다.

<!-- 45차 절 번호 주의 -->
> **§76 · §77 · §78 은 원래 §30 · §31 · §32 로 적혀 있었다.** 병렬 세션 셋이 각자 "다음 번호"를 골라
> 같은 번호를 두 번씩 쓴 결과였고(§30·§31·§32 가 각각 두 절), 2026-09-12 의 PR 머지 정리 중 **자동 해소기가
> 뒤쪽 셋을 §76~§78 로 바꿨다.** 의도한 변경이 아니라 부수 효과였지만 되돌리면 중복이 돌아오므로 남긴다.
> 그래서 **이 파일에서 "§30/§31/§32" 라고만 적힌 상호참조는 앞쪽 셋**(PR #606 · #608 · #609)**을 가리키고,
> 뒤쪽 셋**(PR #612 · #618 · #622)**을 가리키려던 참조는 지금 §76 · §77 · §78 이다.**
> 절 번호는 PR 직전 `origin/main` 에서 최대값을 다시 읽고 고를 것.

### 45차 §76 — 오늘 것 재검토: 인계가 플릿에서 아예 안 돌고 있었다 (2026-09-12, srv4/srv2, PR #612)

운영자 "오늘 구현한 것 중 거친 부분이나 오류를 한번 더 검토해". 세 개가 **실제 버그**였고 둘은 §28 의 간판 기능을 통째로 무력화하고 있었다.

1. **엔진이 자기 리스를 볼 수 없었다.** 런처가 `ST_LEASE_PATH=/home/choiceoh/st-fleet.lock` 을 컨테이너에 넘기는데 **그 경로는 어느 컨테이너에도 마운트돼 있지 않다**(`-v` 목록: `$ENGINE_DIR`, 랭크, 드래프터, `$CACHE_DIR`, `glm53-logs`).
   따라서 `_yield_asked` 의 `read` 는 항상 None → **양보 요청을 영원히 못 보고**, 상태도 한 번도 못 쓴다. §28 의 drain-park-yield 는 플릿에서 **한 번도 동작한 적이 없다**(로컬 테스트만 통과했다).
   → 정본 경로를 `/home/choiceoh/glm53-logs/st-fleet.lock` 으로 옮겼다. 그 디렉터리는 이미 **컨테이너 안팎 같은 경로로** 마운트된다. (`_write` 가 inode 를 갈아치우므로 단일 파일 바인드 마운트는 애초에 못 쓴다.)
   경로를 옮기면 잠금이 갈라지므로, 런처는 옛 경로에 잠금이 있으면 여전히 거부하고 `stop` 은 둘 다 지운다.
2. **머리 노드가 리스를 못 읽었다.** 헬퍼가 항상 `ssh $FLEET_HEAD` 하는데 **노드는 자기 자신에게 ssh 할 수 없다**(srv2 가 자기 키를 거부; 런처는 이미 `is_self` 로 우회하고 있었다). 실측 `exit=255`.
   큐의 컨트롤러가 바로 그 머리 노드에서 돈다 — 즉 **가장 중요한 호출자가 물어볼 수 없는 쪽이었다**. §29 보충에서 "큐가 리스도 본다"고 고친 것이 컨트롤러에서는 실패하고 있었고, 컨테이너 검사가 그걸 가리고 있었다. → 머리 노드면 로컬로 실행한다.
3. **`release` 가 `publish` 와 같은 잠금을 안 잡았다.** 보유자의 다음 하트비트가 read 와 unlink 사이에 끼면 **해제한 리스가 되살아난다**. 런처의 `stop` 과 엔진의 2초 주기 publish 가 정확히 그 조합이다. → 같은 `_Mutation` 안으로.

**검증**: 이미지 안 엔진 스위트 **509 tests OK**(105 skip), 플릿 393 중 실패 4 = origin/main 동일(회귀 0), 리스 42 tests(새 5: 컨테이너가 읽는 경로·옛 경로 존중·머리 노드 로컬 실행·release 잠금·하트비트가 해제를 되살리지 못함).
**여전히 GPU 미검증**: 실제 두 세션 인계. 이제는 배관이 실제로 이어져 있으므로 다음 창에서 판정 가능하다.
### 45차 §32 — 접두사 캐시 블록 수명: 핀을 버리고 등급 있는 자유 목록으로, 스냅샷만 읽는 복원, 테넌트 솔트 (2026-09-12, srv4, GPU 없음, PR #609)

운영자 지적(vLLM 대조 넷 중 둘째): *"vLLM 은 해제된 블록을 해시를 단 채 침습적 free 리스트에 남겨 나중에 O(1) 로 다시 집는다.
**핀 없이** 재사용을 얻는 것이다. 축출 순서도 두 등급으로 나눠 해시 없는 블록을 먼저 쓰고, 한 요청의 블록은 꼬리부터 풀어 공유
접두사가 가장 오래 살아남게 한다. 우리는 항목이 블록을 핀하고 필요할 때 LRU 로 회수한다. 효과는 비슷하지만 기계가 덜 다듬어져
있다. 테넌트 솔트도 없다."* 전부 맞았다. 특히 "덜 다듬어져 있다"가 **얼마나** 덜인지가 이번에 숫자로 나왔다.

**병리 하나, 실측**: 경계들은 **중첩 사슬**이다(한 프롬프트가 768 토큰마다 하나씩 남기므로 경계 i 는 블록 `[:i]`). 이 모양에서
옛 `reclaim(1)` 은 `_victim()`(가장 오래된 미adopt 경계 = **가장 짧은** 경계)부터 축출하는데, 짧은 경계의 블록은 전부 더 긴
경계도 쥐고 있어 **한 블록도 안 풀린다**. 그래서 **1 블록을 얻으려고 96 개 경계를 전부 파괴**한다(96 블록 · 96 경계 모의, 위 구조 그대로).
새 경로는 **1 개**만 파괴한다 — 자유 목록에서 나오는 블록은 정의상 아무 행도 안 쥔 블록이고, 그 블록을 쥔 경계만 경계이기를 그만둔다.

| 중첩 사슬 96 경계, 블록 1 개가 필요할 때 | 전 | 후 |
|---|---:|---:|
| 파괴되는 경계 | **96** | **1** |
| `pool.available` 한 번 읽는 비용 (96 항목 × ≤170 블록) | **148.8 µs** | **0.063 µs** |

`available` 은 입장마다 읽힌다. 옛 `reclaimable()` 이 **매번 모든 항목의 블록 목록을 훑어 딕셔너리를 새로 지었다**.

**고친 것 넷** (`engine/base/kv.py`, `engine/base/prefix.py`):

1. **캐시는 블록을 핀하지 않는다 — 주장(claim)한다.** 경계는 `pool.claim(blocks, CACHED)` 로 이름만 붙이고, 블록은 자유 목록
   안에 그 이름을 단 채 기다린다. 예약이 그 블록을 실제로 집는 순간 `pool.forget(block)` 이 그 블록을 쥔 경계를 떨어뜨린다.
   축출이라는 **별도 동작이 사라졌다**(`reclaim`/`reclaimable` 콜백 둘 제거). 소유는 여전히 카운트다 — 행만 소유자다.
2. **자유 목록은 등급 넷, 침습적 이중 연결 리스트**(`_next`/`_prev`/`_grade` int 배열, 블록 id 자체에 실을 꿴다): 익명 →
   **바랜(faded)** → 캐시됨 → **운영자 핀**. 앞 등급을 다 쓰기 전에는 뒤 등급을 건드리지 않는다. vLLM 의 "해시 없는 것 먼저"
   와 같은 규칙이고, 스냅샷이라는 둘째 자원 때문에 중간 등급이, 45차 §23 C 의 워밍 핀 때문에 마지막 등급이 하나씩 더 있다.
   (핀이 등급이 된 이유: 블록 압박이 이제 `_victim` 을 안 거치므로, 핀이 "아무것도 없을 때만 나간다"를 지키려면 순서 자체에
   박혀 있어야 한다.)
3. **행은 블록을 꼬리부터 돌려준다.** 목록이 FIFO 라 꼬리가 먼저 나가고 **머리가 마지막에** 나간다. 게다가 머리 블록은 보통
   중첩된 짧은 경계가 `cached` 등급으로 주장하고 있어 한 등급 더 뒤에 선다 — 공유 접두사가 가장 오래 사는 성질이 정책이 아니라
   참조 구조에서 나온다.
4. **바랜 경계 = 스냅샷만 읽는 복원.** 여기 희소한 자원은 블록이 아니라 **스냅샷**이다(96 개, 각 77 MiB; 블록은 수천 개).
   스냅샷 압박으로 쫓겨난 경계가 **티어에 사본이 있으면** 블록을 쥔 채 `faded` 로 남고, 같은 프롬프트가 돌아오면 행이 그 블록을
   **있는 자리에서 입양**하고 티어에서 **스냅샷만** 읽는다(`kv_tier.promote(block_ids=None)` — 블록 구간을 건너뛰고 슬롯 바이트만).
   128K 경계면 ~1 GB 읽기가 **77 MiB** 로 줄어든다. 티어가 없으면 스냅샷을 넘기는 것이 그 경계의 끝이다(전과 같다).

**곁다리로 드러난 버그 하나**: 티어로 쓰는 중(`spilling`)인 경계의 블록을 옛 `reclaimable()` 은 **세는데** `reclaim()` 은
내주지 않았다(`_victim` 이 제외) — `available` 이 부풀고, 예약이 D3 `MemoryError("스케줄러가 예산보다 많이 받았다")` 로
죽을 수 있었다. 이제 스필 중에는 **진짜 핀**을 잡는다. 엔진에 남은 유일한 핀 용도가 그것이다: *전송이 읽는 중인 바이트*.

**테넌트 솔트**(vLLM 의 `cache_salt`). **위치 0 의 솔트**로 넣었다 — 미디어 다이제스트가 이미 쓰던 배관을 그대로 타고,
첫 블록에만 접혀 이후 모든 경계가 물려받는다(vLLM 도 첫 블록에만 넣는다: `kv_cache_utils.py:560-562`). 문 셋과
`/v1/prefix/warm` 이 받는다. **대화 문 둘도 같이 막았다** — 히스토리로 이어가는
`_continuation`, 그리고 `conversation=<id>` 를 직접 대는 길(대화 id 는 작은 정수라 대는 것 자체는 아무 증명도 아니다;
남의 것을 대면 "unknown, live or evicted" 409, 모르는 것과 같은 답). 접두사만 막고 대화 문을 열어 두면 반쪽이다.
파킹된 대화는 재부팅 뒤 테넌트를 알 수 없으므로 **무염 호출자에게만** 이어진다(솔트 쓰는 쪽은 미스 → 프롬프트 전체
재전송인데, 그건 접두사 캐시가 받는다. 누수가 아니라 미스).
루트 무작위화는 여전히 못 한다 — 랭크마다 같은 해시가 나와야 메시지 없이 합의한다.

**검증**(GPU 없음, 호스트만): `tests/test_engine_prefix.py` 21 tests(새로 8: 주장·등급 순서·꼬리부터·한 블록에 한 경계·
바랜 경계의 스냅샷만 읽는 복원·핀은 맨 뒤·테넌트 사슬·테넌트 격리), `test_engine_serve.py` 35 tests(새로 1: 대화의 테넌트 — 문 둘 다),
`test_engine_tier.py` 20 tests, `kv.py`/`prefix.py` 자가검증. 스위트 전체는 origin/main 과 실패 목록이 같다(잔여 실패는
flashinfer·CUDA 의존, 워크트리 대조). `test_engine_glm53.py` 의 블록 순서 단언 하나는 옛 LIFO 스택을 박아 둔 것이라
행을 읽어 쓰도록 고쳤다(의도는 "행의 블록은 연속이 아니다").

**아직 안 한 것**: 경계마다 스냅샷이 필요하다는 제약은 그대로다 — vLLM 처럼 블록 하나하나가 독립 재사용 단위가 되려면
그 블록 끝에서 선형 어텐션 상태를 이어받을 수 있어야 하는데, 그건 모델이 강제하는 벽이다(`VLLM_COMPARISON_20260912.md` §3).
그래서 개선은 **블록 수명** 쪽에만 있고 **경계 입도**는 768 블록 그대로다.

**보충 — SGLang 은 어떻게 하나** (운영자 지시로 조사, 전문은 `engine/SGLANG_PREFIX_20260912.md`). `sgl-project/sglang`
`e91c948` 의 `mem_cache` 를 읽었다. **다른 축이다**: 수명 순서를 자유 목록에 넣지 않고 **라딕스 트리의 모양**이 대신하게
한다 — 리프만 축출 후보이므로 짧은 경계는 애초에 후보가 아니고, 우리가 §32 에서 고친 병리(1 블록에 96 경계)가 표현 불가능하다.
자유 목록 순서는 재사용 가치가 아니라 **페이지 번호**(지역성; `alloc_extend` 커널이 연속을 좋아한다)이고, 해제는
`release_pages` 에 쌓아 뒀다가 모자랄 때만 합쳐 정렬한다. 우리 커널은 블록 테이블로 읽어 연속성을 안 따지므로 그건 안 가져온다.
저들 `lock_ref` 는 캐시의 핀이 아니라 *도는 요청이 쓴다*는 표시 — 우리 `pool.refs` 에 해당한다.

**같은 결론에 독립적으로 도달한 곳**: `mamba_radix_cache.py` 는 노드가 `value`(KV)와 `mamba_value`(선형 상태)를 따로 들고,
락도 LRU 도 둘이며, 상태만 잃은 내부 노드를 **tombstone** 으로 남긴다 — **우리 "바랜 경계"와 같은 물건**이다. `evict_mamba`
는 리프가 아니어도 되고(상태는 부모와 공유되지 않으니까) `evict_full` 은 리프만이라는 비대칭까지 우리와 같다.
**갈리는 곳에서 우리가 낫다**: 저들 tombstone 은 `_match_prefix_helper` 가 후보로 안 삼아 **부활하지 않고**, 자손으로 가는
길목을 살리는 표식으로만 쓰이다 리프가 되면 지워진다. 우리 바랜 경계는 티어에서 **스냅샷만 읽어 되살아난다**.
테넌트도 셋 다 자리가 다른데, SGLang 은 `cache_salt` 를 해시에 안 섞고 키 필드로 두고 불일치를 `ValueError` 로 막는 대신
*"external L3/remote storage keys remain token-only and are outside this contract"* 라고 **원격 저장소는 분리 안 됨을
명시**해 뒀다. 우리 티어 키는 솔트 섞인 해시의 앞 56 비트라 디스크까지 따라간다.

**조사가 꺼낸 갭 하나** (SGLang 과 무관, 우리 것): **한 블록 미만 프롬프트는 자기가 생성한 경계를 하나도 안 남겼다.**
`submit` 이 `if chain:` 으로 빈 체인을 떨어뜨렸고, `_rechain` 은 `_chain` 에 있는 행만 본다. 프로덕션 블록이 768 이니
**채팅 첫 턴 대부분**이 여기 해당하고, 그 대화가 2,000 토큰을 답해도 다음 턴은 전부 다시 프리필한다. 실측(BLOCK 4):
프롬프트 6 토큰 → 경계 `[4, 8, 12, 16]`, 프롬프트 3 토큰 → **`[]`**. 고친 뒤 3 토큰도 1 토큰도 `[4, 8, 12]` 다.
같이 막은 구멍: `_rechain` 은 모델이 `media_marks` 를 주면 솔트를 **모델에서 다시 짓는데**, 테넌트 솔트는 모델이 모르는
것이라 빠졌다. 빈 체인이 안 들어오던 동안은 `extend_chain` 이 위치 0 을 안 건드려 무해했지만, 위를 고치는 순간 실제
구멍이 된다(첫 블록부터 다시 해시하므로). `is_tenant_salt` 로 골라 실어 나른다.

**가져온 것 하나**: 명시적 불변식(`PrefixCache.check()`). 저들이 `full_lock_ref >= mamba_lock_ref` 를 주석과 `sanity_check`
로 못박은 자리에 우리는 *동작*만 있고 *단언*이 없었다 — 블록을 내주는 경로가 `forget_block` 하나뿐이라 지금은 맞지만 둘째
경로가 생기는 날 조용히 깨진다. 이제 네 불변식(등급대로 주장했나 · 블록이 자기 주인을 아는가 · 바랜 경계의 상태가 어딘가에
있는가 · 떠난 경계를 가리키는 블록이 없는가)을 `check()` 가 확인하고, `tests/test_engine_prefix.py` 가 **모든 스텝마다**
부른다. 일부러 깨뜨린 상태 셋을 전부 잡는 것으로 단언이 일한다는 것까지 확인했다.
### 45차 §33 — SGLang 대조: 구조화 출력에서 우리가 틀린 것 셋 (사고·크래시·컴파일 자리) (2026-09-12, srv4, GPU 없음)

운영자 "sglang 의 구현도 조사". `sglang 0.5.19` 의 `srt/constrained/` 전부(2,242 줄)와 스펙 디코딩 쪽 문법
경로를 PyPI 휠에서 풀어 읽고 ST 와 대조했다. 대조표는 `engine/SGLANG_COMPARISON_20260912.md`.

**성능은 §31 에서 이미 따라잡았고(오히려 앞선 항목이 있다), SGLang 이 더 가진 것은 *동작*이었다 — 전부 버그다.**

**1. 문법이 모델의 사고 안에서 시작했다 — 실물 버그.** 템플릿을 실제로 렌더해 확인: GLM-5.3 의 기본 경로
(그리고 `reasoning_effort=low` 도)는 프롬프트가 `<think>` 로 끝난다. `json_object` 문법이 첫 생성 토큰으로
허용하는 것은 **23 개**(`{`, `[`, `{"` …)뿐이고 `</think>`(154842)는 없다. 그래서 **기본 설정 + `response_format`
= 사고 블록 안에서 JSON 을 쓰고 블록이 영영 안 닫히고, 문의 `split()` 이 답 전체를 `reasoning_content` 로
보내고 `content` 가 빈다.** 45차 §22 가 고친 것과 같은 종류가 문법 경로로 재발해 있었다. SGLang 은
`ReasonerGrammarObject`, vLLM 은 `reasoning_ended` 로 막는다.
**고침**: 문이 `options["grammar_after"] = reasoning_end` 를 넣고(그 자리에서 `reasoning` 을 이미 계산한다),
`Matcher` 는 그 토큰이 **커밋될 때까지 잠든다** — 마스크도 매처도 커널도 안 건드린다. 드래프트가 그 토큰을
건너는 스텝만 특별하다: 앞쪽 위치는 `-1` 로 열고 뒤쪽만 가린다(커널이 행을 통째로 적용하므로). 드래프트가
깨운 것은 롤백된다 — 깨우는 것은 커밋이다.

**2. 컴파일 실패가 엔진을 죽였다 — 원격 DoS.** 실측: `pattern: "(a)\1"`(역참조)과 `$ref: "#/definitions/nope"`
는 xgrammar C++ 에서 `RuntimeError`. 그 예외는 `engine.add()` → `_admit()` → `once()` 를 나가고, `once()` 의
`except BaseException` 은 **살아 있는 모든 요청을 중단시키고 다시 던진다** — 잘 만들어진 HTTP 요청 하나로
네 랭크가 같이 죽는다. (옛 xgrammar 는 스키마 안의 NUL 에 **세그폴트**했다; 0.2.6 은 `RuntimeError`. SGLang 이
`_grammar_key_contains_nul` 을 들고 있는 이유다.)
**고침**: `_build` 가 스키마 실패를 `ValueError` 로 번역하고, 문이 `prepare_options` 로 입장 전에 물어 400.
D3 는 엔진의 실패에 대한 규칙이고 남의 스키마는 엔진의 실패가 아니다.

**3. 컴파일이 디코드 루프 위에 있었다 — 283 ms.** 실측(xgrammar 0.2.6): 내장 json 4.3 ms, 40 필드 × 20 enum
15.0 ms, 40 단계 중첩 7.0 ms, **400 갈래 정규식 `{1,20}` 282.8 ms**. 그 시간 동안 돌던 디코더가 전부 선다(D10).
**고침**: 컴파일은 스레드로 가고 **첫 마스크에서** 기다린다. SGLang 은 `all_gather_object` 로 랭크끼리 준비
교집합을 잡지만 우리는 필요 없다 — 문이 결정하고 스케줄은 방송되며, 다른 랭크는 같은 지점에서 막힐 뿐이다.
사고하는 행이면 그 지점이 **사고가 끝난 뒤**라 긴 컴파일이 통째로 숨는다.

**4. (덤) 엔진의 종료 토큰을 문법의 stop 토큰으로 넘긴다.** GLM-5.3 은 우연히 안전했다 — 토크나이저의 eos
`<|endoftext|>`(154820)가 generation_config 의 `[154820, 154827, 154829]` 안에 있다. 겹치지 않는 모델이면
문법이 완성된 뒤 **엔진이 멈추지 않는 토큰만 허용**해 행이 상한까지 같은 토큰을 뱉는다.

**안 가져올 것 — 점프 포워드는 SGLang 자신이 안 쓴다.** `try_jump_forward` / `jump_and_retokenize` /
`outlines_jump_forward.py` 는 전부 살아 있지만 **0.5.19 에서 `srt/constrained/` 밖의 호출자가 0** 이다
(스케줄러의 `check_for_jump_forward` 가 사라졌다). 재토큰화 위험과 스펙 디코딩·radix 캐시 상호작용이 값보다
비쌌다는 뜻으로 읽는다. 우리에겐 더 비싸다(D9 균질 스텝, KDA 상태, 드래프터 링). **대신 우리 모양의 후보**:
문법이 한 토큰만 허용하는 자리를 **드래프트로 쓰기** — 타깃이 검증하므로 정확성 위험 0, 스케줄러 무변경.
측정 전에는 안 넣는다.

**그들과 같아서 확인만 한 것**: 배치 하나짜리 비트마스크, 핀 + `non_blocking`(주석의 이유까지 같다), 적용
커널(그들은 xgrammar v0.1.17 커널을 베껴 벤더링), **호스트 일을 포워드 밑으로**(그들 주석: *"every step here is
host work, so it all overlaps that forward"* — §31 에서 우리가 한 것과 같은 말), 드래프트 걸음 + 롤백,
컴파일 캐시 + 행마다 새 매처.

**우리가 다른 쪽**: 랜딩 버퍼 재사용 + 이벤트 가드(그들은 스텝마다 새 디바이스 텐서), 행별 슬라이스(재배치 없음),
죽은 드래프트는 게더도 안 함, 문법 종료 시 그들은 `FINISH_MATCHED_TOKEN` 으로 즉시 끝내고 우리는 강제된
stop 토큰을 실제로 한 번 더 뽑는다(토큰이 실제로 나가는 쪽이 계산·스트림과 일관).

**덧 (12:50) — 디바이스 판정은 창이 아니라 부팅에 얹는다.** 창을 잡으러 갔더니 12:35 부팅의 주인이
**프로덕션**이었다(codex PR #570 핀 릴리스 `5734b29fde84` + srv2 `st-glm53.service` 슈퍼바이저; 운영자 지시로
vLLM 은 복귀하지 않고 지금 GLM-5.3 프로덕션이 ST 엔진이다). 그 릴리스는 커밋 5734b29f(06:34) 라
`engine/base/grammar.py` 가 없고 `boot.py` 의 `grammars(` 도 0회다 — **지금 프로덕션은 `response_format` 을
아예 서빙하지 않는다.** 즉 §31·§33 은 프로덕션에서 터지는 버그가 아니라 다음 릴리스가 켤 기능이고, 별도
다운타임 창을 쓸 이유가 없다(운영자 판단: 다음 릴리스에 얹는다).

그래서 **부팅이 스스로 증명하게 했다**: `Grammars.warm` → **`Grammars.qualify`**(`vision.qualify` 와 같은 모양,
D3). 내장 JSON 문법의 첫 마스크를 실제로 만들어 커널에 걸고, **디바이스 커널의 판정과 같은 워드를 호스트에서
펼친 것을 비교**한다. 다르면 부팅이 죽는다. 마스크가 전부 허용이거나 전부 금지여도 죽는다 — JSON 시작에서
그건 토크나이저와 헤드가 어휘를 다르게 보고 있다는 뜻이고, 이후 모든 마스크가 그걸 조용히 물려받는다.
부팅 단계 이름도 `qualify grammar` 로 맞췄다.

**부팅에 더해지는 값**(실제 GLM-5.3 토크나이저, 호스트 CPU): 토크나이저 로드 1.84 s 와 `TokenizerInfo` **923 ms**
는 **전부터 있던 비용**이다(`Grammars.__init__` 이 이미 냈다). `qualify` 가 새로 내는 것은 **내장 JSON 문법
컴파일 22 ms** + 마스크 하나(fill 115 µs) + 커널 한 번, 그리고 첫 부팅의 Triton JIT 뿐이다 — JIT 은 트리톤
캐시가 영속이라 그다음 부팅부터 0 이고, 애초에 그걸 요청 안에서 내지 않으려고 여기로 옮긴 것이다.
그리고 그 마스크는 **154,880 중 23 개만 허용**한다 — 이 값이 1 이거나 154,880 이면 부팅이 죽는다.

프로브 둘도 남겼다(다음 릴리스 부팅에 얹는다): `probes/st_structured_output.py`(게이트 넷 — 기본 사고 경로 +
`response_format` 이 content 를 내는지, json_schema 강제, 사고 끔, **컴파일 불가 스키마가 400 이고 엔진이
사는지**; 예산 소진은 FAIL 아닌 INCONCLUSIVE), `probes/engine_grammar_gpu.py`(GPU 하나·모델 없음·~2 GiB —
커널·전송·이벤트를 증명하고 옛 경로 대비 가격까지 잰다; 플릿이 필요 없다).

**검증**: 엔진 CPU 스위트 **510 tests OK (120 skip)**. `tests/test_engine_grammar.py` 21 tests(새 7: 컴파일
실패는 ValueError, 컴파일은 스레드·첫 마스크가 기다림, 엔진 종료 토큰 = 문법 stop 토큰, 잠든 행은 아무것도
안 쓴다, 드래프트가 사고를 끝내면 뒤만 가리고 깨움은 롤백, 커밋만이 깨운다, 사고 뒤 토큰만 문법에 먹인다),
`tests/test_engine_serve.py` 85 tests(새 2: 문이 `grammar_after` 를 넣는다·안 넣는다, 컴파일 실패는 400 이고
엔진은 산다). 진짜 xgrammar 0.2.6 은 스크래치 설치로 호스트에서 CPU 실행. GPU 는 안 건드렸다.
### 45차 §77 — 오늘 것 말고: 플릿 큐 자체를 조사 (2026-09-12, srv4/srv2, PR #618)

운영자 "오늘 한 것 말고도 플릿 예약 기능 중 거칠고 잘 안 되는 거 조사해". 넷 나왔고 하나는 **큐의 절약 기구 전체가 꺼져 있다**.

**1. 해시 핀 하나가 좁히기 기구 전체를 끄고 있다(09-11부터).** `cpu_contracts.audited(root)` 는 `tests/test_logic.py` **한 파일의 sha256** 이다 —
핀은 `40e681a1e7a3`(09-07 #445에 기록), 실제는 `ff9b34d0fbcc`(09-11 다른 세션이 utf-8 커밋으로 바꿈). 그 하나가 거짓이 되면 함께 죽는 것:
- `cpu_evidence` 의 세 축소 범위(`audited-logic`/`audited-contracts`/`audited-fleet`)가 전부 **`full-tree` 로 후퇴** → CPU 증거 키가 트리 전체라 **문서·측정 파일 커밋 하나가 캐시를 통째로 무효화**한다.
- `changed_contracts()` → `None` → 플래너가 계약 변경을 한 스위트로 **못 좁힌다** → `checks-math` 대신 `checks-math, sensitivity, gpu` 전부를 계획한다(= GPU 시간).
- `dependencies()` → `None`.
**유일한 신호가 실패하는 테스트 4개뿐이고, 나(오늘 하루 종일)를 포함해 모두가 "기존 실패"로 이름 붙이고 지나갔다.** 조치는 한 줄(핀 갱신)이지만 그 감사는 **사람이 변경을 본다**는 뜻이므로 내가 안 건드린 파일을 내가 축복할 수는 없다 → 운영자 판단.
(핀 넷 중 `tests/test_fleet_onepass.py` 드리프트는 **오늘 내가 만든 것**이라 그건 갱신했다.)

**2. 다른 노드에서 죽은 보유자가 플릿을 `3 × est` 분 막는다.** `holder_alive` 는 보유자가 이 호스트면 pid 를 보지만, **다른 호스트면 증거 없이 `3 × est` 동안 살아 있다고 믿는다**. est 40 이면 2시간, 120 이면 6시간.
그리고 헤더가 약속한 복구(`300초 무활동 뒤 중앙 컨트롤러`)는 **설치돼 있지 않다** — srv2 에서 `fleet-idle-recovery.timer` 가 `not-found/inactive`(유닛 파일은 `launchers/` 에 있다). 게다가 `fleet_idle` 의 일은 "조용해진 뒤 프로덕션 복구"지 죽은 보유자 걷어내기가 아니다.
사람이 `kick --force` 를 돌리는 것 말고 길이 없다.

**3. 부팅 허가의 9.5% 가 부팅을 못 냈다.** 원장 466건을 종류로 가르면: `nogpu` 206(전부 boots=0, **정상** — CPU 작업), `probe` 39(38건 boots=0, **정상** — 도는 서버를 재사용), `boot` **222건 중 21건(9.5%)이 boots=0**.
(첫 집계에서 57% 로 읽었는데 그건 종류를 안 가른 오독이다 — 정정한다.)

**4. 잔해가 안 치워진다.** 09-06 이후 하트비트 파일 97, `launches` 303, `preparations` 181, 핀된 러너 62(62 MB). `fleet_observation_cleanup` 은 진단용 클론을 지우는 것이지 큐 잔해가 아니다. 급하진 않지만 아무도 안 지운다.

**권고 순서**: (1) 핀 갱신 여부 판단(한 줄, 효과가 가장 크다) → (2) `fleet-idle-recovery.timer` 설치 또는 원격 보유자에게도 증거 기반 판정 → (3) 잔해 정리 주기 → (4) boots=0 21건의 사유 분류.

### 45차 §34 — 샘플러 커널: 정렬을 빼고 임계값을 찾는다, 배치 한 벌로 (2026-09-12, srv4, GPU 있음 / 모델 없음)

운영자 지적(셋째 항목): *"vLLM 은 multinomial 을 아예 안 씁니다. 지수 잡음 Gumbel-max 로 동기화를 없앴고, 정렬 없는
top-k/top-p 를 통계 기반 피벗 이분 탐색으로 합니다. 배치 전체를 한 벌로 처리합니다. 우리 리치 경로는 위치마다 파이썬
루프에 정렬 기반 top-p 입니다. 다만 우리 배치가 넷이라 절대값 차이는 작습니다."*

**둘 중 하나는 우리 상자에서 다르게 나왔다. 절대값 차이는 작지 않았다.**

#### 먼저 잰 것 — 전제 확인 (`probes/engine_sampler_bench.py`, 24 행 × 154,880 어휘 = 14.2 MB fp32)

| 잰 것 | 수치 | 판정 |
|---|---:|---|
| `torch.multinomial` 이 호스트로 넘어가나 (`set_sync_debug_mode("error")`) | **안 넘어간다** | vLLM 이 Gumbel 을 쓰는 이유(동기화)는 **torch 2.12.1+cu130 의 우리에겐 해당 없음** |
| `torch.multinomial` 이 CUDA 그래프에 잡히나 | **잡힌다** | 위와 같음 |
| `probs.sort(descending)` | **3,138 µs** | 여기가 진짜 비용이다 |
| 스트리밍 패스 1회(`max`) | **36.5 µs** | 정렬 하나 = 패스 86회분 |

그래서 **지수 잡음은 우리 문제의 답이 아니다**(오히려 행마다 어휘 크기의 난수를 더 만든다). 문제는 정렬이고,
정렬이 하는 일은 **임계값 하나를 찾는 것**뿐이다.

#### 한 것 — `engine/kernels/sampler.py`, 한 커널

`w = exp((logit − max)/T)` 는 (0, 1] 이고 **양수 float 은 비트 패턴이 값 순서와 같다.** 그래서 후보 구간은 정수 구간
`[0, 2^30)` 이고, 임계값은 그 안의 정수 이분 탐색이다 — 근사가 아니라 **정확히 끝난다**. 한 라운드가 스트리밍 패스
하나이므로 라운드당 임계값을 **넷씩**(4-way) 물어 **15 라운드**로 끝낸다. 라운드당 폭을 16 으로 올리면 누산기가
레지스터를 넘겨 되레 느려진다(측정: split 4 = 343 µs, split 8 = 793 µs, split 16 = 2,785 µs @ BLOCK 4096).

뽑기는 **행마다 uniform 한 개**로 하는 누적 걷기(inverse CDF)다. vLLM 이 Gumbel-max 로 행마다 V 개의 지수 난수를
만드는 건 torch 연산을 조립하느라 그런 것이고, 커널 안에서는 누적 걷기가 이미 거기 있다.

top-k 도 같은 탐색(질량 대신 개수), top-p 는 그 위에 얹힌다 — 아래 `tau_k` 에서는 꼬리가 이미 남은 질량 전부라
전체 구간을 다시 훑어도 **`tau_k` 아래로는 내려갈 수 없다**. 브래킷을 둘 둘 필요가 없다.

#### 수치

| | 전 | 후 | 배수 |
|---|---:|---:|---:|
| 배치 정렬 + multinomial (캡처·파이프라인 경로) | **4,437 µs** | **467 µs** (분포만) | **9.5×** |
| 한 행씩 정렬 + multinomial (**리치 경로**) | **11,608 µs** | **632 µs** (분포+id) | **18.4×** |
| bf16 로짓을 그대로 읽을 때 | — | 457 µs | (`.float()` 사본 15 MB 소멸) |
| top_k + top_p 둘 다 (탐색 2회) | 4,437 µs | 1,074 µs | 4.1× |

**측정 조건**: 위 표는 상자가 조용할 때(다른 컨테이너 없음). 옆 세션이 `st-glm53` 을 띄운 뒤 재측정하면 커널
467 → 512 µs, 정렬 4,437 → 5,014 µs, 리치 경로 11,608 → 6,576~13,262 µs 로 흔들린다 — **한 행씩 도는 경로가
런치 바운드라 경합에 가장 민감하다.** 배수는 9~10× / 9~19× 로 유지된다. 자릿수 차이라 잡음 위다.

**리치 경로 11.6 ms 는 디코드 스텝 하나 값이다.** 배치가 넷이어도 스펙 디코딩이 행을 6배로 만들고(4×(K+1)=24),
어휘가 154,880 이라 행마다 도는 정렬이 스텝을 삼킨다. 운영자가 "절대값 차이는 작다"고 한 전제만 틀렸고, 나머지는 맞았다.

#### 판정 — 두 정의가 같은 샘플러인가

`base/sampler._rows_by_sorting` 이 **정의**(정렬로 쓴 것)이고 커널이 **탐색**이다. 1,800 케이스 스윕
(V ∈ {154880, 4096, 777} × scale ∈ {0.5, 2, 8} × top_p ∈ {0.3…0.99} × 40 시드):

- 두 마스크가 다른 토큰: **최대 2개**
- 그 토큰들이 지는 질량: **최대 3.17e-5**
- 둘이 합의한 토큰의 확률 차: **최대 1.44e-6**

경계에서만 다르고, 그 폭은 **참조 자신의 fp32 누적합 오차와 같은 자릿수**다. 두 합산 순서가 다른 이상 그보다 더 붙일
수 없고, 이 숫자가 `tests/test_engine_sampler_kernel.py` 의 상한이 됐다.

**타이 정책은 정정이다.** top-k 는 원래 값 임계값이라 동점을 다 남겼고, top-p 만 정렬 접두사라 동점을 정렬 순서로
갈랐다 — 그런데 그 순서를 torch 가 보장하지 않는다(`modules/vocab.topk` 가 그렇게 적어 뒀다). 이제 **둘 다 값
임계값**이고 동점을 다 남긴다. 랭크 사이 합의가 정렬의 타이브레이크에 매달려 있지 않게 됐다.

#### 딸려 없어진 것들

- **`torch.multinomial` 이 엔진에서 사라졌다** — 커널, 드래프터의 16 후보 워크, `block_verify_batch` 의 복구 뽑기까지.
  드래프터 워크는 행마다 **크로싱 10 → 1**(`int(multinomial().item())` + `int(cand[pick].item())` × K).
- **리치 경로에 위치 루프가 없다.** 스텝의 모든 rich 행·모든 위치가 블록 하나에 처리되어 **커널 1회**로 나온다.
  `process_logits(..., into=)` 가 그 블록의 행에 직접 쓰므로 위치마다 어휘 크기 텐서를 새로 만들지 않는다.
- **all-gather 가 스텝당 1회.** 전에는 rich 행마다 하나였다 — 집합통신 횟수가 "요청이 옵션을 달았는가"의 함수였다.
- **`nucleus` 라는 호스트 술어가 없다.** 절단이 행마다의 숫자라 캡처가 한 판이면 된다(D2).
- **`decodable` 마스킹 사본이 없다.** `-inf` 를 써 넣던 15 MB 복사가 `valid` 폭 하나로 바뀌었다.
- **rich 의 정의가 좁아졌다**: `needs_rich_sampler` 에서 `top_p`·`top_k` 를 뺐다. **다만 문 넓이를 정확히 적으면**
  — 드래프터가 켜진 정상 스텝에서는 `drafts and temperature > 0` 이 여전히 rich 로 만든다(기각 샘플링이 확률을
  요구하니까). 이 변경이 실제로 먹는 자리는 **드래프터가 없을 때(K=0)와 파이프라인이 못 돌 때**(드레인·행 합류·
  그래프 없음)다 — 그때 `temperature + top_p` 행이 캡처 샘플러로 간다. 파이프라인 쪽 실효 변화는 따로 있다:
  `_plain_ahead` 에 `top_k` 를 더해서 **top-k 요청도 호스트보다 앞서 돌 수 있게** 됐다(전에는 top_k 하나로
  동기 경로로 떨어졌다). 파이프라인이 top_k 를 행마다 들고 다니게 된 것이 그 전제다.

#### 아직 안 한 것 (다음 레버, 순서대로)

1. **뽑기 패스의 `tl.cumsum`**: 절단 없는 행 기준 패스 둘이 317 µs 인데 바닥은 ~72 µs 다. 타일 합을 먼저 구해
   목표가 든 타일만 스캔하면 없어진다.
2. **거부 표집**(FlashInfer 방식): 분포가 필요 없는 행(스펙이 아닌 rich 행, `sample()`)은 임계값을 구할 필요가
   없다 — 뽑고 나서 "핵 안인가"만 물으면 기대 **2~3 패스**(~90 µs)다. 지금 15 라운드는 **분포를 내놔야 하는**
   스펙 경로 때문에 치르는 값이다.
3. **스펙 경로가 [n, K+1, V] 를 통째로 요구하는 것 자체**: 필요한 건 드래프트 K개의 확률 스칼라와 복구 행 하나뿐이고,
   드래프터 분포는 16개만 0이 아니다. 이걸 풀면 1 도 2 도 같이 없어진다(버퍼 27 MB 도).

#### §31(구조화 출력)과 겹친 자리

같은 날 다른 세션이 문법 비트마스크를 커널로 옮기면서 `process_logits` 에 목적지 인자(`out=`)를 붙이고 `mask=` 를
뺐다 — **내가 `into=` 로 같은 것을 따로 만들고 있었다.** 병합은 그쪽을 취했다. 합친 결과가 오히려 더 낫다:
스텝 블록이 이미 "한 행의 위치들이 연속" 이라 §31 의 `_positions` 전용 버퍼가 **필요 없어졌고**(`_rich_block` 하나가
둘을 다 한다), 마스크 적용은 행마다 `masks.apply(seq, block[at:at+live])` 한 번이다. §31 의 **죽은 위치 잘라내기**
(`live`)도 그대로 산다 — 이제 게더가 **스텝당 하나**이면서 죽은 행은 안 실어 보낸다(연속이면 뷰, 아니면 `index_select`).
검증은 `block_verify`(§31 이 채택한 블록 검증)로 간다.

**서빙 판정은 `probes/st_sampler_serving.py` 에 있다** — 한 부팅으로 되는 모양으로 짰다(엔진이 프로덕션이라
창은 예약이 아니라 배포다): 비교는 **그리디 행 대 확률 행**이다(그리디는 캡처된 argmax 라 커널에 안 들어가고,
확률 행은 커널이 전부다). 정확성 셋 중 결정적인 것은 **`top_k=1` 이 온도와 무관하게 temperature 0 과 같은 글**
이어야 한다는 것 — 임계값 탐색이 한 자리라도 틀리면 실제 154,880 어휘·실제 로짓에서 즉시 갈라지고, 그게 CPU
테스트가 못 닿는 부분이다. 나머지 둘은 시드 재현(D12, 네 랭크 합의를 겸한다)과 `top_p=0.01` 의 그리디 추종(보고만).

**검증**: `tests/test_engine_sampler_kernel.py` 16 tests(정의 4 + 커널-정렬 대조 + 호스트 크로싱 0 + 그래프 캡처),
기존 `test_engine_sampling` / `_options` / `_async_sampling` / `_pipeline` / `_graph_contracts` / `_drafter` OK.
엔진 스위트 전체 회귀 0 — 실패하는 파일들은 **HEAD 에서도 같은 수로 실패**(임시 체크아웃으로 대조: glm53 1F/6E,
causal_conv·conv_ring·execution·fp4·kda_* ·prefix·serve·state). **스텝 안에서 이 몫이 얼마인지는 안 쟀다** —
모델이 안 떠 있고, **창이 없는 이유가 바뀌었다** — GLM-5.3 프로덕션이 이제 ST 엔진이다(릴리스 `5734b29f`,
09-12 06:34; vLLM 은 운영자 지시로 복귀 안 함). 그래서 GPU 판정은 플릿 예약이 아니라 **배포 창 + 다운타임 승인**
이고, 그건 운영자 결정이다. 덧붙여 그 프로덕션 릴리스에는 `engine/base/grammar.py` 도 `pipeline.py` 도 없다 —
**오늘 main 에 들어간 것 전부가 아직 프로덕션에 없다**. 커널 수치는 실측, 스텝 비중은 다음 배포에 얹는다.

### 45차 §36 — 같은 부분의 SGLang 구현 조사, 그리고 그 조사가 찾아낸 회귀: 한국어 답이 잘려 나가고 있었다 (2026-09-12, srv4, GPU 없음)

운영자 "해당 파트 sglang 구현 조사". SGLang 0.5.19 휠(`srt/managers/detokenizer_manager.py` 560줄 + `schedule_batch.Req`)을 읽었다.
**조사만으로 끝나지 않았다 — 비교하려고 만든 측정이 §30(문의 스트리밍 디토크나이저)이 main 에 넣은 회귀를 찾아냈다.**

#### 1. SGLang 은 무엇을 하나

- **러스트 `DecodeStream` 을 안 쓴다.** 전 소스 grep 0건(겹치는 이름은 전부 CUDA 스트림). vLLM 의 **옛 경로**(surr/read 오프셋 창)를 그대로 들고 있고 주석이 출처를 적어 뒀다(`Based on vllm .../detokenizer.py#L194-L313`). `INIT_INCREMENTAL_DETOKENIZATION_OFFSET = 5` — 프롬프트 끝 5토큰을 문맥으로 깔고 시작. 별도 선택지로 `--tokenizer-backend fastokens` 가 있다.
- **디토크나이저가 별도 프로세스**(`sglang::detokenizer`, ZMQ 로 스케줄러 ↔ HTTP). GIL 을 아예 안 나눈다. **우리 레버가 아니다**: §30 측정으로 문의 스트리밍은 4행 16 µs 대 46 ms 스텝 = 0.03%, 대가는 `BatchTokenIDOutput` 40여 필드를 매 스텝 직렬화하는 것. 동시성이 높을 때 갚는 설계다.
- **배치 디코드**: `_grouped_batch_decode` 가 배치 전체를 `tokenizer.batch_decode` 한 번으로((skip_special, spaces) 별 그룹, 빈 구간은 빼고 되돌려 놓는다). 우리는 행마다 `step` 하나 — `max_seqs=4` 에서 1~2 µs 차이라 안 가져온다.
- **우리 복구 A 에 해당하는 것**: `_clamp_decode_ids` 가 범위 밖·음수 id 를 **0 으로 클램프**(사전 방어). 이유가 다르다 — 멀티모달 자리표시 id 가 **음수**(-101/-102). **우리는 해당 없다**(ST 비전 자리표시는 어휘 안의 `image_token_id` 이고 프롬프트에만 있다). **복구 B 에 해당하는 것은 없다** — `DecodeStream` 을 안 쓰니 그 실패가 없다.
- **정지 문자열은 §30 에서 우리가 한 것을 이미 하고 있었다**: `_stop_match_tail_len` 이 `stop_str_max_len + 1 + (새 수락 토큰 − 1)` 만큼의 **꼬리 토큰만** 디코드해 본다(스펙 디코딩 주석까지 같다). 차이 둘 — 그들은 매 스텝 꼬리를 다시 디코드하고 우리는 이미 만든 문자열을 창으로 본다; **부분 접두사 보류가 그들은 스텝 전체를 막는다**(`should_output &= not check_match_stop_str_prefix()`), 우리는 겹치는 꼬리 글자만 접는다. 이건 우리가 곱다.
- **그들만 하는 것**: 스텝의 글자가 중간에 끊기면 오프셋을 커밋하지 않은 채 **출력 가능한 앞부분만** 내보내고(`find_printable_text`), `sent_offset`/`pending` 으로 중복 송신을 막는다. 우리와 vLLM 은 그 스텝을 통째로 붙잡는다.

#### 2. 그 비교가 드러낸 것 — §30(디토크나이저)의 회귀, main 에 이미 들어가 있었다

세 정책을 같은 토큰으로 재려고 실제 `_Choice` 를 돌렸더니 **스트리밍된 글이 전체 디코드와 안 맞았다.** k=1 은 맞고 **k=6 만 틀렸다.**

- **원인**: §30 의 `_STALL_TOKENS = 8` 은 "한 글자는 4바이트니 바이트폴백 토큰 4개"라는 바이트 논리로 고른 값인데, **스텝은 토큰을 여러 개씩 커밋한다.** 한 바이트 모자란 꼬리가 이미 한 스텝(6~8토큰) 길이고, 두 스텝이면 12~16 > 8 → 가드가 **한 바이트 남은 글자를 포기하고** 꼬리를 U+FFFD 째로 커밋한 뒤 스트림을 새로 만든다.
- **한글은 한 음절이 3바이트다.** 엣지 케이스가 아니라 **스펙 디코딩 하의 모든 한국어 답**이다. 실측: 888자 한국어 답에 **U+FFFD 48개**가 단어 한가운데 박혀 클라이언트에 도달(20회 발동). CJK 도 같다. **영어·코드는 바이트 단위로 무사** — 그래서 안 보였다.
- **왜 테스트가 못 잡았나**: 스트리밍 글 테스트가 **전부 한 토큰씩** 먹였다. 배치 스텝은 §30 이 성능을 위해 도입한 바로 그것인데, 그 모양으로는 아무도 글을 안 봤다. `armed != serving` 의 디토크나이저판이다.
- **고침**: 상한은 64토큰이고 **정확성 상한이 아니다** — 붙잡힌 스텝이 반복하는 decode 가 무한히 커지지 않게 하는 것뿐(가장 넓은 스펙 스텝 8개분). 글자의 기다림을 끝내는 것은 글자다. 빠져 있던 테스트를 넣었다: 다국어 본문 × 스텝당 2·3·5·7 토큰 × 두 경로, 보인 글 대 전체 디코드.

#### 3. 그리고 SGLang 의 아이디어를 글자 경계로 (운영자 승인, "제안대로 별도로")

`_Stream` 이 `text`(확정)와 `_ahead`(그 앞의 온전한 글자들, 보였지만 미확정)를 들고 `decoded` 가 둘을 합쳐 돌려준다 — 합은 **줄지 않으므로** 델타가 두 번 나가지 않는다.
**SGLang 의 휴리스틱은 한국어에 안 맞는다**: `find_printable_text` 는 HF `TextStreamer` 것이라 **마지막 공백**까지 자르고, CJK 예외인 `_is_chinese_char` 는 그 주석이 직접 *"모던 한글은 다른 블록이고 공백으로 띄어 쓰니 특별 취급하지 않는다"* 고 말한다 → 진행 중인 단어를 통째로 붙잡는다. **우리는 글자 경계로 자른다.**

**아무것도 안 보여 주는 스텝의 비율**(실제 tokenizer.json, 실제 `_Choice`):

| 본문 | 스텝당 수락 | 전 | SGLang 휴리스틱 | 후(글자 경계) |
|---|---:|---:|---:|---:|
| 한국어 | 6 | **37.2%** | 0.0% | **0.0%** |
| 한국어 | 1 | 28.1% | 18.7% | **15.6%** |
| CJK+이모지 | 6 | **43.5%** | 0.0% | **0.0%** |
| CJK+이모지 | 1 | 29.5% | 14.8% | **14.8%** |
| 영어·코드 | 1·6 | 0.0% | 0.0% | 0.0% |

**안전장치**: 확정된 부분을 뒤에 온 미완성 바이트가 **다시 쓰는** 디코더(바이트폴백은 한 바이트만 모자라도 런 전체를 U+FFFD 로 만든다)는 접두사 검사에 걸려 **조기 출력을 못 받는다** = 예전 동작. 보인 것을 되돌리는 일은 없다. (이 함정도 실물로 만났다 — 첫 구현이 바이트폴백 픽스처에서 글을 부쉈다.)

**비용**: 붙잡힌 스텝에서 꼬리를 문맥과 함께 두 번 디코드 — 한국어 3.74 → **5.40 µs/스텝**, 영어 3.79 → 3.79. 4행이면 21.6 µs 대 46 ms 스텝 = **0.05%**.

**검증**: `tests/test_engine_serve.py` 139 tests, 실패 목록이 main 과 동일(사전 존재 12). 새 테스트 4개(다국어 배치 회귀, 중간에 끊긴 스텝이 끝낸 글자를 보인다, 보인 것은 안 바뀐다 × 스텝당 1·2·4·7, 재기록 디코더는 조기 출력 없음).

**덤 — 번호가 계속 밀렸다**: 이 조사를 §31 로 적어 `ostcode/rust-discord-stream-fast-path-4a70bd` 에 밀었는데 PR #606 이 **그 커밋 직전 상태로 머지**돼서 원장에 안 들어갔고, 그 뒤로 브랜치를 main 에 맞출 때마다 다른 세션이 다음 번호를 이미 쓰고 있었다(네 번). 그래서 §36 이다. 교훈 둘: 머지 대기 중인 브랜치에 늦게 미는 커밋은 PR 헤드가 이미 지나갔을 수 있으니 **푸시 뒤 PR 헤드를 확인할 것**, 그리고 여러 세션이 같은 날 원장을 쓰면 번호는 **머지 직전에** 정할 것(지금 main 에 §30·§31 이 둘씩 있다 — 내 것이 아니라 건드리지 않았다).

### 45차 §37 — 부팅 뒤 첫 요청이 `add` 에서 죽었다: 지연 생성된 페널티 히스토리에 가드가 둘 빠져 있었다 (2026-09-12, srv4, GPU 없음)

§32 의 스위트를 현재 main 과 대조하다 `tests/test_engine_glm53.py` 의 실패가 **6 → 9** 로 늘어난 것을 봤다. 내 브랜치
탓인 줄 알고 `origin/main` 을 그대로 풀어 돌렸더니 **거기서도 같은 셋이 실패**했다. 즉 회귀는 main 에 있었다.

```
engine/profiles/glm53/adapter.py:375   self.history.forget(seq)
  → AttributeError: 'NoneType' object has no attribute 'forget'
```

`self.history`(`base/sampler.History`)는 **지연 생성**이다 — *"built at the first row that needs penalties"*,
실제로는 `_row_logits` 가 처음 페널티를 쓸 때 만들어진다. 행의 토큰을 **통째로 갈아치우는** 자리가 셋인데
(`add`·`forget`·`resume`), 25cd3f06 이 히스토리 버리기를 더하면서 **`forget` 에만 `if self.history is not None:` 을
달았다**. 나머지 둘은 맨손으로 갔다. `add` 는 **모든 새 요청이 지나는 자리**이고, 페널티를 아직 아무도 안 쓴 부팅은
`self.history` 가 None 이다 — **부팅 뒤 `repetition_penalty` 없는 첫 요청이 거기서 죽는다.** 서빙 경로 한복판이다.

**고침**: 가드를 세 자리에 복사하는 대신 `_forget_history(seq)` 하나로 모았다(네 번째 호출자가 생겨도 같은 실수를
못 한다). 지연 생성이라는 사실을 아는 자리를 하나로 줄이는 것이 요점이다.

**검증**: glm53 스위트 **10 → 7** 실패(사라진 셋이 정확히 그 셋; 남은 일곱은 이전부터의 CUDA·flashinfer 의존).
겨냥한 회귀 테스트 하나 추가 — 아무도 페널티를 안 쓴 엔진에서 `add` → `resume` → `forget` 이 지나가고 `history` 는
끝까지 None 이다. **가드를 도로 지우면 그 테스트가 `AttributeError` 로 잡는 것까지 확인**했다(단언이 일한다는 증명).
serve·prefix·sampling_options·drafter·async_sampling 은 그대로 OK.

**교훈**: 지연 생성 필드는 *만드는 자리*가 하나여도 *읽는 자리*가 여럿이면 그 여럿이 전부 같은 가드를 알아야 한다 —
그게 싫으면 읽는 자리를 하나로 모으는 쪽이 맞다. `prefix.check()`(§32 보충)를 넣은 이유와 같은 종류의 일이다.
### 45차 §78 — §31 의 넷을 해결: 플릿 스위트가 393/393 이 됐다 (2026-09-12, srv4/srv2, PR #622)

**1. 핀은 찍은 게 아니라 감사를 실제로 했다.** `tests/test_logic.py` 드리프트는 09-07 핀 이후 **60 커밋 1,208줄** — 손으로 축복할 수 있는 크기가 아니다. 그래서 핀 주석이 스스로 말하는 범위만 검증했다("세 감사 헬퍼 테스트와 `load_defs` 의 AST"):
`test_prefill_chunker`·`test_sp_ranges`·`test_skip_topk` 는 **AST 완전 동일**, `load_defs` 는 **단 하나** 다르다 — `open(path)` → `open(path, encoding='utf-8')`. 로케일 의존을 없앤 것이고 어떤 계약도 약화하지 않는다.
그 증거를 주석에 적고 `cpu_contracts.LOGIC_AUDIT` 와 `cpu_evidence.LOGIC_SOURCE_AUDIT` 를 갱신했다 → **실패 4개가 전부 사라졌다**(플릿 393/393).
**다시 5일간 조용히 꺼지지 않도록**: `fleet.sh status` 가 핀이 어긋나면 무엇이 죽는지와 고치는 법을 찍는다(맞으면 한 줄도 안 찍는다). 실측으로 양쪽 다 확인.

**2. 원격 보유자를 증거로 판정한다.** `holder_alive` 가 다른 호스트의 보유자를 `3 × est` 동안 눈 감고 믿던 것을 `holder_probe` 로 바꿨다: `alive`/`gone`/`unknown` 을 돌려주고, **모르면 예전 창으로 후퇴**한다(닿지 않는 것은 비어 있는 게 아니다).
**`kill -0` 을 쓰면 안 된다** — 내 것이 아닌 프로세스에 `gone` 이라고 답해서 **쥐고 있는 플릿을 넘겨줄 뻔했다**(실측: pid 1 → gone). `/proc/<pid>` 로 바꿔 셋 다 정확(살아 있음/없음/닿지 않음). 폴링마다 ssh 하지 않도록 20초 캐시.

**3. 실패한 부팅 허가 21건은 계통이 없다.** 사유를 세어 보니 **전부 서로 다른 일회성**(핸드오프 회수, 프로덕션 복구, 각 실험의 개별 실패). 고칠 공통 원인이 없다 — 보고만 한다.

**4. 잔해는 아직 잔해가 아니다.** `bench/fleet_prune.py`(+ `fleet.sh prune`)를 만들었다: 보유자·큐·일시정지 세션과 그들이 지명한 러너는 **절대** 안 건드리고, 참조되지 않으면서 **동시에** 오래된 것만, 기본은 **건조 실행**.
실제 큐에 돌려 보니 14일 기준 **0건** — 잔해가 6일치라 아직 지울 게 없다(내가 §31 에서 과장했다). 3일 기준으로는 314건 13.7 MiB 가 잡힌다. 즉 도구는 맞고 시점이 아직 아니다.

**작업 중 내 실수 둘(테스트가 잡음)**: 드리프트 경고를 확인하려고 핀을 0으로 덮었다가 복구가 안 됐고, 같은 크기·같은 초의 재기록이라 **바이트코드 캐시가 옛 값을 그대로 돌려줬다**. `__pycache__` 를 지우고서야 맞았다.

**검증**: 플릿 스위트 **393/393 OK**(오늘 아침까지 실패 4), 이미지 안 엔진 스위트 OK, 리스 46 tests.


### 45차 §38 — 한국어 최적화 셋: 답 길이 기본값을 글자로, `text_offset` 을 글로, `bytes` 를 진짜 바이트로 (2026-09-12, srv4, GPU 없음)

운영자 "st엔진을 한국어 최적화로 더 개선할게 있나" → "전부 고쳐". §36 이 고친 것은 **보이는 글**이었고, 그 아래에 **예산과 계약**이 셋 더 있었다.

**근본 수치**(이 체크포인트 tokenizer.json): 한국어 0.767 tok/char, 일본어 0.725, 중국어 0.536, **영어 0.173** — 한국어가 영어의 **4.4배**.
게다가 **한글만 음절이 토큰 경계로 쪼개진다**: 혼자서 온전한 글자가 안 되는 토큰이 한국어 **32.1%**, 일본어 4.4%, 중국어·영어 0.0%(어휘가 한자는 통째로 담고 한글은 바이트로 쪼갠다).
엔진의 모든 예산이 토큰 단위라 이 둘이 그대로 손해가 된다.

#### 1. 채팅 기본 `max_tokens` 256 → 한국어 답이 324자에서 잘렸다

vLLM·SGLang 은 **둘 다 기본 `None`**(문맥 한계까지). 우리만 고정 상한인데 그건 실수가 아니라 **대가**다 — 입장 때 `horizon = len(ids) + max_new - 1 + draft_slots` 를 통째로 예약하고 선점이 없다(D3).
그래서 상한은 두되 **단위를 바꿨다**: 기본값은 이제 **1,500 글자**이고, **그 요청이 쓰인 언어의 tok/char 로 환산**한다(`answer_budget`). 재는 대상은 **마지막 사용자 턴의 글**(`written_text`) — 렌더된 프롬프트는 템플릿의 영어가 섞여 짧은 한국어 질문을 영어로 읽는다.

| 본문 | 옛 256 토큰 | 새 기본값 | = 글자 |
|---|---:|---:|---:|
| 한국어 | **324 자** | 1,192 토큰 | 1,508 자 |
| 일본어 | 392 자 | 1,200 토큰 | 1,835 자 |
| 중국어 | 475 자 | 900 토큰 | 1,671 자 |
| 영어 | 1,453 자 | 295 토큰 | 1,674 자 |

신호는 짧은 프롬프트에서도 읽힌다("안녕하세요?" 6자에서 0.833). 바닥 256·천장 2048 토큰. 비용은 요청당 **16.3 µs**(마지막 사용자 턴 인코드 한 번).
**예약이 커지므로** 기본값은 `room_for(len(ids))` 로 깎되 **옛 기본값 아래로는 안 깎는다** — 옛 기본값으로도 안 들어가던 프롬프트는 **같은 말로 거절**된다(한 토큰짜리 답으로 조용히 바꾸지 않는다). 호출자가 `max_tokens` 를 쓰면 아무것도 안 바뀐다.

#### 2. `/v1/completions` 의 `text_offset` 이 한국어에서 글 밖을 가리켰다

`tok.decode([tid])` 를 토큰마다 부르고 `pos += len(text)` 했는데, 쪼개진 음절의 반쪽은 `'�'`(1자)로 세어진다. 실측 드리프트: 한국어 **+12~+44자**, 일본어·중국어·영어 0.
`token_spans` 로 바꿨다 — `_Stream` 으로 글을 한 토큰씩 키워 **그 토큰이 더한 만큼**을 그 토큰의 몫으로 준다. 반쪽은 0자를 더하고 글자를 완성하는 토큰이 통째로 더한다. 이제 `"".join(tokens) == text` 이고 **모든 오프셋이 자기 토큰 위에 떨어진다**(네 언어 전부). vLLM 의 completions 경로도 같은 방식이라 같은 문제가 있다.

#### 3. 채팅 `logprobs` 의 `bytes` 가 한국어 토큰 32%에서 U+FFFD 의 바이트였다

OpenAI 스펙이 `bytes` 를 둔 이유가 **정확히 이 경우**다 — *"여러 토큰에 걸친 문자를 클라이언트가 바이트로 이어 붙여 복원하라"*. 그런데 우리는 `[239,191,189]` 를 줬다. 이어 붙이면 U+FFFD 만 나온다.
`token_bytes` 가 `decode` 가 U+FFFD 를 냈을 때만 **어휘 항목**을 읽는다: 바이트레벨 항목은 GPT-2 표로 자기 바이트를 적고(`'ëį'` → `[235,141]`), 바이트폴백은 `<0xNN>`. 그 외는 옛 답 그대로.
실측: 한국어 문장의 바이트를 이어 붙이면 **원문이 그대로 복원된다**(전 `False` → 후 `True`), 영어는 전후 동일. **vLLM·SGLang 둘 다 여기서 틀렸다**(`decode([id]).encode(...)`) — 앞서는 자리다.

#### 확인했는데 문제 없던 것 / 못 잰 것

정지 문자열 스캔·`partial_suffix` 는 글자 단위라 무관. `stop_token_ids_for` 는 정확한 왕복을 요구해 안전. 프롬프트 토크나이저는 체크포인트 truncation 규칙을 이미 뗐다(§23). 비전 자리표시는 어휘 안의 id.
**못 잰 것 하나**: 드래프터 수용률이 한국어에서 낮은지. 낮으면 한국어가 두 번 낸다(토큰 4.4배 + 스텝당 수락 감소). 카운터는 이미 있다 — `st:spec_accepted_per_step_total` 을 한국어/영어 프롬프트로 각각, GPU 창에서.

**검증**: 네 스위트 157 tests, 실패 목록이 main 과 동일(사전 존재 12). 새 테스트 12개(예산 4·`written_text` 5항목·`token_spans` 4·`token_bytes` 4).

### 45차 §39 — SGLang 전면 대조(남은 영역): 가져올 것은 **하나 반**이었다, 그리고 내가 넷이라고 적은 방식 (2026-09-12, srv4, GPU 없음)

운영자 지시. vLLM 은 영역별 전면 대조가 있는데 SGLang 은 조각 둘뿐이었다 — 접두사 캐시(§32 보충)와 구조화 출력(§33).
나머지를 `sgl-project/sglang` **`e91c948`** 체크아웃(`python/sglang/srt`, 1,852 파일 · 791k 줄)으로 훑었다.
전문은 `engine/SGLANG_STUDY_20260912.md`.

**먼저 이 절의 요점**: 처음에 "가져올 것 넷" 을 적었고 **둘은 틀렸다.** 구현하려고 손을 대자마자 드러났다.
원인은 하나다 — **상대 코드는 읽고 우리 코드는 기억으로 썼다.**

| 처음 적은 것 | 판정 | 근거 (전부 돌려서 나온 값) |
|---|---|---|
| 대기열 **안**의 접두사 중복을 못 거른다 | **틀림** | 둘째 요청이 첫 스텝에 `_deferred` 로 들어가고 `dedup_waits == 1`. `submit` 이 `_chain` 을 채우는 순간 그 행이 `shared_ahead` 의 `live` 가 된다 |
| 벽시계 밸브라 느려지면 "터진다" (40 중 38 개방) | **틀림** | 그 38 은 **내 시뮬레이션이 `advance()` 를 빼먹어** 나온 거짓 수치다. 제대로 돌리면 스텝 0.05 s → 디코드 100%, 3 s → **85%**, 8 s → **85%**, 밸브는 요청당 한 번 |
| 그래프 메모리를 안 잰다 | **절반 틀림** | 잰다 — `checkpoint` 가 모양마다 `reserved_bytes`·`peak_workspace_bytes` 를 남긴다(SGLang 의 여섯 단계보다 세밀). 없던 것은 **합산**이다 |
| 드래프트 길이가 고정 5 다 | **맞음** | `boot.SPEC_K`, `contract.draft_slots = drafter.k` |

**그래서 한 것**: `RuntimeMemory.weigh()` — `spend()` 가 시간에 대해 하는 일(접두사별 합, 큰 것부터)을 **예약 바이트**에
대해서 한다. 열은 구간 증가분이고(GB10 에서 상자가 잃는 것이 예약이다) 메모리를 내놓은 단계는 음수로 남는다.
`report()` 에 `weigh` 로 나간다. 부팅 87 초의 75% 가 캡처인데 "이 캡처가 제 발자국 값을 하나" 를 물으려면 351 줄을 손으로
읽어야 했다. 새 테스트 하나(부분의 합 == 전체까지 단언).

**남은 하나**: 적응형 드래프트 길이. `target_steps = clamp(round(ema_accept_len) + 1, min, max)`, 배치 크기별 슬롯,
**그래프가 캡처된 step 으로만** 전환. 관측은 이미 나간다(`st:spec_accepted_per_step_total`). **비용은 캡처**라
`weigh()` 가 먼저 있어야 값어치를 물을 수 있다 — 그래서 이번에 그걸 먼저 했다.

**찾으러 갔다가 없다고 확인한 것**: `model_executor/` 에 **캡처를 싸게 하는 기계는 없다.**
`get_batch_sizes_to_capture` 는 설정 목록을 거르기만 한다(어텐션 TP 정렬 · `max_running_requests` · 정렬/중복 제거).
지연도 병렬도 건너뛰기도 없다. **캡처 비용은 저쪽도 그냥 낸다.**

**의도적으로 다른 것**: 선점(`retract_decode`)과 입장 추정(`new_token_ratio` + 감쇠) — 우리는 지평선 전체를 예약하고
선점이 없다(D3). 혼합 청크 — D9 와 39차 실측. 대기열 정책 일곱 — D11.
**안 가져올 것**: PD 분리(29.7k 줄) · 빔 서치 · LoRA/elastic EP(D5 밖) · 어텐션 백엔드 선택(72k 줄, D8/D11 로 박아 쓴다).

**소스 주의**: §33 은 PyPI 휠 `0.5.19` 를 읽었는데 `mamba_radix_cache.py`·`evict_policy.py`·`adaptive_spec_params.py` 는
**그 휠에 없다**. 다음 대조는 체크아웃으로 통일할 것.

**규칙 하나 (이번 값)**: 대조 표의 **우리 쪽 칸은 실행한 것**이어야 한다 — 읽은 것도 기억한 것도 아니라 돌려서 나온 값.
틀린 둘 다 스무 줄을 돌려 보면 5 분에 끝나는 확인이었고, 하나는 틀린 진단에 **틀린 증거**까지 붙을 뻔했다.

### 45차 §40 — 한국어, 계속: 줄 위의 바이트와 거절의 숫자와 글자 계량기; 그리고 아니었던 것 둘 (2026-09-12, srv4, GPU 없음)

운영자 "수용률 측정 말고 한국어에서 더 개선할 수 있는거 없어?". §38 이 **예산과 계약**을 고쳤고, 그 밖에 셋이 더 있었다. 그리고 **짚어 봤는데 아니었던 것 둘**도 적는다(원장 규율: 기각도 기록).

#### 1. 문이 한글을 `\uXXXX` 로 내보내고 있었다

`reply`·`sse` 가 `json.dumps(payload)` 를 그대로 썼다 = **`ensure_ascii=True`**. 한글 한 자는 UTF-8 3바이트인데 이스케이프하면 **6바이트 ASCII**(`한`)다.
JSON 은 정의상 UTF-8 이고(RFC 8259) 이스케이프는 선택이다. `ensure_ascii=False` 로:

| | 전 | 후 |
|---|---:|---:|
| 한국어 답 본문(비스트리밍) | 4,987 B | **2,719 B** (0.55×) |
| 한국어 SSE 전체 | 49,455 B | 47,187 B (0.95×) |
| 한국어 SSE 청크 하나 | 194 B | 179 B (0.92×) |
| 영어 | 변화 없음 | 변화 없음 |

스트리밍 이득이 작은 건 SSE 봉투(`id`·`model`·`choices` 뼈대 ~100 B)가 작은 델타를 압도하기 때문이다. **비스트리밍은 몸통의 45% 가 사라진다.**
비용: 청크당 인코드 1.37 → 1.68 µs(+0.31 µs, 46 ms 스텝 대비 무시 가능). 영어는 바이트 동일.

#### 2. 문맥 초과 거절이 숫자를 안 말했다

`"prompt and generation limit exceed the model's context"` — 무엇이 얼마나 넘쳤는지 없다. **이 벽을 제일 먼저 만나는 사람이 한국어 사용자**다(같은 글이 토큰 4.4배).
이제 vLLM 처럼 숫자를 말한다: `this model serves N tokens of context; this request needs H (P for the prompt, G to generate)`. KV 쪽·대화 이어가기 쪽 넷 다 같은 꼴로.

#### 3. `st:generation_characters_total`

토큰은 엔진이 쓰는 것이고 **글자는 독자가 받는 것**인데, 계량기가 토큰뿐이었다. 한 토큰이 영어 5.8자·한국어 1.3자라 **한국어 배포는 자기 처리량을 4.4배 후하게 읽는다.**
`vllm:generation_tokens_total` 옆에 클라이언트가 실제로 읽은 글자 수를 센다(요청이 끝날 때 한 번, 스텝당 비용 0).

#### 아니었던 것 둘

- **`repetition_penalty` 의 언어 비대칭**: 교착어라 어미가 반복되니 한국어가 크게 손해일 것이라 봤는데, 실측은 **완만했다** — 이미 나온 토큰의 비율이 한국어 30.1%, 영어 24.5%, 일본어 24.1%. 게다가 이 체크포인트의 `generation_config.json` 에는 **`repetition_penalty` 가 아예 없다**(temperature 1.0 뿐). 호출자가 켜야만 적용된다. **안 고친다.**
- **구조화 출력 × 한국어**: `Grammars` 는 `xgr.TokenizerInfo.from_huggingface` 로 어휘 종류를 xgrammar 가 스스로 읽게 둔다. 바이트레벨이면 부분 UTF-8 토큰도 마스크가 허용해야 맞는데, **srv4 호스트에 xgrammar 가 없어 여기서는 확인할 수 없다.** 추측을 적는 대신 **관문으로 남긴다**: ST 이미지 안에서 한국어 값이 들어간 JSON 스키마를 컴파일해 마스크가 한글 바이트 토큰을 허용하는지 볼 것(§36 의 교훈 — 안 본 모양은 안 본 것이다).

**검증**: 네 스위트 160 tests, 실패 목록이 main 과 동일(사전 존재 12). 새 테스트 3개(본문이 UTF-8 이고 이스케이프가 아니다 · 거절이 숫자를 말한다 · 읽은 글자가 토큰 옆에 세어진다).

### 45차 — 자동 배포: main 이 스스로 프로덕션에 닿는다, 프로덕션이 감당할 수 있을 때 (2026-09-12, srv4, 무장 안 함, PR #629)

운영자 "자동 배포로 해 머지되면". 결정 둘은 운영자 것이라 물었고 답은 **"조용할 때 합쳐서"** + **"회귀 대조 후 배포"** 였다.

**왜 물었나**: 오늘 main 이 **4시간에 PR 10개**를 받았다. "머지되면 즉시" 는 프로덕션 재시작 10회다 — 그리고
GLM-5.3 프로덕션은 이제 ST 엔진이다(§34). 부팅은 87 초 + 캡처·워밍이고, 깨진 머지는 프로덕션을 내린 **뒤에**
드러나며, 슈퍼바이저는 5회 실패 후 사람을 기다린다. 배포 빈도는 내가 고를 수 있는 값이 아니었다.

**`launchers/st-deploy-watch.py`** (stdlib 전용, `base/fleet_lease.py` 와 같은 이유 — 엔진 가상환경 밖에서 돌아야 한다).
조건 셋, **셋 다 거부할 수 있다**:

| 조건 | 어떻게 아나 | 거부하는 경우 |
|---|---|---|
| main 이 움직였나 | 배포된 sha 는 릴리스 옆 **파일**이지 추측이 아니다 | 게이트가 이미 기각한 sha 는 **main 이 그 너머로 갈 때까지 다시 안 본다** — 깨진 머지가 재시작 루프가 되면 안 된다 |
| 엔진이 조용한가 | `num_requests_running`·`num_requests_waiting` 이 창 내내 0, 샘플링, 하나만 도착해도 리셋 | **문이 답을 못 하면 조용한 게 아니다**(None 은 0 이 아니다). 못 받은 답은 허락이 아니다. `st:handing_over` 도 마찬가지 |
| 스위트가 회귀했나 | 후보 트리와 **실제 배포된 트리** 양쪽에서 돌려 **차이**를 본다 | 새로 실패하거나 더 나쁘게 실패하는 파일 하나면 거부하고 기록 |

**게이트가 차이인 이유**: 지금 어느 트리에서든 엔진 테스트 파일 12~14개가 실패한다(kda_*, serve, glm53 …).
초록 막대는 없고 절대 개수는 신호가 아니다. 오늘 내가 네 번 돌린 그 대조가 그대로 게이트다.
**비교 대상이 없으면 건너뛰지 않고 거부한다** — 건너뛰면 아무도 안 보는 **첫 배포만 게이트 없이** 나간다.
`--seed <sha>` 로 지금 도는 것을 기록시키는 게 그 자리다.

**합치는 효과**: 오늘 같은 날 재시작은 **10회가 아니라 1회**다. 첫 간극에서 그때까지 쌓인 main 을 한 번에 배포한다.
그리고 요청 중에는 절대 안 내린다 — 신선도보다 그 성질이 값지다. `--min-gap`(기본 1,800 초)이 하드 캡이다.

**스스로 무장하지 않는다**: `--install` 은 명령 둘을 **찍기만** 하고, `--dry-run` 은 판단만 하고 아무것도 안 바꾼다.
타이머를 켜는 것은 머리 노드(srv2)에서 사람이 하는 결정이다. srv4 에서 드라이런하면 문이 없으니
*"/metrics did not answer; not treating that as quiet"* 로 **거부**한다 — 의도한 모양이고, 실제로 그렇게 나왔다.

**판정**: `tests/test_st_deploy_watch.py` 19 tests — 거부하는 길 전부(배포된 sha·기각된 sha·머리 없음·핸드오버 중·
라벨과 실수 파싱·이름 접두사·무응답·회귀 넷·비교 대상 없음·유닛이 워크트리가 아니라 rsync 된 트리를 가리키는지).
**안 잰 것, 그리고 그렇게 적는다**: 릴리스 커팅·슈퍼바이저 정지·네 노드 기동은 **해 봐야만** 판정되고
해 보는 것이 프로덕션을 내리는 일이다. 게이트가 rank 0 노드에서 도는 CPU 부하라 `nice -n 19` 를 걸었지만
그것도 서빙 옆에서 재 본 적은 없다.

### 45차 §41 — 구조화 출력 × 한국어: `[가-힣]` 이 한글의 6분의 1을 조용히 버리고 있었다 (2026-09-12, srv4, GPU 없음, ST 이미지 안)

§40 이 "xgrammar 가 호스트에 없어 확인 못 한다"고 관문으로 남긴 것을 **돌리는 컨테이너(st-glm53, `st-engine:perf-f4d7-h`) 안에서 CPU 로** 실행했다. 모델도 GPU 도 안 쓴다 — 토크나이저와 xgrammar 컴파일뿐.

**먼저 멀쩡한 것들**(전부 실측): `vocab_type` 은 `BYTE_LEVEL` 로 바르게 잡히고, 평범한 문자열 값은 한국어·한자·이모지 전부 통과. **`maxLength`/`minLength` 는 바이트가 아니라 글자를 센다**(한국어 5자가 `maxLength:5` 통과, 6자는 6번째 음절에서 거부). 한국어 `enum`, 리터럴 교대 `^(안녕|반가워)$`, 리터럴 접두사 `^한.*$` 도 정상.

**깨진 것 하나, 그리고 그게 크다**: 정규식 **문자 클래스 범위**가 UTF-8 선두 바이트 `0xED` 가지(U+D000–U+D7FF — 서로게이트 구멍 때문에 연속 바이트 범위가 잘린 그 가지) **안에서 끝나면, 그 가지의 내용이 끝점 하나만 남고 전부 사라진다.**

`^[가-힣]+$` (U+AC00–U+D7A3) 의 실제 마스크:

| 이 | 하 | 한 | 타 | 힣 | a |
|---|---|---|---|---|---|
| 허용 | **거부** | **거부** | **거부** | 허용 | 거부(정상) |

거부되는 것: **타 파 하 한 해 호 후 희 흥** … = U+D000–U+D7A2, **한글 음절 블록의 약 17%**, 그것도 한국어가 가장 많이 쓰는 ㅌ·ㅍ·ㅎ 초성 구간이다. `[가-힣a-z]` 처럼 섞어 써도 ASCII 쪽만 산다.

**한국어 전용 버그가 아니라 한국어만 밟는 버그다**: 2→3바이트 경계(`[\u0700-\u0900]`)도 3→4바이트 경계(`[\uFF00-\U0001F600]`)도 멀쩡하고, 3바이트 전체 범위(`[\u0800-\uFFFF]`)도 멀쩡하다. **부분 0xED 가지**만 깨진다 — 그리고 그 가지를 걸치는 흔한 문자 블록은 한글뿐이다.

**고침**(`split_surrogate_branch`): 문을 지나는 스키마의 모든 `pattern` 에서, **0xED 가지 안에서 끝나는 클래스 범위를 둘로 쪼갠다** — `[가-힣]` → `[가-\uCFFF\uD000-힣]`. U+CFFF 와 U+D000 은 이웃이라 **집합이 정확히 같고**, 이 형태는 xgrammar 가 바르게 컴파일한다(실측: 다섯 음절 전부 허용, `a` 는 여전히 거부).
이스케이프 표기(`\uAC00-\uD7A3`, `\x41-\uD7A3`)도 읽고, 부정 클래스도 같은 치환이 성립한다. **자신 있게 못 읽는 것은 손대지 않고 그대로 넘긴다** — 패턴은 호출자의 것이다.

**관문을 코드로**: `probes/st_grammar_korean.py` — 이미지 안에서 A~E 다섯을 돌려 PASS/FAIL 을 낸다(D 는 **스톡 xgrammar 가 떨어뜨리는 것을 그대로 확인**하고, E 는 문이 보내는 형태가 살아나는 것을 확인한다). `ST_REPO` 로 워크트리도 잴 수 있다. 실측 PASS.

**덤**: 부정 클래스에 비ASCII 가 들어가면 xgrammar 가 `Negative Character class contains byte greater than 127, clamping to 127` 를 경고한다 — `[^가-힣]` 은 애초에 바이트로 근사된다는 뜻이고, 우리 치환은 그걸 나쁘게 만들지 않는다(전후 마스크 동일). 이건 upstream 에 남은 한계로 적어 둔다.

**검증**: 네 스위트 166 tests, 실패 목록이 main 과 동일(사전 존재 12). 새 테스트 6개(범위 쪼개기·이스케이프 표기·손대지 않는 것 열 가지·클래스 양끝의 `-`·스키마 전체 순회·문이 실제로 보내는 것).

### 45차 §42 — 같은 한국어를 두 번 토큰화하고 있었다: 조합형/분해형, 그리고 U+FFFD 로 겹치는 top_logprobs (2026-09-12, srv4, GPU 없음)

운영자 "한번만 더 한국어 관련 찾아봐". 토크나이저 파일부터 다시 읽었다. **`"normalizer": null`** — 이 체크포인트는 유니코드 정규화를 안 한다.

#### 1. 분해형(NFD) 한국어가 같은 글의 6.4배를 먹는다

한글 음절은 조합형(NFC, U+AC00–U+D7A3)으로 한 글자지만 분해형(NFD, U+1100–U+11FF 자모)으로 쓰면 두세 글자다. **정규화가 없으면 토크나이저에게 둘은 완전히 다른 글이다.**

| | 글자 | 토큰 |
|---|---:|---:|
| 한국어 프롬프트, NFC | 1,600 | **1,202** |
| 같은 글, NFD | 3,080 | **7,721** (6.4×) |
| "안녕하세요" | 5 / 11 | 4 / **35** (8.75×) |

NFD 는 드문 입력이 아니다 — macOS 파일명, 일부 IME, PDF·OCR 추출 텍스트가 그렇게 온다. 결과는 프롬프트 6배, TTFT 6배, 문맥 한계 6배 빨리, **그리고 접두사 캐시 전멸**(같은 대화의 이전 턴이 NFC 였으면 해시가 다르다). 품질도 나빠진다 — 모델이 거의 못 본 자모 열이 들어간다.

**고침**: 문이 토큰화하는 모든 자리(채팅 렌더 프롬프트·completions 프롬프트 하나/여럿·`/tokenize`·엔진 방언 둘)에서 **NFC 로 맞춘다**(`nfc()`). NFC 와 NFD 는 **유니코드가 같은 글이라고 정의한 것**(정준 동치, UAX #15)이고 NFC 가 웹 교환 권장형이라, 보낸 것을 고쳐 쓰는 게 아니다.
비용: 1,600자 한국어 프롬프트에 **2.5 µs**, 영어 0.1 µs(ASCII 는 `is_normalized` 빠른 경로에서 그대로 반환). **정지 문자열도 NFC 로** — 답은 조합형으로 나오니 분해형 정지 문자열은 영원히 안 맞는다.
`answer_budget` 도 NFC 로 재게 했다(안 그러면 NFD 프롬프트가 tok/char 5.0 으로 읽혀 천장에 붙는다).

#### 2. 레거시 `top_logprobs` 가 후보를 조용히 버리고 있었다

`/v1/completions` 의 `top_logprobs` 는 OpenAI 규격상 **토큰 텍스트를 키로 하는 딕트**다. 그런데 한국어에서는 한 자리의 후보 여럿이 **글자의 반쪽**이고, 반쪽은 어느 쪽이든 `'\ufffd'` 로 디코드된다 → **키가 겹친다.** 딕트 컴프리헨션은 **마지막**을 남기는데, top-k 는 내림차순이라 마지막이 **가장 가능성 낮은 것**이다. 이제 가장 높은 것을 남긴다.
(채팅 방언은 리스트라 안 겹친다. 형식의 한계는 vLLM 도 같지만, 버릴 때 **엉뚱한 쪽을 버리지는 않는다**.)

**검증**: 네 스위트 170 tests, 실패 목록이 main 과 동일(사전 존재 12). 새 테스트 4개(분해형이 조합형으로·ASCII 는 손 안 댐·분해형 정지 문자열·문이 두 형태를 같은 프롬프트로 보낸다).

#### §42 보충 — 스스로 코드 리뷰하고 일곱을 고침 (2026-09-12)

운영자 "오늘 너가 구현한것 코드 리뷰" → "해결해". 오늘 넣은 여섯 PR(#606·#616·#626·#628·#631·#632)의 **현재 상태**를 한 번에 읽었다. 치명적인 것은 없었고(단조성·이중 송신·두 경로 어긋남은 실제로 안 깨진다) **일곱**이 나왔다. 전부 고쳤다.

**확정 다섯**
1. **`split_surrogate_branch` 가 안 깨진 범위까지 고쳤다.** 조건이 `low < 0xD000 <= high` 라 `[가-\uffff]` 처럼 **가지를 지나쳐 끝나는** 범위(이미 정상 컴파일됨을 이미지에서 실측)도 쪼갰다. `high <= 0xD7FF` 를 더해 좁혔다 — 자기 문서("그 한 모양만 건드린다")와 이제 맞는다.
2. **바이트 표 테스트가 자기 자신을 검증했다.** `id_of_byte` 와 `token_bytes` 가 같은 `_byte_level_chars()` 를 쓰니 표가 틀려도 왕복이 상쇄된다. GPT-2 공표값 넷(0x00→U+0100, 0x20→U+0120, 0x41→'A', 0xFF→U+00FF)과 전단사 여부를 **바깥에서** 단언하는 테스트를 넣었다.
3. **글자 계량기가 안 끝난 답을 안 셌다.** 클라이언트가 끊었거나 엔진이 죽은 답은 세지 않아, 하필 토큰 카운터와 대조해 볼 트래픽이 빠졌다. 스트리밍 루프의 **모든 출구**를 `retire()` 하나로 모았다. 회귀 테스트는 진짜로 엔진을 죽이고, 클라이언트가 **읽은 글자 수와 카운터가 정확히 같은지** 본다.
4. **`DETOK_REPAIRS` 가 프로세스 전역이었다.** Server 가 여럿이면 서로의 복구 수를 보고했고, 테스트의 증분 단언이 순서 의존이었다. `Server.detok_repairs` 로 내리고 `_Choice` → `_Stream` 으로 넘긴다(문 없는 `token_spans`·맨 스트림만 전역을 쓴다). 새 테스트는 **다른 문의 복구가 내 것으로 안 보인다**를 단언한다.
5. **`/tokenize` 가 안 보낸 문자열의 답을 줬다.** NFC 합성은 생성 경로와 같아야 하니 유지하되, 답이 다른 문자열에 대한 것일 때 `normalized: "NFC"` 와 **ids 가 가리키는 실제 문자열**을 같이 돌려준다.

**잠복 둘 — 불변식을 코드로 못박았다**
6·7. 붙잡은 꼬리를 **보여줄 땐 문맥과 함께, 확정할 땐 문맥 없이** 디코드하고 있었고(문맥 의존 디코더면 보인 글이 뒤로 갈 수 있다), invalid-prefix 복구가 빈 텍스트를 내면 `_ahead` 가 남았다. 둘 다 **`_in_place(cut)` 하나**로 합쳤다 — 한 번의 디코드에서 (보일 몫, 확정할 몫)을 같이 내므로 **확정본은 언제나 보인 것으로 시작한다**. 복구 경로도 같은 함수를 쓰고 `_ahead` 를 명시적으로 비운다. 새 테스트가 스텝당 1·2·3·5 토큰에 대해 **보인 글이 한 번도 안 바뀐다**를 단언한다(중간 flush 와 final 을 섞어서).

**검증**: 네 스위트 175 tests, 실패 목록이 main 과 동일(사전 존재 12). 새 테스트 5개.
**못 한 것**: 좁힌 조건을 이미지 안 xgrammar 로 다시 돌려보지 못했다 — 그 사이 `st-glm53` 컨테이너가 내려갔다(다른 세션). 좁히기는 **이미 정상인 패턴의 재작성을 없앨 뿐**이고 `[가-힣]` 의 출력은 이미지에서 검증한 것과 바이트 동일이라 검증된 사례는 그대로지만, 다음 창에 `probes/st_grammar_korean.py` 를 한 번 더 돌릴 것.

### 45차 §43 — llama.cpp·Ollama 에서 배워올 것 조사: 하나 있고, 이미 한 것이 넷, 못 하는 것이 둘 (2026-09-12, srv4, GPU 없음)

운영자 "올라마나 라마에 배워올만한게 있나". vLLM(§30)·SGLang(§36)에 이어 셋째 대조. **급이 다른 엔진이라 대부분 해당 없지만, 진짜 빈 곳이 하나 나왔다 — 그리고 그건 llama.cpp 만의 것이 아니라 vLLM·SGLang 에도 있는 것이다.**

#### 1. 가져올 것: `min_p` — 넷 다 있고 우리만 없다

| | min_p | typical_p | DRY | XTC | mirostat |
|---|---|---|---|---|---|
| llama.cpp | `--min-p` | `--typical-p` | `--dry-*` | `--xtc-*` | `--mirostat` |
| Ollama | `min_p` | `typical_p` | — | — | (구) |
| vLLM | `min_p` (프로토콜 필드) | — | — | — | — |
| SGLang | `min_p` | — | — | — | — |
| **ST** | **없음** | 없음 | 없음 | 없음 | 없음 |

`min_p` 는 "가장 가능성 높은 토큰 확률의 X배 미만은 버린다" 로, top_p 와 달리 **분포가 뾰족할 때 자동으로 좁아지고 평평할 때 넓어진다**. 커뮤니티가 top_p 대체로 쓰고, 우리 네 참조 구현이 전부 노출한다.
**들어갈 자리는 정확히 보인다**: `sampler.OPTION_KEYS`+`validate_options`(범위 [0,1]), `needs_rich_sampler` 에 추가(캡처된 커널은 top_k/top_p 텐서만 받으므로 리치 경로로 보낸다), 그리고 `threshold()` 한 줄 — 그 함수의 `weights` 는 `exp((logit−max)/T)` 라 **최대가 1** 이므로 min_p 의 임계값이 곧 `min_p` 자신이다. 순서는 vLLM 과 같게 min_p → top_k → top_p.
**안 넣었다**: `engine/base/sampler.py` 는 지금 다른 세션(`ostcode/st-engine-improvements-a5ab66`, 샘플러 커널 #613/#633)이 잡고 있다. 충돌 대신 자리를 적어 둔다. 나머지 넷(typical_p·DRY·XTC·mirostat)은 vLLM·SGLang 에도 없고 품질 논쟁이 있어 **안 가져온다**.

#### 2. 이미 한 것 넷

- **KV 캐시 양자화**(`--cache-type-k q8_0` 류, llama.cpp 의 대표 메모리 레버): 우리 MLA 잠재는 **이미 fp8**(`cache_spec`: `PagedSpec("mla latent fp8", ...)`). q8_0 과 같은 급이고 f16 보다 낫다.
- **슬롯 디스크 저장/복원**(`/slots/{id}?action=save|restore`, `--slot-save-path`): 우리 D16 NVMe 티어가 **자동·LRU·다중 턴**이라 엄밀히 더 많다.
- **프롬프트 캐시**(`--cache-prompt`): 우리 접두사 캐시(§32 등)와 같은 자리.
- **부분 UTF-8 스트리밍**: llama.cpp 도 "마지막 토큰이 부분 멀티바이트면 한도를 살짝 넘을 수 있다"고 적어 둔 그 문제고, 우리는 §36·§38 에서 글자 경계로 해결했다.

#### 3. 못 하는 것 둘 — 구조가 막는다

- **`--cache-reuse N`(KV 시프팅으로 중간이 달라진 프롬프트도 재사용)**: 우리 프리필의 절반이 **KDA 선형 어텐션의 재귀 상태**다. 그건 위치로 색인된 캐시가 아니라 **달려온 상태**여서, 위치를 밀어 재해석할 방법이 없다(스냅샷도 청크 경계에만 있다). MLA 쪽만 RoPE 재회전으로 밀 수 있어도 반쪽은 못 민다.
- **`--context-shift` / `--keep N`(문맥이 차면 가운데를 버리고 무한 생성)**: 우리는 **입장 때 지평선 전체를 예약하고 선점하지 않는다**(D3, `kv.py`). 문맥을 넘기면 버리는 대신 **숫자를 말하며 거절**한다(§40). 의도된 차이다.

#### 4. 후보로 남기는 것 하나 (GPU 필요)

**n-gram 드래프팅**(llama.cpp `--spec-ngram-*`, vLLM 의 `method: "ngram"`): 모델 없이 프롬프트에서 이어 붙일 조각을 찾아 드래프트한다. **긴 문맥을 인용·편집·요약하는 요청**에서 수락 길이가 길어지고, 우리 DFlash2 와 배타적이지 않다(둘 중 나은 쪽을 고르거나 섞을 수 있다). 한국어에서 특히 값이 클 수 있다 — 같은 글이 토큰 4.4배라 인용 구간이 그만큼 길다. 창이 열리면 `st:spec_accepted_per_step_total` 로 비교할 것.

**Ollama 쪽**: `keep_alive`(유휴 시 언로드)·`num_gpu`(레이어 오프로드)·Modelfile 은 단일 노드 소비자용 운영 기능이라 우리 플릿 리스·파킹이 대신한다. API 파라미터는 위 표가 전부다.

#### 5. 파라미터 말고 — 문의 **행동**에서 나온 셋 (운영자 "그런거 말고 다른건")

위는 전부 API 표면이다. 같은 두 프로젝트를 **엔진이 하는 일** 쪽으로 다시 읽으니 셋이 나왔고, 셋 다 파라미터가 아니다.

**5.1 도구 호출 인자가 스트리밍되지 않는다 — 셋 다 하는데 우리만 안 한다.**
우리 `_Choice.flush` 는 `</tool_call>` 이 닫혀야 그 호출을 통째로 내보낸다(`_TOOL_CALL.findall` → 완성된 블록만). OpenAI 스트리밍 규격은 반대다 — 첫 델타가 `index`·`id`·`function.name`, 이후 델타가 `function.arguments` **조각**이다.
SGLang 은 명시적으로 그렇게 한다(`base_format_detector`: *"Tool names sent first with empty parameters, then arguments stream incrementally"*, `partial_json_loads` 사용), vLLM 은 파서마다 `extract_tool_calls_streaming`, llama.cpp 은 `common/chat-parser.cpp` 의 부분 파싱.
**우리 체감**: GLM 형식은 인자 값이 본문의 대부분이다. 한국어 1,000자 인자면 ~770 토큰 ≈ 128 스텝 ≈ **6초 동안 클라이언트가 아무것도 못 본다**(§38 의 4.4배가 여기서도 곱해진다).
**고칠 자리**: 전부 우리 것이다 — `profiles/glm53/tools.py` 에 부분 파서(열린 `<tool_call>` 의 이름 + 완성된 `<arg_key>/<arg_value>` 쌍 + 열린 값), `base/serve` 는 보낸 만큼을 빼서 조각으로 흘린다. **단조성 규칙**: 값의 첫 글자가 `{[\"-0123456789tfn` 가 아니면 `_value` 가 평문으로 갈 것이 확정이므로 그 문자열만 글자 단위로 흘리고, 나머지 타입은 닫힐 때까지 기다린다(부분 조각이 JSON 일 필요는 없다 — 이어 붙인 것이 JSON 이면 된다).

**5.2 사고 예산이 없다 — 그리고 그게 없어서 난 사고가 이미 원장에 있다.**
llama.cpp 은 `--reasoning-budget N`(−1 무제한, 0 사고 금지, N 토큰)과 예산 소진 시 `--reasoning-budget-message` 를 끼운 뒤 **사고 종료 태그를 강제**한다. 살아 있는 요청에 `/v1/chat/completions/control` `{"action":"reasoning_end"}` 로 "지금 그만 생각해" 를 보내는 것도 있다(요청이 `reasoning_control: true` 였을 때).
우리는 **둘 다 없다**. 그런데 29차 메모의 사고 예산 함정이 바로 이것이다 — GLM-5.3 이 분량 제약 있는 문제에서 **사고 안에 초안을 쓰고 글자 수를 세며 다듬다 상한(16K·40K)에 걸려 답 0 자**. 지금 우회는 프롬프트 쪽 `reasoning_effort=low` 뿐이다.
**고칠 자리**: 문은 토큰을 강제할 수 없다(읽기만 한다). 예산이 다하면 **다음 스텝의 `reasoning_end` 를 강제**해야 하고 그건 리치 샘플러 경로(`_row_logits`)다 — `logit_bias` 의 조건부 판이다. 지금 그 파일은 다른 세션이 잡고 있다.

**5.3 도구 인자에 문법이 안 걸린다 — 우리에겐 이미 `grammar_after` 가 있다.**
llama.cpp 은 **지연 문법**(`grammar_lazy`, `grammar_triggers`)으로 `<tool_call>` 이 나온 **뒤부터만** 도구 스키마 문법을 건다 — 앞의 산문은 자유롭고 인자는 선언된 스키마를 지킨다. 우리는 `tools` 를 **챗 템플릿에만** 넘기고(`serve.py:2286`) 문법을 전혀 안 만든다. 그래서 모델이 잘못된 키·깨진 JSON 을 써도 막을 게 없다.
**그런데 기구는 이미 있다**: `options["grammar_after"]`(다른 세션이 사고 블록 때문에 넣은, "이 토큰 뒤부터 문법을 건다")가 정확히 llama.cpp 의 지연 트리거다. 남은 것은 `tools` → JSON 스키마 변환과, GLM 의 `<arg_key>/<arg_value>` 형식을 문법으로 쓰는 일. **§41 의 한글 범위 수리가 여기서도 값을 한다** — 도구 인자 스키마에 `pattern` 이 있으면 같은 0xED 문제를 밟는다.

**순위**: 5.1(우리 것만으로 가능, 체감 큼) → 5.3(기구가 이미 있음) → 5.2(샘플러 경로 필요, 다른 세션과 겹침).

### 45차 §44 — 도구 호출 인자를 조각으로 흘린다: 닫힐 때까지 기다리던 3.9 초가 0 이 됐다 (2026-09-12, srv4, GPU 없음)

§43 §5 에서 "파라미터 말고" 로 찾아낸 셋 중 첫째. 우리 문은 `</tool_call>` 이 **닫혀야** 그 호출을 통째로 내보냈다. OpenAI 스트리밍 규격은 반대다 — **첫 델타가 `id`·`type`·`function.name`(인자는 빈 문자열), 그 뒤 델타마다 `function.arguments` 조각**. SGLang(`base_format_detector`: *"Tool names sent first with empty parameters, then arguments stream incrementally"*), vLLM(파서마다 `extract_tool_calls_streaming`), llama.cpp(`common/chat-parser.cpp`) 셋 다 그렇게 한다.

**GLM 형식에서는 인자가 호출의 대부분**이라 그 기다림이 답 전체의 기다림이었다. 실측(실제 tokenizer.json, 스텝당 6토큰, 1,000자 인자 하나):

| 본문 | 토큰 | 스텝 | **전**: 이름·인자 첫 글자 | **후** |
|---|---:|---:|---:|---:|
| 한국어 | 504 | 84 | 84번째 스텝 (**~3.9 s**) | **1번째 스텝** (~0.05 s) |
| 영어 | 207 | 35 | 35번째 스텝 (~1.6 s) | **1번째 스텝** |

한국어가 2.4배 더 기다린 건 §38 의 토큰 세금이 여기서도 곱해지기 때문이다.

**부분 파서**(`profiles/glm53/tools.partial_tool_calls`)는 시작된 호출마다 (이름, 지금까지의 인자, 닫혔는지)를 돌려준다. 계약은 하나 — **되돌아가지 않는다**. 조각 하나가 JSON 일 필요는 없고 **이어 붙인 것이 JSON** 이면 된다(그게 OpenAI 의 계약이다). 그래서:
- 값의 **타입이 정해진 뒤에만** 흘린다. `_value` 가 첫 글자로 판단하므로 `{[\"-0123456789tfn` 로 시작하는 값은 `</arg_value>` 까지 기다린다(`3` 이 `30`·`3.5` 가 될 수 있다). 평문 문자열은 타입이 바뀔 수 없으니 도착하는 대로 흘린다.
- **앞뒤 공백은 빼고** 흘린다 — `_value` 가 `strip()` 하므로, 흘린 뒤 도로 가져와야 하는 조각이 생기지 않게.
- **반쯤 도착한 `</arg_value>` 는 값이 아니다**. 정규식의 부정 전방탐색은 **완성된** 태그만 거르므로 꼬리에서 직접 잘라낸다(문이 `<tool_call>` 에 하는 홀드백의 한 단계 아래).
- `ensure_ascii=False` — §40 과 같은 이유로, 안 그러면 조각마다 한글이 6바이트가 된다.

**문 쪽**: `_Choice._tool_deltas` 가 보낸 만큼을 빼서 조각을 만든다. **부분 파서가 없으면 예전 그대로**(한 번에 통째로) 간다 — 기존 테스트가 그 경로를 그대로 통과한다.
**잘린 호출**: 답이 `</tool_call>` 전에 끊기면 그 호출은 `message.tool_calls` 에 **안 들어가고** `finish_reason` 도 `tool_calls` 가 아니다(`length` 그대로). 이미 읽힌 조각은 나갔지만, 인자가 안 닫힌 호출은 **호출자가 부를 수 없는 것**이기 때문이다.

**검증**: 다섯 스위트 188 tests, 실패 목록이 main 과 동일(사전 존재 12). 새 테스트 9개 — 그중 **모든 접두사에 대한 단조성**(여섯 모양 × 글자 단위 전수)과 **끝이 기존 파서와 같은지**가 핵심이다. 실제로 그 전수 검사가 구현 중 버그 둘을 잡았다(한글이 `\uXXXX` 로 새던 것, 반쯤 온 `</arg_value` 가 값에 섞이던 것).


### 45차 §45 — 도구 호출에 지연 문법: 선언 안 한 도구도, 없는 인자도 이제 못 쓴다 (2026-09-12, srv4, GPU 없음, ST 이미지 안에서 판정)

§43 §5 셋 중 셋째. **`tools` 는 챗 템플릿에만 갔고 아무도 그걸 강제하지 않았다** — 모델이 선언 안 된 도구 이름을 쓰거나 그 도구에 없는 인자를 써도 문이 그대로 클라이언트에 넘겼고, 클라이언트는 **부를 수 없는 호출**을 받았다.

llama.cpp 은 **지연 문법**(`grammar_lazy`, `grammar_triggers`)으로 `<tool_call>` **뒤부터만** 도구 스키마를 건다. 우리에겐 그 기구가 이미 있었다 — 다른 세션이 사고 블록 때문에 넣은 `options["grammar_after"]`("이 토큰 뒤부터 문법"). 그리고 실측: **마커가 전부 한 토큰**이다(`<tool_call>` 154843, `</tool_call>` 154844, `<arg_key>` 154847, `</arg_key>` 154848, `<arg_value>` 154849, `</arg_value>` 154850). 그래서 트리거가 정확히 우리 `grammar_after` 다.

**문법**(`profiles/glm53/tools.tool_grammar`, 트리거 뒤부터가 root): `root ::= call ("<tool_call>" call)*` / `call ::= call0 | call1 | ...` / 도구마다 `callN ::= "이름" pairsN "</tool_call>"`, `pairsN ::= ("<arg_key>" keyN "</arg_key>" "<arg_value>" value "</arg_value>")*`, `keyN ::= "선언된 키들"` — 그리고 `value` 는 **모든 문자**를 받는다.

**값은 안 묶는다.** 값은 원문 텍스트라 `<` 도 코드도 들어갈 수 있다. 닫는 태그와 모호해지는 게 **요점**이다: 값 안에서는 마스크가 아무것도 금지하지 않고, 모델이 닫고 싶을 때 닫는다. 묶는 것은 **이름·인자 키·모양** 셋뿐이다.
호출 뒤의 산문은 문법에 없는데 **잃는 게 없다** — `_Choice.flush` 가 이미 첫 `<tool_call>` 부터 뒤를 전부 버린다.

**판정**(ST 이미지 안, 실제 xgrammar·실제 어휘, `probes/st_tool_grammar.py`): 7/7 **PASS**

| | 기대 | 결과 |
|---|---|---|
| 한국어 값 / `<`와 코드가 든 값 / 인자 없는 도구 / 연속 두 호출 | 허용 | 전부 허용 |
| 선언 안 한 도구 `get_stock` | 거부 | 둘째 토큰(`_stock`)에서 거부 |
| 없는 인자 `zipcode` | 거부 | 넷째 토큰에서 거부 |
| 인자 없는 도구에 인자 | 거부 | `<arg_key>` 에서 거부 |

**배관**: `base/grammar` 가 셋째 명세 `{"type": "ebnf", "grammar": ...}` 를 컴파일한다(`Grammar.from_ebnf` -> `compile_grammar`, 둘 다 이 이미지에 있음을 확인). `sampler.validate_options` 가 그 명세를 받되 **문법 텍스트가 없으면 거절**한다(기존 테스트가 `{"type":"ebnf"}` 단독을 거부하도록 잡아 뒀고, 그 테스트가 그대로 산다). 컴파일 실패는 `prepare_options` -> `Grammars.ready` 를 지나므로 **입장 때 400** 이지 스텝에서 터지는 게 아니다.

**경계 셋**: `response_format` 이 있으면 그게 이긴다(한 행에 문법 하나). 트리거가 한 토큰이 아니면 **문법을 아예 안 건다**(D3 의 한 층 아래 — 마커 중간에서 무장하느니 안 한다). 선언된 도구가 없거나 이름이 없으면 `None`.

**검증**: 일곱 스위트 251 tests, 실패 목록이 main 과 동일(사전 존재 8). 새 테스트 7개(문법 모양·값은 자유·묶을 게 없으면 없음·트리거 토큰 네 모양·문이 실제로 보내는 것·response_format 우선·트리거 없으면 무장 안 함).


### 45차 §46 — 사고 예산: 생각이 답을 다 먹어 치우지 못하게 (2026-09-12, srv4, GPU 없음)

§43 §5 셋 중 마지막. 원장에 이미 있는 사고다 — **29차: GLM-5.3 이 분량 제약 있는 문제에서 사고 안에 초안을 쓰고 글자 수를 세며 다듬다 상한(16K·40K)에 걸려 답 0 자.** 그동안 우회는 프롬프트 쪽 `reasoning_effort=low` 뿐이었다.

llama.cpp 은 `--reasoning-budget N` 으로 사고를 묶고, 예산이 다하면 **사고 종료 태그를 강제**한다. 이제 우리도 한다.

**기본값은 숫자가 아니라 몫이다**(D11: 기본값은 코드로). `max_tokens` 자체가 §38 에서 **글자 예산의 환산값**이 됐으니, 사고 상한도 그 위의 몫으로 잡는다 — **답은 상한의 1/4, 최소 128 토큰을 언제나 남긴다**:

| `max_tokens` | 사고 | 답이 갖는 몫 |
|---:|---:|---:|
| 256 | 128 | 128 |
| 1,192 (한국어 기본) | 894 | 298 |
| 2,048 | 1,536 | 512 |

요청이 `reasoning_budget` 으로 덮어쓸 수 있다 — `-1` 은 무제한(llama.cpp 의 같은 이름과 같은 뜻), `0` 은 사고 금지, 그 외는 토큰 수. `-2` 이하는 400.

**강제는 `forbid` 의 거울이다.** `process_logits` 에 `force` 를 더했다: 그 id 하나만 남기고 전부 −inf. **`forbid` 가 이미 막은 토큰은 강제하지 않는다** — min_tokens 는 호출자에게 한 약속이고 예산은 엔진이 자리를 남기는 일이라, 약속이 이긴다.

**어디서 걸리나**: `adapter._reasoning_over` 가 (a) 예산이 있고 (b) 아직 사고 중이고 (c) `생성 수 + 이 스텝의 드래프트 ≥ 예산` 일 때 종료 토큰을 돌려준다. 스캔은 **예산이 다한 뒤에만** 돈다 — 그 순간과 토큰이 실제로 떨어지는 사이의 한두 스텝뿐이고, 블록이 스스로 닫히면 `thinking[seq]` 이 꺼져 **다시는 묻지 않는다**. 예산 전에는 비교 하나, 후에는 0.

**문법과의 자리**: 사고 중에는 문법이 잠들어 있고(`grammar_after` = 사고 종료 토큰, 또는 §45 의 `<tool_call>`), 강제하는 그 토큰이 바로 문법을 깨우는 토큰이다. 충돌이 아니라 맞물림이다.

**경계**: 프롬프트가 이미 사고 종료 토큰으로 끝났으면(블록 밖에서 시작하는 답) 예산을 아예 안 건다. 옵션은 **쌍으로만** 받는다(`reasoning_budget` 과 `reasoning_end` 중 하나만 오면 거절) — 예산은 "무엇을 써서 끝내는가" 없이는 뜻이 없다.

**검증**: 일곱 스위트 251 tests, 실패 목록이 main 과 동일. 새 테스트 11개(예산 아래/다한 뒤/드래프트로 다한 경우·스스로 닫은 블록은 다시 안 묻는다·예산 없으면 무제한·min_tokens 우선·옵션 쌍·기본값이 답에 남기는 몫·문이 실제로 보내는 것·`-1`/`0`/숫자 덮어쓰기·블록 밖에서 시작하면 안 건다).
**못 잰 것**: 이것이 실제 답의 품질을 어떻게 바꾸는지는 GPU 창이 있어야 한다. 지금 판정할 수 있는 것은 **답이 0 자로 끝나는 일은 이제 구조적으로 불가능하다**는 것뿐이다.
### 45차 §48 — 엔진 스위트가 오랫동안 빨갰다: 셋이 썩어 있었고 여덟이 안 돌고 있었다 (2026-09-12, srv4, GPU 없음)

운영자 "고쳐 직접". §46 을 검증하다 **main 자체가 빨간 것**을 봤다. 그런데 이등분해 보니 오늘 것이 아니었다 — **이 세션이 시작한 커밋(`fb7fe803`)에서 이미 같은 실패**였다. 나는 내내 *main 대비 상대 비교*만 했기 때문에 절대 상태를 못 봤다. 그게 첫 교훈이다: **"회귀 0" 은 "초록" 이 아니다.**

**썩어 있던 셋** — 전부 테스트 쪽이 엔진을 못 따라간 표류다(엔진은 멀쩡했다):

1. **`NextTokenNet` 에 `head_local` 이 없다** (6 errors). 어댑터가 랭크별 head 와 게더를 분리하면서 `net.head_local(h)` 를 부르게 됐는데, 가짜 네트는 `head`/`head_tokens` 만 들고 있었다. 실물 `Net` 은 `head = all_gather(head_local)` 이라 한 랭크에서 둘은 같은 것이므로, 가짜에도 그렇게 넣었다.
2. **`NextTokenNet` 에 `comm` 이 없다** (1 error). `_warmup_prefill_memory` 가 `self.net.comm.all_reduce_max(...)` 를 쓴다. `Comm()` 기본값이 한 랭크라 그대로 붙였다.
3. **`test_replacing_a_row_s_tokens_...` 가 메서드 이름을 단언하고 있었다** (1 failure). `assertIsNone(engine.history)` 인데 `history` 는 **행을 받는 메서드**라 항상 not-None 이다. 지연 생성되는 것은 `sampling_history` 다. **이 테스트는 자기가 검사한다고 말한 것을 한 번도 검사한 적이 없다** — 이름이 바뀐 뒤로 통과만 하고 있었다.

**안 돌고 있던 여덟** (12 errors): `tests/test_engine_serve.py` 가 형제 모듈을 `from test_engine_tier import MemoryTier` 로 부른다. 루트에서 `python3 -m unittest tests.test_engine_serve` 로 돌리면 `tests/` 가 경로에 없어 **열두 테스트가 실행 대신 임포트 에러**로 끝났다 — 티어·파킹 쪽 검사 전부다. `ROOT/"tests"` 를 경로에 넣어 어느 쪽으로 시작하든 붙게 했다.

**결과**(호스트, 엔진 스위트 54개 전부):

| | 전 | 후 |
|---|---:|---:|
| failures | 1 | **0** |
| errors | 36 | **29** |

남은 29 는 전부 **환경**이다: `flashinfer` 24, `cutlass` 1, `ninja` 1, "No available kernel" 3 — 이미지 안에서만 도는 커널 스위트다. **호스트에서 돌 수 있는 것은 이제 전부 초록**이다.

**교훈 둘**: (a) 회귀 판정을 main 대비로만 하면 main 이 썩는 것을 아무도 못 본다 — 절대 수를 가끔 봐야 한다. (b) 가짜가 실물을 못 따라가면 테스트는 조용히 **안 도는 상태**가 된다. 특히 3번은 통과하면서 아무것도 안 보고 있었다.


#### §48 보충 — 남은 29 를 점검했더니: 26 은 거짓말이었고, 3 은 설계대로였고, 넷은 안 돌고 있었다 (2026-09-12)

운영자 "나머지 에러 점검". §48 에서 "남은 29 는 전부 환경" 이라고 적었는데, **그 문장이 바로 §48 이 경고한 실수**였다 — 세어 보지 않고 분류했다. 하나씩 봤다.

**증거부터**: `nemotron-vllm` 컨테이너에 flashinfer·cutlass·tilelang 이 있다. 거기에 같은 트리를 넣고 CPU 로 돌리니 **68 tests, 46 skipped, error 1**. 호스트에서 29개 에러를 내던 것이 **패키지만 있으면 조용히 skip** 된다는 뜻이고, 동시에 **에러 하나는 진짜였다.**

1. **26 은 거짓말이었다.** 그 스위트들은 `skipUnless(torch.cuda.is_available())` 로만 막혀 있었는데, **플릿 노드에는 CUDA 가 있다**. 그래서 skip 이 아니라 `setUp` 이 돌고 flashinfer 임포트에서 터졌다 — **"여기 없다" 는 뜻의 에러**다. §48 의 교훈이 그대로 반복됐다: 그런 에러는 진짜 문제가 있는지를 가린다.
   `tests/image_kernels.py` 를 두고(`flashinfer·tilelang·cutlass·deep_gemm·ninja` 유무), 11개 스위트의 관문에 더했다. 이제 호스트에서 **"requires the ST image's kernel packages (... missing)"** 로 skip 한다.
2. **3 은 설계대로였다.** `test_engine_drafter` 의 `No available kernel` 은 `drafter._attn_rows` 가 SDPA 를 **cuDNN/efficient 로 못박고 math 폴백을 거부**하기 때문이다 — 코드에 그렇게 적혀 있다(`# D3: no math fallback on CUDA`). 이 호스트 torch 는 그 둘을 head_dim 4 에서 런타임 거부한다(플래그 `cudnn_sdp_enabled()` 등은 **전부 True 를 반환하면서** 그렇다 — 거부는 호출·모양·아키텍처마다 정해진다). **엔진은 옳게 행동하고 있었다.** 그래서 관문을 "쓸 모양으로 한 번 물어본다"(`fused_sdpa(head_dim)`)로 바꿨다.
3. **넷은 안 돌고 있었다.** `test_engine_{boundary_stage,kda_marks,kda_norm,state}` 가 `from test_engine_glm53 import tiny_facts` — §48 에서 고친 것과 **같은 형제 임포트 버그**다. 호스트에서는 flashinfer 에러에 가려 안 보였고, 컨테이너에서 드러났다.

**결과**(호스트, 엔진 스위트 54개 전부): **671 tests, OK (skipped=79)** — failures 0, errors 0.
같은 트리를 패키지가 있는 컨테이너에서 돌리면 **76 tests, OK (skipped=49)** 로, 호스트에서 못 돌던 27개가 실제로 돈다. 관문이 정직하다는 뜻이다.

**교훈**: "환경 문제" 는 **분류가 아니라 주장**이다. 주장하려면 그 환경을 하나 구해서 돌려 봐야 한다 — 그렇게 하니 26/3/4 로 갈렸고, 그중 넷은 아무도 안 돌리고 있던 테스트였다.


### 45차 §49 — 부팅 창의 호스트 메모리: 스테이징 버퍼가 필요보다 862 MiB 넓었고, 좁히니 더 빨랐다 (2026-09-12, srv4, GPU 없음)

운영자 "st엔진 메모리 최적화". `MEMORY_REVIEW_20260911` 의 다섯(3.1~3.5)은 PR #550·#552 로 끝났고 남은 것은 플릿 실측이라,
**검토가 안 본 쪽**을 봤다 — 그 검토는 전부 **아레나(디바이스)** 얘기고, **오늘 엔진을 실제로 죽인 것은 호스트 쪽**이었다.

**오늘의 증거**(§48 을 검증하다 journal 에서): 13:47:57 earlyoom 이 엔진 본체(root python3, VmRSS 5,364 MiB)에 SIGTERM 셋, 다른 python3 에 SIGKILL —
`mem avail 4,538 / 124,546 MiB (3.64%)`, SIGKILL 선 3.70% 아래. 직전 13:46:36 에 NVRM 이 할당 실패를 찍었다. 아레나가 아니라 **상자의 여유**가 먼저 무너진다.

#### 재는 자리: 로드 중 핀 메모리

`RankLoader.load` 는 읽기와 업로드를 겹치려고 **핀 호스트 버퍼 둘**을 잡는다. 폭은 `staging_bytes = 가장 넓은 run + 2 섹터`, run 은 `max_run` 으로 잘린다.
실제 랭크 파일(`st-glm53-9391-up-gate-full/rank0of4.safetensors`, **1,376 텐서 / 44.50 GiB**, O_DIRECT 가능)로 계산:

| `max_run` | runs | 가장 넓은 run | 버퍼 둘 |
|---:|---:|---:|---:|
| **1,024 MiB (기존)** | 54 | 1,017.5 MiB | **1.987 GiB** |
| 512 MiB | 87 | 576.0 MiB | **1.125 GiB** |
| 256 / 128 / 64 MiB | 174 / 186 / 263 | 576.0 MiB | 1.125 GiB |

**바닥은 텐서 하나**다 — 이 체크포인트의 가장 큰 텐서가 576 MiB라, 그 아래로는 아무리 잘라도 버퍼가 안 줄고 run 수만 는다.
그러니 상한의 유일한 역할은 **합치기가 버퍼를 텐서보다 넓게 만드는 것을 막는 것**이고, 그 점에서 512 MiB 가 최적이다(바닥에 닿으면서 run 이 가장 적다).

#### 그런데 좁은 쪽이 더 빨랐다

로더 자신의 읽기 경로로 실제 파일을 O_DIRECT 로 읽어 3회(상자가 바빠 번갈아 돌리고 각자 최소를 본다, §28 규율):

| | 1,024 MiB | 512 MiB |
|---|---:|---:|
| GiB/s | 3.94 / 4.20 / 4.38 | **4.75 / 4.93 / 5.18** |

로더 문서가 적어 둔 디스크의 O_DIRECT 상한이 5.1 GB/s 다 — **좁은 버퍼는 거기에 닿고 넓은 쪽은 못 닿는다.**
즉 이 상한은 메모리와 속도를 맞바꾸는 게 아니라 **둘 다 잃고 있었다.**

**한 것**: `MAX_RUN = 512 << 20` 을 `base/loader` 의 이름으로 두고 `runs`·`checkpoint.load` 의 기본값으로 썼다. 그리고 **run 이 하나뿐이면 버퍼도 하나**만 잡는다(겹칠 것이 없는데 둘째가 반 GiB를 핀으로 붙들고 있었다).
합쳐서 **로드 중 핀 호스트 메모리 1.987 → 1.125 GiB, 862 MiB 반환**. 핀 메모리는 스왑도 회수도 안 되므로 earlyoom 이 세는 그대로다 — 그리고 반환되는 시점이 **페이지 캐시가 방금 읽은 파일로 차 있고 아레나를 막 달라고 할 참**인, 상자가 가장 좁은 그 순간이다.

#### 같이 본 것: 널 슬롯 247.2 MiB (후보, 관문 있음)

`cache_spec: slots_bytes = slot_bytes * (max_seqs + 1)`, 주석 그대로 `+1: slot 0 is the kernels' null block, never issued`. 엔진은 `0 < slot` 으로 모든 접근을 막고(`caches.py` 다섯 군데), 커널은 인덱스 0 을 **건너뛴다**(`kv.py` 의 설명: no error, no output, state untouched).
그러니 GLM-5.3 의 슬롯 247.2 MiB × 랭크 = **디바이스에서 한 번도 안 닿는 247.2 MiB**, 4랭크면 약 1 GiB.
**안 건드렸다**: 슬롯 0 을 슬롯 1 에 겹치면 "건너뛰지 않는 커널" 이 조용히 남의 상태를 덮는다 — 지금의 247 MiB 는 D3 가 *"그 실수를 표현 불가능하게"* 만든 값이다. 되찾으려면 **모든 커널이 0 을 건너뜀을 장비에서 증명**해야 하고, 아레나 55.41 GiB 의 0.45% 라 값이 크지도 않다. 후보로만 적어 둔다.

**검증**: 엔진 스위트 54개 전부 **683 tests, OK (skipped=79)**. 새 테스트 둘(상한이 버퍼를 텐서 바닥까지 좁힌다 · run 이 하나면 버퍼도 하나).


### 45차 §50 — 메모리를 vLLM 과 줄 맞춰 보니: 부팅은 저쪽이 재고 우리는 선언하고, 서빙은 둘 다 눈이 없다 (2026-09-12, srv4, GPU 없음)

운영자 "vllm과 비교하면 부팅 및 서빙중 메모리 최적화가 어때". 같은 상자 이미지의 vLLM 을 읽고 우리 두 검토와 줄을 맞췄다.
전문은 `engine/MEMORY_VS_VLLM_20260912.md`.

**부팅**: vLLM 의 KV 예산은 측정에서 나온다 — `max_num_batched_tokens` 로 **더미 포워드 한 번**(`profile_run`)을 돌려 과도 피크를 재고,
`profile_cudagraph_memory()` 로 그래프를 추정해 빼고, `total × gpu_memory_utilization` 에서 둘 다 뺀다. 재기 싫으면 `kv_cache_memory_bytes` 로 박는다.
우리 `KV_GIB = 8.73` 은 **40차 vLLM 부팅의 잔여**다. `budget.py` 가 줄은 세웠지만 **프리필 활성화 피크와 그래프·워크스페이스 두 항이 아직 미측정**이라 그 줄이 아직 안 닫힌다.
우리가 나은 자리는 **로드 경로**(합치기·O_DIRECT·뷰, 핀 1.125 GiB — §49)와 **부팅 뒤 발자국이 상수**(아레나 하나)라는 점이다.

**서빙**: 셋으로 갈린다.
- **블록 부족**: vLLM 은 선점(재계산 또는 `--swap-space` 기본 4 GiB CPU), 우리는 과다 입장을 안 한다(D3). 의도된 차이 — vLLM 기본값도 같은 쪽이다.
- **런타임 반납**: vLLM 은 `sleep`/`wake_up` 으로 가중치를 OS 에 돌려줄 수 있다(`CuMemAllocator`). 우리는 **구조적으로 못 한다**(한 할당 + 범프, NVRM 핀). 요구가 없으니 손해는 아니지만 선택지가 없다.
- **상자 밖 대화**: vLLM 의 스왑은 **선점된 블록만** 4 GiB. 우리 NVMe 티어는 **파킹된 대화 전체**를 LRU 로 턴 너머까지. **우리가 더 많다.**

**그리고 둘 다 없는 것 하나 — 서빙 중 메모리를 보는 눈.** vLLM 의 `gpu_cache_usage_perc` 는 **블록 점유율이지 바이트가 아니고**, 메트릭 로거에 `mem_get_info`·`memory_reserved` 가 없다. 우리도 없었다.
그런데 `OOM_STUDY` 의 결론이 정확히 그 자리다 — 이 기계에선 **`memory_reserved` 를 봐야 하고**(예약−할당은 캐시가 아니라 잃은 메모리), **26 → 5 GiB 낙하가 4 초**라 주기는 스크레이프가 아니라 스텝이어야 하며, earlyoom 은 절대 6 GiB 에서 **엔진을 1순위로** 쏜다.
그리고 오늘 13:47 그 일이 났다(§48): `mem avail 4,538 / 124,546 MiB (3.64%)`. **엔진이 내보내던 어떤 숫자도 그게 오는 걸 못 보여줬다.**

**한 것**: `/metrics` 가 상자의 메모리를 말한다 — `st:host_memory_available_bytes`(**earlyoom 이 결정에 쓰는 바로 그 줄**, MemFree 아님),
`st:device_memory_reserved_bytes` / `_peak_bytes` / `_allocated_bytes` / `_free_bytes` / `_total_bytes`.
**고수위(`max_memory_reserved`)를 현재값 옆에 같이 내는 게 4초 절벽 문제의 답**이다 — torch 가 공짜로 들고 있어 **스텝당 비용이 0** 인데 스크레이프 사이의 최악을 보여준다.
없는 숫자는 **줄을 빼지 스크레이프를 깨뜨리지 않는다**(CUDA 없는 상자, /proc 이 답 안 할 때).

**남은 숙제**(전부 장비 필요): (1) 프리필 활성화 피크·그래프 워크스페이스 실측 → `budget.py` 의 두 빈 줄, vLLM 은 `profile_run` 한 번으로 얻는 값이고 우리 `RuntimeMemory.checkpoint` 가 이미 페이즈를 남긴다. (2) 그 값으로 KV 8.73 GiB 재결정(검토 계산으로는 50 GiB 대가 비어 있다). (3) 널 슬롯 247.2 MiB 관문(§49).

**검증**: 엔진 스위트 54개 전부 685 tests, OK (skipped=79). 새 테스트 2개(상자 메모리가 스크레이프에 있다 · 숫자가 없어도 스크레이프는 답한다).

### 45차 §51 — 그 "장비 필요" 숙제는 이미 원장에 있었다, 그리고 서빙이 끝나면 아레나를 내놓는다 (2026-09-12, srv4, GPU 판정 1건)

운영자 "그거 개선하고, 서빙 종료 후에 메모리를 깔끔하게 반납하도록 해". §50 의 숙제 (1) 과 반납, 둘 다.

**1. 두 빈 줄 — 재는 게 아니라 읽는 것이었다.** §50 에 "전부 장비 필요" 라고 적었는데 틀렸다.
`_warmup_prefill_memory` 는 **그래프를 하나도 잡기 전에** 가장 큰 합법 프리필을 돌리고 페이즈를 남기고, 캡처는 캡처대로 전부 체크포인트한다.
vLLM 이 `profile_run` + `profile_cudagraph_memory` **두 번**으로 얻는 분리가 우리 원장에 **이미 있었고 아무도 되읽지 않았다**.
`RuntimeMemory.measured()` 가 그 분리를 한다 — 마지막 `prefill/` 행에 서 있는 피크가 곧 **활성화 피크**(그 앞은 아레나뿐이고 그보다 큰 일은 아직 없었다),
그 뒤 예약이 **더해서 안 돌려준 만큼**이 그래프 풀 + 상주 스크래치다. `__init__` 은 **런타임 바닥**도 같이 적는다 — 아레나가 생기기 전, 캐싱 할당자 밖에서 상자가 이미 내려가 있는 양(`(total − free) − memory_reserved`; 컨텍스트 + NCCL + 원샷).

**오늘 15:16 부팅의 실제 원장(`memory-rank3.json`)으로:**

| 줄 | 그동안 적혀 있던 값 | 이 스택의 실측 |
|---|---|---|
| 프리필 활성화 피크 | 3.5 GiB (vLLM 기울기 0.52 GiB/1K × 6,912) | **5.72 GiB** (자격 검증된 4 형상) |
| 그래프 + 스크래치(상주) | — (선언 12 GiB 안에 뭉개짐) | **+0.60 GiB** |
| 아레나 밖 피크 | — | **6.32 GiB** (`production/ready` 까지) |
| 워크스페이스 천장 | 12.00 GiB 선언 | 12.00 GiB 그대로, **5.68 GiB 가 한 번도 안 쓰임** |

빌려온 기울기는 **39% 낮았다**. 실제 활성화가 5.72 인데 3.5 로 적혀 있었다.

**천장은 줄에서 안 내렸다.** 할당자는 12 GiB 를 끝까지 내준다 — 상자는 그걸 흡수할 수 있어야 하므로 **줄은 천장이 맞다**.
바뀐 건 근거가 vLLM 기울기에서 **이 스택의 분리**로 바뀌었다는 것과, 표가 이제 *"천장을 낮추면 KV 로 얼마가 돌아온다"* 를 말한다는 것이다(+5.68 GiB). 천장을 실제로 낮추는 건 부팅 결정이고 창이 필요하다 — **말했지 집행하지 않았다**.
바닥 줄(5.54 GiB, vLLM 40차 표)은 새 원장부터 실측으로 대체된다. 오늘 원장은 이 변경보다 먼저 쓰였으므로 **다음 부팅까지는 빌린 숫자 그대로 읽힌다** — 표가 `[ledger] ... no ST ledger given` 이라고 스스로 말한다.

**그리고 부팅이 표를 두 번 찍는다**: 한 번은 한 바이트도 할당하기 전 선언으로(지금까지처럼), 한 번은 `production/ready` 직후 **자기가 방금 쓴 원장으로**. 되읽는 비용은 표 하나.

**2. 서빙 종료 = 상자 반납.** 종료 경로는 `close_decode()` 에서 멈춰 있었다 — 그래프는 갔고 **아레나는 안 갔다**.
그런데 아레나의 참조를 놓는 것으로는 **아무것도 안 풀린다**: 모든 carve 는 `buf` 의 **뷰**고, 그 순간 `build` 가 돌려준 `net`·`caches` 를 **아직 스택 위 프레임이 붙잡고 있다**.
그래서 `Arena.release()` 는 참조를 놓지 않고 **스토리지를 0 으로 줄인다**. 누가 가리키고 있든 블록은 할당자로 돌아가고, `empty_cache` 가 expandable 청크를 드라이버에 넘기고, 뷰는 **0 바이트 텐서**로 남아 쓰면 남의 메모리를 읽는 대신 **예외를 낸다**(D3).

**장비 판정**: 256 MiB 아레나에서 64 MiB 뷰를 **release 를 가로질러 붙잡은 채** — `memory_reserved` −256 MiB, 뷰의 스토리지 0 바이트, `carve` 는 `MemoryError`. (srv4, MemAvailable 98 GiB 확인 후 단독 실행)

**아레나 밖에 있던 것도 같이.** NVMe 티어의 스테이징은 **아레나 밖**이라 엔진의 release 가 닿지 못하고 `empty_cache` 도 가져가지 못한다 — 대화 티어 64 MiB 핀 호스트 + 64 MiB 디바이스 스크래치, 접두사 티어 32 + 32, **랭크당 192 MiB, 상자 전체 768 MiB**(그 중 디바이스 512 MiB). `NvmeTier.close()` 가 둘을 내놓는다. **디스크의 인덱스와 파킹된 대화는 안 건드린다** — 프로세스보다 오래 사는 게 티어의 존재 이유다(D16). 티어를 먼저 닫고 아레나를 놓는다.

**놓는 자리가 중요하다.** `fleet` 의 `finally` 안, **교정 블롭을 파일링한 뒤**(그 블롭들이 아레나 뷰다) **`comm.close()` 앞**.
프로세스가 어차피 죽는데 왜 하냐 — **안 죽고 있기 때문이다**: 데스덤프 닫기·블롭 파일링·NCCL 해체가 초 단위고, 인수인계는 **다음 점유자가 같은 55 GiB 를 이미 달라고 하는 중**이며 earlyoom 은 절대 6 GiB 에서 엔진을 1순위로 쏜다(OOM_STUDY §2). 그 초들이 전부다.

**안 한 것**: 천장 12 → 실측 근처로 낮추기(부팅 결정, 창 필요), KV 8.73 재결정(§50 숙제 2), 널 슬롯 관문(§49) — 그대로 남는다.

**검증**: 엔진 스위트 710 tests, OK (skipped=146, `CUDA_VISIBLE_DEVICES=` 단독). 새 테스트 16개 —
아레나(살아 있는 뷰를 가로질러 해제·멱등·해제 뒤 carve 거절·표), 엔진 릴리스(그래프가 먼저·아레나 밖 보유자·아레나 없는 엔진·두 번 호출·리포트·**장비 1건**),
런타임 메모리(바닥·프리필/그래프 분리·페이즈 없으면 침묵), 예산(원장이 두 줄을 채운다·리포트 dict 자체가 원장·원장 없으면 그대로),
티어(스테이징 반납 + 디스크 보존·바 티어는 0·**전송 중이면 기다린다**·실패한 전송이 종료를 막지 않는다).

### 45차 §52 — "확실히 깔끔하게": 아레나만으론 안 끝난다, 그리고 끝났는지 말하는 숫자 (2026-09-12, srv4, GPU 판정 2건)

운영자 "확실히 깔끔하게 메모리를 놔주도록해". §51 의 반납은 **아레나와 티어 스테이징**까지였다. 그 둘은 큰 덩어리지만 전부가 아니다.

**아레나 밖에 살아 있던 것들 — 하나씩 찾아야 했다.** `empty_cache` 는 **아무도 참조하지 않는 것**만 가져간다. 엔진이 붙잡고 있으면 아무 일도 안 일어난다:

| 붙잡고 있던 것 | 무엇 |
|---|---|
| 요청별 | 문법 매처(`matchers`), 그림 행(`embeds`), 요청별 종료 토큰(`_ends_tensor`), 시드 생성기(`gens`) |
| `_ids_stage` | 캡처된 디코드의 **핀 호스트** 업로드 스테이징 |
| `_rich_stage` | 리치 샘플러의 `[rows, vocab]` fp32 **두 장** |
| `sampling_history` | 페널티 텐서 |
| `grammars` | xgrammar 컴파일 문법 + 비트마스크 버퍼 |
| `vision._rope` | **이번 부팅이 본 그림 격자마다 한 장**씩 쌓인 rope 테이블 |
| `caches._id_ring` | 핀 호스트 16개 + 그 이벤트들 |

요청별 상태는 `close(seq)` → `forget(seq)` 순서로 놓는다 — `forget` 은 슬롯을 쥔 요청을 **거부**하므로 순서가 계약이고, 테스트가 그 순서를 박아 둔다.

**그리고 "끝났는지" 를 말하는 숫자.** 그동안 종료 줄은 `memory_reserved` 만 말했다. 그건 **할당자가 들고 있는 양**이지 **살아 있는 텐서가 붙잡은 양**이 아니다. 후자가 진짜 판정이다 — 아무리 `empty_cache` 해도 안 돌아오는 부분이니까.
`base/runtime_memory.live_device_blocks()` 가 `memory_snapshot()` 에서 **아직 `active_allocated` 인 블록을 큰 것부터** 돌려준다. 이름은 없다(`_record_memory_history` 를 켜야 이름이 붙고, 그건 매 부팅에 비용이다) — **268 MiB 하나와 2 MiB 마흔 개는 다른 버그**이고, 리스트의 모양이 그걸 말해 준다.

종료 줄이 이렇게 바뀐다:

```
released: rank 0 gave back 55.41 GiB of 55.41 GiB arena plus 192 MiB of tier staging;
          0.05 GiB reserved and 3 MiB allocated still
```

그리고 `CLEAN_RELEASE_BYTES = 64 MiB` 를 넘으면 **한 줄 더**: `did NOT come back clean -- N MiB is still held by live tensors, largest blocks ...`.
이건 **누수 허용치가 아니다** — CUDA 컨텍스트가 프로세스 수명 동안 들고 있는 것들(NCCL 버퍼, 런타임 게이트의 상태 워드, import 시점에 커널 모듈이 만든 것)의 **바닥**이고, 그 위는 전부 `release` 가 **못 찾은 보유자**다.

**문 전부가 같은 경로를 쓴다.** `boot.release_all(engine, runner)` + `boot.release_line(report, rank)` 하나로 프로덕션 `fleet`, 서브 스모크 `local_serve`, 층 부분집합 스모크 `local` 셋 다.
스모크 둘은 **판정에 넣었다** — `allocated_after > 64 MiB` 인 랭크가 하나라도 있으면 **FAIL**. 즉 다음 창의 아무 스모크나 깨끗한 반납의 **상시 관문**이 된다.

**장비 판정 2건** (srv4, 256 MiB 규모 단독): (1) 살아 있는 64 MiB 뷰를 가로질러 아레나 해제 → `memory_reserved` −256 MiB, 뷰 스토리지 0; (2) `live_device_blocks()` 가 아무도 안 놓은 8 MiB 텐서를 **큰 것부터** 집어낸다.

**안 한 것**: 45층 실물에서의 `allocated_after` 실측 — 플릿 창이 필요하고, 위 관문이 그 창에서 스스로 답한다.

**검증**: 엔진 스위트 **719 tests, OK (skipped=147**, `CUDA_VISIBLE_DEVICES=` 단독). 새 테스트 9개 —
살아 있는 요청은 **닫힌 뒤에 잊힌다**(`forget` 의 거부가 순서 계약), 아레나 밖 보유자 열넷이 전부 비워진다,
리포트가 `allocated_after` 와 생존 블록을 싣는다, 깨끗한 줄/안 깨끗한 줄/바닥값 셋, 티어 먼저·티어가 실패해도 아레나는 간다·러너 없는 문,
`live_device_blocks()` 가 장비 없으면 빈 목록·있으면 큰 것부터.

### 45차 §53 — "종료하면 찌꺼기 안 남아?" 물어서 디스크를 봤더니: 루트 99%, 그중 103 GB 가 티어였다 (2026-09-12, srv4, GPU 없음)

운영자 "이제 확실히 실행후 종료되어도 찌꺼기가 안남아?". **메모리는 그렇고(§51·§52) 디스크는 아니었다.**

```
/dev/nvme0n1p2  3.6T  3.4T  37G  99% /
```

**찾은 것 셋.**

1. **엔진은 티어 디렉터리를 지운 적이 없다.** 실험마다 하나씩 생기고 영원히 남는다 — 죽은 것 여섯 개에 **13.6 GiB**
   (`st-native-f4d7-h` 6.1G · `st-onepass-main-59497b6f` 5.2G · `st-native-f4d7` 2.3G · `-e` 236M · 빈 것 둘).
2. **두 티어 모두 상한이 없었다.** `boot` 가 `capacity_bytes` 를 안 넘겨서 유일한 제동이 `reserve_bytes = 1 GiB` — 체크포인트·이미지·로그가 같이 사는 루트에서
   **디스크가 1 GiB 남을 때까지 먹는다.** 실제로 rank3 하나가 **대화 65 GiB + 접두사 경계 9.7 GiB = 75 GiB**.
3. **레이아웃이 다른 항목은 영원히 남는다.** 부팅은 개수만 세어 찍었고(바이트도 안 말했다) `oldest()` 는 `has()` 통과분만 골랐다.
   오늘 두 체크포인트가 `block_bytes` **13,922,304 vs 4,730,880** 으로 갈렸는데, 그러면 **어느 쪽이 부팅하든 상대의 280 대화 85 GiB 를 "못 쓰지만 보관" 으로 붙잡는다.**

**고친 것.**

- `NvmeTier.oldest()` 가 **외래 레이아웃을 먼저** 내놓는다. 디스크를 차지하고 상한에 계산되는데 이 부팅이 promote 할 수 없으면 그건 대화가 아니라 바이트다.
  이게 없으면 상한을 선언하는 순간 **교착**이다 — stale 만으로 상한이 차면 LRU 가 내놓을 게 없어 **모든 park 가 영원히 TierFull**.
- `TIER_GIB = 64.0` · `PREFIX_TIER_GIB = 16.0` · `TIER_RESERVE_GIB = 16.0` 선언(D1: "파일시스템이 정한다" 는 결정이 아니다).
  파킹 하나가 여기선 ~260 MiB(블록 하나 + **247 MiB 상태 슬롯** — §49 가 남긴 그 크기)이므로 64 GiB ≈ 250 대화, 16 GiB ≈ 30 경계.
  접두사가 작은 몫인 건 **경계는 다시 계산되는 캐시고 대화는 사용자가 돌아올 턴**이기 때문(D16).
- 부팅 줄이 **바이트로 말한다**: `N conversations parked from before, 65.0 GiB of 64 GiB; 280 under another layout holding 85.2 GiB -- not resumable, and the first thing forgotten when the cap bites`.

**집행**: 죽은 실험 티어 여섯 개 삭제(운영자 승인) — 루트 여유 **36 → 49 GiB**. 프로덕션 `st-tier` 85 GB 와 진행 중인 `st-checkpoint-ab` 4.8 GB 는 **안 건드렸다**.
`st-tier` 는 다음 부팅부터 상한이 물어 LRU 가 외래 레이아웃부터 잊는다.

**정정**: 상한을 물을 때 예비 공간도 64 GiB 로 올리겠다고 선택지에 적었는데 **틀렸다** — 지금 여유가 49 GiB 라 그러면 **모든 park 가 즉시 TierFull** 이 되어 st-tier 를 거의 전부 비웠을 것이다. 예비는 16 GiB(기존 1 GiB 의 16배)로 두고 상한만 64 GiB 로 선언했다.

**안 한 것**: `st-tier` 의 85 GB 는 그대로(운영자 선택). SIGKILL 로 죽으면 `finally` 가 한 줄도 안 도는 것도 그대로 — 디바이스는 커널이 컨텍스트 해체로 회수하지만 디스크는 남는다.

**검증**: 엔진 스위트 **725 tests, OK (skipped=147**, `CUDA_VISIBLE_DEVICES=` 단독). 새 테스트 6개 — 외래 먼저 잊는다·`stale_bytes`·**외래로 가득 찬 티어도 자리를 만든다**(교착 회귀)·`deleting` 은 두 번 안 나온다·상한 둘의 크기 관계·부팅 줄이 GiB 로 말한다/외래 없으면 언급 안 한다.

### 45차 §54 — 구현 범위 안에서 vLLM 과 맞대 보니: 앞선 것 여섯, 뒤처진 것 다섯, 나머지는 범위 밖 (2026-09-12, srv4, GPU 없음)

운영자 "구현 범위 내에서 vllm과 st엔진 비교". 프로덕션 이미지의 vLLM 소스를 **꺼내서**(2,421 파일, `0.1.dev20051+g487ecf187`)
ST 코드와 파일 단위로 맞댔다. 전문은 `engine/VLLM_SCOPE_COMPARISON_20260912.md`.
기존 `VLLM_COMPARISON_20260912.md` 가 *"가져올 것"* 이었다면 이건 *"지금 있는 것끼리"* 다 —
**우리가 아예 안 만든 것은 뒤처짐이 아니라 범위 밖**으로 따로 적었다.

**먼저 규모**: ST 엔진 161 파일 80,671줄(그중 커널 60,024). vLLM 은 해당 영역만 봐도 문 13,930 · `v1/core` 11,907 ·
`v1/attention` 47,951 · `v1/kv_offload` 13,467 · 스펙 디코딩 6,062. **줄 수는 기능이 아니다** — 저쪽은 모델 수백 개 ×
하드웨어 세대 여러 개를 덮고, 우리는 GB10 한 상자 × GLM-5.3 하나를 덮는다.

**우리가 앞선 것 여섯** (전부 실측 근거 있음): 디토크나이저(복구에서 **토큰을 안 잃는다**, vLLM 은 하나 잃는다),
페널티(증분, 128K 스텝 **456 → 0.130 ms**; vLLM 은 자기 코드에 *"quite inefficient"* 라고 적어 뒀다),
구조화 출력 **적용 경로**(스텝 하나의 비트마스크, 죽은 드래프트 위치는 게더조차 안 함),
**서빙 중 메모리 관측**(저쪽 `gpu_cache_usage_perc` 는 블록 점유율이지 바이트가 아니다; 우리는 `memory_reserved` 고수위와
earlyoom 이 보는 `MemAvailable` 까지), **종료 시 반납**(§51·§52, vLLM 에는 종료 증명이 없다),
그리고 **지표 개수 69 대 42** + vLLM 에 대응물이 없는 스텝 링·DeathDump.

**동등한 것**: 블록 수명(§32 에서 저쪽 규칙을 그대로 가져왔고 등급이 하나 더 많다), 샘플러 처리 순서와 페널티 범위,
청크 프리필과 그 사이 디코드.

**의도적으로 다른 것**: 선점 없음·혼합 스텝 없음(D3·D9, 39차 실측이 근거). vLLM 기본값 `full_sequence_must_fit=True` 도 같은 쪽이다.

**이 범위 안에서 진짜 뒤처진 것 다섯** —
(1) **`min_p`**(이미지 안 `flashinfer.sampling.min_p_sampling_from_probs` 로 바로 닿는데 미구현),
(2) `bad_words`·`allowed_token_ids`·`prompt_logprobs`,
(3) **동적 드래프트 길이**(우리는 고정 k=5; vLLM 은 배치 크기 → k 표. 위치별 수락 분포는 이미 내고 있으니 **재기만 하면 된다**),
(4) **스냅샷 예산** — 접두사 입자는 **우리가 더 곱다**(블록 768 vs vLLM 의 GLM5Next 기본 1,152). 제약은 입자가 아니라
스냅샷 **96개**라서, 73,728 토큰이 넘는 프롬프트는 진행하며 자기 앞선 경계를 스스로 축출한다
(`runner._note_fade`, 계량기 `st:prefix_snapshot_self_evicts_total` — **이미 나가고 있고 아직 안 읽었다**),
(5) 접두사 캐시를 통째로 버리는 운영 훅(`/v1/prefix/unpin` 은 한 프롬프트의 핀만 푼다).

**발견 하나**: 이 vLLM 포크의 `v1/spec_decode/` 에 **`dflash.py`** 가 있다 — 우리가 쓰는 드래프터와 같은 계열이 저쪽에도 들어가 있다.

**범위 밖**(뒤처짐 아님): 임베딩·리랭크·오디오·Responses·Anthropic `/v1/messages`·LoRA·데이터 병렬·P/D 분리·
멀티 커넥터 KV 전송(LMCache/Mooncake/NIXL)·`sleep`/`wake_up`·우선순위 스케줄링·CPU/TPU/ROCm.

**방법 메모**: `docker run --entrypoint /bin/sh <이미지> -c 'tar cf - vllm'` 로 소스를 꺼내 읽었다 — 컨테이너를 띄우지도,
GPU 를 잡지도 않는다. 상대 README 가 아니라 **양쪽 소스**를 봤고, 우리 쪽 수치는 전부 코드에서 다시 셌다.

### 45차 §55 — 내가 §54 에서 틀린 것 하나, 그리고 진짜였던 것 하나: `POST /v1/prefix/reset` (2026-09-12, srv4, GPU 없음)

운영자가 §54 의 마지막 둘을 짚고 "진행". 하나는 **내가 틀렸고**, 하나는 진짜였다.

**틀린 것 — 접두사 캐시 입자.** §54 에 *"우리 공유 단위는 프리필 청크 6,912 토큰이라 그 미만 공유 접두사를 전부 놓친다"* 고 적었다.
**아니다.** `base/prefix.chain()` 은 `self.block_size` 로 돌고, 그 단위는 **풀의 블록 768 토큰**이다.
청크 안쪽 경계도 전부 잡는다 — `runner._marks` 가 스텝 전에 `{위치: 스냅샷}` 을 지명하고 모델이 지나가며 찍는다.
증명은 한 줄이다:

```
PrefixCache(768, 6912, 96).chain(list(range(5000)))  ->  [768, 1536, 2304, 3072, 3840, 4608]
```

그러니 **입자는 vLLM(GLM5Next 기본 블록 1,152)보다 우리가 곱다.**

**왜 틀렸나.** `PrefixCache` 의 모듈 독스트링 첫 문단이 *"The unit is a chunk -- the prefill chunk ..."* 였다.
그건 **45차 §23 이전** 설계고, §23 이 블록으로 바꾸면서 코드만 바뀌고 그 문단은 안 바뀌었다.
나는 독스트링을 읽고 `chain()` 을 안 열었다 — [[feedback-comparison-verify-our-side]] 가 *"상대 README 만 읽지 말고
우리 함수를 직접 열라"* 고 적어 둔 바로 그 실패를 **우리 문서에 대해** 한 것이다. 독스트링을 고쳤다.

**남은 진짜 미지수**는 입자가 아니라 **스냅샷 96개**다. 96 블록(73,728 토큰)보다 긴 프롬프트는 진행하면서
자기 앞선 경계를 스스로 축출하고, 그 상태 복사는 버린 일이 된다. 코드가 이름을 붙여 뒀고(`runner._note_fade`)
계량기도 이미 나간다(`st:prefix_snapshot_self_evicts_total`) — **프로덕션에서 읽기만 하면 된다.**

**진짜였던 것 — 캐시를 통째로 버리는 문이 없었다.** `POST /v1/prefix/reset` 을 넣었다(vLLM 의 `/reset_prefix_cache`).
필요한 경우는 **엔진이 볼 수 없는 것**이다: 프롬프트의 **의미**가 안 바뀐 접두사 아래에서 바뀔 때 —
도구 목록, 검색해 온 문서, 손본 시스템 템플릿 — 토큰 id 는 똑같아서 모든 해시가 여전히 맞는다. 말해 주는 수밖에 없다.

- `PrefixCache.reset()` — entries·faded 를 전부 잊고, 블록을 익명으로 돌리고, 스냅샷을 자유 목록에 넣고,
  **티어 슬롯 키를 돌려준다**(캐시는 어떤 슬롯이 있었는지 알고, 디스크는 러너 것이다).
- `Runner.reset_prefix()` — 그 키로 티어 블롭을 지운다. **티어를 안 지우면 리셋이 아니다**: 다음 프롬프트가 복원해 온다.
- **쓰는 중인 경계는 안 가져간다.** `spilling` 인 경계의 블록과 스냅샷은 **전송 중인 바이트**라, 가져가면 티어가
  남의 KV 를 가진 슬롯을 받는다. 숨기지 않고 `kept_spilling` 으로 **센다**.
- 컨트롤 큐를 타므로 **네 랭크가 같은 스텝에서 같은 경계를 잊는다**(캐시가 갈리면 합의가 깨진다).
- 이미 붙은 행은 그대로다 — 넘겨준 블록은 그 행의 KV 고, **이름을 잊는다고 블록이 돌아오지는 않는다.**
  리셋이 사는 것은 *"새로 오는 프롬프트가 낡은 접두사를 입양하지 않는다"* 이다.
- `st:prefix_resets_total` 로 센다.

**검증**: 엔진 스위트 전체 초록. 새 테스트 7개 — 블록·스냅샷이 돌아온다 / 리셋 뒤 같은 프롬프트가 미스 /
빈 캐시에 무해 / **쓰는 중인 경계는 남고 세어진다** / 티어 키가 돌아온다 / 문에서 루프 스레드로 적용되고 계량기가 센다 /
접두사 캐시 없는 서버는 404.

### 45차 §56 — 데네브·웜홀과의 연계: 엔진이 자기가 뭘 할 수 있는지 말한 적이 없었다, 그리고 웜홀은 눈을 가리고 있었다 (2026-09-12, srv4/프로덕션 헤드, 장비 판정 2건)

운영자 "st엔진과 데네브, 웜홀과의 연계성 강화". 양쪽 소스를 다 읽었다 — `/home/choiceoh/deneb/gateway-go/cmd/wormhole/` (6,941줄) 과 우리 `base/serve.py`.

**연결 구조** (읽어서 확인):

```
ST 엔진 :8000 --/v1/models--> SparkFleet :18900 --/api/services--> 웜홀 :18800 --> 데네브 게이트웨이 :18789
```

웜홀은 `fleet.go`에서 SparkFleet 인벤토리를 읽어 **`/v1/models` 가 모델 id 를 내는 백엔드만** 라우팅 대상으로 삼는다.
즉 **`/v1/models` 가 계약면이다.**

**이미 잘 맞물려 있던 것 둘** (없는 구멍을 만들지 않으려고 먼저 확인했다):
- **사고 라우팅.** 웜홀 `glmVLLMReasoningRoute` 가 `reasoning_effort` 를 **본문과 `chat_template_kwargs` 양쪽에** 넣고
  (`setGLMVLLMEffort`), 우리 문은 그 둘이 **일치하는지 검사한 뒤** 템플릿에 넘긴다(`serve.py:2390`). 맞물린다.
- **핸드오버.** 넘겨주는 중 우리는 새 요청을 **503** 으로 거절하고, 웜홀은 5xx 를 **바이트가 흐르기 전이면 페일오버**
  시킨다(`router.forward`). 이미 클라우드로 우회한다.

**진짜 문제 둘.**

**1. 엔진이 자기 능력을 말한 적이 없다.** `/v1/models` 가 `{id, object, owned_by}` 세 줄이었다.
그래서 아래 전부가 **손으로 적은 설정**이고, 손으로 적은 능력은 어긋난다. 실제로 어긋나 있었다 → 2번.

`Server.model_card()` 로 바꿨다. `max_model_len` 은 **vLLM 의 필드 이름 그대로** 써서, vLLM `/v1/models` 를 이미 읽는
쪽은 공짜로 맥락 상한을 얻는다. `capabilities` 는 **이 부팅이 실제로 바인딩한 것에서 읽는다** —
비전 타워가 있는지, 프로필이 도구 파서를 줬는지, 문법이 컴파일됐는지, 드래프터의 k 가 몇인지 — 상수가 아니라서
**이 프로세스가 못 하는 걸 할 수 있다고 말할 수 없다**.

**2. 웜홀이 우리 눈을 가리고 있었다.** `~/.wormhole/config.json` 의 `glm-5.3-flash-local` / `-low` 둘 다 **`vision: false`** 다.
`vision.go` 의 게이트는 그런 항목으로 가는 요청에서 **이미지 파트를 떼어낸다**(Z.AI 텍스트 모델이 이미지에 400 을 내고
그 400 이 이후 모든 턴을 오염시키던 사건에서 나온 방어다). 그런데 **ST 는 이미지를 서빙한다** —
`boot.fleet` 은 비전 타워가 없으면 아예 부팅을 거부하고(PR #431 프로덕션 패리티), 문에는 `vision.Door` 가 붙어 있다.

**장비 판정 2건** (프로덕션 헤드 `100.125.220.117:8000`, 합성 요청 둘):

| | 결과 |
|---|---|
| 112×112 빨간 PNG + "색을 한 단어로" | **`빨강`**, `finish=stop`, prompt 49 tok, **0.7 s** (`reasoning_effort: low` 동봉) |
| 같은 요청, 기본 사고 on, `max_tokens=24` | 24 토큰이 전부 추론 채널로 — 비전 입력은 정상, 예산만 모자랐다 |

즉 **엔진은 보는데 라우터가 이미지를 떼고 보낸다.** 설정 한 키의 문제고, 프로덕션 라우터의 라이브 설정이라
**증거만 내고 적용은 운영자 확인 뒤로 남긴다**.

**3. 핸드오버가 카탈로그에 안 보였다.** `alive` 는 드레인 내내 참이다 — 그게 설계다(있던 행은 끝내고 파킹).
하지만 **새 요청은 이미 503** 이라, 그 사이 `/v1/models` 는 멀쩡히 모델을 광고하고 있었다.

**어느 엔드포인트가 실제로 물어지는지 확인했다**: 웜홀 `router_discovery.probeMaxModelLen` 이 **60초마다
`/v1/models`** 를 GET 하고, SparkFleet 은 **`/v1/models` 가 내는 모델 id** 로 서비스의 `Model` 을 채우며,
웜홀 `fleet.go` 는 `if !sv.OK || sv.Model == ""` 로 건너뛴다. **그 사슬에서 `/health` 를 읽는 곳은 없다**
(grep 확인; 웜홀은 자기 `/health` 만 낸다, 슈퍼바이저는 실제 생성으로 검사한다).

그래서 드레인은 **카탈로그에 보여야 보인다**. `catalog()` 가 드레인 중 **빈 목록 + 503** 을 낸다 —
코드를 보는 프로버와 모델 id 를 보는 프로버가 **같은 결론**에 이르고, 호출자가 이미 거절 중인 엔진으로
실패한 홉을 내기 전에 경로가 빠진다. `readiness()`(`/health`)도 `draining` 을 말하지만 그건 **경로를 바꾸는
변경이 아니라 정직함**이다 — 운영자가 `curl` 로 먼저 묻는 곳이고, 이름 없는 상태는 아무도 안 보기 때문이다.

**정정**: 이 절의 첫 판에 *"`/health` 가 ok 라서 프로버가 인벤토리에 남겨 둔다"* 고 적었는데, **그 사슬은 `/health` 를
안 읽는다**. 라우팅을 실제로 바꾸는 것은 `/v1/models` 쪽이고, 위 문단이 그렇게 고친 것이다.

**주의**: 프로덕션은 릴리스 `5734b29fde84` 로 떠 있으므로 위 둘은 **다음 배포부터** 적용된다.

**4. 슈퍼바이저가 핸드오버를 죽음으로 읽었다.** `st-glm53-supervisor.sh` 는 30초마다 `door_up`(`/v1/models` 에
모델 이름이 있나) + `chat_ok`(진짜 생성)를 보고, **3연속 실패면 포렌식을 뜨고 재기동**한다. 그런데 드레인 중에는
새 요청이 이미 503 이라 `chat_ok` 가 실패한다 — **카탈로그 변경 이전에도** 그랬다. 90초보다 긴 핸드오버는
재기동을 부르고, 그 재기동은 **다음 점유자와 경합한다**. `fleet_taken` 이 막아 주지 않느냐 — **리스를 놓은 뒤
다음 점유자가 잡기 전 창**에서는 락이 비어 있어서 못 막는다.

`handing_over()` 를 더했다: `-f` 없이 `/v1/models` 를 읽어 `"status": "draining"` 이면 **실패로 세지 않고
기다린다**(`fleet_taken` 과 같은 자리, 한 번 돌기 모드에도). 진짜 장애는 그대로 재기동한다 — 드레인이 아니면
이 가드는 아무것도 삼키지 않고, 테스트가 그 둘을 따로 박았다.

**웜홀이 이미 묻고 있던 필드**: `probeMaxModelLen` 은 `data[].max_model_len` 을 찾는다. 우리가 안 냈으므로
지금까지 **0** 이었고, 웜홀이 다운스트림(데네브 게이트웨이·네이티브 피커)에 내는 `/v1/models` 행에서
`omitempty` 로 **빠져 있었다**. 즉 이 변경은 추측이 아니라 **이미 비어 있던 칸을 채우는 것**이다.

**검증**: `tools/check.py` 60 files, **763 tests, 0 failed**. 새 테스트 9개 — 카드가 이 부팅이 바인딩한 것을
말한다 / 안 바인딩한 능력은 절대 주장 안 한다 / **드레인 중 카탈로그가 비고 503** / `/v1/models` 가 그 판정을
싣는다 / `readiness()` 세 상태 / `/health` 가 draining 을 싣는다 / GET 헬퍼 /
**슈퍼바이저가 핸드오버를 기다린다**(가짜 플릿에 진짜 셸 제어 흐름) / **진짜 장애는 여전히 재기동으로 읽는다**.

### 45차 §57 — 웜홀이 우리 눈을 가리던 키를 껐다, 그리고 `max_model_len` 이 비어 있는 걸 실물에서 봤다 (2026-09-12, srv4, 라이브 설정 변경 1건)

§56 에서 증거만 내고 멈춰 둔 것을 집행했다. 운영자 "대기중인것 개선".

**무엇이었나.** `~/.wormhole/config.json` 의 `glm-5.3-flash-local` / `-low` 둘 다 **`"vision": false`** 였다.
웜홀 `entryAcceptsImages` 는 **설정의 명시 오버라이드가 내장 표를 이긴다**, 그리고 `vision.go` 의 게이트는
그런 항목으로 가는 요청에서 **이미지 파트를 떼어낸다**. 우리 ST 는 이미지를 서빙하는데(§56 에서 프로덕션 헤드에
112×112 빨간 PNG → **`빨강`**, 0.7 s 로 확인) 라우터가 눈을 가리고 있었다.

**왜 껐나 — 내장 표가 이미 옳다.** `vision.go` 의 `textOnlyImageModels` 는 `glm-5.3-flash` 를 **고의로 뺐다**:

> *"unlike glm-4.7-flash (text-only) and its own base model glm-5.3, the 5.3 flash tier is multimodal.
> Measured on the coding plan endpoint 2026-09-02: a 1x1 PNG content-part answered "Red" on glm-5.3-flash,
> while glm-5.3 / glm-5.2 / glm-5.1 / glm-4.7 all returned 400."*

즉 **클라우드 폴백(`glm-5.3-flash`)도 이미지를 받는다.** 로컬 항목의 `false` 하나만이 유일한 눈가리개였고,
그게 언제 붙었는지는 백엔드가 비전 없는 vLLM 이던 시절의 잔재로 보인다.

**집행**: 타임스탬프 백업(`config.json.bak-vision-*`) → 두 줄만 `true` 로 → 쓰기 전에 JSON 파싱 확인 →
웜홀이 mtime 감시로 **`config reloaded models=8`**(16:57:06) 를 찍었다. 다른 항목은 전부 오버라이드 없음(내장 표를 따름).

**덤으로 실물 확인 하나**: 웜홀의 다운스트림 `/v1/models` 를 찍어 보니

```
glm-5.3-flash-local        max_model_len=-
glm-5.3-flash-local-low    max_model_len=-
```

**§56 이 말한 빈칸이 실제로 비어 있다.** 지금 도는 릴리스가 `model_card()` 이전이라 그렇고, 다음 배포부터 채워진다.

**아직 못 한 것**: 웜홀을 통과하는 이미지 요청의 종단 확인. 옆 세션이 srv2 에서 `st-glm53` 을 계속 올렸다 내렸다 해서
(16:57:16 요청은 웜홀이 **라우팅은 했고**(`reasoning routed off` 로그) 업스트림이 없어서 실패했다) 안정된 헤드가
생길 때까지 미룬다. **게이트를 통과했다는 것 자체는 그 로그가 증명한다** — 예전이라면 이미지가 그 앞에서 떨어졌다.

**되돌리기**: `cp ~/.wormhole/config.json.bak-vision-<타임스탬프> ~/.wormhole/config.json` 하나, 3초 안에 자동 반영.

### 45차 §58 — 데네브까지의 사슬을 끝까지 따라가 보니, 빠진 고리는 우리 한 칸뿐이었다 (2026-09-12, srv4, 종단 판정 1건 + 라이브 설정 1건)

운영자 "데네브와의 연계성 개선". 웜홀 위 한 층을 더 읽었다 — `deneb/gateway-go/internal/ai/modelrole/` 와 `modelcaps/`.

**사슬 전체를 소스로 확인했다.**

```
ST /v1/models  →  웜홀 windows 캐시(60초)  →  웜홀 /v1/models  →  데네브 vllmWindows  →  CapabilityForModel
```

- 데네브 `harvestVllmWindows` 는 **`ServesVllmBacked(id)` 인 모든 프로바이더**를 훑는다 — 거기 **웜홀이 포함된다**.
- 파싱 모양이 세 층에서 **완전히 같다**: `data[].id` + `data[].max_model_len` (웜홀 `probeMaxModelLen`, 데네브 `DiscoverServedVllmModelInfos`, 그리고 우리 `model_card()`).
- 데네브 `CapabilityForModel` 주석이 왜 그게 중요한지 직접 적어 뒀다:

> *"the served model's advertised max_model_len is ground truth, so it wins over the deneb.json static value, which is demoted to a FALLBACK … removing the stale-config footgun where a hand-maintained contextWindow drifts from the server (**too high → prompts that exceed max_model_len 400 the turn; too low → the model's headroom is wasted and compaction fires early**)."*

**즉 데네브는 이 값을 "ground truth" 로 대접할 준비가 되어 있었고, 세 층에서 빠진 칸은 우리 한 칸이었다.**
실물로 확인했다 — 지금 도는 릴리스의 응답과, 그 결과 웜홀이 다운스트림에 내는 행:

```
ST      : {"object":"list","data":[{"id":"glm-5.3-flash","object":"model","owned_by":"st"}]}
웜홀 →  : glm-5.3-flash-local      max_model_len=(absent)
          glm-5.3-flash-local-low  max_model_len=(absent)
```

§56 의 `model_card()` 가 배포되면 이 두 줄이 저절로 채워지고, 데네브의 손으로 적은 값은 폴백으로 강등된다.
**엔진 쪽에 더 할 일은 없다** — 사슬이 이미 우리를 기다리고 있었다.

**종단 판정 1건 — §57 이 미뤄 둔 것.** 헤드가 안정된 틈에 웜홀을 통과하는 이미지를 찍었다:

| | |
|---|---|
| 요청 | `glm-5.3-flash-local-low` + 112×112 파란 PNG, "색을 한 단어로" |
| 답 | **`파랑`**, `model='glm-5.3-flash'`, 20.8 s(부팅 직후 첫 이미지; 웜 상태 직접 호출은 0.7 s) |
| 증거 | **ST 의 `served` 15 → 16** — 클라우드 폴백이 아니라 **우리 엔진**에 닿았다 |

§57 의 `vision: true` 가 실제로 동작한다. 예전이라면 이미지가 게이트에서 떨어져 텍스트만 갔다.

**라이브 설정 1건.** `~/.deneb/deneb.json` 의 `wormhole/glm-5.3-flash-local` / `-low` 가 `contextWindow: 1000000` 이었다.
ST 의 실제 상한은 **1,048,576**(프로덕션 `context_ceiling`). 데네브 자신의 말로 *"too low → 모델의 여유가 낭비되고 압축이 일찍 걸린다"* 인 4.6% 다.
백업 뜨고 **두 줄만** 1048576 로 고쳤다 — 클라우드 항목(`glm-5.3-flash` 등)은 Z.AI 것이라 1000000 그대로 뒀다.
데네브 게이트웨이는 설정을 기동 시 읽으므로 **다음 재기동부터** 적용된다(discovery 가 살아나면 어차피 이 값은 폴백이다).

**되돌리기**: `cp ~/.deneb/deneb.json.bak-ctxwindow-<타임스탬프> ~/.deneb/deneb.json`.

**확인만 하고 안 건드린 것**: 데네브의 `vision`/`reasoning`/`promptCache` 오버라이드는 **discovery 대상이 아니다**
(주석이 명시). 로컬 항목엔 `vision` 키가 아예 없어 관대한 기본값이라 이미지가 통과한다 — 눈가리개는 웜홀 쪽 한 곳뿐이었고 §57 에서 껐다.

### 45차 §59 — `medium` 은 표준인데 우리 문이 400 을 냈다, 그리고 템플릿 kwargs 는 검사를 통째로 건너뛰고 있었다 (2026-09-12, srv4, 라이브 판정 2건)

운영자 "데네브 연계 추가개선". 데네브가 우리 엔진에 **어떤 값을 보내는지**를 코드로 따라갔다.

**데네브의 일곱 역할 중 다섯이 우리 엔진에 있다**(`~/.deneb/deneb.json`):

| 역할 | 모델 |
|---|---|
| `codingModel` · `lightweightModel` · `submainModel` · **`visionModel`** | `wormhole/glm-5.3-flash-local` |
| `tinyModel` | `wormhole/glm-5.3-flash-local-low` |
| `defaultModel` | `kimi/k3` |
| `fallbackModel` | `wormhole/glm-5.3-flash` (클라우드) |

**`visionModel` 이 로컬을 가리킨다.** 즉 §57 에서 끈 그 `vision: false` 는 추상적인 능력 불일치가 아니라
**데네브의 전용 비전 역할이 눈을 가린 채 돌고 있었다**는 뜻이다. §58 의 `파랑` 종단 판정이 정확히 그 경로다.

**찾은 것 — `reasoning_effort: "medium"`.** 데네브 `llm/openai.go` 는 사고 예산을 이렇게 옮긴다:
≤4,096 → `low`, ≤10,240 → **`medium`**, 그 위 → `high`. 그런데 우리 문은:

```
direct ST, reasoning_effort=medium -> 400 {"error": "reasoning_effort must be low, high, or max"}
```

(프로덕션 헤드 실측.) **`medium` 은 이 문이 말한다고 주장하는 API 의 표준 값**이다.

**왜 그렇게 막혀 있었나 — 템플릿 때문이다.** GLM-5.3 의 챗 템플릿은 이렇게 쓴다:

```jinja
{%- set effective_reasoning_effort = reasoning_effort if reasoning_effort is defined
    and reasoning_effort in ['low', 'high'] else 'max' -%}
```

**`low`/`high` 가 아니면 전부 `max`** — 즉 `medium` 을 그냥 통과시키면 *"high 보다 낮게"* 라는 뜻이
**가장 깊은 설정**으로 조용히 뒤집힌다. 거절은 그걸 막았지만 표준 값을 못 받는 대가를 치렀다.

**고친 것**: `EFFORT_RUNGS = {"low": "low", "medium": "high", "high": "high", "max": "max"}`.
순서가 살아 있고(low ≤ medium ≤ high ≤ max) **사고로 `max` 를 사는 일이 없다**. 진짜 상한이 필요한 호출자에겐
`reasoning_budget` 이 있다 — 등급 이름이 아니라 토큰을 센다(45차 §46).

**같이 막은 구멍 하나 더 (이쪽이 더 고약하다)**: `chat_template_kwargs` 로만 보낸 `reasoning_effort` 는
**검사를 통째로 건너뛰고** 템플릿까지 그대로 갔다. `"medium"` 이든 `"enormous"` 든 조용히 `max` 가 됐다.
이제 최상위와 같은 사다리를 타고, 모르는 값은 400 이다.

**정정 — 처음 읽은 게 틀렸다.** `X-Wormhole-No-Effort: 1`(데네브가 붙인다)을 보고 *"그럼 medium 이 그대로
ST 로 가서 데네브 역할 넷이 400 난다"* 고 생각했다. **아니다.** 웜홀 `shapeFor` 는 `applyThinking`(No-Effort 로
억제됨)**과 `applyReasoning`(억제 안 됨)** 을 **둘 다** 부르고, 로컬 항목은 `reasoning: glm-vllm` 이라
후자가 언제나 `high`/`low` 로 **덮어쓴다**. 실측도 그랬다:

```
wormhole -local + No-Effort, medium -> 200
```

그러니 **오늘 데네브 경로는 안전하다.** 남은 것은 (1) 직접 호출하는 OpenAI 클라이언트 — deneb.json 에는
헤드를 직접 가리키는 `vllm` 프로바이더도 있다 — 와 (2) 템플릿 kwargs 구멍이고, 둘 다 이번에 닫았다.
**쓰기 전에 찍어 봐서 가짜 결함 하나를 안 적었다.**

**검증**: `tools/check.py` 60 files, **777 tests, 0 failed**. 새 테스트 2개 — 네 등급이 템플릿에 있는 칸에
떨어진다 / 템플릿 kwargs 만 온 값도 검사받고 모르는 값은 400.

### 45차 §60 — 양방향으로 보니 한쪽이 끊겨 있었다: 통제면이 우리 엔진을 모른다 (2026-09-12, srv4, 라이브 조사)

운영자 "데네브와 양방향 연계 개선". 지금까지는 전부 **데네브 → ST** 아니면 **ST 가 물으면 답하는** 쪽이었다.
반대 방향 — **누가 우리 상태를 알아야 하는가** — 을 따라가 보니 사슬 하나가 끊겨 있었다.

**통제면에 물어봤다** (SparkFleet `/api/services`, 토큰은 웜홀 설정의 것):

```json
{"node":"srv1","name":"paddleocr",     "url":"http://127.0.0.1:18011/health",  "ok":false}
{"node":"srv1","name":"vibevoice-asr", "url":"http://127.0.0.1:18013/health",  "ok":false}
{"node":"srv2","name":"dsv4",          "url":"http://127.0.0.1:8000/v1/models","ok":true,"httpStatus":200}
```

**셋뿐이고, 우리 엔진은 목록에 없다.** `srv2/dsv4` 가 찌르는 `127.0.0.1:8000/v1/models` 가 **바로 ST 헤드**이고
200 을 받는데 — **`model` 필드가 없다**. 웜홀 `discoverFleet` 은 `if !sv.OK || sv.Model == ""` 로 건너뛴다.

**즉 SparkFleet 이 웜홀에 주는 라우팅 가능한 모델은 0개다.** 디스커버리 경로가 통째로 죽어 있고,
모든 라우팅이 손으로 적은 설정으로만 돈다. 이름도 `dsv4` — DeepSeek V4 시절 서비스가 그대로 남아 ST 를 찌르고 있다.
(SparkFleet 본체는 다른 노드에 있고 소스가 여기 없다. 데네브 쪽 클라이언트만 `gateway-go/internal/infra/sparkfleet/` 에 있다.)

**우리 쪽에서 할 수 있는 것 — 수명주기를 문에 올린다.** 엔진은 리스에 대해 셋을 안다: **누가 들고 있는지,
넘겨 달라는 요청을 받았는지, 넘긴 결과가 어땠는지.** 셋 다 `~/st-fleet.lock` 에 쓰지만 **읽으려면 rank 0 에
ssh 해서 cat 해야 했다** — 정작 이 엔진을 지켜보는 것들(슈퍼바이저, 웜홀 프로브, SparkFleet, curl 든 운영자)은
**전부 문에 닿아 있는데**.

`GET /` 이 이제 `fleet` 블록을 싣는다:

```json
"fleet": {"owner": "st-glm53", "path": "…/st-fleet.lock",
          "draining": "another-session", "handed_over": {"to": …, "parked": 3, "lost": 0}}
```

리스가 없는 부팅(`--local`)에는 **필드 자체가 없다** — 아무 의미 없는 칸을 특별 취급할 필요가 없게.

**이걸로 닫히는 것**: §56 의 `catalog()`(드레인 중 빈 목록 + 503)는 *"지금 라우팅하지 마라"* 를 말하고,
`fleet` 블록은 *"왜, 누구에게, 얼마나 옮겼는지"* 를 말한다. 폴링하는 쪽이 전자로 즉시 비키고, 사람이 후자로 사후에 읽는다.

**안 한 것**: SparkFleet 에 등록하는 일. `/api/services` 는 GET 만 있고(다른 경로는 404), 본체는 다른 노드다 —
**서비스 이름을 `dsv4` 에서 고치고 모델 id 를 채우는 건 그쪽 설정**이고, 우리 `/v1/models` 는 이미 표준대로 답한다.

**검증**: `tools/check.py` 60 files, **779 tests, 0 failed**. 새 테스트 2개 — 리스 없으면 필드 없음·있으면 셋 다 /
상태 본문이 리스가 있을 때만 블록을 싣는다.

### 45차 §61 — 양쪽 다 우리 것이니 알 수 있는 것: 에이전트는 매 턴 대화 전체를 다시 토크나이즈시키고 있었다 (2026-09-12, srv4, 실측)

운영자 "우리가 양쪽 코드를 둘 다 갖고 있고, 데네브가 ST엔진에서 서빙하는 에이전트를 아주 많이 사용한다는 점에서
만들 수 있는 개선". 그 두 사실이 가리키는 자리를 **재서** 찾았다.

**데네브 트래픽의 모양**: 일곱 역할 중 다섯이 우리 엔진이고(§59), 에이전트는 도구 루프마다 **대화 전체를 다시 보낸다**.
접두사 캐시가 제 일을 하면 **모델은 새 꼬리만** 계산한다 — 그러면 남는 비용이 드러난다.

**실측(실제 체크포인트, 호스트)**:

| | |
|---|---:|
| 106K 토큰 대화를 통째로 토크나이즈 | **239 ms** |
| 그 턴이 실제로 더한 29 토큰 | **0.05 ms** |
| 턴마다 버려지는 시간 | **239 ms** |

토크나이저 처리량은 대략 **0.45 M tok/s** 로 선형이다(13K→26 ms, 52K→130 ms, 106K→241 ms, 212K→476 ms).
프리필이 큰 턴에서는 이게 숨지만, **접두사가 캐시된 이어지는 턴에서는 이것이 바닥**이 된다.

**고친 것 — `PromptTokens`**: 이어지는 턴은 **꼬리만** 토크나이즈하고 기억해 둔 앞부분에 잇는다.

토큰 스트림을 잇는 건 **머지가 자를 수 없는 자리**에서만 옳다. `tokenizers` 는 **added token** 을 BPE 모델보다
먼저 매칭하므로 added token 이 정확히 그런 자리이고, **렌더된 챗 프롬프트는 구조상 거기서 끝난다**(생성 프롬프트).
그래서 규칙을 템플릿에서 추측하지 않고 **토크나이저의 added-token 집합에서 읽는다**. 양쪽으로 확인했다:

```
<|assistant|> 에서 자른 splice  : 전체 패스와 동일 (45 == 22 + 23)
단어 중간에서 자른 splice       : 다르다 (47 != 45)
```

**실제 어휘로 측정**:

```
대화 52,012 토큰
  첫 턴   61.1 ms  (전체)
  다음 턴  0.76 ms  (splice, +22 토큰)   전체 패스와 결과 동일? True
```

**80배.** 106K 대화라면 239 ms → 1.5 ms 대.

계량기 셋을 낸다 — `st:prompt_tokenize_spliced_total` / `_full_total` / `_chars_saved_total`.
캐시는 **항목 수와 글자 수 양쪽으로 묶는다**(기본 8개 / 8 MiB): 모든 프롬프트를 기억하는 문은 캐시 옷을 입은 누수다.
그리고 캐시는 **토크나이저에 속한다** — `tok` 이 바뀌면 다시 만든다(부팅이 늦게 바인딩하거나 테스트가 바꿔 끼운다).

**왜 이게 "양쪽 다 우리 것" 인가**: 일반 클라이언트에게는 이걸 요구할 수 없다(ids 를 보내라고 하려면 템플릿을
클라이언트가 렌더해야 한다). 반대로 **엔진이 혼자 할 수 있다는 것**을 알려면 대화가 접두사로 자란다는 트래픽의
성질과 템플릿이 특수 토큰으로 턴을 끊는다는 사실을 **둘 다** 알아야 했다. 그래서 데네브는 **한 줄도 안 바뀐다.**

**검증**: `tools/check.py` 60 files, **783 tests, 0 failed**. 새 테스트 4개 — 꼬리만 토크나이즈된다 /
**자를 수 없는 자리면 통째로 한다** / 항목·글자 양쪽으로 묶인다 / 계량기가 스크레이프에 닿는다.

### 45차 §62 — 파킹된 대화 280개가 호스트 램 1.04 GiB 를 잡고 있었다, 스캔이 물어보는 건 숫자 셋인데 (2026-09-12, srv4, 실측)

§61 이 이어지는 턴의 호스트 비용을 239 ms 에서 1 ms 대로 내렸으니, **그다음으로 비싼 것**을 찾아 문의 경로를 마저 쟀다.
싼 것부터 확인해 후보를 지웠다 — JSON 파싱 0.2 ms(136 KiB), **Jinja 템플릿 렌더 5.6 ms**(1.4 MiB 렌더),
NFC 0.55 ms(281 KiB), 접두사 체인 1.1 ms(26K), 도구 문법 EBNF 빌드 0.13 ms(도구 60개).
**전부 밀리초짜리다.** 그런데 하나가 밀리초가 아니었다 — 시간이 아니라 **메모리**였다.

**`_continuation` 은 매 요청마다 파킹된 대화를 전부 훑는다.** 후보마다 `runner.parked_record(key)` 를 부르고,
그건 처음 읽을 때 **레코드 전체를 캐시한다** — 그리고 레코드는 그 대화의 **토큰 리스트 전부**를 들고 있다.

**실측**: 100K 토큰 대화 하나의 레코드가 **3.8 MiB**(파이썬 int 리스트; 같은 것이 디스크 JSON 으로는 0.7 MiB).
이 플릿이 실제로 들고 있는 **파킹 280개** 면 **1.04 GiB** 다. 그것도 **earlyoom 이 절대 6 GiB 에서 엔진을
1순위로 쏘는 상자**에서, 요청 몇 번이면 전부 상주하게 된다(스캔이 게으르게 채운다).

**그런데 후보마다 필요한 건 숫자 셋뿐이다** — 길이, 마지막 토큰, 그 앞 토큰. 싼 검사가 그 셋으로 거의 전부를 거른다.

**고친 것**:
- `Runner.parked_digest(key)` — 길이·마지막 둘·그림 표시. **모든 파킹에 대해 들고 있어도 몇 바이트**다.
- `Runner.parked_record(key)` — 이제 **LRU 8개**(`PARKED_RECORDS_KEPT`). 이 엔진이 들 수 있는 행 수 + 여유.
  통째 레코드는 **싼 검사를 통과한 후보**나 **실제 재개** 때만 읽힌다. 둘 다 드물다.
- `_continuation` 은 다이제스트로 거르고, 통과한 하나만 레코드를 읽는다. **거르는 조건은 이전과 글자 그대로 같다.**
- `forget_parked` 가 다이제스트도 같이 지운다 — 대화보다 오래 사는 다이제스트는 없는 키를 계속 후보로 만든다.

**1.04 GiB → 280 × 수십 바이트.**

**방법 메모**: §61 과 같은 방식으로, **비싼 걸 찾은 게 아니라 싼 걸 지워서** 남은 하나를 찾았다.
그리고 그 하나는 시간이 아니라 메모리였다 — 프로파일러가 아니라 `tracemalloc` 이 답을 줬다.

**검증**: `tools/check.py` 60 files, **791 tests, 0 failed**. 새 테스트 4개 —
다이제스트가 거르고 통과한 하나만 읽힌다 / 통째 레코드는 몇 개만 남는다 / 다이제스트는 한 번만 만든다 /
대화를 잊으면 다이제스트도 간다.

### 45차 §63 — 남은 셋을 코드에 대조했더니 둘은 이미 있고 꺼져 있었다, 그리고 그 이유를 GPU 없이 지웠다 (2026-09-12, srv4, 컴파일만, 다운타임 0)

지시받은 셋: **persistent transformer 커널 · mHC/KDA/MoE/state 경계 · row-parallel 이 요구하는 TP4 collective.**
코드에 대조한 결과는 "아직 안 했다" 가 아니었다.

**① TP4 collective 는 의미상 최소다.** `net.py` 는 레이어마다 정확히 둘을 낸다 — `_dsa/_kda` 의 `all_reduce(o_proj)`,
`_moe/_dense` 의 `all_reduce(down)`. row-parallel 이 요구하는 그 둘이다. 남는 대안은 sequence-parallel 인데
`prefill_transport` 로 **이미 있고**, 디코드에는 안 걸린다(게이트가 `N >= 128`, 디코드 N 은 6).

**② 리덕션↔mHC PDL 은 디코드에선 이미 켜져 있고, 내가 본 플래그는 프리필 것이었다.** 생산자 쪽 `kernels/oneshot/dsv4_oneshot_ar.cu`(949줄, RDMA,
SM121)는 **이미** `cudaLaunchAttributeProgrammaticStreamSerialization` 으로 띄운다. 소비자 쪽
`kernels/mhc/tilelang_kernels.py` 에는 `T.pdl_sync()`/`T.pdl_trigger()` 가 여덟 커널에 박혀 있는데
`ENABLE_PDL = False`, 사유 *"Pinned GB10 image policy; SM12x lowering is unvalidated."*

**그 사유는 컴파일러의 질문이지 GPU 의 질문이 아니다.** `probes/mhc_pdl_lowering.py` 로 이미지 안에서
`get_tir` → `tilelang.compile(target={"kind":"cuda","arch":"sm_121a"})` 를 돌렸다:

| ENABLE_PDL | 내려간 커널 | PDL 인트린식 | `__restrict__` |
|---|---:|---|---|
| False | 6 / 8 | 없음 | **있음** |
| True | 6 / 8 | **여섯 전부 두 개 다** | **없음** |

- 내려간 여섯 전부 `cudaGridDependencySynchronize()` + `cudaTriggerProgrammaticLaunchCompletion()` 을 낸다.
- **안 내려가는 둘은 PDL 을 꺼도 똑같이 실패한다** — PDL 탓이 아니다. **그리고 그 실패는 커널에 대해 아무 말도 안 한다**:
  `served` 레인은 그중 하나(`mhc_pre_big_fuse_with_norm_tilelang`)를 **매 레이어 매 스텝** 부르고, 인자도 여기서 쓴 것과
  같으며(`n_splits=1`, `norm_w` 전달, `h_blk` 기본 1024), **프로덕션은 지금 서빙 중이다.** 디바이스가 없으면
  `determine_target` 에 물을 수 없고, 손으로 쓴 타깃 딕셔너리는 같은 타깃이 아니다(레지스터 속성을 채워 넣어도 같았다).
  **그 두 줄은 "이 하니스가 못 물었다" 이지 결과가 아니다.**
- tilelang 자체 launch 래퍼가 `cudaLaunchAttributeProgrammaticStreamSerialization` 을 건다 —
  **사슬이 종이 위에서는 완결이다.**

**대신 값이 붙는다: 켜면 여섯 전부에서 포인터 인자의 `__restrict__` 가 사라진다.** no-alias 보장을
런치 겹침과 맞바꾸는 것이고, **어느 쪽이 이기는지는 논증이 아니라 숫자다. 그래서 안 켰다.**

**③ persistent transformer 커널**은 34차에 일몰된 메가커널 캠페인의 다음 층이다 — 되살리는 것은
새 캠페인이지 플래그가 아니다.

**정정 — 여기까지 쓰고 나서 경로를 잘못 짚은 걸 찾았다.** `net._hc_post_pre` 는 **토큰 64개 이하인 스텝을
전부 `kernels/dense/mhc.MHC` 로 보낸다**(디코드는 6). 그쪽 런치는 `kernels/dense/kernels.cu` 의 `mk_launch` 이고,
`mk_pdl_enabled()` 는 **`"1"` 로 박혀 있다** — D11 노브 일몰이 `getenv` 를 리터럴로 바꾼 결과다.
**즉 디코드의 리덕션↔mHC 겹침은 이미 무장돼 있다.** 위에서 검증한 `ENABLE_PDL` 은 **프리필(>64 토큰) 레인**의 것이다.
그 파일의 주석은 그동안 `"default off"` 라고 적혀 있었다 — 컴파일되는 것과 반대였다. 둘 다 고쳤다.

**창에 남긴 질문**(이제 둘): (a) 프리필 레인 `ENABLE_PDL=True` 의 스텝 시간 대 `__restrict__` 손실,
(b) `spec_k` 5→8 (§60). 둘 다 같은 부팅에서 잰다.


### 45차 §64 — `__restrict__` 손실의 값을 재 보니 0 이었다 (2026-09-12, srv4, 컴파일만, 다운타임 0)

§63 이 `ENABLE_PDL` 을 켜는 값으로 *"포인터 인자의 `__restrict__` 가 사라진다"* 를 적고 **"어느 쪽이 이기는지는
숫자다"** 로 닫았다. 그 숫자의 **절반은 GPU 없이 나온다** — no-alias 보장을 잃으면 컴파일러가 무엇을 못 하는지는
ptxas 가 말한다. `probes/mhc_pdl_cost.py` 가 생성 CUDA 를 양쪽으로 뽑아 `nvcc -arch=sm_121a` 로 컴파일했다:

| 커널 | 레지스터 | 스필 | smem | `ld.global` | `st.global` |
|---|---:|---:|---:|---:|---:|
| `hc_head_fuse` | 168 → 168 | 0 | = | 7 → 7 | 4 → 4 |
| `hc_prenorm_gemm_block_m` | 56 → 56 | 0 | = | 14 → 14 | 4 → 4 |
| `hc_prenorm_gemm` | 51 → 51 | 0 | = | 26 → 26 | 2 → 2 |
| `mhc_fused` | 48 → 48 | 0 | = | 11 → 11 | 6 → 6 |
| `mhc_post` | 48 → 48 | 0 | 1024 → 1024 | 3 → 3 | 4 → 4 |
| `mhc_pre_big_fuse` | 78 → 78 | 0 | = | 62 → 62 | 8 → 8 |

**여섯 전부 완전히 동일하다.** no-alias 보장은 여기서 아무것도 사고 있지 않았다 — tilelang 생성 코드가 이미
명시적 TMA 디스크립터와 스레드별 인덱스로 주소를 만들어서, 컴파일러가 굳이 말해 주지 않아도 구분한다.

**남길 단서**: 레지스터·스필·smem 은 **ptxas** 출력이라 할당은 정말 같다. `ld/st` 수는 **PTX** 라서 ptxas 아래의
스케줄링 차이는 여기 안 잡힌다. 레지스터가 같고 스필이 0 이면 좁은 통로지만 **0 은 아니고**, 이미지에 `nvdisasm` 이
없어 지금은 못 닫는다.

**그래서 (a) 가 반으로 줄었다**: 비용 쪽은 비었고, **"프리필 레인에서 겹침이 값을 하느냐" 하나만 남았다.**
그건 디바이스의 질문이다.

### 45차 §65 — 프로덕션이 스스로 말했다: 드래프트 폭 5 가 걸리는 곳이다 (2026-09-12, srv4→10.10.10.2, 다운타임 0)

지시로 받은 구조적 한계 넷(혼합 forward · persistent 커널 · mHC/KDA/MoE/state/sampler 경계 · TP4 collective)은
전부 **스텝 하나를 싸게 만드는** 이야기다. 그런데 스텝을 싸게 만들기 전에 물을 것이 있다 — **스텝당 토큰이 몇 개인가.**
그건 창 없이 읽을 수 있다. `GET /metrics` 는 읽기이고, 프로덕션 ST 는 지금 서빙 중이다.

**읽은 것**(요청 19건, 프롬프트 433,842 토큰, 생성 37,745 토큰 누적):

| | |
|---|---:|
| 검증된 세그먼트 | 11,303 |
| 평균 수용 | **4.635 / 5** |
| **5개 전부 수용** | **89.9%** (10,166) |
| 0개 수용 | 3.7% (421) |
| 위치당 수용률 | 0.8855 |

**드래프트 폭이 천장이다.** 세그먼트의 **열에 아홉이 5개를 다 쓰고 끝난다** — 그 하나하나가 "더 있었으면 더 먹었다"는
뜻이다. 그리고 수용은 독립이 아니다: 위치당 0.8855 가 독립이면 5연속은 **0.544** 여야 하는데 실제는 **0.899** 다.
분포가 0 과 5 에 쌓인 **거의 이항적** 모양이고(421 / 264 / 187 / 142 / 123 / 10,166), 이는 "쉬운 구간은 전부 맞고
어려운 구간은 초반에 틀린다"는 뜻이다. **쉬운 구간에서 k 가 자른다.**

**세 상한을 갈라 보면**(`draft_ceilings`, 890 위치 표본):

| 상한 | 값 | 뜻 |
|---|---:|---|
| 측정 수용 | 0.8855 | 지금 |
| reachable `sum min(p,q)` | 0.9430 | 이 드래프트 분포로 **어떤 규칙이든** 얻을 수 있는 최대 |
| covered `p·(q>0)` | 0.9629 | 후보 16개가 **덮기라도 하는** 타깃 질량 |

후보 집합이 못 덮는 질량 **3.71%**, 후보 안에서 모양이 틀린 몫 **1.99%**, 나머지 **5.75%** 는 블록 규칙이
위치별 최적을 포기하고 블록 최적을 취한 대가다. **셋 다 k 보다 작다.**

**같이 읽힌 것: 배치는 1 이다.** `decode_steps_by_sequences`: 시퀀스 1 에서 **10,820 스텝**, 2 에서 236.
`max_seqs=4` 지만 실제 디코드의 **97.9%가 단일 행**이다. 컨텍스트는 길다 — 용량 버킷 131,072 에 5,448 스텝,
65,536 에 5,415.

**정정 둘**:
- **TRT-LLM 대조(#640) §2 가 "NVIDIA 휴리스틱이 우리 k=5 를 외부 확인했다"고 적었다. 철회한다.**
  `max_draft_len = 5 if max_batch_size <= 4 else 3` 은 일반 기본값이고, **우리 수용률은 일반이 아니다**(4.635/5).
  같은 숫자에 도달했다는 것이 같은 이유를 뜻하지 않는다.
- **희소 드래프트 분포(#681) 의 절대 수치는 배치 4 기준이었다.** 배치가 1 이므로 스텝당 절약은
  11.82 MiB 가 아니라 **2.95 MiB**(할당+0채움) + 같은 양의 복사다. **비율 3,227배는 그대로**이고 변환의 정확성도 그대로다.

**다음에 창에서 잴 것**: k 를 5 → 8 로 넓혔을 때 (a) 스텝당 커밋 토큰, (b) 드래프터 제안 비용, (c) verify 폭,
(d) 캡처 그래프 사다리(`spec_k` 가 모양에 들어간다 — `facts.SPEC_K`, 체크포인트의 MTP 블록과 묶여 있다).
**지금 바꾸지 않는다** — 이 측정이 말하는 것은 "k 가 자른다"이지 "8 이 맞다"가 아니다.
### 45차 §66 — 컴파일된 문법 캐시에 천장이 없었다; 이미지 안에서 재 보니 스키마 하나가 173 KiB (2026-09-12, srv4, 실측)

§62 에서 "재지 않고 상한만 박을지" 남겨 뒀던 것. **재고 박았다.**

**무엇이었나**: `base/grammar.Grammars._cache` 는 `json.dumps(spec)` 을 키로 컴파일된 문법을 담는데 **상한이 없었다.**
도구 목록이 다르면 다른 문법이고, `response_format` 의 `json_schema` 는 **요청마다 다를 수 있다** — 에이전트 제품이
과제마다 새 스키마를 보내므로 **개수는 우리 것이 아니라 호출자 것**이다.

**실측** (서빙 이미지 `st-engine:prod-abceb6a0` 안, CPU 만, 이 어휘 154,856):

| 무엇 | 하나당 |
|---|---:|
| 도구 문법 (20개, EBNF 3,914자) | **0.014 MiB** |
| 도구 문법 (60개, EBNF 11,754자) | **0.017 MiB** |
| **`json_schema` (필드 40개)** | **0.173 MiB** |

도구 문법은 싸다. **스키마가 10배 비싸고**, 그게 무한정 쌓이는 쪽이다. 1,000개면 173 MiB, 10,000개면 1.7 GiB —
**earlyoom 이 절대 6 GiB 에서 엔진을 1순위로 쏘는 상자**에서.

**고친 것**: `Grammars.KEPT = 256` 의 LRU. 큰 쪽 크기로 잡아도 **최대 ~44 MiB** 다.
**살아 있는 행이 쓰는 문법을 쫓아내도 안전하다** — `Matcher` 가 컴파일된 문법 자체를 들고 있으므로 이름이 사라져도
그 행은 멀쩡하고, 다음 요청이 다시 컴파일할 뿐이다.

계량기 다섯을 낸다 — `st:grammar_cache_entries` / `_limit` / `st:grammar_compiles_total` /
`st:grammar_cache_hits_total` / `st:grammar_cache_evictions_total`. 구조화 출력을 안 하는 부팅에는 줄이 아예 없다.
카운터는 **클래스 기본값**으로 둔다: 프로브와 테스트가 `__new__` 로 인스턴스를 만들어 필요한 필드만 채우는데,
계량기가 그걸 깨뜨리면 안 된다.

**측정 방법 메모**: `ru_maxrss` 는 고수위라 안 움직인다 — `/proc/self/status` 의 **VmRSS 차이**로, 하나가 아니라
**60~100개를 컴파일해서** 나눠야 숫자가 나온다. 그리고 이건 **GPU 를 안 잡는다**(컴파일은 호스트 일이라,
`docker run --entrypoint python3` 로 이미지만 빌려 쓰면 된다).

**검증**: `tools/check.py` 60 files, **795 tests, 0 failed**. 새 테스트 4개 —
천장에서 가장 오래된 것이 나간다 / 히트는 컴파일이 아니고 순서를 갱신한다 / 천장이 누군가 고른 숫자다 /
계량기 다섯이 스크레이프에 닿고 구조화 출력 없는 부팅엔 없다.

### 45차 §67 — PDL 의 이득을 실제로 쟀다: 공짜지만 프로덕션 청크에선 거의 0 (2026-09-12, srv4, GPU 공유, 프로덕션 무중단)

§64 가 `ENABLE_PDL` 의 **비용이 0** 임을 컴파일러로 닫았다. 남은 절반은 디바이스의 질문이었고, 이제 쟀다.
**플릿 창은 안 썼다** — `probes/mhc_pdl_bench.py` 가 프리필 레인의 진입점(`mhc_pre_tilelang` → `mhc_post_tilelang`)만
GB10 하나에서 1 GiB 미만으로 돌린다. 프로덕션 `st-glm53`(67 GiB)과 `nemotron-vllm` 이 같은 GPU 에서 계속 서빙했다.

**측정 방법이 결과를 바꿨다 — 두 번.**
1. 한 프로세스에서 플래그를 뒤집으면 **tilelang 커널 캐시가 두 번째 상태에 첫 번째의 커널을 준다.** 경고가 떠서 알았다.
   → **상태당 프로세스 하나.**
2. 그래도 중앙값이 6,912 에서 +21%, 512 에서 +35% 로 요동쳤다 — 같은 실행의 최솟값은 −1.8% 였다. GPU 를 넷이
   나눠 쓰는 중이라 경합이다. → **교대 3회, 최솟값으로 판정**(가장 덜 방해받은 반복).

| 토큰 | PDL 끔 (min-of-3) | PDL 켬 (min-of-3) | 차이 |
|---:|---:|---:|---:|
| 512 | 269.9 µs | 251.6 µs | **−18.4 (−6.8%)** |
| 2,304 | 1,656.6 µs | 1,612.6 µs | **−44.0 (−2.7%)** |
| 6,912 | 5,062.9 µs | 5,047.8 µs | −15.0 (**−0.3%**) |

**부호가 셋 다 같고 형상이 커질수록 이득이 준다** — 런치 겹침이 정확히 그렇게 행동한다(고정 런치 비용이 더 많은
일에 분산된다). **프로덕션 프리필 청크는 6,912 이고 거기서 −0.3%, 노이즈 바닥과 구분되지 않는다.**

**그래서 켰다** — 처음엔 "지배적 형상에서 0 이니 그대로 둔다"고 적었는데, 운영자가 *"공짜니까 어떤 때라도 도움이
될 수 있으면 켜는 게 낫지 않나"* 고 물었고, 그 말이 맞는지를 **분포로** 확인했다(교대 5회, 상태당 프로세스 분리):

| 토큰 | | PDL 끔 | PDL 켬 | |
|---:|---|---:|---:|---|
| 512 | p50 | 264.1 µs | 380.8 µs | **+44%** |
| | 라운드별 p50 | [272,264,264,258,284] | [381,255,259,**551**,**494**] | **이항적** |
| 2,304 | p90 | 1,727.4 µs | 1,658.7 µs | **−4.0%** |
| 6,912 | p90 | 6,300.9 µs | 6,002.4 µs | **−4.7%** |

**2,304·6,912 에서는 켠 쪽이 빠르고 무엇보다 꼬리가 짧다** — 끈 쪽은 6,912 에서 10 ms 이상치를 냈고 켠 쪽은 안 냈다.
**512 에서만 가끔 2배로 튄다**(끈 쪽은 한 번도 안 그런다). 그런데 **절대값으로 재면 방향이 분명하다**: 512 의 +44% 는
**+117 µs**(최악 +287)이고, 6,912 의 −4.7% 는 **청크마다 −298 µs** 다. `scheduler._prefill` 이
`min(remaining, chunk_for(...))` 라 **프롬프트 하나에 부분 꼬리 청크는 하나뿐이고 온전한 청크는 여럿**이다.

**출력은 안 바뀐다**: `probes/mhc_pdl_same.py` 가 65·512·2,304·6,912 에서 네 출력 전부를 위치 가중 체크섬까지
대조해 **차이 0**. 되돌리는 건 한 줄이다.

**켠 값은 `ENABLE_PDL = True`(45차 §66).** 플래그 주석이 위 근거 넷을 전부 들고 있다.

**답하지 못한 것**: 여기 잰 건 mHC 쌍뿐이고 **실제 레인에는 그 사이에 `all_reduce` 가 있다.** PDL 의 본래 값은
*그* 집합 통신과의 겹침이고 랭크 넷이 필요하다 = 플릿 창. 지금 숫자는 **커널 간 겹침의 하한**이다.

**부수 정정**: §65 의 제목 줄이 `### 45차 §65### 45차 §63 — …` 로 깨져 있었다(#682 를 새 main 위로 다시 쌓을 때
내 리네임 스크립트의 `.join` 오용). 여기서 고쳤다.

### 45차 §68 — 디코드 체인의 eager 가운데를 쟀다: 융합 문제가 아니라 **디스패치** 문제였다 (2026-09-12, srv4, GPU 공유, 무중단)

지시받은 넷 중 *"각각 별도 그래프로 이어 붙이는 수준에서 더 나아가"* 가 가리킨 자리가 `pipeline.launch` 의
**캡처된 그래프 셋 사이 eager 구간**이다(§63). GPU 가 생겼으니 쟀다. 형상은 프로덕션 실측값 그대로 —
`/metrics` 가 **디코드의 97.9%가 단일 행**이라고 했으므로 n=1, t=6, V=154,880, C=16.

| 단계 | 중앙값 | 최솟값 |
|---|---:|---:|
| `distribution_batch` (커널 **하나**) | **555.5 µs** | 547.0 µs |
| `block_verify_batch` (torch 연산들) | **2,424.7 µs** | 2,380.7 µs |

**검증이 샘플러의 4.4배다.** 그런데 하는 일은 훨씬 적다 — 샘플러는 [6, 154,880] 전체에서 임계값을 찾고,
검증은 그 위에서 몇 번의 reduction 과 한 번의 역CDF 를 한다. 쪼개서 잰 부분의 합은 **229 µs** 였다.
나머지 2.2 ms 는 어디 갔나.

**프로파일러가 답했다**:

```
block_verify_batch 호출당: GPU 397 µs, 호스트 3,411 µs, 디바이스 연산 ~180개
```

**8배 호스트 바운드다.** 호출당 `sum` 7 · `mul` 10 · `clamp_max` 8 · `copy_`(DtoD) 8 · `where` 6 · `clamp_min` 7 …
**n=1, K=5 라 텐서 대부분이 5원소인데 그 위에 커널을 180번 띄운다.** 각 커널은 2~4 µs 일하고 디스패치에 30~40 µs 를 쓴다.

**그러니 이건 mHC·MoE 를 블록 단위로 접는 이야기가 아니다.** 옆자리가 이미 답을 보여 줬다 —
`kernels/decode_commit.advance` 가 커밋 + 원소별 갱신 여섯을 **런치 하나**로 접었다. 같은 일을
`block_verify_batch` 에 하면 **180 → 1** 이고, 바닥은 측정된 GPU 시간 397 µs 보다 아래다(그 397 µs 자체가
180개 커널의 합이라 대부분 런치 꼬리다).

**안 잰 것**: `all_gather` 는 랭크 넷이 필요해 여기 없다. `advance` 와 `stage_boundaries` 는 별도.
`note_ceilings` 는 64스텝마다라 무시할 수 있다.

### 45차 §69 — 블록 검증을 런치 하나로 접었다: 2,451 → 459 µs, 토큰은 한 개도 안 바뀌고 (2026-09-12, srv4, GPU 공유, 무중단)

§68 이 `block_verify_batch` 를 **호출당 디바이스 연산 180개, GPU 397 µs 에 벽시계 2,425 µs** 로 재고
*"융합이 아니라 디스패치 문제"* 라고 닫았다. 접었다.

`engine/kernels/block_verify.verify_rows` — 그리드 (n,), 행마다 프로그램 하나:
후보에서 드래프트 질량 모으기 → `carried` 스캔 → 어휘 한 벌 훑어 K−1 개 행 합 → 임계값 → `accepted`/`at`/`before`
→ 잔여분 쓰기와 정규화. **밖에 남긴 것 둘, 이유가 있다**:

- **난수**는 호출자가 뽑는다. 커널 안으로 넣으면 **랭크 합의와 D12 재현이 런치 안으로 들어간다.**
- **정정 draw(`_inverse_cdf`)** 는 torch 로 남겼다. torch 의 `cumsum` 은 병렬 스캔이고 커널에서 순차로 더하면
  **마지막 비트가 달라져** 경계 근처에서 토큰이 바뀐다. 커널 두 개 아끼자고 비트 일치를 깰 이유가 없다.

| 형상 | torch 연산들 | 커널 하나 | |
|---|---:|---:|---:|
| **n=1, K=5** (프로덕션: 디코드의 97.9%) | 2,392.8 µs | **414.0 µs** | **5.8×** |
| n=4, K=5 | 3,109.3 µs | **705.6 µs** | 4.4× |

디바이스 연산 **180 → 23**, 호스트 **3,411 → 606 µs**.

**남은 414 µs 를 쪼개 보면**:

| | |
|---|---:|
| `verify_rows` (내 커널) | 228.6 µs |
| `_inverse_cdf` (cumsum 9.9 포함) | 33.0 µs |
| 나머지 glue + 디스패치 | ~150 µs |

**내 커널의 그리드는 `(n,)` 이라 n=1 이면 48개 SM 중 하나만 쓴다.** 어휘를 쪼개 병렬화하면 줄지만,
단계가 순차라(carried → thresholds → at → 잔여분) 커널이 넷으로 갈라지고 런치 비용이 이득을 먹는다.

**진짜 답은 캡처다.** 이제 가운데가 **연산 23개**뿐이라(180개였을 땐 생각할 수도 없었다) CUDA 그래프로
잡을 만하고, 그러면 **호스트 606 µs 가 통째로 사라진다**. 생성기를 낀 캡처 배관은 이미 있다
(`decode_graphs.DecodeGraphs(..., generators=(generator,))`). 그게 지시 셋째의 마지막 조각이다.

**난수 스트림은 손대지 않았다.** `rand(n,K)` 와 `rand(n)` 두 번을 `rand(n,K+1)` 한 번으로 접으면 런치가
하나 줄지만 **Philox 가 같은 수열을 주지 않는다**(실측: `torch.equal` False). 토큰은 기록된 스텝이
재생하는 것이라(D12) 35 µs 와 바꿀 것이 아니다.

**정확성**: 25회 무작위 시행에서 `accepted`·`tokens`·`count` 가 **전부 완전 일치**. 참조는
`_block_verify_by_torch` 로 분리해 **CUDA 에서도 부를 수 있게** 했다 — 처음엔 CPU 참조와 대조하다
전부 불일치로 읽혔는데, **CPU 생성기와 CUDA 생성기는 같은 시드에서 다른 난수**를 낸다. 커널 버그가 아니라
스트림이 둘이었다.

**함정 둘**(다음 사람 몫):
- Triton 전역 상수는 `X: tl.constexpr = v` 주석형이 아니라 **`X = tl.constexpr(v)` 호출형**이어야 한다.
- `TritonGPUOptimizeThreadLocality` 가 **루프 결과의 사용처가 둘 이상이면 어서션**에 걸린다
  (`loopResult.hasOneUse()`). 누산기를 `x + 0.0` 으로 한 번 받아 넘기면 통과한다.


### 45차 §70 — 두 시점을 하나로 보고 있었다: 런 안에서는 D16 이 켜지고, 런 사이에서는 접두사 캐시가 이미 최대다 (2026-09-12, srv4, 재현)

운영자 "데네브랑 결합할게 고작 그런것 뿐이야?" → **두 설계가 서로를 모르고 만들어진 자리**를 찾으러 갔고,
찾았다고 생각했다가 **내가 틀렸다는 걸 재서 알았다**. 둘 다 적는다.

**데네브는 이미 우리 접두사 캐시에 맞춰 프롬프트를 다시 지었다.** `run_tail_inject.go` 가 **우리 용어로** 적어 뒀다 —
per-turn 바이트(리콜 근거·자동전달 지시문)를 시스템 프롬프트에 넣으면 *"APC is strict byte-prefix matching over
[system][tool schemas][history]"* 라 **도구 스키마와 대화 전체의 KV 가 매 턴 무효**가 된다. DSV4-Flash 실측
**히트율 80.7% + 대화형 턴에 20~40초 프리필 꼬리**. 그래서 **마지막 사용자 메시지 끝**으로 옮겼다.

**내 첫 결론**: 그 주입이 **와이어 전용**이라 다음 턴엔 깨끗한 메시지가 실리고, 그러면 행의 KV 에만 있는 바이트가
생기니 `_continuation`(정확한 접두사만 잇는다)이 **한 번도 안 켜진다** — D16 이 주 클라이언트에게 구조적으로
닿지 않는다. **틀렸다.** 한 시점만 보고 둘인 줄 몰랐다.

**재 보니 두 시점이다**:

| 언제 | 행의 히스토리가 새 프롬프트의 정확한 접두사인가 | 결과 |
|---|---|---|
| **런 안**(에이전트 도구 루프, step1→2→3) | **그렇다** | **`_continuation` 이 켜진다 — 프리필 0** |
| **런 사이**(다음 실행이 깨끗한 히스토리를 다시 싣는다) | 아니다, **91.7%** 에서 갈라진다 | 접두사 캐시가 **91.5%** 입양 |

주입은 루프 **안에서는 남는다**(*"the agent loop appends assistant/tool messages after the injected message,
so the model sees the additions on every step"*). 그래서 **스텝마다 정확한 확장**이고, 요청의 대부분이 거기 있다.

**런 사이에 남은 8.3% 는 진짜로 다르다** — 주입 바이트가 사라지고 새 사용자 턴이 붙었다. 접두사 캐시가 가져가는
91.5% 가 **가져갈 수 있는 전부**다. 행을 되감아 봐야 되감을 지점의 선형 상태가 필요한데, 그게 바로 **경계 스냅샷**이고
접두사 캐시가 이미 그걸 쓴다. **되감기 = 이미 하는 일**이다.

**그래서 "어떻게 고치냐" 의 답은 "여기는 고칠 게 없다" 이다.** 두 시스템이 맞물려 있다 —
루프 안은 대화 티어가, 루프 사이는 접두사 캐시가, 각자 가져갈 수 있는 최대를 가져간다.

**다만 그 비율을 아무도 잰 적이 없다.** 그래서 계량기는 그대로 넣는다:
`st:reuse_path_total{path="continuation"|"prefix_or_cold"}` — 문에서 프롬프트가 **잇는 대화**를 찾았는지
**공유 블록**만 찾았는지 센다. 첫 결론이 맞았다면 `continuation` 이 0 이었을 것이고, 지금 재현이 맞다면
**도구 루프의 길이만큼 커야** 한다. 어느 쪽이든 프로덕션이 답한다.

**교훈**: "두 설계가 서로를 모른다" 는 가설은 매력적이라 **증거보다 먼저 결론에 도착하기 쉽다**.
이번엔 시뮬레이션 한 번이 30분짜리 오답을 막았다 — 그리고 그 시뮬레이션은 **첫 판에 이미 돌렸는데 시점을 하나만
넣었다**. 재현을 짤 때 *"이 워크로드에 시점이 몇 개인가"* 를 먼저 세는 것이 다음 규칙이다.

**검증**: `tools/check.py` 59 files, **798 tests, 0 failed**. 새 테스트 1개(트래픽 없으면 시리즈도 없고, 두 경로가 각각 센다).

### 45차 §71 — 샘플러는 한 행에 505 µs, 배치가 1이든 48이든 같다: 점유가 벽이다 (2026-09-12, srv4, GPU 공유, 무중단)

§70 이 *"다음 레버는 런치가 아니라 점유다"* 로 닫혔다. 확인했다.

**행 수를 늘려도 시간이 안 는다**(`sample_rows`, V=154,880, top_p 0.95):

| 행 | 중앙값 | 행당 | 6행 대비 |
|---:|---:|---:|---:|
| 1 | 504.9 µs | 504.9 | |
| 6 | 507.4 µs | 84.6 | 1.00× |
| 12 | 507.2 µs | 42.3 | 1.00× |
| 24 | 539.0 µs | 22.5 | 1.06× |
| 48 | 628.9 µs | 13.1 | 1.24× |
| 96 | 1,939.1 µs | 20.2 | 3.82× |

**한 행이 505 µs 고 48행도 629 µs 다.** 그리드가 `(M,)` 이라 프로덕션 형상(M=6)에서 **48개 SM 중 6개**만 돈다.
96행에서 3.82배로 꺾이는 지점이 SM 두 바퀴째다.

**의심했던 원인 하나는 15%였다.** 탐색이 라운드마다 `exp` 를 다시 계산하고 있었다 — 행당
154,880 × 17 회. 가중치를 `probs` 버퍼에 한 번만 만들고 15라운드가 그걸 읽게 바꿨더니 **507.4 → 435.6 µs (−14%)**.
정확성 불변(정렬 참조와 ids 정확 일치, probs 2e-6 이내, 12/12). **챙길 만하지만 벽은 아니다.**

**벽은 한 SM 의 대역폭이다**: 남은 ~17 패스 × 620 KB = 10.5 MB 를 429 µs 에 = **24 GB/s**, SM 하나 몫이다.

**다음 설계(안 했다, 설계만)**: 라운드를 줄이면서 동시에 쪼갤 수 있는 건 **히스토그램**이다. 한 패스로
가중치 상위 비트의 2^11 버킷 히스토그램을 만들면 — **프로그램마다 자기 조각을 세고 원자적으로 합치면 되므로
쪼개기가 공짜다** — 임계값의 버킷은 2,048개짜리 스캔으로 찾는다(어휘 패스 0회). 2단으로 하면 ~5 패스면 끝난다.
17 패스 × 1 SM → 5 패스 × 48 SM.
### 45차 §74 — 디코드 가운데를 CUDA 그래프로 캡처했다. 되고, 값은 1.0~1.1배다 (2026-09-12, srv4, GPU 공유, 무중단)

§69 이 *"진짜 답은 캡처다 — 호스트 606 µs 가 통째로 사라진다"* 로 닫혔다. 캡처했다. **그 문장이 틀렸다.**

**되는 건 확인했다**(`probes/decode_middle_capture.py`): 샘플러 + 검증 + **생성기**가 한 그래프에 잡히고,
재생마다 난수가 **진행한다**(얼어붙지 않는다). Triton 커널도 문제없다. 그게 확인해야 했던 부분이다.

| | 중앙값 | 최솟값 |
|---|---:|---:|
| eager 가운데 | 795.7 µs | 775.6 µs |
| 그래프 재생 하나 | 776.7 µs | 756.0 µs |
| | | **1.0×** |

**왜 안 되나**: eager 벽시계(795~847 µs)가 **두 커널의 GPU 시간 합과 같다**(샘플러 555 + 검증 288 = 843).
호스트는 디바이스가 일하는 동안 다음 런치를 이미 내보내고 있어서 **임계 경로에 없다.** 그래프가 숨길 것이
없다. §69 이 "호스트 606 µs" 를 비용으로 읽은 것이 잘못이었다 — 그건 **겹쳐진 시간**이지 더해지는 시간이 아니다.

**그래서 다음 레버는 런치가 아니라 점유다.** 두 커널 다 **행마다 프로그램 하나**라, 프로덕션 형상(n=1, t=6)에서
**48개 SM 중 6개**를 쓴다. 샘플러는 그 위에서 15라운드 × 어휘 한 벌을 도는데 전부 6개 SM 안에서 돈다.

**캡처는 배선하지 않는다.** 1.0~1.1배를 위해 `pipeline.launch` 에 정적 버퍼와 형상 사다리를 더할 이유가 없다.
프로브가 남으니 나중에 가운데가 더 싸지거나 배치가 커지면 다시 물어보면 된다.

### 45차 §72 — 검증 커널 형상 조정(+13%), 그리고 §69 의 5.8배가 무엇의 함수였는지 (2026-09-12, srv4, GPU 공유, 무중단)

**형상 조정.** `block_verify._verify` 의 런치 형상을 `BLOCK 4096/warps 8` → **`8192/16`**. 교대 4회 최솟값:

| 형상 | 4096/8 | 8192/16 | |
|---|---:|---:|---|
| n=1 | 240.8 µs | 240.7 µs | 동률 |
| **n=4** | 245.2 µs | **213.4 µs** | **−13%** |

**1회 측정은 반대를 말했다** — `4096/16` 이 n=1 에서 227 µs 로 최고로 보였는데 4회 최솟값은 258 µs 였다.
**한 번 재고 고르면 안 된다**는 걸 같은 파일에서 또 확인했다.

**그리고 §69 의 "5.8배"가 무엇의 함수였는지.** 오늘 다시 재니 n=1 에서 torch 참조가 **2,392 → 411 µs** 였다.
제가 건드린 적 없는 코드다. **n=4 참조는 세 번 모두 안정적**(3,109 / 3,222 / 3,065)인데 **n=1 만 5.8배 떨어졌다.**

설명은 §70 에 있다 — **같은 우선순위에서 작은 커널은 큰 커널 뒤에 줄을 선다**(거기선 35배). n=1 의 torch 경로는
**180개의 아주 작은 런치**라 경합에 최대로 노출되고, n=4 는 런치당 일이 많아 덜 물린다. §69 는 프로덕션이 서빙
중일 때 쟀고, 지금은 그렇지 않다.

**그래서 §69 의 5.8배는 "한적한 박스에서의 커널 이득"이 아니라 "경합 중인 박스에서 다런치 경로가 얼마나 나빠지는가"였다.**
정정이자 보강이다 — **프로덕션은 항상 나눠 쓴다.** 접기가 없애는 건 평균만이 아니라 **그 분산**이다:
커널은 경합과 무관하게 ~250 µs 인데, torch 경로는 411~2,392 µs 사이를 오간다.


### 45차 §75 — 스냅샷 미터를 트래픽 실린 플릿에서 처음 읽었다: 96 은 과하지 않고 **포화**다 (2026-09-12 20:26~20:28, srv2 헤드, 프로덕션 트래픽, 무중단)

운영자 "프리픽스 스냅샷 검토" → "이어해". §718 이 남긴 숙제가 **미터 넷을 트래픽 위에서 읽는 것**이었고, 읽었다.
**읽고 나서 방향이 뒤집혔다** — 나는 줄일 후보로 보고 있었다.

프로덕션이 **14 요청 · 2,933 스텝**을 서빙한 시점의 스크레이프:

| 계열 | 값 | 읽는 법 |
|---|---:|---|
| `st:prefix_entries` | **96** | 풀이 꽉 찼다 |
| `st:prefix_snapshots_free` | **0** | 여분 0 — **첫 샘플부터 끝까지** |
| `st:prefix_snapshot_self_evicts_total` | **74** | 프리필이 **자기 앞선 경계**를 밀어낸 횟수 |
| `st:prefix_snapshot_denials_total` | **0** | 내줄 게 없어 포기한 경계는 **없다** |
| `st:prefix_faded_entries` | 21–28 | 스냅샷만 잃고 블록은 살아 있는 경계 |
| `st:prefix_cache_fades_total` | 35 | |
| `st:prefix_tier_spills_total` | 42 → **48** | 창 안에서 계속 증가 |
| `st:prefix_tier_restores_total` | **2** | 창 내내 **고정** |
| `vllm:prefix_cache_hits_total` / `queries` | **8 / 22** | 질의는 늘고 **히트는 8 에서 멈췄다** |
| `st:prefix_reused_tokens_total` | **201,984** | 창 내내 고정 |

**96 은 과하지 않다.** `free` 가 0 이고 `denials` 가 0 이라는 조합이 정확히 "꽉 쓰되 모자라서 포기하진 않는" 상태다.
줄이면 재사용이 줄어든다 — **§718 에서 내가 후보로 적은 방향은 틀렸고, 미터가 그걸 죽였다.**

**자기 축출 74 는 비싸지 않다.** `_marks` 는 프리필 스텝 안의 **모든 768 경계마다** 스냅샷을 잡는다(청크당 9개). 96 슬롯을 4행이
나눠 쓰니 한 행의 몫은 24 경계 = **18,432 토큰**이고, 그보다 긴 프롬프트는 자기 앞선 경계를 밀어낸다. 다만 밀려난 경계는 **죽지 않고
fade** 한다(블록 유지, 상태는 티어). 버려지는 것은 **디바이스 상태 복사 한 번**(45 MiB, 34층)이고 74회면 ~15 ms 급이다.
**아픈 것은 시간이 아니라 메모리 4.24 GiB 쪽이며, 그건 지금 전부 쓰이고 있다.**

**대신 읽을 수 없던 것이 하나 드러났다.** `st:tier_bytes_{written,read}_total` 은 `runner.tiered` — **대화 티어**만 읽는다.
**경계 티어(`runner.prefix_tier`)의 바이트는 어디에도 안 실렸다**: 스필 48 · 복원 2 인데 `tier_bytes_read_total` 은 **0**.
즉 §718 의 핵심 논거("fade 된 경계는 프리필이 아니라 NVMe 읽기 한 번 = 45 MiB 8.7 ms")를 **스크레이프로는 확인할 수 없었다.**
`st:prefix_tier_bytes_{written,read}_total` 을 그 티어 자체에 걸어 분리했다(캐시가 아니라 **티어 존재**로 키를 잡는다).

**아직 답이 없는 것**: 히트가 8 에서 멈춘 채 질의만 22 로 늘었다. 재사용 토큰도 201,984 에서 고정이다. 포화한 풀이
재사용되는 속도보다 빠르게 스필한다는 그림인데, **경계 티어의 읽기 바이트가 보이기 전에는 그게 "티어에서 싸게 되찾고 있다"인지
"되찾지 못하고 있다"인지 가를 수 없다.** 그 계열이 이번 변경으로 생겼으니 다음 창에서 그대로 읽으면 된다.

**검증**: `tools/check.py` 60 files, **805 tests, 0 failed**. 새 가드 하나 — 두 티어의 바이트가 **서로 섞이지 않는다**.

### 45차 §79 — "최근 원패스가 최악" 의 답: 같은 커밋인데 다른 엔진을 쟀다 (2026-09-12, srv4, 부팅 원장 대조)

운영자 "가장 최근의 원패스 결과가 최악으로 느린 이유 조사". 결과 표(`~/glm53-logs/bracket-onepass.jsonl`)는
디스크에 없었지만 **부팅 원장은 남아 있었다** — 그리고 그게 답을 갖고 있었다.

오늘의 원패스 부팅 셋을 `memory-rank3.json` 으로 맞대 봤다:

| 부팅 (덤프 디렉터리) | 디코드 폭 | 문맥 상한 | 아레나 | 캡처 | 부팅 전체 | 페이즈 |
|---|---|---|---|---|---|---|
| 18:18 `…-main-e3328753-dumps` | **1–4** | **1,035,264** | 59.14 GiB | 32.3 s | 163.5 s | 76 |
| **18:45 `…-calibfix-dumps`** | **1–8** | **364,032** | 58.10 GiB | **71.9 s** | **208.7 s** | **124** |
| 18:53 `st-dumps` | **1–4** | **1,035,264** | 59.14 GiB | 32.2 s | 160.8 s | 76 |

**같은 커밋, 다른 엔진이다.** 가운데 것만 디코드 그래프를 폭 8까지 잡았고(`target` 36 → 64,
`drafter` 20 → 32, `sampling` 8 → 16), 문맥 상한은 1M 에서 **356K 로 떨어졌다** — 행이 두 배면 행당 KV 몫이
반이니 상한이 따라 내려간 것이다. 추가된 페이즈가 그대로 증거다: `target/(8, 6, 364032)` · `(8, 6, 262144)` …

**왜 그렇게 됐나**: `boot.py` 의 **`MAX_SEQS = 8` 이 레포 기본값**이고, **프로덕션 릴리스는 4 를 박아 둔다**
(cutover 기록: *"4 request rows, context ceiling 1,048,576"*). 18:18·18:53 은 프로덕션 모양으로, 18:45 는
**레포 기본값 그대로** 떴다. 즉 마지막 원패스는 **프로덕션이 돌리는 엔진이 아닌 것**을 쟀다.

**덤프 디렉터리 이름은 트리를 식별하지 않는다 — 한 겹 더 들어가면 이 표의 이름표부터 틀렸다.** 오늘 남은
릴리스 디렉터리를 직접 읽으면 `st-releases/main-e3328753` 과 `main-e3328753-calibfix` 는 **둘 다**
`MAX_SEQS = 8` 이고(둘의 `boot.py` 는 같은 mtime 17:48:40 — 같은 소스에서 rsync 됐다),
`MAX_SEQS = 4` 인 것은 `prod-abceb6a0-grammar-9391` 쪽이다. 그러니 폭 1–4 로 뜬 18:18 은
**`main-e3328753` 트리에서 뜬 것이 아니다**; 덤프 디렉터리에 그 이름이 붙었을 뿐이다.
`--production` 은 `mla_prefill` 과 `context_ceiling` 만 박고 `max_seqs` 는 **건드리지 않으므로**
(`boot.py` 의 `defaults`), 폭을 정하는 것은 오직 그 트리의 `MAX_SEQS` 상수다.
**증거의 이름은 증거가 아니다** — 이 절이 고치는 것이 정확히 그 구멍이다.

**그래서 "느리다" 는 회귀가 아니라 형상 차이일 가능성이 크다** — 캡처만 2.2배, 그래프 풀이 두 배(그래프 2.68 GiB),
워크스페이스 5.2 GiB, 그리고 C=1 로 도는 원패스에서 행 8개짜리 배치·스케줄러 상한은 얻는 것 없이 자원만 쓴다.
**다만 여기까지가 부팅 원장이 말할 수 있는 전부다** — 레그별 수치가 없으니 얼마나 느렸는지, 어느 다리가 나빠졌는지는
이 증거로 말할 수 없다. 그건 기록이 남지 않아서다.

**그래서 고친 것은 그 기록이다.** `bench/onepass.py` 가 레코드마다 **엔진의 형상을 문에서 읽어 찍는다**
(`rec["engine_shape"]`): 서빙 모델 id, `max_model_len`, **`max_concurrent_requests`**, 드래프트 폭, 접두사 캐시·대화 티어 유무.
45차 §56 이 `/v1/models` 에 능력을 싣게 해 둔 덕에 **한 번의 GET 으로 읽힌다**. 능력 블록이 없는 옛 엔진은
있는 것만 싣고 나머지는 **추측 없이 빈다**. 문이 안 열려 있으면 `{}` 이고, 그것이 실행을 실패시키지 않는다.

가짜 문 셋으로 확인:

```
production shape (4 rows, 1M)  {'max_model_len': 1035264, 'max_concurrent_requests': 4, ...}
repo default  (8 rows, 356K)   {'max_model_len': 364032,  'max_concurrent_requests': 8, ...}
engine without capabilities    {'model': 'glm-5.3-flash', 'engine': 'ST'}
nothing listening              {}
```

**교훈**: 오늘 원패스 두 판이 27분 간격·같은 커밋이었고, **레코드에는 그 둘을 구분할 것이 한 글자도 없었다.**
브래킷은 커밋만 찍으면 비교 가능해지지 않는다 — **서빙 형상도 찍어야** 비교 가능해진다.

**검증**: `tools/check.py` 59 files, **798 tests, 0 failed**; `tests/test_fleet_onepass.py` 22 OK(인자 표면 불변).

### 45차 §73 — 데네브 레포 쪽에서 우리를 보니: 문이 모르는 모델 이름을 그대로 돌려주고 있었다 (2026-09-12, srv4, 코드 대조)

운영자 "데네브 레포 입장에서 st엔진 개선할거 찾아봐". 우리가 뭘 더 주고 싶은지가 아니라 **데네브가 무엇을 우회하고
무엇을 경고해 뒀는지**를 읽었다.

**데네브의 단일 진실원이 우리를 이렇게 적어 뒀다** (`docs/agent-rules/sidecar-models.md`):

> ⚠️ 웜홀의 `deepseek-v4-flash`·`dsv4-nothink` 엔트리는 여전히 이 주소를 가리키지만 `upstreamModel` 이
> `deepseek-v4-flash` 라 **서버에 없는 모델을 요청해 fallback(z.ai)으로 샌다** — 이름은 dsv4 인데 실제로
> 답하는 건 다른 모델이다.

그리고 같은 문서가 **같은 종류의 낡은 엔트리가 2026-08 에 12일간 1,346 회 과금**시킨 사건을 적어 뒀다.

**그런데 지금은 그 경고보다 나쁘다.** 그 문장은 srv2 가 **vLLM** 이던 때의 것이다. vLLM 은 모르는 모델에
404 를 내므로 라우터가 페일오버했다. **ST 는 이름을 검사하지 않는다** — `serve.py` 는 요청의 `model` 을
**응답에 그대로 되돌려 주고** GLM-5.3 으로 답한다. 즉:

```
요청 model=deepseek-v4-flash  →  200, 응답 model="deepseek-v4-flash", 내용은 GLM-5.3
```

**답은 맞는데 이름표가 틀린다.** 그리고 이름을 믿는 것이 전부 아래에 있다 — 데네브의 모델별 사용량,
`metered` 게이트, `ProfileFor`(이름에 `deepseek` 가 있으면 *"reasoning_effort 는 high/max 만"* 규칙을 붙인다).
페일오버로 새는 건 청구서에 보이기라도 하지, 이건 **아무 데도 안 보인다.**

**고친 것**: `Server.check_model()` 이 **모든 OpenAI 호환 서버가 하는 그대로**(vLLM
`entrypoints/openai/models/serving.check_model`) 답한다 — 이름이 없거나 비면 이 엔진 것이고,
서빙하지 않는 이름이면 **404 에 그 문장 그대로**:

```
The model `deepseek-v4-flash` does not exist.
```

문장을 글자까지 맞춘 건 **메시지로 매칭하는 클라이언트가 그대로 돌게** 하기 위해서다.

**부수 확인 — 데네브가 이미 고쳐도 되는 것 하나**: `agentlog.go:274` 가
*"the engine does not report `cached_tokens` in per-request usage"* 라며 **엔진 전역 `/metrics` 를 긁어
차분을 내는 우회**를 들고 있다(겹치는 런에서 "smear" 된다고 자기가 적어 뒀다). **ST 는 낸다** —
스트림이든 아니든 `usage.prompt_tokens_details.cached_tokens` 에. 데네브도 `include_usage: true` 를 보내고
`PromptTokensDetails.CachedTokens` 를 **파싱한다**. 즉 **배선은 이미 다 되어 있고 주석만 옛 vLLM 시절 것**이다.
저쪽 레포의 한 줄이라 여기서는 안 고쳤다.

**검증**: `tools/check.py` 59 files, **796 tests, 0 failed**. 새 테스트 2개 —
없는 이름/빈 이름/서빙하는 이름/모르는 이름 넷 / 문이 404 를 내고 그 문장을 싣는다.
기존 테스트 하나가 `model="m"` 으로 메아리에 기대고 있어 서빙하는 이름으로 고쳤다.

### 45차 §80 — 겹침은 된다: 4배의 일이 1.21배의 시간에. 문제는 실리콘이 아니라 **독립적인 일이 없다는 것** (2026-09-12, srv4, GPU 공유, 무중단)

§71 이 *"프로덕션 형상에서 48개 SM 중 6개만 돈다"* 로 끝났다. 운영자 질문: **48개를 채우도록 파이프라인·비동기·
스케줄러를 만들면?** 하드웨어가 그걸 허락하는지부터 쟀다(`probes/sm_overlap.py`, 샘플러 6행 호출을 k벌 동시에):

| 동시 사본 | 같은 스트림 | 각자 스트림 | 겹침 이득 |
|---:|---:|---:|---:|
| 1 | 435.9 µs | 445.2 µs | 0.98× |
| 2 | 857.8 µs | **468.9 µs** | **1.83×** |
| 4 | 1,728.9 µs | **526.0 µs** | **3.29×** |
| 8 | 3,513.9 µs | 1,018.4 µs | 3.45× |

**4배의 일이 1.21배의 시간에 끝나고, 8벌(8×6 = 48 프로그램)에서 정확히 꺾인다** — SM 수 그대로다.
**실리콘은 있고 겹침은 된다.**

**그런데 배치 1의 디코드 체인에는 겹칠 것이 없다.** forward → 샘플러 → 검증 → 커밋 → observe → propose 는
한 토큰의 의존 사슬이다. 스케줄러가 만들어 낼 수 있는 독립 작업이 아니다. 행이 넷이면 이미 한 호출에
24행으로 들어가므로(24행 539 µs) 배치는 이미 그 길을 쓴다.

**독립적인 일이 실제로 하나 있다: 기다리는 요청의 프리필이다.** 그리고 그건 **D9 가 금지한다** — 한 스텝은
순수 프리필이거나 순수 디코드다. 다만 **D9 의 근거(39차 DF1)는 "혼합 배치"를 쟀다** — 하나의 forward 에
디코더와 프리필 청크를 같이 넣었더니 SP·그래프·메가커널이 전부 꺼져 혼합 스텝 하나가 ~650 ms 였다.
**두 개의 스트림은 혼합 배치가 아니다.** 각자 자기 빠른 경로를 그대로 쓰고 SM 만 나눈다.
**그 형태는 재 본 적이 없다.**

**그래서 열린 질문은 이것 하나다**: 프리필 forward(큰 커널, SM 을 채운다)와 디코드 체인(6 프로그램)을
**별도 스트림으로** 동시에 돌리면, 디코드 ITL 이 얼마나 늘고 프리필이 얼마나 빨라지는가.
39차가 혼합 배치로 답한 것과 **다른 질문**이다. 헌장 D9 를 건드리므로 **운영자의 결정**이고,
플릿 창이 필요하다(랭크 넷).

### 45차 §81 — 디코드 스텝의 구성을 프로덕션이 답했다: 드래프터가 36.4% 다 (2026-09-12, srv4→10.10.10.2, 무중단)

§71 의 단계 계량기가 배포돼 답했다. 표본 31 스텝, 스텝당 **74.99 ms**:

| 단계 | 스텝당 | 비중 |
|---|---:|---:|
| **forward** | 47.60 ms | **63.5%** |
| **observe**(드래프터 관측) | 16.02 ms | **21.4%** |
| **propose**(드래프터 제안) | 11.29 ms | **15.1%** |
| sample | 0.03 ms | 0.0% |
| boundaries | 0.03 ms | 0.0% |
| commit | 0.01 ms | 0.0% |

**`all_gather` 와 `verify` 단계가 없다** — 프로덕션은 **그리디 경로**로 돈다(`sampling_graphs.greedy`, 캡처된
그래프라 0.03 ms). `async_decode_steps 1970`, `sync_drain 0`: 체인에 100% 머문다.

**이것이 오늘의 작업 방향을 정정한다.** 나는 `block_verify_batch`(§69, 5.8배)와 샘플러(§71, −14%)를 깎았는데,
**그 둘은 지금 프로덕션 경로에 없거나 0.0% 다.** 확률 경로가 켜지면 값이 생기지만 지금 도는 것은 아니다.
하루 종일 "가운데"라고 부른 것의 합이 **0.07%** 였다.

**진짜 자리는 드래프터다 — 36.4%, 그리고 아무도 본 적이 없다.**

**첫 삽**: `observe_rows` 의 fast_attention 경로가 **레이어마다, 행마다** 링에 썼다 —
`write_draft_kv` 가 `slot.numel() == 1` 을 하드 요구해서 루프가 강제됐기 때문이다. 행을 그리드에 넣은
`write_draft_kv_rows` 를 만들어 **레이어당 한 번**으로 바꿨다:

| | 런치 | 시간 |
|---|---:|---:|
| 행별 (3 레이어 × 4 행) | 12 | 67.8 µs |
| 배치 | **3** | **12.2 µs** |

**5.6배**, 그리고 **비트 단위로 동일**(최대 차이 0). 다만 이건 16 ms 중 **런치 몫**이고, 나머지는 투영
(`fc` GEMM, `context_kv` GEMM, 레이어별 rmsnorm+rope)이다. **observe 의 21.4% 를 다 가져온 것이 아니다.**

### 45차 §82 — observe 의 16 ms 는 투영이 아니었다: 로터리가 매번 자기 상수표를 다시 만들고 있었다 (2026-09-12, srv4, GPU 공유, 무중단)

§81 이 남긴 질문 — `observe` 21.4% 중 `write_draft_kv_rows` 가 가져간 런치 몫 말고 나머지는 무엇인가.
프로덕션 형상(한 시퀀스, K=5 → **6 행**, TP=4 → local_kv 2, 드래프터 5 레이어)으로 하나씩 쟀다.

| 부분 | 시간 |
|---|---:|
| `fc` W4 (6×20480 → 4096, 5 팩) | 571.0 µs |
| `context_kv` bf16 (6×4096 → 2560) | 46.1 µs |
| **레이어별 rmsnorm+rope ×5** | **4,156.7 µs** |

**투영이 아니라 꼬리다.** 원소 1,536 개짜리 텐서에 레이어당 831 µs. 쪼개 보면:

| | 시간 |
|---|---:|
| `rmsnorm` | 159.8 µs |
| `rope` 전체 | 452.9 µs |
| — 그 중 **역주파수 표 재생성** | 122.1 µs |
| — 각도+cos+sin | 95.2 µs |
| — 회전+cat | 207.9 µs |
| (런치 바닥: `empty_like`) | 2.0 µs |

`rope` 는 **호출마다** `arange`→`div`→`pow`→`reciprocal` 로 64 개의 **모델 상수**를 다시 만들고 있었다.
그리고 드래프터의 로터리 호출은 **예외 없이** `rope(rmsnorm(x, w, eps), positions, theta)` 다 — 10 곳 전부.
스텝당 호출 수: observe 5 + 블록당 10 × 5 블록 = **55**.

**`engine/kernels/norm_rope.py`**: `norm` 과 `norm_rope` 를 각각 한 번의 런치로. 표는 (device, dim, theta)
마다 한 번만 만들고 커널에 넘긴다 — 그래서 각도의 `position * inv` 곱이 torch 경로와 **비트 단위로 같다**.
캡처 중에 표가 없으면 **에러**다(D3): 그래프의 사설 풀에 모델 상수를 넣으면 그 그래프가 닫힐 때 사라진다.
`capture_decode` 가 캡처 전에 데운다.

| | torch | 융합 | |
|---|---:|---:|---|
| observe 꼬리 (×5) | 3,477.1 µs | 522.5 µs | **6.7배** |
| 제안 블록 하나 | 8,823.9 µs | 1,346.0 µs | **6.6배** |
| 디스패치 (스텝당, 정규화만) | **2,315** | **285** | **−2,030** |

**정확도.** 각도는 비트 동일. 정규화 값은 합산 순서가 달라 **최대 1 bf16 스텝** 어긋날 수 있다 — 64 회
무작위 시행 307,200 원소 중 **8 개(0.0026%)**, 전부 정확히 1 스텝. 이 둘을 읽는 것은 **드래프터뿐**이므로
마지막 비트가 움직이는 것은 **초안**이지 서빙된 토큰이 아니다: 무엇을 내보낼지는 타깃의 로짓이, 무엇을
받아들일지는 블록 검증이 정한다. `tests/test_engine_norm_rope.py` 가 1 스텝 상한과 0.1% 이하 이동률,
그리고 캡처 중 표 부재가 에러라는 것을 박는다.

**읽는 법.** 위 µs 는 **이거 경로**를, **프로덕션과 GPU 를 공유하며** 잰 것이다. torch 쪽은 수십 개의
런치라서 경합이 정확히 그걸 부풀린다. 프로덕션은 이 둘을 **CUDA 그래프 안에서** 돌리므로 호스트 런치
몫은 이미 없고, 실제 이득은 이 배수보다 **작다**. 믿을 수 있는 수는 **배수와 디스패치 수**이지 절대값이
아니다. 진짜 판정은 다음 부팅의 `st:decode_stage_seconds_total{stage="observe"|"propose"}` 다.

**정정.** §81 직후 내가 잰 `fc` bf16 783 µs 는 프로덕션 경로가 아니다 — 부팅 로그의 `drafter_w4: 31`
(= fc + 5 레이어 × 6) 이 말하듯 `fc` 는 **W4 팩**이고, 그건 571 µs 다. 어느 쪽이든 꼬리의 1/7 이다.

### 45차 §83 — 드래프터의 나머지 절반: 탭 컨볼루션이 스텝당 1,500 디스패치였다 (2026-09-12, srv4, GPU 공유, 무중단)

§82 가 정규화를 접고 나서 남은 것. DFlash 는 어텐션과 MLP 양쪽에 짧은 인과 컨볼루션을 둔다 —
**레이어당 4번, 블록당 20번, 제안 스텝당 100번**. torch 로 쓰면 한 번이 **15 디스패치**이고,
[rows, taps, groups, group] 계수를 **매번 물질화**한다(호출당 98 KiB, 스텝당 9.8 MiB — 한 번 읽고 버린다).

| | 시간 | 디스패치 |
|---|---:|---:|
| `_conv` 한 번 (6×4096, taps 2, group 16) | 217.6 µs | 15 |
| **스텝당 100번** | **21.76 ms** | **1,500** |

`engine/kernels/draft_conv.tap_mix` 가 한 런치로 접는다. 계수는 **모델이 가진 가중치 + 그 행의 투영이
만든 수**의 합이라 미리 준비할 수 없고, 오직 레지스터에 두고 융합할 수만 있다.

    out[r, c] = Σ_tap (base[tap, c] + delta[r, tap, c // group]) * x[r - tap, c],   r % block < tap 이면 0

`block` 이 행을 독립 블록으로 자른다 — 탭은 블록 **안에서만** 뒤를 본다. `_conv` 는 block=행수,
`_conv_rows` 는 block=t. 그래서 두 형태가 **하나의 커널**이 된다(호출부의 `tap_valid` 도 함께 사라졌다).

| | torch | 융합 | |
|---|---:|---:|---|
| 탭 믹스 한 번 | 318.8 µs | 63.1 µs | **5.1배** |
| 제안 블록 하나 (정규화+믹스) | 15,912.5 µs | 2,659.6 µs | **6.0배** |
| **디스패치 (스텝당, 드래프터 잔가지 전체)** | **4,315** | **485** | **−3,830** |

**정확도.** 항(term)은 **비트 단위로 동일**하다 — 계수를 torch 의 덧셈이 반올림하는 자리에서 똑같이
가중치 dtype 으로 반올림하고, bf16 두 수의 곱은 어느 쪽이든 같게 반올림된다. 다른 것은 **합**뿐:
이쪽은 fp32 로 더해 한 번만 반올림하고, 옛 형태는 탭마다 누계를 반올림했다. 그래서
**블록의 첫 행(살아 있는 탭이 tap 0 하나뿐)은 옛 답과 정확히 같고**, 그 뒤는 항 크기 합 기준
**2 half-step 이내**(프로덕션의 taps=2), 4 탭에서 3 half-step. 차이는 **커널 쪽이 더 정확한** 방향이다.

`tests/test_engine_draft_conv.py` 가 박는 것: 첫 행의 비트 동일성, 2 half-step 상한, 그리고
**탭이 블록 경계를 넘지 않는다**는 것(두 블록, 앞 블록 끝에 1 을 놓고 뒤 블록이 0 인지).

**잡은 함정 하나.** 래퍼가 `delta.contiguous()` 를 **런치하면서 스트라이드는 원본의 것**을 넘기고 있었다.
테스트에서는 안 보인다 — 테스트가 만드는 delta 는 이미 연속이라 둘이 같은 텐서다. 프로덕션은
`coeff[:, 0]`(=[rows, 2, taps, groups] 투영의 슬라이스, 행 스트라이드가 두 배)을 넘기므로 **거기서만**
엉뚱한 계수를 읽는다. 규칙: **넘긴 텐서가 아니라 실제로 런치되는 텐서의 스트라이드를 바인딩한다.**
슬라이스를 먹이는 테스트를 추가했고, 고치기 전 코드에서 실제로 실패하는 것을 확인했다.

**남은 것.** §82 와 같은 읽기 규칙이 적용된다 — 이거 경로, 프로덕션과 공유한 GPU, 그래프 안에서는
호스트 런치 몫이 이미 없다. 판정은 다음 부팅의 `stage="propose"` 다. 그리고 아직 접지 않은 것:
`fc` W4 가 **5 개의 팩을 따로 부르고 fp32 로 더한다**(571 µs, 10 op). 그게 observe 의 다음 표적이다.

### 45차 §85 — 대화 이어가기가 왜 한 번도 안 탔나: 추론을 켜는 쪽과 되돌려주는 쪽이 다른 계층이다 (2026-09-12, srv4, GPU 없음, 실물 템플릿·토크나이저)

§75 의 스크레이프에 **대화 10개가 파킹돼 있는데 `st:reuse_path_total{continuation}` 은 0** 이었다. D16 의 절반이
프로덕션에서 한 번도 안 탔다는 뜻이라 양쪽 레포를 다 열었다.

**문의 조건은 맞다.** `continue_history`(채팅 경로가 True 로 넘긴다) · `keep_idle`(티어가 있으면 True) ·
테넌트 솔트(데네브는 `prompt_cache_key` 를 안 보내므로 양쪽 다 None) — **셋 다 열려 있다.** 남은 것은
`_continuation` 의 본질적 조건 하나뿐이다: **다음 프롬프트가 엔진이 들고 있는 히스토리의 진짜 접두사여야 한다.**

실물 `chat_template_mm_v2.jinja` 와 실물 `tokenizer.json` 으로 두 턴을 렌더해 봤다(GPU 없음):

| 렌더 | 턴1 생성 프롬프트 꼬리 | 히스토리 | 다음 프롬프트 | 공통 접두사 | 이어가기 |
|---|---|---:|---:|---:|---|
| **thinking 끔** | `<\|assistant\|><think></think>` | 22 | 29 | **22** | **탄다** |
| **thinking 켬** | `<\|assistant\|><think>` | 21 | 28 | **19** | **못 탄다** |
| thinking 켬 + `reasoning_content` 되돌려줌 | 〃 | 33 | 39 | **33** | **탄다** |

**템플릿이 `<think>` 를 직접 연다.** 그래서 모델은 `<think>` 를 생성하지 않고 **추론 본문부터** 낸다. 다음 턴에
그 어시스턴트 차례를 다시 그릴 때 **`reasoning_content` 가 없으면 추론 구간이 통째로 빠지고**, 히스토리는 그
지점에서 접두사이기를 그만둔다 — 위 표의 19/21 이 그것이고, **첫 user 턴 이후가 매 턴 전부 재계산**된다.
이어가기만 죽는 게 아니라 **접두사 캐시도 거기서 끊긴다.**

**그런데 켜는 쪽과 되돌려주는 쪽이 다른 계층이다.** 데네브는 자기 thinking 설정으로 `preserveThinking`
(`interleavedEnabled`: `Thinking.Type=="enabled" && Interleaved`)를 정해 놓고 요청을 보낸다. 그 뒤 **웜홀이**
`effort.go` 에서 Ares 판정이나 caller 의 high/max 를 보고 **그 턴을 추론 턴으로 바꿀 수 있다**
(`thinking:{"type":"enabled"}` + `reasoning_effort:"high"`). 즉 **모델이 추론을 내는 턴인지는 웜홀이 정하는데,
그 추론을 다음 턴에 되돌려줄지는 이미 상류에서 정해져 버렸다.** 되돌려줄 쪽은 자기가 추론 턴이었다는 걸 모른다.

라이브 설정은 `agents.defaults.thinking = {level:"low", interleaved:true}` 이고, 웜홀 주석이 적어 둔 대로
**데네브의 "low" 는 thinking 끔**이다 — 그래서 `Type!="enabled"` → `preserveThinking` 은 `interleaved:true`
에도 불구하고 **false**. 설정대로 가는 턴은 표의 첫 줄(이어가기 탄다)이고, **웜홀이 올린 턴은 둘째 줄**이다.

**아직 못 밝힌 것**: 첫 줄 경로도 이어가기가 탔어야 하는데 스크레이프는 0 이었다. 그 창의 트래픽이 어느
줄이었는지는 기록이 없고(요청별 effort 가 어디에도 안 남는다), **플릿이 내려가 있어 두 턴을 실제로 던져
확인하지 못했다.** 다음 창의 첫 번째 할 일이다 — 그리고 그 김에 **문이 자기가 받은 `reasoning_effort` 를
세어 두면**(끔/high/max) 이 질문은 다시는 추측이 되지 않는다.

### 45차 §84 — 드래프터를 **그래프 재생으로** 재니 답이 달라졌다: 14.97 → 6.12 ms (2026-09-12, srv4, GPU 공유, 무중단)

§82·§83 은 이거 경로에서 6~7 배였지만 "그래프 안에서는 더 작다" 고만 적었다. 이제 **재봤다.**

**하니스.** world_size=1 에 **이미 이 랭크의 샤드인** facts(heads 8, kv 2, inter 3072, hidden 4096, 5 레이어)를
주면 `prepare_fast` 의 샤딩이 항등이 되고 모든 GEMM 이 프로덕션 랭크가 도는 크기가 된다(장치 606 MiB).
`observe_rows` 와 `block_rows` 를 각각 CUDA 그래프로 캡처해 **재생 시간**을 잰다 — 호스트 런치는 없고
기록된 커널만 도는, 프로덕션이 보는 바로 그 수치. (TP 콜렉티브는 no-op 으로 뺐다.)

**먼저: 블록 하나(1,789 µs)의 구성.** 융합 뒤 남은 것이 무엇인지 부분별로 캡처해 쟀다.

| 부분 | µs | 비중 |
|---|---:|---:|
| 30 W4 GEMM (레이어당 6) | 854.5 | 47.8% |
| **5 draft_attention** | **724.3** | **40.5%** |
| 20 탭 믹스 (+5 투영) | 87.8 | 4.9% |
| 15 norm + 10 norm_rope | 43.0 | 2.4% |
| 5 silu*mul | 27.7 | 1.5% |
| 부분의 합 | 1,737.4 | (블록 1,789) |

**내 융합분은 이제 4% 다.** 정규화나 컨볼루션을 더 깎아봐야 아무것도 안 나온다.

**진짜 자리: `draft_attention` 이 링을 24번 다시 읽고 있었다.** 그리드가 (행, 헤드)라
TP=4 에서 6×8 = 48 CTA 인데, `H//HK = 4` 개 쿼리 헤드 × 6 행 = **24 CTA 가 같은 kv 헤드의 링을 각자
전부 훑는다**. 레이어당 읽기: 48 × 2,056 × 128 × 2 B × 2 = **50.5 MiB** — 고유한 건 **2.1 MiB** 다.
145 µs / 50.5 MiB = 348 GB/s, 즉 **중복 트래픽에 대역폭으로 묶여 있었다**.

`_attend` 는 **(kv 헤드, 윈도우 조각)마다 CTA 하나**로 그 헤드의 모든 쿼리(행 × 그룹)를 한 타일로 든다.
조각 수는 `kv헤드 × 조각 = 48` 이 되게 자르고, `_combine` 이 조각들의 부분 소프트맥스를 접는다.
블록 자신의 키는 옛 루프의 꼬리에서 그랬듯 마지막 조각에 실린다. 쿼리 타일은 32 로 묶여 그리드에
실린다 — kv 헤드 하나가 모든 쿼리 헤드를 맡는 형상에서 `B*(H//HK)` 가 1024 가 되고 그만한 fp32
누산기는 CTA 에 안 들어간다(공유메모리 131,584 B 요구, 한계 101,376).

| | 옛 커널 | 새 커널 | |
|---|---:|---:|---|
| 레이어당 (캡처 재생) | 179.1 µs | **19.2 µs** | **9.3배** |
| 블록당 5회 | 724.3 µs | **86.1 µs** | **8.4배** |

**정확도.** 소프트맥스 가중치는 fp32 로 값과 곱한다 — 표준 플래시가 하듯 bf16 으로 내리면 출력 벡터의
0.4% 를 잃고, 드래프터의 수락률이 거기서 읽힌다. fp64 대신 fp32 오라클 대조로 **출력 벡터가 자기 노름의
2^-8(= bf16 한 스텝) 이내**, 여러 형상에서 **비트 동일**. bf16 으로 저장하는 이상 그 아래로는 못 간다.

**그래서 스텝 전체:**

| 재생 | 원래 | 세 융합 뒤 | |
|---|---:|---:|---|
| `observe_rows` | 663.8 µs | **392.0 µs** | 1.69배 |
| `block_rows` | 2,861.5 µs | **1,144.8 µs** | 2.50배 |
| **스텝(observe + 5 블록)** | **14.97 ms** | **6.12 ms** | **2.45배** |

**남은 것과 정직한 경고 둘.**

1. 블록의 남은 절반은 **30 개의 W4 GEMM(828.8 µs)** 이다. 레이어당 가중치 28 MB × 5 = 142 MB,
   273 GB/s 바닥이 **520 µs** 이므로 **1.6배 여유**뿐 — 여기는 거의 다 썼다. `fc` 의 5 팩 접기
   (§83) 는 여전히 남았지만 그건 observe 쪽이다.
2. **이 하니스는 프로덕션의 `observe` 16.02 ms 를 재현하지 못한다** — 여기선 0.66 ms 다(24배 차이).
   `propose` 는 맞는다(5 × 2.86 = 14.3 ms 대 11.29 ms, 콜렉티브를 뺀 것 감안). 형상은 확인했다:
   window 2048, block 8, aux 20480, fc 5 팩, local kv 2 / heads 8 — 전부 프로덕션과 같다. 캡처되지
   않은 형상이 이거로 흐르는 것도 아니다(`base/graphs.run` 은 **KeyError 를 던진다**, 폴백이 없다).
   **원인 미상으로 남긴다.** 다음 부팅의 `stage="observe"` 가 답할 것이다.

**하니스는 `probes/drafter_step_replay.py` 로 남겼다** — 부팅도 체크포인트도 플릿도 없이, 랜덤 가중치
600 MiB 로 드래프터 스텝의 재생 시간과 블록의 부분별 구성을 찍는다. 앞으로 드래프터를 건드리는 사람은
이거 측정으로 자기를 속이지 않아도 된다.

**곁다리로 고친 것.** `tests/test_engine_draft_attention.py` 의 `if __name__ == "__main__"` 가 파일
**중간**에 있었다 — 뒤에 붙은 `BatchedRingWriteTests` 가 스크립트로 실행할 때 아예 정의되기 전이라
조용히 안 돌았다. 끝으로 옮겼다.

**번호 정정.** 원장에 §81 이 **둘** 있었다 — 병렬 세션이 「대화 이어가기」를 §81 로 붙였는데 그 번호는
이미 「디코드 스텝의 구성」이 쓰고 있었다(둘 다 main 에 들어간 뒤 발견). 뒤에 온 쪽을 **§85** 로 옮겼다.
앞 것은 §82·§83·§84 와 `probes/drafter_step_replay.py` 가 참조하므로 건드리지 않는다.

### 45차 §86 — 프로브가 틀렸었다: 한 스텝에 블록은 **한 번** 돈다, 그리고 드래프터의 최대 항목은 어휘 헤드다 (2026-09-12, srv4, GPU 공유, 무중단)

§84 의 하니스는 "스텝 = observe + 5 블록" 이라고 찍었다. **틀렸다.** `propose_rows` 는 `block_rows` 를
**한 번** 부른다 — 앵커 + K 개 마스크를 한 블록의 t = K+1 = 6 행으로 **동시에** 민다(확산형 드래프터이지
자기회귀 k 스텝 루프가 아니다). 그래서 스텝의 드래프터는 5.96 ms 가 아니라:

| 재생 (프로덕션 랭크 형상) | µs |
|---|---:|
| `observe_rows` | 334 |
| `propose_rows` | 2,739 |
| — 그 중 `block_rows` | 1,112 |
| — **헤드와 선택기** | **1,627** |
| **드래프터 전체** | **3,080** |

**그리고 그 안에서 제일 큰 것은 어휘 헤드 GEMM 이다 — 1,392 µs(헤드+선택기의 85%).** 여기 하니스는
bf16 헤드(317 MB)를 쓰고 프로덕션은 FP8(158 MB)이니 실제로는 그 절반쯤일 것이다. 어느 쪽이든
**대역폭 바닥이고 접을 데가 없다** — 드래프터는 154,880 어휘 전체에서 위 16 개를 골라야 하므로
헤드 가중치를 다 읽어야 한다. 나머지: topk 172, 워크 37, 코드북 게더 11, einsum 10, 투영 14.

**이번 판 네 가지, 전부 같은 프로세스 인터리브 A/B(min of 3).** 프로세스 간 차이는 못 믿는다 —
워크를 처음엔 **634 µs 절감**으로 읽었는데 A/B 로는 **43.9 µs** 였다(경합 변동).

| 바꾼 것 | 전 | 후 | |
|---|---:|---:|---|
| `fc`: 5 타일 → **한 런치** | 255.8 µs | 210.2 µs | **1.22배** |
| 링 쓰기의 k/v 복사 제거 | 32.8 µs | 10.3 µs | **3.19배** |
| `silu*mul` → `swiglu` (×5) | 27.5 µs | 9.8 µs | **2.80배** |
| 탐욕 워크 (propose 안에서) | 2,766.9 µs | 2,727.7 µs | 1.01배 |

합쳐서 **약 125 µs**, 드래프터 스텝의 **4%**. 작다. 그렇게 적는다.

**`fc` 접기가 커널을 건드린 이유.** `kernels.cu` 의 `KBLK_MAX = 32` 가 K ≤ 4096 을 강제했고, 그래서
[4096, 20480] 짜리 `fc` 는 **5 개 팩을 따로 부르고 fp32 로 더하는** 형태였다. 그 상수는 두 개의
`__device__` 스테이징 배열(`g_mk2_aq`, `g_mk2_axs`)을 재는 데도 쓰이는데, 그건 **smlp2 의 staged-A
경로 전용**이고 평범한 런치는 건드리지 않는다. 그래서 상수를 쪼갰다 — 스테이징은 `KBLK_MAX = 32`
그대로, 평범한 런치의 K 상한만 `KBLK_LIMIT = 160`(K ≤ 20480). 평범한 런치가 staged-A 플래그를 들고
오면 **에러**다.

접는 것 자체는 파이썬이다: `pack_wide`/`pack_w4_wide` 는 **K 전체에 대해 행 시프트를 한 번** 구해
모든 타일에 같은 것을 준다. 그래서 타일들은 **한 양자화를 자른 조각**이고, 타일 축으로 이어붙인 것이
곧 커널이 원하는 넓은 팩이다(`_fold`). 첫 부팅(무교정)이 파일하는 타일은 시프트가 타일마다라 안 접히고,
그건 그대로 둔다 — 다음 부팅의 스토어가 통으로 팩한다.

**정확도.** 옛 경로는 **타일마다 출력을 bf16 으로 반올림한 뒤** 더했고, 접은 쪽은 160 개 k 블록을
fp32 로 누산해 한 번 반올림한다. 둘 다 같은 가중치를 근사하므로 **참값(fp32 원본 곱) 대비**로 판정했다:
16 시행 × 6 행에서 **접은 쪽이 62 행에서 더 가깝고 34 행에서 더 멀다** — 차이는 W4 양자화 오차(8.5e-2)의
1/1000 수준이다. `swiglu` 와 탐욕 워크는 **비트 단위로 동일**(워크는 전부 동점인 경우까지 확인).

**남은 자리.** 블록 1,112 µs 중 **30 개 W4 GEMM 이 830 µs** 이고 바닥이 520 µs — **1.6배**뿐이다.
탭 믹스 88, 어텐션 84, norm 43, swiglu 10 은 전부 런치 바닥에 붙었다. 다음에 볼 것은 `topk`(172 µs):
패킹된 키는 `(score << 32) | (0xffffffff - id)` 라 **동점이 없으므로** 융합해도 답이 유일하다.

### 45차 §87 — 선택기를 접었다, 그리고 **박아둔 계약 하나를 깰 뻔했다** (2026-09-12, srv4, GPU 공유, 무중단)

§86 이 남긴 둘을 접었다. 전부 같은 프로세스 인터리브 A/B(min of 3).

**1. 어휘 topk 의 지역 선택 (98.4 → 16.4 µs, 6.0배).** 키는
`(ordered_score << 32) | (0xffffffff - id)` 라 **유일하다**. 유일하면 "지금까지 고른 것보다 작은 것 중 최대"
가 정확하므로, 이미 쓴 것의 마스크 없이 K 라운드로 top-k 가 나온다. 세그먼트마다 레지스터에 올려 K 라운드,
그 결과를 다시 한 번 — 두 단계면 끝. `BLOCK` 을 512~8192 로 훑어 2048 이 최적(16.4 µs)이었다.

**깰 뻔한 계약.** 처음엔 **병합까지** 접었다 — `restore` 가 [rows, 154,880] fp32 를 −inf 로 채워(3.1 MiB)
거기서 16 개를 고르는 걸 109.7 µs 짜리 낭비로 봤기 때문이다. 집합과 점수는 모든 경우 동일했고 **동점 하나의
순서만** 달랐다(둘 다 3.5156, 옛것 1377 먼저 / 키 순서 1197 먼저). 그런데
`tests/test_engine_vocab_topk.py` 는 **`vocab.topk` 이 `full.topk(k)` 과 id 까지 같아야 한다**고 박아뒀다 —
`test_actual_vocabulary_ties_and_masks_match_dense_cuda_topk`. 이전 캠페인이 **일부러** torch 의 조밀 CUDA
topk 동점 순서에 고정한 것이다. 되돌렸다.

**남긴 형태**: 지역 선택만 융합(`sorted=False` 가 "여기 순서는 답이 아니다" 라고 이미 말한다 — **집합**만
같으면 되고, 키가 유일하니 k 개 최대의 집합은 하나다). 병합은 torch 의 조밀 topk 그대로 — 동점 순서를
박아두는 게 그거다. 다섯 형상에서 **torch 의 지역 topk 와 같은 집합**임을 확인했다.

**2. 선택기의 점수 + 워크 (57.4 → 8.2 µs, 7.0배, 32/32 비트 동일).** torch 형태는
[n, K, 16, 256] 짜리 코드북 게더를 **두 번** 물질화하고(각 164 KiB) einsum 으로 [n, K, 16, 16] 점수를 만든 뒤
그 위를 5 번 걸었다 — 한 번씩만 읽을 것을 위해. 융합하면 한 스텝은 **[256] 벡터 하나를 그 스텝의 후보
행들에 대고** 곱하는 것뿐이고, **점수는 존재하지도 않는다.** 워크가 나르는 것은 인덱스가 아니라 **토큰**이다
(다음 스텝의 predecessor 코드북 행을 그걸로 연다).

샘플링 경로는 루프 그대로다 — 그 뽑기는 엔진의 생성기이고, 그걸 커널에 넣으면 랭크 합의와 D12 재생이
같이 들어간다(`block_verify` 가 긋는 같은 선).

**이번 브랜치 전체, 인터리브 A/B 합계**

| 접은 것 | 전 | 후 | |
|---|---:|---:|---|
| `fc`: 5 타일 → 한 런치 | 255.8 µs | 210.2 µs | 1.22배 |
| 링 쓰기의 k/v 복사 | 32.8 µs | 10.3 µs | 3.19배 |
| `silu*mul` → `swiglu` (×5) | 27.5 µs | 9.8 µs | 2.80배 |
| 어휘 topk 의 지역 선택 | 98.4 µs | 16.4 µs | **6.0배** |
| 선택기 점수 + 워크 | 57.4 µs | 8.2 µs | **7.0배** |
| **합** | **472 µs** | **255 µs** | **−217 µs** |

§86 의 3,080 µs 짜리 드래프터 스텝 기준 **약 7%**. (그 3,080 은 한산할 때 잰 값이다. 이 절의 마지막 측정
동안 프로덕션 부하가 올라 절대값이 2.2 배로 부풀었다 — **그래서 절대값은 쓰지 않고 A/B 만 쓴다.**)

**남은 자리는 변하지 않았다**: 블록의 30 개 W4 GEMM(바닥의 1.6배)과 어휘 헤드 GEMM(대역폭 바닥). 나머지는
전부 런치 바닥에 붙었다.

**곁다리 둘.** (1) `engine/kernels/SOURCES.json` 의 `mla/glm53_megakernel.cu` 기록이 낡아 **main 의 체크가
붉었다** — #732(k6)가 그 파일의 **주석만**(MAX_TOK 설명의 SPEC_K 5→6) 고치고 해시를 안 올렸다. 코드도
산술도 안 바뀌었으므로 기록만 따라잡게 했다. (2) 그 #732 로 **`SPEC_K` 가 6 이 됐다.** 프로브가 5 를
박아두고 있었으니 프로필의 `SPEC_K` 를 읽게 고쳤다 — k 는 프로필이 정하지 프로브가 정하지 않는다.

### 45차 §88 — W4 GEMM 은 바닥의 **1.25배**다: 드래프터에서 접을 것이 끝났다 (2026-09-12, srv4, GPU 공유, 무중단)

§86·§87 이 "블록의 830 µs 는 30 개 W4 GEMM 이고 바닥이 520 µs, 1.6배 여유" 라고 적었다. **바이트를 잘못
셌다.** 팩의 실제 바이트(`data` + `scale`)로 다시 재니:

| 가중치 (레이어당 1, 5 레이어) | µs | 바닥 | |
|---|---:|---:|---:|
| `self_attn.qkv` | 61.4 | 64.8 | **0.95** |
| `self_attn.o_proj` | 55.3 | 43.2 | 1.28 |
| `mlp.gate_up` | 333.3 | 259.3 | 1.29 |
| `mlp.down_proj` | 163.4 | 129.6 | 1.26 |
| `attention_conv.kernel_projection` | 57.4 | 43.2 | 1.33 |
| `mlp_conv.kernel_projection` | 57.4 | 43.2 | 1.33 |
| **서른 개 전부** | **728.1** | **583.3** | **1.25** |

`qkv` 는 **바닥보다 빠르다**(0.95) — 273 GB/s 는 LPDDR 수치이고 L2 가 거들기 때문이다. **여유가 1.25배면
거기엔 남은 게 없다.** 드래프터 블록의 지배항은 이제 하드웨어 한계에 붙어 있다.

**그래서 이번엔 구조만 접었다.**

**1. 어텐션의 행이 그리드로 올라갔다.** `_attn_rows` 가 `draft_attention` 을 **행마다** 부르고 `torch.cat`
으로 붙이고 있었다 — `write_draft_kv_rows` 가 고친 것과 같은 병이다. 행은 서로 독립이다(각자 자기 슬롯의
링을, 자기 컨텍스트 길이에서 읽는다). 그래서 그리드 차원으로 올렸고 붙일 것도 사라졌다.

| | 행별 | 배치 | |
|---|---:|---:|---|
| 레이어 하나, n=4 | 71.7 µs | 47.2 µs | **1.52배** |
| 레이어 하나, n=1 | 20.5 µs | 19.6 µs | 1.05배 |

n=1 인 지금은 거의 안 보이지만 **n=4 에서 1.5배**이고, 무엇보다 런치 수가 행에 비례하지 않게 됐다.
n=1 은 **비트 동일**; n=2·4 는 창 분할 수가 달라져(`SMS // (n * hk)`) 마지막 비트가 움직인다 —
28,672 원소 중 **10 개**, 최대 3.02e-4(bf16 한 스텝의 1/13).

**2. 잔차 덧셈과 그걸 읽는 norm 이 한 런치가 됐다.** 블록은 레이어마다 두 번 `res = res + x` 를 쓰고
곧바로 `res` 를 정규화한다. 나눠 놓으면 같은 행을 두 번 훑고, 그 중 하나는 다른 하나에게 입력을 건네려고
존재한다. **비트 동일**, 열 번의 조인 35.9 → 26.5 µs (**1.35배**).

**지금 드래프터 스텝** (SPEC_K=6, 한산하지 않은 GPU): observe 322, propose 2,639(블록 1,110 / 헤드·선택기
1,529), **합 2,961 µs**. 블록 구성: W4 GEMM 823, 탭 믹스 90, 어텐션 87, norm 43, swiglu 28.

**남은 것은 둘뿐이고 둘 다 대역폭 바닥이다** — 블록의 W4 GEMM(1.25배)과 어휘 헤드 GEMM. 나머지는
전부 런치 바닥에 붙었다. **드래프터에서 융합으로 가져올 것은 여기서 끝난다.**

### 45차 §89 — 다음 파트로 넘어가니 **자가검사 셋이 썩어 있었다**, 그리고 KDA 링은 이미 바닥이다 (2026-09-12, srv4, GPU 공유, 무중단)

드래프터가 끝났으니 다음은 타깃 forward(스텝의 63.5%)다. **먼저 부딪힌 벽**: 이 호스트에는
`served()` 레인이 쓰는 패키지가 없다(flashinfer·tilelang·cutlass·deep_gemm). 즉 **타깃의 프로덕션 경로는
여기서 잴 수도 돌릴 수도 없다.** 잴 수 있는 것만 했다.

**1. KDA 링 레인은 이미 대역폭 바닥이다.** `net._kda` 는 세그먼트마다 파이썬 루프를 돌며
`conv_ring` + `kda_recurrent_ring` + `core[sl] = o[0]` 를 한다 — 드래프터에서 세 번 고친 바로 그 병이라
같은 처방을 하려 했다. 그런데 프로덕션 랭크 형상(34 KDA 레이어, Hl=16, D=128, R=9/WR=7)으로 재보니:

| | 링 레인 | 세그먼트 수 |
|---|---:|---:|
| n=1, t=7 | 816.2 µs | 34 |
| n=4, t=7 | 7,784.1 µs | 136 |

**순환 상태가 (레이어, 세그먼트)당 7.3 MiB** 다([R=7, H=16, K=128, V=128] fp32). n=1 이면 34 × 7.3 =
250 MiB, 273 GB/s 로 **915 µs** — 측정 816 µs 는 **바닥의 0.9배**(L2). **여기엔 접을 것이 없다.**
n=4 는 1 GiB / 3.6 ms 바닥에 7.8 ms 이므로 2.2배 여유가 있지만, 지금 프로덕션은 n=1 이다.

**2. 자가검사 셋이 썩어 있었다.** 엔진의 36 개 모듈이 `_selfcheck()` 를 갖고 있는데 전부
`if __name__ == "__main__"` 아래에서만 불린다 — **아무 테스트도 돌리지 않는다.** 전부 돌려봤더니:

- **`glm53.net`**: `(net.conv_ring, net.rec_ring) == (8, 6)` 에 박혀 있었다. #732 이 SPEC_K 을 6 으로
  올려 실제 값은 (9, 7) 이다. **실패 중이었다.**
- **`glm53.drafter`**: `F.k == 5` 에 박혀 있었다. 같은 이유로 **실패 중이었다.**
- **`modules.rotary`**: 씨앗 없는 `torch.randn` 을 뽑아 **상쇄로 0.047 까지 줄어든 내적**을 절대 허용오차
  1e-4 로 비교한다. 뽑기에 따라 실패하는 **불안정 검사**였다(실제로 실패를 봤다).

고친 방식은 값이 아니라 **불변식**이다. `net` 은 이제 링 폭 숫자가 아니라 **그 폭이 닿아야 하는 레인**을
단언한다: `_kda` 는 `s.length <= min(8, conv_ring)` 일 때만 다이렉트 conv 링을 타는데 디코드 블록은
`spec_k + 1` 이므로, **spec_k 가 7 을 넘으면 매 디코드 스텝이 조용히 프리필 컨볼루션으로 떨어진다** —
이 플릿이 아는 그 실패("무장했는데 안 돌아감"). 이제 그걸 말로 적힌 단언이 잡는다.

`tests/test_engine_selfchecks.py` 가 **모든 모듈의 자가검사를 돌린다.** 체크포인트나 이미지 패키지가
필요한 다섯, GPU 가 필요한 일곱은 **이름과 이유를 적어** 제외한다 — 그 목록이 요점이다: 어느 믿음이
이 상자에서 검사되지 않는지를 말해 주지, 전부를 안 돌리는 것으로 덮지 않는다. 그리고 제외 목록에
**자가검사가 없는 모듈이 남아 있으면** 그것도 실패다.

### 45차 §90 — 창을 예약했고, 그동안 **창에서 쓸 자를 만들었다** (2026-09-12, srv4→srv2, 프로덕션 유지)

§89 가 "타깃 forward 를 건드리려면 플릿 창이 필요하다" 로 끝났다. 예약했다.

**리스는 살아 있었다.** `/home/choiceoh/glm53-logs/st-fleet.lock`(헤드 srv2)을 읽으니
`claude@srv4/main-ff728f43` 가 3 분 전에 잡았고 `st-glm53` 이 2 분째 떠 있었다(est 45 분). **뺏지 않았다** —
`fleet_lease.request_yield` 로 이유를 적어 양보를 요청했다. 그게 이 프로토콜이 큐 혼자서는 못 하는 절반이고,
"죽여라" 가 아니라 "받기를 멈추고, 가진 것을 끝내고, 살아남게 파킹하고, 놓아라" 이다.

**그리고 창에서 무엇으로 잴지가 문제였다.** 디코드 스텝은 캡처된 그래프를 재생한다. 그 안은
**CUDA 이벤트로 못 잰다** — 확인했다:

    g.capture_begin(); s.record(); ...        -> CUDA error: invalid argument (cudaErrorInvalidValue)

`Event.record` 는 캡처 중에 못 쓴다. 그래서 `forward` 안에 마크를 넣어도 재생이 아니라 **캡처**를 재게 된다.
단계 계량기(`base/stage_clock`)가 재생을 통째로만 감쌀 수 있는 이유가 이거다. 그리고 이미지에는 `nsys`/`ncu`
가 없다(호스트에는 있다).

**재생을 꿰뚫어 보는 것은 CUPTI 다.** torch 프로파일러는 재생이 돌린 커널을 보고한다. 그래서
**`/v1/engine/profile`** 을 만들었다 — `POST {"steps": n}` 이면 컨트롤 큐를 타고 루프 스레드에서 다음 n 개
**디코드** 스텝을 프로파일하고, `GET` 이 커널 표를 장치 시간 순으로 돌려준다. 프리필은 안 받는다: 청크 하나가
수 초어치 커널이고, 설명이 안 되는 것은 디코드 스텝이다. 한 번에 한 런, 32 스텝 상한.

이건 창을 **코드 쓰는 데가 아니라 수를 읽는 데** 쓰기 위한 준비다. 창이 열리면 이 표가
"forward 47.60 ms 는 무엇으로 되어 있나" 에 바로 답한다 — 드래프터에서 부분별 캡처 재생이 했던 역할을,
그래프 안을 못 여는 타깃에서는 CUPTI 가 한다.

**정정: 양보 요청은 과했다.** 운영자가 짚었다 — 홀더는 45 분짜리 정당한 작업 중이고 나는 급하지 않은데
(요청 사유에 "No hurry" 라고 적어 놓고 양보를 부른 것 자체가 모순이다) 창을 끊을 이유가 없다.
`request_yield` 를 거뒀고(`clear-yield`, 리스는 다시 깨끗하다) **큐로 갔다.**

리스 모듈이 직접 그렇게 말한다: *"It is deliberately not a queue ... already exist in bench/fleet.sh ...
The queue takes the lease when it grants."* 그리고 `bench/fleet.sh` 는 **`st-*` 컨테이너가 떠 있으면
grant 를 거부**하고 `start-st-glm53.sh` 는 **큐에 홀더가 있으면 거부**한다 — 두 기구가 서로를 본다.
즉 **큐에 거는 것이 곧 대기예약**이고, 홀더를 건드리지 않는다.

**그리고 큐가 이미 받아주는 자리가 비어 있었다.** `bench/fleet_onepass.ST_PROBES` 는 다섯 개의 ST 검사를
이름으로 허용하는데(그 밖의 경로는 거부한다) 그 중 **`probes/engine_graph_profile.py` 는 파일이 없었다.**
큐가 받고 나서 못 돌릴 이름이다 — §89 의 썩은 자가검사와 같은 종류의 구멍이다. 그 자리를 채웠다:
`boot.build` 로 **한 랭크에 실물 가중치**를 올리고(콜렉티브는 항등이라 재는 것은 이 랭크의 산술이다)
프로덕션이 서빙하는 그 스텝을 캡처해 재생을 프로파일한다. 레인별(mHC / MLA·DSA / KDA / MoE / dense GEMM /
norm / collective)로 **스텝당 µs 와 스텝당 런치 수**를 찍는다 — 바이트냐 런치냐가 그 두 열에서 갈린다.
분류되지 않은 커널은 버킷에 숨기지 않고 제 이름으로 남는다.

`tests/test_engine_graph_profile.py` 가 **허용 목록의 모든 이름에 파일이 있는지**를 박는다.

### 45차 §91 — 대기예약을 걸었다, 그리고 **큐가 하지 않은 부탁을 했다고 적고 있었다** (2026-09-12, srv4→srv2, 프로덕션 유지)

§90 에서 양보를 요청한 게 과했다. 운영자가 짚은 대로 **대기예약이면 충분하다.** 요청을 거두고
(`clear-yield`, 리스는 다시 깨끗) 큐로 갔다.

**먼저 큐가 거짓말을 하고 있었다 — 오래된 체크아웃 때문에.** srv2 의 `~/stkernel` 이 origin/main 보다
**639 커밋 뒤**(PR #511)라 `probes/run_engine_probe.sh` 조차 없었고, 그래서 티켓이
"custom GPU scripts and standalone checks are disabled" 로 거부됐다. 같은 트리로 `fleet.sh status` 는
**`fleet: FREE`** 라고 답했다 — 네 노드가 서빙 중인데. (큐 도움말이 그 함정을 이미 적어 뒀다:
*"a tree from before the ST-engine check reported an empty fleet with four nodes serving"*.)
돌고 있는 부팅은 `~/stkernel` 이 아니라 릴리스 스냅샷 `/home/choiceoh/st-releases/main-ff728f43` 를
`/repo` 로 물고 있으므로 트리를 올려도 홀더는 안 건드린다. 기존 브랜치
`codex/fleet-onepass-live-20260909`(5e0216cf)를 남긴 채 main 으로 옮겼다. 이제 status 가 리스와 네 노드를
다 읽고 **`TAKEN by the ST engine, outside this queue`** 라고 답한다. 티켓 `st-fwd-lanes` 가 대기 중이다.

**그리고 이 시스템에서 "대기예약" 은 곧 "양보 요청" 이다.** `bench/fleet.sh` 가 그렇게 설계돼 있다:
*"Refusing is not enough: a queued session would then wait for a human to go and ask ... so the queue asks on
the waiter's behalf -- once per refusal."* 내가 손으로 한 것은 큐가 자동으로 하는 일과 같은 것이었다.

**그런데 그 부탁이 조용히 안 나간다.** `st_engine_yield` 는 `$FLEET_RUNNER_REPO/launchers/lib/fleet-lease.sh`
를 찾는데, 러너는 **`bench/`, `engine/`, `probes/` 만 담긴 스냅샷**에서 실행되고 **`launchers/` 는 거기 없다.**
그래서 함수가 첫 줄에서 `return 0` 하고, 호출부는 성공으로 치고, 로그에는
`asking it to yield to st-fwd-lanes` 가 남는다 — **아무도 부탁받지 않았는데.** 리스를 읽어 확인했다:
`asked: no`. 대기자는 "말해 뒀다" 고 믿고 홀더는 아무것도 못 듣는다.

**동작은 그대로 두고(운영자 판단: 조용히 기다린다) 말만 고쳤다.** `st_engine_yield` 가 이제 **부탁했을 때만
0** 을 돌려주고(`|| true` 를 뺐다 — 상태를 삼키는 것이 거짓말이 들어온 경로다), 호출부가 둘 중 무엇이
일어났는지 적는다:

    hold refused: ... ; asking it to yield to $s
    hold refused: ... ; $s waits, and the holder was NOT asked -- this runner has no launchers/lib/fleet-lease.sh

`tests/test_engine_fleet_lease.py` 가 두 분기와 `|| true` 의 부재를 박는다. `FLEET_AUDIT` 핀도 같이 갱신했다
(하나만 낡아도 모두의 CPU 재사용이 꺼진다).

**남은 판단**: 러너 스냅샷에 `launchers/` 를 넣으면 설계대로 돌아오지만, 그때부터 큐는 티켓이 설 때마다
홀더를 끊는다. 지금은 끊지 않는 쪽을 택했다.

### 45차 §93 — D17 을 처음 집행했다: 오늘 성능 PR 넷에서 잰 이득이 없고, 디코드는 낮게 나왔다 (2026-09-12 22:21~22:27, 네 노드, 같은 부팅 두 판)

헌장 D17(PR #742)이 요구하는 형태 그대로 쟀다 — **같은 부팅에서 두 판**, 레코드에 `engine_shape`,
접두사 캐시를 맞은 브래킷은 비교에서 제외.

| | `e0451b0d` 1/2회차 | `ff728f43` 1/2회차 |
|---|---|---|
| **디코드 step/s** | **12.95 / 12.96** | **12.0 / 12.0** |
| tokens/step | 3.550 / 3.198 | 3.534 / 3.329 |
| 디코드 tok/s | 46.0 / 41.4 | 42.4 / 39.9 |
| 웜 프리필 2K | 3,295 / 3,084 | 3,020 / 3,084 |
| 2K 콜드 TTFT | 21.83s / 16.10s | **6.73s / 5.28s** |
| 32K 콜드 | 46.43s / 19.86s | 53.24s / ⚠️ 캐시 히트 |
| 품질 | 9/9 | 9/9, 깨진 응답 0/5 |

그 사이에 들어간 것: `#734`(프리필 청크 6,912→9,216) · `#735`(드래프터 셀렉터·5회 런치 fc 접기)
· `#736`(스텝 행이 어텐션 루프를 벗어남, residual) · `#737`(prefill32 상한을 청크 법칙에 맞춤).

**디코드는 낮아졌다.** `#735`·`#736` 은 둘 다 스텝에서 런치를 걷어낸다고 했는데 **12.95 → 12.0 step/s** 다.
각 부팅 안의 재현은 단단하지만(12.95/12.96, 12.0/12.0) **부팅이 다르고** IQR 이 `[11.9,14.0]` 대
`[10.9,13.0]` 로 겹친다 — **7% 를 회귀로 단정하지 않는다. 단정할 수 있는 것은 "이득이 안 보인다" 이다.**

**2K 콜드는 진짜로 좋아졌다**: 21.83s → **6.73s**. 오늘 낮 "컴파일로 설명 안 되던 16초 꼬리" 가 줄었다.
`#737` 이 유력하다.

**32K 는 못 가른다.** 1회차 53.24s(이전 46.43s)인데 **2회차가 캐시를 맞았다** — 32K 5.13s · 128K 5.22s 로
둘이 같은 값인 것이 증거다. `#734` 가 청크를 바꿔 32K 프리필의 타일 형상이 전부 새것이므로 1회차가 그
브래킷에서 새 커널을 처음 컴파일했을 수 있다. **캐시를 비우고(`POST /v1/prefix/reset`) 다시 재야 한다.**

**더 큰 그림**: 12.0 이든 12.95 든 옛 스택 기록 **19.87~21.83 step/s** 와 **40% 차이**다. `tokens/step` 은
양쪽 다 3.2~3.6 이라 **드래프터가 아니라 스텝 자체**다(50 ms 대 77~83 ms).

**D17 의 두 판은 컴파일 꼬리를 걸러 주지 그 위의 부팅 간 변동은 못 걸러 준다.** 7% 를 가르려면 같은 창에서
`e0451b0d` ↔ `ff728f43` 를 번갈아 띄워 각각 두 판(부팅 4회)을 재야 한다. 이것이 다음 창의 첫 일이다.

**부수 확인 — §81 의 첫 프로덕션 데이터점**: 원패스 밖에서 들어온 요청 하나가
`st:reasoning_shape_total{thinking="on",effort="high"}` 로 찍혔다(원패스 자신의 10건은 전부 `effort="absent"`).
웜홀이 올린 턴 = **§81 의 둘째 줄**이고, 그 창의 `st:reuse_path_total{continuation}` 은 **0** 이었다.
한 건은 판정이 아니지만, **계열이 실제로 그 질문에 답한다는 것**은 확인됐다.

**부팅 일곱 번 중 다섯 번이 서로 다른 원인으로 죽었다**(#705 교정 캡처 · #721/#727 `draft_ceilings` 형상과
게이지 격리 · #733 랭크 1~3 임대 게이트 · #738 인계 rmtree). 남은 하나는 `one-shot proxy stopped progressing`
(랭크 3, `capture_decode`) 으로 19:02 와 22:07 두 번 나왔고 **재시도로 넘어갔다** — 플래키이고, 재현 조건이
아직 없다.

### 45차 §92 — 테스트 부팅 모드: 플릿을 안 잡고 **프로덕션 레인**을 돌린다 (2026-09-12, srv4, 프로덕션 유지)

이번 세션 내내 같은 벽에 부딪혔다. §89 는 "타깃 forward 를 건드리려면 창이 필요하다" 였고, §90~§91 은
그 창을 얻으려고 리스와 큐를 오갔다. **왜 창이 필요한가** 를 보면 이유가 하나다:

`--local` 은 네 랭크를 네 스레드로 한 박스에서 돌리지만 **`lane_tables.reference()`** 밖에 못 쓴다 —
torch 산술이다. 배관은 증명하지만 **프로덕션이 돌리는 커널은 한 번도 안 돈다.** 그래서 커널에 대한
모든 판정이 플릿 전체를 요구했고, 플릿은 다운타임이다.

**`--test` 는 `--local` 을 프로덕션 레인 위에서 돌린다.** `--lanes {reference,served}` 를 열었고
`--test` 는 `--local --lanes served` 다. 네 랭크는 네 스레드이고 모든 콜렉티브는 로컬이며,
**리스를 잡지 않는다** — 로그가 그렇게 말한다("This is not the fleet and it holds no lease").

**그리고 프로덕션을 죽이지 않는 것이 이 모드의 전제다.** GB10 은 호스트와 장치가 한 풀이고 earlyoom 의
바닥은 절대값이며 엔진은 우선 사살 대상이다 — 2026-09-11 에 프로덕션 옆 스모크 테스트가 자기가 아니라
**플릿의 워커를 죽였다**([[feedback-no-gpu-smoke-beside-production]]). 그래서 `--test` 는 부팅 전에
MemAvailable 을 읽고, 선언한 KV 를 뺀 나머지가 바닥(16 GiB) 아래면 **추측하지 않고 거부한다**(D3):

    --test refuses: 12.3 GiB would be left after this boot's 1.0 GiB of KV and the floor is 16.0.
    Narrow --layers, lower --kv-gib, or wait for the box. A test boot that earlyooms production
    has not tested anything.

통과하면 두 수를 찍는다 — 남는 용량과 바닥. 레버는 `--layers` 와 `--kv-gib` 이고, 메시지가 그걸 말한다.

**이걸로 열리는 것**: 커널 변경(§82~§88 이 A/B 로만 주장하고 플릿 판정을 미뤄둔 것들, 그리고 새 D17 이
"플릿이 재기 전엔 안 끝났다" 고 못박은 것들)을 **창 없이 실물 레인으로** 돌려볼 수 있다. 창은
프로덕션 규모 수치를 위한 것으로 남고, 옳고 그름은 여기서 가려진다.

`tests/test_engine_test_boot.py` 가 박는 것: 거부가 **얼마나 모자라는지와 레버를 말하는지**, 바닥이 0 이
아닌지(0 은 바닥이 아니다 — earlyoom 은 MemAvailable 이 거기 닿기 전에 죽인다), `--test` 가 두 플래그를
함의하는지, 그리고 reference 레인은 지키지 않고 served 레인만 지키는지(전자는 지킬 가중치가 없다).

**그리고 §93 이 이 모드가 왜 지금 필요한지를 말해 준다.** 다른 세션이 D17 을 집행해 오늘 성능 PR 넷을
같은 부팅 두 판으로 쟀고, 그 중 둘(`#735`·`#736`)이 내 것이다. 판정은 **"이득이 안 보인다"** 이고
디코드가 12.95 → 12.0 step/s 로 **낮게** 나왔다(부팅이 달라 7% 를 회귀로 단정하지는 않는다).

내가 §82~§88 에서 주장한 것은 전부 **한 프로세스 인터리브 A/B** 였다 — 그 안에서는 옳지만, 그것이
스텝으로 나타난다는 증거는 아니었다. D17 이 말하는 게 정확히 그것이고, 이번 판정이 그걸 보여 준다.

§93 이 더 중요한 것도 짚는다: 12.0 이든 12.95 든 옛 스택의 19.87~21.83 step/s 와 **40% 차이**이고,
`tokens/step` 은 양쪽 다 3.2~3.6 이라 **드래프터가 아니라 스텝 자체**다(50 ms 대 77~83 ms). 나는 이번
세션을 드래프터(스텝의 36.4%)를 접는 데 썼고, 남은 항이 전부 대역폭 바닥이라는 것까지 쟀다(§88).
**그러면 남는 곳은 forward 다** — 그리고 forward 를 창 없이 실물 레인으로 열어 주는 것이 바로 `--test` 다.

**번호 정정.** 위 D17 집행 기록이 §82 로 붙었는데 그 번호는 이미 「observe 의 16 ms 는 투영이 아니었다」가
쓰고 있었고, §83·§84·§86 과 `probes/drafter_step_replay.py` 가 그걸 참조한다. 뒤에 온 쪽을 **§93** 으로
옮겼다. (같은 일이 §81 에서도 있었다 — §85 참조.)

### 45차 §94 — 테스트 부팅은 **경로가 프로덕션과 같아야** 한다, 그리고 본체는 계측창이다 (2026-09-12, srv4, 프로덕션 유지)

§92 는 `--test` 를 "프로덕션을 안 죽이는 로컬 부팅" 으로 적었다. 운영자가 두 번 정정했다.

**첫째, 그건 곁가지다.** 테스트 부팅의 본체는 (1) 계측창이 넓고 (2) 안 쓰는 기능이 꺼지고
(3) 부팅·컴파일이 빠른 것이다.

**둘째 — 그리고 이게 중요하다 — 이거로 돌면 안 된다.** 나는 처음에 "`--local` 은 캡처를 안 하니
`Event.record` 가 되고, 그러면 `forward` 안을 레인별로 잴 수 있다" 로 갔다. **틀렸다.**
이거 디코드는 **다른 커널을 다른 순서로** 돌린다. 그걸 재면 프로덕션이 아닌 엔진을 재는 것이고,
프리필·디코드 속도가 애초에 달라진다. 넣었던 `forward` 안 마크는 되돌렸다.

**그래서 `--test` 는 경로를 지키고 나머지를 버린다:**

| | |
|---|---|
| **같게** | served 레인, 그리고 **디코드는 캡처된 그래프를 재생한다** — 프로덕션이 하는 그대로 |
| **끄기** | 플릿 부팅이 문 열기 전에 통과시켜야 하는 것들: 비전 타워 자격, 문법 자격, 파킹 티어, 교정 합산 |
| **빠르게** | 캡처 폭을 **하나만**. 캡처는 87 초 부팅의 3/4 이고 **폭마다 든다** |
| **열기** | 1/64 가 아니라 **매 스텝** 계측 |

**그리고 이 모드는 속도 측정이 아니다.** 네 랭크가 **한 GPU 위의 네 스레드**라 장치가 네 랭크어치 산술을
한다. **경로는 프로덕션의 것이고 밀리초는 이 박스의 것**이다 — 찍는 표가 그렇게 말한다
(`this box's time, not the fleet's`). D17 은 그대로다: 속도를 주장하면 플릿이 재야 끝난다.

**그러면 §93 의 질문은 어디서 답하나.** 옛 스택과 40% 차이는 드래프터가 아니라 스텝 자체다.
`forward` 안을 보려면 **CUPTI 밖에 없다** — `/v1/engine/profile`(§90) 과
`probes/engine_graph_profile.py`(큐에 걸어 둔 티켓) 가 그 일을 한다. 테스트 부팅은 그 둘을 **창 없이**
돌릴 수 있게 해 주는 자리이지, 그 자체가 자는 아니다.

### 45차 §95 — 큐가 허용한 검사 여섯 중 셋은 **큐가 시작은 하되 돌릴 수 없었다** (2026-09-12, srv4, 프로덕션 유지)

§90 의 티켓(`st-fwd-lanes`)이 창을 기다리는 동안 **한 번도 실행된 적 없는 프로브가 어떻게 불리는지**를
확인했다. 창을 버릴 뻔했다.

`probes/run_engine_probe.sh` 는 프로브 경로 뒤의 `"$@"` 를 그대로 컨테이너에 넘긴다. 그리고 큐의 정석
호출은 **인자가 없다**(`fleet.sh` 헤더의 예시부터 그렇다). 그런데 `bench/fleet_onepass.ST_FLAGS` 가
허용하는 플래그는 한 줌뿐이고 — `--drafter-dir` 과 `--tier-dir` 은 **거기 없다** — 그러면
그걸 `required` 로 받는 검사는 **큐가 시작은 해도 첫 줄에서 죽는다.**

여섯 중 셋이 그랬다:

| 허용된 검사 | 요구하던 것 |
|---|---|
| `engine_decode_graph_check.py` | `--ranks` |
| `engine_drafter_graph_check.py` | `--ranks`, `--ckpt-meta`, **`--drafter-dir`** |
| `engine_full_check.py` | `--ranks`, `--ckpt-meta`, **`--drafter-dir`**, **`--tier-dir`** |

굵은 둘은 `ST_FLAGS` 에 없으니 **어떤 호출로도 못 넘긴다** — 그 두 검사는 큐를 통해서는 영영 못 돈다.
#741 에서 `engine_graph_profile.py` 가 **이름만 있고 파일이 없던 것**과 같은 종류다: 허용 목록이
돌릴 수 없는 것을 약속하고 있었다.

전부 노드에서 되는 기본값을 줬다 — `facts.RANKS` · `facts.CKPT` · `drafter_mod.DRAFTER`, 그리고
full check 의 티어는 **실행마다 새 임시 디렉터리**(프로덕션 티어를 가리키지 않는다). 내 프로브도
`--ranks` 를 요구하고 있었다.

`tests/test_engine_graph_profile.py` 가 이제 **허용된 검사 중 어느 것도 인자를 요구하지 않는다**는 것을
박는다. 이름에 파일이 있는지(§90)와 함께, 큐가 약속한 것이 실제로 돌 수 있는지를 지킨다.

**부수**: 티켓은 `queued checkout revision changed` 로 멈춰 있었다 — 내가 큐에 건 뒤 `~/stkernel` 이
움직였고(그 사이 PR 셋이 머지됐다) 큐가 **다른 것을 돌리지 않으려고** 멈춘 것이다. 옳은 동작이다.
`edit` 로 같은 명령을 현재 체크아웃에 재제출하고 `resume` 했다(대기 시간은 유지된다).

### 45차 §96 — 관측 겹치기를 준비했다: observe 는 commit 전에 계산을 시작할 수 있다 (2026-09-12, srv4, 프로덕션 유지)

§93 이 "40% 차이는 드래프터가 아니라 스텝 자체" 라고 했다. 스텝 구조를 코드로 보니 디코드 스텝은 한
스트림에서 **forward → sample/verify → commit → observe → propose 를 직렬로** 낸다(스테이지 합 74.91 ms
= 스텝 74.99 ms; `depth=2` 파이프라인은 호스트만 가린다). 그리고 그건 진짜 사슬이다 — 타깃이 드래프터의
드래프트를 검증하므로 드래프터 27.3 ms(36.4%)가 임계경로에 얹힌다.

**한 군데가 겹칠 수 있다. 그걸 준비했다.** observe(관측)를 뜯어 보니 두 부분이다:

- **계산부**: `c = norm(fc·aux)` → `context_kv` → 레이어별 `norm_rope`. `aux` 와 `positions` 만 읽는다.
  그리고 `positions = ctx_before + arange` 인데, `decode_commit.advance` 가 돌려주는 `ctx_before` 는
  **스텝 진입 시점의 컨텍스트**다 — forward 전에 이미 아는 값이다(코드로 확인). 그래서 계산부는 `aux`
  (타깃 레이어 41/45 에서 준비됨) 만 있으면 된다.
- **쓰기부**: `write_draft_kv_rows(..., valid=valid)`. `valid`(=commit 이 수락한 드래프트 수) 만 이게 필요.

즉 **observe 의 계산은 forward 의 꼬리(레이어 42~44 + head) + sample + verify + commit 과 겹칠 수 있고,
쓰기만 commit 뒤로** 가면 된다. `valid` 는 어느 위치를 링에 쓸지 마스크할 뿐, 계산하는 K/V 값 자체엔
영향이 없다(교정 팩 마스크는 서빙 부팅에서 no-op).

**준비물(이 박스에서 검증됨):** `observe_rows` 의 fast 경로를 `observe_kv`(계산) + `observe_write`(쓰기)로
쪼갰다. `tests/test_engine_drafter.py::ObserveOverlapTests` 가 둘을 박는다:
(1) 분리가 **바이트 단위로** 단일 호출과 같다, (2) `observe_kv` 의 K/V 는 **`valid` 를 바꿔도 동일**하다 —
겹치기가 옳기 위한 전제.

**정직한 한계 — 크기는 창의 몫이다.** (1) 실제 겹치기는 캡처된 그래프 둘을 두 스트림에 얹고 레이어 41 의
aux 이벤트로 묶는 배선이고, 그건 served 레인(4랭크)이라 창이 있어야 짓고 잰다. (2) 이득의 상한은
`min(observe 계산, forward 꼬리 + head)` 인데, §84 가 프로덕션 observe 16 ms 를 이 박스에서 재현하지
못했으므로(하니스 0.66 ms) **계산부가 그 16 ms 중 얼마인지 모른다.** 큐에 걸린 `st-fwd-lanes` 프로파일이
forward 꼬리와 head 의 크기를, 그리고 다음 부팅의 `stage="observe"` 가 계산/쓰기 비중을 준다.
**이번 변경은 겹치기를 주장하지 않는다 — 전제가 성립함을 증명하고 그 위에 지을 바닥을 놓았을 뿐이다.**

### 45차 §97 — 서빙 중에 멎은 플릿: 같은 집합통신에 네 랭크가 서로 다른 형상으로 들어갔다 (2026-09-12 22:31, 프로덕션, 진단만)

`ff728f43` 를 띄워 원패스를 돌리던 중 **문이 멎었다**: `running []`, `waiting [0]`, `steps` 가 16분째
2298→2299 고정, 네 컨테이너는 전부 살아 있고 **GPU 는 넷 다 0%**. 요청이 대기열에 들어간 채 스텝이 안 돌았다.

**네 랭크의 스택이 전부 같은 자리였다:**

```
decode(adapter.py:1081) → _gather(:843) → comm.all_gather(comm.py:136) → all_gather_into_tensor
```

**그리고 NCCL watchdog 덤프에 원인이 있었다:**

| 노드 | OpType | NumelIn | 행 |
|---|---|---:|---:|
| srv2 (rank0) | `_ALLGATHER_BASE` | 271,040 | **7** |
| srv1 (rank1) | `_ALLGATHER_BASE` | 271,040 | **7** |
| srv3 (rank2) | `_ALLGATHER_BASE` | 232,320 | **6** |
| srv4 (rank3) | `_ALLGATHER_BASE` | 232,320 | **6** |

vp 38,720 으로 나누면 **일곱 행 대 여섯 행**이다. `all_gather_into_tensor` 은 모든 랭크가 같은 형상이어야
하는데 **그걸 검사하지 않는다 — 기다린다.** 그래서 영원히 멎는다.

**`_gather` 는 디코드 스텝에서 크기가 데이터에 의존하는 유일한 집합통신이다**: rich 행들로 만들고, 문법이
살아 있으면 각 행의 `masks.live` span 으로 만든다. 다른 스텝 형상은 구성상 합의돼 있고(D9) **이것만 계산된다.**

**고친 것**(PR #750, #752): 들어가기 전에 `all_reduce_host` 로 행 수를 투표하고, 어긋나면 **집합통신에 들어가지
않고** 랭크별 숫자를 달아 올린다. 나쁜 스텝에서만 시퀀스별로 한 번 더 투표해 **span 이 어긋났는지(문법)
드래프트가 어긋났는지(랭크별 생성기)** 까지 말한다.

**아직 모르는 것**: 왜 어긋났는가. 용의자는 둘 — `masks.live` 를 만든 문법 matcher, 또는 그 span 을 계산한
대상인 드래프트(`propose_sampled` 가 **랭크마다 생성기에서 뽑는다**). 2/2 로 갈린 것(0,1 대 2,3)이 노드 쌍과
겹치지만 **그건 정황이지 증거가 아니다.** 다음 발생이 한 줄로 답한다.

**증거가 사라지는 방식도 기록해 둔다**: 위의 모든 것은 `docker logs` 안에만 있었고, 다른 세션이 멎은 컨테이너를
치우면서 **함께 사라졌다**. 스텝 링은 0 바이트였다 — `DeathDump` 는 죽을 때 쓰는데 이 프로세스는 **살아서 멎어
있었다.** 살아서 멎은 엔진이 자기 상태를 덤프 디렉터리에 남기게 하는 것이 다음 일이다.

### 760차 — 4박스 없이 스텝을 잔다: 숙주 비용·관측·재계산의 세 경로 (2026-09-12, ost 노트북 macOS arm64·CPU, PR #760)

플릿을 못 잡은 날(큐 점유·엔진 리스·노트북) 스텝/레이턴시 숫자를 얻는 도구 셋을 만들었다. D17 은
그대로다 — 이 도구들은 판정 채널이 아니고, 도구 출력 스스로 그렇게 말한다. 규칙 6 을 이 항목에도
적용: 아래 숫자는 **맥 CPU 의 것이라 형상이 다르다**. 증거: `measurements/step_tools_nofleet_20260912/`.

**세 경로와 각각이 잰 것(아닌 것)**:

- `bench/step_sim.py` — 진짜 `Runner`/스케줄러/`Ring` 을 장치 없는 모델로. **스텝 루프의 숙주 비용**만
  잰다(커널이 없으니 서빙 속도와 무관). `--device-ms` 는 장치를 직렬 단일 서버 스레드로 놓아
  `decode_async` 의 겹침(depth)이 실제로 일어나게 한다.
- `bench/step_peek.py` — 살아있는 부팅의 `/metrics` **관측 전용**. 요청을 보내지 않고 큐/리스를
  건드리지 않는다. 남의 부팅을 보고 있으므로 독점성도 판정도 없다. `st:step_seconds`/TTFT
  히스토그램은 두 스크랩의 누적 차로 창 분포가 되고(버킷 내 보간), 없는 계열은 0 이 아니라 빈칸.
- `bench/step_replay.py` — 저장 증거의 **재계산**: `steps-*.ring`·onepass jsonl·bracket 다리·peek 샘플.
  onepass 기록 둘 이상이면 재부팅 없이 델타.

**이 맥에서 잰 숫자**:

- 숙주 비용(장치 0): decode med **0.039~0.044 ms/스텝**, prefill chunk med 0.042~0.05 ms.
  `StepMeta.build` med **6.3 µs/스텝** @128K(draft 3) — 커널 호출자의 flat array 비용.
  실측 세계(~20 step/s, 스텝 50 ms)에서 **숙주는 병목이 아니다**를 코드 수준으로 확인.
- 겹침(장치 50 ms 주입, depth 2): 측정 캐던스 **18.76 decode step/s**(이론 상한 20),
  숙주 여유 **99.9%**. ring 의 decode in-flight med **105.2 ms ≈ depth(2) × 50 ms** —
  async 스텝의 ring wall 은 launch→readback 이라 depth 대기가 포함된다는 해석 규칙의 실증.
- 재계산 검증: 플릿 증거 `st_onepass_20260912_0746` 를 노트북에서 다시 판 결과
  **10.96 step/s·수용률 46.2%·tokens/step 3.309** — 원본 증거와 동일한 숫자의 재생성.

**아직 없는 것**: peek 의 실물 관측값. 이 날 노트북에서 플릿 헤드(10.10.10.2)는 도달 불가였고,
네트워크 루프는 로컬 가짜 `/metrics` 로만 검증했다(스크랩당 20스텝 증가 → 39.7 step/s).
테스트 13 케이스(GPU·네트워크 없음) 통과, `tools/check.py` 1 ok.
