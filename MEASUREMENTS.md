# 실측 원장 (measurement ledger)

이 스택에서 내린 모든 성능 판정과 그 수치. **여기 없는 주장은 미실측이다.**
방법론·함정은 [README의 bench 표](README.md#bench--측정-도구와-함정) 참조.
기준 환경: 4× GB10(SM121) · TP=4 · CRS812.

**이 파일에는 지금 손대는 자리만 있다.** 원장은 두 파일이다 — 여기와
[MEASUREMENTS_ARCHIVE.md](MEASUREMENTS_ARCHIVE.md). 저쪽에는 **출하돼 더 바뀌지 않는** 것이 **고치지 않고 그대로**
있다: 물러난 vLLM 스택의 항목 113개(08-10~09-11)와, 45차 중 엔진의 건설(§2~§22)·문의 기능·한국어·도구·플릿(§24~§78).
여기 남은 것은 §23 의 판정 회차들과 §79~ 의 최근 성능 작업이다. 아래 "45차 이전" 요약과 절 색인이 **양쪽 모두**를
가리키고(`·보관` 표시), `28차 §8` 처럼 번호로 부르는 참조는 번호가 주소다 — 두 파일 중 어디에 있든 같은 이름으로 찾는다.
어느 레버가 지금 켜져 있는지는 [engine/SERVING_DEFAULTS.md](engine/SERVING_DEFAULTS.md), 디코드
스텝이 무슨 커널로 이루어지는지는 [STEP_KERNEL_MAP.md](STEP_KERNEL_MAP.md),
부팅이 필요한 다음 실험은 [RUNBOOK_KERNEL_CAMPAIGN2.md](RUNBOOK_KERNEL_CAMPAIGN2.md).

## 이 원장을 쓰는 규칙

아홉 줄이 이 파일의 판정 규율 전부다. 항목마다 되풀이하지 않는다.

1. **여기 없는 주장은 미실측이다.** 프로브·모델·추정은 원장 항목이 아니라 그 항목의 재료다.
2. **판정 채널은 C=1 step/s**(`bench/bench-dec.py`), 부팅 CV **1.7%**. 그보다 작은 기대값은
   단독 부팅으로 판정하지 않고 다른 팔에 얹는다.
3. **브래킷은 base→cand→base**, 노브 하나. **base 팔의 값은 명시**한다 — 프로필 기본값이
   움직인 뒤로는 "노브 없음"이 곧 stock 이 아니다(28차 §8).
4. **채택 게이트**: 품질 9/9 · 한국어 0/16 · pos-1 수용률 ±2 pct · 프리필 사다리 무회귀.
   수용률을 움직이는 변경은 tok/s 와 tokens/step 을 함께 적는다.
5. **무장 ≠ 서빙.** 노브가 1 이라는 사실은 커널이 돈다는 뜻이 아니다(22차·28차). 증거는
   셋 중 하나 — 부팅 로그의 증명 줄, 트레이스, 또는 거부가 스스로 말하는 로그.
6. **오프라인 프로브 숫자는 상한이거나 형상이 다르다.** 27차에서 상한 하나가 프로브 하나에
   절반 이하로 줄었다. 상한은 목표와 **같은 측정 형태**로 만든다.
7. **CUPTI 시간은 트레이스 사이에서 비교하지 않는다.** 개수가 정본이고 시간은 같은 트레이스
   안에서의 상대 비교로만 쓴다([STEP_KERNEL_MAP.md](STEP_KERNEL_MAP.md)).
8. **그리디 텍스트 diff 로는 판정하지 않는다** — 같은 구성의 두 부팅에서도 온도 0 응답이
   갈린다(28차 §8). 판정은 게이트와 브래킷이다.
9. **항목 번호는 PR 번호다**(2026-09-12). 절 번호를 파일 끝을 읽어서 정하는 규약은 세션이
   하나일 때만 성립한다 — 그날 하루에 세 줄의 작업이 몇 분 간격으로 머지되면서 **§30·§31·§32 가
   각각 두 번씩** 쓰였고, 그때부터 "§31 참조" 가 어느 쪽인지 알 수 없게 됐다. PR 번호는 GitHub 이
   유일하게 발급하니 조율이 필요 없고 상호 참조가 정확하다. 새 항목은
   `### <차> — <제목> (<날짜>, <노드>, PR #NNN)`, 참조는 `PR #NNN`. 이미 쓰인 §2~§40 은 그대로 두되,
   **겹친 여섯 개는 제목 끝에 PR 번호를 달아** 어느 쪽인지 읽을 수 있게 했다.

## 45차 이전 — 보관된 항목 (요약)

2026-08-10~09-11 의 항목 113개, 8685 줄은 [MEASUREMENTS_ARCHIVE.md](MEASUREMENTS_ARCHIVE.md) 로 옮겼다.
물러난 vLLM 스택의 기록이고 더 이상 바뀌지 않는다. **하나도 고치지 않았다** — 이 파일이 길어져 읽히지 않는 것이
기록을 잃는 것보다 나쁘기에 옮겼을 뿐이다. 제목이 곧 판정이라 아래 한 줄씩이 요약이다; 수치와 근거는 저 파일에 있다.

- GLM53 EP + 입력 준비: 67 tok/s 절대 목표 통과, 기본값 채택 (2026-09-10, PR #511)
- GLM53 EP K3: engine 회복, 출력 67 tok/s 미달 (2026-09-10, PR #511)
- GLM53 EP BF16 scatter: 품질 통과, 67 tok/s 목표 미달 (2026-09-10, PR #511)
- GLM53 EP tiled + SF6 원패스: 기본값 보류 (2026-09-09, PR #511)
- GLM53 TP 프리필 Q0 기본값 선택 (2026-09-09, PR #478)
- GLM53 AR–MHC consumer 결합 기본값 채택 (2026-09-08, PR #473)
- 찾아보기
- 기준선 — `dsv4` (슬리밍+인시던트픽스, 노브 전부 OFF)
- 기준선 — `glm53` (메가커널 세트 기본값, 2026-09-04 20:36 부팅)
- 커널 귀속 — 프리필의 완전 분해 (2026-08-09)
- 디코드의 실체 — 콘텐츠 바운드 (2026-08-09)
- 디코드 스텝 예산 — 커널 귀속 (2026-08-09, with_stack=false 캡처)
- 디코드 수용률 배터리 (2026-08-09, 동일 엔진·연속 5창 — 상대 비교 유효)
- 판정 원장
- ★★패브릭 200G 복구 — 프리필 +9.1% (2026-08-10, 채택)
- ★★UMA 페이지 마이그레이션 스톨 — 프리필 간헐 −15% 요동 근절 (2026-08-11, 채택)
- ★REFINE_PASS A/B — 수용률 레버 실측, 기각 (−38%, 수용률 반토막) (2026-08-12)
- ★MBU 효율 분석 — 포화≠효율, MoE 56%가 speculative 대역폭 (2026-08-12)
- 정밀 관측 — idle 뒤에 여지 없음, 4스트림 포화 확증 (2026-08-12)
- 미세 이익 라운드 — 실측 종결 (2026-08-11~12, "미세라도 여러개 모으면")
- 전 축 재확인 — 프리필·장문·attn/indexer 전수 점검 (2026-08-11, "다른 걸 더")
- 단일 스트림 극한 — 작은 GEMM 융합 여지 조사·이미 소진 판정 (2026-08-11)
- ★P2b — mhc small-M 타일 채택·tf32 prenorm 기각 (2026-08-11, "소소한 이익 묶음" 1호)
- 정찰 일괄 기각 — 잔여 후보 전수 소진 2회차 (2026-08-11)
- 소프트웨어 개선 경로 — 종결 선언 (2026-08-09)
- 남은 검증된 레버 (우선순위순, 2026-08-11 전면 갱신)
- 업스트림 비교체크 (2026-08-09, PR·이슈 ~25건 vs 이 스택)
- ★런북 정비 창 실행 (2026-08-11 심야, 운영자 위임)
- 인시던트 로그
- 방법론 교훈 (체인 운용)
- 인접 모델 브링업에서 역수입한 항목 (2026-08-29/30)
- GB10 메모리 예산 — 부팅 실패 세 얼굴이 한 원인 (2026-08-30, GLM-5.3 TP=4)
- 한국어 토큰 손상 — 용의자 8종 기각, 서명 확정 (2026-08-30, GLM-5.3 TP=4)
- 오버레이가 한 번도 실린 적 없었다 — 베이스 계약이 첫 실행에서 잡은 것 (2026-08-30)
- 토큰은 버려지지 않는다 — 계측기가 답한 것과, 계측기 자신의 결함 (2026-08-30)
- ★★GLM-5.3-Flash 2026-09-01 — 프리필 첫 실측, 그리고 축 셋이 닫혔다
- ★★GLM-5.3-Flash 스텝 재분해 — 62%가 줄일 수 없는 전문가 읽기 (2026-08-31)
- ★메가커널 2차 — 벤치 함정 넷, W4 −33%, MHC −22%, PDL, 그리고 deepgemm 과의 진짜 격차 (2026-09-02)
- ★★★39차 — 드래프터 축: DFlash 학습 레시피·DSpark 비교·HF 드래프터 15곳 조사, cfontes TR3-v3 브래킷 기각, 하니스 onepass 단일화 (2026-09-06 13:40~14:40 KST)
- ★★★38차 — b12x 정적 MoE 커널 2라운드: 남은 레버 셋의 실측 — 짝수 웨이브 기각, 분할 −1~2%, v4(256 B 세그먼트) −3.5% (2026-09-06 08:00~10:30 KST, srv2 프로브, 무부팅)
- ★★★38차 부록 — v4 `u` 원샷 벤치: 부팅 1회, 디코드 창 +2%, 프리필·품질·한국어 불변 (2026-09-06 12:20~12:48 KST, 운영자 지시 "원샷 벤치")
- ★★38차 §8 — 3라운드: A 링 상쇄, L2 프리페치 기각(요청률 병목의 증거), 프로덕션 `u` 검증 (2026-09-06 14:39~14:50 KST, 묶음 프로브 + 프로덕션 부팅)
- ★★38차 §9 — `w` 원패스 재검증(같은 프로덕션 구성)과 `b`(SF bulk 복사) 첫 실측 (2026-09-06 14:54~15:03 KST)
- ★★38차 §10 — 4라운드 교차 묶음: `b`(SF bulk 복사) 상쇄, "요청률 병목" 가설 기각 — v4 `u` 가 이 설계 계열의 끝 (2026-09-06 15:35~15:38 KST)
- ★★38차 §11 — 5라운드: 꼬리 교환 `t`(마지막 웨이브 FC1 을 48 CTA 로) — 수치 PASS, 시간 손해, 기각 (2026-09-06 15:5x~17:00 KST)
- ★★37차 — "컴파일 엄청 오래 걸리네": 부팅 예산 귀속, 5분 공백은 일과성, 컴파일이 아니다 (2026-09-06, 로그 판독 + 계측 추가, 무재부팅)
- ★★★36차 — 다음 레버를 숫자로 고르기: C>1 처리량, 128K 프리필 지도, 하니스 결함 둘 (2026-09-06 07:00~07:15 KST, 프로덕션 부팅 위 무재부팅 측정)
- ★★★35차 부록 — b12x v3 정적 MoE 커널 기본값 검증 부팅과 통합 측정(onepass) (2026-09-06 새벽, 브랜치 ostcode/glm-flash-kernel-improvement-5a65a8)
- ★★★35차 — b12x 정적(디코드) MoE 커널 접수: 구조 진단과 v2 레인, 측정 대기 (2026-09-05, 골격)
- ★★★33차 — W4 팩 정확도 레버 4종: 정확 활성 스케일·행별 shift·GPTQ·저랭크 보정 (2026-09-05 오후, 브랜치 ostcode/glm-flash-kernel-improvement-5a65a8)
- ★★★34차 — 메가커널 모듈·노드 융합: 프로덕션 스텝의 첫 노드 인구조사, 남은 자리 넷, 그리고 드래프터 앞의 호스트 갭 (2026-09-05 오후, 무부팅 + 프로파일 캡처 1회)
- ★★★32차 — 레버 2~7 브래킷 체인: EXP-7 이 +7%, 나머지는 0 이거나 죽었고, srv4 가 rank 3 이다 (2026-09-04 밤)
- ★스텝 커널 지도 최신화 — 무장 부팅의 인구조사, 그리고 도구가 세고 있던 남의 커널 (2026-09-04 밤, 무부팅)
- ★★★메가커널 30차 — mk_gemm 이 deep_gemm 과 같은 14 ms 인 이유: 상주 커널은 MoE 옆에서 진다 (2026-09-05, 09-04 18:42 무장 트레이스 vs 09-01 스톡 트레이스)
- ★★메가커널 29차 — 공유 전문가 GEMM 쌍의 고정비: 배리어 없는 로컬 양자화 커널은 단독으로 손해, MoE 아래에서는 발사 순서가 전부 (2026-09-04 밤, srv2)
- ★★★28차 — 드래프터 W4 는 서빙된 적이 없었다(컴파일 캐시), MK-MLA 서빙 사망의 원인은 스크래치 재할당, 그리고 드래프터 W4 판정 (2026-09-04)
- ★★메가커널 27차 — 서빙 PDL 은 꺼져 있었다, MoE 커널은 우리 레인과 같은 속도, L2-warm 은 W4 GEMM 에 7% (2026-09-04, srv2)
- ★★★26차 — 부팅의 메모리 절벽: fp8-dense 패스가 로드 중에 여러 번 돌았고, W4 팩 빌더가 예약 메모리를 흘렸다 (2026-09-04, 4노드 계측)
- ★★★메가커널 25차 — 24차 온디바이스 마감, 그리고 디코드 스텝의 꼬리: 드래프터가 임계경로에 있다 (2026-09-04, srv2)
- ★★메가커널 24차 — W4 팩이 프로덕션 그룹을 전부 클램프하고 있었다 (2026-09-04)
- ★★메가커널 23차 — srv3 호스트 OOM: nvfp4 첫 부팅이 덴스 패스 안에서 죽었다 (2026-09-03)
- ★★메가커널 22차 — "무장" 이 "서빙" 을 뜻하지 않았다: KDA 레이아웃 게이트의 침묵 (2026-09-03)
- ★★메가커널 21차 — 무장 상태의 디코드 스텝 실측: 대역폭 포화이고 커널 여지는 끝났다 (2026-09-03)
- 메가커널 20차 — two-shot all-reduce 기각: 병목은 바이트가 아니라 고정비 22.7 µs (2026-09-03)
- 메가커널 19차 — all-reduce 의 wait 38.7 µs 는 무엇인가 (2026-09-03, 확인 중)
- ★메가커널 18차 — 가중치 로드 296초는 우리 벤치가 만든 숫자였다 (2026-09-03)
- 메가커널 17차 — KV 캐시를 토큰 수로 고정 (2026-09-03, 운영자 지시)
- 메가커널 16차 — 부팅: cudagraph 메모리 프로파일러 끄기 (2026-09-03, 운영자 승인)
- 메가커널 15차 — 부팅: `init engine` 192초를 쪼갤 스탬프 (2026-09-03)
- ★메가커널 14차 — 부팅: 형제 자체 커널 두 개도 영속 캐시로 (2026-09-03)
- ★메가커널 13차 — 부팅: 자체 커널 컴파일을 영속 캐시로 (2026-09-03)
- 메가커널 12차 — MHC 발행 배치 채택, KDA delta 3-sync 는 기각 (2026-09-03, srv2)
- 메가커널 11차 — 인접 커널 협력: 둘 다 접음 (2026-09-03, 브랜치 feat/adjacent-fusion 에 기록)
- ★메가커널 10차 — KDA 2라운드: 스팬 199 → 176, 벤치 320 → 280 (2026-09-02 밤, srv2)
- ★★★메가커널 결산 — "최대한 더 개선해" 라운드 뒤 (2026-09-02 저녁, main 0995825, srv2 한 번의 전체 벤치)
- ★메가커널 9차 — MHC 재구성: T=8 30.7→27.4, T=32 59.4→42.0 µs (2026-09-02, srv2)
- ★★메가커널 8차 — MK-KDA 단계 귀속과 재작성: 커널 스팬 402 → 199 µs (2026-09-02, srv2)
- ★메가커널 7차 — MK-KDA 의 state-index 계약 확정과 수정 (2026-09-02, srv2)
- 메가커널 6차 — W8 팔 제거, W4 단일 레인 (2026-09-02, 운영자 결정)
- ★★★메가커널 5차 — W4 확장을 prmt 테이블 조회로 (2026-09-02, srv2)
- ★★★메가커널 4차 — W8 팔도 stock 을 넘었다: smem 기준 정렬 + 밀집 W 행 + 벤치 dirty 배수 (2026-09-02, srv2)
- ★★메가커널 3차 — W4 팔이 stock 을 넘었다 (2026-09-02 오전 후반)
- ★★준비 커널 통합 (`glm53_prep_fused`) — 스텝의 12% 가 GPU 유휴, 그 자리에 발사 하나 (2026-09-02, srv4 오프라인)
- ★★★dflash 는 이름 때문에 async scheduling 을 잃고 있다 (`glm53_async_dflash`, 2026-09-02, 코드 읽기)
- ★인덱서 fp32 head-gate: cuBLAS 2블록 47 us → split-K 10 us (`glm53_indexer_gate_splitk`, 2026-09-02, srv4 오프라인)
- ★오프라인 레버 소진 판정 — 9월 1일 트레이스의 스텝 구성 (2026-09-02 밤, 무부팅)
- ★★리뷰 정정 — split-K head gate 는 플릿 형상(N=32)에서 한 번도 돌지 않았다; v2 결정적 커널 (2026-09-03, srv4 오프라인)
- ★★★EXP-8 기각 — dflash async scheduling 은 이미 켜져 있었다 (2026-09-03, 전 팔 부팅)
- ★★MK_SEG_MLA — sm_121a 수제 sparse MLA 디코드, 3판에서 FlashInfer 를 넘었다 (2026-09-03, srv4)
- ★★GLM-5.3 프리필 첫 프로파일 — 32K 에서 시간의 정체와 레버 (2026-09-03, MK-MLA 부팅, rank 0)
- NCCL 채널 스윕 — 프리필 AllReduce 129 → 142 Gb/s (2026-09-03, 엔진 다운, 채택)
- ★★sm_121a 텐서코어 ISA 실측 — fp4 는 fp8 보다 빠르지 않다, 2배는 2:4 희소뿐 (2026-09-03)
- MK_SEG_MLA 전용 명령 검토 — fp8 mma 는 5% 를 위해 순위 30% 를 흔든다, 기각 (2026-09-03)
- ★★★MK_SEG_MLA v4/v5 — 점유율이 병목이었다, 그리고 프리필까지 (2026-09-03)
- 기각: 프리필 밀집 GEMM 의 스케일 입도 — deepgemm 은 이미 천장의 67% 였다 (2026-09-03)
- ★정정: fp4 는 fp8 의 2배다 — "fp4 연산 이득 0" 기록은 틀렸다 (2026-09-03)
- nvfp4 밀집 GEMM — 2.3배 · 메모리 절반 · 오차 3.7배 (2026-09-03)
- 부팅 로더: instanttensor 는 내내 있었다, 프로필이 안 가리켰을 뿐 (2026-09-03)
- bf16 원본 해제 — 랭크당 3.67 GB, KV 토큰 +39.5% (2026-09-03)
- 부팅 시간 901 → 345초 (2026-09-03)
- ★osar 에필로그 융합(AR+residual+norm+quant) — 인접성 실측으로 기각 (2026-09-04, 무부팅)
- ★PDL은 vllm-mhc 쪽에서 이미지가 봉인 — pdl_sync/trigger는 dead code (2026-09-04, 신규 컨테이너 실측)
- ★31차 — 자체 소유 미세 융합 묶음 2 (EXP-20): 오프라인 게이트 결과 (2026-09-04~05, srv4)
- ★★★33차 — 프리필 축 탐구, 첫 표적 완결: 콜드 JIT 세금을 부팅으로 (PREFILL_WARMUP=1, 2026-09-05)
- 메가커널 정확성 리뷰 수정 (2026-09-05, 기준 3b2f180, 미배포)
- ★★★39차 — b12x 정적 MoE 커널 3라운드: 커널 내부 — 타일 우선 가중치 배치(v5, 셀 `t`) (2026-09-06 14:0x~ KST, 무부팅, 프로브 대기)
- C=1 MK-GEMM: FP8 묶음 변환과 8행 전용 타일 (2026-09-07)
- GLM53 W4 키 SHA256 — warm 부팅 234 → 226.5초 (2026-09-07)
- GLM53 C=1 MoE 타일 묶음과 FP4 주소 수정 (2026-09-08, PR #461)
- ★★40차 — 프리필 스텝 고정비, 측정으로 확정: `a = 211 ms/step` (2026-09-08, pstep0908v1)
- ★42차 — 지금 사전샤딩된 DSv4.1 은 GB10 에 **안 들어간다**(TP 를 안 해서다): 랭크 84.70 → TP 면 73.30 (2026-09-11, srv4 로컬, 부팅 없음)
- ★43차 — 계획 변경: 자체 엔진 첫 대상 DSv4.1 → **Qwen3.8-Flash-Next** (2026-09-11, srv4 로컬, 부팅 없음)
- ★44차 — 방향 전환: GLM-5.3 이 공통 부분의 기준 모델 (운영자, 2026-09-11) + `profiles/glm53/plan.py` 현실 대조

## ★45차 — 엔진 네이티브 GLM-5.3: 서빙 4파일 재호스팅 대신 우리 계약으로 다시 씀 (2026-09-11)

44차 끝에서 "vLLM 없이 임포트되나"를 파일별 이정표로 삼았는데, 자동 치환된 4파일이 여전히 요구한 이름이 44개
(`VllmConfig`·`get_forward_context`·`AutoWeightsLoader`·`FusedMoEFactory`·`MambaStateShapeCalculator`…)였다. 그 44개를
`base/`에 채우면 vLLM 의 추상을 엔진 안에 다시 짓는 것이고, 운영자 지시("특정 하드웨어·NVFP4만, 레거시 없이 처음부터")와
D7(전방 컨텍스트 없음, 플랫 메타)·D11(사실 + 만료 노브)·D16(아레나만이 할당)에 정면으로 어긋난다. 그래서 4파일을 지우고
(`git rm`, 3adc8d47 의 산물), GLM-5.3 을 엔진의 계약으로 **다시 썼다**. 서빙 파일은 대수(algebra)의 출처로만 읽었다.

### 무엇이 생겼나

- `profiles/glm53/facts.py` — 상수 전부를 config.json 에서 읽고 **코드가 기대는 가정을 로드 시 단언**(nope·noaux_tc·
  sigmoid·fp32 라우터·NVFP4 대상 = 3~44층 전문가뿐·kpool 4·hc 4…). vLLM `Glm5NextTextConfig` 가 파생하던 값
  (post_mult 2.0, sinkhorn 20, mla_nope)은 판정 이미지에서 읽어 박았다. **런처 사실**: ENABLE_EP=0 → 전문가는 TP
  (중간차원 분할). `plan.py` 의 "EP" 라벨은 종류가 틀렸었다(바이트는 같음) — 고침. MTP(45층, fp8 전문가)는 **서빙되지
  않는다**(DFlash2 드래프터) → 프로필 범위 밖.
- `base/params.py` — `Spec(name, shape, dtype, sources, build)` 와 `bind(specs, views)`: 모델은 가중치를 선언만 하고
  로더가 아레나에서 깎은 뷰에 묶인다. 캐스트·리셰이프 없이, 이름·모양·dtype 불일치는 예외.
- `base/preshard.py` — safetensors 를 **스트리밍**으로 쓰는 `RankWriter`(헤더 선계산, 텐서는 제 오프셋에 pwrite)
  + `write_ranks`(층 단위로 소스를 한 번 읽어 4랭크에 동시에 씀). `RankLoader` 와 safetensors 둘 다로 읽어 확인.
- `profiles/glm53/specs.py` — 랭크가 쥐는 가중치 지도. 서빙 로더가 로드 때 하던 병합을 여기서 한 번(q|k|v|b|f_a|g_a
  → `in_proj`, q_a|kv_a → `qkv_a`, 세 conv → `[3HD,K]`, gate|up, 전문가별 gate|up → `w13`), dtype 승격도(A_log·dt_bias·
  mHC·인덱서 head-gate/k_norm → fp32), 전역 스케일은 **곱셈자로 저장**(`*_mult`, 압축텐서의 제수를 뒤집음). 자가검증:
  랭크당 1,288 텐서 **44.50 GiB = plan 인구조사 44.48**(MTP 전문가 제외, 나머지 0.02 는 승격), 체크포인트의 텍스트 텐서
  전부가 정확히 한 spec 의 소스.
- `profiles/glm53/lanes.py` — 커널 레인 여덟(conv·kda_chunk·mhc_pre·mhc_post·indexer_logits·kpool·mla_sparse·expert)을
  `reference()`(modules 의 판정된 참조) 또는 `served()`(오버레이 커널, 이미지 안 마운트 경로로 임포트, 전부 아니면 예외)
  로 묶는 표. 활성화는 `swiglu_clamped` 하나: 서빙 dense 는 SiluAndMulWithClamp, 서빙 b12x 는 "swigluoai α1 β0 limit
  10" — **같은 식**이다(flashinfer_b12x_moe 1380: "whose math reduces to clamped SiLU").
- `profiles/glm53/net.py` — 조합. 임베드 → 45층 [mHC pre(+in_norm) → KDA | DSA → mHC post/pre(+post_norm) → dense |
  MoE] → contract(mean) → norm → 헤드. 뷰 위의 평범한 함수, 상태는 `Caches` 프로토콜(층별 플랫 latent/pool-key/
  pool-scale, 슬롯별 KDA conv·recurrent·tail 링, 호출자의 블록표가 슬롯을 준다)로 받는다. KDA: in_proj → conv(상태
  carry) → q,k,v / raw gate / beta=sigmoid / 출력 게이트 → 레인 → o_norm(rmsnorm·sigmoid) → o_proj → all_reduce.
  DSA: qkv_a → q_a_norm → q_b; kv_a_norm → **latent fp8 쓰기(스케일 1, 체크포인트에 kv 스케일 없음)** → 인덱서(wq_b·
  wk+LayerNorm·fp32 head-gate·gate score → FWHT-fp8 q, 완성 풀 압축·쓰기, 꼬리 링, 후보 풀 = seq_len//4 미만, top-512
  풀 → 꼬리 포함 토큰 → 슬롯) → W_UK 흡수 MQA(레인) → W_UV → o_proj. MoE: noaux_tc 라우터(fp32 sigmoid, 선택은 +bias,
  가중치는 재정규화×2.5) → 전문가별 레인(W4A4, gate/up 전역 스케일 따로) → 공유 전문가 → all_reduce 한 번.
- `profiles/glm53/preshard.py --world 1 --layers 0-4` → dev 랭크파일 12.17 GiB, **14 s**. `--world 4` 전체는 백그라운드로.
- `profiles/glm53/check.py` — 진짜 가중치로 0~4층(KDA 4, DSA 1, MoE 2)을 한 아레나(12.21 GiB, 25 영역)에서 돌림.
  로드 **2.5 s**(13 런, 5.2 GiB/s), 512 토큰 프리필 8.0 s(참조 레인, KDA 참조가 파이썬 루프). 유한, |h| mean 1.10.

### 판정 설계에서 배운 것 — "두 청크 = 한 프리필"은 MoE 를 지나면 토큰 단위로 성립하지 않는다

처음 판정(최종 hidden 의 rel)이 2.4e-1 로 실패했고, **첫 청크**(캐시를 읽지 않는)도 똑같이 틀렸다. 블록별 추적:
M=512 와 M=256 의 bf16 GEMM 은 누적 순서가 달라 1 ulp(p50 4.6e-3)씩 다르고, 3층을 지나 1e-2 가 되며, 3층 라우터가
그 차이로 **256 토큰 중 30 토큰의 top-8 을 바꾼다**(인덱서의 슬롯 집합은 256/256 동일). 뒤집힌 토큰은 4.6e-1 까지
벌어진다. 어느 엔진이든 배치 크기가 다르면 MoE 뒤의 토큰은 다르다 — 서빙 경로도 같다. 그래서 판정은 **어텐션 블록의
둘째 청크 출력(KDA 상태·latent·풀·꼬리를 읽는 유일한 곳)이 첫 청크의 잡음 바닥을 넘지 않는가**로 바꿨다(블록별
p50·max ≤ 1.5×, 상류 MoE 의 뒤집힘은 물려받는다): L3 dsa 첫 7.4e-3/1.6e-2 vs 둘째 6.7e-3/1.3e-2, L4 kda 3.7e-2/1.4e-1
vs 3.4e-2/1.4e-1 — **캐시 경로는 아무것도 더하지 않는다**. PASS. 이 판정은 조합이 GLM 의 대수인지는 말하지 않는다;
그것은 서빙 층을 옆에 놓는 다음 판정의 몫이다.

### 계기

`hosting.py` 를 엔진 네이티브 지도로 다시 씀(서빙 3파일; mtp 는 서빙 안 됨): **114 심볼 = 89 ours(78%) + 25 drop, real 0**.
"ours" 의 뜻이 바뀌었다 — 이름을 우리 모듈로 바꾼 것이 아니라 **그 자리에 엔진이 무엇을 두었는가**(대개 `net.py` 의
한 줄이거나 `specs.py` 의 분할 규칙, 혹은 "사실이라 필요 없음": rope·yarn·fp8 dense·kv 스케일).

### 남은 것(순서대로)

1. `check.py --lanes served` 를 판정 이미지 안에서 — 같은 조합 위에서 참조 레인 vs 서빙 커널(레인 어댑터 판정).
2. 서빙 `Glm5NextDecoderLayer` 를 옆에 놓는 층 판정(조합 판정, D4).  3. 디코드 경로(recurrent KDA·conv update·
   드래프트 슬롯 K=5 의 희소 MLA·꼬리 링 갱신).  4. 러너 결합(`cache_spec` 으로 아레나에서 캐시를 깎고 블록표로 슬롯을
   주는 `Caches`).  5. b12x 전문가 레인 바인딩, DFlash2 드래프터, 4노드.

### 45차 — 절 색인 (§ 번호로 찾는다)

이 캠페인은 절 94개다(§2~§97, 결번 §35 · §47). 파일 순서는 절 번호 순이 아니다 — 세 절이 뒤에 다시 번호를 받았고(§76~§78 은 옛 §30~§32, 그 사연은 그 절들이 적고 있다) 새 절은 앞에 끼워 넣기도 했다. **찾을 때는 `45차 §N` 으로 검색한다.** `·보관` 이 붙은 절은 본문이 [MEASUREMENTS_ARCHIVE.md](MEASUREMENTS_ARCHIVE.md) 에 있다 — 출하돼 더 바뀌지 않는 것들이고, 고치지 않고 옮겼다. 절을 새로 쓰면 이 표에 한 줄 더한다.

- **§2** — 네 기본값을 형태로: TP=4·GB10·NVFP4 기본형·레거시 없음  ·보관
- **§3** — 한 스텝 함수, 위치 링 상태: 디코드·검증 스텝과 롤백이 프리필과 같은 코드  ·보관
- **§4** — 서빙 커널은 메인 스레드에서만; 캐시는 프로필의 실물로  ·보관
- **§5** — 러너가 GLM 을 돈다: 엔진 어댑터 + boot --local  ·보관
- **§6** — 서브 루프와 문, 플릿 부팅 경로, 런처  ·보관
- **§7** — DFlash2 드래프터가 엔진 안에서 돈다; 서빙 recurrent KDA 레인  ·보관
- **§8** — b12x 전문가 레인: 서빙이 로드 때 하던 팩을 사전샤딩이 한 번  ·보관
- **§9** — 간판(D16)을 실물 캐시에: 다중 영역 티어, 대화가 턴을 넘어 산다  ·보관
- **§10** — 간판이 돈다: park → 아레나 회수 → resume → 이어서 디코드 = 한 번에 돌린 것과 같다  ·보관
- **§11** — 정렬: 서빙 mHC 레인의 CUDA_ERROR_INVALID_VALUE 는 스레드가 아니라 12 바이트였다  ·보관
- **§12** — 정렬 뒤: 서빙 mHC·KDA 레인 통과, 파킹된 대화 HTTP 로 이어짐  ·보관
- **§13** — PR #535(codex) 평가와 병합; 서빙 레인 전 구간 판정에서 b12x 만 참조와 다르다  ·보관
- **§14** — 병합본 검증  ·보관
- **§15** — b12x 수치 판정: 커널은 w13 의 **둘째 반**을 게이트한다 ([up; gate])  ·보관
- **§16** — 랭크 파일 v4([up; gate]) 판정: 서빙 b12x 레인이 참조와 청크 잡음 바닥 안에서 같다  ·보관
- **§17** — 커널 패키징: vLLM 이름공간을 떠난 엔진, ST 이미지 안에서 판정  ·보관
- **§18** — 병합본(main #541 + 이 브랜치) 판정: 표식 랭크 파일로, ST 이미지 안  ·보관
- **§19** — 첫 45층 4노드 부팅: 돌았으나 다른 세션의 플릿과 충돌해 죽었다; 문(chat)·잠금  ·보관
- **§20** — 운영자 지시 넷: prefix 재사용, 취소·타임아웃, 문의 나머지, 슈퍼바이저  ·보관
- **§21** — 커널 패키지 노브 43개의 D11 정리: 채택값은 코드로, 후보 둘은 선언된 노브로, 나머지는 폐기  ·보관
- **§22** — 쿠다 그래프 경로 검토와 개선 넷: 캡처 안전 통신, 서빙 천장 하나, 풀 분리 단언, 재생 경로 고정 메모리  ·보관
- **§23** — 프로덕션 전환 시도: 창·45층 4노드 부팅·문 사다리, 그리고 깨진 글의 원인 = KDA `o_norm` epsilon
- **§24** — ST 엔진 관측 메트릭: 세 질문에 답하도록  ·보관
- **§25** — vLLM 이 못 내는 관측: 어느 그래프가 돌았나, 스텝이 얼마나 걸렸나, 수용은 어떤 모양인가, 무엇이 묶였나  ·보관
- **§26** — 플릿 예약을 하나로: 큐가 ST 를 받고, ST 가 큐를 본다  ·보관
- **§27** — 예약을 엔진 것으로: 리스는 엔진, 큐는 벤치, 그리고 유령 큐  ·보관
- **§28** — 엔진에 결합해야 가능한 것 넷, 그리고 컨트롤러  ·보관
- **§29** — "완벽하게 스무스하게 됐어?" 로 §28 을 감사: 거친 곳 여섯, 그중 실제 버그 하나  ·보관
- **§30** — 문의 스트리밍 디토크나이저: 러스트 디코드 스트림과 프로덕션에서 겪고 고친 복구 둘  ·보관
- **§31** — 구조화 출력: 스텝 하나의 비트마스크, 전용 커널, 그리고 죽은 드래프트  ·보관
- **§32** — 접두사 캐시 블록 수명: 핀을 버리고 등급 있는 자유 목록으로, 스냅샷만 읽는 복원, 테넌트 솔트  ·보관
- **§33** — SGLang 대조: 구조화 출력에서 우리가 틀린 것 셋 (사고·크래시·컴파일 자리)  ·보관
- **§34** — 샘플러 커널: 정렬을 빼고 임계값을 찾는다, 배치 한 벌로  ·보관
- **§36** — 같은 부분의 SGLang 구현 조사, 그리고 그 조사가 찾아낸 회귀: 한국어 답이 잘려 나가고 있었다  ·보관
- **§37** — 부팅 뒤 첫 요청이 `add` 에서 죽었다: 지연 생성된 페널티 히스토리에 가드가 둘 빠져 있었다  ·보관
- **§38** — 한국어 최적화 셋: 답 길이 기본값을 글자로, `text_offset` 을 글로, `bytes` 를 진짜 바이트로  ·보관
- **§39** — SGLang 전면 대조(남은 영역): 가져올 것은 **하나 반**이었다, 그리고 내가 넷이라고 적은 방식  ·보관
- **§40** — 한국어, 계속: 줄 위의 바이트와 거절의 숫자와 글자 계량기; 그리고 아니었던 것 둘  ·보관
- **§41** — 구조화 출력 × 한국어: `[가-힣]` 이 한글의 6분의 1을 조용히 버리고 있었다  ·보관
- **§42** — 같은 한국어를 두 번 토큰화하고 있었다: 조합형/분해형, 그리고 U+FFFD 로 겹치는 top_logprobs  ·보관
- **§43** — llama.cpp·Ollama 에서 배워올 것 조사: 하나 있고, 이미 한 것이 넷, 못 하는 것이 둘  ·보관
- **§44** — 도구 호출 인자를 조각으로 흘린다: 닫힐 때까지 기다리던 3.9 초가 0 이 됐다  ·보관
- **§45** — 도구 호출에 지연 문법: 선언 안 한 도구도, 없는 인자도 이제 못 쓴다  ·보관
- **§46** — 사고 예산: 생각이 답을 다 먹어 치우지 못하게  ·보관
- **§48** — 엔진 스위트가 오랫동안 빨갰다: 셋이 썩어 있었고 여덟이 안 돌고 있었다  ·보관
- **§49** — 부팅 창의 호스트 메모리: 스테이징 버퍼가 필요보다 862 MiB 넓었고, 좁히니 더 빨랐다  ·보관
- **§50** — 메모리를 vLLM 과 줄 맞춰 보니: 부팅은 저쪽이 재고 우리는 선언하고, 서빙은 둘 다 눈이 없다  ·보관
- **§51** — 그 "장비 필요" 숙제는 이미 원장에 있었다, 그리고 서빙이 끝나면 아레나를 내놓는다  ·보관
- **§52** — "확실히 깔끔하게": 아레나만으론 안 끝난다, 그리고 끝났는지 말하는 숫자  ·보관
- **§53** — "종료하면 찌꺼기 안 남아?" 물어서 디스크를 봤더니: 루트 99%, 그중 103 GB 가 티어였다  ·보관
- **§54** — 구현 범위 안에서 vLLM 과 맞대 보니: 앞선 것 여섯, 뒤처진 것 다섯, 나머지는 범위 밖  ·보관
- **§55** — 내가 §54 에서 틀린 것 하나, 그리고 진짜였던 것 하나: `POST /v1/prefix/reset`  ·보관
- **§56** — 데네브·웜홀과의 연계: 엔진이 자기가 뭘 할 수 있는지 말한 적이 없었다, 그리고 웜홀은 눈을 가리고 있었다  ·보관
- **§57** — 웜홀이 우리 눈을 가리던 키를 껐다, 그리고 `max_model_len` 이 비어 있는 걸 실물에서 봤다  ·보관
- **§58** — 데네브까지의 사슬을 끝까지 따라가 보니, 빠진 고리는 우리 한 칸뿐이었다  ·보관
- **§59** — `medium` 은 표준인데 우리 문이 400 을 냈다, 그리고 템플릿 kwargs 는 검사를 통째로 건너뛰고 있었다  ·보관
- **§60** — 양방향으로 보니 한쪽이 끊겨 있었다: 통제면이 우리 엔진을 모른다  ·보관
- **§61** — 양쪽 다 우리 것이니 알 수 있는 것: 에이전트는 매 턴 대화 전체를 다시 토크나이즈시키고 있었다  ·보관
- **§62** — 파킹된 대화 280개가 호스트 램 1.04 GiB 를 잡고 있었다, 스캔이 물어보는 건 숫자 셋인데  ·보관
- **§63** — 남은 셋을 코드에 대조했더니 둘은 이미 있고 꺼져 있었다, 그리고 그 이유를 GPU 없이 지웠다  ·보관
- **§64** — `__restrict__` 손실의 값을 재 보니 0 이었다  ·보관
- **§65** — 프로덕션이 스스로 말했다: 드래프트 폭 5 가 걸리는 곳이다  ·보관
- **§66** — 컴파일된 문법 캐시에 천장이 없었다; 이미지 안에서 재 보니 스키마 하나가 173 KiB  ·보관
- **§67** — PDL 의 이득을 실제로 쟀다: 공짜지만 프로덕션 청크에선 거의 0  ·보관
- **§68** — 디코드 체인의 eager 가운데를 쟀다: 융합 문제가 아니라 **디스패치** 문제였다  ·보관
- **§69** — 블록 검증을 런치 하나로 접었다: 2,451 → 459 µs, 토큰은 한 개도 안 바뀌고  ·보관
- **§70** — 두 시점을 하나로 보고 있었다: 런 안에서는 D16 이 켜지고, 런 사이에서는 접두사 캐시가 이미 최대다  ·보관
- **§71** — 샘플러는 한 행에 505 µs, 배치가 1이든 48이든 같다: 점유가 벽이다  ·보관
- **§72** — 검증 커널 형상 조정(+13%), 그리고 §69 의 5.8배가 무엇의 함수였는지  ·보관
- **§73** — 데네브 레포 쪽에서 우리를 보니: 문이 모르는 모델 이름을 그대로 돌려주고 있었다  ·보관
- **§74** — 디코드 가운데를 CUDA 그래프로 캡처했다. 되고, 값은 1.0~1.1배다  ·보관
- **§75** — 스냅샷 미터를 트래픽 실린 플릿에서 처음 읽었다: 96 은 과하지 않고 **포화**다  ·보관
- **§76** — 오늘 것 재검토: 인계가 플릿에서 아예 안 돌고 있었다  ·보관
- **§77** — 오늘 것 말고: 플릿 큐 자체를 조사  ·보관
- **§78** — §31 의 넷을 해결: 플릿 스위트가 393/393 이 됐다  ·보관
- **§79** — "최근 원패스가 최악" 의 답: 같은 커밋인데 다른 엔진을 쟀다
- **§80** — 겹침은 된다: 4배의 일이 1.21배의 시간에. 문제는 실리콘이 아니라 **독립적인 일이 없다는 것**
- **§81** — 디코드 스텝의 구성을 프로덕션이 답했다: 드래프터가 36.4% 다
- **§82** — observe 의 16 ms 는 투영이 아니었다: 로터리가 매번 자기 상수표를 다시 만들고 있었다
- **§83** — 드래프터의 나머지 절반: 탭 컨볼루션이 스텝당 1,500 디스패치였다
- **§84** — 드래프터를 **그래프 재생으로** 재니 답이 달라졌다: 14.97 → 6.12 ms
- **§85** — 대화 이어가기가 왜 한 번도 안 탔나: 추론을 켜는 쪽과 되돌려주는 쪽이 다른 계층이다
- **§86** — 프로브가 틀렸었다: 한 스텝에 블록은 **한 번** 돈다, 그리고 드래프터의 최대 항목은 어휘 헤드다
- **§87** — 선택기를 접었다, 그리고 **박아둔 계약 하나를 깰 뻔했다**
- **§88** — W4 GEMM 은 바닥의 **1.25배**다: 드래프터에서 접을 것이 끝났다
- **§89** — 다음 파트로 넘어가니 **자가검사 셋이 썩어 있었다**, 그리고 KDA 링은 이미 바닥이다
- **§90** — 창을 예약했고, 그동안 **창에서 쓸 자를 만들었다**
- **§91** — 대기예약을 걸었다, 그리고 **큐가 하지 않은 부탁을 했다고 적고 있었다**
- **§92** — 테스트 부팅 모드: 플릿을 안 잡고 **프로덕션 레인**을 돌린다
- **§93** — D17 을 처음 집행했다: 오늘 성능 PR 넷에서 잰 이득이 없고, 디코드는 낮게 나왔다
- **§94** — 테스트 부팅은 **경로가 프로덕션과 같아야** 한다, 그리고 본체는 계측창이다
- **§95** — 큐가 허용한 검사 여섯 중 셋은 **큐가 시작은 하되 돌릴 수 없었다**
- **§96** — 관측 겹치기를 준비했다: observe 는 commit 전에 계산을 시작할 수 있다
- **§97** — 서빙 중에 멎은 플릿: 같은 집합통신에 네 랭크가 서로 다른 형상으로 들어갔다
- **PR #767** — GPU 하나면 되는 검사는 플릿이 아니라 **ost-97x 의 5050** 으로: 큐에 레인이 둘
- **PR #770** — 리스를 큐 아래로: 기록 하나, 주인은 큐, 인계는 이전 — 그리고 러너가 **영원히 거부**하고 있었다
- **PR #771** — ost-97x 는 테일넷의 **Windows 박스**였다: 이름은 srv2 의 ssh 별칭이 풀고, 사용자·포트도 별칭이 정한다
- **PR #774** — 단일 GPU 레인은 **srv4 한 대에서 프로덕션 옆에**: 증거는 여유 메모리, 첫 실측이 OOM 과 b12x m=8 을 가르쳐 줬다
- **PR #775** — ST 브래킷을 큐에: 커밋 하나가 팔, 프로덕션 형상, 부팅당 두 판 — 큐가 드디어 **부팅할 것**을 얻었다
- **PR #778** — 배포와 큐가 만난다: D17 프로브 티켓이 배포된 커밋의 웜 표본을 **공짜로** 남기고, 컨트롤러가 배포된 sha 를 따른다 (#776 의 재상륙)
- **PR #793** — 무장한 첫 세 시간의 기록이 가르친 것: 부팅 중인 플릿은 **입양**하고, 채팅이 답해야 건강이고, 큐가 막 놓은 플릿은 **유예** 뒤에 되돌린다 (앞의 셋은 #786)
- **PR #796** — 랭크 하나의 크래시가 프로덕션을 영영 못 뜨게 하지 않도록: 티어가 어긋난 부팅은 **버리고 뜬다**, 리스 경쟁은 실패한 시도가 아니다, D17 은 사이클마다 스스로 채운다
- **PR #800** — "커널 결함" 의 정체: 랭크 절반이 호스트 투표에, 절반이 GPU 집단통신에 — 시작 못 한 전송도 그 랭크의 **표**다
- **PR #804** — 큐의 전환 비용: 유예는 **기록의 p75**, 게이트는 아무도 안 쓴 프로덕션에 바로 열리고, 창과 자리 승계가 생겼다
- **PR #807** — 기준선은 최소로: 도입된 후보의 표본이 **그대로 다음 기준선**(정체성은 엔진 트리), 바닥은 기록에서 빌리고, 프로브는 한 판
- **PR #808** — `engine/` 이 그대로인 main 은 **부팅 없이** 배포된 것으로 기록한다
- **PR #810** — C=4 는 왜 C=1 의 1.4~1.7배뿐인가: 배치 4 스텝은 전문가 스트리밍이 60% 이상, 긴 컨텍스트는 **직렬 프리필**이 36%; 라우터 TC 를 전폭으로
- **PR #782** — 움직인 체크아웃은 큐가 **다시 핀**하고, 유휴 복구는 배포된 체크아웃에서 돌고, D17 표본은 스스로 채워진다

### 45차 §23 — 프로덕션 전환 시도: 창·45층 4노드 부팅·문 사다리, 그리고 깨진 글의 원인 = KDA `o_norm` epsilon (2026-09-11 밤 ~ 09-12 새벽)

**창**: 22:21:56 srv2 `fleet-idle-recovery.timer` 정지(이 컨트롤러만 프로덕션을 되살린다), 22:22:01 네 노드 `glm53`/`glm53-worker` 제거. 직전 프로덕션 카운터
prompt 198,017 · generation 1,947 · success stop 10 + length 6. wormhole 의 `glm-5.3-flash-local` 은 `fallback: glm-5.3-flash`(z.ai) 라 창 동안 상류 API 가 받았다(로그 확인).
운영자 지시로 창은 진단이 끝날 때까지 열어 둔다(23:58 "프로덕션 복귀는 시키지마").

**부팅**(`start-st-glm53.sh`, RANKS_DIR=`~/models/st-glm53-9391-up-gate-full`, 네 노드 표식본): arena 2.26 s(+57.17 GiB 한 덩어리 — 부팅 OOM 가설 기각), load 41.2 s(44.5 GiB, 54 블록),
drafter 2.8 s, **capture decode 238.9 s**(콜드; 두 번째부터 63.7 s), 총 5분 20초(프로덕션 vLLM 웜 부팅 345 s 급). `ST engine: GLM-5.3, TP=4, lanes=served, KV 8.73 GiB, serving on :8000`,
`/v1/models` = `glm-5.3-flash`. 예산 표(rank 0): 상자 119.69 − OS 11.97 − 런타임 바닥 5.54 − 가중치 44.50 − 드래프터 2.18 − 슬롯 1.21 − prefix 스냅샷 0.59 − 워크스페이스 12.00 − 티어 0.12
= **KV 41.58 GiB**, 선언 7.52(580 블록) → 미할당 34.06 GiB; 동시 4 에서 1,048,576 tok/요청.

**부팅 넷의 판정**:
| 부팅 | 티어 | 디코드 | 결과 |
|---|---|---|---|
| 22:22 | srv4 에 16:52 로컬 테스트 잔재(rank0~3, seq-0/1) | 캡처 그래프 | 첫 요청 안 끝남, 네 랭크 96% GPU 스핀, `adapter.decode` 안 |
| 22:36 | 같음 | 이거(`STK_decode_eager=1`) | 64 토큰 나왔으나 글이 깨짐; rank 3 이 retire 에서 `conversation 0 is already parked` 로 사망 → 셋이 다음 all_reduce 에서 스핀 |
| 22:39 | 청소 | 캡처 그래프 | 3분 20초 진전 0(카운터 전부 0), 네 랭크 살아서 96% — 재현 2/2 |
| 22:45 | 청소 | 이거 | 요청 정상 종료(10.7 tok/s, 수용률 25%), **글은 여전히 깨짐** |

**뿌리 셋** (3 은 codex 세션이 같은 시각에 독립적으로 찾아 **PR #565** 로 먼저 머지했다 — 그쪽은 한 상자 LocalTP 45층 추적으로, 여기는 vLLM 층별 훅 대조와 플릿 4노드 서빙으로 같은 상수에 닿았다; 이 절의 몫은 판정 사슬·도구·플릿 검증이고 수정 커밋은 #565 의 것이다):
1. **티어 잔재 = 랭크 발산**(고침). 티어는 랭크·노드마다라 한 노드의 옛 파일이 그 랭크만 "이미 파킹된 대화"로 만들어 `_admit` 이 프리필 대신 복원을 골랐다.
   조치: 네 노드 `st-tier` 청소 + `Server._agree_on_parked`(개수·마지막 키·체크섬을 all_reduce_max 로 맞추고 다르면 네 랭크가 같은 메시지로 죽는다, D3; 캡처 앞에서도 한 번).
2. **NCCL all_reduce 가 든 캡처 디코드 그래프는 첫 replay 에서 안 돌아온다**(청소 뒤에도 2/2). 같은 레인을 이거로 돌리면 돈다 — 커널·NCCL 무죄, 그래프 replay 의 문제. 열린 항목(§23 보충에서 재확인).
3. **깨진 글 = `net.py` `O_NORM_EPS`**. 판정 사슬: (a) `max_tokens=1` 에서 이미 첫 토큰이 틀림("Twinkle, twinkle, little" → `,`, "import numpy as" → `s`, "One, two, three, four," → `chr`) → 프리필 경로;
   (b) 참조 레인표(`STK_lanes=reference`)로 45층을 돌려도 깨짐(`, and the, and, and`) → 커널 레인 무죄, 두 표가 공유하는 조합·가중치·캐시·문;
   (c) 랭크 파일 == 현재 `specs.py` 빌더(rank 0·3, top/L0/L3/L44 + L3 전문가, `probes/glm53_rankfile_audit.py`), 아레나(O_DIRECT·H2D) 경로 == CPU 경로(`probes/glm53_arena_bytes_check.py`) → 가중치 무죄;
   (d) **외부 참조**: 프로덕션 vLLM 모델 코드(오버레이)를 srv4 한 대·TP=1·앞 3층으로 띄워(로더에 층 범위 가드 한 줄) embed·각 층 attn/mlp·L0 내부(mhc_pre·in_proj·f_b·g_b·o_norm·o_proj) 를 훅으로 덤프, ST 프로브(`probes/glm53_first_token.py`, 참조 레인, LocalTP, 0~2층)와 대조 —
   **embed 완전 일치, L0 KDA 블록부터 per-token rel 1.9**(ST rms 0.0188 vs vLLM 0.0064);
   (e) CPU 재현(HF 텐서, ST 모듈 함수): x·투영·f_a·f_b·g_b 출력이 vLLM 과 4자리 일치 → 코어(conv·recurrence·o_norm) 안. KDA 코어 출력은 rms ~2e-4, **mean(x²) ~7e-10** 이라 `rmsnorm` 이 eps 에 지배된다:
   vLLM 은 `FusedRMSNormGated(head_dim, activation="sigmoid")` **클래스 기본값 eps=1e-5**(`third_party/flash_linear_attention/ops/kda.py:483`; 1e-6 은 맨 함수 `rms_norm_gated` 의 기본값), ST 는 1e-6 → 모든 KDA 블록 출력이 √10 = 3.16배(0층 실측 2.9배).
   `O_NORM_EPS=1e-5` 로 L0 블록 rel **1.94 → 0.008**. 플릿(서빙 레인·이거) 재부팅: 첫 토큰 " Paris"·" star"·" np"·" Thursday"·" five", 본문 `' Paris. In French, Paris is spelled "Paris", but…'`,
   `' 도쿄입니다. 대한민국의 수도는 서울이고, …'`, `' 7, 8, 9, 10, 11, 12, 13, 14,'`, chat(thinking off) `'대한민국의 수도는 서울입니다.'`. 자기일관성 판정이 전부 통과한 이유: 상수 하나를 두 레인표가 공유한다.
   **교훈(D14)**: 참조 레인은 커널의 참조이지 알고리즘의 참조가 아니다 — 알고리즘의 참조는 vLLM 스택이고, 층별 훅 대조(`scratch vllm_ref_hooks.py`, 이번에 만든 절단 로더 가드)가 그 판정기다.

**문**: thinking off 이면 템플릿이 `<think></think>` 로 블록을 닫으므로 생성 전부가 content — 문이 `</think>` 를 기다리며 답을 `reasoning_content` 에 넣던 것을 고침(렌더된 프롬프트가 reasoning-end 로 끝나면 content 모드).
**리베이스 뒤(main #561~#565: KDA 상태·후보 교환·그래프 검토·KDA 출력 노름 융합 커널·eps)**: 융합 노름 경로로 첫 토큰 " Paris"·" star" 정상, 그러나 3토큰 프롬프트("import numpy as")에서 네 랭크가 `moe_dispatch._get_direct_micro_kernel` 의 `kernel.__cache_key__()` TypeError 로 사망 — #558 의 direct-micro 디스크 캐시 래퍼가 GPU 에서 처음 도는 순간이었고 `__cache_key__` 는 프로퍼티다(`moe_w4a16_kernel` 은 맨 속성으로 읽음). 고침(호출 가능하면 호출, 아니면 속성).
**부수 수정**: 런처·슈퍼바이저의 자기 ssh(헤드 srv2 는 자기 키를 거부 → 로컬 셸), `facts.RANKS`·런처 기본값을 실재하는 표식본으로, `STK_*` 노브 컨테이너 전달, `qualify_eager_decode(warmup=)`.
**뿌리 2 의 답(06:28, main #567 병합 뒤 재부팅)**: codex 가 원인을 찾았다 — 캡처 워밍업·첫 프리필이 MoE 워크스페이스 캐시를 키우며 **그래프에 기록된 작은 할당을 해제**해 replay 가 해제된 주소를 읽었다; #567 이 소유자를 그래프 수명 동안 붙든다. 이 브랜치 + #567 로 그래프 부팅(capture 92.9 s): `The capital of France is` → ` Paris. In French, …`, 3토큰·7토큰 첫 토큰 정상, **256 토큰 스트림 29.7 tok/s(TTFT 4.78 s, 수용 173/525)** — 이거 10.7 tok/s 의 2.8배, 프로덕션 C=1(16.6 step/s × ~2.2) 의 ~80%. 표본 하나, 띠 아님(D14).
**그래프 경로 첫 onepass(ST-GRAPH-45, 06:31, 123 s)**: 2K 2/3 · 32K 0/3 · 128K 0/3 = **2/9**, 한국어 깨진 응답 1/5, 디코드 창 없음 — 그런데 표의 프롬프트 토큰이 세 문맥 모두 **2048**, 32K 프리필이 1.18 s. 원인: `tokenizer.json` 이 `truncation {max_length 2048, direction Right}` 를 싣고 있고 문의 `tokenizers.Tokenizer.from_file` 이 그 규칙을 그대로 적용해 **모든 프롬프트를 앞 2048 토큰으로 자른다**(질문이 끝에 있으니 채움글 얘기만: 32K 답이 사원수·해밀턴). transformers 의 AutoTokenizer(vLLM 의 토크나이즈 경로)는 이 설정을 무시한다. 직접 확인: 2,684자·10,583자·30,085자 프롬프트 모두 `prompt_tokens` 2048, 끝에 심은 코드 못 찾음. 조치: `boot.tokenizer()` 가 `no_truncation()`·`no_padding()`; `tests/test_engine_tokenizer.py`.
**잘림 수정 뒤(06:37 부팅, 그래프 경로)**: 바늘 찾기 2,684자/10,583자/30,085자 → prompt_tokens 2,220/8,545/24,251, 코드 셋 다 정답(BLUE-7742·RED-3319·GREEN-5561; 1·1.2·3 청크). **onepass ST-GRAPH-45b(173 s): 9/9 정답 · 한국어 깨짐 0/5 · 프리필 2K 2,028 tok/s(웜, TTFT 1.05 s) · 32K 2,117 tok/s(TTFT 15.4 s) · 128K 1,990 tok/s(TTFT 64.6 s)**; 디코드 창은 안 잡힘(이 워크로드의 답이 짧아 2 s 창 없음 — 고정 길이 다리 `--fixed-decode-tokens` 로 따로). 프로덕션 기준선(23K 2,696 tok/s·TTFT 8.55 s)의 프리필 ~79%. 한 부팅 한 표본 — 띠가 아니다(D14).
**디코드(고정 길이 다리, ST-GRAPH-45c, 297 s, 같은 부팅)**: `--fixed-decode-tokens 2048 --fixed-decode-reps 3` → C=1 **32.8 / 32.3 / 35.4 tok/s**(2048/2048 토큰씩), 품질 18/18, 한국어 0/8. 프로덕션 C=1 기준선 16.6 step/s × raw acc 18~25%(≈2.0~2.2 tok/step) ≈ 33~36 tok/s 와 같은 급. onepass 의 디코드 창(step/s)은 ST 문에 `vllm:iteration_tokens_total_count` 가 없어 0개였다("INVALID: too few fixed decode windows") — 문에 그 행(= runner.steps)을 추가, 다음 부팅부터 창이 잡힌다.
**남은 것**: (1) 부팅 반복으로 띠(프리필·디코드 창) 세우기; (2) 이거 디코드 10.7 tok/s 는 프로덕션(16.6 step/s × ~2.2)의 1/7 — 그래프가 돌아야 전환 논의 가능; (3) onepass 품질 게이트(9/9·한국어) 미측정; (4) KV·max_seqs 결정(미할당 34 GiB).

**A/B 전체 구현(09:05, PR #575)**: vLLM 대비 미구현 조사(A 계약·B 엔진)를 문(`base/serve.py`)·어댑터·샘플러에 다 넣었다 — A1 샘플링 옵션(top_k/top_p/min_p/반복·빈도·존재 페널티/logit_bias/seed/stop ids/min_tokens/n≤4/best_of/echo; 옵션 있는 행만 base/sampler 의 리치 경로, 드래프터는 `propose_sampled` + 기각 샘플링으로 유지), A2 `response_format`(xgrammar, 드래프트 K=5 를 롤백으로 걷는 마스크), A3 logprobs/top_logprobs, A4 tools/tool_choice + `<tool_call>` 스트리밍(부분 태그 보류), A5 reasoning_effort/enable_thinking, A6 레거시 `/v1/completions`·`/tokenize`·`/detokenize`(엔진 방언은 `/v1/engine/completions` 로), B1 이어가기(히스토리가 진접두면 `extend`), B2 generation_config 기본값·KV 24 GiB, B4 웜업 사다리(64…4096 × 폭 1…4), B6 `vllm:iteration_tokens_total_count`. B3(비동기 스케줄링)·B5(선점) 는 보류·설계대로. CPU 스위트 305 OK; 플릿 검증은 #570 슈퍼바이저가 수동 부팅을 죽여 창 대기.
**A7 그림(09:25, 같은 PR)**: 운영자 정정 — 프로덕션 vLLM 은 이미지 4·영상 1 을 서빙한다(PR #431); "텍스트 전용" 은 틀린 옛 요약. 프로덕션 이미지에서 `glm5next/nvidia/multimodal.py`(타워)와 `transformers_utils/processors/glm5next.py`(프로세서)를 읽어 vLLM 없이 다시 썼다(`profiles/glm53/vision.py`, 700줄): 24블록 ViT(qkv 편향, q/k RMSNorm eps 1e-5, 2-D neox rope 32/64, 클램프 SwiGLU 10) → post norm → 2×2 conv 다운샘플 → 머저(proj·LayerNorm·GELU·SwiGLU 10240) 4096폭; 프로세서는 토큰 예산 → 픽셀 예산(이미지 16~8000 토큰, 영상 16~30000 = 프로덕션의 `_MAX_VIDEO_TOKENS` 캡), 28 정렬 위로 올림·예산 초과면 이진 탐색, pad 모드(비율 유지·우하 0 패딩·축소만), bicubic antialias, CLIP 평균/표준편차를 rescale 과 융합, 시간 패치 2(정지 이미지는 같은 프레임 둘), Qwen-VL 패치 순서; 영상은 프로덕션 로더처럼 32 프레임 균등 → 전부 실렸으면 GLM 샘플러(`fps_interval` 2, 2048 프레임 상한, 짝수화) → 프레임 쌍마다 `<|begin_of_image|>` + 토큰 + `<|end_of_image|>` + "N.N seconds"; EXIF 회전 정규화·투명은 흰 바탕 합성, 178,956,970 픽셀 초과 거부. **판정**: 합성 이미지(407×613)·합성 영상(8 프레임)의 `pixel_values` 가 프로덕션 프로세서와 sha256 비트 일치, 캔버스 크기 9종·영상 5종·프레임 인덱스 7종 일치(`tests/fixtures/glm53_vision_reference.json`, 프로덕션 이미지에서 뽑음). 배관: 문이 `image_url`/`video_url` 파트를 받아(data:·http(s), 이미지 5 s·영상 30 s, 64/512 MiB 상한) 캔버스(uint8)를 만들고 자리표시자를 늘려 `media` 로 넘긴다 → 도착 브로드캐스트에 실려 네 랭크 → 어댑터가 그 위치에 닿는 첫 프리필 청크에서 타워를 돌려(`net.Step.patches` 로 임베딩 행 교체) 청크 창으로 자르고 지난 뒤 버린다; 네 랭크의 행 합을 max-reduce 로 대조(D3). 가중치는 `vision.safetensors`(347 텐서 1.05 GiB, `preshard.py --vision`, 1.4 s; RankLoader 1.0 s) — 4노드 배포 완료(체크섬 609aaf3d…), 플릿 부팅은 없으면 죽는다; 부팅이 최대 이미지(32,000 패치)·최대 영상(16쌍)을 한 번 인코딩해 SDPA 백엔드·워크스페이스를 증명한다(수학 백엔드 배제: 최대 이미지에서 32 GiB). 같은 자리표시자에 다른 그림은 다른 프롬프트: 이어가기 판정과 prefix 캐시 체인에 그림 digest 를 섞었다. 실제 가중치로 CPU 인코딩 448×448 → 256×4096 유한(rms 0.019). CPU 스위트 326 OK; **vLLM 타워와의 수치 대조와 플릿 검증은 운영자 지시로 생략**(09:30).
**블록 단위 prefix + 비동기 스케줄링(10:20, 같은 PR; 운영자 "블록단위 프리픽스, 비동기 스케줄링 구현")**: (1) prefix 캐시의 단위를 프리필 청크 6,912 에서 풀의 블록 2,304 로(프로덕션 APC 와 같음). 청크 안 경계의 상태는 링에 남지 않으므로 러너가 스텝 전에 `marks={위치: 스냅샷}` 로 이름 붙이고 스텝이 받는다: `net._kda` 가 KDA 되풀이를 경계에서 끊어(조각마다 `kda_chunk`, 상태 이어 넘김; 경계가 청크 시작에서 64 정렬이라 커널 청크가 거기서 다시 시작 → 자르지 않은 계산과 같은 순서) 조각 끝 상태·conv 입력 3개를 `caches.mark_kda` 로 스냅샷에, 드래프터 링은 스텝 전 링 복사 + 경계 전 위치 관측(`mark_draft`); 스텝 끝 경계는 종전대로 `checkpoint`. 스냅샷 8 → 24(경계당 77 MiB, 1.8 GiB/랭크). CPU 판정: 참조 레인 tiny facts 로 자른 것과 안 자른 것의 출력·끝 상태 일치(atol 2e-2 bf16), 경계 상태 = 접두만 돌린 상태, conv 탭 = 경계 전 입력(`tests/test_engine_kda_marks.py`); 러너: 19 토큰 프롬프트가 4·8·12·16 에 경계, 13 토큰 공유 프롬프트가 12 를 재사용(전엔 8). (2) 디코드가 호스트보다 앞서 돈다(`pipeline.py`): 장치에 타깃 그래프 → 샘플러 → `commit_batch`(수용·한도·끝 토큰 클립) → 마스크 관측(`observe_masked`, 유효 개수 장치 스칼라) → 제안(`propose_from`/`propose_sampled_tensor`) → 다음 ids 를 남기고 결과만 핀 버퍼 + CUDA 이벤트로; 러너는 `inflight` 깊이 2 로 띄우고 `resolve_oldest` 로 한 스텝 늦게 적용, 프리필·동기 디코드·취소·정지 전엔 비운다(`drain`). 끝난 행은 장치가 `alive=0` 으로 슬롯을 null 로 돌려 유령 스텝이 링·꼬리에 아무것도 못 쓰고 KV 는 ctx 뒤 자리라 무해; 예약은 `horizon = ctx + 6 × (1 + 앞선 스텝 수)`. 온도/top_p 행은 장치 기각 샘플링 `speculative_pick_batch`(그리디 행은 원-핫 드래프트 분포로 같은 식), 나머지 옵션 행은 동기. 루프의 객체 브로드캐스트·투표는 gloo 제어 그룹으로(NCCL 그룹의 `broadcast_object_list` 는 스텝 커널 뒤에 줄 서고 읽기에 장치 동기화). CPU 판정: 러너 파이프라인(가짜 모델: 깊이 2, 유령 무시, 프리필/동기/취소 전 비움, 지평선), 기각 샘플러·커밋 클립 성질(같은 분포면 전부 수용, 분리 지지면 첫 기각·복구, 시드 결정성, 한도·끝 토큰), 분포 배치·readback 부기 — 스위트 354 OK. **GPU 경로(그래프 device fill·마스크 관측 그래프·이벤트 readback)와 실측은 미검증**(운영자 지시로 생략) — 첫 플릿 창의 할 일: 부팅 qualify + 원패스 디코드 창(동기 대비 스텝 간 거품 제거분).
**prefix 캐시를 더 관용적으로(11:10, PR #585; 운영자 "네개 전부")**: 토큰이 다르면 못 쓴다는 한계(vLLM 도 같음) 안에서 넷을 바꿨다. (1) 단위 2,304 → **768**(`facts.BLOCK`; 청크 정렬 2,304 는 `facts.CHUNK_ALIGN` 로 분리, 6,912 법칙 그대로 — 768 = 12×64 로 KDA 커널 청크 정렬, kpool 4 의 배수, 청크당 9 블록): 3,000 토큰 공유가 2,304 → 2,304 지만 3,100 이면 3,072. 페이지 레이아웃·인덱서 커널은 블록 크기를 인자로 받으므로 커널 변경 없음; 블록 테이블 3배(수십 KB). (2) 스냅샷 24 → **96**(7.2 GiB/랭크, 예산 여유 ~40 GiB 안) + 퇴거 정책: 채택된 적 없는 경계부터(`_victim`), 그 다음 LRU — 128K 프롬프트 하나(170 경계)가 공유 시스템 프롬프트를 밀어내지 못한다. (3) **생성 중 넘은 경계**: 동기 경로는 스텝 직후 링에서 `checkpoint`(rec 6칸·conv 8칸이 아직 그 자리를 들고 있다), 앞서 도는 경로는 다음 스텝이 링을 덮으므로 장치 스테이지(`caches.stage_boundaries`, Triton `_stage_rec`/`_stage_conv`: 넘은 행만 KDA 상태 1개 + conv 탭 복사 — 행당 스텝당 ≤36 MB, 46 ms 스텝의 ~0.3%; 슬롯당 스테이지 35.7 MB 는 아레나에 선언)에 놓고 `resolve` 때 `checkpoint_from_stage`; 러너는 `history()`·`media_marks()` 로 체인을 다시 계산해 이어간 턴의 경계까지 마크한다. 이득: 티어에서 잊힌 대화의 재전송이 답변 블록까지 재사용. (4) 이어가기 관용: 히스토리 끝이 끝 토큰이고 그 앞까지 일치하면 이어간다(`extend(drop_unfed=True)`: 그 토큰은 샘플됐지만 먹인 적이 없어 상태가 정확히 그 앞) — 템플릿 확인(이미지 안): thinking off 재전송은 답 뒤에 바로 `<|user|>` 라 모델이 `<|user|>` 로 끝냈으면 원래 맞고 `<|endoftext|>` 로 끝냈으면 이 규칙으로 맞는다; **thinking on 은 이전 턴 추론을 템플릿이 되그리지 않아(`<think></think>`) 이어가기 불가 — 그 앞 블록까지는 prefix 캐시가 산다**(reasoning_content 를 되보내면 일치). CPU 판정: 스테이지 = 링 `checkpoint` 와 비트 일치·안 넘은 스텝은 손 안 댐(`test_engine_boundary_stage.py`), 생성 경계 8·12 채취와 재전송 대화의 8 재사용, 채택된 경계 생존, 끝 토큰 없는 재전송 이어가기 — 스위트 **390 OK**. GPU(스테이지 커널·768 블록 부팅) 미검증.
**prefix 캐시 더(11:50, 운영자 "abcf")**: (A) **NVMe prefix 티어** — 러너가 스텝마다 `maintain_prefix`: 빈 스냅샷이 8개 아래면 퇴거 순서의 잎 경계(다른 경계가 잇지 않는 것; 프리필 중인 프롬프트의 경계는 다 들어올 때까지 제외)를 대화 티어 옆 `prefix/` 디렉터리에 미리 쓴다(블록 + 스냅샷 바이트 + `{hash, tokens}` 기록; `TieredKV`/`NvmeTier` 그대로, 키 = 해시 56비트, 스테이징 32 MiB 별도). 쓰인 경계는 `_victim` 에서 같은 등급 안 먼저 나가고(잃는 게 없다) `tier_keys` 에 남는다; 새 프롬프트가 메모리엔 없고 티어에 있는 경계를 지나면 rank 0 이 도착에 후보를 실어 보내고 `_admit` 이 네 랭크 투표(`have`) 뒤 요청 행에 `restore_begin`(블록 예약 + 빈 스냅샷 + 티어 스레드 읽기) → `_settle` 투표 → `submit(prepared=(tokens, snap))` 로 그 뒤부터 프리필; 한 랭크라도 못 읽으면 되돌리고 보통 입장. 부팅 때 티어 키를 읽어 `_agree_on_parked` 로 네 랭크 일치 확인. (B) **동시 중복** — `runner.shared_ahead`: 프리필 중인 행의 체인이 같은 경계를 앞으로 만들면 둘째 요청은 뒤 요청을 먼저 보내고 기다렸다가(`dedup_waits`) 경계가 캐시되면 맨 앞으로(`_reorder_waiting`) 채택. (C) `POST /v1/prefix/warm`(messages/prompt/ids, 1 토큰 생성 뒤 버림, `pin`)·`/v1/prefix/unpin`; 핀·해제는 rank 0 의 제어 큐가 도착 브로드캐스트에 실려 네 랭크에 같은 반복에 적용(캐시 상태 동일 유지); `probes/st_prefix_warm.py`. (F) `/metrics` 7 계열. CPU 판정: 잎 스필 → 퇴거 → 티어 복원(스냅샷 바이트 왕복 일치)·`submit(prepared)` → 접두 12 재사용; `shared_ahead` 가 진행 중 프리필의 앞 경계를 보고 캐시되면 None; 문: 같은 프롬프트 둘 동시 → 둘째 대기 1회·16 토큰 재사용, warm/pin/unpin, 티어 복원 1회 — 스위트 통과(아래). GPU·플릿 미검증(지시).
**호스트 경로 최적화(12:50, 리뷰 후속 "비슷하게 더", PR #598)**: 스텝마다·요청마다 반복되던 것 일곱. (1) 비동기 스텝 안 `stage_boundaries` 가 오프셋 표 4개를 매 스텝 `torch.tensor(..., device=cuda)` 로 만들었다 — 페이지 가능 H2D 복사는 스트림을 기다리므로 **앞서 띄운 스텝을 매번 기다리게 해 겹침을 없애던** 결함; 한 번 만들어 둔다. 파이프라인 `_shrink` 의 인덱스도 핀 + non_blocking, 호스트 스텝의 자리표시자 ids 는 n 별 캐시. (2) `caches.prepare` 의 블록 테이블 업로드가 페이지 가능 복사(행이 블록을 넘을 때마다 스트림 대기) → 핀 링 16칸 + 이벤트. (3) 문의 스트리밍이 매 스텝 활성 행마다 생성 전체를 슬라이스 복사(`generated()`) — 같은 것을 #597 이 먼저 고쳐(`generated_count`/`generated_since`) 리베이스에서 그쪽을 취함. (4) 이어가기 스캔이 파킹된 대화마다 티어의 JSON 기록을 매 요청 읽고 O(m) 리스트 비교 → 기록 메모이즈 + 마지막 토큰 한 개 필터. (5) sha1 체인을 요청당 4~5회, 대기 중 중복 요청은 스텝마다 재계산 → rank 0 이 한 번 계산해 도착에 실어 보내고(128K 프롬프트 3.4 KB) 입장·중복 대기·티어 후보·`submit` 이 그것을 읽음(`peek_chain`/`lookup_chain`/`tier_lookup_chain`). (6) 생성 경계마다 히스토리 전체 복사 + 체인 재계산 → `extend_chain` 이 마지막 경계 뒤 블록만 해시(`history_from`). (7) `maintain_prefix` 의 후보 정렬·growing 집합을 캐시 `version` 이 바뀌었을 때만. 스위트 443 OK. 발견 방식은 같다: 스텝 루프·입장 루프 안에서 크기가 프롬프트·생성·파킹 수에 비례하는 것을 찾는다.
**호스트 경로 2차(13:20, PR #601)**: (1) 동기 디코드(옵션 있는 행·이거 레인)의 `Step` ids 가 `torch.tensor(flat, device=cuda)` — 그래프가 어차피 정적 입력으로 복사하므로 핀 호스트 버퍼에 두면 그 복사가 비동기가 된다(페이지 가능 텐서는 만들 때 스트림 전체를 기다림). (2) 이어가기 스캔이 유휴 행마다 `history()` 로 128K 토큰을 복사한 뒤 한 토큰 필터를 적용 → `history_ref`(복사 없음, 비교만). (3) `adapter.validate` 는 입장 때 네 랭크가 루프 안에서 도는데 Python 루프가 120K 토큰에 3.8 ms; `array('q')`+min/max 는 오히려 4.7 ms(요소마다 PyLong 생성), numpy 변환+min/max 1.3 ms — dtype 을 강제하지 않아 float·문자열은 정수 배열이 안 돼 거부(잘림 없음). **측정하고 골랐다** — 첫 시도(array)는 더 느렸다. 스위트 450 OK. 남은 것은 GPU 쪽이라 창이 필요: (a) 프리필 마크가 KDA 되풀이를 청크당 9조각으로 끊어 KDA 커널 런치 ~9배(+2.2k 런치/스텝 ≈ 5~8% 추정) — 커널의 64-청크 상태 `h` 가 bf16 이라 스냅샷엔 못 쓰고, 요청한 청크의 fp32 상태를 옆 출력으로 내는 Triton 수정이 답; (b) 비동기 파이프라인의 행별 드래프터 제안 그래프(n 회 순차 재생) → n 행 배치 제안 그래프; (c) 파이프라인 꼬리(기각 샘플링·커밋·마스크 관측·제안, ~40 런치)의 그래프 캡처; (d) `distribution_batch` 의 정렬을 top_p<1 행에만.

**GPU 판정 3차(14:30, 운영자 "필요하면 측정해", PR #615)**: 프로덕션이 네 GPU 를 96% 쓰는 옆에서 작은 판정기만(직전 MemAvailable 확인·한 번에 하나·`--memory 10g`·timeout; 절대값은 잡음이라 **상대값만**). (1) **KDA 컷 대신 커널 옆출력**: 마크가 프리필 청크의 KDA 되풀이를 끊는 비용을 서빙 커널로 쟀다 — 층당 T=6,912 uncut 8.64 ms, 3×2,304 9.86 ms(+14%), 9×768 17.60 ms(**+104%**; 34층이면 스텝당 ~300 ms). 컷은 정확하다(출력·상태 max|d| 0 — 커널은 64-청크 경계에서 조각 합성이 비트 동일) 그러나 비싸므로 컷을 버리고 Triton `chunk_gated_delta_rule_fwd_h` 에 **요청한 청크 앞 상태를 fp32 로 내는 옆출력**(`states_at`/`N_AT`, 청크 상태 `h` 는 bf16 그대로, 본 계산 무변경)을 달았다: 옆출력 8개 있는 층 6.98 ms vs 없는 층 6.99 ms(**공짜**; 부하가 다른 두 번째 실행은 20.61 vs 19.99 — 프로덕션 옆 잡음 폭이 이만큼이라 차이는 잡음 안), 옆출력 == 잘라 돌린 prefix 의 최종 상태 **비트 동일**(8 마크 전부, 유한), 옆출력 유무에 출력·최종 상태 비트 동일, 같은 입력 두 번 비트 동일. 커널 vs torch 되풀이(fp32 토큰별)는 상태 max|d| 3.2e-3(상태 최대 0.66; `h` 가 bf16 인 커널의 원래 오차, 옆출력도 prefix 실행도 같은 값). 마크는 이제 64 토큰 커널 청크 위에만(`lanes.kda_chunk_tokens`; 768·2,304 는 배수) — 아니면 ValueError. 기준 레인은 조각을 이어 돌려 같은 계약을 낸다. **판정기 함정 하나(정직하게)**: 첫 판정은 "옆출력이 잘라 돌린 상태와 0.32 다르다, 컷이 상태를 0.3 옮긴다" 였는데 커널 오류가 아니라 **판정기 오류** — `dt_bias` 를 [D]=128 로 만들었고 커널은 [H·D] 로 읽으니 나머지 15 헤드 몫이 범위 밖 읽기(이웃 텐서 내용 → 실행마다 다른 게이트)였다. 형상을 고치니 전부 0. 교훈: 커널 판정기는 입력 형상을 프로필 계약(`lanes.py` 주석)에서 베껴 올 것. (2) `distribution_batch` 는 **nucleus(top_p<1) 행만 정렬**: 배치 분포가 n=4(×6 위치) 에서 정렬 있으면 4.58 ms, 없으면 0.70 ms(정렬 [24, 154,880] 만 2.91 ms; n=1 은 1.30 vs 0.23) — 그리디·온도만 행은 정렬 없음, 행 위치는 배치를 짤 때 장치 텐서로 한 번(스텝마다 호스트 리스트를 복사하면 페이지 가능 H2D 가 진행 중 스텝을 기다림). top-k 2,048 뒤 정렬(0.69 ms) 은 전체 정렬과 1.8e-2 차이라 채택 안 함. 기각 샘플링 pick 1.1 ms + commit 0.6 ms 는 스텝당 그대로. (3) 같은 창에서 판정한 것: 경계 스테이지 Triton 커널 == 링 체크포인트 비트 동일, 마스크 관측 의미(관측 안 한 행의 드래프터 셀 불변), 비전 타워 SM121 qualify 통과(fused SDPA 만; 최대 이미지 3.7 s·영상 9.1 s, 최고 4.24 GiB — 프로덕션 옆이라 절대값은 참고만). GPU 후보로 남은 것: 배치 제안 그래프, 파이프라인 꼬리 캡처, 비동기·768 블록 부팅의 플릿 검증(창 필요).

**GPU 판정 4차 — 드래프터를 행 배치로(15:40, 운영자 "추가 조사", PR #627)**: 디코드 스텝의 호스트·장치 경로를 다시 훑다가 가장 큰 것을 찾았다. **파이프라인이 드래프터(DFlash2, 5층 bf16 2.18 GiB, `DRAFT_TP=1` 이라 랭크마다 전부)를 행마다 따로 돌렸다** — 제안 그래프 n 회 재생, 관측 그래프 n 회 재생, 그리고 그래프마다 그 행의 40 MiB 링을 `index_select` 로 복사해 들어가고 `index_copy_` 로 돌려놓았다(행당 스텝당 링 복사 셋 = 120 MiB). 실측(프로덕션 96% GPU 옆이라 절대값은 잡음, 상대값만; 실물 가중치, 가짜 타깃 embed/head): 행 하나의 제안 그래프 재생 **40.5 ms**, 관측 그래프 4.1 ms + 링 복사 2.8/3.1 ms; 드래프터 GEMM 47개(2.03 GiB)를 B=6 행으로 한 번 읽으면 20.8 ms, **B=48(8행)이면 26.5 ms** — 가중치 읽기가 바닥이라 행을 묶으면 거의 공짜. 어텐션은 층당 einsum 경로 4.3 ms(2048 창의 K/V 를 fp32 로 펼치고 `repeat_interleave`) vs 융합 SDPA 3.2 ms. 프로덕션 vLLM 의 DFlash 는 행을 묶어 한 번(꼬리 7.5~8.5 ms/스텝, W4 GEMM) 돌았으니 이건 vLLM 대비 미흡 경로였다. **고친 것**: (1) `drafter.observe_rows / block_rows / propose_rows` — 모든 행을 한 번에: GEMM 은 [n·t] 행으로, 관측은 `index_put` 으로 필드의 셀에 **제자리 쓰기**(링 복사 없음), 어텐션은 **필드 전체를 한 번의 융합 SDPA**(cuDNN/efficient, D3 라 math 폴백 없음)로 — 행의 질의를 슬롯 자리에 흩고, 링은 **저장 순서 그대로** 읽고(키가 자기 위치로 rope 돼 있어 순서가 무의미), 창보다 짧은 컨텍스트는 키 마스크로, kv 헤드를 나눠 쓰는 헤드들은 그 kv 헤드에 대한 질의 행으로 펼쳐 헤드 복제도 없음. 블록 자신의 키는 링 뒤의 **여유 셀**(`ring_cells = window + block`, 링 +0.4%)에 두어 창+블록이 한 연속 구간이 된다. (2) `DrafterDecodeGraphs` 에 폭 1..max_seqs 의 `rows_masked / rows_propose / rows_sampled` 그래프(표본 걷기는 엔진 생성기를 그래프에 등록, 캡처 뒤 상태 복원); 파이프라인은 스텝마다 관측 한 번·제안 한 번. 온도 0 행은 표본 배치 안에서도 argmax(원-핫 분포). **판정(같은 창, 상대값)**: 제안 n=1 **17.1 vs 23.3 ms**, n=2 **17.1 vs 46.9**, n=4 **18.8 vs 101.2**(행별 ×4); 관측 1.9/1.9/4.1 vs 3.2/6.2/11.9; 표본 걷기 n=4 그래프 18.9 ms. n=4 스텝의 드래프터 몫 ~113 → ~23 ms. **정밀도(정직하게)**: 배치 경로는 행별 경로와 비트 동일하지 않다 — 층당 어텐션 차이는 bf16 1 ulp(efficient vs fp32 참조 max|d| 1.2e-4/3.5e-2) 인데 블록 5층을 지나면 어느 두 bf16 실현 사이에서도(행별 SDPA vs 행별 einsum 조차) hidden 의 중앙 상대차 4~7%, 최대 |d|/max|ref| 2~8% 가 되고, 임의 가중치·임의 링 판정에서 4 행 중 2 행의 드래프트가 바뀌었다. 같은 입력 두 번은 비트 동일(결정적)이고 네 랭크가 같은 그래프를 돌리니 랭크 간 동일. 드래프트는 검증이 지키므로 수용률 말고는 영향이 없고, 프로덕션 vLLM 의 DFlash(플래시 어텐션·W4)는 이보다 더 먼 실현이었다. 관측 쓰기의 차이(값 64~128 에서 0.25~0.5)도 M 이 다른 GEMM 의 bf16 1 ulp. **남은 레버**: 이제 스텝당 드래프터 바닥은 가중치 2.03 GiB 읽기(비경합 추정 ~12 ms) — W4/NVFP4 로 4배 줄이는 것이 다음이며 M=6..24 용 밀집 W4 GEMM 커널이 엔진에 없어 창·커널 작업이 필요. 티어 주의: 스냅샷 바이트가 +0.16 MiB 라 이전 부팅의 prefix/대화 티어 기록은 `promote` 의 크기 검사에서 거부돼 한 번 실패하고 잊힌다(조용한 오염 없음). **플릿 미검증**(창 필요): 새 그래프 3세트 × 폭 4 의 캡처, 웜업 사다리, null 슬롯의 NaN 격리.

**GPU 판정 6차 — GPTQ: 커널·패커는 철회, 자가 교정을 기본으로(19:30~21:00, 운영자 "gptq 패킹 진행" → "노브가 아니고 아예 기본으로 해버려, w4 랑 gptq 전부"; 노브 판은 PR #644 로 머지, 기본화는 PR #650)**: GPTQ 를 붙이려 main 을 다시 보니 **codex 의 PR #591(13:48 머지, "네이티브 TP4 채택")이 W4 레인을 통째로 먼저 가져와 있었다** — `engine/kernels/dense/`: 33차의 플릿 검증된 MK W4A8 디코드 커널(e2m1 × e4m3 스케일, CUDA 확장)·`pack_w4(hessian=)`(같은 캠페인의 `_w4_gptq_codes`, fp64 대칭화·댐핑 사다리)·`PackStore`(`/cache/mkcalib/rank<r>/<이름>.pt` 의 {H, ntok, name} 을 읽어 GPTQ, 없으면 RTN 으로 세어 둠)·`DenseLinear`(≤32 행 W4A8, FP8 프리필, NVFP4); 드래프터는 TP4 로 **샤딩**(qkv·gate_up 융합, o/down 은 K 분할 + all_reduce, 랭크당 링에 kv 헤드 둘)돼 그 레인으로 서빙되며 부팅 증명이 `drafter_w4 == 31` 을 요구한다. 그러니 **W4 는 이미 기본**이다. 내가 5차에 쓴 Triton W4A16 커널(int4 g32)과 이번에 쓴 `pack_w4_gptq`·`preshard --drafter-w4` 는 **같은 자리의 두 번째 구현**이라 철회했다(PR #636 닫음; D3·"서빙 형태는 하나"). 실수의 원인은 [[stkernel-parallel-sessions]] 규칙 그대로 — 커널을 쓰기 전에 origin/main 을 다시 보지 않았다(#591 은 내 분기점 29 분 뒤에 머지됐다). **남은 빈자리는 교정 데이터였다**: 스토어가 읽는 mkcalib 블롭은 물러난 vLLM 스택의 `MK_CALIB` 덤프인데, 이 노드(srv4)의 /cache 에는 없고(→ 지금 네이티브 부팅은 드래프터를 RTN 으로 팩; #591 의 README 는 자기 환경의 옛 캐시로 GPTQ 였다), 있어도 샤딩된 o_proj/down 의 K 와 맞지 않아 `incompatible calibration` 으로 부팅이 죽는다. 첫 판은 노브(`STK_drafter_calib`)로 켜는 교정 부팅이었는데 운영자가 "노브가 아니고 아예 기본으로" 라 해서 **자가 교정을 기본 동작**으로 다시 썼다(`engine/kernels/dense/calibration.py`): (1) 부팅이 아레나를 잡기 전에 `PackStore.missing_calibration(name, cols)` 로 **스토어에 교정이 없는 팩 타일**(K>4096 은 4,096 열 타일)을 드래프터(`drafter.dense_shapes`)·타깃(`net.dense_weight_names` + spec 형상) 순으로 모으고, 예산 2 GiB/랭크 안에서 아레나에 XᵀX 버퍼를 깎는다(넘치는 층은 다음 부팅으로 미룸 — 부팅마다 조금씩 채워져 수렴). 맞지 않는 옛 블롭은 파일명을 대며 죽인다(D3: 조용한 재교정 없음). (2) `DenseLinear` 에 `observer` 와 `rows_ok` 를 달아 **모든 호출자**(타깃·드래프터)의 입력이 한 자리에서 합산된다 — 캡처된 그래프 안 `addmm_`, 기여마다 장치 스칼라 `armed`(웜업·캡처가 쓰레기를 흘리므로 문이 열릴 때 1)와 행 마스크(파이프라인 유령 행 `alive` → 제안 그래프 입력, 관측의 `valid` 너머)를 곱하고, 마스크 없이 ≤32 행이 올 수 있는 층(타깃의 디코드: null 슬롯 유령 행)은 **프리필 행만**(33차 덤프도 프리필 행) 합산. 모든 랭크가 자기 것(샤딩된 투영은 랭크마다 다른 열). (3) 파일링은 **스스로**: 스텝 루프의 `housekeeping` 훅(base/serve 가 256 스텝마다 부름, 장치 읽기 한 번)이 모든 블롭이 32,768 행을 넘으면 `<루트>/mkcalib/rank<r>/…pt` 로 쓰고, 종료 때도(4,096 행 미만이면 굶은 헤시안이라 안 씀), `POST /v1/engine/calibration` 으로도. 다음 부팅의 스토어가 그대로 GPTQ 팩을 짓는다 — 노브도 프리샤드도 없다. `/metrics` 의 `st:lane_info{packs="gptq N rtn M", calibration="collecting|filed|complete …"}` 가 무장≠서빙을 증명. **판정(GPU, 프로덕션 옆)**: 관측기의 블롭을 실제 `PackStore` 가 읽어 `{'built': 1, 'gptq': 1}`(K=4096, 5.0 s), 저장 뒤 `missing_calibration` 은 빈 목록; 상관 입력(랭크 64 혼합, N=1024)에서 같은 분포의 새 입력 출력 상대 오차 **RTN 0.082 → GPTQ 0.009**(main 의 e2m1 패커; 저랭크 합성 입력이라 상한, 실 활성은 33차 기준 −69~−74% 가 기대치); 64 스텝 × 24 행의 합산 94 ms(스텝당 1.5 ms, K=4096 한 타일 — 랭크당 ~35 타일이면 교정 부팅의 스텝은 수 ms 느리고, 파일링 뒤 재부팅하면 사라진다). CPU 판정: arm 전엔 0, 마스크된 행만, 작은 호출은 허용된 층만, 넓은 입력은 타일별, 예산 초과 층은 유예, 블롭 형식·스토어 왕복, 맞지 않는 블롭 거부, 드래프터 배치 경로가 넘기는 마스크, 노브 목록은 원래대로 둘. **절차(기본)**: 재부팅 두 번이면 끝 — 첫 부팅이 트래픽 30K 토큰 안팎에서 스스로 파일링(로그 "calibration: rank r filed …"), 두 번째 부팅의 `store.stats` 가 gptq 로 채워진다; 그 뒤 tok/s·raw acc 브래킷.

**GPU 판정 7차 — GPTQ 를 더 넓게: act-order·전체 K·NVFP4 프리필 레인(21:30~23:00, 운영자 "gptq 더 여러곳에 적용 가능한가" → "최선의 길로 해", PR #659)**: 양자화되는 가중치를 전부 놓고 보니 GPTQ 가 닿는 자리는 (1) 밀집 투영의 디코드 팩(W4A8, 이미 GPTQ), (2) 같은 가중치의 프리필 레인 FP8·NVFP4(RTN), (3) MoE 전문가(체크포인트가 RTN NVFP4, bf16 원본 없음), (4) 헤드(FP8). 최선은 **패커 개선(모든 팩에 동시에) + NVFP4 레인 승계**; FP8 레인은 GPTQ 해(e2m1 격자)를 fp8 로 재양자화하면 보정된 8% 위에 보정 안 된 3% 가 얹혀 bf16→fp8 RTN 보다 나빠지므로 **제외**(같은 논리로 GPTQ 해는 fp4 격자에서만 승계 가능), 전문가는 원본이 없어 제외. **(A) act-order + 정적 그룹**(`_w4_gptq_codes(act_order=)`, 기본 켬): 열을 헤시안 대각 내림차순으로(가장 중요한 입력부터 반올림, 그 오차를 나머지가 흡수), 그룹 스케일은 RTN 패커의 것을 걷기 전에 고정(커널의 16-그룹은 그대로). 실물 드래프터 가중치 셋 × 합성 헤시안 둘(랭크 64 상관; + 33차의 "이상치 채널 20배"): 이상치 없으면 plain 과 같고(0.010 vs 0.010, RTN 대비 목적함수), **이상치 있으면 4~9배 낫다**(q_proj 0.012 → 0.002, down 0.009 → 0.001, gate 0.004 → 0.002; 출력 상대오차 0.0093 → 0.0039) — 실 헤시안이 그 모양이다. 게다가 **더 빠르다**(3.2 vs 5.2 s: 루프 안 그룹 탐색이 없다). **(B) 전체 K GPTQ**(`pack_w4_wide`, `PackStore.pack_wide`): 커널 타일 4,096 보다 넓은 가중치(드래프터 fc 20,480 = 다섯 타일; 입력이 타깃 다섯 층의 hidden 이라 타일 사이 상관이 큼)를 **전체 헤시안**으로 한 번에 — 교정도 타일이 아니라 가중치당 블롭 하나(20,480², 1.6 GiB; 랭크당 예산 2 GiB 안). 인수분해는 fp64 로 CPU 에서, **fp64 행렬 둘까지만**(재시도는 원본을 다시 읽음; 첫 판은 셋+순열 사본으로 20 GiB 컨테이너를 죽였다): 합성(타일 가로지르는 상관)에서 타일별 0.001 → 전체 0.000(목적함수/RTN), 출력 상대오차 0.0029 → **0.0023**(−21%), 77 s vs 16 s(부팅 한 번, 캐시). **(C) NVFP4 프리필 레인이 GPTQ 해를 승계**(`nvfp4_from_w4`): W4 팩의 니블은 e2m1 그 자체이고 그룹 스케일 e4m3 × 행 시프트 × 2 의 거듭제곱 전역 스케일은 e4m3 로 정확히 표현되므로(1,048,576 그룹 중 **부정확 0**) `mm_fp4` 가 **디코드 커널과 같은 GPTQ 해**를 곱한다 — bf16 을 새로 RTN 하던 것을 대체. 남는 차이는 W4 커널이 곱(코드×스케일)을 e4m3 로 반올림하는 것뿐(rms 1.9%). 실 q_proj + fp4 활성(flashinfer 양자화)에서 출력 상대오차 **RTN-NVFP4 0.112 → 팩 승계 0.064**, 활성의 fp4 오차만으로 0.061 — 가중치 쪽 오차가 활성 오차 밑으로 거의 사라졌다. **(D) 배관**: `W4Pack.calibrated`, `DenseLinear.calibrated/nvfp4_inexact`, 스토어 `calibrated()`·팩당 블롭 하나, 부팅 게이지 `dense_calibrated / dense_nvfp4_from_packs / dense_nvfp4_inexact_groups`, `lane_info{nvfp4_from_packs}`. 스토어의 알고리즘 해시가 바뀌므로 다음 부팅은 팩을 전부 다시 짓는다(교정 있는 곳은 GPTQ ~3 s/팩: 옛 덤프가 있는 노드라면 ~11 분 한 번). **정직하게**: 셋 다 합성 헤시안 판정이고 실 수용률·프리필 품질은 플릿 창의 브래킷 몫; NVFP4 승계는 e4m3 곱 반올림 차이(1.9%)를 남기며, 이상치 없는 입력에서 act-order 의 이득은 0 이다. CPU 스위트 682 OK(교정 테스트는 가중치당 블롭 하나로).

**GPU 판정 8차 — FP8 레인은 NVFP4 로 가지 않고, 자기 격자에서 GPTQ 한다(23:30, 운영자 "정확도가 올라갔으니 fp8 레인도 nvfp4 화 시키면 어때", PR #661)**: 같은 입력(상관 + 이상치 채널, 33..1023 행 레인의 512 행)에서 세 가지를 쟀다 — 실물 드래프터 q_proj 와 타깃 L3.mla.o_proj(랭크 0 샤드), bf16 진실 대비 출력 상대오차. **FP8 W8A8(지금, RTN 가중치) 0.0375 / 0.0364** = 가중치 몫 0.027 + 활성 몫 0.026; **NVFP4 W4A4(7차의 GPTQ 팩) 0.064 / 0.075**, 그중 활성 fp4 몫이 0.061 / 0.071 — 가중치가 완벽해도 fp4 활성 하나가 fp8 레인 전체보다 크다. 그러니 FP8 → NVFP4 는 짧은 프롬프트(1,023 토큰 이하: 대화의 대부분)의 프리필 오차를 **두 배**로 만든다. 답은 반대쪽: **fp8 격자 위의 GPTQ**(`packing.fp8_gptq`): 서빙 레인의 128×128 UE8M0 블록 스케일은 그대로(정적), 열마다 그 블록 스케일 아래 e4m3 로 반올림하되 헤시안 대각 내림차순(act-order)으로 오차를 되먹인다 → 가중치 몫 **0.027 → 0.0009**, 합계 **0.0375 → 0.0264**(활성 몫만 남음), 실제 DeepGEMM fp8 GEMM(`FP8Linear` 에 준비된 가중치를 물림)으로 0.0266. 엔진의 fp8 RTN(`fp8_block_scales/fp8_rtn`)은 DeepGEMM 의 `per_block_cast_to_fp8(use_ue8m0=True)` 와 **코드·스케일 바이트 동일**(세 가중치 전부 0 바이트 차이) — 패커가 라이브러리 형식을 재현한다. **헤드도**: `FP8Linear` 로 서빙되는 어휘 헤드(운영자가 W4 는 접었음)를 같은 방법으로, 랭크 0 샤드 [38,720, 4,096] 에서 0.0377 → **0.0269**, 3.0 s — 헤드는 `Glm5NextForCausalLM/lm_head` 로 자가 교정에 든다(행은 배치 자체라 마스크 없이 합산). **배관**: `PackStore.pack_fp8`(가중치·헤시안·패커 신원으로 캐시; 3.1 s 생성, 0.06 s 재적재), `DenseLinear` 의 FP8 레인과 `net.prepare_dense` 의 헤드가 교정이 있으면 그것을 씀, `FP8Linear(quantized=, name=)` + observer, 게이지 `dense_fp8_calibrated`, `lane_info{fp8_gptq}`. 넓은 K(fc 20,480)는 CPU fp64 인수분해. 이로써 **밀집 가중치의 세 레인(W4A8 디코드·FP8 프리필·NVFP4 큰 M)이 전부 같은 교정에서 나온 GPTQ 해**를 쓴다. 정직하게: 합성 헤시안 판정이고, fp8 GPTQ 의 이득은 활성 fp8 오차(0.026)에 막혀 합계 −30% 가 상한이다.

**조사 9차 — 같은 방식으로 더 줄일 곳(00:10, 운영자 "같은 방식으로 오차를 더 줄일수 있는 곳 없어?", 판정만 — 원장만이던 PR #664 는 닫고 PR #669 에 실었다)**: 세 레인의 가중치 몫이 0.001 수준이 된 뒤 남은 오차는 활성 양자화다. 교정 통계로 채널 이상치를 가중치로 옮기는 SmoothQuant(α=0.5: s_k = amax_x^α / amax_w^(1−α), W·diag(s) 를 GPTQ, X/diag(s) 를 양자화)를 같은 입력(상관 + 이상치 채널 20배, 실물 q_proj·o_proj)에서 재 보니: **FP8 W8A8 0.0268 → 0.0262(효과 없음)**, **W4A8 디코드(행별 활성 스케일) 0.0207 → 0.0211(없음)** — 둘 다 e4m3 의 가수 3비트가 바닥이라 이상치를 옮겨도 남는 게 없다; **NVFP4 W4A4(큰 M 프리필) 0.064 / 0.072 → 0.046 / 0.050(−28%)** — fp4 는 가수 1비트라 16-그룹 안의 이상치가 그룹 전체를 망치고, 그것은 옮길 수 있다. α=0.75 는 더 못하다. **MoE 전문가 체제**(체크포인트의 RTN NVFP4 가중치 + fp4 활성, 실물 sh_gate_up 으로 흉내): 0.113 → 채널별 평활 **0.059(−48%)** 이지만 bf16 전문가가 있어야 접을 수 있고, e4m3 블록 스케일에 정확히 접히는 16-그룹 2의 거듭제곱 평활은 **0.114(효과 없음)** — 그룹 단위론 이상치 채널을 고립시키지 못한다. **결론**: 같은 방식으로 남은 자리는 하나, NVFP4 큰 M 레인의 채널 평활(관측기에 채널별 amax, prepare 때 s 를 앞 norm 가중치에 접어 런타임 비용 0 — norm 이 앞에 없는 o_proj·down·fc 는 명시 곱이 필요, 팩 신원에 s 포함); 가장 큰 오차 덩어리(전문가 0.11)는 bf16 원본 + 전문가별 라우팅 토큰 교정(평활 + GPTQ → 추정 ~0.04)이라는 다른 규모의 일이다. 판정기 주석: 평활한 행의 "weights only" 열은 평활 전 입력으로 곱해 무의미(합계·활성 열만 유효).

**GPU 판정 10차 — NVFP4 큰 M 레인의 채널 평활, 앞 norm 에 접어서(01:30, 운영자 "진행", PR #669)**: 9차의 남은 자리 하나. `engine/kernels/dense/smoothing.py`: 교정의 채널별 amax(관측기가 헤시안 옆에 합산; 블롭에 `amax`, 없는 옛 블롭은 다시 합산하되 H 는 그대로 팩)로 SmoothQuant 계수 s_k = amax_x^0.5 / amax_w^0.5 를 만들고, **앞 RMSNorm 의 가중치를 s 로 나눠 두고 그 norm 출력을 읽는 모든 가중치의 열에 s 를 곱한다** — 런타임 비용 0, x·Wᵀ 는 그대로. **읽는 이를 하나라도 빠뜨리면 그 가중치는 준비 안 된 입력을 보므로** 층 종류마다 소비자 집합을 박았다(`Glm53Net.smoothing_groups`): KDA 층 in_norm → in_proj; MLA 층 in_norm → qkv_a + 인덱서의 wk·gate(bf16, 제자리 재스케일), q_a_norm → q_b + 인덱서 wq_b; 밀집 MLP 층 post_norm → gate_up; **MoE 층의 post_norm 은 라우터와 전문가도 읽으므로 안 한다**. 드래프터(`Drafter.smoothing_plan`): input_layernorm → attention_conv 커널 투영 + (그룹 conv 를 지나) q/k/v, post_attention_layernorm → mlp_conv 커널 투영 + gate/up — 그룹 conv 는 채널별 선형이라 계수가 통과하고, **컨텍스트 투영(context_kv)은 평활 안 된 k/v 원본으로**(그 입력 c 는 나눠지지 않으므로). 스토어는 `smooth` 를 받아 헤시안을 diag(1/s)H diag(1/s) 로 바꾸고 신원에 s 의 해시를 넣으며, 관측기는 이번 부팅의 합을 **평활 전 도메인으로 되돌려 파일링**(H → sHs, amax → amax·s)해 부팅마다 s 를 처음부터 다시 구한다(이중 평활 방지). **계수는 2 의 거듭제곱으로 반올림**: 연속 계수의 첫 판은 실물 드래프터 블록 출력을 rms 16% 움직였는데 — 접기 자체의 bf16 반올림(조각당 0.3~0.5%, 작은 드래프터 1%)이 5 층을 지나며 증폭된 것(4차의 민감도 그대로) — 2 의 거듭제곱이면 norm 나눗셈도 열 곱도 bf16 에서 정확해 곱이 하나도 안 바뀌고 **블록 출력이 비트 동일**(작은 드래프터·실물 드래프터 모두 0.0). **판정**: 실물 드래프터 융합 qkv 의 NVFP4 W4A4 레인, 상관 + 이상치 채널 입력에서 **0.0740 → 0.0566(−23%)**, 활성 fp4 몫 0.0718 → 0.0522(연속 계수는 0.0556: 거듭제곱의 손해는 1% p); FP8·W4A8 레인은 9차대로 ±0. 게이지 `dense_smoothed`, `lane_info{smoothed}`. CPU 판정: 접기의 비트 동일(norm→독자, 타깃 MLA 그룹의 세 독자, 드래프터 블록 전체), 그룹 집합(층 종류별, MoE 제외), 관측기의 amax 와 되돌린 파일링, 옛 블롭 재합산. **정직하게**: 합성 헤시안·합성 amax 판정이고 실 입력의 이상치 구조가 이득을 정한다; o_proj·down·fc·sh_gate_up 은 앞에 norm 이 없거나 공유돼 평활하지 않는다(명시 곱은 청크당 수십 ms 라 보류).

**GPU 판정 11차 — GPTQ 라운드 전부를 실제로 기본으로: 교정 계획이 한 부팅에 들어오게(운영자 "gptq 라운드 전부 디폴트화", PR #673)**: 6~10차는 코드에 노브가 없어 이미 기본이지만 **플릿에서 실제로 켜지는 것은 그중 일부**였다. 네 노드의 `/cache/mkcalib/rank<r>/` 에는 33차 vLLM 스택의 덤프가 랭크별로 남아 있고, 형상을 하나씩 대 보니 **전부 맞는다** — 드래프터도 그때 이미 TP4 로 샤딩돼 덤프됐다(o_proj K=1024 = heads/4·D, down K=3072 = I/4). 6차에 "샤딩된 o_proj/down 의 K 와 맞지 않아 `incompatible calibration` 으로 죽는다" 고 적은 것은 **틀렸다**(부적합 0건). 그래서 228 개 팩 타일 중 **204 개는 헤시안을 이미 갖고 있다**. 문제는 그 블롭에 10차가 읽는 **채널 피크(`amax`)가 없다**는 것: `missing_calibration` 이 그것들을 통째로 "없음" 으로 돌려주므로 부팅은 [K] 짜리 피크를 얻으려고 [K,K] 헤시안 버퍼를 새로 잡는다 — 계획 **9.96 GiB** 대 랭크당 예산 2 GiB, 한 부팅에 **20/228** 만 들어가고 나머지는 다음 부팅으로. 평활은 그동안 **어느 가중치에도 안 걸린다**. **고침 (A)**: `store.Need(key, start, width, hessian)` — 스토어에 맞는 헤시안이 있는 타일은 **피크만** 합산한다(버퍼 [K] floats = 20 KB, 헤시안 67 MiB 대신). 계획 **1.82 GiB**, 예산 2 GiB 안에서 **228/228 이 한 부팅에**(남은 24 개는 애초에 헤시안이 없는 것들 — 드래프터 fc 20480² 1.56 GiB, 헤드, MLA q_b 와 인덱서 wq_b). 파일링은 디스크의 헤시안과 그 토큰 수를 **그대로 두고** 피크만 얹으며(테스트가 H 바이트 동일을 건다), 임시 파일 + `os.replace` 로 쓴다 — 쓰다 죽은 부팅이 잘린 블롭을 남기면 다음 부팅이 D3 로 죽기 때문. **고침 (B) 팩 시간**: 한 가중치의 W4 레인과 FP8 레인은 **같은 H 의 같은 열을 act-order 로 걷는데 fp64 인수분해를 각자** 했고, W4 쪽은 블롭이 호스트에 로드돼 있다는 이유만으로 **CPU 에서** 했다. `packing.gptq_factor` 가 (순열, U, dead) 를 한 번 만들어 둘에 넘긴다(`store._factor`: 항목 하나, 다음 가중치가 갈아치움; 256 MiB 넘는 인수분해는 쓰고 버린다 — 드래프터 fc 의 1.6 GiB 를 가득 찬 아레나 옆에서 들고 있지 않는다), 타일 폭 K 는 가중치의 장치에서. **판정(실물 랭크3 가중치 넷, 프로덕션 옆, 후보 교차 반복 5회 최솟값)**: 가중치당 **−10~29%** (mla.o_proj 2.44 → 1.73 s, kda.in_proj 2.69 → 2.14, moe.sh_gate_up 4.00 → 3.14, moe.sh_down 0.47 → 0.43), W4 출력 상대오차는 그대로(0.0031 → 0.0031), fp8 코드는 지금 것과 **바이트 동일**. 스토어 알고리즘 해시가 바뀌어 첫 부팅이 랭크당 204 개를 다시 굽는데(10 분대), 그 값이 그만큼 준다. 게이지 `calibration_blobs`(전체 타일)와 `calibration_hessians`(그중 헤시안을 새로 만드는 것), 스토어 통계 `factor_built/factor_reused`. CPU 스위트 **758 OK**. **끝에서 끝까지(실물 블롭, 프로덕션 옆, PR #677)**: 이 노드 랭크3 의 진짜 33차 덤프(`…layers.3.self_attn.o_proj`, H 4096², ntok 17,189, amax 없음)를 임시 스토어에 놓고 기본 경로를 그대로 돌렸다 — 스토어가 `Need(hessian=False)` 를 돌려주고, 교정이 Gram 버퍼 0 개에 **20.0 KiB**(헤시안이었다면 64 MiB)를 잡고, 32,768 행을 합산해 파일링하니 **H 가 덤프와 바이트 동일이고 ntok 도 그대로**, 피크 [4096] 만 새로. 그 블롭으로 다음 부팅을 흉내내면 `missing []`, 그리고 한 가중치에서 **세 레인이 모두** 나온다: W4 GPTQ·NVFP4 승계(부정확 그룹 0)·FP8 GPTQ·평활 적용, 스토어 통계 `factor_built 1, factor_reused 1`(두 레인이 인수분해를 **하나만**). NVFP4 프리필 출력 상대오차 0.0590. 인수분해를 공유할 때 순열과 `act_order` 가 어긋나면 죽도록 막았다(내가 남긴 함정: 어긋난 채로 부르면 `sc_static` 미정의로 NameError 였다). **정직하게**: "한 부팅" 은 계획이 예산에 다 들어온다는 뜻이고 32,768 행을 채우는 속도는 트래픽이 정한다; 팩 시간은 프로덕션이 GPU 를 쓰는 옆에서 잰 상대값이다; 피크는 블롭마다 한 번 모이고 다시 갱신되지 않는다(재교정은 블롭을 지우는 것).

**조사 12차 — 평활이 더 갈 곳과, 실제 트래픽의 채널 구조(운영자 "추가 개선해", 판정만 — PR #686 에 함께 실렸다)**: 10차가 남긴 자리는 "앞에 norm 이 없는" o_proj·down·fc·sh_gate_up 이었다. 코드를 보니 o_proj 둘은 **선형 생산자**가 있어 명시 곱 없이 정확히 접힌다: MLA 는 `o = einsum(ctx_lat, w_uv)` 이므로 `kv_b` 의 v 행(bf16, 양자화 안 됨)을 1/s 로 나누면 **채널마다** 정확히 접히고, KDA 는 `kda_output_norm` 의 가중치가 [D] 라 **머리마다 공유**되므로 head-dim 마다만 접힌다. down·sh_down 은 **불가** — `swiglu_clamped` 가 u 를 ±10 으로 자른 뒤 곱하므로 u 행을 나누면 잘리는 지점이 바뀌어 서빙 함수가 달라진다. 그래서 두 o_proj 만 실측했는데, 입력을 **실물 생산자 가중치로** 만들어 보니(KDA: 실제 o_norm 으로 게이트된 RMS 출력, MLA: 실제 w_uv 를 지난 latent) **±0 이었다**: KDA 0.1280 → 0.1280(head-dim) / 0.1297(채널), MLA 0.0765 → 0.0770. 이유는 입력의 채널 피크 퍼짐이 각각 **1.4 배·1.9 배**뿐이라는 것 — 옮길 이상치가 없다. 그래서 합성이 아니라 **실제 트래픽**을 보았다: 이 노드의 33차 덤프 215 개에서 sqrt(diag(H)) = 채널별 rms 의 max/median. **드래프터가 이상치의 집이다** — qkv 25~61, mlp_conv/attention_conv 커널 투영 10~61, mlp.down 14~44, gate_up 11~32, fc.k2~k4 13~19. **타깃은 앞쪽 몇 층만** 크다(L0~L3: o_proj 31·6.5, down 58.7·19.1·15.9, gate_up 14.6·11.2) 그리고 **나머지는 평평하다**(깊은 층의 in_proj·sh_gate_up 1.8~3.5, sh_down 1.0~1.7). 즉 **10차의 −23% 는 합성 이상치(×20) 위의 수치이고 실서빙의 타깃에서는 그만큼 나오지 않는다**; 반면 10차가 같이 넣은 드래프터 평활은 이상치가 실제로 있는 자리다. 결론: 평활은 여기서 끝. o_proj 두 접기는 **가능하지만 가져갈 것이 없어** 안 한다(기록만).

**GPU 판정 13차 — NVFP4 밀집 프리필 레인을 걷어낸다: 사는 것이 없고 6~11차가 산 정확도를 쓴다(운영자 "추가 개선해", PR #686)**: 남은 가장 큰 밀집 오차는 가중치가 아니라 **NVFP4 레인의 fp4 활성**이었다(8차 이후 고정된 사실). 그 레인은 39차 §4g P3C2 가 **vLLM 스택의 처리량만 보고**(프리필 +3.5~5.1%) 기본으로 올린 것이다. 이 엔진의 FP8(DeepGEMM) 레인과 **이 랭크가 실제로 서빙하는 여덟 형상 전부**에서 다시 쟀다(프로덕션 옆, 후보를 repetition 마다 교차, 최솟값 8회): 형상별로 `moe.sh_gate_up`[1024,4096]×42 **0.64~0.87배**, `moe.sh_down`[4096,512]×42 **0.66~0.90배**(NVFP4 가 더 느리다), `kda.in_proj`[6416,4096]×34 1.20~1.32배, `mla.o_proj` 1.02~1.25배, 나머지는 0.80~1.21배. 인구를 가중해 합치면 오늘의 전-NVFP4 대비 전-FP8 이 M=1024/2048/4096/8192 에서 **+3.7% / −0.1% / +0.4% / −2.3%** — **무승부**다(형상마다 빠른 쪽을 고르면 −9.4~9.9% 지만 그건 레인 둘을 다 들고 가는 값이다). 정확도는 무승부가 아니다: 실물 가중치 + **실물 교정 헤시안의 공분산**으로 뽑은 입력에서 kda.in_proj 0.0382 대 0.0104, mla.o_proj 0.0761 대 0.0207, moe.sh_gate_up 0.0701 대 0.0193 — **FP8 이 3.65배 정확하다**(NVFP4 쪽은 7차의 팩 승계를 쓴 뒤의 값이다). 사는 것이 0 이고 쓰는 것이 3.65배면 레인이 아니다. **걷어냈다**: `DenseLinear` 의 세 번째 가지와 `nvfp4=` 인자(드래프터는 이미 False 였다), `packing` 의 `nvfp4_from_w4`·`w4_rowmajor`·`_fp4_encode_nibbles`(58 줄), 게이지 `dense_nvfp4_from_packs`·`dense_nvfp4_inexact_groups`, `lane_info{nvfp4_from_packs}`, 부팅 증명의 `target_nvfp4`(이제 가중치마다 W4 + FP8 둘). 그 레인의 PDL 의존성 해저드를 박아 두던 `tests/test_engine_nvfp4_order.py` 도 레인과 함께 사라진다. **결과**: 실물 L3.mla.o_proj 의 프리필 행이 0.0761 → **0.0207**, 디스패치는 "≤32 행 W4A8, 그 위 FP8" 둘뿐(D3: 서빙 형태는 하나), 그리고 이 랭크에서 레인이 들고 있던 **fp4 가중치 사본 0.98 GiB** 가 없어진다. CPU 스위트 **782 OK**. **정직하게**: 첫 측정(비교차)은 전-FP8 이 10~34% 빠르다고 했는데 그건 프로덕션 경합 편향이었다 — 교차 반복의 답이 무승부다; 밀집 GEMM 은 프리필 스텝의 10% 안팎이라 종단 처리량 변화는 그보다 작다; 라우팅 전문가의 NVFP4(b12x)는 체크포인트가 fp4 라 그대로다.

**조사 14차 — 프리필 구성표와, 라우팅 전문가 레인이 실제로 무엇에 묶여 있나(운영자 "개선해"→"전문가 레인 꼬리", 판정만 — PR #692)**: ST 프리필은 옛 vLLM 기준선의 **79%**(2,028~2,117 vs 2,696 tok/s)다. 그 21% 가 어디인지 보려고 서빙 청크(6,912 토큰 = 블록 3개, 3.27 s)를 구성요소별로 쟀다(프로덕션 옆, 실물 랭크3 가중치, 최솟값): **라우팅 전문가 42층 0.82 s(25%)**, **MLA 희소 어텐션 11층 0.42 s(13%)**, **KDA 청크+conv+o_norm 34층 0.28 s(9%)**, **밀집 GEMM 202개 0.27 s(8%)**, kv_b 아인슈타인합 0.04 s, **미측정 1.44 s(44%: mHC·집합통신·인덱서·라우터·호스트)**. 2026-09-03 vLLM 프로파일(32K, 층-청크당)과 대면 **ST 의 MLA 어텐션은 76 ms → 38 ms 로 절반**(39차의 prefill32 register-Q 타일이 ST 에 있다), KDA 는 5.4 → 7.0 ms. **전문가 레인의 정체**: 같은 층·같은 T 에서 라우팅만 바꿔 쟀더니 시간이 일의 양을 안 따라간다 — 전문가마다 **정확히 192 행**으로 같고 타일 1,728개·패딩 행 55,296 으로 **완전히 동일한데**, 토큰이 규칙적으로 성기면(`sel[t] = (8t+j) mod 288`) **12.78 ms**, 같은 배정을 무작위로 섞으면 **19.98 ms**(+56%). 균일 난수 20.65, 교정 공분산에서 뽑은 실물 라우터 19.61(max 2,296·median 40·활성 216) — **실 라우팅은 나쁜 쪽 끝에 있고, 행 수 쏠림(max/median 57배)은 시간을 거의 안 움직인다**(균일 난수와 실물 라우터가 같은 자리). 즉 이 레인은 **일의 양이나 전문가 불균형이 아니라 토큰 배정 무늬**에 묶여 있다. **고쳐 보려다 둘 다 실패**: (1) 전문가마다 연속 토큰 블록 **25.96 ms**, (2) 실물 카운트 그대로 두고 토큰을 전문가 정렬로 재배치 **24.91 ms** — 둘 다 더 나쁘다. 이유는 그 구성들이 전문가의 192 행을 **소수의 서로 다른 토큰**으로 접어(각 토큰이 8번씩) 에필로그 scatter-add 가 같은 출력 행에 몰리기 때문이다. 게다가 디스패처는 **이미 A 를 전문가 major 로 팩한다**(`packed_input [state_E, max_rows, k/2]`), 그래서 "A 를 미리 정렬해 두자" 는 빠진 조각이 아니다. (3) 같은 라우팅·같은 수학에서 **활성값의 행 순서만** 바꿔도(top-1 전문가로 정렬, 전문가 집합으로 정렬; 출력은 역치환해 값이 같음을 확인) **0%·−1%** 로 꿈쩍하지 않는다 — 즉 메모리 순서가 아니다. strided 가 빠른 것은 그 구성에서 **한 토큰의 8 전문가가 연속된 id** 라는 점뿐이고, 그것은 실 라우팅이 가질 수 없는 성질이다. 커널 스케줄도 범인이 아니다: 항목은 (전문가, m-타일, n-블록) 단위로 균등하고(`row % tile_m == 0` 마다 `next_item += output_tile_count_n`), CTA 는 `bidz` 에서 `gdim_z` 씩 건너뛰며 라운드로빈으로 가져간다. **결론**: 층당 ~7 ms(청크의 0.29 s, 프리필의 9%)가 게이트·양자화 프리패스 안의 무늬 의존 비용으로 남아 있다 — 호스트 재정렬로는 못 꺼내고 커널 프로파일이 필요하다. **그리고 이것은 21% 격차의 원인이 아니다**: 옛 vLLM 스택도 같은 커널에 같은 실 라우팅을 물렸다. 21% 는 아직 안 잰 44% 안에 있다(vLLM 표에서 그 자리는 mHC 11.4% · NCCL AllReduce 16.1% · elementwise 8.9% 였다). **정직하게**: 절대 시간은 부팅마다 흔들린다(같은 strided 구성이 플릿이 붙어 있을 때 18.01, 비었을 때 12.78) — 유효한 것은 한 실행 안의 비교뿐이고, 위의 배수는 전부 한 실행 안에서 잰 것이다.

**GPU 판정 15차 — 구성표의 나머지(mHC 7%·인덱서 2%)와, 풀 선택이 필요 없는 정렬을 사던 것(운영자 "다른 곳 개선", PR #693)**: 14차의 미측정 44% 를 둘 더 깎았다. **mHC**(`lanes.mhc_pre/post`, tilelang): 청크에서 pre 2.77 ms + post 2.38 ms, 모듈 89개 → **0.229 s(청크의 7%)** — vLLM 표의 11.4%(청크당 384 ms)보다 **40% 낫다**. 형상을 여기 박아 둔다: `hc.attn_fn` 은 `[(2+hc)·hc, hc·hidden] = [24, 16384]` 이므로 **hc 는 마지막 차원이 아니라 그 식에서 나온다(hc=4, hidden=4096)** — 이걸 16384 로 읽어 `[6912, 16384, 4096]`(927 TB)을 요청했다가 earlyoom 이 프로덕션 워커를 쏴 플릿이 내려갔다([[gb10-docker-memory-does-not-bound-cuda]]: `--memory` 는 CUDA 통합메모리를 못 막는다). **DSA 인덱서** 11층: quant 0.45 + kpool_compress 0.10 + logits 1.10 + **top-k 선택 4.85** = 6.25 ms/층 → 0.069 s(2%). 즉 **인덱서 시간의 74% 가 점수 계산이 아니라 고르기**다. 누적 구성표(청크 3.27 s): 전문가 0.82(25%) · MLA 0.42(13%) · KDA 0.28(9%) · 밀집 GEMM 0.27(8%) · mHC 0.23(7%) · 인덱서 0.07(2%) · kv_b 0.04 → **2.13 s(65%) 설명됨**, 남은 1.14 s(35%)는 집합통신(랭크 넷이 필요해 단독 측정 불가)·라우터·elementwise·호스트. **여기까지에서 ST 가 vLLM 보다 나쁜 곳은 KDA 하나뿐이다**(7.0 vs 5.4 ms/층-청크). **고친 것**: `sparse_indexer.topk_positions` 는 정렬된 top-k 를 사고 있었는데 **뒤의 `pool_slots` 가 어차피 토큰 위치 내림차순으로 다시 쓴다** — 필요한 것은 집합뿐이다. `sorted=False` 로 바꾸고, 패딩 표시도 정렬이 돌려준 fp32 값의 `isinf` 대신 **int32 승자에 대한 위치 비교**로 바꿔 [rows, k] 를 두 번 덜 지난다. 실물 형상 판정(프로덕션 옆, 최솟값 8회): 청크 [6912, 1728]×512 **4.85 → 3.86 ms(−20%)**, 32K 문맥 [6912, 8192] 14.49 → 13.54(−7%), 디코드 형상 −5~6%; **고른 집합은 전부 동일**(랜덤 valid 포함, CPU 테스트가 건다). 청크 기준 11층 × 1 ms = 11 ms(프리필의 0.3%)로 작지만 공짜다. **다음 레버는 그 선택 자체다**: [6912, 8192] fp32 216 MB 를 14.5 ms 에 고르는 것은 **15 GB/s**, 대역폭 바닥의 30분의 1이다 — 정렬 없이 행마다 k 번째 값을 히스토그램으로 찾고 그 위를 모으면 두 번 지나면 되고(≈1 ms) 문맥이 길수록 커진다(128K 에서 n_cand 32,768 → 층당 ~58 ms × 11). 샘플러의 `_search`(비트 임계 이분 탐색)는 **그 배치가 L2 에 든다는 전제**라 216 MB 에는 그대로 못 쓴다; 라딕스 두 패스가 맞는 모양이다. **정직하게**: 구성요소 시간은 격리 측정이라 실제 스텝의 겹침·런치 간격을 포함하지 않는다(합이 3.27 s 를 밑도는 이유의 일부); 절대값은 플릿이 붙어 있느냐에 따라 흔들려 유효한 것은 한 실행 안의 비교다.

**조사 16차 + 집행 — 희소 MLA 프리필의 유니온 후보(`pair`·`pair4`)를 재고 걷어냈다(운영자 "다른 레인", PR #698)**: 39차가 남긴 유일한 미판정 노브였다(`STK_mla_prefill`, "프로덕션 off, numerics·TTFT 브래킷 대기" — 원장에 수치가 없다). 두 후보는 **인접 쿼리의 선택이 크게 겹친다**는 예보(“75% 공통이면 트래픽 0.4375”)에 기대 그룹의 선택 **합집합을 한 번만 읽는다**. 그 예보를 변수로 놓고 쟀다(T=6,912·topk 2,048, 겹침 0/50/75/90/100%, 세 변종을 각각의 컨테이너에서, 최솟값 6회). **latent 16 MiB(32K 문맥, L2 초과)**: tile32 는 겹침과 **무관하게 18.3 ms 로 평평**하고, pair 는 42~61 ms, pair4 는 47~111 ms — **모든 겹침에서 tile32 가 2.3~6배 빠르다**. 선택이 **100% 공통**이어서 유니온이 최선일 때조차 tile32 37/18.3 < pair 41.6/42.1 < pair4 47.2/47.7 이다. 세 변종의 출력 체크섬은 같다(같은 것을 계산한다). 처음엔 latent 를 T 와 같은 6,912(3.5 MiB)로 잡았는데 그건 **전부 L2 에 들어** 게더가 DRAM 을 안 치므로 유니온의 전제 자체가 시험되지 않았고 시간도 비단조였다(50% 에서 18.2, 양 끝에서 36~37) — 문맥 크기를 실제로 키운 뒤에야 답이 나왔다. **집행**: `PREFILL_MODES` 는 `("stock", "tile32")`, `_mla_prefill_pair`·`PAIR_STATS`·`ENABLE_MLA_PREFILL_PAIR`·`MLA_PREFILL_GROUP` 과 그 부팅 자가시험 분기를 지웠다(파이썬 −110 줄); 노브 설명도 고쳤다. **남은 것**: `glm53_megakernel.cu` 의 `run_mla_prefill_pair`·`run_mla_prefill_group4` 와 그 `mk_mla_group4_*` 커널(≈1,200 줄)은 이제 아무도 안 부른다 — 그 파일을 다음에 건드리는 사람이 같이 걷으면 된다(지금 지우면 네 노드의 MLA nvcc 캐시가 통째로 무효화되고 그 파일을 만지는 다른 세션과 충돌한다). **정직하게**: 겹침은 합성으로 만들었다(블록마다 공통 선택 + 나머지 무작위); 실 서빙의 인접 쿼리 자카드는 이 레인이 부팅 때 스스로 찍게 돼 있었는데(`mla pair stats`) 그 진단도 후보와 함께 사라진다 — 없어진 후보의 전제였으므로 남길 이유가 없다.

**조사 17차 — 프리필의 고정비: 라우팅 전문가 레인은 토큰이 아니라 청크마다 뱅크를 읽고, 그 읽기가 메모리 바닥의 3배다(운영자 "프리필 개선 방법이나 찾아" → "고정비를 낮추는게 답인것 같은데", PR #711)**: 14차의 구성표에서 가장 큰 조각은 라우팅 전문가 0.82 s(청크의 25%)였다. 그 비용이 무엇에 비례하는지 청크 크기로 쓸어 보니 **토큰이 아니다**: 같은 층에서 T=1,728 → 14.18 ms, 3,456 → 18.96, 6,912 → 21.97, **13,824 → 22.17** — 토큰을 두 배로 실어도 **0.9%** 만 는다. 두 점으로 분해하면 **층당 고정 13.0 ms + 토큰당 0.66 µs** 이고, 고정분이 하는 일은 이 랭크의 전문가 뱅크(w13 576 + w13_sf 72 + w2 288 + w2_sf 36 = **972 MiB**, 42층이면 청크당 39.9 GiB)를 한 번 읽는 것이다. **그 읽기의 바닥을 이 상자에서 직접 쟀다**: 1 GiB fp32 읽기 235 GB/s, `copy_` 221 GB/s, 그리고 **실물 w13 텐서를 그대로 읽으면 576 MiB 를 2.57 ms = 235 GB/s** — 레이아웃 탓이 아니다(처음 `sum()` 으로 11 GB/s 가 나온 것은 uint8 리덕션이 벡터화되지 않는 torch 쪽 함정이었다). 뱅크 972 MiB 의 순수 읽기는 **4.3 ms**. 즉 **고정비 13.0 ms 는 메모리 바닥의 3.0 배**이고, 초과분은 층당 8.7 ms · 청크당 **0.37 s · 프리필의 11%** 다. **청크를 키우는 것은 이 낭비를 없애는 게 아니라 토큰으로 나누는 것**이다(6,912 → 13,824 이면 토큰당 12% 싸지지만 낭비는 그대로). 청크를 정하는 것은 `boot.TOKEN_BUDGET = 8192` 한 줄이고(`floor((8192−5)/2304)×2304 = 6,912`), vLLM 에서는 APC align 이 3 블록에 못박아 예산을 올려도 안 움직였지만 **ST 는 예산에서 직접 계산한다** — 다만 프리필 활성 피크가 비례해 커지므로 메모리 관문이 판정할 값이고, `decode_token_budget` 이 혼합 스텝을 이미 2,304 로 따로 막고 있어 큰 청크는 순수 프리필 스텝에만 걸린다. **막힌 곳**: 그 13.0 ms 가 큰 가중치 스트림인지 작은 스케일·활성 박스의 요청률인지 가르는 계측이 **커널 안에 이미 있는데**(`xs` = FC1 SFB 박스 생략, `xa` = A+SFA 박스 생략, 수치는 쓰레기·시간은 유효) **프로덕션 조합과 배타다**: `sf6` 는 `t,r` 을 요구하고 `r`(decode_reform)은 `a_ring·sf_pack·skip_sf·skip_a` 중 하나라도 있으면 거부한다(`_parse_glm53_static_v2`). 그래서 서빙 레시피(`t,r,sf6,q0`)에서는 이 질문을 물을 수 없다 — 계측을 `r` 과 같이 쓸 수 있게 푸는 것이 이 길의 첫 걸음이다. **참고**: 38차 피어 3라운드가 같은 자리를 쳐서 "메모리 경로는 바이트가 아니라 **L2 요청률**에 묶인다"고 결론했고 연속 16 KB TMA 박스 셀 `t` 는 `u` 대비 무이동이었다 — 그 판정은 vLLM 오버레이의 형상에서 나온 것이라 ST 청크에서 다시 유효한지는 열려 있다. **정직하게**: 절대 시간은 플릿이 붙어 있느냐에 따라 크게 흔들린다(같은 T=1,728 기준이 9.3~14.2 ms) — 위의 분해는 한 실행 안의 네 점에서 얻은 것이고, 바닥 4.3 ms 와의 비교는 같은 조건의 대역폭 측정과 나란히 잰 것이다.

**GPU 판정 18차 — 계측을 서빙 레시피에 겨눌 수 있게 풀고, 고정비가 무엇인지 물었다(운영자 "풀어", PR #714)**: 17차가 막힌 곳은 계측이었다 — 층당 13.0 ms 의 고정비가 **큰 가중치 스트림**인지 **작은 스케일·활성 박스**인지 가르는 셀 `xs`(FC1 SFB 박스 생략)·`xa`(A+SFA 박스 생략)가 서빙 레시피 `t,r,sf6` 에서 안 켜졌다. 관문이 둘이었다: 파서의 `r` 은 `a_ring·sf_pack·skip_sf·skip_a` 중 하나라도 있으면 거부했고, 커널의 `reform_sf_pack`(sf6)은 그 넷에 `split` 을 더해 거부했다. **둘 다 진짜 충돌이 아니었다**: 스킵은 레이아웃이 아니라 `const_expr(not self.skip_*)` 로 **TMA 발행 자체를 컴파일 타임에 빼는 것**이라 어떤 스케일 기하와도 합성된다. 두 관문을 좁혀 `skip_sf`·`skip_a` 만 통과시켰다(스케일 경로를 실제로 바꾸는 `a_ring`·`sf_pack`·`split` 은 그대로 배타, 그리고 **서빙 파스는 여전히 `probe-only` 로 거부**한다 — 프로덕션이 실수로 집을 수 없다). **판정(서빙 레시피 `t,r,sf6`, T=1,728, 네 변종을 한 프로세스에서 교차, 최솟값 8회)**: served **9.43 ms** · xs 9.56(+1.4%) · xa 9.32(−1.2%) · **xs+xa 9.31(−1.3%)**. **작은 박스를 둘 다 꺼도 1.3% 다** — 고정비는 스케일도 활성도 아니고 **전문가 가중치 스트림 그 자체**다(커널 주석의 "xa 진단이 그 로드를 ~3% 로 매겼다" 와 같은 자리). 그러니 17차의 3.0 배 간극(13.0 ms vs 바닥 4.3 ms)은 w13/w2 의 B-박스 발행 패턴에 있고, 38차 피어의 "L2 요청률에 묶임" 도 작은 박스가 아니라 **가중치 박스** 이야기로 읽어야 한다. **측정 규약**: 변종마다 컨테이너를 따로 쓴 첫 판은 쓸 수 없었다 — 같은 기준이 9.89 와 15.69 로 59% 흔들렸다(플릿이 그 사이 떴다). `md._WEIGHT_CACHE`·`_REFORM_SF_CACHE` 를 비우고 `lanes` 를 다시 만들면 **한 프로세스에서 네 변종을 다 세울 수 있다** — 그 교차가 답을 냈다. **그래서 셀 공간을 쓸었다(서빙 청크 T=6,912, 기준과 후보를 한 프로세스에 둘씩 교차, 최솟값 8회)**: 서빙 `t,r,sf6,q0` 대비 **평범한 `t` −0.3%**, `t,f4,g2`(FC1 스테이지 4) **−1.1%**, `t,f2,g4`(FC2 스테이지 4) **+3.3%** — **전부 ±3% 안이다**. 스펙 언어가 여는 손잡이(스테이지 수, 타일 주요·재배열, 스케일 패킹)로는 가중치 스트림이 안 움직인다. 38차 피어의 "t 는 u 대비 무이동" 과 같은 결론에 ST 의 청크에서 따로 도달한 셈이다. **그러니 17차의 3.0 배 간극은 셀이 아니라 커널 안의 B-박스 발행 방식**(동시 진행 수·스위즐·생산자/소비자 분할)에 있고 그건 CuTe-DSL 개조다 — 값은 이미 알려져 있다(청크당 0.37 s, 프리필의 11%). 한 가지 더: `t,r,sf6` 를 쓰지 않는 후보는 `q0`(SF6 메타데이터 캐시)도 같이 잃으므로 위 `t` 행은 기하 하나가 아니라 **레시피 전체**의 비교다. CPU 스위트 845 OK(이미지 안 `test_engine_knobs` 12 OK). **정정(운영자 "너가 커널 본문 공략해" 뒤)**: 셀을 쓸어도 평평하길래(서빙 `t,r,sf6,q0` 대비 `t` −0.3%, `t,f4,g2` −1.1%, `t,f2,g4` +3.3%) 커널 본문으로 들어가려고 `s`(스탬프) 셀을 켰더니 **스탬프가 한 줄도 안 찍혔다**. 이유는 단순했다 — **프리필은 정적 커널을 쓰지 않는다**. 토큰 수로 백엔드를 확인하면 T=8(routed 64) → `static2_m8`, T=64(512) → `static2_m64`, **T=1,728(13,824)·T=6,912(55,296) → `[tp-sf6-q0]`** 이고, 프리필 호출 동안 정적 커널의 스탬프 텐서는 하나도 늘지 않는다. `xs`·`xa`·`f`·`g`·`t`·`r` 은 전부 `moe_static_kernel_v4` 를 설정하는 셀이고 그것은 **디코드** 레인이다. 그래서 (a) 18차의 "고정비는 작은 박스가 아니라 가중치 스트림" 은 **프리필에 대해 근거가 없고**(프로브가 도는 커널에 닿지 않았다; ±1.4% 는 잡음), (b) 셀 스윕이 평평한 것도 같은 이유다 — 안 도는 커널의 손잡이를 돌렸다. **살아남는 것**: PR #714 의 관문 완화는 그대로 옳다(디코드 레시피에 계측을 겨눌 수 있게 했다); 17차의 고정비 자체(층당 13.0 ms, 뱅크 순수 읽기 4.3 ms 의 3.0 배, 청크당 0.37 s = 프리필의 11%)는 `lanes.moe` 를 통째로 잰 것이라 유효하다. **다음 사람에게**: 프리필 전문가 레인을 공략하려면 `engine/kernels/b12x/moe_dynamic_gated_sf6_q0.py` 를 보라 — 정적 커널의 셀 스윕과 스탬프는 디코드 질문에만 답한다. **그리고 17차의 귀속도 틀렸다(운영자 "거기부터 다시 봐")**: 프리필 레인에서 청크와 라우팅 행 수를 고정한 채 "몇 개의 서로 다른 전문가에 떨어지나" 만 바꾸면 8개(그 가중치 27 MiB) 14.73 ms · 96개(324 MiB) **10.44** · 288개(972 MiB) 12.86 이다 — **읽는 가중치를 36배 늘려도 시간이 오히려 줄거나 비슷하다**. 디스패처의 `tile_m` 도 평평하다(표 128 대비 명시 64 −0.4%, 명시 128 −1.0% = 잡음 바닥). 그러니 17차의 "고정비 13.0 ms 는 뱅크를 한 번 읽는 것이고 메모리 바닥의 3.0 배" 는 **추론이었지 측정이 아니었고**, 그 위의 "청크당 0.37 s 낭비" 도 함께 철회한다 — 진짜 답은 19차다(점유율). **정직하게**: T=1,728 한 점이고(고정비가 지배하는 지점으로 골랐다), 절대값은 여전히 플릿에 따라 흔들린다 — 유효한 것은 한 실행 안의 네 값뿐이다.

**조사 19차 — 프리필 전문가 레인의 "고정비" 는 위상이 아니라 점유율이었다: 서빙 청크가 정확히 무릎에 앉아 있다(운영자 "계측해봐", PR #744 — #731 이 원장 충돌로 닫히고 §23 정리에 통합)**: 17·18차의 귀속이 둘 다 틀린 뒤, 이 레인을 **밖에서** 계측했다. `torch.profiler` 로 `lanes.moe` 한 콜을 보니 **CUDA 커널이 둘뿐**이다 — `kernel_cutlass_kernel_...b12x moe_dynamic_gated...` **18.741 ms(96.3%)** 와 elementwise 0.723 ms(3.7%). 라우팅 히스토그램도, 활성 양자화도, 스캐터도 **따로 없다**: 전부 그 한 융합 커널 안이다. 그러니 "고정비를 위상으로 가른다" 는 물음 자체가 틀렸고, 남는 물음은 **그 커널이 얼마나 차 있나**다. 행 수로 쓸었다(실 라우터, 최솟값 6회, 커널은 미리 구워 둠 — 첫 판의 T=13,824 는 중간에 CuTe-DSL 컴파일이 끼어 49.04 ms 로 오염됐다): **T=6,912(서빙) 19.38 ms · 35.9 TFLOP/s · 1K 토큰당 2.803 ms**, T=13,824 21.69 · **64.2** · **1.569**, T=20,736 30.69 · 68.0 · 1.480, T=27,648 39.90 · 69.8 · 1.443. **서빙 청크는 무릎 바로 아래에 있다** — 한 번 두 배로 하면 커널 효율이 **1.79 배**(35.9 → 64.2 TFLOP/s), 토큰당 값이 **−44%** 이고, 그 위로는 평평하다(68~70). 그래서 앞선 관찰들이 전부 설명된다: "토큰에 평평하다"(반쯤 빈 커널이 채워지는 구간), "가중치 36배를 읽어도 공짜"(계산에 묶여 있지 대역폭이 아니다), "셀·타일이 안 움직인다"(모양 손잡이가 점유율을 못 바꾼다). **값**: 전문가 레인은 청크의 25%(0.82 s)이고 토큰당 −44% 면 종단 프리필 **약 11%**. **바꿀 것은 한 줄**: `boot.TOKEN_BUDGET = 8192` → 16,384 이면 `floor((16384−5)/2304)×2304 = 13,824`, 정확히 무릎이다. **대가**: 프리필 활성 피크가 대략 두 배(부팅의 메모리 관문이 판정한다 — 안 들어가면 D3 로 죽으므로 창에서 확인해야 한다); 혼합 스텝은 `decode_token_budget` 이 이미 2,304+k 로 막아 두어 영향이 없다; 짧은 프롬프트의 TTFT 는 청크가 상한이라 그대로다. **정직하게**: 한 층·합성 hidden 의 라우팅이고 절대 시간은 플릿에 따라 흔들린다 — 유효한 것은 한 실행 안의 곡선이다. 그리고 1K 토큰당 값은 전문가 레인만의 것이지 스텝 전체가 아니다(나머지 레인은 토큰에 선형이라 안 싸진다).

**GPU 판정 20차 — 프리필 청크를 세 블록에서 네 블록으로(운영자 "메모리가 얼마나 튈것 같은데? 해봐", PR #734)**: 19차가 잰 무릎을 실제로 넘는다. 청크 후보를 한 프로세스에서 교차해 재면(실 라우터, 10회, 워크스페이스는 미리 다 만들어 둠) **6,912(3블록) 1K 토큰당 2.827 ms · 9,216(4블록) 1.728(−39%) · 11,520(5블록) 1.637(−42%) · 13,824(6블록) 1.560(−45%)** 이다 — **4블록이 이득의 87% 를 가져간다**. 메모리가 그 선택을 정했다: 프리필 활성 피크는 토큰에 선형이고(레인 자체의 할당자 피크도 6,912 에서 54 MiB, 13,824 에서 108 MiB 로 정확히 2배) 원장의 자격 검증된 피크가 6,912 에서 **5.72 GiB** 인데 `budget.WORKSPACE_GIB = 12.0` 이 아레나 밖 전부의 상한이다 — 6블록이면 ~11.4 GiB 로 **상한선에 붙고**, 4블록이면 ~7.6 GiB 로 4.4 GiB 가 남는다. 그래서 `boot.TOKEN_BUDGET` 을 8,192 → **10,240** 으로 올렸다(`floor((10240−6)/2304)×2304 = 9,216`; `shapes._selfcheck` 에 그 줄을 더했다). **전문가 레인은 청크의 25% 이므로 종단 프리필 기대치는 −10% 안팎**이고, 혼합 스텝은 `decode_token_budget = chunk_align + spec_k` 가 이미 2,304+6 으로 따로 막아 영향이 없으며, 짧은 프롬프트의 TTFT 는 청크가 상한이라 그대로다. **부팅이 판정한다**: 메모리 관문이 가장 큰 legal 프리필을 실제로 돌려 `peak_workspace_bytes` 를 재고, 12 GiB 를 넘으면 D3 로 죽는다 — 그러니 이 값은 다음 릴리스 부팅의 `prefill/9216/...` 행이 확인해 준다. **측정 함정 하나**: 청크 크기를 프로세스 안에서 바꿔 잴 때 `torch.cuda.empty_cache()` 를 사이에 부르면 MoE 워크스페이스가 반납돼 다음 호출이 21.6 대신 **48.9 ms** 로 나온다(두 번 속았다) — 후보의 워크스페이스를 **먼저 다 만들고** 그 뒤에 교차할 것. CPU 스위트 883 중 882 OK; 실패 하나는 main 자체의 것(`SOURCES.json` 의 provenance 핀이 `kda/`·`_moe_dynamic/gated.py` 중 하나에 대해 낡았다 — 내 변경을 빼도 실패한다).

**GPU 판정 21차 — 20차의 숨은 대가를 걷어냈다: 청크가 8,192 을 넘으면 MLA 가 prefill32 에서 떨어져 나간다(운영자 "무조건 더 빠르게해 어떻게든", PR #737)**: 20차로 청크를 9,216 으로 올린 뒤 **다른 레인들도 이득을 보는지** 확인했다(세 청크, 한 프로세스 교차, 버퍼는 미리 다 만듦). KDA 는 토큰당 −4%, 밀집 FP8 은 ±0 인데 **MLA 희소 어텐션이 +19% 악화**했다(2,648 → 3,162 µs/1K, 그리고 13,824 에서도 같은 값 — 계단이지 기울기가 아니다). 계단의 정체는 `mla/__init__.py` 의 문 `4096 <= T <= 8192` 였다: **청크가 그 상한을 넘으면 prefill32 register-Q 타일 커널이 안 물리고 `mla_decode` 로 떨어진다**(39차가 76 → 38 ms 로 반으로 줄인 바로 그 커널이다). 8,192 는 구조가 아니라 **옛 청크의 천장**이었다 — prefill32 는 행마다 독립 CTA 고(`grid = T`) **스크래치도, prepare 패스도, membership 맵도 없으며**(커널 주석이 그것들을 피했다고 적어 둔 268 MB 는 안 쓰는 쪽이다) 본문의 T 의존은 `blockIdx.x` 하나, 오프셋은 전부 `size_t` 다. 그래서 CUDA 쪽 `TORCH_CHECK(ints[0] <= 8192)` 와 파이썬 문을 **16,384** 로 올렸다. **판정**: T=6,912 1,449.5 µs/1K · **9,216 1,440.9** · 13,824 1,442.4 로 **완전히 평평**하고, 참조 대비 상대오차는 1.91e-03 / **1.82e-03** / 1.85e-03 으로 6,912 의 값과 같다(무장 관문의 2e-2 한참 아래). 즉 20차의 −39% 는 그대로 두고 MLA 의 +19% 만 사라진다 — 구성 가중치로 **종단 프리필 −7.6% → −10% 안팎**으로 복구된다. `glm53_megakernel.cu` 는 provenance 핀 대상이라 `SOURCES.json` 의 `local_sha256` 도 갱신했다(`local_modifications` 는 원래대로 참, 벤더 `sha256` 과 다름). **대가**: 그 파일이 바뀌면 네 노드의 MLA nvcc 캐시가 무효화돼 다음 부팅이 한 번 다시 굽는다. CPU 스위트 **904 OK**. **정직하게**: 절대 µs/1K 값은 이 실행 안에서만 비교된다(앞선 레인 스윕의 2,648 은 `lens` 가 꽉 찬 경우였고 여기는 ragged 다); 바뀐 것은 계단이 사라졌다는 사실이다.

### 45차 §79 — "최근 원패스가 최악" 의 답: 같은 커밋인데 다른 엔진을 쟀다 (2026-09-12, srv4, 부팅 원장 대조)

운영자 "가장 최근의 원패스 결과가 최악으로 느린 이유 조사". 결과 표(`~/glm53-logs/bracket-onepass.jsonl`)는
디스크에 없었지만 **부팅 원장은 남아 있었다** — 그리고 그게 답을 갖고 있었다.

오늘의 원패스 부팅 셋을 `memory-rank3.json` 으로 맞대 봤다:

| 부팅 (덤프 디렉터리) | 디코드 폭 | 문맥 상한 | 아레나 | 캡처 | 부팅 전체 | 페이즈 |
|---|---|---|---|---|---|---|
| 18:18 `…-main-e3328753-dumps` | **1–4** | **1,035,264** | 59.14 GiB | 32.3 s | 163.5 s | 76 |
| **18:45 `…-calibfix-dumps`** | **1–8** | **364,032** | 58.10 GiB | **71.9 s** | **208.7 s** | **124** |
| 18:53 `st-dumps` | **1–4** | **1,035,264** | 59.14 GiB | 32.2 s | 160.8 s | 76 |

**같은 커밋, 다른 엔진이다.** 가운데 것만 디코드 그래프를 폭 8까지 잡았고(`target` 36 → 64,
`drafter` 20 → 32, `sampling` 8 → 16), 문맥 상한은 1M 에서 **356K 로 떨어졌다** — 행이 두 배면 행당 KV 몫이
반이니 상한이 따라 내려간 것이다. 추가된 페이즈가 그대로 증거다: `target/(8, 6, 364032)` · `(8, 6, 262144)` …

**왜 그렇게 됐나**: `boot.py` 의 **`MAX_SEQS = 8` 이 레포 기본값**이고, **프로덕션 릴리스는 4 를 박아 둔다**
(cutover 기록: *"4 request rows, context ceiling 1,048,576"*). 18:18·18:53 은 프로덕션 모양으로, 18:45 는
**레포 기본값 그대로** 떴다. 즉 마지막 원패스는 **프로덕션이 돌리는 엔진이 아닌 것**을 쟀다.

**덤프 디렉터리 이름은 트리를 식별하지 않는다 — 한 겹 더 들어가면 이 표의 이름표부터 틀렸다.** 오늘 남은
릴리스 디렉터리를 직접 읽으면 `st-releases/main-e3328753` 과 `main-e3328753-calibfix` 는 **둘 다**
`MAX_SEQS = 8` 이고(둘의 `boot.py` 는 같은 mtime 17:48:40 — 같은 소스에서 rsync 됐다),
`MAX_SEQS = 4` 인 것은 `prod-abceb6a0-grammar-9391` 쪽이다. 그러니 폭 1–4 로 뜬 18:18 은
**`main-e3328753` 트리에서 뜬 것이 아니다**; 덤프 디렉터리에 그 이름이 붙었을 뿐이다.
`--production` 은 `mla_prefill` 과 `context_ceiling` 만 박고 `max_seqs` 는 **건드리지 않으므로**
(`boot.py` 의 `defaults`), 폭을 정하는 것은 오직 그 트리의 `MAX_SEQS` 상수다.
**증거의 이름은 증거가 아니다** — 이 절이 고치는 것이 정확히 그 구멍이다.

**그래서 "느리다" 는 회귀가 아니라 형상 차이일 가능성이 크다** — 캡처만 2.2배, 그래프 풀이 두 배(그래프 2.68 GiB),
워크스페이스 5.2 GiB, 그리고 C=1 로 도는 원패스에서 행 8개짜리 배치·스케줄러 상한은 얻는 것 없이 자원만 쓴다.
**다만 여기까지가 부팅 원장이 말할 수 있는 전부다** — 레그별 수치가 없으니 얼마나 느렸는지, 어느 다리가 나빠졌는지는
이 증거로 말할 수 없다. 그건 기록이 남지 않아서다.

**그래서 고친 것은 그 기록이다.** `bench/onepass.py` 가 레코드마다 **엔진의 형상을 문에서 읽어 찍는다**
(`rec["engine_shape"]`): 서빙 모델 id, `max_model_len`, **`max_concurrent_requests`**, 드래프트 폭, 접두사 캐시·대화 티어 유무.
45차 §56 이 `/v1/models` 에 능력을 싣게 해 둔 덕에 **한 번의 GET 으로 읽힌다**. 능력 블록이 없는 옛 엔진은
있는 것만 싣고 나머지는 **추측 없이 빈다**. 문이 안 열려 있으면 `{}` 이고, 그것이 실행을 실패시키지 않는다.

가짜 문 셋으로 확인:

```
production shape (4 rows, 1M)  {'max_model_len': 1035264, 'max_concurrent_requests': 4, ...}
repo default  (8 rows, 356K)   {'max_model_len': 364032,  'max_concurrent_requests': 8, ...}
engine without capabilities    {'model': 'glm-5.3-flash', 'engine': 'ST'}
nothing listening              {}
```

**교훈**: 오늘 원패스 두 판이 27분 간격·같은 커밋이었고, **레코드에는 그 둘을 구분할 것이 한 글자도 없었다.**
브래킷은 커밋만 찍으면 비교 가능해지지 않는다 — **서빙 형상도 찍어야** 비교 가능해진다.

**검증**: `tools/check.py` 59 files, **798 tests, 0 failed**; `tests/test_fleet_onepass.py` 22 OK(인자 표면 불변).

### 45차 §80 — 겹침은 된다: 4배의 일이 1.21배의 시간에. 문제는 실리콘이 아니라 **독립적인 일이 없다는 것** (2026-09-12, srv4, GPU 공유, 무중단)

§71 이 *"프로덕션 형상에서 48개 SM 중 6개만 돈다"* 로 끝났다. 운영자 질문: **48개를 채우도록 파이프라인·비동기·
스케줄러를 만들면?** 하드웨어가 그걸 허락하는지부터 쟀다(`probes/sm_overlap.py`, 샘플러 6행 호출을 k벌 동시에):

| 동시 사본 | 같은 스트림 | 각자 스트림 | 겹침 이득 |
|---:|---:|---:|---:|
| 1 | 435.9 µs | 445.2 µs | 0.98× |
| 2 | 857.8 µs | **468.9 µs** | **1.83×** |
| 4 | 1,728.9 µs | **526.0 µs** | **3.29×** |
| 8 | 3,513.9 µs | 1,018.4 µs | 3.45× |

**4배의 일이 1.21배의 시간에 끝나고, 8벌(8×6 = 48 프로그램)에서 정확히 꺾인다** — SM 수 그대로다.
**실리콘은 있고 겹침은 된다.**

**그런데 배치 1의 디코드 체인에는 겹칠 것이 없다.** forward → 샘플러 → 검증 → 커밋 → observe → propose 는
한 토큰의 의존 사슬이다. 스케줄러가 만들어 낼 수 있는 독립 작업이 아니다. 행이 넷이면 이미 한 호출에
24행으로 들어가므로(24행 539 µs) 배치는 이미 그 길을 쓴다.

**독립적인 일이 실제로 하나 있다: 기다리는 요청의 프리필이다.** 그리고 그건 **D9 가 금지한다** — 한 스텝은
순수 프리필이거나 순수 디코드다. 다만 **D9 의 근거(39차 DF1)는 "혼합 배치"를 쟀다** — 하나의 forward 에
디코더와 프리필 청크를 같이 넣었더니 SP·그래프·메가커널이 전부 꺼져 혼합 스텝 하나가 ~650 ms 였다.
**두 개의 스트림은 혼합 배치가 아니다.** 각자 자기 빠른 경로를 그대로 쓰고 SM 만 나눈다.
**그 형태는 재 본 적이 없다.**

**그래서 열린 질문은 이것 하나다**: 프리필 forward(큰 커널, SM 을 채운다)와 디코드 체인(6 프로그램)을
**별도 스트림으로** 동시에 돌리면, 디코드 ITL 이 얼마나 늘고 프리필이 얼마나 빨라지는가.
39차가 혼합 배치로 답한 것과 **다른 질문**이다. 헌장 D9 를 건드리므로 **운영자의 결정**이고,
플릿 창이 필요하다(랭크 넷).

### 45차 §81 — 디코드 스텝의 구성을 프로덕션이 답했다: 드래프터가 36.4% 다 (2026-09-12, srv4→10.10.10.2, 무중단)

§71 의 단계 계량기가 배포돼 답했다. 표본 31 스텝, 스텝당 **74.99 ms**:

| 단계 | 스텝당 | 비중 |
|---|---:|---:|
| **forward** | 47.60 ms | **63.5%** |
| **observe**(드래프터 관측) | 16.02 ms | **21.4%** |
| **propose**(드래프터 제안) | 11.29 ms | **15.1%** |
| sample | 0.03 ms | 0.0% |
| boundaries | 0.03 ms | 0.0% |
| commit | 0.01 ms | 0.0% |

**`all_gather` 와 `verify` 단계가 없다** — 프로덕션은 **그리디 경로**로 돈다(`sampling_graphs.greedy`, 캡처된
그래프라 0.03 ms). `async_decode_steps 1970`, `sync_drain 0`: 체인에 100% 머문다.

**이것이 오늘의 작업 방향을 정정한다.** 나는 `block_verify_batch`(§69, 5.8배)와 샘플러(§71, −14%)를 깎았는데,
**그 둘은 지금 프로덕션 경로에 없거나 0.0% 다.** 확률 경로가 켜지면 값이 생기지만 지금 도는 것은 아니다.
하루 종일 "가운데"라고 부른 것의 합이 **0.07%** 였다.

**진짜 자리는 드래프터다 — 36.4%, 그리고 아무도 본 적이 없다.**

**첫 삽**: `observe_rows` 의 fast_attention 경로가 **레이어마다, 행마다** 링에 썼다 —
`write_draft_kv` 가 `slot.numel() == 1` 을 하드 요구해서 루프가 강제됐기 때문이다. 행을 그리드에 넣은
`write_draft_kv_rows` 를 만들어 **레이어당 한 번**으로 바꿨다:

| | 런치 | 시간 |
|---|---:|---:|
| 행별 (3 레이어 × 4 행) | 12 | 67.8 µs |
| 배치 | **3** | **12.2 µs** |

**5.6배**, 그리고 **비트 단위로 동일**(최대 차이 0). 다만 이건 16 ms 중 **런치 몫**이고, 나머지는 투영
(`fc` GEMM, `context_kv` GEMM, 레이어별 rmsnorm+rope)이다. **observe 의 21.4% 를 다 가져온 것이 아니다.**

### 45차 §82 — observe 의 16 ms 는 투영이 아니었다: 로터리가 매번 자기 상수표를 다시 만들고 있었다 (2026-09-12, srv4, GPU 공유, 무중단)

§81 이 남긴 질문 — `observe` 21.4% 중 `write_draft_kv_rows` 가 가져간 런치 몫 말고 나머지는 무엇인가.
프로덕션 형상(한 시퀀스, K=5 → **6 행**, TP=4 → local_kv 2, 드래프터 5 레이어)으로 하나씩 쟀다.

| 부분 | 시간 |
|---|---:|
| `fc` W4 (6×20480 → 4096, 5 팩) | 571.0 µs |
| `context_kv` bf16 (6×4096 → 2560) | 46.1 µs |
| **레이어별 rmsnorm+rope ×5** | **4,156.7 µs** |

**투영이 아니라 꼬리다.** 원소 1,536 개짜리 텐서에 레이어당 831 µs. 쪼개 보면:

| | 시간 |
|---|---:|
| `rmsnorm` | 159.8 µs |
| `rope` 전체 | 452.9 µs |
| — 그 중 **역주파수 표 재생성** | 122.1 µs |
| — 각도+cos+sin | 95.2 µs |
| — 회전+cat | 207.9 µs |
| (런치 바닥: `empty_like`) | 2.0 µs |

`rope` 는 **호출마다** `arange`→`div`→`pow`→`reciprocal` 로 64 개의 **모델 상수**를 다시 만들고 있었다.
그리고 드래프터의 로터리 호출은 **예외 없이** `rope(rmsnorm(x, w, eps), positions, theta)` 다 — 10 곳 전부.
스텝당 호출 수: observe 5 + 블록당 10 × 5 블록 = **55**.

**`engine/kernels/norm_rope.py`**: `norm` 과 `norm_rope` 를 각각 한 번의 런치로. 표는 (device, dim, theta)
마다 한 번만 만들고 커널에 넘긴다 — 그래서 각도의 `position * inv` 곱이 torch 경로와 **비트 단위로 같다**.
캡처 중에 표가 없으면 **에러**다(D3): 그래프의 사설 풀에 모델 상수를 넣으면 그 그래프가 닫힐 때 사라진다.
`capture_decode` 가 캡처 전에 데운다.

| | torch | 융합 | |
|---|---:|---:|---|
| observe 꼬리 (×5) | 3,477.1 µs | 522.5 µs | **6.7배** |
| 제안 블록 하나 | 8,823.9 µs | 1,346.0 µs | **6.6배** |
| 디스패치 (스텝당, 정규화만) | **2,315** | **285** | **−2,030** |

**정확도.** 각도는 비트 동일. 정규화 값은 합산 순서가 달라 **최대 1 bf16 스텝** 어긋날 수 있다 — 64 회
무작위 시행 307,200 원소 중 **8 개(0.0026%)**, 전부 정확히 1 스텝. 이 둘을 읽는 것은 **드래프터뿐**이므로
마지막 비트가 움직이는 것은 **초안**이지 서빙된 토큰이 아니다: 무엇을 내보낼지는 타깃의 로짓이, 무엇을
받아들일지는 블록 검증이 정한다. `tests/test_engine_norm_rope.py` 가 1 스텝 상한과 0.1% 이하 이동률,
그리고 캡처 중 표 부재가 에러라는 것을 박는다.

**읽는 법.** 위 µs 는 **이거 경로**를, **프로덕션과 GPU 를 공유하며** 잰 것이다. torch 쪽은 수십 개의
런치라서 경합이 정확히 그걸 부풀린다. 프로덕션은 이 둘을 **CUDA 그래프 안에서** 돌리므로 호스트 런치
몫은 이미 없고, 실제 이득은 이 배수보다 **작다**. 믿을 수 있는 수는 **배수와 디스패치 수**이지 절대값이
아니다. 진짜 판정은 다음 부팅의 `st:decode_stage_seconds_total{stage="observe"|"propose"}` 다.

**정정.** §81 직후 내가 잰 `fc` bf16 783 µs 는 프로덕션 경로가 아니다 — 부팅 로그의 `drafter_w4: 31`
(= fc + 5 레이어 × 6) 이 말하듯 `fc` 는 **W4 팩**이고, 그건 571 µs 다. 어느 쪽이든 꼬리의 1/7 이다.

### 45차 §83 — 드래프터의 나머지 절반: 탭 컨볼루션이 스텝당 1,500 디스패치였다 (2026-09-12, srv4, GPU 공유, 무중단)

§82 가 정규화를 접고 나서 남은 것. DFlash 는 어텐션과 MLP 양쪽에 짧은 인과 컨볼루션을 둔다 —
**레이어당 4번, 블록당 20번, 제안 스텝당 100번**. torch 로 쓰면 한 번이 **15 디스패치**이고,
[rows, taps, groups, group] 계수를 **매번 물질화**한다(호출당 98 KiB, 스텝당 9.8 MiB — 한 번 읽고 버린다).

| | 시간 | 디스패치 |
|---|---:|---:|
| `_conv` 한 번 (6×4096, taps 2, group 16) | 217.6 µs | 15 |
| **스텝당 100번** | **21.76 ms** | **1,500** |

`engine/kernels/draft_conv.tap_mix` 가 한 런치로 접는다. 계수는 **모델이 가진 가중치 + 그 행의 투영이
만든 수**의 합이라 미리 준비할 수 없고, 오직 레지스터에 두고 융합할 수만 있다.

    out[r, c] = Σ_tap (base[tap, c] + delta[r, tap, c // group]) * x[r - tap, c],   r % block < tap 이면 0

`block` 이 행을 독립 블록으로 자른다 — 탭은 블록 **안에서만** 뒤를 본다. `_conv` 는 block=행수,
`_conv_rows` 는 block=t. 그래서 두 형태가 **하나의 커널**이 된다(호출부의 `tap_valid` 도 함께 사라졌다).

| | torch | 융합 | |
|---|---:|---:|---|
| 탭 믹스 한 번 | 318.8 µs | 63.1 µs | **5.1배** |
| 제안 블록 하나 (정규화+믹스) | 15,912.5 µs | 2,659.6 µs | **6.0배** |
| **디스패치 (스텝당, 드래프터 잔가지 전체)** | **4,315** | **485** | **−3,830** |

**정확도.** 항(term)은 **비트 단위로 동일**하다 — 계수를 torch 의 덧셈이 반올림하는 자리에서 똑같이
가중치 dtype 으로 반올림하고, bf16 두 수의 곱은 어느 쪽이든 같게 반올림된다. 다른 것은 **합**뿐:
이쪽은 fp32 로 더해 한 번만 반올림하고, 옛 형태는 탭마다 누계를 반올림했다. 그래서
**블록의 첫 행(살아 있는 탭이 tap 0 하나뿐)은 옛 답과 정확히 같고**, 그 뒤는 항 크기 합 기준
**2 half-step 이내**(프로덕션의 taps=2), 4 탭에서 3 half-step. 차이는 **커널 쪽이 더 정확한** 방향이다.

`tests/test_engine_draft_conv.py` 가 박는 것: 첫 행의 비트 동일성, 2 half-step 상한, 그리고
**탭이 블록 경계를 넘지 않는다**는 것(두 블록, 앞 블록 끝에 1 을 놓고 뒤 블록이 0 인지).

**잡은 함정 하나.** 래퍼가 `delta.contiguous()` 를 **런치하면서 스트라이드는 원본의 것**을 넘기고 있었다.
테스트에서는 안 보인다 — 테스트가 만드는 delta 는 이미 연속이라 둘이 같은 텐서다. 프로덕션은
`coeff[:, 0]`(=[rows, 2, taps, groups] 투영의 슬라이스, 행 스트라이드가 두 배)을 넘기므로 **거기서만**
엉뚱한 계수를 읽는다. 규칙: **넘긴 텐서가 아니라 실제로 런치되는 텐서의 스트라이드를 바인딩한다.**
슬라이스를 먹이는 테스트를 추가했고, 고치기 전 코드에서 실제로 실패하는 것을 확인했다.

**남은 것.** §82 와 같은 읽기 규칙이 적용된다 — 이거 경로, 프로덕션과 공유한 GPU, 그래프 안에서는
호스트 런치 몫이 이미 없다. 판정은 다음 부팅의 `stage="propose"` 다. 그리고 아직 접지 않은 것:
`fc` W4 가 **5 개의 팩을 따로 부르고 fp32 로 더한다**(571 µs, 10 op). 그게 observe 의 다음 표적이다.

### 45차 §85 — 대화 이어가기가 왜 한 번도 안 탔나: 추론을 켜는 쪽과 되돌려주는 쪽이 다른 계층이다 (2026-09-12, srv4, GPU 없음, 실물 템플릿·토크나이저)

§75 의 스크레이프에 **대화 10개가 파킹돼 있는데 `st:reuse_path_total{continuation}` 은 0** 이었다. D16 의 절반이
프로덕션에서 한 번도 안 탔다는 뜻이라 양쪽 레포를 다 열었다.

**문의 조건은 맞다.** `continue_history`(채팅 경로가 True 로 넘긴다) · `keep_idle`(티어가 있으면 True) ·
테넌트 솔트(데네브는 `prompt_cache_key` 를 안 보내므로 양쪽 다 None) — **셋 다 열려 있다.** 남은 것은
`_continuation` 의 본질적 조건 하나뿐이다: **다음 프롬프트가 엔진이 들고 있는 히스토리의 진짜 접두사여야 한다.**

실물 `chat_template_mm_v2.jinja` 와 실물 `tokenizer.json` 으로 두 턴을 렌더해 봤다(GPU 없음):

| 렌더 | 턴1 생성 프롬프트 꼬리 | 히스토리 | 다음 프롬프트 | 공통 접두사 | 이어가기 |
|---|---|---:|---:|---:|---|
| **thinking 끔** | `<\|assistant\|><think></think>` | 22 | 29 | **22** | **탄다** |
| **thinking 켬** | `<\|assistant\|><think>` | 21 | 28 | **19** | **못 탄다** |
| thinking 켬 + `reasoning_content` 되돌려줌 | 〃 | 33 | 39 | **33** | **탄다** |

**템플릿이 `<think>` 를 직접 연다.** 그래서 모델은 `<think>` 를 생성하지 않고 **추론 본문부터** 낸다. 다음 턴에
그 어시스턴트 차례를 다시 그릴 때 **`reasoning_content` 가 없으면 추론 구간이 통째로 빠지고**, 히스토리는 그
지점에서 접두사이기를 그만둔다 — 위 표의 19/21 이 그것이고, **첫 user 턴 이후가 매 턴 전부 재계산**된다.
이어가기만 죽는 게 아니라 **접두사 캐시도 거기서 끊긴다.**

**그런데 켜는 쪽과 되돌려주는 쪽이 다른 계층이다.** 데네브는 자기 thinking 설정으로 `preserveThinking`
(`interleavedEnabled`: `Thinking.Type=="enabled" && Interleaved`)를 정해 놓고 요청을 보낸다. 그 뒤 **웜홀이**
`effort.go` 에서 Ares 판정이나 caller 의 high/max 를 보고 **그 턴을 추론 턴으로 바꿀 수 있다**
(`thinking:{"type":"enabled"}` + `reasoning_effort:"high"`). 즉 **모델이 추론을 내는 턴인지는 웜홀이 정하는데,
그 추론을 다음 턴에 되돌려줄지는 이미 상류에서 정해져 버렸다.** 되돌려줄 쪽은 자기가 추론 턴이었다는 걸 모른다.

라이브 설정은 `agents.defaults.thinking = {level:"low", interleaved:true}` 이고, 웜홀 주석이 적어 둔 대로
**데네브의 "low" 는 thinking 끔**이다 — 그래서 `Type!="enabled"` → `preserveThinking` 은 `interleaved:true`
에도 불구하고 **false**. 설정대로 가는 턴은 표의 첫 줄(이어가기 탄다)이고, **웜홀이 올린 턴은 둘째 줄**이다.

**아직 못 밝힌 것**: 첫 줄 경로도 이어가기가 탔어야 하는데 스크레이프는 0 이었다. 그 창의 트래픽이 어느
줄이었는지는 기록이 없고(요청별 effort 가 어디에도 안 남는다), **플릿이 내려가 있어 두 턴을 실제로 던져
확인하지 못했다.** 다음 창의 첫 번째 할 일이다 — 그리고 그 김에 **문이 자기가 받은 `reasoning_effort` 를
세어 두면**(끔/high/max) 이 질문은 다시는 추측이 되지 않는다.

### 45차 §84 — 드래프터를 **그래프 재생으로** 재니 답이 달라졌다: 14.97 → 6.12 ms (2026-09-12, srv4, GPU 공유, 무중단)

§82·§83 은 이거 경로에서 6~7 배였지만 "그래프 안에서는 더 작다" 고만 적었다. 이제 **재봤다.**

**하니스.** world_size=1 에 **이미 이 랭크의 샤드인** facts(heads 8, kv 2, inter 3072, hidden 4096, 5 레이어)를
주면 `prepare_fast` 의 샤딩이 항등이 되고 모든 GEMM 이 프로덕션 랭크가 도는 크기가 된다(장치 606 MiB).
`observe_rows` 와 `block_rows` 를 각각 CUDA 그래프로 캡처해 **재생 시간**을 잰다 — 호스트 런치는 없고
기록된 커널만 도는, 프로덕션이 보는 바로 그 수치. (TP 콜렉티브는 no-op 으로 뺐다.)

**먼저: 블록 하나(1,789 µs)의 구성.** 융합 뒤 남은 것이 무엇인지 부분별로 캡처해 쟀다.

| 부분 | µs | 비중 |
|---|---:|---:|
| 30 W4 GEMM (레이어당 6) | 854.5 | 47.8% |
| **5 draft_attention** | **724.3** | **40.5%** |
| 20 탭 믹스 (+5 투영) | 87.8 | 4.9% |
| 15 norm + 10 norm_rope | 43.0 | 2.4% |
| 5 silu*mul | 27.7 | 1.5% |
| 부분의 합 | 1,737.4 | (블록 1,789) |

**내 융합분은 이제 4% 다.** 정규화나 컨볼루션을 더 깎아봐야 아무것도 안 나온다.

**진짜 자리: `draft_attention` 이 링을 24번 다시 읽고 있었다.** 그리드가 (행, 헤드)라
TP=4 에서 6×8 = 48 CTA 인데, `H//HK = 4` 개 쿼리 헤드 × 6 행 = **24 CTA 가 같은 kv 헤드의 링을 각자
전부 훑는다**. 레이어당 읽기: 48 × 2,056 × 128 × 2 B × 2 = **50.5 MiB** — 고유한 건 **2.1 MiB** 다.
145 µs / 50.5 MiB = 348 GB/s, 즉 **중복 트래픽에 대역폭으로 묶여 있었다**.

`_attend` 는 **(kv 헤드, 윈도우 조각)마다 CTA 하나**로 그 헤드의 모든 쿼리(행 × 그룹)를 한 타일로 든다.
조각 수는 `kv헤드 × 조각 = 48` 이 되게 자르고, `_combine` 이 조각들의 부분 소프트맥스를 접는다.
블록 자신의 키는 옛 루프의 꼬리에서 그랬듯 마지막 조각에 실린다. 쿼리 타일은 32 로 묶여 그리드에
실린다 — kv 헤드 하나가 모든 쿼리 헤드를 맡는 형상에서 `B*(H//HK)` 가 1024 가 되고 그만한 fp32
누산기는 CTA 에 안 들어간다(공유메모리 131,584 B 요구, 한계 101,376).

| | 옛 커널 | 새 커널 | |
|---|---:|---:|---|
| 레이어당 (캡처 재생) | 179.1 µs | **19.2 µs** | **9.3배** |
| 블록당 5회 | 724.3 µs | **86.1 µs** | **8.4배** |

**정확도.** 소프트맥스 가중치는 fp32 로 값과 곱한다 — 표준 플래시가 하듯 bf16 으로 내리면 출력 벡터의
0.4% 를 잃고, 드래프터의 수락률이 거기서 읽힌다. fp64 대신 fp32 오라클 대조로 **출력 벡터가 자기 노름의
2^-8(= bf16 한 스텝) 이내**, 여러 형상에서 **비트 동일**. bf16 으로 저장하는 이상 그 아래로는 못 간다.

**그래서 스텝 전체:**

| 재생 | 원래 | 세 융합 뒤 | |
|---|---:|---:|---|
| `observe_rows` | 663.8 µs | **392.0 µs** | 1.69배 |
| `block_rows` | 2,861.5 µs | **1,144.8 µs** | 2.50배 |
| **스텝(observe + 5 블록)** | **14.97 ms** | **6.12 ms** | **2.45배** |

**남은 것과 정직한 경고 둘.**

1. 블록의 남은 절반은 **30 개의 W4 GEMM(828.8 µs)** 이다. 레이어당 가중치 28 MB × 5 = 142 MB,
   273 GB/s 바닥이 **520 µs** 이므로 **1.6배 여유**뿐 — 여기는 거의 다 썼다. `fc` 의 5 팩 접기
   (§83) 는 여전히 남았지만 그건 observe 쪽이다.
2. **이 하니스는 프로덕션의 `observe` 16.02 ms 를 재현하지 못한다** — 여기선 0.66 ms 다(24배 차이).
   `propose` 는 맞는다(5 × 2.86 = 14.3 ms 대 11.29 ms, 콜렉티브를 뺀 것 감안). 형상은 확인했다:
   window 2048, block 8, aux 20480, fc 5 팩, local kv 2 / heads 8 — 전부 프로덕션과 같다. 캡처되지
   않은 형상이 이거로 흐르는 것도 아니다(`base/graphs.run` 은 **KeyError 를 던진다**, 폴백이 없다).
   **원인 미상으로 남긴다.** 다음 부팅의 `stage="observe"` 가 답할 것이다.

**하니스는 `probes/drafter_step_replay.py` 로 남겼다** — 부팅도 체크포인트도 플릿도 없이, 랜덤 가중치
600 MiB 로 드래프터 스텝의 재생 시간과 블록의 부분별 구성을 찍는다. 앞으로 드래프터를 건드리는 사람은
이거 측정으로 자기를 속이지 않아도 된다.

**곁다리로 고친 것.** `tests/test_engine_draft_attention.py` 의 `if __name__ == "__main__"` 가 파일
**중간**에 있었다 — 뒤에 붙은 `BatchedRingWriteTests` 가 스크립트로 실행할 때 아예 정의되기 전이라
조용히 안 돌았다. 끝으로 옮겼다.

**번호 정정.** 원장에 §81 이 **둘** 있었다 — 병렬 세션이 「대화 이어가기」를 §81 로 붙였는데 그 번호는
이미 「디코드 스텝의 구성」이 쓰고 있었다(둘 다 main 에 들어간 뒤 발견). 뒤에 온 쪽을 **§85** 로 옮겼다.
앞 것은 §82·§83·§84 와 `probes/drafter_step_replay.py` 가 참조하므로 건드리지 않는다.

### 45차 §86 — 프로브가 틀렸었다: 한 스텝에 블록은 **한 번** 돈다, 그리고 드래프터의 최대 항목은 어휘 헤드다 (2026-09-12, srv4, GPU 공유, 무중단)

§84 의 하니스는 "스텝 = observe + 5 블록" 이라고 찍었다. **틀렸다.** `propose_rows` 는 `block_rows` 를
**한 번** 부른다 — 앵커 + K 개 마스크를 한 블록의 t = K+1 = 6 행으로 **동시에** 민다(확산형 드래프터이지
자기회귀 k 스텝 루프가 아니다). 그래서 스텝의 드래프터는 5.96 ms 가 아니라:

| 재생 (프로덕션 랭크 형상) | µs |
|---|---:|
| `observe_rows` | 334 |
| `propose_rows` | 2,739 |
| — 그 중 `block_rows` | 1,112 |
| — **헤드와 선택기** | **1,627** |
| **드래프터 전체** | **3,080** |

**그리고 그 안에서 제일 큰 것은 어휘 헤드 GEMM 이다 — 1,392 µs(헤드+선택기의 85%).** 여기 하니스는
bf16 헤드(317 MB)를 쓰고 프로덕션은 FP8(158 MB)이니 실제로는 그 절반쯤일 것이다. 어느 쪽이든
**대역폭 바닥이고 접을 데가 없다** — 드래프터는 154,880 어휘 전체에서 위 16 개를 골라야 하므로
헤드 가중치를 다 읽어야 한다. 나머지: topk 172, 워크 37, 코드북 게더 11, einsum 10, 투영 14.

**이번 판 네 가지, 전부 같은 프로세스 인터리브 A/B(min of 3).** 프로세스 간 차이는 못 믿는다 —
워크를 처음엔 **634 µs 절감**으로 읽었는데 A/B 로는 **43.9 µs** 였다(경합 변동).

| 바꾼 것 | 전 | 후 | |
|---|---:|---:|---|
| `fc`: 5 타일 → **한 런치** | 255.8 µs | 210.2 µs | **1.22배** |
| 링 쓰기의 k/v 복사 제거 | 32.8 µs | 10.3 µs | **3.19배** |
| `silu*mul` → `swiglu` (×5) | 27.5 µs | 9.8 µs | **2.80배** |
| 탐욕 워크 (propose 안에서) | 2,766.9 µs | 2,727.7 µs | 1.01배 |

합쳐서 **약 125 µs**, 드래프터 스텝의 **4%**. 작다. 그렇게 적는다.

**`fc` 접기가 커널을 건드린 이유.** `kernels.cu` 의 `KBLK_MAX = 32` 가 K ≤ 4096 을 강제했고, 그래서
[4096, 20480] 짜리 `fc` 는 **5 개 팩을 따로 부르고 fp32 로 더하는** 형태였다. 그 상수는 두 개의
`__device__` 스테이징 배열(`g_mk2_aq`, `g_mk2_axs`)을 재는 데도 쓰이는데, 그건 **smlp2 의 staged-A
경로 전용**이고 평범한 런치는 건드리지 않는다. 그래서 상수를 쪼갰다 — 스테이징은 `KBLK_MAX = 32`
그대로, 평범한 런치의 K 상한만 `KBLK_LIMIT = 160`(K ≤ 20480). 평범한 런치가 staged-A 플래그를 들고
오면 **에러**다.

접는 것 자체는 파이썬이다: `pack_wide`/`pack_w4_wide` 는 **K 전체에 대해 행 시프트를 한 번** 구해
모든 타일에 같은 것을 준다. 그래서 타일들은 **한 양자화를 자른 조각**이고, 타일 축으로 이어붙인 것이
곧 커널이 원하는 넓은 팩이다(`_fold`). 첫 부팅(무교정)이 파일하는 타일은 시프트가 타일마다라 안 접히고,
그건 그대로 둔다 — 다음 부팅의 스토어가 통으로 팩한다.

**정확도.** 옛 경로는 **타일마다 출력을 bf16 으로 반올림한 뒤** 더했고, 접은 쪽은 160 개 k 블록을
fp32 로 누산해 한 번 반올림한다. 둘 다 같은 가중치를 근사하므로 **참값(fp32 원본 곱) 대비**로 판정했다:
16 시행 × 6 행에서 **접은 쪽이 62 행에서 더 가깝고 34 행에서 더 멀다** — 차이는 W4 양자화 오차(8.5e-2)의
1/1000 수준이다. `swiglu` 와 탐욕 워크는 **비트 단위로 동일**(워크는 전부 동점인 경우까지 확인).

**남은 자리.** 블록 1,112 µs 중 **30 개 W4 GEMM 이 830 µs** 이고 바닥이 520 µs — **1.6배**뿐이다.
탭 믹스 88, 어텐션 84, norm 43, swiglu 10 은 전부 런치 바닥에 붙었다. 다음에 볼 것은 `topk`(172 µs):
패킹된 키는 `(score << 32) | (0xffffffff - id)` 라 **동점이 없으므로** 융합해도 답이 유일하다.

### 45차 §87 — 선택기를 접었다, 그리고 **박아둔 계약 하나를 깰 뻔했다** (2026-09-12, srv4, GPU 공유, 무중단)

§86 이 남긴 둘을 접었다. 전부 같은 프로세스 인터리브 A/B(min of 3).

**1. 어휘 topk 의 지역 선택 (98.4 → 16.4 µs, 6.0배).** 키는
`(ordered_score << 32) | (0xffffffff - id)` 라 **유일하다**. 유일하면 "지금까지 고른 것보다 작은 것 중 최대"
가 정확하므로, 이미 쓴 것의 마스크 없이 K 라운드로 top-k 가 나온다. 세그먼트마다 레지스터에 올려 K 라운드,
그 결과를 다시 한 번 — 두 단계면 끝. `BLOCK` 을 512~8192 로 훑어 2048 이 최적(16.4 µs)이었다.

**깰 뻔한 계약.** 처음엔 **병합까지** 접었다 — `restore` 가 [rows, 154,880] fp32 를 −inf 로 채워(3.1 MiB)
거기서 16 개를 고르는 걸 109.7 µs 짜리 낭비로 봤기 때문이다. 집합과 점수는 모든 경우 동일했고 **동점 하나의
순서만** 달랐다(둘 다 3.5156, 옛것 1377 먼저 / 키 순서 1197 먼저). 그런데
`tests/test_engine_vocab_topk.py` 는 **`vocab.topk` 이 `full.topk(k)` 과 id 까지 같아야 한다**고 박아뒀다 —
`test_actual_vocabulary_ties_and_masks_match_dense_cuda_topk`. 이전 캠페인이 **일부러** torch 의 조밀 CUDA
topk 동점 순서에 고정한 것이다. 되돌렸다.

**남긴 형태**: 지역 선택만 융합(`sorted=False` 가 "여기 순서는 답이 아니다" 라고 이미 말한다 — **집합**만
같으면 되고, 키가 유일하니 k 개 최대의 집합은 하나다). 병합은 torch 의 조밀 topk 그대로 — 동점 순서를
박아두는 게 그거다. 다섯 형상에서 **torch 의 지역 topk 와 같은 집합**임을 확인했다.

**2. 선택기의 점수 + 워크 (57.4 → 8.2 µs, 7.0배, 32/32 비트 동일).** torch 형태는
[n, K, 16, 256] 짜리 코드북 게더를 **두 번** 물질화하고(각 164 KiB) einsum 으로 [n, K, 16, 16] 점수를 만든 뒤
그 위를 5 번 걸었다 — 한 번씩만 읽을 것을 위해. 융합하면 한 스텝은 **[256] 벡터 하나를 그 스텝의 후보
행들에 대고** 곱하는 것뿐이고, **점수는 존재하지도 않는다.** 워크가 나르는 것은 인덱스가 아니라 **토큰**이다
(다음 스텝의 predecessor 코드북 행을 그걸로 연다).

샘플링 경로는 루프 그대로다 — 그 뽑기는 엔진의 생성기이고, 그걸 커널에 넣으면 랭크 합의와 D12 재생이
같이 들어간다(`block_verify` 가 긋는 같은 선).

**이번 브랜치 전체, 인터리브 A/B 합계**

| 접은 것 | 전 | 후 | |
|---|---:|---:|---|
| `fc`: 5 타일 → 한 런치 | 255.8 µs | 210.2 µs | 1.22배 |
| 링 쓰기의 k/v 복사 | 32.8 µs | 10.3 µs | 3.19배 |
| `silu*mul` → `swiglu` (×5) | 27.5 µs | 9.8 µs | 2.80배 |
| 어휘 topk 의 지역 선택 | 98.4 µs | 16.4 µs | **6.0배** |
| 선택기 점수 + 워크 | 57.4 µs | 8.2 µs | **7.0배** |
| **합** | **472 µs** | **255 µs** | **−217 µs** |

§86 의 3,080 µs 짜리 드래프터 스텝 기준 **약 7%**. (그 3,080 은 한산할 때 잰 값이다. 이 절의 마지막 측정
동안 프로덕션 부하가 올라 절대값이 2.2 배로 부풀었다 — **그래서 절대값은 쓰지 않고 A/B 만 쓴다.**)

**남은 자리는 변하지 않았다**: 블록의 30 개 W4 GEMM(바닥의 1.6배)과 어휘 헤드 GEMM(대역폭 바닥). 나머지는
전부 런치 바닥에 붙었다.

**곁다리 둘.** (1) `engine/kernels/SOURCES.json` 의 `mla/glm53_megakernel.cu` 기록이 낡아 **main 의 체크가
붉었다** — #732(k6)가 그 파일의 **주석만**(MAX_TOK 설명의 SPEC_K 5→6) 고치고 해시를 안 올렸다. 코드도
산술도 안 바뀌었으므로 기록만 따라잡게 했다. (2) 그 #732 로 **`SPEC_K` 가 6 이 됐다.** 프로브가 5 를
박아두고 있었으니 프로필의 `SPEC_K` 를 읽게 고쳤다 — k 는 프로필이 정하지 프로브가 정하지 않는다.

### 45차 §88 — W4 GEMM 은 바닥의 **1.25배**다: 드래프터에서 접을 것이 끝났다 (2026-09-12, srv4, GPU 공유, 무중단)

§86·§87 이 "블록의 830 µs 는 30 개 W4 GEMM 이고 바닥이 520 µs, 1.6배 여유" 라고 적었다. **바이트를 잘못
셌다.** 팩의 실제 바이트(`data` + `scale`)로 다시 재니:

| 가중치 (레이어당 1, 5 레이어) | µs | 바닥 | |
|---|---:|---:|---:|
| `self_attn.qkv` | 61.4 | 64.8 | **0.95** |
| `self_attn.o_proj` | 55.3 | 43.2 | 1.28 |
| `mlp.gate_up` | 333.3 | 259.3 | 1.29 |
| `mlp.down_proj` | 163.4 | 129.6 | 1.26 |
| `attention_conv.kernel_projection` | 57.4 | 43.2 | 1.33 |
| `mlp_conv.kernel_projection` | 57.4 | 43.2 | 1.33 |
| **서른 개 전부** | **728.1** | **583.3** | **1.25** |

`qkv` 는 **바닥보다 빠르다**(0.95) — 273 GB/s 는 LPDDR 수치이고 L2 가 거들기 때문이다. **여유가 1.25배면
거기엔 남은 게 없다.** 드래프터 블록의 지배항은 이제 하드웨어 한계에 붙어 있다.

**그래서 이번엔 구조만 접었다.**

**1. 어텐션의 행이 그리드로 올라갔다.** `_attn_rows` 가 `draft_attention` 을 **행마다** 부르고 `torch.cat`
으로 붙이고 있었다 — `write_draft_kv_rows` 가 고친 것과 같은 병이다. 행은 서로 독립이다(각자 자기 슬롯의
링을, 자기 컨텍스트 길이에서 읽는다). 그래서 그리드 차원으로 올렸고 붙일 것도 사라졌다.

| | 행별 | 배치 | |
|---|---:|---:|---|
| 레이어 하나, n=4 | 71.7 µs | 47.2 µs | **1.52배** |
| 레이어 하나, n=1 | 20.5 µs | 19.6 µs | 1.05배 |

n=1 인 지금은 거의 안 보이지만 **n=4 에서 1.5배**이고, 무엇보다 런치 수가 행에 비례하지 않게 됐다.
n=1 은 **비트 동일**; n=2·4 는 창 분할 수가 달라져(`SMS // (n * hk)`) 마지막 비트가 움직인다 —
28,672 원소 중 **10 개**, 최대 3.02e-4(bf16 한 스텝의 1/13).

**2. 잔차 덧셈과 그걸 읽는 norm 이 한 런치가 됐다.** 블록은 레이어마다 두 번 `res = res + x` 를 쓰고
곧바로 `res` 를 정규화한다. 나눠 놓으면 같은 행을 두 번 훑고, 그 중 하나는 다른 하나에게 입력을 건네려고
존재한다. **비트 동일**, 열 번의 조인 35.9 → 26.5 µs (**1.35배**).

**지금 드래프터 스텝** (SPEC_K=6, 한산하지 않은 GPU): observe 322, propose 2,639(블록 1,110 / 헤드·선택기
1,529), **합 2,961 µs**. 블록 구성: W4 GEMM 823, 탭 믹스 90, 어텐션 87, norm 43, swiglu 28.

**남은 것은 둘뿐이고 둘 다 대역폭 바닥이다** — 블록의 W4 GEMM(1.25배)과 어휘 헤드 GEMM. 나머지는
전부 런치 바닥에 붙었다. **드래프터에서 융합으로 가져올 것은 여기서 끝난다.**

### 45차 §89 — 다음 파트로 넘어가니 **자가검사 셋이 썩어 있었다**, 그리고 KDA 링은 이미 바닥이다 (2026-09-12, srv4, GPU 공유, 무중단)

드래프터가 끝났으니 다음은 타깃 forward(스텝의 63.5%)다. **먼저 부딪힌 벽**: 이 호스트에는
`served()` 레인이 쓰는 패키지가 없다(flashinfer·tilelang·cutlass·deep_gemm). 즉 **타깃의 프로덕션 경로는
여기서 잴 수도 돌릴 수도 없다.** 잴 수 있는 것만 했다.

**1. KDA 링 레인은 이미 대역폭 바닥이다.** `net._kda` 는 세그먼트마다 파이썬 루프를 돌며
`conv_ring` + `kda_recurrent_ring` + `core[sl] = o[0]` 를 한다 — 드래프터에서 세 번 고친 바로 그 병이라
같은 처방을 하려 했다. 그런데 프로덕션 랭크 형상(34 KDA 레이어, Hl=16, D=128, R=9/WR=7)으로 재보니:

| | 링 레인 | 세그먼트 수 |
|---|---:|---:|
| n=1, t=7 | 816.2 µs | 34 |
| n=4, t=7 | 7,784.1 µs | 136 |

**순환 상태가 (레이어, 세그먼트)당 7.3 MiB** 다([R=7, H=16, K=128, V=128] fp32). n=1 이면 34 × 7.3 =
250 MiB, 273 GB/s 로 **915 µs** — 측정 816 µs 는 **바닥의 0.9배**(L2). **여기엔 접을 것이 없다.**
n=4 는 1 GiB / 3.6 ms 바닥에 7.8 ms 이므로 2.2배 여유가 있지만, 지금 프로덕션은 n=1 이다.

**2. 자가검사 셋이 썩어 있었다.** 엔진의 36 개 모듈이 `_selfcheck()` 를 갖고 있는데 전부
`if __name__ == "__main__"` 아래에서만 불린다 — **아무 테스트도 돌리지 않는다.** 전부 돌려봤더니:

- **`glm53.net`**: `(net.conv_ring, net.rec_ring) == (8, 6)` 에 박혀 있었다. #732 이 SPEC_K 을 6 으로
  올려 실제 값은 (9, 7) 이다. **실패 중이었다.**
- **`glm53.drafter`**: `F.k == 5` 에 박혀 있었다. 같은 이유로 **실패 중이었다.**
- **`modules.rotary`**: 씨앗 없는 `torch.randn` 을 뽑아 **상쇄로 0.047 까지 줄어든 내적**을 절대 허용오차
  1e-4 로 비교한다. 뽑기에 따라 실패하는 **불안정 검사**였다(실제로 실패를 봤다).

고친 방식은 값이 아니라 **불변식**이다. `net` 은 이제 링 폭 숫자가 아니라 **그 폭이 닿아야 하는 레인**을
단언한다: `_kda` 는 `s.length <= min(8, conv_ring)` 일 때만 다이렉트 conv 링을 타는데 디코드 블록은
`spec_k + 1` 이므로, **spec_k 가 7 을 넘으면 매 디코드 스텝이 조용히 프리필 컨볼루션으로 떨어진다** —
이 플릿이 아는 그 실패("무장했는데 안 돌아감"). 이제 그걸 말로 적힌 단언이 잡는다.

`tests/test_engine_selfchecks.py` 가 **모든 모듈의 자가검사를 돌린다.** 체크포인트나 이미지 패키지가
필요한 다섯, GPU 가 필요한 일곱은 **이름과 이유를 적어** 제외한다 — 그 목록이 요점이다: 어느 믿음이
이 상자에서 검사되지 않는지를 말해 주지, 전부를 안 돌리는 것으로 덮지 않는다. 그리고 제외 목록에
**자가검사가 없는 모듈이 남아 있으면** 그것도 실패다.

### 45차 §90 — 창을 예약했고, 그동안 **창에서 쓸 자를 만들었다** (2026-09-12, srv4→srv2, 프로덕션 유지)

§89 가 "타깃 forward 를 건드리려면 플릿 창이 필요하다" 로 끝났다. 예약했다.

**리스는 살아 있었다.** `/home/choiceoh/glm53-logs/st-fleet.lock`(헤드 srv2)을 읽으니
`claude@srv4/main-ff728f43` 가 3 분 전에 잡았고 `st-glm53` 이 2 분째 떠 있었다(est 45 분). **뺏지 않았다** —
`fleet_lease.request_yield` 로 이유를 적어 양보를 요청했다. 그게 이 프로토콜이 큐 혼자서는 못 하는 절반이고,
"죽여라" 가 아니라 "받기를 멈추고, 가진 것을 끝내고, 살아남게 파킹하고, 놓아라" 이다.

**그리고 창에서 무엇으로 잴지가 문제였다.** 디코드 스텝은 캡처된 그래프를 재생한다. 그 안은
**CUDA 이벤트로 못 잰다** — 확인했다:

    g.capture_begin(); s.record(); ...        -> CUDA error: invalid argument (cudaErrorInvalidValue)

`Event.record` 는 캡처 중에 못 쓴다. 그래서 `forward` 안에 마크를 넣어도 재생이 아니라 **캡처**를 재게 된다.
단계 계량기(`base/stage_clock`)가 재생을 통째로만 감쌀 수 있는 이유가 이거다. 그리고 이미지에는 `nsys`/`ncu`
가 없다(호스트에는 있다).

**재생을 꿰뚫어 보는 것은 CUPTI 다.** torch 프로파일러는 재생이 돌린 커널을 보고한다. 그래서
**`/v1/engine/profile`** 을 만들었다 — `POST {"steps": n}` 이면 컨트롤 큐를 타고 루프 스레드에서 다음 n 개
**디코드** 스텝을 프로파일하고, `GET` 이 커널 표를 장치 시간 순으로 돌려준다. 프리필은 안 받는다: 청크 하나가
수 초어치 커널이고, 설명이 안 되는 것은 디코드 스텝이다. 한 번에 한 런, 32 스텝 상한.

이건 창을 **코드 쓰는 데가 아니라 수를 읽는 데** 쓰기 위한 준비다. 창이 열리면 이 표가
"forward 47.60 ms 는 무엇으로 되어 있나" 에 바로 답한다 — 드래프터에서 부분별 캡처 재생이 했던 역할을,
그래프 안을 못 여는 타깃에서는 CUPTI 가 한다.

**정정: 양보 요청은 과했다.** 운영자가 짚었다 — 홀더는 45 분짜리 정당한 작업 중이고 나는 급하지 않은데
(요청 사유에 "No hurry" 라고 적어 놓고 양보를 부른 것 자체가 모순이다) 창을 끊을 이유가 없다.
`request_yield` 를 거뒀고(`clear-yield`, 리스는 다시 깨끗하다) **큐로 갔다.**

리스 모듈이 직접 그렇게 말한다: *"It is deliberately not a queue ... already exist in bench/fleet.sh ...
The queue takes the lease when it grants."* 그리고 `bench/fleet.sh` 는 **`st-*` 컨테이너가 떠 있으면
grant 를 거부**하고 `start-st-glm53.sh` 는 **큐에 홀더가 있으면 거부**한다 — 두 기구가 서로를 본다.
즉 **큐에 거는 것이 곧 대기예약**이고, 홀더를 건드리지 않는다.

**그리고 큐가 이미 받아주는 자리가 비어 있었다.** `bench/fleet_onepass.ST_PROBES` 는 다섯 개의 ST 검사를
이름으로 허용하는데(그 밖의 경로는 거부한다) 그 중 **`probes/engine_graph_profile.py` 는 파일이 없었다.**
큐가 받고 나서 못 돌릴 이름이다 — §89 의 썩은 자가검사와 같은 종류의 구멍이다. 그 자리를 채웠다:
`boot.build` 로 **한 랭크에 실물 가중치**를 올리고(콜렉티브는 항등이라 재는 것은 이 랭크의 산술이다)
프로덕션이 서빙하는 그 스텝을 캡처해 재생을 프로파일한다. 레인별(mHC / MLA·DSA / KDA / MoE / dense GEMM /
norm / collective)로 **스텝당 µs 와 스텝당 런치 수**를 찍는다 — 바이트냐 런치냐가 그 두 열에서 갈린다.
분류되지 않은 커널은 버킷에 숨기지 않고 제 이름으로 남는다.

`tests/test_engine_graph_profile.py` 가 **허용 목록의 모든 이름에 파일이 있는지**를 박는다.

### 45차 §91 — 대기예약을 걸었다, 그리고 **큐가 하지 않은 부탁을 했다고 적고 있었다** (2026-09-12, srv4→srv2, 프로덕션 유지)

§90 에서 양보를 요청한 게 과했다. 운영자가 짚은 대로 **대기예약이면 충분하다.** 요청을 거두고
(`clear-yield`, 리스는 다시 깨끗) 큐로 갔다.

**먼저 큐가 거짓말을 하고 있었다 — 오래된 체크아웃 때문에.** srv2 의 `~/stkernel` 이 origin/main 보다
**639 커밋 뒤**(PR #511)라 `probes/run_engine_probe.sh` 조차 없었고, 그래서 티켓이
"custom GPU scripts and standalone checks are disabled" 로 거부됐다. 같은 트리로 `fleet.sh status` 는
**`fleet: FREE`** 라고 답했다 — 네 노드가 서빙 중인데. (큐 도움말이 그 함정을 이미 적어 뒀다:
*"a tree from before the ST-engine check reported an empty fleet with four nodes serving"*.)
돌고 있는 부팅은 `~/stkernel` 이 아니라 릴리스 스냅샷 `/home/choiceoh/st-releases/main-ff728f43` 를
`/repo` 로 물고 있으므로 트리를 올려도 홀더는 안 건드린다. 기존 브랜치
`codex/fleet-onepass-live-20260909`(5e0216cf)를 남긴 채 main 으로 옮겼다. 이제 status 가 리스와 네 노드를
다 읽고 **`TAKEN by the ST engine, outside this queue`** 라고 답한다. 티켓 `st-fwd-lanes` 가 대기 중이다.

**그리고 이 시스템에서 "대기예약" 은 곧 "양보 요청" 이다.** `bench/fleet.sh` 가 그렇게 설계돼 있다:
*"Refusing is not enough: a queued session would then wait for a human to go and ask ... so the queue asks on
the waiter's behalf -- once per refusal."* 내가 손으로 한 것은 큐가 자동으로 하는 일과 같은 것이었다.

**그런데 그 부탁이 조용히 안 나간다.** `st_engine_yield` 는 `$FLEET_RUNNER_REPO/launchers/lib/fleet-lease.sh`
를 찾는데, 러너는 **`bench/`, `engine/`, `probes/` 만 담긴 스냅샷**에서 실행되고 **`launchers/` 는 거기 없다.**
그래서 함수가 첫 줄에서 `return 0` 하고, 호출부는 성공으로 치고, 로그에는
`asking it to yield to st-fwd-lanes` 가 남는다 — **아무도 부탁받지 않았는데.** 리스를 읽어 확인했다:
`asked: no`. 대기자는 "말해 뒀다" 고 믿고 홀더는 아무것도 못 듣는다.

**동작은 그대로 두고(운영자 판단: 조용히 기다린다) 말만 고쳤다.** `st_engine_yield` 가 이제 **부탁했을 때만
0** 을 돌려주고(`|| true` 를 뺐다 — 상태를 삼키는 것이 거짓말이 들어온 경로다), 호출부가 둘 중 무엇이
일어났는지 적는다:

    hold refused: ... ; asking it to yield to $s
    hold refused: ... ; $s waits, and the holder was NOT asked -- this runner has no launchers/lib/fleet-lease.sh

`tests/test_engine_fleet_lease.py` 가 두 분기와 `|| true` 의 부재를 박는다. `FLEET_AUDIT` 핀도 같이 갱신했다
(하나만 낡아도 모두의 CPU 재사용이 꺼진다).

**남은 판단**: 러너 스냅샷에 `launchers/` 를 넣으면 설계대로 돌아오지만, 그때부터 큐는 티켓이 설 때마다
홀더를 끊는다. 지금은 끊지 않는 쪽을 택했다.

### 45차 §93 — D17 을 처음 집행했다: 오늘 성능 PR 넷에서 잰 이득이 없고, 디코드는 낮게 나왔다 (2026-09-12 22:21~22:27, 네 노드, 같은 부팅 두 판)

헌장 D17(PR #742)이 요구하는 형태 그대로 쟀다 — **같은 부팅에서 두 판**, 레코드에 `engine_shape`,
접두사 캐시를 맞은 브래킷은 비교에서 제외.

| | `e0451b0d` 1/2회차 | `ff728f43` 1/2회차 |
|---|---|---|
| **디코드 step/s** | **12.95 / 12.96** | **12.0 / 12.0** |
| tokens/step | 3.550 / 3.198 | 3.534 / 3.329 |
| 디코드 tok/s | 46.0 / 41.4 | 42.4 / 39.9 |
| 웜 프리필 2K | 3,295 / 3,084 | 3,020 / 3,084 |
| 2K 콜드 TTFT | 21.83s / 16.10s | **6.73s / 5.28s** |
| 32K 콜드 | 46.43s / 19.86s | 53.24s / ⚠️ 캐시 히트 |
| 품질 | 9/9 | 9/9, 깨진 응답 0/5 |

그 사이에 들어간 것: `#734`(프리필 청크 6,912→9,216) · `#735`(드래프터 셀렉터·5회 런치 fc 접기)
· `#736`(스텝 행이 어텐션 루프를 벗어남, residual) · `#737`(prefill32 상한을 청크 법칙에 맞춤).

**디코드는 낮아졌다.** `#735`·`#736` 은 둘 다 스텝에서 런치를 걷어낸다고 했는데 **12.95 → 12.0 step/s** 다.
각 부팅 안의 재현은 단단하지만(12.95/12.96, 12.0/12.0) **부팅이 다르고** IQR 이 `[11.9,14.0]` 대
`[10.9,13.0]` 로 겹친다 — **7% 를 회귀로 단정하지 않는다. 단정할 수 있는 것은 "이득이 안 보인다" 이다.**

**2K 콜드는 진짜로 좋아졌다**: 21.83s → **6.73s**. 오늘 낮 "컴파일로 설명 안 되던 16초 꼬리" 가 줄었다.
`#737` 이 유력하다.

**32K 는 못 가른다.** 1회차 53.24s(이전 46.43s)인데 **2회차가 캐시를 맞았다** — 32K 5.13s · 128K 5.22s 로
둘이 같은 값인 것이 증거다. `#734` 가 청크를 바꿔 32K 프리필의 타일 형상이 전부 새것이므로 1회차가 그
브래킷에서 새 커널을 처음 컴파일했을 수 있다. **캐시를 비우고(`POST /v1/prefix/reset`) 다시 재야 한다.**

**더 큰 그림**: 12.0 이든 12.95 든 옛 스택 기록 **19.87~21.83 step/s** 와 **40% 차이**다. `tokens/step` 은
양쪽 다 3.2~3.6 이라 **드래프터가 아니라 스텝 자체**다(50 ms 대 77~83 ms).

**D17 의 두 판은 컴파일 꼬리를 걸러 주지 그 위의 부팅 간 변동은 못 걸러 준다.** 7% 를 가르려면 같은 창에서
`e0451b0d` ↔ `ff728f43` 를 번갈아 띄워 각각 두 판(부팅 4회)을 재야 한다. 이것이 다음 창의 첫 일이다.

**부수 확인 — §81 의 첫 프로덕션 데이터점**: 원패스 밖에서 들어온 요청 하나가
`st:reasoning_shape_total{thinking="on",effort="high"}` 로 찍혔다(원패스 자신의 10건은 전부 `effort="absent"`).
웜홀이 올린 턴 = **§81 의 둘째 줄**이고, 그 창의 `st:reuse_path_total{continuation}` 은 **0** 이었다.
한 건은 판정이 아니지만, **계열이 실제로 그 질문에 답한다는 것**은 확인됐다.

**부팅 일곱 번 중 다섯 번이 서로 다른 원인으로 죽었다**(#705 교정 캡처 · #721/#727 `draft_ceilings` 형상과
게이지 격리 · #733 랭크 1~3 임대 게이트 · #738 인계 rmtree). 남은 하나는 `one-shot proxy stopped progressing`
(랭크 3, `capture_decode`) 으로 19:02 와 22:07 두 번 나왔고 **재시도로 넘어갔다** — 플래키이고, 재현 조건이
아직 없다.

### 45차 §92 — 테스트 부팅 모드: 플릿을 안 잡고 **프로덕션 레인**을 돌린다 (2026-09-12, srv4, 프로덕션 유지)

이번 세션 내내 같은 벽에 부딪혔다. §89 는 "타깃 forward 를 건드리려면 창이 필요하다" 였고, §90~§91 은
그 창을 얻으려고 리스와 큐를 오갔다. **왜 창이 필요한가** 를 보면 이유가 하나다:

`--local` 은 네 랭크를 네 스레드로 한 박스에서 돌리지만 **`lane_tables.reference()`** 밖에 못 쓴다 —
torch 산술이다. 배관은 증명하지만 **프로덕션이 돌리는 커널은 한 번도 안 돈다.** 그래서 커널에 대한
모든 판정이 플릿 전체를 요구했고, 플릿은 다운타임이다.

**`--test` 는 `--local` 을 프로덕션 레인 위에서 돌린다.** `--lanes {reference,served}` 를 열었고
`--test` 는 `--local --lanes served` 다. 네 랭크는 네 스레드이고 모든 콜렉티브는 로컬이며,
**리스를 잡지 않는다** — 로그가 그렇게 말한다("This is not the fleet and it holds no lease").

**그리고 프로덕션을 죽이지 않는 것이 이 모드의 전제다.** GB10 은 호스트와 장치가 한 풀이고 earlyoom 의
바닥은 절대값이며 엔진은 우선 사살 대상이다 — 2026-09-11 에 프로덕션 옆 스모크 테스트가 자기가 아니라
**플릿의 워커를 죽였다**([[feedback-no-gpu-smoke-beside-production]]). 그래서 `--test` 는 부팅 전에
MemAvailable 을 읽고, 선언한 KV 를 뺀 나머지가 바닥(16 GiB) 아래면 **추측하지 않고 거부한다**(D3):

    --test refuses: 12.3 GiB would be left after this boot's 1.0 GiB of KV and the floor is 16.0.
    Narrow --layers, lower --kv-gib, or wait for the box. A test boot that earlyooms production
    has not tested anything.

통과하면 두 수를 찍는다 — 남는 용량과 바닥. 레버는 `--layers` 와 `--kv-gib` 이고, 메시지가 그걸 말한다.

**이걸로 열리는 것**: 커널 변경(§82~§88 이 A/B 로만 주장하고 플릿 판정을 미뤄둔 것들, 그리고 새 D17 이
"플릿이 재기 전엔 안 끝났다" 고 못박은 것들)을 **창 없이 실물 레인으로** 돌려볼 수 있다. 창은
프로덕션 규모 수치를 위한 것으로 남고, 옳고 그름은 여기서 가려진다.

`tests/test_engine_test_boot.py` 가 박는 것: 거부가 **얼마나 모자라는지와 레버를 말하는지**, 바닥이 0 이
아닌지(0 은 바닥이 아니다 — earlyoom 은 MemAvailable 이 거기 닿기 전에 죽인다), `--test` 가 두 플래그를
함의하는지, 그리고 reference 레인은 지키지 않고 served 레인만 지키는지(전자는 지킬 가중치가 없다).

**그리고 §93 이 이 모드가 왜 지금 필요한지를 말해 준다.** 다른 세션이 D17 을 집행해 오늘 성능 PR 넷을
같은 부팅 두 판으로 쟀고, 그 중 둘(`#735`·`#736`)이 내 것이다. 판정은 **"이득이 안 보인다"** 이고
디코드가 12.95 → 12.0 step/s 로 **낮게** 나왔다(부팅이 달라 7% 를 회귀로 단정하지는 않는다).

내가 §82~§88 에서 주장한 것은 전부 **한 프로세스 인터리브 A/B** 였다 — 그 안에서는 옳지만, 그것이
스텝으로 나타난다는 증거는 아니었다. D17 이 말하는 게 정확히 그것이고, 이번 판정이 그걸 보여 준다.

§93 이 더 중요한 것도 짚는다: 12.0 이든 12.95 든 옛 스택의 19.87~21.83 step/s 와 **40% 차이**이고,
`tokens/step` 은 양쪽 다 3.2~3.6 이라 **드래프터가 아니라 스텝 자체**다(50 ms 대 77~83 ms). 나는 이번
세션을 드래프터(스텝의 36.4%)를 접는 데 썼고, 남은 항이 전부 대역폭 바닥이라는 것까지 쟀다(§88).
**그러면 남는 곳은 forward 다** — 그리고 forward 를 창 없이 실물 레인으로 열어 주는 것이 바로 `--test` 다.

**번호 정정.** 위 D17 집행 기록이 §82 로 붙었는데 그 번호는 이미 「observe 의 16 ms 는 투영이 아니었다」가
쓰고 있었고, §83·§84·§86 과 `probes/drafter_step_replay.py` 가 그걸 참조한다. 뒤에 온 쪽을 **§93** 으로
옮겼다. (같은 일이 §81 에서도 있었다 — §85 참조.)

### 45차 §94 — 테스트 부팅은 **경로가 프로덕션과 같아야** 한다, 그리고 본체는 계측창이다 (2026-09-12, srv4, 프로덕션 유지)

§92 는 `--test` 를 "프로덕션을 안 죽이는 로컬 부팅" 으로 적었다. 운영자가 두 번 정정했다.

**첫째, 그건 곁가지다.** 테스트 부팅의 본체는 (1) 계측창이 넓고 (2) 안 쓰는 기능이 꺼지고
(3) 부팅·컴파일이 빠른 것이다.

**둘째 — 그리고 이게 중요하다 — 이거로 돌면 안 된다.** 나는 처음에 "`--local` 은 캡처를 안 하니
`Event.record` 가 되고, 그러면 `forward` 안을 레인별로 잴 수 있다" 로 갔다. **틀렸다.**
이거 디코드는 **다른 커널을 다른 순서로** 돌린다. 그걸 재면 프로덕션이 아닌 엔진을 재는 것이고,
프리필·디코드 속도가 애초에 달라진다. 넣었던 `forward` 안 마크는 되돌렸다.

**그래서 `--test` 는 경로를 지키고 나머지를 버린다:**

| | |
|---|---|
| **같게** | served 레인, 그리고 **디코드는 캡처된 그래프를 재생한다** — 프로덕션이 하는 그대로 |
| **끄기** | 플릿 부팅이 문 열기 전에 통과시켜야 하는 것들: 비전 타워 자격, 문법 자격, 파킹 티어, 교정 합산 |
| **빠르게** | 캡처 폭을 **하나만**. 캡처는 87 초 부팅의 3/4 이고 **폭마다 든다** |
| **열기** | 1/64 가 아니라 **매 스텝** 계측 |

**그리고 이 모드는 속도 측정이 아니다.** 네 랭크가 **한 GPU 위의 네 스레드**라 장치가 네 랭크어치 산술을
한다. **경로는 프로덕션의 것이고 밀리초는 이 박스의 것**이다 — 찍는 표가 그렇게 말한다
(`this box's time, not the fleet's`). D17 은 그대로다: 속도를 주장하면 플릿이 재야 끝난다.

**그러면 §93 의 질문은 어디서 답하나.** 옛 스택과 40% 차이는 드래프터가 아니라 스텝 자체다.
`forward` 안을 보려면 **CUPTI 밖에 없다** — `/v1/engine/profile`(§90) 과
`probes/engine_graph_profile.py`(큐에 걸어 둔 티켓) 가 그 일을 한다. 테스트 부팅은 그 둘을 **창 없이**
돌릴 수 있게 해 주는 자리이지, 그 자체가 자는 아니다.

### 45차 §95 — 큐가 허용한 검사 여섯 중 셋은 **큐가 시작은 하되 돌릴 수 없었다** (2026-09-12, srv4, 프로덕션 유지)

§90 의 티켓(`st-fwd-lanes`)이 창을 기다리는 동안 **한 번도 실행된 적 없는 프로브가 어떻게 불리는지**를
확인했다. 창을 버릴 뻔했다.

`probes/run_engine_probe.sh` 는 프로브 경로 뒤의 `"$@"` 를 그대로 컨테이너에 넘긴다. 그리고 큐의 정석
호출은 **인자가 없다**(`fleet.sh` 헤더의 예시부터 그렇다). 그런데 `bench/fleet_onepass.ST_FLAGS` 가
허용하는 플래그는 한 줌뿐이고 — `--drafter-dir` 과 `--tier-dir` 은 **거기 없다** — 그러면
그걸 `required` 로 받는 검사는 **큐가 시작은 해도 첫 줄에서 죽는다.**

여섯 중 셋이 그랬다:

| 허용된 검사 | 요구하던 것 |
|---|---|
| `engine_decode_graph_check.py` | `--ranks` |
| `engine_drafter_graph_check.py` | `--ranks`, `--ckpt-meta`, **`--drafter-dir`** |
| `engine_full_check.py` | `--ranks`, `--ckpt-meta`, **`--drafter-dir`**, **`--tier-dir`** |

굵은 둘은 `ST_FLAGS` 에 없으니 **어떤 호출로도 못 넘긴다** — 그 두 검사는 큐를 통해서는 영영 못 돈다.
#741 에서 `engine_graph_profile.py` 가 **이름만 있고 파일이 없던 것**과 같은 종류다: 허용 목록이
돌릴 수 없는 것을 약속하고 있었다.

전부 노드에서 되는 기본값을 줬다 — `facts.RANKS` · `facts.CKPT` · `drafter_mod.DRAFTER`, 그리고
full check 의 티어는 **실행마다 새 임시 디렉터리**(프로덕션 티어를 가리키지 않는다). 내 프로브도
`--ranks` 를 요구하고 있었다.

`tests/test_engine_graph_profile.py` 가 이제 **허용된 검사 중 어느 것도 인자를 요구하지 않는다**는 것을
박는다. 이름에 파일이 있는지(§90)와 함께, 큐가 약속한 것이 실제로 돌 수 있는지를 지킨다.

**부수**: 티켓은 `queued checkout revision changed` 로 멈춰 있었다 — 내가 큐에 건 뒤 `~/stkernel` 이
움직였고(그 사이 PR 셋이 머지됐다) 큐가 **다른 것을 돌리지 않으려고** 멈춘 것이다. 옳은 동작이다.
`edit` 로 같은 명령을 현재 체크아웃에 재제출하고 `resume` 했다(대기 시간은 유지된다).

### 45차 §96 — 관측 겹치기를 준비했다: observe 는 commit 전에 계산을 시작할 수 있다 (2026-09-12, srv4, 프로덕션 유지)

§93 이 "40% 차이는 드래프터가 아니라 스텝 자체" 라고 했다. 스텝 구조를 코드로 보니 디코드 스텝은 한
스트림에서 **forward → sample/verify → commit → observe → propose 를 직렬로** 낸다(스테이지 합 74.91 ms
= 스텝 74.99 ms; `depth=2` 파이프라인은 호스트만 가린다). 그리고 그건 진짜 사슬이다 — 타깃이 드래프터의
드래프트를 검증하므로 드래프터 27.3 ms(36.4%)가 임계경로에 얹힌다.

**한 군데가 겹칠 수 있다. 그걸 준비했다.** observe(관측)를 뜯어 보니 두 부분이다:

- **계산부**: `c = norm(fc·aux)` → `context_kv` → 레이어별 `norm_rope`. `aux` 와 `positions` 만 읽는다.
  그리고 `positions = ctx_before + arange` 인데, `decode_commit.advance` 가 돌려주는 `ctx_before` 는
  **스텝 진입 시점의 컨텍스트**다 — forward 전에 이미 아는 값이다(코드로 확인). 그래서 계산부는 `aux`
  (타깃 레이어 41/45 에서 준비됨) 만 있으면 된다.
- **쓰기부**: `write_draft_kv_rows(..., valid=valid)`. `valid`(=commit 이 수락한 드래프트 수) 만 이게 필요.

즉 **observe 의 계산은 forward 의 꼬리(레이어 42~44 + head) + sample + verify + commit 과 겹칠 수 있고,
쓰기만 commit 뒤로** 가면 된다. `valid` 는 어느 위치를 링에 쓸지 마스크할 뿐, 계산하는 K/V 값 자체엔
영향이 없다(교정 팩 마스크는 서빙 부팅에서 no-op).

**준비물(이 박스에서 검증됨):** `observe_rows` 의 fast 경로를 `observe_kv`(계산) + `observe_write`(쓰기)로
쪼갰다. `tests/test_engine_drafter.py::ObserveOverlapTests` 가 둘을 박는다:
(1) 분리가 **바이트 단위로** 단일 호출과 같다, (2) `observe_kv` 의 K/V 는 **`valid` 를 바꿔도 동일**하다 —
겹치기가 옳기 위한 전제.

**정직한 한계 — 크기는 창의 몫이다.** (1) 실제 겹치기는 캡처된 그래프 둘을 두 스트림에 얹고 레이어 41 의
aux 이벤트로 묶는 배선이고, 그건 served 레인(4랭크)이라 창이 있어야 짓고 잰다. (2) 이득의 상한은
`min(observe 계산, forward 꼬리 + head)` 인데, §84 가 프로덕션 observe 16 ms 를 이 박스에서 재현하지
못했으므로(하니스 0.66 ms) **계산부가 그 16 ms 중 얼마인지 모른다.** 큐에 걸린 `st-fwd-lanes` 프로파일이
forward 꼬리와 head 의 크기를, 그리고 다음 부팅의 `stage="observe"` 가 계산/쓰기 비중을 준다.
**이번 변경은 겹치기를 주장하지 않는다 — 전제가 성립함을 증명하고 그 위에 지을 바닥을 놓았을 뿐이다.**

### 45차 §97 — 서빙 중에 멎은 플릿: 같은 집합통신에 네 랭크가 서로 다른 형상으로 들어갔다 (2026-09-12 22:31, 프로덕션, 진단만)

`ff728f43` 를 띄워 원패스를 돌리던 중 **문이 멎었다**: `running []`, `waiting [0]`, `steps` 가 16분째
2298→2299 고정, 네 컨테이너는 전부 살아 있고 **GPU 는 넷 다 0%**. 요청이 대기열에 들어간 채 스텝이 안 돌았다.

**네 랭크의 스택이 전부 같은 자리였다:**

```
decode(adapter.py:1081) → _gather(:843) → comm.all_gather(comm.py:136) → all_gather_into_tensor
```

**그리고 NCCL watchdog 덤프에 원인이 있었다:**

| 노드 | OpType | NumelIn | 행 |
|---|---|---:|---:|
| srv2 (rank0) | `_ALLGATHER_BASE` | 271,040 | **7** |
| srv1 (rank1) | `_ALLGATHER_BASE` | 271,040 | **7** |
| srv3 (rank2) | `_ALLGATHER_BASE` | 232,320 | **6** |
| srv4 (rank3) | `_ALLGATHER_BASE` | 232,320 | **6** |

vp 38,720 으로 나누면 **일곱 행 대 여섯 행**이다. `all_gather_into_tensor` 은 모든 랭크가 같은 형상이어야
하는데 **그걸 검사하지 않는다 — 기다린다.** 그래서 영원히 멎는다.

**`_gather` 는 디코드 스텝에서 크기가 데이터에 의존하는 유일한 집합통신이다**: rich 행들로 만들고, 문법이
살아 있으면 각 행의 `masks.live` span 으로 만든다. 다른 스텝 형상은 구성상 합의돼 있고(D9) **이것만 계산된다.**

**고친 것**(PR #750, #752): 들어가기 전에 `all_reduce_host` 로 행 수를 투표하고, 어긋나면 **집합통신에 들어가지
않고** 랭크별 숫자를 달아 올린다. 나쁜 스텝에서만 시퀀스별로 한 번 더 투표해 **span 이 어긋났는지(문법)
드래프트가 어긋났는지(랭크별 생성기)** 까지 말한다.

**아직 모르는 것**: 왜 어긋났는가. 용의자는 둘 — `masks.live` 를 만든 문법 matcher, 또는 그 span 을 계산한
대상인 드래프트(`propose_sampled` 가 **랭크마다 생성기에서 뽑는다**). 2/2 로 갈린 것(0,1 대 2,3)이 노드 쌍과
겹치지만 **그건 정황이지 증거가 아니다.** 다음 발생이 한 줄로 답한다.

**증거가 사라지는 방식도 기록해 둔다**: 위의 모든 것은 `docker logs` 안에만 있었고, 다른 세션이 멎은 컨테이너를
치우면서 **함께 사라졌다**. 스텝 링은 0 바이트였다 — `DeathDump` 는 죽을 때 쓰는데 이 프로세스는 **살아서 멎어
있었다.** 살아서 멎은 엔진이 자기 상태를 덤프 디렉터리에 남기게 하는 것이 다음 일이다.

### 760차 — 4박스 없이 스텝을 잔다: 숙주 비용·관측·재계산의 세 경로 (2026-09-12, ost 노트북 macOS arm64·CPU, PR #760)

플릿을 못 잡은 날(큐 점유·엔진 리스·노트북) 스텝/레이턴시 숫자를 얻는 도구 셋을 만들었다. D17 은
그대로다 — 이 도구들은 판정 채널이 아니고, 도구 출력 스스로 그렇게 말한다. 규칙 6 을 이 항목에도
적용: 아래 숫자는 **맥 CPU 의 것이라 형상이 다르다**. 증거: `measurements/step_tools_nofleet_20260912/`.

**세 경로와 각각이 잰 것(아닌 것)**:

- `bench/step_sim.py` — 진짜 `Runner`/스케줄러/`Ring` 을 장치 없는 모델로. **스텝 루프의 숙주 비용**만
  잰다(커널이 없으니 서빙 속도와 무관). `--device-ms` 는 장치를 직렬 단일 서버 스레드로 놓아
  `decode_async` 의 겹침(depth)이 실제로 일어나게 한다.
- `bench/step_peek.py` — 살아있는 부팅의 `/metrics` **관측 전용**. 요청을 보내지 않고 큐/리스를
  건드리지 않는다. 남의 부팅을 보고 있으므로 독점성도 판정도 없다. `st:step_seconds`/TTFT
  히스토그램은 두 스크랩의 누적 차로 창 분포가 되고(버킷 내 보간), 없는 계열은 0 이 아니라 빈칸.
- `bench/step_replay.py` — 저장 증거의 **재계산**: `steps-*.ring`·onepass jsonl·bracket 다리·peek 샘플.
  onepass 기록 둘 이상이면 재부팅 없이 델타.

**이 맥에서 잰 숫자**:

- 숙주 비용(장치 0): decode med **0.039~0.044 ms/스텝**, prefill chunk med 0.042~0.05 ms.
  `StepMeta.build` med **6.3 µs/스텝** @128K(draft 3) — 커널 호출자의 flat array 비용.
  실측 세계(~20 step/s, 스텝 50 ms)에서 **숙주는 병목이 아니다**를 코드 수준으로 확인.
- 겹침(장치 50 ms 주입, depth 2): 측정 캐던스 **18.76 decode step/s**(이론 상한 20),
  숙주 여유 **99.9%**. ring 의 decode in-flight med **105.2 ms ≈ depth(2) × 50 ms** —
  async 스텝의 ring wall 은 launch→readback 이라 depth 대기가 포함된다는 해석 규칙의 실증.
- 재계산 검증: 플릿 증거 `st_onepass_20260912_0746` 를 노트북에서 다시 판 결과
  **10.96 step/s·수용률 46.2%·tokens/step 3.309** — 원본 증거와 동일한 숫자의 재생성.

**아직 없는 것**: peek 의 실물 관측값. 이 날 노트북에서 플릿 헤드(10.10.10.2)는 도달 불가였고,
네트워크 루프는 로컬 가짜 `/metrics` 로만 검증했다(스크랩당 20스텝 증가 → 39.7 step/s).
테스트 13 케이스(GPU·네트워크 없음) 통과, `tools/check.py` 1 ok.

### 45차 — GPU 하나면 되는 검사는 플릿이 아니라 **ost-97x 의 5050** 으로: 큐에 레인이 둘 (2026-09-12~13, 맥→srv4/srv2, PR #767)

운영자 "4gpu말고 1gpu만 필요한 작업은 ost-97x에서 5050으로 하게끔 하자 플릿예약에서".

**왜 필요했나.** §26 이 ST 검사를 큐에 세운 이유는 "같은 네 노드를 쓴다" 였다. 그런데 큐가 허용한 ST 검사
여섯(§95) 중 `--distributed` 가 아닌 것은 전부 **컨테이너 하나, GPU 하나**다 — 네 랭크가 한 카드 위의 스레드
넷이다(`docker run --gpus all <probe>`; engine/README 의 "한 노드, TP=4 스레드"). 그 검사가 플릿 보유자 자리를
차지하면 §90~§91 처럼 창을 기다리고, 돌 때는 스파크 넷을 잡고 하나만 쓴다. 한 장짜리 카드가 따로 있으면
거기서 돌면 된다.

**해결 — 큐에 레인을 둘로.**
- **분류는 승인 계약이 한다**: `fleet_onepass.validate` 가 `gpus` 를 돌려준다. ST 엔트리(`run_engine_check.sh`,
  `run_engine_probe.sh`)에 `--distributed` 가 없으면 1, 나머지(pair·chain·ab-lever·onepass·experiments·AR
  캠페인·`--distributed`)는 4. `run --gpu` 는 gpus=1 이고 boot 종류이면 `single` 로 바꾼다("needs one GPU, not
  four: single-GPU lane (5050 on ost-97x)"). `--fleet` 로 스파크를 고집할 수 있고, `FLEET_SINGLE_GPU_HOST=` 를
  비우면 레인이 꺼진다(그러면 예전대로 플릿). 레인 이름으로 부팅을 한 장에 보낼 수는 없다 — `single` 은
  gpus=1 이 아닌 명령을 거부한다.
- **레인은 자기 holder 를 가진다**: `holder-single`. 파일을 따로 둔 이유 — `holder` 를 읽는 것들(ST 런처, 유휴
  컨트롤러, restore debt, AR 캠페인)은 전부 그걸 **플릿**의 뜻으로 읽는다. 5050 의 검사가 그들에게 플릿 점유로
  보이면 안 된다. `fleet_handoff.holder_path(kind)` 가 한 곳에서 정한다.
- **서로 기다리지 않는다**: `_try_hold` 는 큐(하나, 우선순위로 정렬됨)에서 **자기 레인의 첫 줄**을 잡는다
  (`lane_front`). 부팅 뒤에 선 검사는 지금 돌고, 검사 뒤에 선 부팅도 지금 돈다. 단일 레인은 ST 엔진·서빙·레거시
  체인 검사를 건너뛴다 — 그건 스파크 얘기다.
- **증거는 그 호스트 것**: 허가 직전에 `bench/fleet_single.py` 가 ssh 로 `nvidia-smi --query-compute-apps` 를
  읽는다(20 초 캐시). 프로세스가 있으면 "busy outside this queue -- pid …", 못 닿거나 못 읽으면 "cannot say it is
  free" — 둘 다 거부(D3). 거부 사유는 **바뀔 때만** 로그에 적는다(초당 한 줄이 아니라).
- **실행은 러너가 그리로 간다**: 슈퍼바이저(`fleet_boot.py`)가 단일 레인의 페이로드에 `ST_PROBE_HOST` 를 준다 —
  페이로드의 env 접두사 **뒤에**, 명령이 자기 호스트를 고를 수 없도록(`validate` 는 명령 쪽 `ST_PROBE_HOST` 를
  거부한다). `probes/run_engine_probe.sh` 는 그 값이 있으면 `engine/`·`probes/` 를 rsync 하고, 컨테이너를 거기서
  돌리고, **플릿 리스를 잡지 않는다**. 여기서 죽으면 원격 컨테이너도 지운다(trap).
- 유휴 컨트롤러는 single 줄을 "돌 일" 로 세지 않고(`runnable_queue`), 단일 레인의 enqueue/acquire/release 는
  플릿 시계를 건드리지 않는다. `status` 에 `single (5050 on ost-97x): …` 한 줄, `kick [--force] single`,
  `show`/board 에 표시, 원장(ledger.tsv)에 kind `single`. `FLEET_RULES=2`.

**정직한 한계.** (1) ost-97x 쪽 준비는 이 변경이 하지 않는다: srv2 에서 BatchMode ssh(사용자는 플릿과 같은
`choiceoh` 를 가정; `FLEET_SINGLE_GPU_HOST=user@ost-97x` 로 바꿀 수 있다), docker + NVIDIA 런타임, 거기서 빌드한
`st-engine:glm53`(seed 이미지 ID 고정은 build.sh 그대로), 가중치가 필요한 검사면 `/home/choiceoh/models`. 없으면
러너가 ABORT 로 어느 것인지 말한다. (2) RTX 5050 은 sm_120 이고 스파크는 sm_121a, 메모리는 8 GiB — 거기서 나온
판정은 **그 카드의 것**이다. 가중치 검사(`--layers 0-4` 의 served 레인)가 8 GiB 에 들어가는지는 재 봐야 안다;
안 들어가면 `--fleet`. (3) ost-97x 는 2026-09-13 현재 srv2 에서 이름이 안 풀린다(DNS 도 `/etc/hosts` 도 없다). 호스트가 생기기
전까지 단일 레인 티켓은 그 이유를 말하며 기다리고(`status`·`wait`), `run --gpu --fleet` 가 우회다. 실물 5050
왕복은 아직 없다.

**검증.** (1) 맥(GPU 없음): 플릿 스위트 409 tests 중 실패 29 — 깨끗한 main 복사본도 같은 29 → 회귀 0.
(2) **srv4(Linux, aarch64)**: 같은 스위트를 임시 얕은 클론에서 브랜치와 origin/main 으로 각각 — 브랜치
`409 tests, FAILED (failures=27, errors=2)`, main `393 tests, FAILED (failures=27, errors=2)`. **그 29 는 맥 탓이 아니다**: srv4 에서도 main 이 똑같이 실패한다
(experiments·coalescing·feedback·retry — 이미 빨갛던 테스트들, 이 PR 밖). 새 12 케이스(`tests/test_fleet_single.py`)는
진짜 `flock`·`timeout` 위에서 전부 통과. (3) **srv2(컨트롤러, 라이브 큐, 읽기만)**:
이 브랜치의 `fleet.sh status` 가 `fleet: FREE` 옆에 `single (5050 on ost-97x): ost-97x: cannot read its GPU (rc 255:
ssh: Could not resolve hostname ost-97x …) -- this queue cannot say it is free` 를 답했다 — 레인은 보이고, 이름이 안
풀리는 호스트를 자유로 치지 않는다(D3). `classify` → gpu, `preflight --single` → PASS(`SKIP declared-knob check
(single: no launcher in the path)`).

**그때 본 것 하나(내 것 아님).** 큐의 `stkda-deferred0912v7` 이 매초 `hold refused: ST engine occupies the fleet
(lease: unreadable from …/runners/40b69dc8…)` 로 거부되고 있었다 — 러너 스냅샷에 `launchers/` 가 없어 리스를 못
읽는 기존 결함이고, "리스를 큐 아래로" 옮기는 다른 세션의 PR 이 닫는다. 단일 레인은 리스를 쓰지 않으므로 이
결함과 무관하다.

**부수 사고와 복구.** 브랜치에서 돌린 `preflight` 가 공유 진입점 `~/glm53-logs/fleet.sh` 를 rules 2 로 동기화해
버렸다(preflight 의 설계된 동작 — 머지된 코드라면 옳다). 머지 전에는 main 체크아웃의 preflight 가 "refusing to
move the shared entry back" 으로 FAIL 하는 창이 생기므로 즉시 main 의 사본으로 되돌렸다(md5 대조, rules 1).
머지 뒤 첫 preflight 가 정상적으로 2 로 올린다. 머지 안 된 브랜치로 컨트롤러에서 preflight 를 돌리지 말 것.

새 테스트 16: `tests/test_fleet_single.py` 12 — 증거 넷(자유·바쁨·불통·질의 실패, 사용자 지정, TTL 캐시, CLI),
계약 넷(셸과 모듈의 기본값 일치, holder 분리와 런처는 플릿 holder 만 읽음, 러너의 원격 경로에 리스 없음, gpus
셈), 그리고 **실제 `bench/fleet.sh` 의 `_try_hold`/`_release`/`_kick` 을 셸 심(ssh·docker·flock·timeout·date)으로
돌린 레인 시나리오** — A(boot) 보유 중 B(single) 허가, C(boot) 거부, 5050 이 바쁘거나 불통이면 D 거부(사유별 로그
한 줄), 풀리면 D 허가, A 반납 뒤 C 허가, `kick --force single`; 레인이 꺼지면 거부; `run --gpu` 가 준비 단계 전에
레인을 고르는 것과 `--fleet`·빈 호스트·`--distributed`·pair 는 플릿에 남는 것; `preflight --single` 이 부팅을
거부하는 것. 여기에 onepass 1(계약의 gpus 와 single 의 거부) + handoff 1(자기 holder, 플릿 시계·debt 불변) +
inspect 1(두 holder 나란히, 대기 대상은 자기 레인) + idle 1(single 줄은 플릿 일이 아님).
`test_fleet_experiments` 의 실제 승인 함수 추출 테스트는 새 한 줄 헬퍼 넷(`lane_of`·`holder_file`·`lane_front`·
`kind_of`)을 같이 뽑도록 고쳤다. `FLEET_AUDIT` 갱신. 이름: 규칙 9 대로 PR 번호다 — §97 로 썼다가 main 의
§97(PR #759)이 먼저 들어왔고, 그 사이 원장이 얇아졌다(PR #765).

### ST decode 22 step/s 후보 — GPU 구성요소 검증과 준비 구간 관측 (2026-09-13)

적용: GB10 한 장의 **구성요소 측정**이다. 엔진 22 step/s 달성 또는 기본값 채택의 근거가 아니다.
후보 `ccbea87c`, 공통 기준 main `21cb0539`, 런타임 `st-engine:main-ff728f43`.
공식 예약 `stdecode22bundle8` / `17892270522509140`에서 수치·변경 입력 CUDA 그래프 재생 12개가
통과했고 B/A/A/B를 측정했다. 앞선 BF16 테스트는 저장 피크 FP32와 기대값 BF16의 형식 차이로
실패하여 기대값을 저장 계약에 맞췄다. 수치 허용 오차는 완화하지 않았다.

| 구성요소 | B 두 구간 | A 두 구간 |
|---|---:|---:|
| 7행, 폭 20480 보정 Gram 관측·부분 flush | 15.453 / 15.406 ms | 0.669 / 0.670 ms |
| 1행, 같은 보정 경로 | 15.310 / 15.302 ms | 0.102 / 0.105 ms |
| 드래프터 5개 레이어 KV 쓰기 | 20.58 / 20.50 µs | 5.17 / 5.14 µs |
| FP32 라우터 투영: 가중치 변환 포함→상주 | 47.32 / 47.18 µs | 34.94 / 34.86 µs |

보정은 입력 BF16 값을 그대로 256행까지 모아 FP32로 누적한다. 일반 FP32 관측 경로는 유지한다.
라우터 가중치 42개는 부팅 때 변환하여 아레나에 상주시킨다(랭크당 189 MiB 예산 포함).
KV 통합 쓰기는 기존 개별 커널과 비트 동일하고 슬롯 패딩·거절 토큰·링 순환을 검증했다.
GLM 점별 연산의 이전 GPU 비교도 재현했다. 세부 수치·로그·실패 이력은
`measurements/st_decode_22step_20260912/README.md`와 `gpu-kernels-ccbea87c.log/json`에 있다.

후속 후보 `7b687301`은 추론 한도 경계 수정, 보정 저장 후 관측 중지, 부팅 임시 allocator 캐시 반환을
포함한다. 별도 GPU 예약 `stdecode22finalize3`에서 보정 수치·그래프·저장 후 재생 6개 검사를 통과했다.
운영자 지시로 수정 기준판은 측정하지 않고 KV를 7→6 GiB로 낮췄다. TP4/K6 네 랭크가 production
메모리 검사를 통과했다. 소비자 v6은 01:42~01:51의 harness-42 **준비 구간**만 실행했다.

| 준비 구간 | 실제 출력 tok/s | PR760 관측 창 중앙 step/s | 관측 창 드래프트 수용률 |
|---|---:|---:|---:|
| 2K 개별 3문제 | 73.00–79.45 | 18.903 (56창, pooled 18.572) | 51.82% |
| 32K 묶음 | 72.85 | 17.909 (초반 5창만) | 52.01% |
| 128K 묶음 | 71.93 | 별도 창 없음 | 별도 창 없음 |
| 고정 7200토큰 준비 2회 | 66.57 / 56.60 | 본측정 미완료 | 별도 창 없음 |

중단까지 누적 accepted/drafted=21536/44100 (48.83%). 세 2K 요청 모두 800 추론 토큰에서 문장 중간에
끊겼고 최종 정답 인증은 실패했다. 한도 부족이 유력하나 4096 대조 요청은 실행되지 않아 단독 원인으로
확정하지 않는다. 네 랭크 소스·이미지·부팅은 전후 동일하다(Docker mount 배열 순서만 정규화).
로그 보존 후 공식 stop으로 자기 부팅과 임대를 해제했다. 근거: `consumer-v6-stopped/`.

운영자 지시로 추가 측정을 중단하고 구현 통합을 진행한다. 원패스 두 판, C4, 품질 통과, 22 step/s는
**완료되지 않았다**. `no baseline on this build`; 맞춘 소비자 성능 향상률을 주장하지 않는다.
하니스 43은 개별 총/추론 8192/4096, 묶음 24576/12288로 늘리고 질문·정답·판정기는 유지한다.
증량 후 실측 정답률은 아직 없다. 새 예산의 원패스 CPU 검사 103개 통과. 이전 하니스 수치를 새 예산의
성능 근거로 재사용하지 않는다.
### 45차 — ost-97x 는 테일넷의 **Windows 박스**였다: 이름은 srv2 의 ssh 별칭이 풀고, 사용자·포트도 별칭이 정한다 (2026-09-13, 맥→srv2, PR #771)

PR #767 의 "준비물" 을 실제로 만들려고 ost-97x 부터 찾았다.

**찾은 것.** 맥·srv4·srv2 어디에도 DNS·mDNS·`/etc/hosts` 에 없다. 테일넷에 있다: **`OST-97X`**(테일넷 이름
`office-topsolar.tail7fec17.ts.net`, 100.116.174.65) — **Windows**, DERP 릴레이로만 닿고(직결 없음), 22·3389 닫힘.
집 LAN(192.168.68.0/22) 스윕에서 ssh 가 열린 호스트는 스파크 넷(srv1 .59, srv4 .72, srv3 .73, srv2 .77)뿐이다.
즉 이 박스에는 아직 sshd 가 없고, 리눅스도 아니다.

**한 것.** (1) srv2 `~/.ssh/config` 에 `Host ost-97x → HostName office-topsolar.tail7fec17.ts.net`(srv2 의 MagicDNS 가
푼다). `ssh -o BatchMode=yes ost-97x true` 의 답이 "Could not resolve hostname" 에서 "port 22: Connection timed out"
으로 바뀌었다 — 이름은 풀리고, 그쪽에 sshd 가 없다. `status` 의 single 줄도 이제 그렇게 말한다. (2) 코드: 레인이
`choiceoh@` 를 강제하지 않는다. `fleet_single.target()` 과 `probes/run_engine_probe.sh` 는 호스트를 준 그대로 쓰고,
컨트롤러의 ssh 별칭이 주소·사용자·포트를 정한다 — 그 박스는 스파크도, choiceoh 의 것도 아니다.

**막힌 것 — 운영자의 손이 필요하다.** (1) OST-97X 에 srv2 에서 BatchMode 로 닿는 sshd. 러너가 bash·rsync·docker
(NVIDIA 런타임)를 쓰므로 **WSL2 Ubuntu 안의 sshd** 가 실용적인 모양이다(테일넷 100.116.174.65:22 로 노출). srv2 의
공개키(`~/.ssh/id_ed25519.pub`, `choiceoh@spark4tb`)를 그 계정의 `authorized_keys` 에, 사용자·포트를 srv2 별칭에.
(2) **x86_64 ST 이미지가 없다.** 지금 이미지는 GB10 시드(aarch64) 위에 짓고, DeepGEMM 은 시드에서 뽑은
`_C.cpython-3xx-aarch64-linux-gnu.so` + JIT 헤더(821 파일)뿐 — `csrc` 가 없고, API 는 포크 것(`fp8_fp4_mqa_logits`,
`tf32_hc_prenorm_gemm`, 2.6.1). x86 에서 다시 지으려면 그 포크의 확장 소스가 있어야 한다. ST 검사는 전부
`deep_gemm` 을 임포트하므로, 이게 없는 한 5050 에서 도는 ST 검사는 없다. (3) 그때까지 단일 레인 티켓은
"Connection timed out" 을 말하며 기다리고, `run --gpu --fleet` 가 우회다.

**검증.** 맥: `test_fleet_single` 12 · onepass · source · ledger_numbering 통과, `FLEET_AUDIT` 갱신(`test_docs_status`
는 main 의 `bench/FLEET_BOOT_READINESS.md` 배너 누락으로 빨갛다 — 이 PR 밖, 칩으로 띄움). srv2: 별칭 실측은 위와
같다. 5050 실물 왕복은 여전히 없다.

### 773차 — 시뮬레이터가 기록의 나머지 숫자들도 맞춘다: 폴드아웃 검증으로 잡은 버그 셋 (2026-09-12~13, ost 노트북 macOS arm64·CPU, PR #773)

760차의 `step_sim` 은 스텝 캐던스 하나를 맞추고 나머지는 못 맞췄다. "다른 측정값과도 거의 일치"를 요구받고
네 기록(ST 엔진 + 노브가 서로 다른 glm53 부팅 셋: EP tiled·EP local·MLA)에 대해 폴드아웃 검증을 돌린 결과,
**일치하지 않던 것은 전부 모형의 버그였다**:

1. **수용률 매핑** — 원장의 `raw_acc` 는 accepted/drafted 비율이라 위치별 수용 확률은
   q≈0.75 인데(k=5, acc 46.2%에서) q=0.462 로 추첨해 tokens/step 이 절반쯤으로 나갔다.
   `per_position_acc()` 가 이등분법으로 q 를 푼다. 이 매핑은 프로브가 아니라 산술이다 —
   원장 숫자를 시뮬레이션에 옮기는 모두가 밟는 자리.
2. **행 기준** — tokens/step 을 스텝 수로 나눠 폭 2 스텝이 2배로 읽혔다. 행-스텝 기준
   (`st:decode_row_steps` 와 같은 정의)으로 고쳤다.
3. **도착 증폭** — 기록 e2e 누적으로 도착을 유도하니 시뮬이 살짝 느릴 때 다음 프리필이
   겹쳐 프리필-디코드 간섭이 되먹임으로 커졌다(32K e2e +171%). 하네스는 순차 실행이므로
   검증은 **closed-loop**(엔진이 한가해야 다음 제출, 폭 1)로 재현한다.

**폴딩·검증**: `fit_cost` 가 기록에서 상수를 폴딩한다(prefill 은 컨텍스트별 실측 처리량 계단 —
실제 토큰수·warm, decode ms 는 판정 채널의 역수, k·수용률은 스펙 카운터). `--against` 는
기록마다 closed-loop 재현 후 델타표를 내고 매 행에 [입력](폼 상수, 일치는 자명)과 [예측]
(폼에 얹히지 않은 값)을 단다.

**결과** — 예측 24행(ST 6 + glm53 18) 중 |잔여|>10% 는 **0행**:

| 기록 | 예측 행 최대 잔여 |
|---|---|
| ST-PROD-5734-20260912 (ST 엔진) | +4.9% (e2e 2K) |
| EPTILEDWORD4B (EP tiled) | +6.8% (TPOT) |
| EPONEPASS25B0 (EP local) | −6.7% (TTFT warm 2K) |
| MLAPREF50907B1PRIME (MLA) | +9.1% (TPOT) |

TTFT(warm) 32K/128K 는 네 기록 전부 ±0.5% 이내, tokens/step ±1.2% 이내, 클라이언트 tok/s·TPOT
−8.3~+2.4%. 남은 −5~−9% 클라이언트 체계 잔여는 모형 오차가 아니라 **기록 안의 채널 차이**다 — 같은
기록에서 창 메디안×tokens/step 와 요청별 실측이 2~5% 다르고, `--fit-channel client` 로 폼을 옮기면
step/s 가 +3.4% 예측이 되는 대칭이 관측된다. 어느 쪽에 맞추든 반대채널이 채널 차이만큼 벌어진다.

미계수는 명시돼 있다: decode 의 폭·컨텍스트 의존은 평탄 가정(요약 줄마다 표시). C=2/4 기록(#754)이
쌓이면 `--cost-json` 으로 폴딩할 자리. 증거: `measurements/step_tools_nofleet_20260912/sim_validate_4records.txt`.
D17 그대로 — 이것은 시뮬레이션이고 판정은 플릿 onepass 두 판이다.

### 45차 — 리스를 큐 아래로: 기록 하나, 주인은 큐, 인계는 이전 — 그리고 러너가 **영원히 거부**하고 있었다 (2026-09-12~13, 맥 + 리눅스 컨테이너, GPU·플릿 미검증, PR #770)

운영자 "리스와 큐가 왜 따로 있는거지? 리스를 큐의 기능화 하는게 좋지않나". **가르는 답**: 따로일 이유는 반쪽이다.
엔진이 양보 요청을 알아채려면 리스가 컨테이너 안에서 읽혀야 하고(bind-mount 된 `glm53-logs/`, stdlib 모듈), 큐를
안 거치는 부팅(수동 세션·슈퍼바이저·deploy-watch)이 있다 — 여기까지가 진짜다. 나머지는 하루 안에 생긴 역사다:
§27 은 "큐를 엔진에 넣을까" 만 따졌고 거울상은 따지지 않았다. 코드 자체가 과도기라고 적어 놓았다 — 런처 "until they
become one", 리스 모듈 "The queue takes the lease when it grants" — 그런데 **그 후반부는 구현된 적이 없었다.**
페이로드(런처·프로브 러너)가 리스를 잡고 큐는 `holder` 파일을 따로 썼다. 기록 둘, 생존 규칙 둘, 양방향 교차 검사.

**이음매가 낸 사고 — 오늘 것 셋에 더해, 살아 있던 결함 둘**(코드 판독 + 로컬 재현; srv2 실측은 아님):

1. **러너에서 도는 티켓은 플릿이 비어 있어도 영원히 거부됐다.** 큐의 대기 루프는 핀된 러너 스냅샷(`bench/`,
   `engine/`, `probes/`)에서 돌고 거기엔 `launchers/lib/fleet-lease.sh` 가 없었다(같은 날 PR #768 이 독립적으로 같은 구멍을
   찾아 헬퍼를 스냅샷에 핀했다; 이 PR 은 헬퍼 대신 모듈을 직접 읽어 큐의 `launchers/` 의존 자체를 없앤다). `st_engine_lease` 는 그때
   "unreadable -- this node cannot say the fleet is free" 를 답하고, #706 이후 그 답은 (옳게도) 점유로 친다.
   로컬에서 `fleet_pin.py` 로 러너를 핀해 재현: `lease: unreadable from .../runners/9637dec1... -- this node
   cannot say the fleet is free`. #706 이후 GO 를 받은 러너 티켓이 하나도 없는 이유다.
2. **슈퍼바이저는 옛 경로를 읽었다.** `st-glm53-supervisor.sh` 의 `fleet_taken` 이 `~/st-fleet.lock` 을 보는데
   런처는 `glm53-logs/st-fleet.lock` 에 쓴다. 티켓의 부팅(컨테이너 이름도 `st-glm53`)은 슈퍼바이저에게 보이지 않고,
   90초 뒤 건강검사 3회 실패로 `stop`→`start` 에 죽었을 것이다. 손으로 하던 A/B 스크립트(`run-exclusive-*.sh`)가 맨
   먼저 `systemctl stop st-glm53.service` 를 한 이유가 이거였다.
3. 덤: `holder_alive` 의 `read` 가 `local` 없이 호출자의 `s`/`pid` 를 덮어썼다(bash 동적 스코프). 죽은 홀더를
   킥한 바로 그 폴에서 `_try_hold` 의 세션이 죽은 홀더의 이름이 되어 GO 검사가 한 번 헛돌았다. 샌드박스가 잡았다.

**결정(운영자, 인터뷰 여섯 문답):** (1) 리스를 큐의 **권한**으로 — 기록과 모듈은 엔진 쪽에 둔다(컨테이너 안에서 읽혀야
하므로). (2) 모든 부팅은 리스를 쥔다; 벤치 부팅은 큐가 GO 때 `queue/<session>` 으로 잡아 소유자만 넘기고 런처·프로브
러너는 **검증만** 한다. 프로덕션은 `kind=production` 리스를 직접 잡는다(릴리스 트리에 `bench/` 가 없다). (3) 홀더를
대하는 규칙은 **kind** 로 갈린다: production 은 **정숙 게이트**(deploy-watch 의 규칙: `st:quiet`=1·요청 0 이
`FLEET_QUIET_S`=120 s 내내)일 때만 양보 요청, session 은 §91 대로 묻지 않음, 큐 자신은 자기 것. (4) 인계는 해제가
아니라 **이전**: 엔진 핸드오버의 끝은 요청자에게 소유권 이전(빈 창 없음). (5) 티켓이 끝나면 **대기 부팅 티켓이 있으면
그 티켓으로 이전, 없으면 해제** → 슈퍼바이저가 고정 릴리스 복구 ("대기 예약이 없을 때만 되돌리면 되지"). 대기 프로브
티켓은 프로덕션이 있어야 도니 해제 쪽. (6) PR 셋: 이것(리스를 큐 아래로) → ST 브래킷(`st-pair`/`st-chain`/`st-hold`,
sha 하나·프로덕션 형상) → 배포·큐 연결(D17 프로브 티켓, 컨트롤러 워크트리 추종).

**구현(이 PR; PR #767 의 단일 GPU 레인과 병합 — 그 레인은 플릿이 아니라 리스를 잡지 않는다):**
- `engine/base/fleet_lease.py`: 기록에 `kind`(session/production/queue/probe); `transfer`(이전), `verify`, `taken`(kind 로
  판정), `attach`(컨테이너 증거 뒤늦게), `withdraw_yield`, `door_load`(deploy-watch 의 `busy` 를 옮김 — 정숙의 정의 하나).
  `queue`/`probe` 리스는 pid 가 **결정적**: 헤드의 슈퍼바이저 프로세스가 곧 홀더라 죽으면 유예 없이 free. kind 없는
  옛 기록(지금 srv2 의 프로덕션 리스가 그렇다)과 평문 잠금은 **컨테이너 이름으로 판정**(st-glm53 = production) — 옛
  슈퍼바이저의 규칙 그대로. 이게 없으면 첫 배포가 자기 프로덕션 뒤에서 영원히 기다린다.
- `bench/fleet.sh`: `_try_hold` 가 GO 때 리스를 잡거나(빈 플릿) 이미 넘겨받은 것을 확인; `st_engine_ask` 가 kind 로
  묻는다(production 만, 정숙 게이트, 티켓당 한 번, `.asked.<s>` 마커); `_release` → `_lease_pass_on`(다음 부팅 티켓으로
  `transfer`, 없으면 `release`); `holder_alive` 는 부팅 홀더를 **리스로** 판정(리스 없는 옛 홀더·프로브 홀더는 전처럼
  pid); `_dequeue` 가 자기 요청을 거둔다(`withdraw-yield`); `yield`(프로브에 양보)는 `FLEET_KEEP_LEASE=1` 로 리스를 쥔
  채; 핀된 모듈에서 직접 읽는다(`launchers/` 헬퍼 참조 제거); `status`·`waiting:` 줄에 `lease:`; `FLEET_RULES=3`(PR #767 의 2 다음).
- `bench/fleet_pin.py` 가 `engine/base/fleet_lease.py`·`launchers/lib/fleet-lease.sh` 를 핀; `bench/fleet_boot.py` 가
  페이로드에 `ST_LEASE_OWNER=queue/<s>` 를 넘김; `bench/fleet_handoff.py next` = 이전 받을 다음 부팅 티켓(우선순위
  순, 살아 있는 슈퍼바이저, 프로브 제외).
- `engine/base/serve.py::_hand_over`: 요청이 지명한 기록(kind·pid·host)으로 `transfer`, 요청자가 없을 때만 `release`.
- `launchers/start-st-glm53.sh`: `ST_LEASE_OWNER`(검증만) / `ST_LEASE_KIND=production`(획득) / `ST_LEASE_KIND=session`
  (수동 세션 부팅, `st-hold` 가 오기 전까지의 다리) 셋 중 하나가 없으면 거부. `stop` 은 **홀더만**: 티켓은 소유자 일치,
  production/session 은 kind 일치, 사람은 `STOP_FORCE=1`; kind 이전 기록은 옛 규칙. 티켓 모드의 `stop` 은 리스를 놓지
  않고 `phase=stopped` 만 적는다(놓으면 다음 티켓에 못 넘긴다). 큐 holder 파일 검사(`FLEET_HOLDER`) 제거. 부팅 뒤 `attach`.
- `launchers/st-glm53-supervisor.sh`: 한 리스 파일을 `taken --kind production` 으로 판정(옛 경로는 옛 규칙으로 계속),
  자기 부팅은 `ST_LEASE_KIND=production`. `launchers/st-deploy-watch.py`: 리스가 프로덕션 것이 아니면 **미룸**(거부로
  기록하지 않음 — `cycle()` 에서 `deploy()` 전에), `busy`/`unsupported` 는 리스 모듈의 것.
- `probes/run_engine_probe.sh`: `ST_LEASE_OWNER` 검증만(획득·하트비트·해제 없음); 티켓 없는 GPU 프로브는 거부.

**검증(GPU 없음):** 리스 자가검증 통과. 리눅스 컨테이너(python:3.12 aarch64, CPU torch + triton):
`tests/test_engine_fleet_lease.py` **76 tests OK**(전 58; 새로 kind·이전·verify/taken·요청 회수·attach·정숙 정의 하나·
핸드오버가 실제 리스 파일을 이전함·러너 핀·슈퍼바이저 경로·deploy-watch 미룸·런처 거부/검증·`stop` 규칙·kind 이전 기록),
새 `tests/test_engine_fleet_queue.py` **6 시나리오** — `bench/fleet.sh` 의 진짜 함수들을 샌드박스(사설 FLEET_DIR·리스
파일, 테스트가 쓰는 `/metrics`, 컨테이너 없는 docker, ssh 없음)에서: 정숙 게이트 → 한 번의 요청 → 이전 → GO;
티켓→티켓 이전 → 큐 비면 해제; 대기 프로브는 해제 사유; session 홀더는 묻지 않음 + 취소 티켓은 요청 회수; 죽은 홀더
킥과 리스 회수; `status` 의 `lease:` 줄. `test_engine_fleet_ops`(슈퍼바이저·런처 실제 셸 흐름)·`test_engine_queue_lease_shell`
(PR #768 의 핀된 러너 셸 테스트, 새 설계로 갱신)·`test_st_deploy_watch`·`test_fleet_single`(PR #767 단일 GPU 레인)·`test_fleet_onepass`
도 OK. origin/main(PR #767·#768 포함)에 병합한 트리로 전체 스위트를 origin/main 사본과 대조: 플릿 스위트 **409**, 엔진 스위트
**991**(기준선 969 + 새 22) — 실패 집합이 기준선과 **완전히 같다**(플릿 28·2, 엔진 1·3, 전부 환경: 프로세스 정체·git·docker·모델
파일 없음), 회귀 0. `test_fleet_onepass`·`test_fleet_single` 의 옛 설계 핀(런처가 큐 holder 파일을 읽는다, 프로브가 리스를 잡는다)은
새 설계로 갱신, `FLEET_AUDIT` 재생성. 맥에서는 torch 없는 6개만 오류.
**실측 없음**: 플릿(10.10.10.x)이 이 맥에서 닿지 않는다.

**다음 창의 첫 일(srv2):** (1) `~/.config/systemd/user/st-glm53.service.d/release.conf` 드롭인을 새 릴리스로 옮기거나
지워 `~/st-engine` 을 따르게 — 슈퍼바이저 수정은 deploy-watch 로는 프로덕션에 닿지 않는다(실행 파일이 릴리스
`5734b29fde84` 에 고정). (2) `bash bench/fleet.sh status` 가 `lease: production ...` 을 읽는지. (3) 티켓 하나
(`run --gpu s -- bash probes/run_engine_probe.sh probes/engine_kernel_check.py`)가 정숙 게이트 → 양보 요청 → 엔진 이전
→ GO → 해제 → 슈퍼바이저 복구를 지나는지 — 이것이 §90 의 `st-fwd-lanes` 가 못 받은 GO 다. (4) PR 2(ST 브래킷), PR 3.

**남는 노출(정직하게):** 배포된 엔진(옛 릴리스)의 핸드오버는 여전히 `release()` 라 새 코드가 프로덕션에 오르기 전까지
빈 창이 남는다 — 그동안은 옛 런처의 `stop` 이 남의 리스(컨테이너 없는 큐 리스)를 거부하므로 슈퍼바이저가 티켓 부팅을
죽이지는 않는다. `--probe` 레인은 아직 ST 를 모른다(PR 3). `ST_LEASE_KIND=session` 수동 부팅은 `st-hold` 전까지의
다리이고, 그 `stop` 은 kind 만 맞으면 된다(세션끼리 서로 내릴 수 있음 — 지금과 같다).

### ST decode22 consumer scope correction (2026-09-13 00:55 KST)

The v3 baseline boot reached canonical preparation, not a completed measurement. Its PR #760 `step_peek` window was 11.93 median step/s. The candidate never ran. Preparation exposed a pre-existing pipeline bug: reasoning_budget=800 was ignored, so 2400-token requests returned only reasoning and no final answer. The owned driver was interrupted and the official launcher stopped its four ranks at 00:44:40, retaining logs and raw preparation requests.

`21a4c816` adds a conservative in-flight-aware boundary drain, with the existing rich sampler enforcing the cap and async decoding resuming after committed reasoning end. It also keeps hypothetical rejected draft end tokens from disabling the cap. Focused CPU validation: 19 reasoning/gate tests plus 35 async runner/pipeline/sampling tests passed.

The user explicitly requested **no corrected-baseline measurement** and to proceed directly with the corrected improved candidate. The next boot therefore runs two canonical onepass invocations on the candidate alone. Record absolute step/s against the 22 step/s target, actual output tok/s and quality, with `no baseline on this build`; do not claim a matched consumer speedup. Evidence: `measurements/st_decode_22step_20260912/`.
### 45차 — 단일 GPU 레인은 **srv4 한 대에서 프로덕션 옆에**: 증거는 여유 메모리, 첫 실측이 OOM 과 b12x m=8 을 가르쳐 줬다 (2026-09-13, 맥→srv2/srv4, PR #774)

**결정.** PR #771 뒤의 세 갈래(5050 = WSL2 sshd + x86 이미지 + DeepGEMM 포크 소스, 스파크 한 대 옆, OST-97X 우분투)
중 운영자가 **2 — 스파크 한 대 옆** 을 골랐다. 맥은 안 된다: 검사가 전부 CUDA(Triton·TileLang·CuTe DSL·DeepGEMM
JIT·nvcc MLA·b12x)다.

**설계.** 단일 레인의 기본 호스트는 srv4(`FLEET_SINGLE_GPU_HOST`), 프로덕션이 서빙 중인 박스다. 옆에서는 GPU 가
"비어 있는" 순간이 없으니 증거는 **여유**다: 그 박스의 MemAvailable 에서 검사의 예산(`ST_PROBE_GIB`, 기본 8 GiB)을
뺀 값이 `--test` 부팅이 지키는 16 GiB 바닥(`boot.TEST_FLOOR_GIB`)을 넘어야 하고, 박스당 프로브 컨테이너는 하나,
못 닿으면 여유 없음(D3). 플릿 박스에서는 두 레인이 서로 배타다 — 플릿 **boot** 는 그 박스의 빈 메모리를 admission 에
쓰고, 검사는 earlyoom 이 먼저 찾을 표적이니까 — 서빙 옆에서는 동시에 돈다. 러너는 그 박스에서 **프로덕션이 돌리는
이미지**(`docker inspect st-glm53`)로 컨테이너를 띄우고 리스는 잡지 않는다. #770(리스를 큐 아래로) 위에 다시 세웠다:
단일 레인은 여전히 리스와 무관하고, 배타 규칙은 리스·점유 검사 바로 뒤에 선다. `FLEET_RULES=4`. 한 박스 안에서만
성립하는 규칙이라 `on_fleet`(srv1-4·패브릭 주소·spark*)이 이름으로 가르고, 제 것인 박스(ost-97x)에서는 레인이 서로
안 기다린다.

**실측 1 — 01:06, srv2 에서 라이브 큐에 티켓(브랜치 7bd41141, LOGD 는 임시 디렉터리라 공유 진입점을 건드리지 않았다).**
분류 "needs one GPU, not four" → preflight PASS → GO(holder-single) → srv4 에 rsync, 이미지 `st-engine:decode22-a`
(그 순간 srv4 에는 codex 세션의 부팅 `st-decode22-A` 가 6 분째) → 임포트 76 모듈 OK → 첫 `torch.randn(device="cuda")`
에서 **CUDA out of memory**, 10 초 만에 실패. MemAvailable 은 26.7 GiB 였다. 원인: 그 숫자는 페이지 캐시를 세고, 방금
부팅이 free 페이지를 다 fault 한 뒤라 **장치 할당이 쓸 수 있는 페이지가 없었다** — 엔진의 arena 가 정확히 같은 이유로
`touch_pages` 를 하는 것(`engine/base/arena.py prepare_allocation`)을 레인은 몰랐다. 바닥 규칙만으로는 부족하다.

**보강(5fb71658).** 러너가 컨테이너 직전에 `fleet_single.py reclaim` 을 그 박스에서 돌린다: MemFree 가 예산을 덮으면
그대로, 아니면 바닥 안에서 예산만큼 익명 페이지를 `MAP_POPULATE` 로 잠깐 잡았다 놓아 캐시를 밀어낸다(arena 의
touch_pages 그대로); 그래도 모자라면(`short`) 박스가 아직 fault 중인 것(부팅)이니 기다린다. srv2→srv4 실측: evidence
는 도는 중인 프로브를 `a probe is already running there (st-probe-srv2-2705884)` 로 잡았고, reclaim 은 MemFree
14.9/85 GiB 에서 각각 rc 0.

**실측 2 — 01:08~01:12, 같은 티켓을 다시(보강 전 코드).** 요청 직후 큐가 `hold refused (single): srv4: no room beside
production -- MemAvailable 23.7 GiB, this check's budget 8.0 GiB, floor 16.0: 15.7 GiB would be left` 로 20 초 기다리게
했고(가드가 처음으로 일했다), 여유가 생기자 GO → 페이로드 196 초: kda 6 케이스 통과, mhc(1·6·8·65 토큰) 수치, indexer·
kpool 통과, mla 부팅 자가검사·그래프 재생 3 통과, moe m=1 통과, moe_graph 통과 — 그리고 **moe m=8 에서 `b12x
migration` 오차 0.295 > 허용 0.02 로 실패**. 바로 앞에 `compiled micro kernel did not expose a CUDA library ->
disabling the direct micro MoE backend` 경고가 있었다. 이건 **이미지 `st-engine:decode22-a`(codex 세션의 후보 빌드)에
대한 판정**이지 레인의 결함이 아니다 — 레인이 플릿을 안 잡고 3 분 만에 그걸 찾아낸 것이 이 항목의 실측이다. 로그
`fleet/run-logs/8f920027…log`, 원장 행 `st-kc-lane single 3m`. 그 빌드의 주인이 볼 일이다(칩으로 띄움).

**한계.** (1) 랭크 파일 넷이 필요한 검사는 한 노드에서 못 돈다(srv4 에는 `rank3of4` 뿐) — `run --gpu --fleet`.
지금 레인의 일감은 `engine_kernel_check.py` 다. (2) 예산은 선언값이지 잰 값이 아니다: 예산보다 더 먹는 검사는 여전히
earlyoom 의 몫이고, 무거운 검사는 `ST_PROBE_GIB` 를 올려 그만큼의 여유를 기다리게 한다. (3) 세션 부팅(리스 kind
`session`)은 큐의 holder 가 아니라 배타 규칙이 못 본다 — 실측 1 이 그 창이었고, 지금은 reclaim/MemFree 대기가 막는다.

**검증.** 맥: `test_fleet_single` 17(여유·프로브·불통·reclaim 의 넷·호스트 판별 셸/파이썬 일치·캐시·셸 시나리오 셋),
`test_fleet_experiments` 의 실제 승인 함수 추출 테스트(#770 이후 main 에서 `st_engine_up`·`lease_mine` 미정의로 빨갛던
것을 스텁으로 고침), `test_engine_fleet_lease`(`FLEET_RULES` 는 "3 이상"으로), fleet_queue·queue_lease_shell·onepass·
source·ledger_numbering·docs_status — 이 맥에서 돌 수 있는 것 전부 통과. CI engine check success(7bd41141, 5fb71658).
`FLEET_AUDIT` 갱신. 실물: 위의 둘.
### 45차 — ST 브래킷을 큐에: 커밋 하나가 팔, 프로덕션 형상, 부팅당 두 판 — 큐가 드디어 **부팅할 것**을 얻었다 (2026-09-13, 맥 + 리눅스 컨테이너, GPU·플릿 미검증, PR #775)

PR #770 으로 큐는 ST 엔진의 리스를 쥘 수 있게 됐지만 **부팅할 것이 없었다.** 승인된 부팅 러너는 vLLM 시대의
`pair.sh`·`chain.sh`·`ab-lever.sh`(`VLLM_*` 노브, `start-glm53`) 뿐이라, §93 의 D17 측정은 손으로 했고 후보 비교는
`measurements/st_native_20260912/run-exclusive-*.sh` 다섯 벌의 손글루(서비스 내리기 → iptables → 드레인 대기 → 8001 부팅 →
onepass → 복구)로 했다. 39차가 "손으로 쓴 체인의 버그 셋" 때문에 `pair.sh` 를 만든 그 자리다. 운영자 인터뷰(§97 의 문답
4)에서 정한 대로 만들었다.

**팔 = origin 에 있는 sha 하나, 프로덕션 형상.** `bench/st_bracket.sh` 가 `launchers/st_release.py` 로 `~/st-releases/<sha12>`
를 자르고(deploy-watch 의 `cut()` 을 이 모듈로 옮겨 **절단은 하나** — 이긴 팔은 프로덕션이 그 디렉터리를 가리키면 승격),
네 노드에 밀어 `ST_ENGINE_DIR` 로 잡고(기본값이면 `push_tree` 가 프로덕션의 `~/st-engine` 을 덮어쓴다 — 손글루가 그렇게
한 이유), `~/.config/st-glm53.env` 의 형상(KV·행 수·`ST_PRODUCTION=1`)으로 **포트 8001** 에, 티켓별 티어·덤프 디렉터리로
띄운다. STK_ 노브 팔은 없다(`--production` 이 거부한다; sha 가 곧 팔이다). 릴리스의 **자기 런처**가 티켓의 리스
(`ST_LEASE_OWNER`)를 검증하므로 **팔은 #770 이후의 sha 여야 한다** — 그 전 런처는 큐가 이미 쥔 리스를 잡으려다 죽는다.

**레그는 고정(D17):** 부팅 → onepass 1회차(콜드 열: TTFT·컴파일 꼬리) → `POST /v1/prefix/reset`(§55) → 2회차(웜 열) → stop.
둘 다 `--require-exclusive`. `bench/st_judge.py` 가 **웜끼리** 판정하고 콜드 열은 옆에 찍는다(§93 의 표). 바닥은 기준선의
부팅 간 편차 — 부팅이 둘 미만이면 "바닥 없음" 이라고 말한다(§93 이 "D17 의 두 판은 컴파일 꼬리를 걸러 주지 부팅 간 변동은
못 걸러 준다" 고 적은 그것). 게이트(품질 전부·한국어 0·트래픽 없음)를 못 넘긴 기록은 증거가 아니다. 기록의 정체성은
브래킷이 지명한 sha(`arm_sha`) 또는 런처가 컨테이너 env 에 찍은 릴리스(`ST_RELEASE`) — `onepass.py` 의 `_served_build` 가
`st-glm53` 컨테이너를 보면 런타임 매니페스트의 `engine_source_sha256`·`ST_RELEASE`·STK_ 노브를 적는다. 전엔 ST 기록에
**vLLM 오버레이 스탬프**가 찍혀 릴리스가 달라도 같은 빌드로 보였다(측정 디렉터리의 `run_onepass_st.py` 가 몽키패치로 풀어
두던 것을 본체로). `bracket.py` 는 `vllm:iteration_tokens_total_count` 가 없으면 `st:steps_prefill_total + st:steps_decode_total`
을 스텝 카운터로 읽는다.

**문법:** `fleet.sh st-pair s <sha> [--base <sha>] [est] [note]`(base 기본 = `deploy-state.json` 의 deployed; base sha 에 웜
표본이 있으면 base 부팅 생략), `fleet.sh st-chain s [est] [note] -- A=<sha> B=<sha> A B`(반복 이름 = 같은 커밋의 다음 부팅
→ `A B A B` 가 §93 이 요구한 교대), `fleet.sh st-hold s <sha> [est]`(세션 창; `fleet.sh cancel s` 로 끝낸다). 셋 다 부팅
티켓이라 큐가 GO 때 리스를 잡는다. 승인(`fleet_onepass.py`)은 sha·리터럴 이름 문법만 받고 onepass·judge·릴리스 절단과
함께 바이트 고정한다; 런처는 팔의 것이라 고정하지 않는다. `fleet_prepare` 는 이 티켓의 리비전을 체크아웃 HEAD 가 아니라
**팔의 sha** 로 적어, 컨트롤러 체크아웃이 움직여도 "queued checkout revision changed"(§95) 로 멈추지 않는다.

**런처 정정 둘:** 티켓 리스에 컨테이너를 붙이던 `attach`(#770)를 뺐다 — kind 이전의 슈퍼바이저(지금 배포된 것)는 `stop` 을
컨테이너 이름으로 풀기 때문에 리스가 `st-glm53` 을 이름하면 그 크래시 복구가 티켓 부팅을 죽인다; 이름이 없으면 옛 `stop` 은
거부한다. 컨테이너 env 에 `ST_RELEASE` 를 넣는다.

**검증(GPU 없음):** 새 `tests/test_fleet_st_bracket.py` — 승인 문법·핀·단일 GPU 레인 거부, `st_release.cut` 을 git 픽스처에서,
judge 의 바닥·게이트·정체성·CLI, 그리고 **리허설 끝까지**(`FLEET_REHEARSE=1`: pair 두 판×두 팔 → 판정 → base 재사용,
deployed 기본 base, chain `A B A B`, hold). 리눅스 컨테이너에서 관련 모듈 247 tests OK; 플릿 스위트 **433**(+24), 엔진 991,
onepass 102 를 origin/main 사본과 대조해 새 실패 0. **실측 없음.**

**다음 창(srv2):** #770 의 셋(드롭인 이동 → `status` 의 `lease:` → 프로브 티켓 하나) 다음에 `fleet.sh st-pair <s> <sha>` —
#770 이후 sha 하나를 deployed 에 대고 — 가 정숙 게이트 → 이전 → 두 판 → base 부팅(표본이 없으므로) → 판정 → 해제 → 복구를
지나는지. 그리고 PR 3: 배포마다 D17 프로브 티켓(유휴 프로덕션 위 `--probe` 레인의 ST 인식)이 base 표본을 공짜로 남기게, 컨트롤러
워크트리가 배포된 sha 를 따르게.

**남는 노출:** base 재사용은 base sha 의 웜 표본이 있을 때만이라 PR 3 전엔 `st-pair` 가 base 도 부팅한다(팔당 ~10분). 후보가
도는 동안 프로덕션(:8000)은 내려가 있다(#770 의 설계 그대로: 정숙 게이트가 문이고 대기 부팅 티켓이 없을 때만 되돌린다).

### 45차 — 배포와 큐가 만난다: D17 프로브 티켓, 라이브 레인의 ST 인식, 배포된 sha 를 따르는 컨트롤러 (2026-09-13, 맥 + 리눅스 컨테이너, GPU·플릿 미검증, PR #778 — 처음 #776 으로 열렸으나 스택의 base 브랜치에 병합돼 main 에 다시 올림)

PR #775 뒤에 남은 자리 둘. `st-pair` 의 "base 재사용" 은 base sha 의 **웜 표본**이 있을 때만이라 배포된 커밋의 표본이 없으면
팔당 ~10분을 base 부팅에 더 썼고, 큐의 라이브 레인(`run --gpu --probe`)은 vLLM 시대의 `glm53` 컨테이너와 `/health` 만 알아
ST 엔진 옆에서는 쓸 수 없었다. 그리고 §91 — 컨트롤러 체크아웃이 main 보다 639 커밋 뒤라 네 노드가 서빙 중인 플릿을 FREE 라고
답한 일 — 은 사람이 체크아웃을 옮기는 한 되풀이된다. 운영자 인터뷰(§97 의 문답 5)에서 정한 대로 셋을 한 PR 로 잇는다.

**라이브 레인의 ST 인식** (`bench/fleet.sh`). `serving_up`·`booting`·`serving_idle` 이 `st-glm53` 을 프로덕션으로 읽고, 유휴 판정은
문 자신의 `st:quiet`(정숙 게이트와 **같은 읽기** — `fleet_lease load`; `st:quiet` 를 못 내는 옛 엔진은 요청 게이지 0 + `/v1/models` 200,
답 없는 문은 유휴가 아니다)로 한다. `_try_hold` 에서 **프로브 티켓은 `production` 리스 옆에서** GO 한다: 프로덕션의 리스는 프로브에게
점유가 아니고, 유휴한 문이 조건이고, 리스는 잡지 않는다. `session` 이나 다른 티켓의 부팅 뒤에서는 남들처럼 기다리고, 세션 홀더에게는
여전히 묻지 않는다(#770 의 규칙 그대로).

**`fleet.sh st-probe [--detach] s [sha] [est] [note]`** → `bench/st_bracket.sh probe`: 라이브 문에서 onepass 두 판, 각 판 앞에
`POST /v1/prefix/reset`, 부팅 없음·리스 없음, sha 를 안 주면 `deploy-state.json` 의 배포된 커밋. 첫 판은 부팅이 아니라 리셋 뒤라
기록에 `cold=reset`(onepass 가 `ST_BRACKET_COLD` 를 배움)이고 `st_judge.colds` 가 콜드 열(부팅의 것)에서 뺀다. 승인
(`bench/fleet_onepass.py`)은 라이브 레인에 `bench/onepass.py` 와 브래킷의 `probe` 만 두고, 부팅 레인에 온 `probe` 는 "라이브 레인의
것" 이라고 거부한다.

**deploy-watch 의 배포 후 훅** (`launchers/st-deploy-watch.py`). 배포를 **기록한 뒤에만** — 실패한 런치 뒤도, dry-run 도 아니다 —
① 큐의 체크아웃 `~/fleet-controller`(`--controller` / `FLEET_CONTROLLER_REPO`; `--no-follow`)를 배포된 커밋으로 옮긴다(`fetch origin
<sha>` → `checkout --detach`; sha 를 거부하는 원격이면 refs 전체 fetch 로 한 번 더). ② `d17-<sha12>` 프로브 티켓 하나를 건다
(`st-probe --detach`, `--no-probe` 로 끔). 둘 다 배포를 실패시키지 못한다 — 로그 한 줄이 전부다. 이로써 배포된 커밋은 언제나 웜 표본을
갖고 `st-pair` 는 base 를 부팅하지 않으며, 큐는 프로덕션의 규칙으로 답한다.

**검증** (리눅스 컨테이너, GPU·플릿 없음): `tests/test_engine_fleet_queue.py` 의 샌드박스(진짜 `_try_hold`/`_release`)에 유휴 production
리스 옆의 프로브 — 문이 바쁘면 거부, 정숙하면 GO, 리스는 잡지도 놓지도 않음, 세션 부팅 뒤에서는 묻지 않고 대기; 샌드박스의
`serving_idle`/`booting` 은 이제 **진짜 함수**(가짜 docker/curl 이 먹인다). `tests/test_fleet_st_bracket.py` 에 레인별 `probe` 문법·거부,
`cold=reset` 의 콜드 열 제외, 프로브 리허설(두 판, deployed 기본 sha). `tests/test_st_deploy_watch.py` 에 진짜 clone 을 배포 커밋으로
옮기는 검사, 티켓 argv 고정, 거부 경로 셋(rc≠0·`fleet.sh` 없음·체크아웃 아님), dry-run 과 두 플래그, "기록된 배포 뒤에만" 소스 핀.
플릿 스위트(414)·엔진 스위트: main(#774) 기준선과 **실패 집합 동일**(27F+2E / 1F+3E, 환경), 새 실패 0. `FLEET_AUDIT` 갱신.

**병합에서 배운 것.** 이 사이 #770 은 main 에 **스쿼시**로 들어갔고 #774(단일 GPU 레인, srv4 옆)가 뒤따랐다. 그리고 이 항목 자체: #776 은 base 가 PR 2 의 브랜치인 채로 병합 버튼이 눌려 **그 브랜치로** 스쿼시됐다(#775 스쿼시 9초 뒤) — main 에는 아무것도 안 들어왔고, 두 커밋을 main 위에 체리픽해 #778 로 다시 올렸다. 스택 PR 은 아래가 들어간 뒤 base 를 main 으로 돌린 다음에 눌러야 한다. #775 는 main 위로 리베이스
(충돌은 `FLEET_AUDIT` 한 줄과 원장 둘 — 둘 다 그대로 겹쳐 둠), #776 은 그 위로. `fleet.sh` 에서 #774 의 단일 레인 `elif` 가 이 PR 의
`if` 조건(프로브·production 예외) 뒤에 붙으면서 **프로덕션 옆 프로브까지** 그 분기에 걸릴 수 있었다 — `[ "$kind" = single ]` 로
못 박았다. 세 갈래 병합이 문법상 깨끗해도 조건의 **여집합**은 바뀐다는 것: 샌드박스 테스트가 아니라 읽어서 잡았다.

**실측 없음.** 첫 실제 D17 프로브는 이 PR 을 담은 첫 배포 **다음** 배포부터 deploy-watch 가 건다(deploy-watch 자체가 `~/st-engine` 의
rsync 본이라). 운영자: `~/fleet-controller` 가 git 체크아웃인지 확인(아니면 훅이 "not a checkout" 을 남기고 넘어간다), 유휴 복구
타이머(`fleet-idle-recovery.service`)는 아직 `~/stkernel` 을 본다(이 PR 은 유닛을 안 건드렸다), 그리고 #770 의 `release.conf` 드롭인.

**남는 노출:** 프로브의 onepass 는 `--require-exclusive` 라 GO 뒤에 사용자가 오면 판이 중단되고 기록은 안 남는다(정직한 실패 —
큐가 다시 걸지는 않으니 `st-probe` 를 다시 건다). `serving_idle` 의 옛 엔진 분기는 NVMe 티어 이동 중을 유휴로 볼 수 있다(#770 뒤
엔진은 모두 `st:quiet` 를 낸다).

### 45차 — 움직인 체크아웃은 큐가 **다시 핀**하고, 유휴 복구는 배포된 체크아웃에서 돌고, D17 표본은 스스로 채워진다 (2026-09-13, 맥 + 리눅스 컨테이너, GPU·플릿 미검증, PR #782)

#778 뒤에 "PR 4 도 필요하냐" 는 물음에 셋을 답했고, 운영자가 셋 다 하라고 했다("자동 재핀해 그리고 작고 유용한 2개도 해").
§95 의 결정 하나를 뒤집는 것이 들어 있으니 그 경위를 적는다.

**자동 재핀** (`bench/fleet_prepare.py`, `bench/fleet_pending.py`). §95 는 대기 중 체크아웃이 움직인 티켓을 `queued checkout
revision changed` 로 **멈추는** 쪽을 택했다 — "큐가 다른 것을 돌리지 않으려고 멈춘 것이다. 옳은 동작이다" — 그리고 사람이 `edit` 로
같은 명령을 현재 체크아웃에 재제출하고 `resume` 했다. 운영자는 그 `edit` 를 큐의 것으로 하라고 했다. GO 앞 검사(`check_pending`)가
영수증의 HEAD 와 지금의 HEAD 가 다르면 `fleet_pending.edit(repin=…)` 로 같은 명령을 **지금 있는 리비전**에 다시 준비한다 — 입력
다이제스트·문법·CPU 준비·배포 승인 전부 새로, 옛 영수증에서 재사용하는 것은 없다 — 레코드의 revision 이 오르고 history 에 두 커밋이
적힌다(`show`). 자리와 나이는 그대로. 새 트리가 검사를 못 넘기면(컴파일조차 안 되면 `re-pin at … failed`) 전처럼 이유와 함께 멈춘다.
`FLEET_AUTO_REPIN=0` 이면 §95 그대로. `validate()` 자체는 여전히 엄격하다(영수증은 제 리비전과 맞아야 한다) — 재핀은 그 위의
`check_pending` 에서만 일어난다. sha 가 팔인 ST 티켓은 애초에 HEAD 를 보지 않는다(#775).

**유휴 복구 유닛** (`launchers/fleet-idle-recovery.service`). `~/stkernel` 이 아니라 `~/fleet-controller` — deploy-watch 가 배포된 커밋에
맞춰 두는 큐의 체크아웃(#778) — 에서 돈다. `fleet_idle.py` 는 제 파일의 부모를 ROOT 로 삼으니 옮기는 데 코드 변경은 없다. 운영자:
유닛 재설치(`install` + `daemon-reload`).

**D17 표본 자가 치유** (`launchers/st-deploy-watch.py`). 배포 직후의 프로브 티켓은 첫 장일 뿐이다 — `--require-exclusive` 인 판은
사용자가 오면 기록 없이 끝난다. 이제 "배포할 것 없음" 인 사이클마다 컨트롤러의 판정기(`bench/st_judge.py samples`)에 배포된 커밋의
**웜 표본**이 있는지 묻고, 없고 `d17-<sha12>` 티켓이 큐에도 홀더에도 없으면 한 장 더 건다: sha 당 `--probe-attempts`(3)까지,
`--probe-gap`(1800 s) 간격, `deploy-state.json` 의 `probe` 에 집계, 포기는 한 번만 말한다. 판정기에 물을 수 없으면 아무것도 걸지
않는다(None 은 0 이 아니다). 표본의 정의는 하나 — 판정기의 것(웜·유효·부팅당 하나)이라 deploy-watch 는 세지 않고 **묻는다**.

**프로브의 정체성 가드** (`bench/st_bracket.sh probe`). 자가 치유를 넣으면서 보인 구멍: 배포 전에 걸린 프로브가 배포 뒤에 돌면 새
엔진의 수치를 옛 sha 로 적는다(기록의 `arm_sha` 는 건 사람의 주장이다). 문의 컨테이너 `ST_RELEASE`(릴리스 디렉터리 이름 = sha12)가
건 sha 와 다르면 첫 판 앞에서 거부하고 `fleet.sh st-probe s <served>` 를 안내한다. 릴리스를 말하지 않는 옛 부팅은 믿고 간다.

**검증** (리눅스 컨테이너, GPU·플릿 없음): `tests/test_fleet_prepare.py` 에 진짜 큐 레코드(이 프로세스가 주인, `/proc` 필요 → 리눅스
전용)로 재핀 **끝까지** — 안 움직이면 조용, 움직이면 새 영수증·revision 2·history·자리 유지·다음 검사 조용, 스위치 끄면 §95 의 거부,
깨진 트리는 멈추고 티켓 그대로. `tests/test_st_deploy_watch.py` 에 자가 치유의 가지 전부(표본 없음/있음/콜드 판/다른 sha/큐·홀더에
이미 있음/3장 뒤 포기/간격/off 스위치/판정기 없음)와 유닛 경로 핀, "배포할 것 없음일 때만" 소스 핀. `tests/test_fleet_st_bracket.py`
에 가드 핀. 플릿·엔진 스위트는 main 기준선과 실패 집합 동일(27F+2E / 1F+3E), 새 실패 0. `FLEET_AUDIT` 갱신.

**실측 (srv2, 02:30 KST, 운영자 "너가 srv2 에 하면 되잖아").** 이 브랜치의 워크트리에서 프로브 티켓 `repin-trial` 을 걸었다 — 큐는
`prefill3000-batch10i`(codex 세션의 부팅, 리스 `queue/…`)가 쥐고 있어 "a queue holder is not asked" 로 기다렸다 — 워크트리 HEAD 를
빈 커밋으로 옮기니(2cb63b1e → 6d63b897) **30 초 뒤** 레코드가 revision 2, history 에 `checkout moved 2cb63b1ecd68 -> 6d63b897eb17:
prepared again at 6d63b897eb17`, 영수증은 새 것(379e4c28 → 39575ebc), 대기 로그에 `RE-PIN repin-trial: …`. `cancel` 로 GO 전에
거뒀고 홀더는 건드리지 않았다. 같은 워크트리에서 이 PR 의 리눅스 전용 테스트 83 개도 OK. 그리고 srv2 에서 한 것: `~/fleet-controller`
를 f9e776c2(#612 시절, 639 커밋 뒤의 §91 그 상태)에서 **origin/main 639bd63a(#783)** 로 옮겼고(깨끗한 워크트리, 쓰는 프로세스 없음),
새 유휴 복구 유닛을 설치했다(`daemon-reload`; 타이머는 운영자가 꺼 둔 대로 **disabled** 유지, 백업 `.bak-20260913`). **안 한 것**과 이유:
슈퍼바이저 드롭인은 그대로다 — `~/.config/st-glm53.env` 가 프로덕션 트리를 `st-releases/prod-abceb6a0-grammar-9391`(codex 가 자른
릴리스, main 에 없는 grammar 패치)로, CKPT 를 `5734b29fde84/st-glm53-meta` 로 박아 두었고 슈퍼바이저 스크립트는 5734b29f(#770 이전)다.
프로덕션은 20:36 부터 내려 있고(세션들이 큐로 플릿을 번갈아 쓴다) deploy-watch 유닛은 srv2 에 **설치된 적이 없다**(`not-found`,
`~/st-engine` 에 `st-deploy-watch.py` 없음). 즉 #770 뒤의 프로덕션 리스·컨트롤러 추종·D17 프로브·자가 치유는 모두 deploy-watch 를 무장하고
드롭인·env 를 main 의 릴리스로 옮길 때 비로소 산다 — 어느 트리를 프로덕션으로 할지는 운영자의 결정이라 손대지 않았다.
남는 노출: 재핀은 준비를 통째로 다시 하므로 CPU 준비 명령이 있는 티켓은 그만큼 대기 루프가 멈춘다 — §95 에서 사람이 `edit` 하던 비용과 같다.


### 45차 — ST C=1 일곱 행 디코드: W4 경로 복구 · 라우터 TC · 공유 전문가 융합 (2026-09-13, PR #789, 소비자 검증 진행 중)

목표는 TP4/SPEC_K=6/C=1의 22 step/s다. 기존 중단 런 `7b6873019af6`의 2K 요청 내부 중앙값은
18.903 step/s, 수용률 51.818%였지만 onepass를 완주하지 않았고 800 추론 토큰에서 정답 계산이 잘렸다.
운영자 지시로 수정 기준판은 다시 재지 않는다: **no baseline on this build**. 현재 후보의 소비자 수치는 아직 없다.

일곱 검증 행을 여섯 행 전용 W4 입력 재사용 경로가 받지 못하던 것을 수정했다. 각 투영의 K 분할과 합산 순서는
그대로며 N6144/M7은 두 분할 전용 CTA를 쓴다. 공유 전문가 GU/활성화/down 융합은 C=1에서 routed MoE와 겹치고,
넓은 배치는 GPU에서 퇴행해 기존 순서를 유지한다. KDA 입력의 행 stride를 넘겨 작은 복사를 없앴다.
42개 라우터는 BF16 입력의 텐서코어와 FP32 누산/출력을 쓰며 선택된 전문가를 대조한다.

예약 GPU 부품 검사 `c80c59cb`: 일곱 행 W4의 세 형상별 시간 감소는 1.87–2.58%, 42개 라우터 전체는
3.173 → 1.550 ms. 작은 공유 MLP만은 22.6 → 20.5 us, routed+shared 합계는 약 1%다.
원패스 tok/s나 22 step/s 증명이 아니다. 기본 진단 랭크와 원패스 랭크의 라우터 행렬은 같지만 보정 편향은 달라,
원패스 파일로 선택 검사를 추가한다. 가장 큰 프로파일 구간인 전문가 커널(~32 ms)에 공유 메모리 축소 후보를
추가했고 아직 프로브 전용이다. 실제 cubin의 두 블록/SM 상주는 확인했으나 첫 실행의 축소 타일 저장 좌표가
범위를 벗어났다. 좌표와 CPU 범위 검사를 수정했으며 GPU 재검증 중이다. KDA BV16/M7 실험은 비트 일치 실패로 되돌렸다.

샘플링 첫 요청에서 대상 Facts의 없는 `sel_top_k`를 읽어 네 랭크가 죽는 문제도 고쳤다. 이 값은 드래프터 Facts의
것이다. 실제 대상 Facts를 쓰는 회귀 검사를 포함해 CPU 34 pass/7 GPU skip. 크래시 수정은 별도 PR #790에서 CI를 통과해 `644fbbea6095`로 main에 병합했다.
원시 로그·실패·정체성·정확한 실행 범위: `measurements/st_decode_forward_20260913/README.md`.
추론 예산 4096/12288, KV 6 GiB, 독립 캐시로 **개선판 한 부팅에서 정식 onepass 두 판**을 이어 검증한다.


### 45차 — ST compact MoE 기각, 실제 소비자 라우터 가중치 통과 (2026-09-13, 후속 검증)

`st-moe-compact0913v5`가 04:18:42에 정식 큐에서 실행됐다(`f9a96262`, 동일 ST 이미지).
축소 타일의 SF6 그래프는 입력·경로 변경과 정확한 0 출력을 통과했고, 실제 cubin은 44,032 B 공유 메모리,
88 레지스터, 로컬 메모리 0으로 SM당 2블록 상주를 확인했다. **그러나 속도는 이기지 못했다.**
U=8/32/56에서 C96 시간은 기준보다 각각 5.78%/0.73%/1.63% 늘었고 C48도 안정적인 이득이 없다.
원시 scale 검사는 packed-only 소유자를 재사용한 프로브 오류로 기존 가드가 거부했다(미실행, 통과 아님).
성능이 나빠 더 돌리지 않고 후보의 생성자·캐시 키·분기를 제거했다. 생산 경로에서 켠 적은 없다.
실험 재현점은 `f9a96262`, 원시 로그와 `moe-compact-verdict.json`을 보존한다.

원패스가 쓸 up/gate-full 파일의 42 라우터도 별도로 통과했다: 2352행 전문가 ID 일치,
42개 라우터 합계 3.206 → 1.576 ms. 부품 수치이며 소비자 22 step/s 판정은 아직 아니다.
PR #789는 `938de3f93a19`로 병합됐고, 운영 부팅은 04:38:32에 수정된 샘플링으로 실제 ping을 통과했다.

### 45차 — 무장한 첫 세 시간의 기록이 가르친 것: 부팅 중인 플릿은 **입양**하고, 채팅이 답해야 건강이고, 큐가 막 놓은 플릿은 **유예** 뒤에 되돌린다 (2026-09-13, srv2 운영 기록 + 리눅스 컨테이너, PR #793 — 앞의 셋(KillMode·시도 이름·포렌식)은 #786 에 먼저 들어갔다)

운영자 "운영 기록 보고 다음 개선". 02:49 에 deploy-watch 를 무장하고 04:41 까지의 srv2 기록(`st-supervisor.log`, `st-deploy-watch.log`,
큐의 `log`·`ledger.tsv`, 런치 로그)을 읽었다. 세션들의 캠페인(codex, 부팅 티켓이 5–15분씩 이어짐)이 밤새 큐를 채우고 있었다.

**기록이 말한 것.**
- 프로덕션은 세 번 되돌아왔고(03:08, 03:48, 04:34 — 큐가 리스를 놓은 **즉시**), 두 번은 6분 안에 다음 부팅 티켓이 왔다. 그 티켓은 정숙
  게이트(120 s)와 프로덕션의 드레인을 기다려 **20–30분 뒤**에야 GO 했다(03:08 복구 → 03:28 GO, 03:48 복구 → 04:18 GO).
- deploy-watch 의 배포 두 번(03:54, 04:42) 뒤에 슈퍼바이저가 **막 시작된 부팅을 내리고 다시 띄웠다**: 새 pid 의 첫 검사가 "문 없음" 이라
  `attempt_launch` → 런처 `stop` → `start`. 그리고 문이 뜬 뒤 첫 채팅이 답하지 않으면(콜드 부팅의 워밍업·캘리브레이션 중) 대기 중 쌓인
  `fails≥3` 가 그대로라 **66초 만에 또 띄웠다** — 03:23, 03:59, 04:04, 04:11, 04:52: 스무 분에 네 번, 부팅 하나 값에 넷. 하루 부팅 39회.
- D17 프로브 셋(02:57, 03:54, 04:42)이 전부 GO 1초 뒤 "no engine answers" 로 죽었다(기록 0). `serving_idle` 이 **아무것도 서빙하지 않는
  것을 유휴로** 읽어(vLLM 시절의 `! serving_up && return 0`) 티켓 사이의 빈 플릿과 컨테이너가 막 뜨는 순간에 프로브를 내보냈다.
- 대기 중인 슈퍼바이저는 30초마다 포렌식을 떴고(10칸 링이 "fleet taken" 스냅샷으로만 찼다), 내 02:56 핫픽스는 `set -u` 아래 초기화
  안 된 변수로 110초마다 죽어 systemd 가 일곱 번 다시 띄웠다(02:56–03:07). "health check failed (n/3)" 는 **무엇이** 실패했는지 말하지 않았다.

**한 것 (모두 이 PR, srv2 설치본에는 손으로 먼저).**
- `launchers/st-glm53-supervisor.sh`: (1) **부팅 중인 플릿은 입양한다** — 네 랭크가 떠 있고 문이 아직 없고 헤드 컨테이너가 부팅 시간보다
  젊으면(`booting_fleet`) 내리지 않고 문·채팅을 기다린다. (2) **런치는 채팅이 답해야 끝난다**(`wait_for_health`: 문 → 진짜 채팅) 그리고
  성공하면 `fails=0`, 실패 카운트는 런치 뒤에 새로 센다. (3) **건강 = 채팅이 답함**(`health`); 컨테이너 census 와 문은 로그에 적을 뿐
  요구하지 않는다(ssh 가 멈춘 노드는 죽은 링이 아니다) — 실패 줄에 `containers=… door=… chat=…`. (4) **유예**: 큐의 활동 시계
  (`fleet_idle.py` 의 `idle-recovery.json`, enqueue·grant·release 마다 갱신)가 `ST_RESTORE_GRACE_S`(300 s) 안이면 빈 플릿을 되돌리지
  않고 기다린다 — 다음 티켓이 그대로 가져간다. (5) 기다리는 이유(남의 플릿·핸드오버·부팅 중·유예)마다 **한 줄**, 포렌식 없음, 시도 소비
  없음(`wait_reason`). (6) 테스트용 `ST_SUPERVISOR_LOOPS`·`ST_SUPERVISOR_SLEEP`·`ST_BOOT_POLL`.
- `bench/fleet.sh` `serving_idle`: **아무것도 서빙하지 않으면 유휴가 아니다** — 프로브는 문을 잰다.
- `launchers/st-deploy-watch.py`: 같은 유예(`--queue-grace` 300 s) — 리스가 남의 것이 아니어도 큐가 방금 움직였으면 배포를 미룬다.
- 그리고 #786 으로 먼저 들어간 셋: 프로브 대기자가 oneshot 의 cgroup 과 죽음(`KillMode=process`), 같은 이름 재요청은 옛 런치 기록의 재생
  (시도마다 `d17-<sha>-N`), 대기 중 포렌식 스팸 — #786 은 그 셋만 담은 채 병합됐고 나머지가 이 PR 이다.

**검증.** `tests/test_engine_fleet_ops.py` 에 슈퍼바이저 **루프 자체**를 두 바퀴 도는 테스트(가짜 문은 호출 횟수로 상태를 바꾸고, 가짜 런처는
호출을 적는다): 남의 플릿은 한 줄·덤프 없음·크래시 없음, 부팅 중인 플릿은 입양(런처 호출 0), 막 놓인 플릿은 유예, 런치는 채팅이 답할 때까지
한 번, census 없이도 채팅이 답하면 건강, 실패 줄이 무엇을 말하는지. 큐 샌드박스에 "서빙 없음 → 프로브 거부", deploy-watch 에 유예 단위
테스트와 순서 핀. 플릿 스위트는 main 기준선과 실패 집합 동일. **실측**은 다음 창의 일 — 이 PR 이 배포되면 슈퍼바이저 로그에 "adopting",
"healthy after", "grace" 가 찍히고 `ledger.tsv` 의 D17 행에 records=1 이 붙어야 한다.

**남는 노출.** 채팅 하나가 건강이라 링이 죽고 소켓만 산 경우는 `CHAT_TIMEOUT`(300 s)만큼 늦게 안다(전과 같다). 유예 300 s 는 vLLM 시절
유휴 복구의 값을 그대로 가져온 것이고 캠페인의 티켓 간격(2–6분)에 맞춰 조정할 수 있다(`ST_RESTORE_GRACE_S`, `--queue-grace`). main 이
시간당 두세 번 움직이면 deploy-watch 는 `--min-gap`(1800 s)마다 프로덕션을 다시 띄운다 — 배포마다 부팅 3.5분은 설계의 값이다.

**설치하고 30분 (05:01–05:10).** 새 슈퍼바이저를 srv2 에 넣고 재시작하니 첫 줄이 "a fleet is booting (head container 39s old): adopting"
— 옛 루프가 그 직전에 또 띄운 부팅을 입양했다 — 그리고 36초 뒤 "a rank died during boot". 포렌식의 네 랭크 로그가 같은 말을 했다:
`RuntimeError: the ranks' NVMe tiers hold different conversations` — 랭크 0·1 은 파킹된 대화 14개(30 파일 3.6 GiB), 랭크 2·3 은 13개(28 파일
3.3 GiB). 옛 루프가 워밍업·파킹 중인 엔진을 거듭 죽이면서 D16 티어가 랭크마다 어긋났고, **04:52 부터 프로덕션은 아예 뜨지 못하고
있었다**(부팅마다 40초 만에 같은 예외). 엔진의 예외 메시지가 시키는 대로 했다 — "Clear glm53-logs/st-tier on every node" — 지우지는
않고 네 노드의 `st-tier` 를 `st-tier.skew-20260913-0505` 로 옮겨 두었다(파킹된 대화의 KV 상태; 대화 자체는 클라이언트에 있다).
재시작한 루프는 큐가 2분 전에 움직인 것을 보고 **유예**했고("fleet free, but the queue was active 99s ago"), 05:09:57 의 런치는
런처가 거부했다 — 그 순간 대기하던 부팅 티켓 `st-decode-consumer0913v4` 가 빈 리스를 가져갔기 때문이다. 유예의 문장 그대로다:
"its next ticket takes the fleet as it is". 프로덕션 부팅 하나를 안 썼다. 그 사이 deploy-watch 에 규칙 하나를 더 넣었다(같은 PR):
**대기 부팅 티켓이 있으면 배포하지 않는다** — 배포는 프로덕션 부팅이고, 프로덕션은 대기 부팅 티켓이 없을 때만 돌아온다는 운영자의
규칙(§97 문답 5)을 배포에도 적용한 것이다. 05:03 에 큐에 든 60분짜리 티켓 앞에서 05:12 의 배포 사이클이 플릿을 가져갈 뻔했다.

**다음 후보(안 함).** 티어 어긋남을 슈퍼바이저가 알아서 풀지는 않게 두었다 — 파킹된 대화를 버리는 결정이라 사람 몫이다; 다만 같은 예외가
연속 두 번이면 로그에 그 remedy 를 적어 주는 것은 값싸다. 그리고 죽은 프로덕션 리스가 큐를 최대 15분 막는다(kind production 은 pid 로
판정하지 않는다): 슈퍼바이저 재시작 직후의 티켓이 그만큼 기다렸다.

### 45차 — 랭크 하나의 크래시가 프로덕션을 영영 못 뜨게 하지 않도록: 티어가 어긋난 부팅은 **버리고 뜬다**, 리스 경쟁은 실패한 시도가 아니다, D17 은 사이클마다 스스로 채운다 (2026-09-13, srv2 운영 기록 + 리눅스 컨테이너, PR #796)

운영자 "다음 개선". 05:10–07:00 의 기록을 읽었다.

**기록.** 05:20 새 루프의 첫 런치는 정상이었다(launch: door up after 180s, healthy after 180s). **05:24 랭크 2 가 죽었다** — `CUDA error:
unspecified launch failure`(NCCL 워치독, 랭크 3 도 같은 오류; 오늘 덤프 열 개 중 이 하나) — 랭크 0·1 은 `Connection closed by peer` 로
링이 끊긴 것을 알고 내려가면서 상주 행 하나(seq-0, 건강 검사의 대화)를 파킹했고, 죽은 랭크 2·3 은 아무것도 파킹하지 못했다. 티어 파일:
랭크 0·1 은 `seq-0-*.json/.kv` 넷, 랭크 2·3 은 `.fleet-tenant` 하나. 그 뒤 05:30 의 티켓은 **손 들어 준 것이 아니라** 슈퍼바이저의 런치가
런처 `stop` 으로 리스를 놓은 순간 빈 리스를 가져갔고(런치는 거부 → "launch attempt 1/5 failed"), 06:06·06:10·06:15 의 부팅 셋은 전부
`the ranks' NVMe tiers hold different conversations: this rank has 1 [0], the fleet's highest is [1, 0, 1]` 로 40초 만에 죽었으며, 06:25 의
경쟁 하나가 다섯 번째 시도로 세어져 루프가 **HELD** 됐다. 한 랭크의 크래시가 프로덕션을 영영 못 뜨게 만드는 경로다 — 밤새 두 번째.

**한 것.**
- `engine/base/serve.py` `_agree_on_parked`: D3 의 투표(count·last key·checksum 의 all-reduce max)는 그대로. 다만 dropper 를 받으면
  어긋난 랭크들이 **전부 쥔 것을 버리고 빈 채로 뜬다** — 같은 투표에서 같은 결정이니 모든 랭크가 함께 버린다(자기 것이 highest 와
  같아도). 어차피 모든 랭크의 몫이 없으면 재개할 수 없던 것들이다. 접두사 티어의 경계도 같은 방식(`_forget_prefix_boundary`: 캐시의
  hold 와 티어의 사본). dropper 없이 부르면 옛 계약 그대로 죽는다. `tests/test_engine_tier_agreement.py` 여섯 개(동의·불일치·남의
  불일치·drop 실패·옛 계약·1랭크) + 호출부 핀.
- `launchers/st-glm53-supervisor.sh`: 런치 뒤 `fleet_taken` 이면 "the fleet was taken while launching: not a failed attempt"; 남의 창이
  끼면 `launch_fails` 리셋("a foreign window resets the launch count") — hold 는 연속 다섯 번의 것이다.
- `launchers/st-deploy-watch.py`: `ensure_probe` 를 사이클 **시작**으로(후보가 있건 없건); 정숙 대기 루프는 `fleet_busy_with_tickets`
  (남의 리스 또는 대기 부팅 티켓)면 그 사이클을 접는다.

**검증.** 컨테이너에서 엔진·플릿 스위트의 실패 집합이 main(#793)과 동일. srv2: 티어를 다시 치우고(`st-tier.skew-20260913-0700`)
HELD 된 루프를 재시작해 두었다(codex 티켓 est 180m 뒤에 뜬다); 슈퍼바이저·deploy-watch 설치본에 이 PR 을 넣었다. **엔진 수정은
병합 → 배포 뒤에야 프로덕션에 닿는다.** 그때까지 랭크가 또 죽으면 티어는 또 어긋난다.

**운영자가 볼 것.** 05:24 의 CUDA `unspecified launch failure`(랭크 2·3, sha 644fbbea, 첫 채팅 ~1분 뒤). 커널 결함일 수 있다. 그리고
남는 것: 죽은 프로덕션 리스가 큐를 최대 15분 막는다(kind production 은 pid 로 판정하지 않는다).

### 45차 — "커널 결함" 의 정체: 랭크 절반이 호스트 투표에, 절반이 GPU 집단통신에 — 시작 못 한 전송도 그 랭크의 **표**다 (2026-09-13, srv2 포렌식 + 리눅스 컨테이너, PR #800)

운영자 "커널 결함 추정 코드 찾기" → "넣고 pr 머지". 05:24 의 `CUDA error: unspecified launch failure` 를 따라갔다.

**무엇이 죽였나.** srv3·srv4 의 커널 로그에 같은 초의 `Xid 43` 둘(04:57:16, 05:24:16), SM 예외(Xid 13)는 없음. 랭크 2·3 의 로그는 죽기 전
30여 초 동안 `[oneshot] STALL rank=2 phase=wait(peer-flags) seq=5518 missing_peer_mask=0x7` — 원샷 all-reduce 커널이 랭크 0·1 의
플래그를 기다리며 스핀 — 그리고 `engine/kernels/oneshot/dsv4_oneshot_ar.cu` 의 스톨 트랩(`OSAR_STALL_TRAP_S=30` 초 뒤 `__trap()`)이
그 "launch failure" 다. 커널은 설계대로 자살했다. 랭크 0·1 은 같은 시각 `serve.py _settle → _votes → comm.all_reduce_host`(gloo, 컨트롤
그룹)에서 상대를 기다리다 `Connection closed by peer` 로 죽었다. **플릿의 절반이 호스트 투표에, 절반이 GPU 집단통신에** — 서로 다른
집단통신에서 서로를 기다렸다.

**분기를 만든 코드.** 전부 "랭크-지역 조건이 집단통신을 가로막는" 자리다. (1) `_settle` 은 `rows = runner.transfers(); if not rows: return`
— 이 랭크의 티어 스레드에 전송이 없으면 투표를 건너뛴다. (2) 모든 랭크가 `_votes([have])` 로 승인한 접두사 복원의 `restore_begin` 이 한
랭크에서 실패하면 **그 랭크만** "prefill it instead" 로 폴백했다 — 성공한 랭크는 `_restores` 에 남아 (1)의 투표로 갔다. 트레이스백과
정확히 맞는다. (3) `resume_begin` 실패는 `raise` — 그 랭크가 죽는다. (4) `_retire` 의 `park_begin` 실패는 잡히지 않는다. 접두사 티어의
후보 자체(`tier`)는 랭크 0 이 골라 브로드캐스트하니 대칭이다(1429행의 주석 "rank 0's view; every rank votes") — 문제는 승인 뒤의 실패였다.

**용의 커밋.** #783(접두사 스냅샷의 압축 RAM 캐시). 오늘 밤 프로덕션 부팅 다섯 번(03:09, 03:54, 04:42, 04:56, 05:20)이 전부 #783 을
담은 main 이었고 전부 두 번째 헬스 채팅(동일 프롬프트 → 접두사 재사용 경로) 뒤 1분 안에 무너졌다; #783 이전의 grammar 릴리스는 몇 시간을
버텼다. 랭크 2·3 에서만 복원이 왜 실패했는지는 그 순간의 로그가 없어 못 박지 못했다 — 이 수정은 원인이 무엇이든 분기가 플릿을 죽이지
못하게 한다.

**한 것** (`engine/base/serve.py`). begin 이 실패한 행은 `_failed_begins` 에 표시하고 서버의 장부(`_retiring`/`_resuming`/`_restoring` —
결정이 스텝의 브로드캐스트를 타므로 모든 랭크가 같게 든다)에 그대로 둔다. `_settle` 은 행 집합을 그 장부에서 만들고(`runner.transfers()`
가 아니라), 실패한 랭크는 "done, not ok" 로 투표한다(`_transfer_done`). 결과 투표가 모두를 같은 길로 보낸다 — 파킹은 모두 버림, 복원은
모두 프리필(요청을 줄 앞에 다시), 재개는 모두 503. 혼자 폴백하거나 혼자 죽는 길은 없어졌다. `engine/README.md` 에 한 줄.

**검증.** `tests/test_engine_settle_symmetry.py`: 파킹 begin 실패 → 투표로 모두 버림; 이 랭크에 전송이 없어도 투표가 던져짐(2랭크
가짜 comm 이 `[1]`, `[0, 0]` 을 기록 — 옛 코드는 여기서 돌아섰다); **복원 begin 실패 → 투표로 프리필**(크래시의 랭크 2·3 재현); 재개
begin 실패 → 503 에 랭크는 삶; 소스 핀. 엔진·플릿 스위트 실패 집합은 main(#793)과 동일. 실측은 다음 부팅의 일: 두 번째 헬스 채팅을
넘기면 된다.

### 45차 — 큐의 전환 비용: 유예는 **기록의 p75**, 게이트는 아무도 안 쓴 프로덕션에 바로 열리고, 창과 자리 승계가 생겼다 (2026-09-13, srv2 24시간 기록 + 리눅스 컨테이너, PR #804)

운영자 "큐 시스템을 최대한 개선해서 세션의 유휴 대기시간과 플릿 테스트 소요시간을 줄이는 방법" → 제안 아홉 중 1~6 도입.

**24시간의 기록(09:00 까지).** 요청 77, GO 51, GO 전 취소 19(25%). 부팅 티켓 대기 중앙값 2.9분·p90 13.4분·최대 24.6분. 정숙 게이트를 만난
티켓 12개는 게이트→GO 중앙값 **13.1분**. 부팅 티켓 39개는 전부 기록 0(프로브·검사), 점유 중앙값 1분, 합계 142분. 프로덕션 부팅 **46회**
(문 뜨기 90~210초, 720초는 6시간 내려가 있던 뒤 첫 부팅 한 번), 그중 17회는 15분 안에 다음 부팅 요청을 맞았다. 티켓 끝→다음 부팅 요청
간격 중앙값 6.3분, 10분 안 30/42, 15분 안 34/42. 진입점: chain.sh 82, run_engine_probe 56(그중 `--fleet` 33), st_bracket 9. 단일 GPU
레인 8건은 대기 0분. 플릿이 일한 시간(142분)보다 프로덕션을 내리고 올린 시간이 길다 — 표적은 티켓이 아니라 **전환**이다.

**한 것.**
1. `bench/fleet_pace.py`: 릴리스마다 `restore-grace.json` — 최근 6시간 "끝→다음 부팅 요청" 간격의 p75, [300, 1200]초. 슈퍼바이저
   (`queue_grace`)와 deploy-watch(`pace_grace`)가 읽고, 둘의 상수(`ST_RESTORE_GRACE_S`, `--queue-grace`)는 그대로 덮어쓴다. `status` 에
   `pace:` 줄. 어젯밤 값이면 10분: 복구 42회 중 34회가 사라진다.
2. `production_quiet`: `vllm:request_success_total`=0(부팅 뒤 아무도 안 씀)이거나 `st:idle_seconds`(새 게이지 — 요청이 도착하거나
   답한 뒤 지난 초)가 이미 게이트 이상이면 **즉시**. 게이트가 지키는 것은 대화 중인 사용자지, 아무도 안 쓴 프로덕션의 워밍업이 아니다.
3. 1-GPU 검사가 `--fleet` 를 말하면 한 줄로 비용을 알린다(단일 레인은 #774 부터 기본이었고, 33건은 명시적 선택이었다).
4. `fleet.sh window s MINUTES|off`: 창이 열린 동안 유예 ≥ 남은 창. 세션의 잔 티켓들 사이에 프로덕션이 돌아오지 않는다.
5. 부팅 시간: 720초는 콜드 스타트 한 번이었다(새 트리마다가 아니다) — 제안의 전제를 기록이 정정했다. 코드 없음. 180초의 구성은 다음 창.
6. `run --replaces OLD`: 새 티켓이 OLD 의 enqueue 시각을 물려받고 OLD 를 취소한다(`_enqueue` 의 여섯째 인자).

**검증.** `tests/test_fleet_pace.py` 다섯, 큐 샌드박스 셋(게이트 즉시 열림의 세 경우, 릴리스가 pace 를 씀, 자리 승계 + 소스 핀),
슈퍼바이저 루프 하나(900초 pace·창·상수), deploy-watch 하나(`pace_grace`), 엔진 게이지 하나. 엔진·플릿 스위트는 main(#802)과 실패
집합 동일. 실측은 오늘 밤의 캠페인이 잰다: `status` 의 pace 줄과 `fleet log` 의 "served nobody since it booted" 가 찍히면 된다.

**남는 후보(안 함).** 7 배포 빈도(main 이 시간당 2~3번이면 30분마다 프로덕션 재시작), 8 야간 지연 복구, 9 GO 푸시 알림.
### 45차 — 원패스 난이도 한 단 낮추고 토큰 예산 두 배: 하니스 44 / ko-reasoning-v2 (2026-09-13, 맥, CPU 검사만; 운영자 "난이도를 약간 낮추고 토큰 제한을 늘려")

**기록.** consumer-v5(하니스 43, `measurements/st_decode_forward_20260913/consumer-v5-incomplete`)의 측정 요청 다섯 개는 모두
`finish_reason=stop` 이지만 **추론은 전부 상한에서 문장 중간에 잘렸다**: 2K 개별 셋은 완료 4,336–4,598 토큰(추론 상한 4,096 + 보이는 답
240–500), 32K·128K 묶음은 14,022–14,294(상한 12,288). 128K 묶음은 상한이 닫힐 때 아직 ledger 의 반사실을 계산하던 중이라 portfolio·logic
은 추론 없이 답했다. 품질 **1/9**(2K portfolio 만 통과). 실패의 모양: 2K ledger 는 JSON 뒤에 "검증 계산표"와 "반례 증명서" 문단을 붙여
파싱 실패("설명 대신 계산표·근거 ID·반례 증명서를 작성한다"를 초대장으로 읽었다); 묶음 답 셋은 evidence 배열에 ID 대신 설명문; received 는
세 가지로 읽혔다(순증감 138, 상자×개수+반품 225·241); 2K logic 은 statuses 를 unknown/true/false 로 썼고 나머지 여섯 차원은 다 맞았다;
128K logic 은 섞인 변수 이름과 ABCDEF 비트 순서를 잘못 대응해 다른 세계를 풀었다.

**한 것.**
- `bench/measurement_contract.py`: HARNESS 44, 개별 16,384/8,192, 묶음 49,152/24,576(하니스 43 의 두 배; 128K 프롬프트 129,295 + 49,152 =
  178,447 < max_model_len 1,048,576). `judge.compatible` 이 harness·generation_budget·quality_protocol 을 보므로 하니스 43 기준선은
  자동으로 재사용 불가.
- `bench/onepass_quality.py` ko-reasoning-v2: 답변 지시는 JSON 밖에 아무것도 쓰지 않게, 스키마는 열거값을 그대로 보여 주고(`전량승인|보류`,
  `참|거짓|판단불가`, `6자리 비트열|null`, `budget|staff|dependency|conflict`) evidence 배열은 `기록 ID`; ledger 는 received/net/after_loss
  를 식으로 정의하고 L5 는 counterfactual 에만 적용한다고 못박고 손실률 7/9/13 → 5/10/15%; portfolio 는 위반 없는 조합만 점수(위반 행은
  score null, 비용·인력·위반은 그대로 10행); logic 은 U0 에 인코딩 예(100110) 추가. 마감 둘·개정 교체·취소, 10행 전부, 세계·증인·모든 최소
  core, 채점 규칙은 그대로다. `bench/ONEPASS_QUALITY.md` 가 v2 를 적는다.
- 테스트: 독립 오라클(포트폴리오 비트마스크)이 위반 행 score None 을 확인, 위반 행에 점수를 쓰거나 가능 행 점수를 비운 답은 derivation
  실패, 프롬프트가 열거값·기록 ID·null 점수를 말하는지 검사, NaN 픽스처를 시드 독립으로. `tests/test_fleet_onepass.py` 의
  `--combined-reasoning-budget 7200` 거부 픽스처는 하니스 43 부터 낡아 있었다(7200 < 24576 이라 통과) — 기본 묶음 상한값으로 바꿨다.

**검증.** `test_onepass*.py` 109 OK(이전 107 + 2), `test_fleet_onepass*` 36 OK(이전 실패 1 은 위 낡은 픽스처). **GPU 실측 없음**: 새 예산·새
문제의 정답률은 다음 정식 onepass 가 답한다. 상한까지 쓰면 디코드 시간은 두 배까지 늘 수 있다(하니스 43 에서 2K 요청 65–72 s, 묶음 190 s).

### 45차 — 기준선은 최소로: 도입된 후보의 표본이 **그대로 다음 기준선**(정체성은 엔진 트리), 바닥은 기록에서 빌리고, 프로브는 한 판 (2026-09-13, 리눅스 컨테이너, PR #807)

운영자 "기준선을 최대한 조금 뽑게 하는 게 소요시간에 또 도움이 될 거야" → 규칙 하나: **후보가 도입되면 그 후보의 측정값이 그대로 다음
기준선이고, 도입이 안 되면 그때의 기준선을 그대로 쓴다.** 기준선을 위해 따로 부팅하는 일은 원칙적으로 없어야 한다.

**왜 안 되고 있었나.** 기록에는 후보의 웜 표본이 그 커밋 아래 이미 있었다(`arm_sha`). 규칙을 깨는 것은 sha 다 — 잰 브랜치를 main 이
스쿼시하면 같은 엔진이 다른 커밋이 되고(#800 의 브랜치 끝과 스쿼시의 `engine/` 트리는 다르지만 그 사이에 main 의 엔진이 움직였기
때문이고, 안 움직였으면 같다), 플릿 쪽만 바뀐 병합(#804 이전의 여러 개)은 `engine/` 은 그대로인데 커밋만 바뀐다. 커밋으로만 맞추면
도입된 후보의 표본은 남의 것이 되고, `st-pair` 는 기준선을 다시 부팅했다.

**한 것.** 표본이 **엔진 트리**를 함께 든다 — `launchers/st_release.py tree <sha>` = `git rev-parse <sha>:engine` 앞 12자, 브래킷이
`ST_BRACKET_TREE` 로 넘기고 onepass 가 `arm_tree` 로 적는다. `st_judge.same()` 은 커밋으로도 트리로도 맞추고(`--tree`, `--cand-tree`,
`--base-tree`), `st-pair` 의 기준선·`st-chain` 의 재사용·deploy-watch 의 D17 자가 치유가 그 트리로 찾는다: 도입된 후보는 부팅도 프로브도
없이 기준선이 된다. 같이: (1) 프로브는 **한 판** — 리셋 뒤의 판은 웜 표본이고(`warm()`: `cold=reset` 은 웜), 부팅 하나는 판 수와 무관하게
표본 하나(`ST_PROBE_RUNS=2` 로 예전 모양); (2) 기준선 부팅이 하나면 판정기가 **바닥을 빌린다**(`pooled_floor`: 두 번 이상 부팅된 모든
커밋의 판 간 편차의 중앙값, 판정문에 "pooled floor"); (3) `st-chain --reuse`: 표본이 있는 팔은 부팅하지 않는다(`A B A B` 의 교대 부팅은
`--reuse` 없이 그대로 — 편차 자체가 질문일 때); (4) deploy-watch 는 배포된 엔진을 `--probe-samples`(1)로 유지하되 **프로덕션 부팅당
하나**로 센다(`st_judge boots`, 표본을 준 부팅은 다시 찌르지 않고, 서빙이 없으면 찌르지 않는다).

**검증.** 판정기: 스쿼시 커밋이 트리로 후보의 표본을 쓰고 다른 트리는 남; 빌린 바닥(중앙값; 기준선이 둘이 되면 자기 바닥); 프로브의
첫 판이 웜; `boots` CLI. 리허설: 프로브 한 판 + 진짜 커밋의 `arm_tree`(소스가 모르는 sha 는 트리 없음); `chain --reuse` 는 기록이 8에서
늘지 않는다. deploy-watch: 도입된 후보의 표본이 프로브를 면제, 부팅당 하나, 서빙 없음. 엔진·플릿 스위트 실패 집합은 main 과 동일.
**실측**: 다음 `st-pair` 가 기준선을 부팅하지 않고 "base … reused" 를 찍으면 된다.

### 45차 — `engine/` 이 그대로인 main 은 **부팅 없이** 배포된 것으로 기록한다 (2026-09-13, 리눅스 컨테이너, PR #808)

#807 의 따름. 어젯밤 병합의 절반이 bench·launchers·원장 커밋이었고 그때마다 main 이 움직여 deploy-watch 가 프로덕션을 다시 띄웠다
(부팅 3분 + 드레인 + D17 프로브 — 바뀌지 않은 엔진을 위해). 표본의 정체성이 엔진 트리가 됐으니 배포도 같은 눈으로 본다: `same_engine`
(두 커밋의 `engine/` 트리가 같음)이면 릴리스를 자르고 `deployed` 를 그 sha 로 기록하고(`same_engine_as` 에 이전 sha) 컨트롤러를 옮긴다.
문은 건드리지 않고 정숙 게이트도 묻지 않는다. 검증: 트리 비교 단위 테스트와 사이클 안의 자리(정숙 대기 앞) 핀. 실측: 다음 플릿 쪽 병합에서
`recorded as deployed without a boot` 한 줄.


### ST prefill phase 1 — measured K campaign, reconciled landing (2026-09-13)

Campaign source `2c2ee77f`, controller `de67dc0e`, GLM-5.3-Flash / 4 GB10 / C=1 / KV 2 GiB per rank / FP32 KDA / spec 6 / harness 40. Profiler off and prefix reuse 0 for both stages. Actual tokens / TTFT: 32K 2695.852 first, 3412.559 later; 128K 3318.997 first, 3360.426 later. First-pass quality 18/18, Korean 0/8; later long requests 6/6, 0/2. Fixed 1024 decode pooled 49.951264 tok/s, with different trajectories from J4 so no decode nonregression claim.

C=4 omitted and the later two long requests are not a second full C=1 pass. No baseline engine rerun, as directed by the user. The port retains newer main behavior and therefore has a different engine identity: these are K campaign results, not measured results of the merged runtime. See [scope, full raw evidence and limitations](measurements/st_prefill_phase1_20260913/README.md). Phase 2 targets C=1 2K >= 3300 and 128K >= 4000 tok/s under the same no-cache, unprofiled metric.

### 45차 — C=4 는 왜 C=1 의 1.4~1.7배뿐인가: 배치 4 스텝은 전문가 스트리밍이 60% 이상, 긴 컨텍스트는 **직렬 프리필**이 36% (2026-09-13, srv2 09-12 기록 재해석 + srv4 단일 GPU 레인, PR #810)

운영자: "프리필을 포함한 동일 기준으로 C=4 합산 처리량이 C=1 의 2K 1.66–1.71배, 32K 1.50배, 128K 1.41배 — 너무 낮다, 개선해."
그 숫자는 srv2 의 onepass 기록 `20260912T213057`(커밋 8c8b031b, 하니스 43 예산)에서 그대로 재구성된다(C=1 은 같은 질문의
`completion/elapsed`, C=4 는 하니스의 합산 정의). **새 플릿 실측은 없다.** 기록의 스텝 열·디바이스 단계 표본·진단 커널 표를 다시 읽고,
남는 물음 둘을 단일 GPU 레인(srv4, rank 3)의 새 프로브로 쟀다. 전부 `measurements/c4_scaling_20260913/`.

**1. 디코드: 네 행은 한 행의 2.3~2.5배, 드래프터는 그대로.** 디바이스 단계 표본(랭크 0): forward 47.7 → 73.7 → 95.6 → 110.8 ms(2K, 행 1..4),
128K 는 51.7 → 127.3; propose+observe 3.4 → 4.6 ms. 단일 랭크 재생(집단통신 항등, 합성 라우팅): 36.9 / 55.6 / 67.1 / 76.3 ms, 층당 고유
전문가 28.7 / 43.9 / 55.3 / 61.5, **정적 MoE 커널은 고유 전문가당 0.69~0.70 ms = 144.6 MB 를 207 GB/s 로**(이론 273 의 76%). 비 MoE 는
22 → 35 ms: KDA +5.9(순환 상태 링 K+1 개 FP32 쓰기 = 행·층당 8 MB, 대역폭에 묶임), 행별 루프의 elementwise +3.0(런치 948 → 2,143),
라우터 +1.7(M=28 은 FP32 SGEMM), mHC +1.2, MLA +0.8. 플릿의 실제 고유 전문가 수는 여기서 역산하면 **대략 33 / 95** — 처음 세운
"288 중 top-8 균등 라우팅" 모델(51 / 157)이 플릿 forward 를 1 ms 안에 맞춘 것은 우연이었다. 바른 그림: **배치 4 스텝 ≈ MoE 60~65% +
비 MoE 30% + 집단통신 5~10%**, 커널로 닿는 몫은 10% 안팎(라우터 TC 1.7 ms, 행별 루프 ~3 ms, MoE 207 → 240 GB/s 면 −6 ms).

**2. 2K 의 상한.** 배치 4 스텝 115.6 ms 에 4 × 3.67 토큰 = 127 tok/s; C=1 의 같은 기준 63~70 tok/s 에 대해 램프·꼬리 없는 상한 **1.8~2.0배**.
측정 1.66~1.71 과의 차이는 램프(넷째 요청의 첫 토큰이 6.2 s 에: 프리필 1.3 + 디코더 옆 프리필 1.5~1.8 s 씩 셋)와 꼬리(완료 길이 4,240~4,881 로
갈려 마지막 9~10 s 가 2행·1행) — 스케줄러가 없앨 것이 아니다.

**3. 긴 컨텍스트: 디코더 옆의 2,304 청크는 토큰당 1.4배.** 스텝 열은 D10 그대로다 — 첫 요청은 9,216 청크로 혼자, 뒤의 셋은 `2,304 청크 → 디코드 1 스텝`
을 번갈아(128K: 57 회씩). TTFT 사다리 48.6 / 119.4 / 191.7 / 265.2 s: 둘째부터의 간격 70.7 / 72.3 / 73.5 s 는 혼자 프리필한 48.4 s 의
1.5 배(32K 도 17.8~18.3 vs 11.9). 청크 하나 1,188 ms(128K) vs 9,216 청크 3,374 ms → 토큰당 516 vs 366 µs. 128K C=4 벽시계 731 s =
첫 프리필 48.6 + 옆 프리필 셋 216.6 + 4행 디코드 394(3,105 스텝 × 127 ms) + 2·3·1행 62(꼬리 49 s 는 수용률 편차: 넷째 요청 4.7 토큰/스텝,
둘째 3.7). **프리필이 36%다.** 살아 있는 행은 옆 프리필 동안 1.19 s 에 한 스텝만 받는다(둘째 요청은 217 s 동안 168 스텝).

**4. 청크 고정비의 정체 — 병적 상수가 아니라 "청크마다 288 전문가를 한 번 읽는다".** 단일 랭크 스윕(27,648 토큰, 2,304/4,608/6,912/9,216 청크):
11,114 / 9,701 / 9,400 / 8,367 ms, 최소제곱 **288 µs/토큰 + 270 ms/청크**(헌장 D9 의 vLLM 스택 214.7 ms/step 대응값). 첫 청크 커널 표
2,304 vs 9,216: dynamic MoE(SF6) 335 → 646 ms(1.93 배) = **고정 231 ms + 45 µs/토큰** — 층당 5.5 ms = 907 MB 의 전문가 가중치를
165 GB/s 로 한 번; 나머지 커널은 토큰 비례(mHC 4.1 배, deep_gemm 3.7, KDA 3.9, 라우터 SGEMM 3.8). 없앨 수 있는 것은 그 읽기의 효율
(165 → 200+ GB/s 면 청크당 −40~60 ms)이고, 나머지는 청크를 키워야만 나뉜다 — 그 방향은 D10 이 금한다(총 처리량을 목적함수로).
플릿의 청크 고정비 ~380 ms 와의 차 ~110 ms 는 이 랭크에 없는 것들(청크당 NCCL 90 회, 드래프터 관측, prefix 마크).
`--moe-static t,r`(SF6 없이 raw 스케일) 팔은 돌지 않았다(타일 가중치의 프리필 커널은 SF6 판뿐). 대신 행 우선 `u` 셀(스톡 정적 v4 +
스톡 동적, raw 스케일 TMA)이 같은 스윕에서 **278 µs/토큰 + 202 ms/청크**: 2,304 청크 12 개가 11,114 → 10,119 ms(−9%), 9,216 청크는 −1%,
첫 2,304 청크의 MoE 커널 335 → 263 ms. 청크 고정비 68 ms 는 SF6 gated 파이프라인의 몫이다 — M128 타일 고정(전문가당 64 행에서 반이 빔)과
producer 워프의 6 비트 전개. 디코드는 SF6 정적 커널이 2 ms 빠르므로 셀을 되돌릴 일은 아니고, 남는 길은 SF6 파이프라인에 M64 타일을 더하거나
전개를 파이프라인화하는 커널 작업(128K 옆 프리필 −5%), 또는 4,608 토큰 아래에서만 generic 커널로 가는 하이브리드(raw 사본 4.75 GB/랭크).

**5. 같은 커널로 스케줄만 바꾸면(계산, 미실측).** 128K C=4: 청크 고정비를 0 으로 → ~674 s(1.53배); 디코더 옆도 9,216 청크 → ~665 s(1.55배,
ITL 정지 1.2 → 3.4 s, D10 금지); 2K 는 어느 것도 못 바꾼다. **두 스트림 겹침(§80 이 열어 둔 D9 의 질문)은 단일 랭크에서 실물 커널로 재어
지웠다**(`--lanes coexist`, 티켓 `c4-coexist2`): 스트림 A 에 4행 디코드 재생 38 회, 스트림 B 에 2,304 청크 — 청크 안에서 돈 디코드 스텝
**261 ms(단독 77.2, 3.38 배)**, 청크 **1,122 ms(단독 883, 1.27 배)**, 같은 일을 순서대로 했을 1,115 ms 대 실제 1,122 ms = **겹침 이득 0.99.**
둘 다 DRAM 에 묶인 커널이라 나눌 SM 이 없다; §80 의 1.21 배는 대역폭을 안 쓰는 샘플러의 이야기였다. 겹침은 처리량이 아니라 ITL 의
도구다(청크당 1.2 s 정지 → ~270 ms 스텝).

**6. 이 PR 이 바꾼 것.** (a) `probes/engine_prefill_chunk_profile.py`(큐 admit): 청크 스윕 + 행별 디코드 + 두 스트림 공존, 단일 랭크; 격리 랭크
프로브 둘 다 **노드가 가진 랭크로** 돌고(srv4 = rank 3) 토큰 id 를 그 랭크의 어휘 조각 안에서 뽑는다(남의 조각 id 는 영벡터로 박혀 편향
8개 전문가로만 라우팅됐다); `--seqs/--steps` admit. (b) **라우터 텐서코어 경로를 모든 폭으로**(`net.route`): PR #789 는 M ≤ 7 만 열고
나머지를 FP32 SGEMM 에 뒀는데, 그 나머지가 9,216 청크당 `magma_sgemmEx` 121 ms + `x.float()` 복사(토큰당 13 µs, 프리필의 ~5%)와 4행
스텝 1.7 ms 였다. 곱은 양쪽 다 정확(BF16 피연산자를 FP32 로)하고 합 순서만 다르다; GPU 검사(`test_engine_decode_seven.RouterTensorCoreTests`)가
동점 전문가에서 1·7·28·2,304 행의 선택 일치를 박고, CPU 검사(`test_engine_router_widths.py`)가 폭별 경로를 박는다. **미실측**: 서빙
라우팅 수치가 바뀌는 변경이라 채택은 브래킷(품질 9/9·한국어·수용률)이 답한다. 후속: 상주 FP32 라우터 사본 189 MiB/랭크는 이제 읽히지 않는다.
(c) **캡처된 디코드 스텝의 행별 루프 접기**(운영자 "남은 레버 작업"): `net._kda` 가 세그먼트마다 띄우던 conv 링·순환 링 커널과 행 출력
복사, `_dsa` 의 행별 latent 흩뿌림을 층당 한 번으로. 두 링 커널에 행 축을 더했다(순환: 프로그램 i_n 이 자기 행의 slot/context 를 읽고
토큰 구간 `[i_n·T, (i_n+1)·T)` 를 맡는다; conv: grid 셋째 축이 행; `RING_INDEX_STRIDE`). 프로그램별 산술은 한 행 런치와 같아 출력과 링
쓰기가 바이트 단위로 같다 — `tests/test_engine_kda_ring.py`·`test_engine_conv_ring.py` 의 rows 검사(캡처 재생 포함, `engine_kernel_check
--lanes kda_ring` 이 둘 다 돌린다), `GraphCaches.token_rows` 는 모은 블록표에서 모든 행의 latent 슬롯을 한 번에, `Glm53Net._ring_rows` 가
어느 스텝이 접히는지 정한다(CPU 검사 `test_engine_decode_rows.py`). 단일 레인 실측: `engine_kernel_check --lanes kda_ring` 18 검사 OK(rows 패리티
둘 포함), 재생 행 1/2/3/4 = 36.8 / 53.3 / 64.3 / 73.0 ms(전 36.9 / 55.6 / 67.1 / 76.3) — **4행 −3.3 ms(−4.3%)**; 순환 링 136 → 34 런치, conv
136 → 34, elementwise 2,143 → 1,782, 라우터 SIMT SGEMM 2.45 → TC 1.17 ms. eager 대 재생 검사(`engine_decode_graph_check`)는 플릿 부팅 뒤 대기.
(d) `probes/engine_decode_graph_check.py` 는 큐가 admit 하되 srv4 에서 돌 수 없던 검사였다(없는 Red Hat 메타 디렉터리, #732 이후 틀린 검증 폭 6,
랭크 0 의 어휘 조각) — 서빙 체크포인트·노드의 랭크·`spec_k+1` 로 고쳤다.

**7. 레버 순위** (2K / 32K / 128K 비에 대한 추정): dynamic MoE 프리필 커널의 읽기 효율 165 → 200+ GB/s (0 / +2 / +8%, 커널 작업) ·
라우터 TC 전폭 (+1.5 / +2 / +2, 이 PR) · 배치가 클 때 K 축소 (+3~10 / 0 / 0, 캡처 폭·링·파이프라인 전부 K 고정) · 디코드 정적 MoE 207 → 240 GB/s
(+5 / +5 / +3, compact 타일은 졌다) · 행별 루프 접기 (+2~3 / 0 / 0) · 디코더 옆 청크 확대 (0 / +3~5 / +6~10, **D10 금지 — 운영자만 풀 수 있다**) ·
~~두 스트림 겹침~~ (0, 실측). **2K 의 비는 이미 물리 상한(1.8~2.0)의 90% 이고, 긴 컨텍스트의 비는 직렬 프리필이 정한다 — 그 프리필의 청크
고정비는 구조(청크마다 288 전문가 한 번 읽기)라 커널 효율과 청크 정책 둘만이 움직인다.**

**검증.** 컨테이너 CPU: `test_engine_router_widths` 3, `test_engine_prefill_chunk_profile` 5, `test_engine_graph_profile`·`test_fleet_onepass`·
`test_fleet_single`·`test_engine_decode_seven`·`test_engine_decode_residency`·`test_engine_native_execution` 모두 OK(GPU 검사는 skip).
srv4 단일 레인: `c4-chunk-profile` 완주(위 수치), `c4-chunk-profile-raw` 디스패처 거부, `c4-coexist2` 완주(§5).

**같은 날 오후 — SF6 프리필 커널은 Q0 가 돈다, 그리고 둘째 접기 (운영자 "sf6 다이내믹 프리필 커널 진행" → "바이트 투 바이트 동일 접기 또는 융합
작업만 하면 굳이 gpu 검증 필요없잖아", README §9·§10, 브랜치 `c4-throughput`, 미푸시).** SF6 dynamic 프리필 커널의 세 실측은 한 식
`시간 = 바이트 / ~188 GB/s`(바이트 = m 타일 × (가중치 3.1 MB + SF6 0.3 MB + FC1 반쪽마다 다시 읽는 A 타일 2 MB))에 맞는다 — SF6 전개가 아니라
**트래픽**이 주범이고, 레버는 M64 타일(2,304 토큰에서 −20%)과 FC1 A 공유(−20%)다. 그런데 M64 팔 티켓 `c4-moe-lane` 은 첫 호출에서
`TP SF6 Q0 requires pinned SM121 source` 로 죽었다: 플릿의 프리필 MoE 는 `moe_dynamic_gated_sf6.py` 가 아니라 서브클래스
**`moe_dynamic_gated_sf6_q0.py`** 가 돌고(18차의 "여기를 보라"), 그것은 `tile_m == 128` 을 요구하며 부모 파일의 sha256 을 핀한다 — M64 를 위해
부모의 형상 검사를 완화한 순간 핀이 깨졌다. M64 는 Q0 에필로그(M128 공유메모리 배치 전제) 재작성 + 핀 갱신 + 스캐터 원자합 순서가 바뀌는
수치 검증이 필요한 일이라 **바이트 동일이 아니다**; 운영자 방침대로 훅을 브랜치에서 빼고 두 후보를 보류했다(`--lanes moe` MAC 스윕만 남김).
바이트 동일한 접기는 디코드 스텝에 남아 있었다: 첫 접기 뒤 4행 재생에 행마다 남은 DSA **인덱서 선택**(후보 주소·gather·마스크·top-k·id 정리·
`_pool_slots`, 행·층당 ~20 런치)과 **풀·테일 쓰기**(`_scatter_rows` 둘 + stack + `_write_ring`)를 층당 한 벌로 접었다 — `_select_rows`·
`candidate_rows`·`token_maps`, `_pool_slots` 의 행 그룹 블록표(`table_s1`·`TOKENS`), `_scatter_rows` 세그먼트 축, `write_ring_rows`, kept `zeros`.
logits 커널과 `torch.topk` 만 행마다 그대로 둔다(같은 텐서·같은 슬라이스 크기 → 같은 알고리즘; torch 는 슬라이스 수로 top-k 알고리즘을 골라
행 너머 배치는 동점 처리가 달라질 수 있다). 프로그램별 산술은 한 행 런치와 같다(축 하나면 seg 0·스트라이드 0). CPU 검사 `test_engine_decode_rows`
(결정·행별 주소 대조·`_select_rows` 대 루프 선택) + `test_engine_graph_contracts` 갱신, GPU 검사 `test_engine_state`(rows 쓰기·재생)·
`test_engine_pool_slots`(2-D 블록표) 는 `engine_kernel_check --lanes decode_rows` 에 두었고 GPU 창은 기다리지 않는다. 기대 4행 스텝
**−790 런치 ≈ −2 ms(−2.7%)**; 시간 티켓도 운영자 지시("바이트 동등이니까 큐 잡지 말고")로 큐에서 뺐다 — 실측은 다음 onepass 의 스텝 열. PR #815.

**셋째 접기 (운영자 "추가로 접기와 융합 작업", 같은 날 오후, README §9).** 둘째 접기 뒤 DSA 층에 층당 남은 62 런치(+행당 3)는 정수 주소
산술과 gather 였다 — latent 쓰기의 `token_rows` + index_put, `complete_pools` 의 창 구성 11 과 주소 13, 선택의 `candidate_rows` 8 + gather 2 + 길이 3
+ id 정리 3. 다섯 Triton 커널로 접었다(`row_lengths`·`latent_write_rows`·`gather_candidates`·`pool_window`·`pool_addresses`, 레인 묶음
`Lanes.decode_rows`; 참조 표는 접기 전 torch 합성) → 층당 14 + 행당 3, 스텝당 **−528 런치 ≈ −1.3 ms**(1행 −3.6%, 4행 −1.9%). 전부 바이트 복사
아니면 정수라 서빙과 참조가 바이트 동일하고, **컨테이너의 Triton 인터프리터(`TRITON_INTERPRET=1`, CPU)** 가 다섯 커널을 참조와 바이트 대조로
박았다(`test_engine_indexer_rows`) — #815 의 `_pool_slots` 2-D 블록표도 같은 길로 CPU 에서 박혔다. 선택의 `masked_fill_(ids ≥ ke, -1)` 은 뺐다
(`_pool_slots`·오라클이 같은 조건을 스스로 -1 로 다룬다; CPU 검사가 출력 동일을 박는다). GPU 티켓 없음. PR #819.

**넷째 접기 (운영자 "추가 접기 및 융합 작업", 같은 날 오후, README §9 표).** 남은 두 런치짜리들 — head gate 의 곱 둘, 풀 레코드의 키·스케일
쓰기, 테일 링의 stack+쓰기, 선택의 지평 마스크 텐서, 드래프터 샘플 워크의 루프 불변식, 관찰 훅의 arange — 를 각각 하나로(`head_gate`·
`scatter_pools`·`write_tails`·`mask_horizon` 커널, 인터프리터 바이트 대조 OK): 스텝당 −45 런치 + 샘플 워크 −16 ≈ −0.15 ms. **런치 레버는 여기서
끝**: 1행 스텝 ~800 런치의 나머지는 커널 자체(dense GEMM 365, mHC 89, MoE 45, KDA 링 68, 라우터 층당 6, 스텝 진입·마무리·샘플러·드래프터)이고
바이트 동일하게 접히는 것이 없다(GEMM 병합은 split-K 선택을, 라우터 `sorted=False` 는 전문가 합 순서를, aux `mhc_post` 재사용은 fused post+pre 를
바꾼다). 남는 레버는 전부 수치가 바뀌는 커널 작업이다: dense W4 GEMM 의 소형 M 효율(1행 13 ms, 35%), MoE 207 → 240 GB/s, mHC 3 ms, fp32 head
gate SGEMM 0.5 ms — 브래킷 필요.

**커널 효율화 대상 고르기 (운영자 "커널 효율화 작업 진행해", 같은 날 오후, README §11, PR #838).** 커널 표의 합이 아니라 **노출 시간**(다른 스트림이
덮지 않는 몫)으로 4행 재생 70.25 ms 를 나눴다: MoE 정적 43.85 · dense W4 GEMM 12.80 · KDA 6.38 · mHC 4.22 · 틈 0.42(숙주는 병목이 아니다); 1행에서는
공유 전문가가 둘째 스트림에서 dense GEMM 4.45 ms 를 MoE 아래로 숨긴다. dense W4 GEMM 의 발사당 고정비는 #825/#826/#827 줄이 같은 커널 파일을 작업
중이고, MoE·mHC 기하 후보는 `measurements/st_decode_capacity_20260913` 에서 오늘 기각됐으며, 정적 MoE 는 39차 §4 에서 이미 선형 읽기 239 GB/s 의
93~95% — **이번 판의 레버는 둘 다 아니다.** 미점유였던 KDA 순환 링(4행 192 GB/s vs 1행 318)은 벤치 `kda_ring_bench` 로 갈랐다: FP16 이 바이트를
반으로 줄였을 때 4행 시간은 0.64 로 줄고, 두 점 풀이로 **바이트 몫 124 µs 가 이미 270 GB/s(DRAM 속도), 나머지 50 µs** — 배치를 바꿔 얻을 것은
없고 레버는 쓰는 바이트(토큰마다 남기는 되감기 상태 7 MiB/행)를 체크포인트 + 재계산으로 줄이는 구조 변경뿐이다(4행 스텝 최대 −3.2 ms, 캐시 설계,
운영자 판단). 같은 날의 큐 변경(작은 티켓 묶음, 원장 기록 추정, 프로브 예산·결과 자동)은 PR #836.

### 45차 — 갈라진 랭크는 기다리지 않고 말하며 죽는다: 호스트 집합통신의 트립와이어, 스텝 감시, 부팅 표의 실패 투표, 브래킷 포렌식 (2026-09-13, 맥 + srv4 CPU, 운영자 "통신장애가 일반 vLLM 보다 안 일어나게 만들어야 하는데 맨날 일어나네" → "1~3 전부")

**기록.** 36시간에 세 번 같은 모양으로 죽었고 셋 다 사망증명서가 틀렸다. §97(9/12 22:31) 디코드 `_gather` 에 랭크 0·1 은 7행,
2·3 은 6행으로 들어가 16분 정지(GPU 0%); #796(9/13 새벽) NVMe 티어의 대화가 랭크마다 달라 부팅 일곱 번 사망; #800(05:24) 접두사
복원 실패를 랭크 2·3 만 혼자 프리필로 돌려 절반은 gloo 투표에 절반은 원샷 all-reduce 스톨 트랩에. 회선 증거는 없다: 3일치 큐
run-logs·오늘 프로덕션 포렌식 10건에 NCCL WARN 0, 링크 플랩 0(9/11 20:24 이후). "Connection closed by peer"·"TCPStore recvValue
failed"·"timed out" 은 전부 **먼저 죽은 다른 랭크**를 기다리던 쪽의 말이었다. vLLM 이 덜 죽는 이유는 결정 주체가 하나(드라이버)라
워커가 랭크-지역 판단으로 집합통신을 가르는 자리가 없기 때문이고, ST 는 그 자리를 여럿 만들었다(티어 정산·복원·파킹, 티어 합의,
메모리 자격, 교정 관측, 랭크별 드래프트 생성기, 피어 플래그 스핀). 감사(에이전트 인벤토리 60여 지점) 결과 진짜 위험은 일곱:
`_admit` 의 접두사 캐시(`peek_chain`/`shared_ahead`)와 `kv.available`(스필 핀), `RuntimeMemory.checkpoint` 의 표 전 예외, 캡처
루프의 실패, 원샷 레인 선택의 `data_ptr()%16`, `_gather_divergence` 의 랭크-지역 루프 횟수, 성공 경로에서만 닿는 부팅 랑데부·표.

**한 것.**
- `engine/base/tripwire.py` **트립와이어**: 모든 호스트 투표·교환이 고정 길이(80 int64) 벡터에 랭크마다 (순번, 지점, 개수, 방식)
  꼬리표를 싣는다. 지점이 달라도 같은 all-reduce 를 완주하고, 네 랭크가 같은 표를 읽어 **같은** `CollectiveDivergence` 를 던진다
  (실패 경로에서만 `gather_objects` 로 이름을 모은다). 스텝 브로드캐스트는 rank 0 이 찍고(`stamp`) 나머지가 대조한다(`expect`).
  `Tripwire.of(comm)` 하나가 서버 투표·엔진 gather·부팅 시드를 같은 순번으로 센다. 지점: `settle:done`·`settle:outcome`·
  `admit:restore`·`admit:fits`·`admit:prefix`·`admit:reorder`·`gather:rows`·`gather:detail`·`boot:seed`. 꼬리표는 2^24 아래(순번
  mod 2^20, 지점 16비트): LocalTP 가 float32 로 합쳐 701907001 을 701907008 로 만든 것을 테스트가 잡았다.
- `serve.py` **입장은 플릿의 결정**: 접두사 경계·진행 중 여부는 `admit:prefix` 교환으로 최소/any, 재정렬 인덱스는 `admit:reorder`
  로 최소, 풀 여유는 `admit:fits` 투표로 "모두 맞을 때만" 입장 — §97 의 7행 대 6행이 여기서 나올 수 있는 모양이었다.
  죽을 때 `death-rank{r}-*.json`(divergence / peer-left / local, 단계, 마지막 지점)을 덤프 디렉터리에 남긴다.
- `adapter._gather`: 행 수 교환이 트립와이어를 타고, 보고서는 고정 5행 교환 하나(전엔 랭크-지역 횟수만큼 집합통신을 돌았다).
  "rich 행이 다르다" 도 이름을 댄다. `runtime_memory.checkpoint(phase, failed=)`: 읽기 예외를 표로 던진다. `graphs.py`·
  `capture_decode`·`fleet`: 실패한 랭크가 나머지가 기다리는 다음 표에 `failed` 를 던져 120 s 대신 지금 모두 세운다. `build`:
  `weights-loaded` 전에 죽는 랭크는 `failed:` 단계로 랑데부에 합류(1800 s 대신 지금). `boot:seed` 합의. `Comm._settled`: 원샷
  레인은 포인터 정렬이 아니라 dtype·형상으로(정렬 안 된 뷰는 복사).
- `engine/base/stall.py` **스텝 감시**: 옆 스레드가 60 s 에 기록, 300 s 에 링을 직접 쓰고 SIGKILL(`ST_STEP_STALL_NOTE_S`/
  `_TRAP_S`; SIGTERM 은 CUDA 호출에 갇힌 메인 스레드의 파이썬 핸들러에 못 닿는다). `TORCH_NCCL_HEARTBEAT_TIMEOUT_SEC` 7200 → 300
  (`ST_NCCL_HEARTBEAT_S`): §97 의 멎은 플릿이 두 시간 서 있을 값이었다; 캡처된 집합통신은 워치독이 보지 않으므로 부팅 캡처는 무관.
- `bench/st_bracket.sh` **포렌식**: 네 컨테이너를 모두 보고, 죽으면 정지 전에 네 랭크 `docker logs --tail=400` 을
  `st-bracket-dumps/<session>-<arm>/rank{r}-<ip>.log` 에 남기고 죽은 랭크의 마지막 오류 줄을 실행 로그에 찍는다(오늘 브래킷 사망
  4건 중 2건은 로그가 지워져 원인을 영영 모른다).
- 남은 것(별도 PR): 랭크별 생성기가 한 스트림을 데이터 의존 횟수만큼 전진시키는 드래프트/샘플 난수 — (시드, seq, 세대, 위치)로
  키를 매기는 무상태 균일난수로 바꾸는 일(에이전트 분석: `propose_rows` 의 `multinomial(generator)`, `SamplingGraphs`, `block_verify`).

**검증.** srv4(Linux, CPU): `tools/check.py --list` 126 파일 1,237 테스트 전부 ok(리베이스 후 main b1c33d45 위). 새 테스트: 트립와이어
스레드 4랭크(지점 불일치·한 랭크 누락·시드 불일치 → 네 랭크가 같은 문장으로 죽고 아무도 기다리지 않음; 사망 노트), 스텝 감시(기록·
트랩·링·SIGKILL), 입장 합의(피어가 "안 맞다"/"진행 중" 이면 여기서도 대기, 합의되면 입장), gather 보고서(rich 행 불일치), 메모리
표(읽기 예외도 표), 소스 핀(serve·adapter·comm·graphs·runtime_memory·boot·런처·브래킷). LocalTP 4랭크 락스텝 테스트가 첫 판에
잡은 것: 꼬리표를 float32 로 합치면 지점 id 가 깨진다 → 2^24 아래로. **GPU·플릿 실측 없음**: 다음 부팅·다음 사고가 답한다 —
사고가 나면 이제 `death-rank*.json` 과 브래킷의 `rank{r}-<ip>.log` 가 원인을 말해야 한다.

### 828차 — 큐의 대기가 밥값을 한다: 기다리는 동안 CPU 작업이 자동으로 돈다 + 시뮬레이터의 최신 부팅 5개 폴드아웃 (2026-09-13, 맥·CPU, PR #828)

운영자 규칙 "gpu 없이 할수 있는 작업 같으면 병렬로"가 분류 레인에서 끝났다. 네 박스를
기다리는 티켓은 컨트롤러에 앉아 CPU 가 한가한데, 대기는 `waiting: pos 2/3` 를 1초마다
찍기만 했다. 이제 `fleet.sh wait`(모든 대기 경로 — 감독자가 부르는 유일한 명령)가
`bench/fleet_waitwork.py` 의 `start` 로 작업을 자동 시작한다: **기본은 step_sim 재검증** —
onepass 원장의 최근 기록 3개로 비용 상수를 다시 폴딩해 예측 표를 다시 낸다
(`~/glm53-logs/wait-sim/`). GO 에도 끝나지 않았으면 두고 가고(컨트롤러 CPU, 플릿·리스
무접촉), 포기하는 끝남(TIMEOUT·실패·부모 사망)만 EXIT 트랩이 끊는다. `FLEET_WAIT_WORK`
교체·`off` 해제. 시뮬레이터에는 요청별 **큐 대기**(도착→첫 프리필 청크) 측정이 들어갔다:
D10 밸브 2s 실험에서 두번째 요청의 대기 2.025s 가 잡힌다 — 밸브와 직렬 프리필이 미루는
만큼이 이제 측정값이다.

**최신 부팅 5개 폴드아웃**(09-12 ST 네이티브: onepass-a/h·ab-kv7·redhat-tile32-control·
nvidia-cutover; 증거 `step_tools_nofleet_20260912/sim_validate_native_5.txt`). 세 부팅은
전 예측 행 ±7.6%(대부분 ±5%), **프리픽스 캐시 적중 부팅**(32K TTFT 0.8s·128K 1.0s)도
폴딩 사다리가 그대로 재현, 수용률 5.4% 극단에서 tokens/step +1.3%(773차 q-매핑의 검증).
두 기록의 큰 잔여는 **기록 안의 채널 불일치**다 — 카운터(step/s×tokens/step)와 요청별
실측이 스스로 1.87x·0.76x, 2K warm 이 1.93x·4.40x 로 갈라지고, 시뮬레이터는 다섯 부팅
전부에서 카운터 채널에 −4.3% 이내로 앉는다. 시뮬을 나란히 놓으면 이 갈림이 바로
보인다 — 대기 작업이 매번 최신 기록으로 이 표를 다시 만드는 이유.

테스트: `test_fleet_waitwork` 6(계획 4분기·CLI 경계 왕복·실행 중 stop), `test_step_tools`
27(큐 대기·밸브·closed-loop 0·pick_last). `test_fleet_coalescing` 2 실패는 clean main
재현 확인(이 PR 밖). D17 그대로 — 시뮬레이션은 판정이 아니다.

### 45차 — 난수는 스트림이 아니라 키다: 드래프트·샘플·검증의 모든 균일난수를 (시드, nonce, 생성 수, 용도, 위치)의 해시로 (2026-09-13, 맥 + srv4 CPU, 운영자 "시작해")

**기록.** §97(9/12 22:31)의 7행 대 6행은 원인이 두 갈래로 남아 있었다: 문법 matcher 의 span, 또는 그 span 을 계산한 대상인 드래프트
(`propose_sampled` 가 랭크마다 생성기에서 뽑는다). #750·#752 가 둘을 가르는 투표를 넣었고 #823 이 멈추지 않고 말하게 했지만 원인은
그대로였다. 엔진의 난수는 랭크마다 Philox 스트림 하나(`self.gen`, 시드 0, 합의 없음)였고, 그 스트림을 **데이터 의존 횟수만큼**
전진시켰다: rich 행은 문법 span 만큼 `rand(count)`, walk 는 `rand(K)`, 검증은 `rand(n,K)`+`rand(n)`, 캡처 그래프 안의 `multinomial`
×K, 그리고 스텝이 동기 경로냐 체인이냐로 뽑는 모양 자체가 달랐다. 스텝 t 의 스트림 위치는 프로세스 전체 이력의 함수였고, 한 번의
차이(한 ULP 로 span 이 하나 달라지는 것)가 플릿이 서 있는 내내 두 스트림으로 남았다. 비교하는 곳은 없었다 — 토큰이 조용히 달랐다.

**한 것** (`engine/base/draws.py`).
- **행 키** = splitmix64(시드, 행의 입장 nonce, 스텝 시작 시점 생성 수). nonce 는 `add`·`extend`·`resume` 마다 1 씩 느는 입장
  순번이라 네 랭크가 같은 순서로 같은 값을 갖는다(시드를 가진 요청은 그 시드만 — 같은 시드는 어느 행·어느 부팅에서든 같은 토큰).
  **난수** = 키 ^ (용도<<32 | 위치)를 한 번 더 섞은 상위 53비트/2^53 을 float32 로. 용도: `DRAFT`·`PICK`·`VERIFY`·`FRESH`·`RICH`.
  파이썬 정수 구현과 int64 텐서 구현이 비트 단위로 같다(부호 있는 시프트는 마스크로 논리 시프트).
- `sampler`: `sample`·`draw`·`pick_each`·`speculative_pick(_batch)`·`block_verify(_batch)` 가 생성기 대신 균일난수를 받는다(검증은
  K+1: 위치 K 개, 뒤따르는 뽑기 1 개). `drafter.propose_rows`·`propose_sampled(_tensor)`: `multinomial(generator)` 대신 받은 균일난수로
  누적 walk.
- **체인**: 한 스텝이 한 행에 쓰는 전부(walk K, 검증 K, 보정 1)를 `step_block` 해시 한 번으로 — **드래프트 캡처 그래프 안에서**
  `b["nonce"]`·`b["generated"]` 를 입력으로 계산한다. walk 가 앞 K 를 쓰고 뒤 K+1 은 `b["draws"]` 로 남아 다음 스텝 검증이 쓴다(그 사이
  생성 수는 그대로). 해시는 eager 로 55 개 연산이지만 그래프 안에서는 재생의 일부다: 체인의 순증가는 입력 복사 2 + 출력 복사 1 −
  검증 `rand` 2 = **스텝당 런치 +1**(확률 행이 있을 때만; greedy 스텝·버스트는 0). greedy 생존 행이 확률 배치에 합류하는 드문 병합만
  eager 해시 한 번.
- **동기 경로**: 같은 워드를 호스트에서 계산해 캡처 샘플러엔 온도처럼 pinned 입력으로(`SamplingGraphs.run(..., uniforms)`), rich 행·
  walk 엔 텐서로. 캡처 그래프에 생성기 상태가 없다(`register_generator_state` 제거). `burst_decode` 의 행 스키마에 `nonce`·`draws`.
- 프로브 다섯(`engine_tp_vocab_check`·`engine_graph_lifecycle_check`·`block_verify_bench`·`decode_middle_cost`·`decode_middle_capture`)을
  새 API 로.

**검증.** srv4 CPU `tools/check.py --list`: 130 파일 1,275 테스트 전부 ok(main 0cd1ca66 위로 리베이스 후). 해시 호스트·텐서 비트 일치(무작위 300 키, 7 위치씩), 단계 블록 = 호스트 경로의 DRAFT·VERIFY·FRESH, 키 성분마다
분리, 균일성(20,000 개, 10 칸 ±10%), 호스트 검증과 배치 검증이 같은 키에서 같은 결정, 서빙 모듈에 생성기·`multinomial`·
`register_generator_state` 없음(소스 핀). 기존 샘플링·드래프터·파이프라인·버스트·문법·릴리스 테스트를 새 API 로. **GPU 실측 없음**:
CUDA 에서의 비트 일치(테스트 `test_the_device_agrees_with_the_host_on_cuda_too` 는 CUDA 에서만 돈다)와 캡처 그래프 안의 해시는 다음
GPU 판정이 답한다. 하나 더: 같은 시드의 이전 부팅과 토큰이 달라진다(스트림에서 키로) — 기준선 비교는 greedy(onepass 기본)만 유효.

### 834차 — 시뮬레이터가 못 담던 디테일을 흡수한다: 문 앞면·JIT 꼬리·컨텍스트별 decode — 11개 기록 폴드아웃에서 9개 전 행 ±10% (2026-09-13, 맥·CPU, PR #834)

828차까지의 시뮬레이터에 남아 있던 이름붙은 잔여가 셋이었다: warm 2K TTFT 의 −6~−9%(서빙
앞면), 아예 범위 밖이던 cold TTFT(JIT 꼬리), "컨텍스트 미계수(평탄)" 표시. 셋 다 새 부팅
없이 **이미 있는 기록에서 폴딩**했다:

- **앞면**(`front_ms`): 가장 짧은 컨텍스트의 warm TTFT 잔여. 요청당 한 번, 첫 프리필
  청크 안에서 — 청크마다 내는 첫 구현은 8청크×67ms 를 32K TTFT 에 얹었고 스팟체크가
  잡았다(+0.5s 버그). 큐 대기는 앞면을 세지 않고 TTFT 만 센다.
- **JIT 꼬리**(`cold_extra_s`): 컨텍스트별 cold−warm, 그 길이의 **첫 요청만**. cold TTFT
  가 예측 행이 됐다 — 전 기록에서 +0.1~3.9%.
- **컨텍스트별 decode 계단**(`decode_ms_by_ctx`): `windows_by_ctx` 폴딩. 실측으로 밝혀진
  사실: 대부분 부팅에서 decode 스텝 시간은 컨텍스트에 **평탄**하다(onepass-h
  16.94/16.95/16.93 step/s) — 미계수 표시가 사실은 측정 결과였다.

**11개 기록 폴드아웃**(ST 구엔진 1 + glm53 3 + ST 네이티브 5 + prefill-K 2; 증거
`step_tools_nofleet_20260912/sim_validate_all_11.txt`): **9개 기록이 모든 예측 행
±10% 이내**(대부분 ±6%), prefill-K1 은 13행 중 12행(2K e2e 혼합 중앙값 +28% 1행),
NATIVE-A·CUTOVER 의 큰 잔여는 828차가 확립한 기록 자체의 채널 분열(1.87x·0.76x)이다.

**정직한 상한**(도구 docstring 에 명시): "실측 불필요"의 경계 — 측정된 빌드의 상수를 한 판
폴딩하면 그 빌드의 다른 구성 전부(컨텍스트 사다리·도착 순서·큐잉·기아 밸브·프리픽스
재사용·JIT·앞면)를 부팅 없이 예측한다. 엔진이 바뀌면 상수가 무효 — 한 판이 다시 필요하다
(상수의 원천이 항상 실측). 폭(C=4) 계수는 완결 기록 대기(최신 기록의 c4 팔이 비어 있음),
품질(한국어·9/9)과 고장(랭크 갈림·OOM)은 모형의 언어 밖. D17 유지 — 부팅 수를 줄이는
도구지 판정을 대신하지 않는다.

테스트 31(신규 4: 앞면-큐대기 분리·길이별 cold 1회·컨텍스트 계단·fit_cost 폴딩),
`tools/check.py` 1 ok.

### 45차 — NVMe 티어는 키별로 재조정한다: 네 랭크가 똑같이 가진 대화만 남기고 나머지는 가진 랭크에서 지운다 (2026-09-13, srv2 포렌식 읽기 + srv4 CPU, 운영자 "키별 재조정")

**기록.** 13:01:47 세 번째 헬스체크 채팅("ping", max_tokens 4, temperature 기본값 1.0)에서 플릿이 갈라졌다. 랭크 0·1 은 턴을 끝내고
파킹한 뒤 `_settle` 투표에서 기다렸고 랭크 2·3 은 아직 디코드 스텝의 원샷 all-reduce 에 있었다(05:24 와 같은 모양, #800 배포본에서).
티어 파일(읽기만): 대화 0(13:00:44)·1(13:01:16)은 네 랭크의 JSON 기록 내용이 같고, 대화 2(13:01:47)는 랭크 0·1 에만 있다(답 토큰도
0·1 과 다르다). 13:04~13:20 재기동 여섯 번이 전부 약 70 초 만에 `the ranks' NVMe tiers hold different conversations ... Clear
glm53-logs/st-tier on every node` 로 죽었다. 트레이스백은 `boot.py fleet` 의 **캡처 전 검사**였다: #796 은 서버 생성자의 검사에만
"모두 버리고 뜬다"를 달았고, 그보다 먼저 도는 이 검사는 `forget` 없이 옛 "죽는다" 계약을 썼다. 배포본(0ac226a8)과 main 에 그대로였다.
그리고 생성자 쪽도 어긋나면 **전부** 버렸다 — 네 랭크가 멀쩡히 가진 대화 0·1 까지.

**한 것** (`engine/base/serve.py`).
- `Server._parked_entries(tier, keys, rank)`: 키마다 digest = sha256(호스트 기록 JSON(문맥·pending·토큰 전부·limits·options·그림) +
  필요한 블록 수). 랭크별 파일 이름·쓰기 시각은 넣지 않는다. 읽을 수 없는 기록은 자기 랭크 번호를 넣은 digest 를 받아 어느 피어와도
  맞지 않는다(네 랭크가 똑같이 못 읽어도 남지 않는다).
- `Server._reconcile_parked(comm, entries, forget, what)`: (키, digest) 목록을 **호스트 그룹 `gather_objects`** 로 교환하고, 모든 랭크가
  같은 digest 로 가진 키만 남긴다. 나머지는 가진 랭크에서 `forget`. 표가 모든 랭크에서 같으니 결정도 같고 `next_seq` 도 같다. 지운 것은
  이유와 함께 한 줄("2 (only rank 0,1)", "0 (records differ)"). 지우기 실패는 보고만 하고 부팅은 계속.
- 두 지점 모두 이것으로: `boot.py fleet` 의 캡처 전 검사(이제 죽이지 않는다), 서버 생성자의 대화·접두사 경계. 옛 `_agree_on_parked`
  (장치 MAX 투표 + 전부 버림/죽음)는 없어졌다 — torch 도 필요 없어져 두 랭크 가짜 comm 의 서버 테스트가 torch 없이 돈다.

**검증.** srv4 CPU `tools/check.py --list`: 131 파일 1,289 테스트 전부 ok. 새 `tests/test_engine_tier_agreement.py`(스레드 4 랭크): 13:01:47 의 모양(0·1 에만 대화 2) → 네 랭크 모두 [0, 1]; 같은 키
다른 기록 → 모두에서 지움; 같은 기록 다른 블록 수 → 지움; 한 랭크 못 읽음·모두 못 읽음 → 지움; 지우기 실패 → 보고 후 계속; 1 랭크는
교환 없음; 모자란 응답 거부; **실제 서버 생성자 + Runner + TieredKV + 메모리 티어** 네 개가 어긋난 티어(0·1 에만 2, 3 에만 5)로 떠서
모두 [0, 1]·`next_seq` 2, 디스크 사본도 [0, 1] 만 남음; 소스 핀(두 지점, 옛 계약 문구 없음, 캡처 전). **GPU·플릿 실측 없음**: 다음
프로덕션 부팅이 답한다. 지금 네 노드의 티어(0·1 에 대화 3 개, 2·3 에 2 개)는 이 수정이 배포되면 스스로 풀린다 — 그 전에 프로덕션을
띄우려면 `glm53-logs/st-tier/rank*` 를 비워야 한다(헬스 ping 셋뿐). 남은 것: 갈라짐 자체(확률 샘플링 헬스 ping 직후 두 번; #823
트립와이어 미배포, #832 난수 키), 헬스 ping 이 매번 랭크당 약 256 MiB 를 파킹하는 것.

### 839차 — 스텝을 바이트로 조립한다: 구조에서 유도하는 디코드 예산 — 평탄성이 필연임이 예측됨 (2026-09-13, 맥·CPU, PR #839)

시뮬레이터의 장치 상수가 *왜* 그 값인지를 말하는 층을 깔았다(`bench/step_kernels.py`).
바이트는 전부 리포 문서에서: 랭크당 전문가 39.87 GiB(288×42), 정적 가중치 4.63 GiB,
드래프터 2.03 GiB(랭크마다 통째), KV 5.90 KiB/토큰(블록 13.28 MiB=2304×11DSA), 상태 링
247.2 MiB/행, AR 102 회/스텝(실측). 자유도는 대역폭 도달률 0.85 와 AR 지연 하나(41µs,
K-steady 71.7ms 에 1회 폴딩)뿐이다.

**맞추지 않고 설명한 것**: (1) **평탄성** — 컨텍스트에 자라는 항은 어텐션 KV 뿐이고
2K→128K 에서 0.05→3.33ms(스텝의 4.7%): "대부분 부팅에서 decode 가 컨텍스트에 평탄"이
구조의 필연으로 예측된다. (2) **노브 델타** — 드래프터 bf16→W4 예측 −6.85ms(64.9 대
실측 NATIVE-H 59.0, +10%), +k=5(60.5 대 KV7 55.7, +9%): "이 PR 이 X 바이트를 없앤다 →
−Y ms" 가 부팅 전 진술이 됐다. (3) **폭 예측**(C=4 완결 기록이 없어 미계수였던 자리) —
4행 2.07×: 전문가·드래프터는 스텝에 한 번, KV·상태만 행마다(45차 실측 1.4~1.7× 와 같은
메커니즘, 스트리밍·직렬 프리필이 더 누르는 것).

`step_sim` 에 `acc_hist`: 수용률을 실측 분포에서 추첨(쌍봉은 쌍봉으로) — 기하 추첨은
평균만 맞춘다. 저장 스크랩에 `st:spec_accepted_per_step_total` 이 아직 없어 폴딩 슬롯은
데이터 대기. 정직성: 바이트 예산은 상한·델타 예측이지 판정이 아니다(규칙 6, D17);
전문가 기댓값(288×(1−(1−8/288)⁷)+1≈52.6)은 무작위 라우팅 가정이라 몰림이 있으면
그 아래로 읽힌다. 테스트 36(신규 5) 통과.

### 842차 — 바이트 예산이 플릿을 만났다: #838 실측 대조, 가정 셋을 정정하고 자유도 제로 (2026-09-13, 맥·CPU, PR #842)

839차의 바이트 예산을 #838(c4_scaling) 의 타임라인 실측과 나란히 놓았다. **바이트는
맞았다**(전문가 3.54 vs 실측 3.44 MB/랭크·전문가, 3%) — 틀린 것은 가정 셋: (1) **라우팅**:
균등 가정 U(7)=52.6/U(28)=157 에 대해 실측 역산은 **33/95** (합성 라우팅 프로브조차
28.7/61.5 — 라우터 가중치가 실제로 몰린다) → 멱법칙 U(t)≈7.28·t^0.763. (2) **MoE 대역폭**:
232(0.85×273) 에 대해 정적 커널 실측 **207 GB/s**(76%), 프리필 dynamic 은 165. (3) **비MoE**:
가중치 읽기 일괄 평탄으로 봤으나 실측은 22ms + 4.33ms/행(KDA 링 쓰기·행별 elementwise·
FP32 SGEMM 라우터) + 집단통신 ≈1+1.9×행 ms(스텝의 5~10%) + 드래프터 별개 3.4ms(W4).

정정 후 조립은 **재현만 하고 폼판 데가 없다**(AR 지연 폴딩 삭제): #838 §3 의 단계 합
12개 지점(3컨텍스트×4폭)을 전부 ±10% 이내로 재현 — 전스텝 1행/2K **+0.6%**, 4행/2K
**−1.8%**, 128K/4행 **−1.8%**. 폭 배율 2.24×(실측 2.32~2.58×). #838 이 밝힌 "균등
라우팅이 forward 를 맞춘 것은 우연"이 이제 인과로 섰다.

부수: step_sim 의 직렬 장치 모델(한 줄 락)이 실측 뒷받침을 얻었다(#838 공존 팔 — 두
스트림 겹침 이득 **0.99**, 프리필 dynamic 165·디코드 정적 207 로 둘 다 DRAM 벽: SM 을
나눌 것이 없다). 평탄성 설명은 정확한 컨텍스트 항(인덱서 logits 풀 32K×질의 + 희소 MLA
top-2048, 31.7µs/1K-토큰/행)으로 이월. 테스트 37(12지점 재현·멱법칙·평탄성·노브 바이트
델타·아선형 폭) 통과.

### 845차 — 시뮬레이터가 엔진을 읽는다: 형상은 라이브, 상수는 아티팩트에서, 매 실행이 출처를 말한다 (2026-09-13, 맥·CPU, PR #845)

엔진과 시뮬레이션 사이의 마지막 사람 손 — 손으로 옮겨 적은 상수 — 을 없앴다. (1) **형상**:
`load_engine_facts()` 가 `engine/profiles/glm53/facts.py` 를 실행 시점에 import 한다(TP·SPEC_K·
청크 정렬·KV dtype): 엔진이 바뀌면 시뮬레이터가 다음 실행에서 따라간다. (2) **라우팅**:
#838 타임라인 아티팩트의 층별 실측 고유 전문가 분포 [(7,28.9),(14,43.7),(21,55.5),(28,62.0)]를
로그-로그 최소제곱으로 폴딩(`--routing artifact`; artifact 분기가 균등로 흘러들던 오류도 수정).
(3) **커널별 GB/s**: 격리 벤치 32항목을 읽어 바이트 묶임(5: MoE 정적 180~210, kda.in_proj
135~168) 대 런치 바닥(27: ~78µs 급)으로 가른다 — 커널 표적 선정이 도구 안으로. (4) **프리필**:
chunk-profile 아티팩트 자신의 최소제곱(287.9µs/토큰+269.5ms/청크, 단일 랭크)을 직접 읽어
`--prefill N` 이 예측: 129,295 tok@9216 청크 → 플릿 42.9s(#838 §4 실측과 일치; 플릿 +110ms/청크).

**수용률 파이프라인 완결**: step_peek 이 `st:spec_accepted_per_step_total` 라벨 계열을 저장하고
`acc_hist_from_scrapes()`(순수)가 두 스크랩 차이를 [0..k] 분포로, `step_sim --acc-hist-from` 이
기하 추첨을 그 분포로 바꾼다 — 라이브 부팅 한 번 관측으로 834차의 슬롯이 채워진다. **폭(C=4)
폴딩 경로**: `fold_width_from_records()`+`--fold-width` 가 완결 C=1/C=4 짝에서 decode_ms(w)=
base+per_row×(w−1) 을 못박는다 — 오늘 리포로 돌리면 "짝이 없다"가 정답(조립 예측 2.24× 가
자리를 지킨다), 완결 기록이 오는 순간 실측으로 대체.

모든 실행이 `[출처]` 줄로 상수의 출처를 말한다(facts.py / 아티팩트 경로 / #838 역산) — 출처
표시 자체가 정직성이다. 테스트 42(실제 facts import·실물 아티팩트 왕복·스크랩→분포→시뮬·
합성 짝 폭 풀이) 통과. D17 그대로: 코드를 읽는 것은 상수의 출처를 사람 손에서 엔진·증거로
옮기는 일이지, 측정을 만들지는 않는다.

### 846차 — 조립이 시뮬레이터 안으로 들어온다: --compose·청크 구조 프리필·--conc — C=4 램프가 구조적으로 재생 (2026-09-13, 맥·CPU, PR #846)

origin 추정 때 사람 손으로 만들던 cost JSON 을 없앴다. `--compose` 한 번으로: 형상(facts.py
라이브) + 디코드 사다리(#838 실측 분해, 자유도 0) + **폭 계수**(4행 조립에서 풀어 decode_ms_per_row
= 20.7ms — 마지막 "미계수"가 사라지고 요약이 "전 계수 폴딩됨") + 프리필 청크 모형(아티팩트
폴딩, 플릿 +110ms/청크). 사람이 쥐는 숫자는 수용률 하나(실측 분포가 오면 --acc-hist-from).
계약도 엔진 실측값: 정렬 2304, 혼자 프리필 9216, 디코더 옆 2304. 프리필은 청크 구조
`total = v·토큰 + F·청크`(#838 §4) — 청크가 작을수록 토큰당 비싼 이유가 모형 안에 있고,
TTFT 가 청크 크기·개수에 구조적으로 반응한다. `--conc N` 이 동시성을 만든다.

**검증(#838 §2 실측 C=4, 2K×4300토큰)**: TTFT 사다리 0.99/2.1/3.2/4.2s 대 실측
1.3/…/6.0~6.2s — **둘째부터 프리필이 비싸지는 램프**(디코더 옆 2304청크 + 청크-디코드
교대)가 구조적으로 재생된다. 합산 ~127 tok/s 대 실측 106~120, 배율 1.77~1.9× 대
1.66~1.71×. 남은 4번째 TTFT 갭(−31%)은 이름 있는 원인이다: 실제 디코더-옆 청크는 공존
벌(#838 공존 팔: 883→1122ms, 1.27×)을 내는데 이번 청크 모형은 아직 적용하지 않는다 —
다음 폴딩. 테스트 45 통과.

### 850차 — 정확도 round: 공존 벌 1.27× 와 플릿에 못박힌 폭 계수 — C=4 래더가 실측을 감싼다 (2026-09-13, 맥·CPU, PR #850)

846차가 이름 붙인 잔여 둘. (1) **공존 벌**: 디코더 옆 프리필 청크가 #838 공존 팔 실측의
1.27×(883→1122ms — 두 스트림 다 DRAM 벽, 한 일의 합은 같고 청크는 늦게 끝난다)를 낸다.
러너 상태로만 판정하고 문 앞면·JIT 꼬리는 제외하며, 순차 하네스(11개 기록 폴드아웃)는
무벌 — 불변이 테스트로 고정됐다. 4번째 C=4 TTFT 4.2→4.97s(실측 6.0~6.2, 갭 −31%→−19%).
(2) **폭 계수**: "완결 C=4 기록 대기"가 기다림 없이 닫혔다 — #838 §3 의 스테이지 표(CUDA
이벤트, rows1 47.7→rows4 110.8ms @2K)가 플릿 폭 실측이다. fold_width_from_stage() 가
composed 의 decode_ms_per_row=21.03 을 못박고, 조립의 자체 풀이(20.7)가 교차검증으로 옆에
선다 — **독립 출처 둘이 1.5% 안**.

**C=4 래더는 이제 실측을 양쪽에서 감싼다**: 2128토큰(단일 청크) 넷째 TTFT 4.97s(−19%),
2498토큰(2청크 — 194토큰 꼬리가 온전한 고정 F 를 공존 벌로 냄) 7.34s(+18%). 감쌈의 폭이
모형의 청크 경계 민감도다: 실 엔진이 꼬리 청크를 병합하는지가 다음 질문이고 host_step 열
(c4_scaling 추출 JSON 의 phases) 에서 읽힌다. 테스트 47 통과.

### 851차 — ST 오라클: 이름, 모델 레지스트리, 그리고 결측이 있어도 신뢰도와 함께 값을 주는 것 (2026-09-13, 맥·CPU, PR #851)

시뮬레이션 도구군의 이름을 **ST 오라클(ST Oracle)** 로 못박고 하나의 문(bench/storacle.py —
models 리포트 + sim/kernels/peek/replay 전달)을 열었다. 다중 모델은 레지스트리로: 각 모델의
바이트가 측정/구조/결측인지 명시한다 — glm53 전부 실측(조립 가능), qwen38 구조+실측 일부
(512전문가×top-10·hidden 2560×inter 640 NVFP4, EP 로 랭크마다 전체 전문가 128개·GDN 상태
27.5MiB/행 실측; MoE 층수·정적 가중치·KV/토큰 결측), dsv4 실측 일부(KV 12.75KiB/토큰·상태
27.5MiB/행; 전문가 형상 결측). load_engine_facts 는 모델을 검사해 glm53 의 facts 를 다른
모델에 조용히 입히지 않고, 이관은 매 줄에 고지된다.

**이번의 규칙(운영자): 결측은 답을 금지하는 게 아니라 신뢰도를 낮춘다.** 결측 필드마다 근거
있는 상하한(MODEL_BOUNDS)을 두고 양끝으로 스텝을 조립해 신뢰구간과 신뢰도(=100×(1−
spread/2))를 함께 준다: **glm53 100%(52.4ms) · qwen38 92%(40.0~46.6ms; 가정 moe_layers[32..47]·
static[1.5..4GiB]·kv[4..12.75KiB]) · dsv4 54%(36.9~100.7ms; 전문가 형상 개방)**. --partial 이
step_sim 까지 관통해 cost JSON 에 confidence 필드로 새겨진다. 근거 없는 구간의 결측은 여전히
거부한다 — 모르는 것은 0 이 아니라고 말하는 쪽을 지킨다. 결측이 채워지면(체크포인트 한 번
읽기) 같은 명령·같은 검증 경로로 100% 에 수렴한다. 테스트 51 통과(레지스트리 정체성·거부
규칙·구간 수학·신뢰도 서열 glm53>qwen38>dsv4·composed confidence 전달·파사드).

### 852차 — storacle predict: 마법사의 커널 판정과 오라클의 예상 속도를 한 장으로 (2026-09-13, 맥·CPU, PR #852)

새 모델 예측이 속도만 답하던 것을 고쳤다 — "어떤 커널이 돌지"는 엔진의 형상 마법사
(engine/kernels/cells.admission, #824/#831/#844: 형상을 받아들이면 컴파일된 셀에 대해
레인을 판정하고 빈 자리에 레시피를 남긴다)가 이미 답하고 있었다. `storacle predict
--model <이름>` 이 둘을 부른다: (1) 14레인 판정(admitted/unmeasured/refused + 각 층의
서빙 커널), (2) #838 격리 아티팩트의 커널별 실측(GB/s·런치 바닥)이 레인 옆에, (3) 예상
속도·신뢰도(glm53: 14 admitted · 스텝 52.4ms · 신뢰도 100%).

**거부 레인이 답의 종류를 바꾼다**: 그 형상은 이 엔진에 커널이 없으므로 예상 속도를 내는
대신 작업 목록(무엇을 만들고 무엇이 판정하는가, 저렴한 것부터)이 답이고 종료코드 1 —
부팅할 수 없는 모델에 가격을 매기지 않는다. 형상 미완 모델(qwen38·dsv4)은 판정 대기를
고지하고 --partial 로 구간·신뢰도 속도를 준다(qwen38 92%). config.json 읽기가 형상을
채우면 마법사 판정이 같은 명령에 붙는다. 테스트 52 통과.

### 854차 — 오라클이 마법사의 서빙 계층에 가격을 매긴다: 전용은 실측, 범용은 근거 구간 (2026-09-13, 맥·CPU, PR #854)

마법사는 이미 어느 커널이 각 레인을 서빙하는지 말했다(specialized 셀·generic 고속 커널·
nothing). 오라클은 셋을 같은 가격으로 계산했다. LANE_COMPONENT 지도로 레인을 조립 성분에
옮기고: **전용**은 실측 그대로(207~241GB/s, 공칭의 76~88%), **범용**(engine/kernels/common,
같은 수학의 모양 무관 커널)은 격차 직접 실측이 없어 **근거 있는 구간 ×1.25~3** — 스텝
구간이 벌어지고 신뢰도가 내려가며 결측 프로브가 이름으로 남는다. 'nothing fast' 는 가격을
매기지 않는다(레시피가 답). 바이트 구간과 페널티 구간은 같이 열린다(lo 예산×하한, hi
예산×상한).

**`storacle predict --generic <레인들>` what-if**: 새 모델이 전용 셀을 못 얻는 레인을
가정하면 한 줄로 — glm53 에서 mhc·kda 를 범용으로 보면 **~71 → ~48 tok/s, 신뢰도 75%**;
moe 를 범용으로 보면 74%. 커널을 쓰기 전에 "이 셀의 가치"가 나온다.

과정에서 잡힌 버구 둘: 완결 모델이 티어 적용을 우회하던 것(조기 반환), bounds 갈래가 hi
예산을 계산하고 버리던 것(qwen38 신뢰도가 잘못 100% — 수정으로 92% 복원). 테스트 53 통과.

### 45차 — 헬스 ping 같은 턴은 파킹하지 않는다: `retain: false` 와 짧은 턴 하한 (2026-09-13, 맥 + srv4 CPU, 운영자 "헬스 핑 같은건 파킹 제외하게 해야지")

**기록.** srv2 의 네 노드 티어(읽기만): 슈퍼바이저 헬스 ping("ping", max_tokens 4)이 문맥 17 토큰짜리 대화를 30 초마다 파킹했고, 파일 하나가
랭크당 268,591,104 바이트(슬롯의 순환 상태가 거의 전부)였다. 64 GiB 티어는 약 250 개면 차고, LRU 라 ping 이 실제 대화를 밀어낸다.

**한 것.**
- 문(`sampling_options`): 요청의 `"retain": false`(ST 확장, 불리언 아니면 400)를 내부 옵션 `_transient` 로. 옵션은 도착 브로드캐스트를 타므로
  네 랭크가 같은 표시를 갖는다(`OPTION_KEYS`·`validate_options` 에 `_host_stop` 처럼 불리언으로).
- `Server(park_min_tokens=0)`: 끝난 턴의 문맥이 이보다 짧으면 반납. GLM 프로덕션 부팅은 `boot.PARK_MIN_TOKENS = 128` — 짧은 기록은 다시
  프리필하는 편이 슬롯 상태를 티어에 쓰고 되읽는 것보다 싸다고 보고 정한 값이다(교차점은 측정하지 않았다).
- `_retire(row, request)`: 요청이 표시했거나(`asked`) 하한 미만(`short`)이면 행을 비우고(`runner.evict` + `engine.forget`) 대화 장부에서
  지운다 — 티어 쓰기도, 유휴 상주도 없다. 두 입력 다 복제된 것(옵션·행의 문맥)이라 네 랭크가 같은 턴을 반납한다. 반납한 대화 ID 로
  이어가면 409, 채팅은 새 프롬프트. `st:turns_not_retained_total{reason}`.
- `launchers/st-glm53-supervisor.sh` 의 ping 에 `"retain":false`. 옛 엔진은 모르는 필드를 무시하므로 설치 순서와 무관하다. 하한은 엔진만
  배포돼도 ping(17 토큰)을 거른다.

**검증.** srv4 CPU `tools/check.py --list`: 144 파일 1,348 테스트 전부 ok. 새 `tests/test_engine_turn_retention.py`: 표시한 턴은 티어에 안
쓰이고 행·장부가 비며 다음 보통 턴은 파킹됨; 하한 미만 반납·하한 이상 파킹; 티어 없이도 상주하지 않음; 반납한 ID 이어가기 409; 기본값은
예전 그대로; 문의 불리언 검사; LocalTP 네 랭크가 같은 턴을 반납(표시 1·짧음 1·파킹 1); 소스 핀(부팅 하한, ping 본문, retire 두 곳).
**GPU·플릿 실측 없음.** 남은 것: 라이브 문에 붙는 D17 프로브(onepass 두 판)도 요청마다 파킹된다 — onepass 요청 본문을 바꾸면 워크로드
identity 가 바뀌어 이번에는 두지 않았다.

### 45차 — 측정 요청도 파킹하지 않는다: onepass 가 `retain: false` 를 보내고 하니스 45 (2026-09-13, 맥 CPU, 운영자 "그것도 어떻게 해봐")

**기록.** 부팅은 티어가 있으면 `keep_idle` 로 뜨므로(`boot.py`), onepass 요청은 끝날 때마다 파킹됐다: 2K 는 슬롯 상태 약 256 MiB + 블록
하나, 128K 는 블록 약 56 개가 더해져 랭크당 약 0.5 GiB. 브래킷 부팅에서는 그 쓰기가 다음 요청과 겹쳤고, 라이브 문에 붙는 D17 프로브는
프로덕션 티어를 아무도 이어가지 않을 대화로 채웠다(LRU 라 실제 대화를 밀어낸다). 헬스 ping 은 #858 이 막았지만 onepass 요청은 2K 이상이라
짧은 턴 하한에 걸리지 않는다.

**한 것.**
- `bench/onepass.py` `ask_stream`: 요청 본문에 `"retain": false`. 끝난 측정 요청은 파킹도 유휴 상주도 없이 반납된다(#858 의 엔진 동작;
  그보다 옛 엔진은 필드를 무시하고 예전처럼 파킹한다).
- **하니스 45**(`measurement_contract.HARNESS`): 질문·예산은 44 그대로지만 서버가 요청 뒤에 하는 일이 달라졌으므로 44 기준선을 재사용하지
  않는다. 브래킷과 프로브가 같은 본문을 보내므로 D17 표본과 브래킷 표본은 계속 한 풀이다. `st_screen.py` 가 하니스를 하드코딩(44)하던 것을
  계약값으로.
- 요청 바이트를 대조하는 옛 `ask_stream` 오라클(`test_onepass_channel_diagnostics`)에는 이 필드 하나를 테스트 안에서 드러내 놓고
  적용했다 — 나머지는 여전히 바이트 단위로 같아야 한다. `test_onepass_measurements` 가 본문의 `retain: false` 를 핀.
- `ONEPASS_QUALITY.md`·`ONEPASS_RECORDING.md`(머리글이 하니스 42 로 낡아 있었다)·`onepass.py` 문서를 45 로.

**검증.** 맥 CPU: `test_onepass*.py` 전부 ok, `test_st_screen`·`test_fleet_onepass*` ok, 계약·판정·실험을 import 하는 39 모듈을 main 사본과
대조해 새 실패 없음(main 사본 쪽 추가 실패는 `.git` 없는 사본에서만 나는 st_bracket 리허설 부류). **GPU 실측 없음.** 대가: 다음 pair 는
배포된 sha 의 하니스 45 웜 표본이 없으면 base 를 한 번 더 부팅한다. 남은 것: D17 프로브는 라이브 문의 접두사 캐시를 `/v1/prefix/reset`
으로 비우고 시작한다(설계대로이지만 프로덕션 캐시가 매 배포 뒤 한 번 비워진다).

### 873차 — D17 프로브가 프로덕션 접두사 캐시를 비우지 않는다: 리셋을 빼고, 반납될 턴의 경계는 그 턴 동안만 둔다 (2026-09-13, 맥 + srv4 CPU, PR #873, 운영자 "하나 더 진행")

**기록.** `st_bracket.sh probe` 는 판마다 먼저 라이브 문에 `POST /v1/prefix/reset` 을 보냈다. 이 리셋은 메모리의 경계와 faded 전부,
그리고 접두사 티어(16 GiB)의 슬롯 전부를 지운다(`Runner.reset_prefix`). deploy-watch 가 배포마다 프로브를 하나씩 큐에 넣으므로
프로덕션 접두사 캐시는 배포 뒤마다 한 번씩 통째로 비워졌다. 리셋은 측정에 필요하지도 않았다. onepass 는 기록 구간 안의 모든 요청
(C=4 작업 스레드 포함)에 요청마다 새 `cache_salt`(uuid4)를 붙이고, 테넌트 솔트는 첫 블록부터 체인 전체를 가른다. 그래서 프로브 요청은
프로덕션이 캐시한 것도, 서로가 캐시한 것도 맞힐 수 없다.

리셋만 빼면 프로브의 경계가 보통 경계처럼 남는다. 잎은 접두사 티어로 스필되어 아무도 다시 쓰지 않을 솔트로 티어를 채운다. 행이
끝난 뒤에도 그 블록은 CACHED 로 남아, 다음 예약이 프로덕션의 오래된 경계 블록부터 가져가게 한다. 스냅숏이 모자라면 `_victim` 은
미채택 경계 중 가장 오래된 것부터 내보내므로, 긴 프로브 프롬프트는 뒤쪽 스텝에서도 프로덕션의 남은 미채택 경계를 자기 것보다 먼저
내보낸다.

**한 것.**
- `bench/st_bracket.sh probe`: `reset_prefix` 를 빼고 기록을 `cold=live` 로. 브래킷 레그(자기 부팅의 문)는 판 사이 리셋을 그대로 한다.
  `st_judge.warm` 은 `live` 와 옛 기록의 `reset` 을 둘 다 웜으로, `colds` 는 여전히 `boot` 만.
- 엔진 `runner.transient`: 서버가 `retain: false` 요청이 잡은 행을 `_admit` 뒤·스텝 앞에 표시한다(옵션이 도착 브로드캐스트를 타므로 네
  랭크가 같은 행). 그 행의 프리필은 여느 프롬프트와 같은 일을 한다 — 경계를 계산해 캐시하므로 측정하는 것은 프로덕션이 도는 것 그대로다.
  그 경계(`Entry.transient`)는 스필 후보가 아니고, 스냅숏이 필요할 때 모든 부류보다 먼저 내보내지며, 행이 반납될 때(`_release`, 파킹이면
  `park_finish` 에서 표시만 지움) 함께 사라진다. 순서가 막지 못하는 것이 하나 있다. 한 프리필 스텝의 마크 스냅숏은 스텝 전에 한꺼번에
  받으므로, 행이 아직 자기 경계를 하나도 갖지 않은 첫 스텝의 몫(32K 스텝이면 수십 개)은 프로덕션 스냅숏 48 개에서 나온다. 그 뒤 스텝과
  다른 프로브 행은 프로브 자기 것을 먼저 내준다. 밀려난 프로덕션 경계 중 티어 사본이 있는 것은 블록을 든 faded 로 남고, 리셋이
  사라졌으므로 티어에서 돌아온다. 다른 프롬프트가 채택했거나(`lookup_chain`) 운영자가 핀하면 보통 경계가 된다. 티어에서
  읽어 온 경계(`restore_finish`)는 표시하지 않는다. 프로브가 채택한 프로덕션 경계는 원래 프로덕션 것이므로 건드리지 않는다.
  `st:prefix_transient_dropped_total`.
- `bench/EXPERIMENTS.md` 의 st-probe 설명, `test_fleet_st_bracket` 의 리허설 기대(`cold=live`)와 `FLEET_AUDIT` 재핀.

**검증.** 새 `tests/test_engine_transient_prefix.py`(13): 캐시 정책(스필 제외·채택/핀이 표시 해제·희생 순서 맨 앞), 러너(반납 시 경계·
블록·스냅숏 반환, 살아 있는 동안 채택된 잎은 남음, 경계 6 개·스냅숏 2 개인 표시 프롬프트가 첫 스텝의 마크 하나만 프로덕션에서
받고 나머지는 자기 것에서 받음, 파킹한 행에 표시가 남지 않음), 서버(같은 행의 다음 보통 턴은 경계를 유지, 답이 넘은 경계와 유휴 반납, 접두사 티어에 쓰지 않음,
프로덕션 경계를 채택한 표시 턴은 자기 것만 지움), LocalTP 네 랭크가 같은 경계를 지움, 소스 핀. HEAD 엔진에서는 맥에서 도는 12 개가
전부 실패하고, 변경을 하나씩 되돌린 변이 열 개가 전부 하나 이상에서 실패한다. `tests/test_probe_live_cache.py`: 판정(`live`·`reset` 웜, `boot` 콜드)과
프로브 소스. 맥: `test_fleet*`·`test_onepass*`·`test_st_*` 를 main 사본과 대조해 새 실패 없음(`test_docs_status` 는 main 의
`docs/GLM53_DRAFT_ACCEPTANCE.md` 배너 누락). srv4 CPU: 새 테스트·`test_engine_turn_retention`·`test_engine_prefix` 61 개 ok.
**GPU·플릿 실측 없음.** 남은 것: 이미 티어에 스필된 옛 프로브 경계는 남아 있다(솔트가 고유해 아무도 복원하지 않고, 티어 LRU 로 밀려난다).

### 885차 — 부팅 메모리 입장: 파일 캐시를 컨테이너 직전에 호스트에서 반납한다 (2026-09-13, srv2 로그 읽기 + 맥·srv4 CPU, PR #885, 운영자 "캐시 회수나 메모리 관리나 메모리 최적화가 제일 중요")

**기록.** 이날 프로덕션은 13:00 이후 정상 부팅하지 못했다. 랭크 로그가 남은 프로덕션 부팅 아홉 번(19:29~19:48)은 전부 아레나
입장에서 멈췄고, 같은 날 브래킷 부팅의 사망 기록 12 건 중 8 건도 메모리 입장이었다. 원인은 둘이다.
- **srv2 랭크 0(여섯 번): `[Errno 12] Cannot allocate memory`.** srv2 는 `overcommit_memory=2`·`overcommit_ratio=50` 이라 CommitLimit 가
  75.8 GiB 다. 입장의 캐시 회수(`touch_pages`)는 아레나 55.47 + 헤드룸 21.00 = 76.47 GiB 를 익명 매핑 하나로 잡으므로, 캐시가 아무리
  깨끗해도 매핑 자체가 거절된다. 실패 직후 free: 여유 34 GiB, 버퍼/캐시 75 GiB.
- **srv4 랭크 3(세 번): SIGTERM 선.** MemFree 71.02·68.74 GiB, MemAvailable 82.94·83.02 GiB 에서 76.47 GiB 를 폴트하면 SIGTERM 선 +
  여유(8 GiB) 아래로 내려가 거절됐다. srv4 에는 다른 서비스가 익명 메모리 약 27 GiB 를 쥐고 있다(21:57 기준).
- 캐시는 엔진 것이 아니다. 로더와 두 티어는 O_DIRECT 이고, 컨테이너에는 ST 체크포인트와 드래프터만 마운트되어
  `release_model_cache` 의 fadvise 도 다른 작업의 캐시에 닿지 못한다.
- 노드 설정은 제각각이다. srv1 은 `overcommit_memory=2`·ratio 95(CommitLimit 131.6 GiB), srv3·srv4 는 0, `min_free_kbytes` 는 srv4 만
  45 MB(나머지 4 GiB). 네 노드 모두 `sudo -n` 이 된다.
- 같은 날 srv2 에서 뜬 부팅들의 메모리 원장(`memory-rank0.json` 열네 개): 작업 공간 최고치 5.07~7.48 GiB(상한 12 GiB, 실패한 실험
  하나는 10.56), 준비 중 최소 즉시 가용 9.35~30.89 GiB.
- PR #866 의 `ST_RECLAIM_FILE_CACHE=1` 은 기본값이 꺼져 있었고, 켜도 vLLM 용 GMU 계산(`memfree-preflight.sh`)을 거쳐 rsync·이미지
  빌드 전에 한 번 돌았다.

**한 것.**
- `launchers/st-return-file-cache.sh`: 노드에서 `sync`(60 s 한도) 뒤 `sudo -n` 으로 `drop_caches` 3. 전후의 MemFree·MemAvailable·Cached 를
  한 줄로 말하고, 못 하면 exit 3.
- `launchers/start-st-glm53.sh`: 노드마다 rsync·이미지 빌드 뒤, 옛 컨테이너를 지운 다음 `docker run` 직전에 그 스크립트를 stdin 으로
  돌린다. **기본 켜짐**(`ST_RECLAIM_FILE_CACHE=0` 이면 끔). 리스를 쥐고 노드가 비어 있음을 확인한 뒤에만 닿는다. 반납하지 못한 노드는
  그렇다고 말하고 그대로 띄운다 — 판정은 엔진의 입장이 한다. #866 의 사전 단계는 뺐다. 감독자의 프로덕션 부팅과 브래킷 부팅이 모두
  이 런처를 쓴다.
- 엔진: `prepare_allocation` 보고에 입장이 시작될 때의 파일 캐시(`file_cache`), 두 거절 문구에 그 양, ENOMEM 문구에 Cached 와 이
  스크립트. 부팅 게이지 `boot_file_cache_GiB`·`boot_available_GiB` 로 호스트의 반납이 랭크에 닿았는지 다음 부팅이 말한다.

**검증.** `tests/test_engine_fleet_ops.py` 의 가짜 플릿이 실제 런처를 `docker run` 까지 돌린다: 네 노드 모두 제거 → 반납 → 시작,
sudo 거절이면 네 노드가 경고 후 계속, 0 이면 반납 없음, 남의 컨테이너나 잘못된 값이면 아무것도 안 함, 반납이 트리·이미지 뒤라는 소스
순서, 스크립트 단독(성공·exit 3·meminfo 없음). 변이 네 개(기본값 0, `docker run` 뒤로 옮김, 실패 시 중단, sudo 무시)가 모두 잡힌다.
입장 테스트 둘(보고·거절 문구의 파일 캐시, 게이지 핀). 맥: 런처·메모리 관련 14 모듈과 fleet 스위트를 main 사본과 대조해 새 실패
없음. srv4 CPU: 리눅스에서만 도는 입장·감독자 테스트를 포함한 9 모듈 187 테스트 ok. **GPU·플릿 부팅 실측 없음** — 재양자화 세션이
플릿을 쥐고 있다.

**남은 것.** (1) 프로덕션은 배포된 릴리스의 런처를 쓰므로 배포가 필요한데, 배포 감시는 문이 답하지 않으면 배포하지 않는다(프로덕션이
죽어 있으면 교착). (2) srv4 는 다른 서비스 몫 때문에 캐시를 다 비워도 여유가 몇 GiB 뿐이다. (3) 작업 공간 상한 12 GiB 는 실측 최고치
7.48 GiB 보다 4.5 GiB 크다 — 낮추면 입장 필요량이 그만큼 준다(부팅으로 검증할 일). (4) srv2 의 `overcommit_ratio=50` 은 설정 문제라
운영자 결정이다.

### 891차 — 작업 공간 상한 12 → 9 GiB: 네 랭크 실측 최고치 7.48 GiB 위로 1.52 GiB, 입장 필요량이 노드마다 3 GiB 준다 (2026-09-13, srv1~4 원장 읽기 + 맥·srv4 CPU, PR #891, 운영자 "2~3번 작업")

**기록.**
- 상한(`budget.WORKSPACE_GIB`)은 #549 이후 12 GiB 였다. 입장은 아레나 + 상한 + OS 여유 + 접두사 티어 호스트 캐시를 즉시 가용으로
  요구하므로, 상한 중 어느 단계도 쓰지 않는 부분까지 부팅을 거절할 이유가 된다. 같은 날 srv4 는 약 1.5 GiB 모자라 프로덕션을 거절했다(PR #885).
- 2026-09-13 준비 완료 부팅 아홉 번이 네 랭크 모두에 남긴 원장 36 개: 예약 최고치 5.07~7.48 GiB, 할당 최고치 4.48~6.67 GiB. 32,256 토큰
  청크를 쓰는 트리의 최고치는 모두 가장 큰 프리필 청크(`prefill/32256/…/prepared`)에서 나고, 한 트리의 네 랭크는 0.04 GiB 안에서 같다.
- 넓은 디코드 배치를 데운 실험 부팅 둘(둘 다 실패)은 7.74·10.56 GiB 였다.
- srv2 의 `/etc/sysctl.d/99-overcommit.conf` 는 `vm.overcommit_memory=2` 만 켜고 비율은 기본값 50 이라 CommitLimit 가 75.8 GiB 다. srv1 은
  `99-deneb-vibevoice.conf` 에서 95 로 올려 두었다(2026-06-01, 이력 50 → 80 → 90 → 95).

**한 것.**
- `WORKSPACE_GIB = 9.0`: 예약 최고치보다 1.52 GiB, 할당 최고치보다 2.33 GiB 위. 할당자는 한도에 닿으면 캐시 블록을 먼저 돌려준다.
  프로덕션 형상의 입장 필요량이 76.47 → 73.47 GiB.
- `boot.py --workspace-gib`, 런처 `ST_WORKSPACE_GIB`(양수만): 더 쓰는 형상이 자기 원장과 함께 올린다. 세 부팅 모드가 모두 넘기고,
  예산표의 상한 줄·근거·"unspent" 줄이 실제 강제 상한을 쓴다.
- `st-return-file-cache.sh` 가 반납 줄에 노드의 `overcommit_memory`·CommitLimit·Committed_AS 를 붙인다. 엄격 모드 노드의 커밋 여유가
  부팅마다 로그에 남는다.
- srv2 의 비율은 시스템 설정이라 세션이 바꾸지 않았다. 운영자가 srv2 에서 실행할 명령:
  `echo 'vm.overcommit_ratio = 95' | sudo tee /etc/sysctl.d/99-overcommit-ratio.conf` 뒤 `sudo sysctl -p /etc/sysctl.d/99-overcommit-ratio.conf`.
  기대값: CommitLimit 약 129.7 GiB(스왑 16 GiB + 119.7 GiB × 0.95). 한도만 올리고 모드 2 와 `min_free_kbytes` 는 그대로다.

**검증.** 가짜 플릿: `ST_WORKSPACE_GIB` 가 없으면 부팅 명령에 플래그가 없고, 10.5 면 네 랭크 모두 `--workspace-gib 10.5`, `0`·`0.0`·음수·
문자·지수 표기면 exit 2 로 아무것도 띄우지 않는다; 반납 줄의 커밋 상태. 예산(srv4, GLM 설정 있음): 올린 상한이 줄·근거·KV 나머지에 반영.
부팅 경로: 인자가 없으면 `build` 가 None(프로필 값)을, 10.5 면 local·fleet 모두 10.5 를 받는다. 소스 핀: 상수와 근거, 입장이 플래그 값을
쓰고 상수를 직접 곱하는 경로가 남지 않음, 세 `build` 호출. srv4 CPU 8 모듈 187 테스트 ok. **GPU 부팅 없음** — 9 GiB 로 가장 큰 프리필
청크와 그래프 캡처를 통과하는지는 재양자화 뒤 첫 부팅의 원장이 말한다. 넘치면 감독자 환경에 `ST_WORKSPACE_GIB=12` 를 두면 코드 변경
없이 되돌아간다.

### 898차 — srv2 의 엄격 오버커밋이 부팅을 막지 못하게: 캐시 회수를 입장 순간 호스트가 한다 (2026-09-13, 맥·srv4 CPU, PR #898, 운영자 "3번도 어떻게 해결해")

**기록.** srv2 는 `/etc/sysctl.d/99-overcommit.conf` 가 `vm.overcommit_memory=2` 만 켜고 비율은 기본 50 이라 CommitLimit 가
75.8 GiB 다(PR #891 항목). 엔진의 캐시 회수는 필요량 전체를 익명 매핑 하나로 잡으므로, 상한을 9 GiB 로 내린 뒤에도 73.47 GiB 에
그 순간의 Committed_AS(이날 21시대 srv2 에서 4.5~15.5 GiB)를 더하면 한도를 넘는다. 비율을 올리는 것은 시스템 설정이라 세션이 바꾸지
않는다. PR #885 의 반납은 컨테이너 시작 직전 한 번뿐이라 부팅이 읽는 것이 다시 채우는 캐시는 남는다.

**한 것.**
- `launchers/st-reclaim-broker.sh`: 랭크마다 그 노드 호스트에 뜨는 짧은 브로커. `glm53-logs/st-reclaim/rank<r>` 의 파일로 요청을 받고
  (`heartbeat`·`request`·`done`), `st-return-file-cache.sh` 로 캐시를 버린 뒤 id·종료 코드·보고 한 줄로 답한다. 컨테이너가 떴다
  사라지면, 끝내 안 뜨면(300 s), 두 시간이 지나면 끝난다. `start` 는 같은 랭크의 옛 브로커와 남은 요청을 치운다.
- `start-st-glm53.sh`: 반납 뒤 `docker run` 직전에 브로커를 띄우고 컨테이너에 `ST_RECLAIM_DIR` 를 넘긴다. 못 띄우면 말하고 변수 없이
  띄운다. `stop` 은 컨테이너를 지운 뒤 브로커를 끝낸다. `ST_RECLAIM_FILE_CACHE=0` 이면 반납도 브로커도 없다.
- 엔진 `arena.host_reclaim`: 하트비트가 5 s 안이면 요청하고 자기 id 의 답을 150 s 까지 기다린다. 없으면 기다리지 않고 None.
  `prepare_allocation` 과 준비 단계의 `reclaim_preparation_pages` 가 모델 캐시 반납 뒤·익명 폴트 전에 묻고, 버려서 충분하면 매핑하지
  않는다. 거절 문구에 호스트의 답, 랭크 로그 한 줄, 게이지 `boot_host_reclaim`(0 안 물음·1 못 버림·2 버림).
- 안전장치 둘(PR #901 — #898 은 이 커밋 전에 머지됐다). 모자란 만큼이 회수 가능한 캐시로 있을 때만 묻는다(`MemAvailable ≥ 필요량`) — 메모리 자체가 모자란 노드에서 캐시를
  버려 봐야 소용없다. 한 랭크는 30 s 에 한 번만 묻는다 — 준비 단계 체크포인트는 수백 번이라, 빠듯한 노드가 체크포인트마다 상자의
  캐시를 버리면 부팅이 느려지고 같은 상자의 다른 서비스가 파일을 다시 읽는다.

**검증.** `tests/test_engine_host_reclaim.py`(18): 클라이언트(브로커 없음·멈춘 하트비트는 즉시 None 이고 요청을 남기지 않음, 답·거절·
다른 id 의 답은 시간 초과, 30 s 안의 두 번째 질문은 쓰지 않고 None), 입장(호스트로 충분하면 매핑 없음, 모자라면 예전 폴트, 거절
문구에 호스트의 답, 여유가 있으면 묻지 않음, 캐시로 못 메우면 묻지 않음), 준비 단계 회수(캐시로 못 메우면 묻지 않음), 실제 브로커 스크립트(sudo·sync·docker 스텁으로 요청에 답하고 컨테이너가 사라지면 끝남, 새 부팅이 옛 브로커를
대체하고 `stop-all` 이 끝냄, 이전 부팅의 요청은 처리하지 않음, 안 뜬 컨테이너는 브로커를 붙잡지 않음), 배선 핀. 가짜 플릿: 네 노드
모두 제거 → 반납 → 브로커 → 시작, 각 컨테이너에 자기 랭크 디렉터리, 브로커가 안 뜨면 경고 후 변수 없이 시작, `stop` 이 컨테이너 뒤
브로커를 끝냄. 변이 일곱 개(입장이 안 물음, 멈춘 하트비트에도 물음, 남의 답 수락, 준비 단계가 안 물음, 디렉터리 안 넘김, 브로커가
컨테이너보다 오래 삶, 남은 요청 유지)와 안전장치 변이 셋(입장·준비 단계가 캐시 없이도 물음, 간격 없음)이 모두 잡힌다. **GPU·플릿 실측 없음** — 첫 부팅의 랭크 로그와 `boot_host_reclaim` 이 말한다.
srv2 의 비율은 그대로이며, 올리는 명령은 PR #891 항목에 있다.

### C1 MoE FC1 스케일 복원 동기화·scatter 캐시 축소 (2026-09-14, CPU/compiler only)

`t,r,sf6`의 1–8행에서 FC1 압축 입력을 별도 공유 메모리에 두어 복원 전 장벽을 없앴다.
복원 후 게시 장벽과 파이프라인 슬롯 소유권은 유지한다. H4096에서 작업당 128-thread 장벽
32회를 줄이는 코드 변경이며, 시간 감소율은 아직 모른다. Scatter 캐시도 실제 M16에 맞춰
128→16행으로 줄여 작업당 32-bit 초기화 쓰기 224회를 없앴다. 추가 공유 메모리는 2048 B,
C1 네이티브 자원은 control과 같은 126 registers·stack/local 0이다.

8 CPU tests, 실제 CuTe/PTXAS/TVM-FFI 6개 형상, 반복 그래프용 복원 helper 2개 컴파일 통과.
GPU 수치·그래프 실행·품질·수용률·step/s는 미측정이고 큐 제출·엔진 부팅은 하지 않았다.
PR #875 업그레이드 오라클의 C1 2K/32K/128K 총 개선율도 새 MoE 계수가 없어 null이다.
기본 경로에 켜져 있지만 성능 승리 판정은 아니다. 소스 해시·자원·검증·재현 명령은
[상세 기록](measurements/st_moe_sf6_staging_20260914/README.md)에 있다.

### C1 SF6 복원을 4바이트씩 처리 (2026-09-14, CPU/compiler only)

PR #913의 별도 FC1 스케일 버퍼에서 바이트별 복원을 32-bit 정수 연산으로 합쳤다. 기준값의
하위 7비트를 더한 뒤 최상위 비트를 XOR하여 바이트 간 carry 없이 기존 modulo-256 값을
보존한다. 기존 버퍼·게시 장벽·파이프라인·FP32 KDA 상태·K=7은 유지하며 M1–8 기본 ON이다.

M8 실제 바이너리의 NOP 제외 정적 명령어가 3825→3741(-84)이다. 레지스터는 양쪽 126개,
stack/local 0, 공유 메모리는 추가되지 않았다. 이것은 실행 시간 2.20% 개선 주장이 아니다.
9 CPU tests와 네이티브 7형상, 반복 그래프 fixture 3개의 컴파일이 통과했다. GPU 실행·큐 제출·
엔진 부팅은 하지 않았고 품질·수용률·step/s는 미측정이다. 업그레이드 오라클도 새 MoE 계수가
없어 C1 2K/32K/128K 총 개선율을 null로 남긴다.
[소스 해시·명령어·자원·재현 기록](measurements/st_moe_sf6_word_20260914/README.md).

### 916차 — C1 MoE 중간 활성값을 64비트로 직접 저장 (2026-09-14, srv2 CPU, PR #916)

양자화 함수가 반환한 FP4 8바이트를 다시 쪼개지 않고 정렬된 64-bit shared store 한 번으로
저장한다. 실제 FC2 소비자 주소·8-byte 정렬·연속성을 컴파일 시 모든 바이트에 대해 검증한다.
양자화·스케일·BF16 반올림·게시 장벽은 유지하고, M1–8 decode reform에 기본 ON이다.

같은 소스의 M8 control/candidate에서 NOP 제외 정적 명령어는 3741→3708(-33),
바이트 저장 8개는 64-bit 저장 1개로 바뀐다. 레지스터 126·stack/local 0·staged shared
100352 B는 동일하다. 12개 CPU 테스트, 실제 전체 커널 6형상과 반복 그래프 helper 2개의
컴파일이 통과했다. GPU 큐 제출·엔진 부팅은 하지 않았으며, 품질·수용률·step/s는 미측정이다.
업그레이드 오라클도 paired MoE 계수가 없어 총 개선율을 null로 남긴다.
[소스 해시·명령어·자원·재현 기록](measurements/st_moe_activation_store_20260914/README.md).

### 918차 — C1 FC2 SF6 스케일도 4바이트씩 복원 (2026-09-14, srv2 CPU, PR #918)

FC2의 byte별 복원을 FC1과 같은 32-bit 정수 복원으로 합쳤다. 같은 공유 메모리를 덮어쓰므로
volatile 읽기·복원 전 장벽·복원 후 게시 장벽·파이프라인 슬롯 수명은 모두 유지한다.
M1–8 SF6 decode reform에 기본 ON이며, 더 큰 행 수와 기존 q 경로는 유지한다.

같은 소스의 M8 scalar-FC2/word-FC2에서 NOP 제외 정적 명령어는 3708→3540(-168)이다.
변한 opcode 수는 정수 복원 연산뿐이다. FP·MMA·shared load/store·장벽 명령 수, 레지스터
126·stack/local 0·staged shared 100352 B는 동일하다. 14개 CPU 테스트, 전체 커널 7형상과
그래프 helper 4개의 네이티브 컴파일이 통과했다. GPU 큐 제출·엔진 부팅은 하지 않았으며
품질·수용률·step/s는 미측정이다. 오라클 총 개선율도 paired MoE 계수가 없어 null이다.
[소스 해시·명령어·자원·재현 기록](measurements/st_moe_fc2_words_20260914/README.md).

### 920차 — C1 FC1 gate/up 입력·스케일 재사용 (2026-09-14, srv2 CPU, PR #920)

gate가 읽은 K256 입력·스케일 레지스터를 up도 쓰고, up의 중복 A/SFA DMA를 없앴다.
기존 B/SFB 스테이지 링을 유지하며 gate 리더만 전송 전에 4096 기대 바이트를 더한다.
실제 레지스터 레이아웃에서 네 K64 블록이 겹치지 않음을 컴파일 시 검증한다. 별도 A 링을
추가하지 않으며 M1–8 SF6 decode reform에 기본 ON이다.

H4096 작업당 A/SFA 전송 32회·65536 B가 줄고 공유 메모리→레지스터 입력 로드도 절반이다.
같은 소스 M8 control/candidate는 레지스터 126→121, NOP 제외 정적 명령어 3540→3485,
stack/local 0·staged shared 100352 B다. 이는 성능 개선율 주장이 아니다. 실제 생산자·소비자
루프, 전송량, 리더 한 명의 expect_tx, 해제 버퍼 poison·반복 재사용 등을 포함한 18 CPU
테스트와 전체 커널 6형상의 네이티브 컴파일이 통과했다. 실물 GPU 비교 레인은 준비했으나
큐 제출·엔진 부팅·GPU 실행은 하지 않았다. 품질·수용률·step/s는 미측정이고 오라클의 총
개선율도 paired MoE 계수가 없어 null이다.
[소스 해시·데이터 흐름·명령어·자원·재현 기록](measurements/st_moe_fc1_reuse_20260914/README.md).

### 923차 — C1 입력 버퍼 축소로 FC2 복원 장벽 제거 (2026-09-14, srv2 CPU, PR #923)

PR #920 이후 up이 쓰지 않는 A/SFA 공유 슬롯을 회수했다. 짝수 FC1 링의 gate 슬롯만
`stage // 2`에 대응시키며, 기존 gate 슬롯을 네 MMA 워프가 해제해야 같은 입력 버퍼를
다시 쓸 수 있다. up은 유지된 레지스터를 읽는다. 실제 컴파일에서 스테이지 내부 소비자
레이아웃이 같고 전체 입력 저장 공간이 절반임을 검증한다.

회수한 공간 일부에 FC2 packed ring을 분리하여 복원 전 read-before-write 장벽을 없애고
게시 장벽은 유지했다. H4096/N256 작업당 네 워프 장벽 16회가 줄며 기존 DMA 전송량과
B/SFB 파이프라인 깊이는 같다. A/SFA 8192→4096 B, FC2 packed +3104 B, 정렬 포함 staged
shared 100352→99328 B다. 같은 소스 M8 control/candidate의 NOP 제외 정적 명령어는
3485→3474, 정적 named barrier는 32→28, 레지스터 121·stack/local 0은 동일하다.
M1–8 SF6의 입력 재사용·별도 FC1 스케일·짝수 FC1 링에서 기본 ON이고 홀수 링은 유지한다.

실제 생산자·소비자 블록, 네 워프가 엇갈리는 입력 수명, 큐에 찬 FC2 스테이지와 인접 버퍼
보존, 모든 기준 바이트·코드를 포함한 22 CPU 테스트 및 전체 네이티브 7형상이 통과했다.
이전 FC1 재사용 프로브는 이번 축을 양쪽 OFF로 고정해 비교가 섞이지 않게 했다. GPU 큐
제출·엔진 부팅·실행은 없었으며 품질·수용률·step/s는 미측정이다. 업그레이드 오라클도
paired MoE 계수가 없어 총 개선율을 null로 남긴다. 위 수치는 속도 개선율이 아니다.
[소스 해시·버퍼 수명·명령어·자원·재현 기록](measurements/st_moe_compact_staging_20260914/README.md).

### 924차 — Mojo host commit CPU 실험, 서빙 전환 보류 (2026-09-14, 로컬 Apple M5 CPU, PR #924)

Mojo 1.0.0으로 bounded decode 결과의 호스트 반영을 컴파일하고 실제 Python 호출 경계를
포함해 비교했다. Python 기준은 현재 `BurstDecode._apply_outcome` 소스에서 직접 추출했다.
합성 readback 4096건·준비 2쌍·기록 12쌍, 실행 순서 교대, 쌍마다 최종 상태 동일이다.
컴파일·import·준비·상태 해시는 시간에서 제외하고 공통 랭크 합의 호출은 no-op으로 둔다.

32K 초기 이력에서 C1 **1.030→1.519 µs**, C4 **2.596→3.742 µs**;
128K에서는 C1 **1.021→1.591 µs**, C4 **2.628→4.289 µs**다.
이 CPU 호스트 함수의 Mojo 지연이 **44~63% 증가**하여 서빙 전환은 보류한다.
성공/오류·행 해제·EOS·랭크 합의 선행을 비교하는 CPU 테스트 10개를 통과했다.

이 수치는 Mac의 호스트 마이크로벤치이며 GB10의 문맥별 추론 속도가 아니다. GPU 큐·
부팅·서빙 연결은 없고 tok/s·TTFT·수용률·답변 품질은 미측정이다. SDK는 선택적인
실험/CPU CI 환경에만 설치한다. [재현 방법과 판정](bench/mojo_host/README.md),
[버전·소스/바이너리 해시·쌍별 표본](measurements/mojo_host_20260914/macos_arm64.json).

### ST decode V·logits 복사 제거 — CPU/컴파일 검증 (2026-09-14)

드래프터 어텐션이 QKV 안의 V를 원래 stride로 읽고, target FP8 head가 문맥 버킷들이
공유하는 패딩 포함 출력 버퍼에 직접 쓴다. 논리 어휘 view의 주소·객체 공유를 유지하며
패딩 64열은 토큰 선택에서 제외한다. 둘 다 기본 경로에 적용한다.
C1 K7의 일반 greedy target+proposal step에서 랭크당 복사 6회·640,000 B를 제거하는
소스 작업량이다. 확률/상세 샘플링은 통신을 위한 논리 어휘 packing이 여전히 필요하다.

CPU 37개 중 24통과·GPU 전용 13skip, 실제 Triton CPU 인터프리터 비교 15개 exact,
SM121 네이티브 컴파일 6개 통과다. GPU 큐·부팅·실행은 없으며 step/s·수용률·품질과
그래프 풀의 실제 메모리 감소는 미측정이다.
[소스 해시·정확한 범위·재현 기록](measurements/st_decode_buffers_20260914/README.md).

### ST draft 입력·난수 준비 통합 — CPU/컴파일 검증 (2026-09-14)

anchor/mask 토큰·위치를 한 커널로 만들고, keyed SplitMix64 step draws도 기존 키·목적·
FP64→FP32 반올림을 유지한 한 커널로 통합했다. sampled walk가 모두 덮어쓰는 확률
버퍼의 선행 zero-fill도 제거했다. 셋 모두 CUDA 기본 경로에 적용한다.

C1/C4 K7의 CPU 레퍼런스에서 결과 저장소를 만드는 텐서 연산은 입력 준비 4개,
난수 블록 53개였으며 후보는 각 1개 CUDA 커널이다. GPU 호출 수나 속도 실측은 아니다.
CPU 48개 중 39통과·GPU 9skip, 실제 커널 인터프리터 43개 exact, SM121 컴파일
11개가 통과했다. GPU 큐·부팅·실행은 없고 실제 step/s·수용률·품질은 미측정이다.
[소스 해시·비트 일치·경로별 작업량·재현 기록](measurements/st_decode_inputs_20260914/README.md).

### ST rank-local 임베딩 조회 통합 — CPU/컴파일 검증 (2026-09-14)

target·drafter가 공유하는 임베딩 경로의 토큰 보정·소유 랭크 판정·조회·0 마스킹을
한 CUDA 커널로 합쳤다. BF16 값을 uint16 비트로 옮기고, 기존 TP 합산은 유지한다.
greedy와 target prefill에도 적용되며 CUDA 기본 경로에 켠다.

기존 레퍼런스는 결과 저장소를 만드는 텐서 연산 7개, 후보는 커널 1개다. 이는 CPU
dispatch 작업량이며 GPU 속도 실측이 아니다. CPU 67개 중 32통과·GPU 35skip,
실제 커널 인터프리터 28개 비트 일치, SM121 컴파일 8개 통과(shared 0 B)다.
GPU 큐·부팅·실행은 없고 실제 step/s·수용률·품질·그래프 풀 메모리는 미측정이다.
[소스 해시·랭크 경계·비트 검증·재현 기록](measurements/st_token_embedding_20260914/README.md).

### ST C1 forward CTA 내 부분합 — 기본 적용 (2026-09-14)

KDA 출력 34개, dense MLP 출력 3개, DSA query 22개의 W4 GEMM에서 기존 세 K 분할을
한 CTA 안에서 합치도록 확장했다. 기존 분할·MMA·FP32 합산·BF16 반올림 순서를 유지한다.
전역 부분합 쓰기/읽기 44.25 MiB와 arrival atomic 5,664회를 제거하는 소스 작업량이며
실제 지연 감소 수치가 아니다. C1 query pack은 50,688→12,672 B다.

CPU 30개 중 21통과·GPU 9skip, 실제 production-flag native 컴파일 5개 새 specialization
통과(76/78 registers, stack/local spill 0). srv4 실제 rank3 가중치의 동일 RTN pack으로
기존 dsa_inputs 포함 10개 GPU 구성요소 검사를 92.8초에 통과했다(모델 부팅 없음).
새 경로 8개 exact/replay 그룹이 bit-exact이며, warm/evicted B/A/A/B에서 KDA 출력
−11.58/−6.37%, MLP 출력 −4.04/−3.57%, query pair −7.65/−5.51%였다.
이는 구성요소 시간이며 현재 24 step/s·수용률 개선 실측은 없다.
[구현·검증·재현 근거](measurements/st_forward_cta_20260914/README.md).

### 945차 — ST 오라클 토큰·C4 계산과 별도 기록 검증 보완 (2026-09-14, 로컬 CPU, PR #945)

K=0·수용률=0을 그대로 유지하고, 첫 토큰에서 종료한 요청의 추가 디코드와 생성 한도를
넘긴 토큰 집계를 없앴다. 실제 요청 토큰 수·C4 동시 파동·보정 채널·기준 폭을 보존한다.
phase step/s는 실행한 폭과 문맥의 모형 시간 합을 쓰고, warm TTFT는 양쪽에서 같은
요청 집합을 비교한다. CPU 시뮬레이터의 메모리 조회만 꺼 첫 요청의 torch import
오염을 없앴으며 서빙 계측 기본값은 유지한다.

이전 Git 소스를 읽은 정확한 산술 반례에서 생성 길이 2·10의 디코드 토큰 합은
24→10(정답 10), 혼합 폭 phase step/s는 500→89.66(정답 89.655…)이었다.
과거 C4 자료는 누락 대신 3파동·12요청을 실제 프롬프트 길이로 읽는다. 기존 도구 58개와
새 정확도 계약 19개가 CPU에서 통과했고 두 모듈을 Linux 엔진 CI에 연결했다.

`--fit-from`으로 비용을 고정하고 별도 자료의 오차·입력 해시·런타임 차이·제외 이유를
JSON에 남긴다. onepass-a→다른 빌드 onepass-h의 2K 기록은 비교 7행의 절대 상대오차
중앙값 약 175%, 최대 약 642%로, 소스·이미지·노브가 다른 계수 이관의 한계도 보관했다.
동일 자료 재구성과 별도 기록 검증을 구분하며 기존 confidence는 정확도 보장이 아니다.
GPU·큐·부팅·배포·실시간 onepass 없이 수행했고 실측 속도·품질 개선 주장은 없다.
[사용법](bench/ORACLE_ACCURACY.md), [재현·해시·비교 결과](measurements/st_oracle_accuracy_20260914/README.md).


### ST C1 K-block 병렬 곱·DSA query grid 통합 — 기본 적용 (2026-09-14, PR #946)

기존 async W4 reader로 독립 K-block MMA 곱을 8워프에 나누고, epilogue가 원래
slice별 FMA·합산·BF16 반올림 순서를 재현한다. C1 DSA query 두 개도 기존 reader와
3분할 합산을 유지한 한 grid로 묶었다. 최종 실행 소스는 `ec5c6718`이며 기본 경로에 켠다.

srv4 GB10, 같은 빌드·실가중치 RTN W4 pack·captured B/A/A/B warm 비교에서
MLP gate/up **76.73→30.27 µs(-60.5%)**, query pair **24.33→17.96 µs(-26.2%)**다.
실제 출력 주소에 직접 쓰는 MLA/KDA/MLP 출력은 각각 **-7.6/-10.5/-10.1%**였다.
수치·재생 16개 그룹이 zero-tolerance로 통과했고, production-flag compile과 engine CI도
통과했다. Evicted 결과는 변동과 퇴행이 있어 판정 보류다. 위 수치는 구성요소 warm 시간이며
전체 디코드·수용률 개선이나 24 step/s 달성을 뜻하지 않는다.

K7·2K/32K/128K·C1 두 번/C4 한 번의 단일 후보 원패스 예약을 `ec5c6718`로 교체했다.
기존 enqueue 시각을 보존한 `st-forward-pipeline-onepass0914`가 대기 중이다.
운영자가 warm 개선판의 즉시 기본 머지를 지시했으며 소비자 tok/s·step/s·수용률은 미측정이다.
[원본 표본·거부한 register 구현·컴파일·GPU·큐 기록](measurements/st_forward_register_20260914/README.md).

### ST expert 비용 트리·FP32 KDA·packed persistent MLP — 실험 API (2026-09-14, 로컬 CPU, PR #947)

DFlash 후보 1회 생성 → expert 비용 기반 트리 선택 → FP32 KDA factor 검증 →
선택 경로 캐시·drafter 관찰까지 연결했다. 일반 dense는 기존 GPTQ `W4Pack`을 직접
읽는 **W4A8**이며 activation의 group-128 FP8·BF16 반올림 경계를 유지한다.
별도 ModelOpt 고정 1-expert dense만 원래 **W4A4 NVFP4**의 raw/tiled/SF6 경로를 쓴다.
가중치 재양자화·상주 BF16 복원·FP16 KDA 전환은 없다. Routed MoE는 기존 레인이다.

CPU **74 통과·6 스킵**, SM121 **17종 오프라인 컴파일**, 실제 packed reader/scaling
**10종 CPU 해석 대조**를 기록했다. FP8 변환에 대한 Triton 해석기의 한계도 명시했다.
16-node KDA의 factor 384 KiB + initial 1 MiB와 W4A8 MLP scratch 6,473,456 B는
할당식이며 실측 메모리·속도 개선이 아니다. greedy C=1 eager API로 남기며 기본 서빙에
켜지지 않는다. GPU·큐·부팅 없이 수행했고 실제 수용률·tok/s·품질은 미측정이다.
[재현·해시·제약·검증 기록](measurements/st_tree_dataflow_20260914/README.md).


### 작업 공간 상한 9 → 12 GiB: 서빙 문맥 끝의 가장 큰 프리필이 9.67 GiB 를 쓴다 (2026-09-14, srv2 플릿 부팅 3회, 운영자 "12")

**기록.**
- main `87304780`(#946)과 `68f7c1d6`(#944)이 네 랭크 모두 부팅 중에 같은 수치로 죽었다. `_warmup_prefill_memory` 의
  `prefill/32256/943872`(가장 큰 청크를 서빙 문맥 끝에서)에서 `torch.isfinite(aux)` 가 630 MiB 를 더 받지 못했다:
  "64.52 GiB allowed", PyTorch 할당 63.87 GiB. 그 순간 노드에는 21.37~34.37 GiB 가 비어 있었다(가장 적은 쪽이 랭크 3 srv4).
- 9 GiB 에서 준비 완료한 마지막 부팅(`2ed1f047` 기반, 같은 날 15:40)의 같은 행 최고치는 예약 62.28 GiB, 작업 공간 6.75 GiB 다.
  0 문맥 32,256 청크까지의 행은 두 부팅이 0.01 GiB 안에서 같고, 늘어난 것은 944K 문맥 행뿐이다.
- 그 사이 엔진 변경은 #943(MoE 정적 커널 저장소), #945(계측 플래그), #939(C1 디코드 GEMM), #944(one-shot 두 레일)다.
  #946 은 무관하다(`68f7c1d6` 이 같은 수치로 죽었다). 넷 중 무엇이 늘렸는지는 가르지 않았다.
- 같은 `87304780` 을 `ST_WORKSPACE_GIB=12` 로 띄우면 준비 완료. 랭크 0 원장: 프리필 최고치 9.67 GiB(944K 행 할당 64.49 GiB,
  예약 65.20 GiB), 준비 뒤 남는 작업 공간 2.15 GiB(9 GiB 부팅은 2.12), OOM 여유 최소 23.02 GiB.

**한 것.**
- `WORKSPACE_GIB = 12.0`: 최고치보다 2.33 GiB 위(#891 의 9 GiB 는 당시 최고치 7.48 GiB 보다 1.52 GiB 위였다). 입장 필요량은 노드마다
  3 GiB 늘어 #891 이전으로 돌아간다. srv4 가 그만큼 빠듯해진다.
- 근거 문장, README 두 곳, 예산 테스트의 핀. 상한을 올린 부팅을 확인하는 테스트의 예시는 10.5 → 13.5 GiB 로 바꿨다(프로필보다 커야
  "올린" 경우다).

**검증.** 맥 CPU `tests.test_engine_budget` 13개 중 토치·GLM 설정이 필요 없는 2개 통과(11개는 그 조건으로 건너뜀). 이 브랜치로는
GPU 부팅을 하지 않았다: 같은 값을 환경 변수로 준 위의 부팅이 근거다. #895(프리필 FP8 패킷 기본값)와 #947 은 이 측정 뒤에 머지됐으므로
그 트리의 최고치는 다음 부팅의 원장이 말한다.


### GLM-5.3 사고 블록 서두를 "We need to parse the problem. We have" 로 (2026-09-14, srv2 플릿, 운영자 "위 해브로 결정")

**기록.** Red Hat 랭크, 운영 env, 그리디. onepass JSON 30문항을 C=4 로 돌리고 `bench/onepass_quality.assess` 로 채점했다(닫는 괄호만 빠진 답은
괄호를 채워 채점, #931). 서두는 요청별 `reasoning_opener` kwarg 로 주었다(#942 의 도어 경로 그대로).

| 서두 | 점수 | 최종답 | 완전정답 | 평균 토큰 | 수락률 | 30문항 소요 |
|---|---|---|---|---|---|---|
| 없음 | 175/190 | 29 | 21 | 4,390 | 38.0% | 1,163 s |
| "Let me parse the problem." (#942 기본값) | 164 | 25 | 18 | 1,954 | 54.2% | 422 s |
| "We need to parse the problem." | 170 | 27 | 18 | 2,601 | 51.8% | 596 s |
| **"We need to parse the problem. We have"** | **175** | **28** | **19** | **2,442** | **52.0%** | **538 s** |
| 같은 서두, main `87304780` 재측정 | 172 | 27 | 21 | 2,621 | 51.0% | 565 s |

- 서두 없음과 같은 점수를 그 토큰의 56% 로 낸다. "Let me" 는 더 짧지만 검산을 덜 해 점수가 11점 낮다.
- 같은 서두의 두 번은 30개 중 14개만 글자가 같았고 점수가 3점 벌어졌다. 한 번의 C=4 는 그만큼 흔들린다.
- 다른 형태의 새 문항 62개(날짜, 경로, 배낭, 표, 좌석, 단위, 한국어 퍼즐)에서 61개가 맞았다.
- 다른 서두 여섯(carefully, facts, each-case, first, step, given)과 "We need to parse the problem." + 최대 사고 수준은 같은 문항에서
  이 서두보다 낫지 않아 30문항을 다 돌기 전에 멈췄다(6~24문항).
- main `87304780` 에서 이 서두의 C=1 그리디 12문항: 19.32 step/s, 88.5 tok/s, 수락률 51.1%(서두 없음 70.1 tok/s·37.5%, "Let me" 91.2 tok/s·54.2%,
  둘 다 이전 main). 수락률이 "Let me" 보다 낮은 몫은 대부분 검산을 길게 한 원장 문항 둘에서 나온다(그 둘을 빼면 52.5% 대 53.2%).
- 같은 부팅의 C=4 합산 139.2 tok/s, 요청당 중앙값 36.8 tok/s. 프리필 첫 토큰(최소) 2K 1.08 s, 32K 9.83 s, 128K 38.15 s.

**한 것.** `boot.REASONING_OPENER` 를 이 문장으로 바꾸고 근거를 그 옆에 적었다. 템플릿 테스트의 예시 서두도 같은 문장으로 바꿨다.
도어와 템플릿 경로는 #942 그대로다.

**검증.** 위 측정은 모두 요청별 kwarg 로 같은 템플릿 경로를 탔다(부팅마다 `/tokenize` 로 렌더 끝을 확인). CPU 테스트 컨테이너에서
`tests.test_glm53_chat` 21개, `tests.test_engine_serve` 199개(18개 건너뜀) 통과. 이 브랜치 자체로 GPU 부팅은 하지 않았다.

### ST CUDA 13.2.1 전체 런타임 이전 및 B12X 세부 이식 (2026-09-14)

운영자 “13.2 전체 마이그레이션해”, “전부 가져와”, **“큐 태우지마”**.
Torch 2.13.0+cu132, CUDA SDK/런타임 13.2.1, NVCC/PTXAS/NVRTC/NVVM/nvJitLink 13.2.78을
34개 wheel SHA256으로 고정했다. 네 노드에서 같은 seed ID와 engine source SHA256을 확인하고
기본 `st-engine:glm53` 이미지 태그를 이전했다. 호스트 드라이버/툴킷, 실행 중인 서비스와 GPU 임대는 유지했다.

SF6는 u16/u8 공유 메모리 직접 읽기와 immediate offset, MLA는 native E4M3x2 → BF16x2 변환을 적용했다.
같은 NVCC 13.2.78의 정적 명령 위치는 MLA decode 1,360 → 1,264, prefill32 1,112 → 936이다.
이 수치는 **step/s나 tok/s가 아니다**. 큐·onepass·GPU 수치/그래프 검사는 하지 않았다.

Linux CPU 203 files / 1,872 tests: 203 ok, 0 failed, 0 cannot run, 314 skipped.
실제 native 확장 7종 compile/dlopen, SF6 전체 커널 7종/helper 4종/CPU operand 24,576개,
Triton KDA 55종, TileLang MHC 컴파일 통과. #947 실험용 트리 양자화 경로는 고정된 Triton 3.7.1에서
컴파일 제약이 있어 실패를 따로 기록했다. 기존 cu130 이미지에서도 NVFP4 실패가 재현된다.
CuTe가 요구하는 nvdisasm 13.3.73 진단 도구 및 기존 패키지 메타데이터 제약도 숨기지 않았다.

버전·이미지·원시 결과는 [런타임 이전 기록](measurements/st_cuda132_20260914/README.md),
기준 대비 명령/레지스터와 적용 범위는 [세부 이식 기록](measurements/st_upstream_microopts_20260914/README.md)에 있다.


### MLA 의 E4M3 → BF16 확장을 반 정밀도 다리로 되돌림: #952 뒤 128K 프리필 51~55 s, 되돌리면 37~39 s (2026-09-14, srv2 플릿 부팅 4회)

**기록.** Red Hat 랭크, 운영 env, 서두 기본값. 프리필은 캐시 없이 첫 토큰까지(2K ×3, 32K ×2, 128K ×2 = 129,784 토큰),
C=1 은 onepass JSON 12문항 그리디다. "뒤" 는 같은 부팅에서 C=1 을 돌린 다음 한 번 더 잰 값이다.

| 트리 | CUDA | 128K | 128K 뒤 | 32K | 32K 뒤 | C=1 step/s | 느린 문항 |
|---|---|---|---|---|---|---|---|
| `87304780` (#946) | 13.0 | 38.43 / 38.15 s | – | 10.31 / 9.83 s | – | 19.32 | 없음 |
| `b8bef130` (#949, #952 직전) | 13.0 | 39.11 / 37.46 s | 37.30 / 37.21 s | 10.26 / 9.76 s | 9.51 / 9.58 s | 19.26 | 없음 |
| `c58a8eb5` (#952) | 13.2.1 | **54.66 / 50.95 s** | – | 12.21 / 9.56 s | – | 18.51 | 2개(14.68, 16.19) |
| `83140b14` (c58a8eb5 + 이 커널 파일만 #952 이전) | 13.2.1 | **38.51 / 37.30 s** | 36.89 / 37.17 s | 9.96 / 9.87 s | 9.50 / 9.55 s | 19.32 | 없음 |

- 2K 는 네 트리 모두 최솟값 1.08~1.11 s 다. 32K 최솟값도 움직이지 않았고, 느려진 것은 128K 뿐이다.
- #895(프리필 FP8 패킷)는 `b8bef130` 에 들어 있고 빠르다. #951 은 실험용 트리 모듈만 바꿔 기본 경로와 무관하다.
- 같은 CUDA 13.2.1 스택에서 `glm53_megakernel.cu` 한 파일만 #952 이전으로 되돌리자 128K 가 돌아왔다. 원인은 스택이 아니라
  #952 가 넣은 `cvt.rn.bf16x2.e4m3x2` 한 줄 확장이다. `SOURCES.json` 기록상 이 확장은 디코드와 strided 프리필에 쓰인다. 앞 문맥이 있는
  청크에서만 드러나는 것으로 보이지만(128K 는 32,256 청크 넷 중 뒤 셋이 풀을 읽는다), 그 기제는 재지 않았다.
- `c58a8eb5` C=1 에서 부팅 뒤 3·4번째로 느렸던 두 문항은 `83140b14` 에서 같은 순서로 돌려도 19.39·19.42 step/s 였다. 디코드 4스텝 묶음의
  평균도 `b8bef130` 0.194 s, `83140b14` 0.194 s, `c58a8eb5` 0.205 s 다. 문항당 한 번씩이라 디코드 쪽 원인은 확정하지 않는다.

**한 것.** `glm53_megakernel.cu` 의 세 확장 함수(`mla_e4m3x2`, `mla_e4m3x2_strided`, `mla_e4m3x2_value`)를 #952 이전의 반 정밀도 다리로
되돌리고, 확장에 딸린 CUDA 13.2 컴파일 가드를 뺐다. 다리는 13.2.1 에서 컴파일되고 돈다(`83140b14` 부팅). #954 가 같은 파일의 다른
곳을 바꿨으므로 파일이 아니라 그 부분만 되돌렸고, 그 자리에 근거 주석을 달았다. `SOURCES.json` 해시와 기록. CUDA 13.2.1 스택과
#952 의 나머지는 그대로다.

**검증.** 위 표의 플릿 부팅. 이 브랜치의 세 함수는 `83140b14` 와 같고, 주석과 #954 가 더해졌다. 이 브랜치 자체로 GPU 부팅은 하지 않았다.
맥 CPU `test_cuda_translation_unit_and_pinned_dynamic_helpers_match_provenance` 통과.

## 2026-09-14 — 트리 W4A8: 입력 공유, packed pair, SM121 FP8 컴파일 선택

명시적 트리 실험의 W4A8 실행기를 개선했다. H4096/I3072에서 입력 group128 양자화 24회 반복을
1회 발행으로 바꾸고, packed 한 바이트의 두 가중치를 함께 읽어 스케일/LUT 결정을 공유한다.
Triton 3.7.1은 native FP8 판정에서 SM121을 빠뜨려 이 실험의 dot을 FP16 MMA로 바꾸고 있었다.
이 두 GEMM과 큐 수치 오라클만 consumer Blackwell lowering을 지정한다. 출력 PTX/cubin은 SM121a이며
FP32 누적 정책과 BF16 경계, GPTQ 가중치, 일반 서빙 dense·W4A4 MoE·KDA FP32는 유지한다.

8행 오프라인 컴파일: 배포 frontend인 Triton 3.7.1에서 FC1/FC2 공유메모리 각각 32,768→4,096B,
레지스터 255→175 / 173→128. Triton 3.8.0 비교에서도 감소했다. 각 컴파일러의 후보 36개 형상 모두
스필 0, FP8 MMA와 SM121a 타깃 확인. CPU 1,888 tests, 실패/실행불가 0, skip 374; 별도 interpreter
10 tests 통과(65,536 packed-byte/scale 조합, 양자화 경계, 버퍼 소유권, 기존 target 수치 오라클).

대가: 런치 2→3, 16행 작업공간 181,760→249,344B(+67,584B). **GPU 큐·부팅·실측은 하지 않았다.**
컴파일 자원 감소는 serving tok/s 증명이 아니다. 추가 런치 비용·실가중치 품질·수용률·그래프 재생은 미판정이다.
기록과 재현: `measurements/st_w4a8_shared_input_20260914/`.

## 2026-09-14 — CUDA 13.2 SF6·W4A8 바이트 연산 및 cuBLAS 튜닝 검토

SF6 word 복원의 split-base 덧셈과 dense/MLA W4A8 LUT의 `__vadd4`를 native `add.u8x4`로 바꿨다.
바이트별 modulo-256 의미, 기존 FP8 값·스케일, 부동소수점 누적과 BF16 경계는 유지하며 기본 적용한다.
MLA의 #956 half bridge도 유지한다. CuTe 캐시 지문에 공통 device helper를 포함했다.

CUDA 13.2.1 이미지의 CPU-only 검증: 엔진 204개 파일, 1887 tests, 실패·실행불가 0, skip 318.
dense/MLA 전체 native compile·dlopen 통과. SF6 static 7개와 short Q0/long prefill 2개가 컴파일됐다.
실제 SASS의 `VIADD.U8x4`는 dense 440곳, MLA 144곳, static word 경로별 24곳, dynamic 경로별 160곳이다.
기존 probe가 명령 이름의 소문자 `x`를 누락하던 것도 수정하고 동일 바이너리를 재집계했다.
**이 수치는 명령 생성 증거이며 속도 개선율이 아니다. GPU·큐·부팅·원패스는 실행하지 않았다.**

cuBLAS는 백엔드 추가 대신 튜닝 가능성을 검토했다. 큰 FP8 프리필과 이미 FP8인 draft FC/head가 우선 후보다.
기존 group128 scale을 MX32 UE8M0 배치로 반복해 동일 입력값을 표현하는 CPU oracle을 만들었고 통과했다.
양자화 생산자에 scale 배치를 합치면 별도 변환 발사를 피할 수 있다. W4를 FP8로 푸는 경우의 1.83배 payload와
기존 융합 후처리 비용도 따졌다. 기존 DeepGEMM에도 native FP8·스케일 재사용·Split-K가 있으므로
cuBLAS 우위는 실제 형상별 알고리즘·전체 입출력 비용에서 확인해야 한다. 개선율은 아직 미측정이다.
기록·수치 계약·재현·상세 검토: [st_cuda132_packed_20260914](measurements/st_cuda132_packed_20260914/README.md).

## 2026-09-14 — 트리 인덱서 접두사 중복 복사와 KDA 이력 준비 제거

#958 이후 명시적 eager 트리 실험을 더 줄였다. paged 인덱서 접두사와 private pool을 최종 버퍼에
한 번에 모아, 기존 gather 결과를 다시 concat하던 복사를 없앴다. private pool이 완성되는 트리에서
DSA층당 32K/128K 접두사 임시 버퍼 1.03125/4.125MiB, 추가 concat 읽기·쓰기 2.0625/8.25MiB가
사라진다. 형상으로 계산한 바이트 수이며 실측 대역폭이나 엔진 상주 메모리 절감량은 아니다.
KDA 합성곱은 canonical 링에서 바로 읽어 6,144채널의 최종 이력 버퍼 36,864B와 gather/mask 준비를
없앴다. indexer starts/ends/branch columns도 트랜잭션당 한 번만 만든다. KDA 상태는 FP32 유지.

Triton 3.7.1의 SM121a 오프라인 컴파일 13종 모두 스필 0. 바이트 복사 커널은 78 registers/0 shared,
링 합성곱은 이력 버퍼 모드보다 레지스터 1개가 늘어나는 대가가 있다. CPU interpreter 16 tests와
#958 동일 입력·연산자 비교에서 출력, feature, canonical state, paged bytes, 토큰·커밋 경로가 전부 같다.
Docker CPU 시간은 -15.63%~+16.10%로 흔들렸고, 맥 직접 비교는 -0.83%~+0.50%로 사실상 동일했다.

**GPU 큐·부팅·실측은 하지 않았다.** 일반 서빙 기본값이나 HTTP/전체 트리 그래프 연결을 바꾸는 PR이
아니다. 실제 GPU 수치·품질·수용률·tok/s는 미판정. 기록과 재현: `measurements/st_tree_bank_20260914/`.


### ST 디코드 스텝이 어디서 시간을 쓰나: main `de8bfff6` C=1 K=7 커널 분해 (2026-09-14, srv2 플릿, 운영자 "vLLM 때 디코딩 한계는 24 step 정도")

**기록.** 판정이 아니라 개선 후보를 고르기 위한 분해다.
- 트리와 조건: main `de8bfff6`(#956 뒤, CUDA 13.2.1), Red Hat 랭크, 운영 env, C=1 K=7(검증 8행), 약 70 토큰 프롬프트의 긴 그리디 생성.
- 측정 방법: 도어의 `POST /v1/engine/profile {"steps": 32}` 로 32 버스트씩 두 창을 잡았다. `/metrics` 로 반복 수와 벽시계를 구하고, 프로파일러 없는 옆 창과 대조했다.
- 벽시계: 프로파일러 하 54.0 ms/반복, 없으면 46.8~47.8 ms. 프로파일러 비용은 6~7 ms 다.
- 커널: 시간 합 54.89 ms(스트림이 겹쳐 벽시계를 넘는다), 호출 1,359 회/반복.

| 범주 | ms/반복 | 비중 | 호출/반복 |
|---|---|---|---|
| MoE 전문가 (`MoEStaticKernelV5`, 층당 1회) | 25.13 | 45.8% | 40 |
| dense GEMM (mk_gemm2 4.48, KDA 입력 CTA 3.58, input pack 1.79, cutlass bf16 1.49) | 13.65 | 24.9% | 356 |
| TP 통신 (publish 1.81, moe_packets 1.43, consumer 1.00) | 4.43 | 8.1% | 147 |
| mHC (`mk_mhc_packets` 80회 4.04) | 4.42 | 8.0% | 93 |
| LM head·드래프터 fc (deep_gemm fp8×fp4 ×3, 이름은 형상 추정) | 2.21 | 4.0% | 3 |
| MLA / DSA (torch `gatherTopK`+정렬 0.85 포함) | 1.57 | 2.9% | 168 |
| ST 글루 커널 · KDA · norm/elementwise | 1.41 · 1.25 · 0.83 | 6.4% | 206 · 98 · 249 |

- 같은 날 onepass(arm `32fb2892`, C=1 2K 프롬프트)의 장치 단계 합은 반복당 forward 46.6 ms, 드래프터 propose 3.05 ms,
  observe 0.66 ms, `gpu_iteration` 평균 50.5 ms 다. 짧은 프롬프트의 이번 창은 그보다 3 ms 쯤 빠르다.
- `STEP_KERNEL_MAP.md` 보충 분해 5·6(vLLM 포크, 같은 CUPTI 계열)과 대조:
  - 스텝 전체: 09-05 프로덕션은 프로파일러 하 55.8 ms/스텝, 커널 1,166 개였다.
  - MoE: 29.4~32.5 ms 에서 25.1 ms 로 가벼워졌다.
  - mHC: MK MHC 89회 2.03 ms(호출당 23 µs)에서 `mk_mhc_packets` 호출당 50 µs 로 무거워 보인다. 이 커널이 TP 패킷 소비를 포함하는지는 확인하지 않았다.
  - 그 트레이스들의 SPEC_K 는 확인하지 않았다(아카이브 09-07 onepass 는 SPEC_K=5 에서 디코드 중앙 21.4~21.9 step/s).
- 같은 부팅의 메모리 장부: 프리필 최고치 9.89 GiB(`prefill/32256/1016320`, 상한 12), 준비 뒤 1.99 GiB, OOM 여유 최소 23.16 GiB.

**못 한 것.** 한 프로세스에서 세 번째 프로파일부터 표가 비었다(13~29 µs/버스트). 그래서 C=1 셋째 창과 C=4 두 창은
버렸고, C=4 분해는 없다. 원인은 확인하지 않았다.

**다음 후보(미실측).** mHC 호출당 비용(TP 패킷 소비 포함 여부부터 확인), DSA 디코드 top-k 의 네이티브화, dense GEMM
입력 pack 호출 접기. MoE 는 가장 크지만 8행이 층마다 약 58 전문가를 읽는 대역폭에 묶여 있다.
원시 표·창별 수치·도구·재현: `measurements/st_decode_profile_20260914/README.md`.

### ST 디코드 C=1 대 C=2 커널 분해: main `9c45086a`, K=7 (2026-09-15, srv2 플릿 보류 1회, 운영자 "st커널 c=2 최적화 개선")

#950 뒤 프로덕션 디코드는 C=1(8행)과 C=2(16행)뿐인데, C=2 분해는 없었다. 같은 부팅에서 도어 프로파일 창을 C=2 → C=1 → C=2 순서로 잡았다. 셋째 창은 빈 표다.

- 조건: 약 70 토큰 프롬프트, thinking 끔, 긴 그리디 생성.
- 프로파일러 없는 스텝은 C=1 44.3 ms, C=2 55.3 ms 로 **×1.25** 다. 커널 합은 48.3 → 59.7 ms 다.
- 증가분(ms/스텝):
  - MoE +5.6. 16행이 층마다 약 104 전문가를 읽는다(8행은 약 58). codex C2 MoE 작업의 몫이다.
  - TP 통신 +2.5. `k_publish_packets` 1.56 → 2.94, `k_oneshot_moe_packets` 1.30 → 2.34 가 대부분이다. 전체합이 PDL consumer(0.91)에서 일반 경로(0.98)로 바뀐 몫은 커널 시간으로 0.07 이다.
  - mHC +1.9. 압축 계수 커널은 8행 이하 전용이라, C=2 는 FP32 커널(호출당 82 µs, C=1 은 50 µs)로 돈다.
  - 글루 +0.7. `_commit_layers` 0.26 → 0.64, 공유 전문가 직렬 활성 호출 2.5 → 36.5 회.
  - DSA +0.6. 인덱서 행별 루프 때문에 logits 호출이 두 배다.
  - KDA +0.6, LM head +0.3.
- **dense GEMM 은 C=2 가 1.1 ms 싸다.** C=2 일반 경로가 10.97 ms, C=1 특화 경로가 12.03 ms 이고, C1 입력 pack 만 1.60 ms(80.6회)다. 그러니 C1 특화를 16행으로 옮기는 것은 레버가 아닐 가능성이 크고, 8행에 C2 식 경로를 쓰는 편이 후보다(미실측).
- 같은 main 의 서빙 처리량(09-14 23:15 보류, srv2 `~/expert-capture/c1c2.log`)은 1024 토큰 고정 C=1 66.7/65.0 → C=2 79.3/88.0 tok/s, JSON C=1 85.7 → C=2 100.7 tok/s 다.

원시 표·도구·재현·옛 기록 대조(onepass 행 수별 반복 비용, C=1 대 C=4 트레이스): `measurements/st_decode_profile_c2_20260915/README.md`.

### 첫 거절의 타깃은 드래프터 2순위가 가장 많고, 맞는 형제 행은 전문가 읽기를 +7.3% 늘린다: main `9c45086a` C=1 K=7 (2026-09-15, srv2 플릿 보류 2회, 운영자 "형제 행과 체인 행의 MoE 전문가 겹침률 측정 (트리 판단용)")

**기록.** 트리 검증(#947/#951/#954/#961)을 이 하드웨어에서 더 밀지 정하는 재료다. 판정이 아니다.
- 조건: main `9c45086a` 에 디버그 커밋을 하나씩 얹었다(머지 안 함, 패치는 기록 폴더). Red Hat 랭크, 운영 env, 서두 기본값,
  onepass JSON 12문항 C=1 그리디. rank 0 의 도어 latency 행을 읽었다.
- **첫 거절에서 타깃의 자리**(`cc3ab619`, 6,291 스텝, tokens/step 4.66):
  - 결과: 전부 수락 27.7%, selector_miss 61.3%, candidate_miss 11.1%.
  - 드래프터 후보 순위 누적(첫 거절 분모): ≤1 7.8%, ≤2 39.8%, ≤3 53.8%, ≤8 76.3%, ≤16 84.7%.
  - walk 가 틀린 1순위를 따른 경우가 첫 거절의 61.3%, 에지 점수가 맞는 1순위를 뒤집은 경우가 7.8% 다.
- **전문가 겹침**(`ef2e5b6e`, 모든 스텝 동기, 6,167 스텝):
  - 검증 8행은 층마다 서로 다른 전문가 41.9 개를 읽는다. 무작위 top-8 가정이면 58.1 이고, #963·#965 의 "약 58" 이 이 값이다.
  - 체인 드래프트 행 하나의 몫은 읽기 +8.4% 다.
  - 거절 뒤 앵커(= 맞는 형제가 탈 라우트)는 +7.3%, 위치별로 7.1~7.8% 로 평탄하다.
- **추정**(위 수치의 산술, 트리를 잰 것이 아니다):
  - 형제 1개의 이득은 p0 에서 +0.068 tokens/step(+1.5%)이 최대다.
  - 9번째 행으로 붙이면 스텝 시간이 +3.3% 다. 읽기 +7.2% × MoE 비중 0.456(#965) 으로 MoE 몫만 센 하한이다.
  - 고정 8노드 예산이면 형제가 가장 깊은 체인 노드(+0.277)를 밀어낸다.
  - 2순위 miss 의 절반이 맞게 드래프트되면 4.66 → 5.07 tokens/step(+8.8%)이다. 레버는 트리보다 드래프터 품질이다.

원시 행·클라이언트·요약 스크립트·디버그 패치·재현: `measurements/st_draft_rank_overlap_20260915/README.md`.

### 1024 토큰 고정 팔은 비동기 버스트 밖에서 돈다: C=1 대 C=2 서빙 비교는 JSON 팔로 (2026-09-15, srv2 `c1c2.log` 다시 읽기)

바로 앞 C=1 대 C=2 커널 분해 항목(#965)이 인용한 서빙 처리량 중 "1024 토큰 고정" 팔은 서빙 경로가 아니다.
- 이유: 그 팔은 `min_tokens = max_tokens` 로 보낸다. 생성 수가 `min_tokens` 에 못 미치는 동안 `_blocked_by` 가 `"min_tokens"` 를
  돌려주므로 요청 전체가 비동기 체인에 못 들어간다. 한 행만 막혀도 배치 전체가 동기로 돈다.
- 같은 부팅(09-14 23:15, main `9c45086a`) 기록이 이를 보여 준다.

  | 팔 | 버스트 | 행-반복 | 버스트 ms |
  |---|---|---|---|
  | fixed-c1 | 817 | 817 | 60.5 (한 반복) |
  | json-c1 | 1,738 | 6,884 | 197.2 (3.96 반복, 반복당 약 49.8 ms) |
  | fixed-c2 | 375 | 731 | 89.1 (두 행 × 한 반복) |

- 서빙 경로의 C=1 → C=2 합산 처리량은 JSON 팔의 85.74 → 100.71 tok/s(×1.17)다. 요청당 디코드 중앙은 89.7 → 59.9 tok/s 다.

### 925차 — C1 MoE 미사용 초기화·중복 동기화 제거 (2026-09-14, srv2 CPU, PR #925)

머지된 PR #923 뒤의 별도 변경이다. SF6가 사용하지 않는 A 파이프라인의 장벽·상태 생성을
생략하고, FC1/FC2는 실제 CuTe API의 defer_sync로 모두 초기화한 뒤 fence+CTA sync 한 번에
게시한다. C1의 한 FC1 half에서 양자화 뒤 첫 장벽은 최종 fence+게시 장벽과 중복되므로
일반 실행에서 제거한다. stamps 모드는 다른 워프가 끝나기 전 완료 시각을 쓰지 않도록
이전 장벽도 유지한다. 한 FC1 half·한 CTA 클러스터 조건을 생성 시 검증하며 M1–8 SF6 기본 ON이다.

초기 CTA sync 4→1회/CTA, 초기화 장벽 12→8개, 일반 실행 양자화 뒤 rendezvous 2→1회/작업이다.
같은 소스 M8 control/candidate의 NOP 제외 정적 명령어는 3474→3456, 정적 barrier는 28→24다.
레지스터 121·staged shared 99328 B·static shared 1024 B·stack/local 0은 동일하며 FP/MMA와
DMA 명령 수는 변하지 않았다. 미사용 장벽 메타데이터 32 B는 정렬 전 감소량이고 총 공유
할당은 같다. 속도 개선율로 환산하지 않는다.

실제 초기화·게시 코드 실행, 128개 쓰기 스레드의 지연·stamps ON/OFF, 기존 바이트·수명 검사를
포함한 CPU 25개와 전체 네이티브 9형상이 통과했다. 추가 native 범위는 stamped M8, FC1
1-stage 호환성 및 compact OFF이며 기본 FC1은 2-stage로 유지한다. 이전 비교 프로브는 이번
축을 양쪽 OFF로 고정한다. 새 이미지·기준 엔진 빌드, GPU 큐·실행·부팅·서비스 재시작은 없었다.
품질·수용률·step/s는 미측정이고 오라클 총 개선율도 paired MoE 계수가 없어 null이다.
main `7306b4a6`(#952 CUDA 13.2.1, #955 C2 M16 reform, #960 SF6 레지스터, #962 prefetch)과 병합하며
적용 범위를 M1–8로 명시 제한했다. `batch`의 M16 타일은 같은 형상이지만 scatter·prefetch 경로와 함께
컴파일·측정된 적이 없어 기존 동기화를 유지한다. 위 명령어·장벽 수는 병합 전 CUDA 13.0 이미지와
레지스터 이전 커널 기준이다. 병합본의 네이티브 컴파일(`--sync-cleanup`은 이제 레지스터 위에서 컴파일)과
GPU 레인은 아직 돌리지 않았다.
[소스 해시·초기화·게시·프로파일 시각·컴파일 근거](measurements/st_moe_sync_cleanup_20260914/README.md).

### 970차 — C2 batch M16 타일에도 MoE 미사용 초기화·중복 동기화 제거 (2026-09-15, srv2 CPU, PR #970)

PR #925의 정리(미사용 A 링 생략, 사용하는 두 파이프라인의 지연 fence+sync 한 번, 양자화 뒤 중복 장벽 제거)를
`batch`의 C2 M16 타일로 넓혔다. #925를 병합할 때는 C2에서 한 번도 돌려 보지 않아 C1으로 막아 두었다. C2 타일도
M16·FC1 half 하나·CTA 하나로 형상이 같다. C2에만 있는 읽기(Phase B 시작의 retained route 행, A2/SFA2 복사,
direct scatter)는 모두 정리가 남기는 FC1 fence+게시 장벽 뒤에서 일어난다. prefetch의 FC2 3슬롯 링도 같은
지연 fence+sync로 게시된다. stamps 모드는 이전 장벽을 유지하고, `sync_cleanup=False`는 별도 control 핸들이다.
서빙 레시피 `t,r,sf6,q0`에는 `batch`가 없으므로 서빙 핸들은 바뀌지 않는다.

CPU 56개가 통과했다. 실제 초기화 블록(3슬롯 prefetch 포함)과 실제 게시 꼬리를 128 레인에서
C1/C2·정리 ON/OFF·stamps ON/OFF로 각 20개 스케줄씩 실행했다. C2에서는 다른 레인이 쓴 route 행을
읽는 경우까지 포함한다. 남기는 장벽을 빼면 게시 전 읽기가 잡히는 음성 대조가 있고, 결함 넣기 4종도 모두 잡혔다.
srv2 CUDA 13.2.1 시드(CuTe DSL 4.6.2, 장치 숨김)의 네이티브 컴파일은 14개 핸들이 모두 통과했다.
정리 전후 정적 명령어는 C1 M8(레지스터) 3563→3547, C2 M16(scatter reuse+prefetch) 4029→4013으로
양쪽 모두 16개 줄었다. SASS에서 초기화 CTA sync 3개, A 링 장벽 교환 4개, 작업 항목당 전 레인 rendezvous 1개가
사라졌고, stamped C2는 그 rendezvous를 유지했다. 공유 할당(91136/100352 B)과 레지스터는 같다.
작업 수이며 속도로 환산하지 않는다. GPU 수치·replay·step/s·수용률·품질은 미측정이다.
`moe_pair_sync` GPU 레인은 준비만 했고, 플릿 티켓·GPU 컨텍스트·부팅은 없었다.
[CPU 시뮬레이션·네이티브 컴파일·동기화 명령 순서 근거](measurements/st_moe_sync_cleanup_c2_20260915/README.md).

### ST C=2 mHC 소비 커널이 BF16 계수 팩을 읽는다 — 기본 적용 (2026-09-15, srv4 단일 GPU)

K=7 C=2 검증 16행의 mHC 소비 커널이 C=1 처럼 무손실 BF16 계수 팩을 읽는다. 스텝당 84회인 패킷 소비와
all-reduce 뒤 일반 소비 둘 다다. 8행 제한은 산술이 아니라 디스패치였다.

- BF16→FP32 는 가수에 0 16비트를 붙이는 정확한 변환이다.
- 누산 순서도 FP32 커널과 같다.
- 제한은 `mhc.py` 의 `n <= 8` 과 네이티브 AR consumer 의 1..8 토큰 검사에 있었다.

같은 빌드 대조군은 `MHC(..., packed_rows=8)` 로 남는다. 서빙 노브가 아니다.

**수치.** 출하 소스 `cd067569`(티켓 `st-c2-mhc-final0915`)에서 실가중치 90팩으로 비트 단위 영오차 23그룹이 통과했다.

**시간.** 같은 이미지 단일 GPU captured B/A/A/B 의 호출당 µs 다.

| 소비 커널 | 행 | warm | evicted |
|---|---:|---:|---:|
| 패킷 (84팩 체인) | 16 | 24.25 → 20.87 (**−13.9%**) | 24.59 → 21.07 (**−14.3%**) |
| 일반 (5팩 체인) | 16 | 23.30 → 20.96 (**−10.0%**) | 33.80 → 25.40 (**−24.9%**) |
| 패킷 · 일반 | 8 | 같은 커널 ±0.4% | 같은 커널 ±0.6% |

- 16행 컴포넌트 합은 C=2 스텝당 약 0.30 ms(warm)다.
- 프로파일(#965)의 호출당 82 µs 와는 다른 양이다. 거기에는 PDL 대기가 포함될 수 있고, 이 프로브는 연산만 잰다.

**기각한 형태.** 블록마다 계수를 한 번 펼치는 형태도 정확했다. 하지만 C=1 의 곱셈마다 펼치는 형태보다 16행에서
2~3%, 8행에서 5.6% 느렸다. 레지스터도 168 개로 128 개보다 많다. 그래서 제거했고, 출하 커널 자원은 main 과 같다.

**09-13 M14 실패.** 프로브 어댑터가 `[output, hidden, stream]` 팩을 `[output, stream, hidden]` 을 읽는
`mk_mhc_bf16_kernel` 에 넣은 레이아웃 버그였다. 정밀도나 누산 순서 문제가 아니다.
- 그 레인의 시드로 GPU 에서 재현했다: post 56/56 불일치, 최대 절대차 0.000341951847076416, 기록과 같다.
- 맞는 레이아웃을 넣으면 14·16·28행에서 정확하다.

**측정하지 않은 것.** TP4 소비자 부팅, NIC·PDL 겹침, onepass, 수용률. 16행 일반 소비가 one-shot 합과 겹치려면
`ostcode/c2-oneshot-consumer` 가 필요하다.

[근거·표·재현](measurements/st_c2_mhc_packed_20260915/README.md).

### ST C=2 DSA 인덱서 선택 두 행 합치기 — 기본 적용 (2026-09-15, srv4 단일 GPU 레인 두 티켓, 운영자 "st커널 c=2 최적화 개선", PR #971)

**채택, 기본값 켜짐.** `_select_rows` 가 C=2 스텝에서 두 행을 합쳐 한 번에 선택한다.

- 방법: 행들의 후보를 이어 붙이고, 질의마다 제 행의 창을 DeepGEMM 압축 logits(`max_seqlen_k`)로 한 런치에 점수 매긴다.
  지평 마스크와 torch top-k 는 `[16, n_cand]` 블록에 한 번씩이다.
- 창 `(i*n_cand, i*n_cand+ke)` 는 길이 런치가 같이 낸다(`_row_windows`).
- 게이트: 행 둘 이상, 슬라이스 ≤ 20(torch 선택 알고리즘 규칙이 하나인 구간), t·n_cand 가 4 의 배수(DeepGEMM 질의 블록이 한 행만 담음).
- C=1 과 1 토큰 스텝은 코드가 그대로다. 같은 빌드 대조군은 `joined=False` 다.

**수치 게이트.** `st-c2idx-sel0915`(`9b41064c`)와 `st-c2idx-final0915`(`6982cdf4`, 최종 게이트) 둘 다 불일치 원소 0 이다.
- C=2 7 버킷(4096~202752), C=1 3 버킷, 1 토큰 2 버킷.
- 동점 키·0.0 동점·짧은 행·용량 끝·블록표 재결합·문맥 되감기, 정/역 재생 순서.

**시간.** 캡처 11 층 재생당 ms, B/A/A/B 두 번, warm 이다.

| 문맥 ×2 행 | 대조군 | joined | 비율 | 런치/재생 |
|---|---|---|---|---|
| 2K | 0.749 | 0.404 | 0.539 | 113 → 58 |
| 32K | 1.762 | 1.016 | 0.577 | 113 → 58 |
| 128K | 3.715 | 2.447 | 0.659 | 553 → 278 |

- evicted 비율은 0.557 / 0.589 / 0.644 다.
- C=1 대조군 대 같은 코드는 0.998~1.000 이다(잡음 바닥).
- 옵션 비교(같은 브래킷, 2K/32K/128K warm):
  - 행별 logits + cat 한 top-k: 0.683 / 0.731 / 0.62~0.85
  - 비압축 wide logits + wide top-k: 0.716 / 0.859 / 0.898
  - 둘 다 정확했지만 joined 보다 느려 기각했다.

**못 한 것.** 네 노드 onepass(수용률·tok/s)는 없다. 운영자 상시 지시로 컴포넌트 게이트 뒤 병합한다. 입력은 합성 FP8 풀 레코드다.
원시 사건·표·재현: `measurements/st_c2_indexer_rows_20260915/README.md`.

### 969차 — C=2 16행 dense W4: KDA 입력과 KDA·MLA 출력은 16행 CTA 로, 넷은 거절 (2026-09-15, srv4 단일 GPU 레인 1회, 운영자 "st커널 c=2 최적화 개선" → "c=2 저거도 도입", PR #969)

같은 빌드, 실제 랭크(`st-glm53-9391-up-gate-full/rank3of4`)의 RTN 팩으로 16행 dense 경로를 쟀다. 티켓 `c2dense-cells0915d`(81 초,
동결 트리 `3c128c9b`)의 28개 정확성 묶음이 모든 팔에서 0 허용오차로 일치했다.

- **16행 CTA.** 새 커널 `mk_gemm_rows16_kernel` 은 CTA 하나가 출력 8열을 맡는다. 일반 16행 레인과 같은 연산자로 X[16] @ W[8] 을
  돌리고, 워프 8개가 K 블록을 나눠 맡는다. 합산은 #946 의 슬라이스별 FMA 사슬을 그대로 재생한다. SM 당 CTA 3개, 레지스터 74~80 이다.
- **채택.** 거리 층 사슬 기준, warm / evicted 로 잰 값이다.
  - KDA 입력(6416×4096): 일반 `mk_gemm2_kernel<2>` 대비 −6.3% / −7.9%.
  - KDA 출력(4096×2048, TX): 와이드 셀 대비 −6.0% / −7.5%.
  - MLA 출력(4096×4096, TX): 와이드 셀 대비 −10.3% / −7.9%. 다만 L2 에 올라 있는 단일 층 warm 은 +11.6% 다.
  - 합계는 C=2 forward 당 dense 커널 시간 약 −0.3 / −0.4 ms 로, 스텝의 1% 미만이다. 컴포넌트 시간일 뿐 소비자 판정이 아니다.
- **거절 후 제거.** 아래 셋 가운데 gate/up·down·qkv_a 는 같은 날 판정 기준을 바꿔 채택했다(다음 항목).
  - gate/up: 사슬 −3.4%, 단일 warm +49.6%.
  - down: 사슬 warm +2.8%.
  - qkv_a: 사슬 warm +7.8%.
  - 16행 합친 쿼리 격자: 사슬 warm +6.8%, evicted −0.4%.
- **유지.** 기존 16행 와이드 셀과 QueryPair 공유 팩은 일반 경로와 사슬 ±3% 안이고 단일 warm 에서 6~11% 빠르다.
  1536 폭 쿼리 셀은 M14 거절대로 QueryPair 에만 둔다.
- **CPU 근거.** 운영 플래그 네이티브 컴파일(CUDA 숨김)에서 C1 특화 14종의 자원이 변하지 않았다. 바이트 주소 에뮬레이션은 8형상에서
  불일치 0, 변이 7종을 검출했다. 관련 단위 테스트는 52개 중 40 통과, 12 GPU 스킵이다.
- **미측정.** onepass·수용률·step/s, NIC 전송, GPTQ 팩. `run_gemm_bound_input` 기본 경로로의 8·16행 확인은 머지 뒤 티켓으로 잰다.

[표·명령·컴파일 기록·원시 이벤트](measurements/st_c2_dense_cells_20260915/README.md).

### C=2 16행 dense W4 둘째 채택: gate/up·MLP down·qkv_a 도 16행 CTA 로 — 판정 기준을 L2 evicted 사슬로 (2026-09-15, 새 GPU 측정 없음, 운영자 "캐시따뜻한건 어차피 충분히 빠르니 느려져도 되지않아?" → "그냥 너가 도입해서 기본값 pr 머지해")

바로 앞 항목에서 거절한 세 셀을 같은 측정(`c2dense-cells0915d`, 동결 트리 `3c128c9b`, 28개 정확성 묶음 0 허용오차)으로 다시 판정했다.

- **판정 기준.** 디코드 스텝은 층마다 다른 가중치를 읽으므로 그 가중치가 L2 에 없다. 이 서빙 조건에 해당하는 거리 층 사슬의
  evicted 값을 판정에 쓴다. warm 회귀는 받아들인다.
- **셋의 수치.** 와이드 셀 대비 사슬 warm / 사슬 evicted / 단일 evicted 이고, 괄호는 forward 당 ms(warm / evicted)다.
  - gate/up(6144×4096, `<false,32,2>`): −3.4% / −1.7% / −4.3%, 단일 warm +49.6% (−0.007 / −0.005).
  - down(4096×3072 TX, `<true,24,3>`): +2.8% / −8.2% / −28.3% (+0.003 / −0.015).
  - qkv_a(2048×4096, `<false,32,6>`): +7.8% / −4.4% / −15.5% (+0.015 / −0.019).
  - 셋의 합은 forward 당 warm +0.011 ms, evicted −0.039 ms 다. 스텝의 0.1% 미만인 컴포넌트 시간이다.
- **16행 합친 쿼리 격자는 계속 거절한다.** evicted −0.4% 는 잡음 안이고 warm 은 +6.8% 다.
- **바뀐 것.** `mk_run_gemm_bound_input` 이 16행에서 non-direct `k=4096, n∈{6416,6144,2048}` 과 direct
  `n=4096, k∈{2048,3072,4096}` 을 16행 CTA 로 보낸다.
  - 인스턴스 셋과 `mk_run_gemm_rows16` 의 검사·런치 가지는 측정한 소스(`3c128c9b`)에서 되살렸다.
  - `run_gemm_bound_input` 의 16행 조건은 #969 의 조건을 세 형상으로 넓힌 것이다. 측정은 프로브 입구
    `run_gemm_rows16` 을 직접 불렀다.
  - `forward_pipeline=False` 는 같은 빌드의 와이드 대조군으로 남는다.
- **CPU 근거.** 운영 플래그 네이티브 컴파일(CUDA 숨김)에서 16행 특화 6종이 나왔다. 새 셋의 레지스터는 72 / 72 / 70 으로 측정 트리와
  같다. 기존 16행 셋과 C1 특화 14종의 자원은 `compile-final.json` 과 같다. 관련 단위 테스트는 74개 중 46 통과, 28 GPU 스킵이다.
- **미측정.** 이 트리를 GPU 에서 돌리지 않았다(운영자 지시로 머지 먼저). `run_gemm_bound_input` 기본 경로로의 16행 확인은 머지 뒤
  이 트리로 확인 티켓을 다시 넣는다. onepass·수용률·step/s 도 재지 않았다.

[표·컴파일 기록](measurements/st_c2_dense_cells_20260915/README.md).

### C=2 의 16행 전체합을 one-shot PDL consumer 로 (2026-09-15, 기본값 채택 — 운영자 상시 지시로 TP4 확인 없이 머지)

C=2 스텝의 전체합(타깃·드래프터, 스텝당 14.6회)은 16 × 4096 = 65,536 원소라 consumer 상한 8행에 걸려 일반 `k_oneshot` 으로 갔다.
- **그 상한은 이 빌드의 커널 한계가 아니었다.** 엔진 `build()` 는 `OSAR_COMPACT_CTA` 를 정의하지 않는다.
  - 그래서 `k_oneshot_consumer` 는 일반 커널과 같은 48 CTA × 256 격자, CTA 당 티켓 1, MAXEL 을 덮는 `VECITER`=3 stash 다.
  - "12 CTA·두 벡터 stash" 는 vLLM overlay 의 선택 옵션(`VLLM_GLM53_AR_COMPACT_CTA=1`, 기본 0)에만 있다.
  - 8행은 vLLM 시절 C=1 A/B 범위가 옮겨 온 값이고, `cells.py` 주석이 그 출처를 잘못 적었다.
- 변경: `ONESHOT_CONSUMER_MAX_ELEMENTS` 16행(기본값). 네이티브 소스는 main 과 바이트가 같다(캐시 키 동일).
  - 일반 커널은 내부 대조군 인자 `consumer_max_elements`(0)로 고를 수 있다. STK_ 노브는 없다.
  - 16행 소거 시험·캡처 재생과 게이지 셀을 더했다. 16행 consumer 는 멈춤 감시의 부트스트랩 유예(16 시퀀스)를 지난 23번째 집합통신에서 처음 돈다. 틀리면 매달리지 않고 STALL·trap 으로 끝난다.
- 정확성:
  - 단일 GPU(`c2cons-gpu2-a4b9787e`): 네 rank, 1~64행과 꼬리 크기에서 consumer == 일반 == rank 순서 fold 가 BF16 바이트까지 같다. 늦은 PDL 생산자·조기 PDL 후속·NaN 독을 둔 캡처 사슬도 같다.
  - 조기 출발 증명(`c2cons-gpu3-01657002`): consumer 의 PDL 후속은 3 ms 늦은 착지를 기다린 뒤(SM 시각 도장) 최종 합을 복사했다. dependency wait 는 해제가 아니라 완료까지 막는다. 리뷰(Bugbot) 주장에 대한 반증이다.
  - CPU: 엔진 소스에서 뽑은 stash·소유·혼합 티켓 g++ 오라클 PASS, 변이 4/4 잡음. 확장 4변형·오라클 컴파일 PASS. 관련 19 모듈 118 시험 중 110 통과, 8 skip(GB10), 실패 0.
- **이득(단일 GPU, GPU 쪽만, 피어 미리 착지, B/A/A/B)**: 16행 합 한 번이 consumer 에서 **−9.1~−9.7%**(생산자 PDL/일반 × warm/evicted, 반복 폭 ≤0.5%)다.
  - 8행은 −18~−21% 로 C=1 의 기존 선택과 맞는다. 이득은 쉬는 CTA(16행 16개)에서 오고, 조기 출발에서는 오지 않는다(09-13 트레이스 분해와 일치).
  - 절대 µs 는 프로덕션 옆 공유 GPU 라 믿지 않는다. **TP4 스텝 이득·onepass 는 미측정이다.**
- 첫 단일 GPU 티켓(`c2cons-gpu-4d50fd37`)은 시험 결함으로 실패했다. 캡처가 프록시 요청을 먹는다고 잘못 셌다. 고친 뒤 통과했고 원래 로그를 남겼다.
- 패킷 커널(코디네이터 추가 과제): `k_publish_packets`·`k_oneshot_moe_packets` 에서 정확성을 지키며 뺄 16행 고정비·직렬화 비용은 못 찾았다. 코드 변경은 없다.
  - 행에 비례해 느는 것은 페이로드 전송이다(floor 0.24~0.29 µs/KiB, 한 레일 상한). 넣지 않은 후보 넷과 이유는 README 에 있다.

단일 GPU·타이밍 원자료, 4랭크 미검증 목록, 재현: `measurements/st_c2_oneshot_consumer_20260915/README.md`.

### 976차 — C=2 드래프터 블록 MLP 도 16행 CTA 로, 그리고 16행 기본값 두 번의 머지 뒤 확인 (2026-09-15, srv4 단일 GPU 레인 3회, PR #976)

- **머지 뒤 확인 두 판.** 둘 다 PASS, 정확성 묶음 각 26개다.
  - `c2dense-postmerge0915`(main `28e8bd02`, #969) 와 `c2dense-six0915`(main `1dd3e606`, #975) 모두 `run_gemm_bound_input` 기본 경로를 쟀다.
  - 8행: C1 셀은 그대로이고 일반 경로보다 빠르다.
  - 16행 사슬 evicted 기준, 기본 CTA 대비 대조군이 더 걸린 비율(`wide_control` / 일반):
    - KDA 입력 +5.9 / +7.1%
    - KDA 출력 +5.4 / +9.0%
    - MLA 출력 +7.7 / +8.2%
    - MLP down +5.1 / +9.1%
    - qkv_a +3.5 / +3.2% (warm 은 −8.3% 로 느리다. 기준상 허용)
    - gate/up +1.1 / −1.0%
- **드래프터 블록 MLP.** gate/up 6144×4096, down 4096×3072 로 타깃과 같은 형상이다. 이제 16행에서도 셀에 묶인다.
  - down(행렬 출력)은 새 `rows16<false,24,3>` 이다(레지스터 68, SM 당 CTA 3).
  - 측정은 `c2drafter16-0915`: DFlash2 5블록을 rank 3 샤드로 잘랐고, 모든 팔이 비트 동일했다.
  - 사슬 evicted, 일반 경로 대비: gate/up −1.7%(최소 −0.8%), down −0.3%. 두 셀 모두 표본 넷이 전부 일반 경로 넷보다 낮았다.
  - 크기는 propose 스텝당 약 −7 µs 로 작다. down 의 warm 은 +3.1% 로 느려졌다.
- **미측정.** onepass·수용률·step/s. 이 수치는 컴포넌트 시간이다.

[표·원시 이벤트·명령](measurements/st_c2_dense_cells_20260915/README.md).

### 977차 — C=2 공유 전문가 겹침(SharedOverlap) 16행: 정확하지만 빠르지 않아 거절 (2026-09-15, srv4 단일 GPU 레인 2회, 운영자 "st커널 c=2 최적화 개선" → "전부 개선해", PR #977)

C=1(8행)만 쓰는 공유 전문가 겹침을 C=2 16행에 여는 후보를 같은 빌드로 쟀다. 후보는 main `1dd3e606` 위의 `b916b9b4`(게이트·부트 증명),
프로브는 `74943746` 이다. 병합하지 않고 `ostcode/c2-shared-overlap-candidate` 에 남긴다. 게이트는 그대로 `rows <= spec_k + 1` 이다.

- **정확성.** 서빙 직렬 finalizer 대비 0 허용오차로 일치했다(두 티켓 `c2so-gpu1-b916b9b4`, `c2so-gpu2-74943746`).
  - 소비자 셀 178개(8행 74, 16행 104)에서 다른 바이트가 0 이다.
  - 융합 공유 MLP 대 직렬 사슬(W4 gate/up → Triton SwiGLU → W4 down): 16행 출력 9,227만 원소, BF16 활성 577만 원소,
    캡처 재생 88회 모두 다른 바이트 0 이다. 8행도 0 이다.
- **속도.** 실제 L3 라우터, 독립 16행(U=100, C=2 디코드의 실제 점유)에서 겹침 대 서빙(1회차 / 2회차):
  - warm +0.25% / −0.00%, evicted +0.10% / −0.12%. 이득이 없다.
  - U20(요청 둘 × 관련 8행): warm +0.06% / +0.33%, evicted −0.30% / −0.14%. 중립이다.
  - U10(관련 16행 한 묶음)만 evicted −1.9% / −1.8% 로 일관되게 빠르다. C=2 디코드 스텝의 점유가 아니다.
- **이유.** 공유 MLP 단독은 16행에서 warm 27 µs, evicted 57 µs 로 FFN 의 1.7% / 3.5% 다(스텝 42층이면 1.1~2.4 ms 가 상한).
  FFN 안에서는 라우팅 커널 뒤에 두든 옆에 두든 34~38 µs 로 같다. 겹침이 되찾은 몫은 warm 0%, evicted 3% 다.
- **C=1 대조.** 같은 코드 두 팔의 warm 차가 U8 에서 최대 2.8% 다. B 그래프를 따라다녔고 원인은 가리지 못했다. 그래서 작은 warm 차는
  근거가 아니다. 직렬 양성 대조(S)는 U53 에서 겹침 대비 warm −0.04%, evicted +0.41% 다. C=1 의 겹침도 이 빌드의 실제 점유에서는
  중립이다(바꾸지 않았다).
- **스트림 충돌(09-14 overlap v2).** main 의 `_capture` 가 이미 스트림 하나를 재사용한다. 새 프로브는 그 스트림을 먼저 잡고
  SharedOverlap 스트림과의 별칭을 거부했다. 두 티켓 모두 부모 스트림 오류 없이 돌았다.
- **범위.** GB10 한 대, 부팅·전송 없음, 항등 집합통신. 두 티켓 동안 srv4 에 서빙 컨테이너가 없었다(st-glm53 가 10:28 부팅에서
  파킹 계층 불일치로 종료). onepass·수용률·tok/s 는 재지 않았다. 컴포넌트 이득이 없으니 잴 것이 없다.

[표·명령·원시 기록](measurements/st_c2_shared_overlap_20260915/README.md).

### 978차 — C=2 KDA o_proj 입력 pack 도 출력 정규화가 쓴다 (2026-09-15, srv4 단일 GPU 레인 1회, 운영자 "c=1 전용을 c=2로 마저 이식해" → "진행해", PR #978)

#968 의 생산자 pack 을 16행에도 적용했다.

- **바뀐 것.**
  - 16행 KDA 출력(4096×2048)은 16행 CTA 가 와이드 pack 을 읽는다. 그래서 `_output_norm_pack` 에 `WIDE` 레이아웃을 더했다.
  - 레이아웃은 [heads×32×128] 바이트와 [heads×32] 행 스케일이다.
  - `run_gemm_bound_input` 은 그 경로(`forward_pipeline`, 정확한 크기)에서만 pack 을 받는다.
  - 대조군이자 롤백은 `Net.producer_packs=False` 다.
- **GPU `c2oprojpack16-0915`(동결 `563e2c18`, 머지 트리와 관련 파일 동일).** 8·16행 모두 PASS 다.
  - 정규화 출력, TX 슬롯, pack 사용 바이트가 셀 자체 pack 커널 결과와 0 허용오차로 같다.
  - 16행 8층 사슬 evicted: −0.40%(최소 −0.35%). 표본 넷이 모두 대조군 넷보다 낮다.
  - 같은 사슬 warm 은 최소 −0.74% 다.
  - 크기는 KDA 층당 약 −1.2 µs 로 작다.
- **CPU.**
  - 바이트 레이아웃 에뮬레이션: 헤드 1/3/16/32 × 16행에서 불일치 0, 오프셋 변이 셋을 검출했다.
  - SM121 오프라인 컴파일: C1 레이아웃은 main 과 같은 명령어 구간이다(BF16/FP32 가중치). 와이드 레이아웃 레지스터는 36/35 다.
  - 운영 플래그 네이티브 컴파일 PASS.
  - 관련 테스트 63개: 40 통과, 23 GPU 스킵.
- **머지 뒤 확인 `c2oprojpack16-postmerge0915`(main `d5f0b1aa`).**
  - 8·16행 모두 0 허용오차로 PASS 다.
  - 16행 사슬 evicted 는 −0.54%(최소 −0.60%)다. 표본 넷이 다시 모두 대조군보다 낮다.
  - 16행 단일 층 evicted 는 비김이다(최소 +0.36%).
- **미측정.** onepass·수용률·step/s, 4랭크 실행.

[표·원시 이벤트·에뮬레이션](measurements/st_c2_dense_cells_20260915/README.md).

### 프로덕션 NVMe 티어 기본 끔 — 랭크 티어가 또 어긋나 프로덕션이 뜨지 못했다 (2026-09-15 10:28–11:10, 운영자 "당분간 nvme 대화캐시 기능 자체를 꺼")

- **기록.**
  - 10:28 프로덕션(배포 빌드 `0ac226a8`)이 내려갔다.
  - 10:44 슈퍼바이저 런치가 `the ranks' NVMe tiers hold different conversations` 로 죽었다. 랭크 0·1 은 파킹된 대화 3개(769M), 랭크 2·3 은 2개(513M)였다.
  - 사이사이 큐 티켓(tempab2·tempab3)이 플릿을 가져가 슈퍼바이저는 유예했다.
- **원인.**
  - 배포 빌드에는 45차의 드롭(#796)이 있다. 하지만 캡처 앞의 확인(`boot.py:1000`, `Server._agree_on_parked(comm, sorted(runner.parked_keys()))`)은 dropper 없이 불러 옛 계약대로 죽는다.
  - key 단위 화해(#837)는 main 에만 있다. deploy-watch 는 큐가 조용하지 않아 main 을 올리지 못했다.
  - 핸드오버는 플릿이 넘어갈 때마다 대화를 파킹하므로, 랭크마다 파킹이 엇갈릴 기회가 티켓 수만큼 생긴다.
- **조치.**
  - **티어 격리.** 네 노드 `st-tier` 를 `st-tier.diverged-0915` 로 옮기고 빈 디렉터리를 만들었다. 지우지 않았다.
  - **기본값 끔.** 운영자 지시로 NVMe 티어(대화 파킹과 prefix 경계의 디스크 사본)를 끈다.
    - `launchers/start-st-glm53.sh` 의 기본값은 `ST_TIER_DIR=off` 이고, 이때 `--tier-dir=` 를 넘긴다. boot 의 티어 가드(`if tier_dir:`)는 빈 문자열을 티어 없음으로 읽는다.
    - `ST_TIER_DIR=<dir>` 를 주면 그 부팅만 켠다.
    - 브래킷(`bench/st_bracket.sh`)은 팔마다 디렉터리를 주므로 그대로 켜진다.
  - **배포 트리.** srv2 `~/st-engine` 의 런처에도 같은 변경을 손으로 넣었다(원본 `~/st-engine-start-st-glm53.sh.bak-0915`). 다음 슈퍼바이저 런치부터 적용된다.
- **값.**
  - 부팅 예산에서 prefix 스냅샷이 2.12 → 4.25 GiB 로 늘고, 압축 prefix RAM 캐시 1.00 GiB 와 NVMe 스테이징 0.19 GiB 가 빠진다. 랭크당 약 +0.94 GiB 로, 10:45 부팅의 미할당 19.73 GiB 안이다.
  - RAM prefix 캐시는 그대로다. 파킹할 곳이 없으니 유휴 대화의 상태와 핸드오버 때의 대화는 디스크에 남지 않는다.
- **확인.**
  - `tests.test_engine_bootpaths` 에 두 테스트를 더했다. 런처 스니펫은 기본·`off`·경로 세 경우를, boot 는 `--tier-dir=` → `""` 를 본다.
  - 이를 포함한 런처 관련 13개 모듈에서 219 테스트가 통과했다(11 스킵).
  - 부팅 확인은 다음 프로덕션 런치로 한다.

### 네이티브 확장 넷이 부팅마다 다시 컴파일되고 있었다 (2026-09-15)

- **발견.**
  - `bounded_graph`·`decode_queue`·`mapped_staging`·`prefill_topk` 는 `$HOME/.cache/st` 에 빌드했다. 컨테이너 안에서는 `/root` 이고, `docker rm` 과 함께 사라진다.
  - tempab5 hold 의 rank 3 컨테이너 안에서 네 빌드의 시각을 읽었다: 46.06 · 45.52 · 56.12 · 47.38 s, 합 195.07 s.
  - 원장의 네 행(`prefill/128/0/before`, `prefill/32256/0/prepared`, `target/(2, 8, 1048576)`, `bounded-decode/(2, 8, 1048576)/4`)과 rank 3 로그의 두 공백(각 약 47 s)이 여기서 설명된다.
- **바꾼 것.**
  - `native_cache.build_root()` 가 `$ST_NATIVE_BUILD_ROOT/<이름>` 을 돌려준다.
  - Dockerfile ENV, 런처 `-e`, 프로브 러너가 `/cache/cu132/st-native` 를 준다.
- **CPU 확인.** 같은 이미지에서 두 컨테이너를 차례로 띄웠다. 첫 컨테이너는 217.10 s 동안 컴파일했고, 둘째는 0.20 s 에 읽었다(`.cuda.o` mtime 이 같다). 키는 살아 있던 부팅의 키와 같다.
- **미측정.** GPU 부팅. 노드마다 첫 부팅은 한 번 컴파일하고, 그다음 부팅부터 네 행이 줄어야 한다.

[표·명령·원시 출력](measurements/st_native_build_root_20260915/README.md).

### 프리필 메모리 게이트 재사용 — 같은 빌드·설정·노드의 재부팅은 먼 끝 패스를 기록으로 대신한다 (2026-09-15)

- **근거.**
  - 먼 끝 패스(32,256 토큰 @ 1,016,320)는 브래킷 부팅 다섯 번에서 29.52–30.57 s, 프로덕션 main `3acae017` 부팅에서 47.40 s 였다(게이트 84.8 s 중). 늘어난 17.9 s 는 컴파일·회수·레인 경합이 아니었고, 원인은 미상이다.
  - 위치 0·먼 끝 피크 워크스페이스는 네 부팅, 세 빌드에서 바이트까지 같았다(7,059,170,264 / 10,624,328,664).
- **바꾼 것.** `engine/base/prefill_record.py` 와 `RuntimeMemory.agree`/`reused` 를 더했다. 어댑터는 위치 0 뒤에 투표하고, boot 는 `production/ready` 뒤에만 기록을 쓴다.
  - 키: 엔진 트리, 런타임 패키지, 가중치 헤더, 메타 내용, 선언 설정, 노드.
  - 판정: 위치 0 피크가 기록 +64 MiB 안이어야 한다. 투영한 먼 끝 피크를 오늘 상자의 할당자 상한, OS 예약, SIGTERM 선에 대본다.
  - 합의: 한 랭크라도 못 쓰면 모든 랭크가 먼 끝 패스를 돈다.
  - 재사용 부팅은 1,024 토큰 @ 32,256 이어 쓰기 패스를 대신 돈다.
  - 강제: `ST_FULL_MEMORY_GATE=1`, `--full-memory-gate`, 또는 `~/glm53-cache/st-gate` 삭제.
- **위치 0 은 유지.** `_single_conv` 의 (T=32,256, 상태 없음) constexpr 특수화를 부르는 패스가 위치 0 뿐이다.
- **컴파일 빚.** tempab5 의 먼 끝 패스 창에 JIT 캐시 쓰기는 srv2·srv4 모두 0 개였다. 재사용 부팅이 먼 끝 전용 특수화에 내는 비용은 로드뿐이다.
- **검증.** CPU 147 테스트 OK(새 모듈 24).
- **미측정.** GPU 부팅. 재부팅 한 번에 프로덕션 약 −45 s(브래킷 모양 약 −28 s)는 추정이다.

[설계·요구 대응·근거](measurements/st_prefill_gate_reuse_20260915/README.md).

### 팩 스토어 — 가중치 하나의 두 레인이 해시와 교정 로드를 한 번만 한다 (2026-09-15)

- **근거.**
  - main `3acae017` 프로덕션 두 번째 부팅(03:38 UTC)에서 `loaded` 는 53.5 s, rank 0 의 `prepare native execution` 은 32.16 s 였다.
  - dense 가중치마다 W4 레인(`pack`)과 FP8 레인(`pack_fp8`)이 각자 가중치를 복사해 sha256 하고, 교정 blob 을 로드·`isfinite`·스무딩·sha256 했다. rank 3 의 대상 blob 은 203개 7.25 GiB 다.
- **바꾼 것.**
  - `PackStore.weight_digest` → `WeightDigest`. 가중치는 워커 스레드에서 한 번 해시하고, 교정 해시는 그 가중치의 레인들에 한 번이다.
  - digest 는 해시한 바이트(저장소·오프셋·레이아웃·`_version`)에만 답한다. 빌드 경로는 Hessian 을 다시 읽어 해시가 같은지 확인한다.
  - 식별자 dict 는 그대로다. 이미 파일된 팩을 같은 이름으로 찾으므로 팩 바이트와 수치가 바뀌지 않는다.
- **CPU 측정.** rank 3 의 실제 blob, 차가운 페이지 캐시, 가중치 234개(합성 BF16, 실제 모양).
  - old 25.88 / 24.64 s → new 13.85 / 14.86 s, 랭크당 −10.9 s(−43%).
  - 두 변형 모두 같은 437개 팩 파일을 찾았고, 빌드는 0 이었다.
- **검증.** CPU 85 테스트 OK(7 스킵, CUDA 전용).
- **미측정.** GPU 부팅. 다음 부팅의 rank 0 `prepare native execution`(기준 32.16 s)과 `loaded`(기준 53.5 s)로 확인한다. 약 −10 s 는 추정이다.

[표·명령·원시 출력](measurements/st_boot_pack_digests_20260915/README.md).

### 네이티브 확장 — 모든 랭크가 첫 집합통신 전에 한꺼번에 빌드하고 랑데부한다 (2026-09-15)

- **근거.**
  - main `3acae017` 의 첫 콜드 부팅(03:25 UTC)은 네이티브 확장을 첫 사용 자리에서 하나씩 빌드했다: 32K 프리필 안, 타깃 캡처 안, 버스트 파이프라인 안.
  - 노드별 빌드 끝 시각이 17.6–33.9 s 벌어졌다. decode queue 를 먼저 끝낸 rank 1 이 one-shot 합에서 약 31 s 를 기다리다 `unspecified launch failure` 로 죽었고, 나머지 랭크는 `WC error 12` 뒤 따라 죽었다.
  - 노드마다 네 빌드에 158.6–182.4 s 가 들었다.
- **바꾼 것.**
  - `profiles/glm53/natives.py` 가 fleet 부팅이 싣는 확장 일곱 개를 확장당 스레드 하나로 한꺼번에 빌드한다.
  - `boot.fleet` 은 `Comm.init()` 전에 빌드를 시작하고, one-shot 준비 전에 `native-builds` 랑데부에서 만난다. 실패한 랭크는 실패 단계로 피어를 즉시 멈춘다.
  - `dense.extension()` 을 `build()` 와 장치 확인으로 나눴다. 키·플래그·경로·수치·노브는 그대로다.
- **CPU 측정(`st-engine:glm53`, CUDA 숨김).**
  - 키가 있을 때: 하나씩 0.27 s, 한꺼번에 0.09 s. 오늘 흩어져 치르는 로드와 같은 크기다.
  - 키가 없을 때: 하나씩 372.9 s, 한꺼번에 65.7 s(가장 긴 dense). cgroup 최고 15.57 GiB(16 GiB 상한, 페이지 캐시 포함)였다. 두 표본은 배경 부하가 달랐다.
- **검증.** CPU 66 테스트 OK(7 스킵). 목록 누락 검사는 `engine/kernels` 의 모든 `cpp_extension.load` 모듈을 대조한다.
- **미측정.** GPU 부팅. 다음 부팅 로그의 `rankN: native builds in X s` 줄과 rank 0 표의 `native builds` 행, 그리고 콜드 부팅의 `STALL` 로 확인한다. CuTe-DSL·Triton JIT 은 여전히 첫 사용 때 랭크마다 컴파일한다.

[표·로그·명령](measurements/st_boot_native_prebuild_20260915/README.md).

### 문 열기 전 워밍업 — 프리필 길이 여섯 개를 뺀다 (2026-09-15)

- **근거.**
  - main `3acae017` 의 두 따뜻한 부팅(03:38, 03:51 UTC)에서 `warmup shapes` 는 5.25 / 5.15 s 였고, 그중 프리필 64·256·1,024·2,048·2,304·4,096 토큰이 4.66 / 4.56 s 였다.
  - 이 여섯 번이 도는 레인(32행 dense W4, 64행 mHC, 128 토큰 샤딩, 640 routed pair, 8,192행 라우터)은 모두 메모리 워밍업과 커널 워밍업이 이미 돈다.
  - 커널 워밍업이 끝난 때부터 문이 열릴 때까지 srv2·srv4 `/cache` 에 새로 쓰인 파일은 두 부팅 모두 0 개였다.
- **바꾼 것.** `boot.fleet` 이 `warmup_shapes(lengths=())` 를 부른다. 디코드 폭 1·2 의 동기·비동기 워밍업은 남긴다.
- **검증.** CPU 11 테스트 OK(`warmup_admission`, `warmup_draws`, `bootpaths`).
- **미측정.** GPU 부팅. 다음 부팅의 rank 0 `warmup:` 줄에서 `prefill/…` 가 사라지고 `warmup shapes` 가 0.6 s 안팎이어야 한다. 부팅마다 약 −4.6 s 는 추정이다.
  - 캐시가 빈 노드의 첫 요청이 그 네 길이(256·2,048·2,304·4,096)와 정확히 같으면, conv 의 `T` 특수화가 그 요청 안에서 컴파일한다. 다른 모든 길이가 원래 치르던 비용이다.

[근거 줄·캐시 확인·명령](measurements/st_boot_warmup_decode_only_20260915/README.md).

### 문법 자격 검사 — xgrammar 가 문의 토크나이저에서 입력을 읽는다 (2026-09-15)

- **근거.**
  - `qualify grammar` 는 main `3acae017` 따뜻한 부팅 두 번에서 6.75 / 10.18 s(rank 0)였다.
  - `for_checkpoint` 는 transformers `AutoTokenizer` 를 따로 만들어(CPU 2.2 s) `TokenizerInfo.from_huggingface`(1.1 s)에 넘겼다. 엔진은 같은 `tokenizer.json` 을 이미 `tokenizers.Tokenizer` 로 읽는다.
  - GLM-5.3 메타에서 백엔드의 `get_vocab(with_added_tokens=True)`·`to_str()` 로 만든 TokenizerInfo 는 vocab dict, metadata, decoded vocab 154,880 개, stop/special id, `dump_metadata` 가 모두 같았다.
- **바꾼 것.**
  - `base/grammar.tokenizer_info` 를 더했고, `for_checkpoint(tokenizer=)` 와 `Grammars(info=)` 가 이를 받는다.
  - GLM 부팅은 문의 토크나이저를 `qualify grammar` 에서 한 번 로드해 문법과 문이 함께 쓴다.
  - 기본 경로(토크나이저 없음)는 그대로다.
- **버린 것.** `TokenizerInfo.serialize_json` 디스크 캐시는 NUL 토큰 다섯 개가 빈 바이트열로 왕복해 넣지 않았다.
- **검증.** CPU 44 테스트 OK. 합성 byte-level·Metaspace 토크나이저에서 두 경로의 TokenizerInfo 와 JSON/스키마 마스크가 같다.
- **미측정.** GPU 부팅. 다음 부팅의 `qualify grammar`·`door` 행으로 확인한다. 랭크당 약 −2.2 s 는 CPU 추정이다.

[단계별 시간·대조·버린 방법](measurements/st_boot_grammar_tokenizer_20260915/README.md).

### 디코드 DSA 인덱서 선택이 커널 하나가 됐다 — 지평 마스크 + top-k, sm_120 컴포넌트 3.40배 (2026-09-15, 로컬 sm_120, 운영자 "DSA 인덱서 훨씬 개선해")

**기록이다. 판정이 아니다.** 시간은 전부 **sm_120(RTX 5050, 20 SM, GDDR)** 컴포넌트 측정이고 GB10 도 플릿도 아니다.
onepass·수용률·step/s·품질은 재지 않았다. D17 대로 속도 주장은 플릿이 재야 끝난다.

- **표적.** GPU 판정 15차가 이미 적어 둔 자리다 — 인덱서 시간의 74%가 점수 계산이 아니라 고르기이고,
  `[6912, 8192]` fp32 216 MB 를 14.5 ms 에 고르는 것은 **15 GB/s, 대역폭 바닥의 30분의 1**이며 "라딕스 두 패스가
  맞는 모양"이라고 되어 있었다. 디코드 쪽은 그대로 남아 있었다: `_mask_horizon` 이 `[rows, n_cand]` fp32 를
  통째로 read-modify-write 하고, `torch.topk` 가 20k 열을 넘으면 **호출 하나가 커널 21 개**(#971 의 인구조사)에
  전역 작업공간·아무도 안 읽는 `values` fp32 버퍼·`pool_slots` 가 다시 좁히는 int64 승자를 낸다.
- **바꾼 것.** `engine/kernels/decode_topk.{cu,py}` 의 `st_dsa_select` — **런치 하나**, 작업공간 없음, 캡처 안전.
  행을 한 번만 읽고(스레드당 float4, 지평은 레지스터 비교라 logits 를 안 건드린다), 같은 스윕에서 원소별 8비트
  bin 을 공유메모리에 남겨 sift 가 DRAM 대신 그 캐시를 읽고, 자르는 bin 은 워프 하나가 셔플로 고르고, 후보는
  정렬 키를 id 옆에 달아 stash 에 들어가 정제 라운드가 DRAM 으로 돌아가지 않는다. 출력은 `pool_slots` 가 이미
  받는 int32 다. 폴백 둘(넓은 행은 두 번 읽기, 넓은 bin 은 행 상대 정제) 다 정확하고, 형상을 못 받으면 `None` 을
  돌려 호출자가 예전 경로를 탄다 — **노브도 수치 분기도 없다.**
- **왜 비트 단위로 같은가.** 키가 `(ordered_float_key(score) << 32) | ~index` 라 정확한 동점은 **낮은 pool id** 가
  이긴다. 그게 이 하드웨어에서 `torch.topk(sorted=False)` 가 하는 일임을 먼저 실험으로 확인했다(단일·다중 블록
  형상에 경계 평원을 강제, **동점이 실제로 난 89 행에서 불일치 0**). `pool_slots` 는 집합을 받으므로(승자를 토큰
  위치 내림차순으로 다시 쓴다) 집합이 같으면 슬롯·카운트가 같다.
- **수치 게이트.** 마스크된 `torch.topk` 의 집합과 원소 단위로 **25,920 행 전부 일치**. 분포 8종(relu 의 정확한 0
  평원, 경계 강제 동점, 전 원소 동일 → 동점이 순전히 id, fp16 범위 초과, fp16 서브노멀 포함) × 행 1·8·16·20 ×
  폭 600~50,688 × 지평 3종 × 구성 4종(bin 캐시 on/off, 블록 폭 128·1024·자동).
  - **이 게이트가 실제 버그를 잡았다**: 블록 폭을 런타임화하면서 히스토그램 병합이 `tx < ST_RADIX` 로 남아
    256 보다 좁은 블록에서 상위 bin 이 0 이 됐고 자르는 bin 을 틀리게 골랐다. 그 상태로 잰 블록 폭 표는 깨진
    커널을 잰 것이었다 — 고치고 다시 쟀다.
- **시간(그래프 재생, 양쪽 팔 다 캡처).** 12 형상 합 501.3 → 147.3 µs = **3.40배**. 형상별 1.62~**5.09배**,
  `n_cand` 16,384(64K 문맥)에서 가장 크다. 런치는 층당 2 → 1, 장문에서는 22 → 1 이다.
- **어디가 바닥인가.** 단계를 잘라 재면 **행을 읽는 스윕이 271 GB/s**, 이 상자 피크의 **85%** 다. 읽기는 이미
  루프라인이고 16×50688 의 22.7 µs 는 한 번 읽기 바닥(10.1 µs)의 2.2 배 — 나머지는 공유메모리 단계와 고정비다.
- **져서 넣지 않은 것.** 복제 히스토그램 1·2·4·8 은 전 arm 3% 안(스윕은 카운터에서 경합하지 않는다). 워프 집계
  원자연산은 스윕에서 154 → 185 µs, 후보가 한 bin 에 몰리는 정제에서도 sift 8.1 → 8.9 µs 로 **둘 다 졌다** —
  Blackwell 의 공유 원자연산이 이미 같은 주소를 합친다.
- **검증.** CPU `tests/test_engine_decode_topk.py` 10 개 + `test_engine_decode_rows`·`test_engine_indexer_select`·
  `test_engine_indexer_rows`·`test_engine_indexer_slots` 46 개 OK. 네이티브는 `natives.MODULES` 에 들어가 부팅의
  첫 집합통신 전에 다른 넷과 함께 빌드된다.
- **미측정.** GB10·TP4·onepass·수용률·step/s·품질. 캡처 첫 호출의 `cudaFuncSetAttribute`. 블록 폭 표와 "복제·워프
  집계가 진다"는 판정은 이 상자(20 SM, GDDR)의 것이라 48 SM·LPDDR 에서 다시 봐야 한다.
- **다음.** `probes/engine_decode_select_rows.py` 에 `fused`·`fused_rows` arm 을 더했고 기존 `control`·`joined` 은
  `native=False` 로 같은 빌드의 토치 경로가 됐다. 7 용량 버킷 × 5 단계에서 슬롯·카운트 비트 대조 + 타이밍:
  `bash bench/fleet.sh run --gpu st-decode-topk 15 ... probes/engine_kernel_check.py --lanes select_rows`.

[동점 실험·25,920 행 게이트·단계 분해·블록 폭·진 후보들·재현](measurements/st_decode_topk_20260915/README.md).

### M64 프리필 MoE 는 네 타일까지 정확하고 다섯 번째에서 깨진다 — 자격 미달 (2026-09-15, srv4 단일 GPU 레인 6판, 운영자 "ssh로 들어가면 되잖아")

**판정.** #876 의 M64 프리필 후보는 **자격을 얻지 못했다.** 257 행부터 같은 입력에 대해 자기 출력과
어긋난다. M128 대조군은 모든 폭에서 퍼짐 0.0 이다.

- **경계는 256 → 257, 즉 M64 타일 넉 장이다.** 129·193·201·209·217·225·233·241·249·**256** 은 퍼짐 0.0
  이고 129·256 은 M128 과 **비트 동일**(팔 사이 차이 0.0). **257**·385·513·1024·2304 는 M64 자기 퍼짐
  1.10~1.34 다. 1.1 은 반올림이 아니라 경쟁이다. `_prefill_m64_bodies` 의 "16-row-per-warp scatter
  strips"(타일당 넉 줄)와 #876 의 "independent activation-scale atoms" 가 같은 자리를 가리킨다.
- **속도는 주장하지 않는다.** 프로덕션 옆 eager B/A/A/B 라 대조군 브래킷 드리프트가 0.04~0.73 이다.
  2304 행의 `ratio 0.81` 은 그 잡음 안이고, 그 폭의 M64 출력은 애초에 틀렸다. 원장의 **−20% 는 이
  기록으로 확인되지도 반박되지도 않았다** — 경쟁을 고쳐야 물어볼 수 있는 질문이다.
- **환경.** srv4 GB10 sm_121a, 프로덕션 옆 단일 GPU 레인(holder-single, 8 GiB, 플릿 리스 없음),
  이미지 `bracket-b39f2bda8014`, 랭크 `st-glm53-9391-up-gate-full/rank3of4` 의 `L3.moe.*`(folded).

**게이트가 자기 자신에 대해 잡은 것.** 첫 판정은 `passed: true` 였다. 규칙이 `across <= floor × factor`
였고 floor 가 후보 자신의 퍼짐이라 **망가진 팔이 자기가 재어질 기준을 올렸다.** 자기 일치를 먼저 본다:
`candidate_spread <= max(control_spread × factor, 1e-3)`.

**실행이 잡은 배선 결함 셋** (전부 첫 실행 전에는 보이지 않았다): 큐의 `ST_PROBES` 허용 목록에 프로브가
없어 접수 거부; `eligibility()` 가 셀 객체 대신 리포트용 dict 를 돌려줘 `cell.topk` 가 죽음;
`b12x_fused_moe` 가 `_prefill_tile64=None` 을 명시적으로 넘겨 **호출 시점 키워드가 `functools.partial`
을 덮는 바람에 두 팔이 모두 M128 로 돌았다**(게이트가 `reached (128, 128)` 로 잡음).

**부수 발견.** 자격 창은 `64 < m` 이지만 디스패처는 routed-pair 컷오버를 넘어야 dynamic 백엔드에 닿는다
— m=65 는 static 계열이 서빙한다. 프로브가 이제 행마다 백엔드를 물어 건너뛴 것으로 적는다.

**바뀐 것.** `launch_sm120_moe(_prefill_tile64=)` 가 M64 워크스페이스를 스스로 파생하고 `b12x_fused_moe`
가 그것을 넘긴다(기본 `None`, 엔진의 어떤 코드도 넘기지 않음 — 테스트가 박는다). 그래서 게이트가 재는
것이 **출하될 경로**가 됐다. 프로브는 `bench/fleet_onepass.ST_PROBES` 에 등재됐다.

**미측정.** tok/s·수용률·품질·TP4. 경쟁을 고친 뒤 같은 사다리로 다시 판정한다.

[여섯 판의 행별 원시 JSON·경계 이분·배선 결함 셋·재현](measurements/st_m64_prefill_qual_20260915/README.md).

### M64 프리필의 결함은 어디가 *아닌지* — 가설 셋을 실측으로 지웠다 (2026-09-15, srv4 단일 GPU 레인 3판 추가, 운영자 "그러던가")

바로 앞 항목의 256 → 257 경계에서 원인을 찾았다. **세운 가설 셋이 전부 틀렸고, 셋 다 실측으로 지웠다.**

- **타일 넉 장이 아니다.** 256 = 4 × 64 라 의심했지만 타일은 전문가마다 선다 — m=129 에서 이미 304 개다.
- **split-task 창이 아니다.** `_prefill_m64_bodies` 에 정확히 그 경계가 있다(`<=256` uniform,
  `256<m<=4096` **variable**, `>4096` uniform). 깨진 폭이 전부 가운데 창이었으니 "4096 초과는 멀쩡할
  것"을 예측했는데, m=4097·4608·6912·8192 **전부 깨진다**(퍼짐 1.05~1.25). 창이 아니다.
- **워크스페이스가 아니다.** 용량이 `1 << (m-1).bit_length()` 라 256 → 257 에서 정확히 두 배가 된다.
  사다리를 내림차순(`2304,256,129,256`)으로 돌리니 2304 가 깨진 뒤에도 **256·129 는 퍼짐 0.0, 불일치
  0 행**이다. 같은 프로세스, 이미 커진 워크스페이스. 용량이 아니라 `num_tokens` 다.

**어느 행이 틀리나 — 꼬리가 아니라 전부다.** 프로브에 행 단위 진단을 더했다: m=257 은 257 행 중
**257 행**이, m=2304 는 2304 행 중 **2304 행**이 틀린다. 첫 행부터다. 용량 끝을 넘는 주소 오류라면
꼬리만 틀려야 한다 — 런치 전체가 오염된다.

**관찰.** `producer_batch_tokens = tile_m × tile_n / hidden` 이라 M128 은 4, M64 는 2 다. Q0 스테이징
복사는 여전히 4 에서 쪼개 둘째 조각을 `+4 × cols × 2` 에 쓴다(2 에서는 0 바이트라 무해). M64 는 CTA 당
토큰 둘만 집으므로 프로듀서 반복이 두 배다.

**다음.** 전 행 오염 + `num_tokens` 의존 + 워크스페이스 무관 → 런치 전체가 공유하는 무언가가 토큰 수에
따라 어긋난다. 남은 후보는 그리드 일 분배(`resident_grid_barrier`·`pair_head` 클레임)와 커널이 직접
0 으로 채우는 scatter 출력이 뒤의 원자합과 순서 지어지는지다. 여기서부터는 커널 계측이 필요하고,
컴파일 없이 갈 수 있는 데까지는 왔다.

[가설 셋의 실측 반증·행 단위 진단·재현](measurements/st_m64_prefill_qual_20260915/README.md)

### 문 열기 전 게이트 — 행마다 forward·vote·reclaim 을 가르고, 컴파일은 한 번의 walk 이 귀속한다 (2026-09-15)

- **근거.**
  - main `3acae017` 프로덕션 부팅(168.9 s)의 `capture decode` 96.17 s 중 **프리필 메모리 게이트가 84.8 s** 이고 그래프 캡처는 8.5 s 였다. 게이트 재사용(같은 날)이 먼 끝 47.40 s 를 걷어내도 **위치 0 쪽 36.97 s 가 남는다**: 128 토큰 15.41 s, 1,024 토큰 2.07 s, 32,256@0 19.49 s.
  - **여덟 배 짧은 패스가 여섯 배 긴 값을 냈다.** 원장의 행은 `seconds` 하나뿐이고 그 안에 forward·랭크 합의·회수가 같이 들어 있어, 15.41 s 가 첫 포워드의 일회성 초기화인지, 컴파일인지, 늦은 피어인지 가를 수 없다.
  - 그 질문은 두 번 손으로 답해졌다 — 서빙 선언 43 초 뒤의 b12x 컴파일 1.55 s(부팅 시간 탐구 5-g), 먼 끝 패스의 미상 17.9 s(그 창의 `/cache` 쓰기 0 개, PR 게이트 재사용). 둘 다 사람이 부팅 뒤에 mtime 으로 센 것이다.
- **바꾼 것.**
  - `RuntimeMemory.checkpoint(stamps=)` — 호출자가 잰 경계가 행에 같이 실린다. 행이 스스로 재는 열은 덮이지 않는다.
  - `adapter._warmup_prefill_memory` 가 패스마다 `forward_seconds`·`vote_seconds`·`observe_seconds` 를 적는다. 합의 **앞에 동기화**를 하나 넣어, 위는 이 랭크의 장치 일이고 아래는 플릿의 대기가 되게 했다(전에는 `.item()` 이 둘을 한 값으로 모았다).
  - `engine/base/jit_writes.py` — 페이즈가 벽시계 창을 남기고, **부팅당 walk 한 번**이 첫 창보다 새 아티팩트를 mtime 으로 창에 나눈다. 루트는 런처가 선언한 것들(`FLASHINFER_WORKSPACE_BASE`·`TRITON_CACHE_DIR`…)을 환경에서 읽고, 중첩된 루트는 부모로 접혀 프로덕션에서는 `/cache/cu132` 한 번이다. 파일 20만·2 초 상한이 있고 어느 상한에 걸렸는지 말한다.
  - walk 은 `ready` 투표 **뒤에** 돈다(집합통신 앞의 낙오는 피어가 낸다), 실패해도 줄 하나일 뿐 부팅을 세우지 않는다.
- **읽는 법.** 다음 부팅의 `memory-rank{N}.json` 에서 `prefill/128/0/prepared` 의 세 스탬프, 그리고 로그의 `jit writes: rank N ...` 한 줄. 128 이 forward 에 있으면 첫 포워드의 일회성 초기화이고, 그 창에 아티팩트가 있으면 컴파일이며, vote 에 있으면 그건 게이트가 아니라 스큐다.
- **검증.** CPU 16 테스트(`test_engine_boot_windows`, `test_engine_boot_prelude`) OK. 엔진 CPU 스위트 전체는 손대기 전과 같은 실패 집합이다(이 상자에서 73개, 전부 triton·/proc·fcntl 부재).
- **미측정.** GPU 부팅. walk 자체의 초(`jit_scan_s` 에 해당하는 줄의 값)도 다음 부팅이 처음 말한다.

### 문의 호스트 절반을 로드 밑으로 — 문법 컴파일러와 챗 렌더러가 스레드로 간다 (2026-09-15)

- **근거.**
  - `qualify grammar` 는 main `3acae017` 따뜻한 부팅 두 번에서 rank 0 **6.75 / 10.18 s** 였다. `door` 행(표가 그 한 줄 위에서 나가므로 출력되지 않는다)은 챗 템플릿용 transformers 토크나이저다.
  - 둘 다 남의 C++ 안의 호스트 일이고(체크포인트 토크나이저, 154,880 토큰의 `TokenizerInfo`, `GrammarCompiler`, 렌더러), **엔진이 만든 것을 하나도 읽지 않는다** — config.json·tokenizer.json·generation_config.json 뿐이고 셋 다 `Comm.init` 전에 디스크에 있다.
  - 그 옆에서 `load`(5.2–9.6 s)와 `prepare native execution`(32.16 s)이 장치 일을 하고 있다.
- **바꾼 것.** `boot.Prelude` 가 `build()` 직전에 스레드를 띄워 토크나이저·문법 컴파일러·(rank 0) 렌더러를 만든다.
  - **스레드에 CUDA 없음.** `Grammars.qualify(device)` 는 그대로 메인 스레드의 `qualify grammar` 페이즈에 남는다 — 비트마스크 할당과 마스크 커널과 그 Triton JIT 은 원장의 행이 봐야 하는 바이트다(D1).
  - **캡처 전에 조인한다.** 캡처는 파이썬 디스패치 바운드라(부팅 시간 탐구 5-h) 거기서 도는 호스트 스레드는 GIL 을 빼앗는다. 조인은 `wait for the prelude` 라는 자기 행을 갖고, 프리루드가 로드보다 오래 살면 그 초로 말한다.
  - 양쪽이 읽은 vocab 과 stop id 가 다르면 문을 열지 않는다(스레드가 체크포인트를 두 번째로 읽으므로).
- **값.** 랭크당 약 **−7 s** 추정(문법 6.75–10.18 + 렌더러). 부팅에서 빠지는 것이지 사라지는 일이 아니다 — 로드가 장치 일이라 그 밑에 숨는다.
- **검증.** CPU 6 테스트. 스레드가 device 를 `None` 으로만 묻는지, 렌더러가 rank 0 만인지, 스레드의 예외가 메인 스레드의 페이즈에서 다시 던져지는지(피어의 원장 투표가 거기 있다), 반쪽이 어긋나면 죽는지, 그리고 시작·조인 위치가 로드 앞·캡처 앞인지.
- **미측정.** GPU 부팅. 다음 부팅의 `wait for the prelude`(0 에 가까워야 한다)·`qualify grammar`(마스크 자격검사만 남는다)·`prelude_s` 로 확인한다. GIL 경합은 로드 구간에서만 가능하고 재지 않았다.

### 부팅 표가 부팅 전체가 된다 — `front` 행, 합계, 랭크마다의 JSON, 그리고 로더가 어느 길로 읽었는지 (2026-09-16)

- **근거 — 표 밖에 있던 것 셋.**
  - **앞부분.** 레코더는 `comm` 직전에 열린다. 그 위의 파이썬 시작·torch·커널 모듈·lease·`check_box`·설정·kernel shape 는 행이 없다. 90.2 s 부팅에서 **15.6 s**, 87 s 부팅에서 **9.17 s** 였고 **두 번 다 사람이 컨테이너 타임스탬프에서 빼서** 알아냈다(부팅 시간 탐구 §2·5-f).
  - **합계.** `table()` 은 `root.children` 만 찍는다. "부팅 168.9 s" 는 원장이 아니라 `memory-rank0.json` 의 `at_seconds` 나 docker 타임스탬프에서 사람이 계산한 값이다.
  - **랭크 1~3.** 표는 rank 0 만 찍고 `rec` 는 어디에도 안 남는다. 그래서 `wait for weight preparation` **9.69 s** 는 "rank 0 이 가장 느린 피어를 기다렸다" 까지만 말하고 **누구를** 은 말하지 못한다. 같은 빌드 두 부팅의 `capture decode` 가 96.2 대 68.3 인 것도 행 단위로 뺄 수가 없다.
- **바꾼 것.**
  - `instruments.process_seconds()` — `/proc/self/stat` 의 22번째 필드(부팅 이후 클럭 틱)와 `/proc/stat` 의 `btime`. 모듈 상단의 `perf_counter` 로는 못 닫는다(파이썬 시작과 그 위의 임포트가 여전히 밖이다). /proc 이 없으면 **None 이고 행이 안 나간다** — 추측하지 않는다.
  - `Recorder.mark(name, seconds, **counters)` — 다른 데서 잰 페이즈를 행으로 넣는다. `fleet` 이 첫 행으로 `front` 를 찍고, 그 행에 `import_s` 를 단다(임포트 블록을 스탬프로 감쌌다: torch 1.22·tilelang 1.70·flashinfer 1.06·deep_gemm 0.15·triton 0.09 가 유일한 실측이고 GPU 없는 컨테이너에서 손으로 잰 것이다). 그러면 앞부분이 **임포트**와 **그 나머지**로 갈린다.
  - `table()` 의 마지막 줄이 최상위 행의 합이다.
  - `rec.dump(dump_dir/boot-rank{N}.json)` — **모든 랭크**가, 마지막 페이즈 뒤에. 부팅 둘을 빼는 일도, 스큐의 주인을 찾는 일도 이제 파일 대 파일이다.
  - **로더 게이지**(§5-i 가 2026-09-12 에 요구하고 나흘 비어 있던 것): `direct`(O_DIRECT 로 읽었나), `wait_s`(앞 업로드 밑에 숨지 **못한** 읽기), `copy_s`(H2D), `read_bytes`(가장 넓은 런 + 섹터 = 한 번의 읽기 크기).
- **왜 로더 게이지가 최적화보다 먼저인가.** 부팅 표의 `load` 는 44.5 GiB / 5.245 s = **8.49 GiB/s** 였는데 이 드라이브의 O_DIRECT 상한은 **6.2** 다. 상한 위의 값이라 그 부팅은 순수 O_DIRECT 가 아니었거나 바이트 카운터가 과장한다 — 그리고 **어느 쪽인지 원장이 말하지 않는다.** 말하기 전에는 읽기 크기도 스레드 수도 세울 수 없다. `ThreadPoolExecutor(max_workers=1)` 은 **그대로 두었다**(2 스레드 6.24 대 1 스레드 5.52 GiB/s 는 오프라인 프로브의 값이고, 다음 부팅이 경로를 말한 뒤에 쓴다).
- **검증.** CPU 12 테스트. `/proc` 산술은 합성 픽스처(필드 번호로 만든)와, 실제 리눅스 커널에서 `starttime/hz` 가 `/proc/uptime` 과 맞는지로 확인했다. 엔진 CPU 스위트는 같은 커밋의 깨끗한 워크트리와 실패 집합이 같다(이 상자 74개, 전부 triton·/proc·fcntl 부재).
- **미측정.** GPU 부팅. `front` 의 실제 값과 그 안의 `import_s`, `load` 의 `direct`·`wait_s`·`copy_s`, 그리고 네 랭크의 표가 서로 얼마나 벌어지는지는 다음 부팅이 처음 말한다.

### 부팅 168.9 → 94.3 초, 그리고 게이트의 15.6 초는 컴파일이 아니다 (2026-09-16, srv2 프로덕션 부팅 1회, 내가 띄운 것 아님)

- **기록.** 컨테이너 시작 2026-09-15T21:49:45.93Z → `serving on :8000` 21:51:35.04Z = **벽시계 109.1 s**. 런타임 메모리 원장 **94.29 s**, 같은 자로 잰 main `3acae017`(09-15 12:38 KST)의 **168.87 s** 대비 **−74.6**.
- **09-15 에 머지된 것들이 각각 얼마였나.** 전부 그날은 "미측정(GPU 부팅)" 이었다.

| 항목 | 기준 | 이 부팅 | 차 | 추정이었던 값 |
|---|---:|---:|---:|---:|
| 먼 끝 패스 (게이트 재사용) | 47.40 | **0.007** | −47.4 | −45 |
| `qualify grammar` (프리루드) | 10.18 | **0.044** | −10.1 | −7 |
| `prepare native execution` (팩 digest) | 32.16 | **23.50** | −8.7 | −10 |
| `wait for weight preparation` | 9.69 | **3.47** | −6.2 | — |
| `warmup shapes` | 5.25 | **0.564** | −4.7 | −4.6 |
| `native builds` | 부팅마다 재컴파일(195 s) | **0.078** | 캐시 적중 | — |
| `load` | 9.63 | **13.21** | **+3.6** | — |

  - 프리루드는 `prelude_s=13.141`, `wait for the prelude 0.000` — **13.14 s 가 통째로 로드 밑에 숨었다.** 추정의 두 배다(`door` 의 챗 렌더러까지 같이 갔다).
  - `load` 만 반대로 갔다(3.62 GiB/s). 이 부팅엔 로더 게이지가 없어(#1029 는 이 배포본 뒤) 페이지 캐시였는지 O_DIRECT 였는지 **말할 수 없다**. 다음 부팅이 답한다.
- **판정 — `prefill/128` 의 15.6 초는 첫 포워드 그 자체다.** 행마다의 스탬프(#1023)가 처음 말했고, **rank 0 과 rank 3 이 바이트까지 같다**(forward 15.628 / 1.579 / 10.159 / 1.458).
  - **합의가 아니다**: 네 패스의 `vote` 합이 1.8 ms.
  - **회수가 아니다**: 행과 forward 의 차가 128 에서 58 ms.
  - **컴파일이 아니다**: `jit writes: rank 3 none -- 0 artifacts newer than the first window over 44785 files in 0.83 s`. 게이트·커널 워밍업·캡처 전 구간에 새 아티팩트 0 개.
  - 1,024 와 32,256 이 토큰당 **0.2747 ms** 의 직선을 주고 그 직선이 128 에 주는 값은 **1.33 s** 다. 실제 15.63 → **일회성 +14.3 s**.
- **다음 한 부팅이 가른다.** 두 랭크가 같은 값이므로 경쟁도 노드 사정도 아니다. 컨테이너에서 `CUDA_MODULE_LOADING` 은 **비어 있다**(CUDA 13.2 기본값 LAZY). 1순위 가설은 **첫 런치마다의 cuModuleLoad** 이고, 그것은 캐시에 쓰지 않으니 walk 이 못 보는 종류다. `CUDA_MODULE_LOADING=EAGER` 로 한 번 띄우면 14.3 초가 컨텍스트 생성 쪽으로 옮겨가거나(성립) 그대로 있거나(기각) 한다.
- **부수.** rank 0 의 walk 은 `stopped at the 2 s cap`(37,451 파일)이라 **부분 답**이었다. 상한을 10 s 로 올리고, 끝까지 돈 walk 만 "none anywhere" 라고 말하게 했다 — 부분 "none" 은 온전한 "none" 과 똑같이 읽힌다.
- **미측정.** 앞부분 9.2 s 의 구성(#1029 의 `front`·`import_s`), `load` 의 경로, `prepare native execution` 23.5 s 의 분해.

[표·두 랭크 원장·로그 줄](measurements/st_boot_20260916/README.md).

### 남은 두 통짜 행을 쪼개고, 커널 임포트를 랑데부 밑으로 (2026-09-16)

- **근거.** 09-16 부팅(99.86 s 표) 에서 아직 크고 안 갈라진 행이 둘이다.
  - `prepare native execution` **23.50 s** — `3acae017` 에서 32.16 이었고 팩 digest 가 −8.7 을 냈는데, **두 번 다 분해가 없다.** 이 안에는 라우터 준비, dense 팩 빌드·읽기, 디코드 투영, 드래프터 팩이 한 숫자로 들어 있다.
  - `lanes` **3.22 s** — 커널 패키지 임포트와 테이블 구성이 한 숫자다.
  - 같은 자리에 있던 게이트의 15.6 s 는 행이 셋으로 갈린 그 주에 답이 나왔다(2026-09-16: forward 다). 남은 둘도 같은 이유로 가른다.
- **바꾼 것 — 행.** `prepare native execution` 이 `routers`·`dense packs`·`decode projections`·`drafter packs` 를 중첩 행으로 갖는다(디코드 fastpath 셋은 한 결정이고 한 비용이라 투영 행 안에 둔다). `lanes` 는 `kernel imports` 를 갖는다(`served(recorder=)`, 없으면 `nullcontext`).
- **바꾼 것 — 당김.** `lanes.import_kernels()` 와 `boot.Background`.
  - 커널 패키지 임포트는 남의 모듈 스코프(triton·tilelang·deep_gemm)이고 CUDA 를 잡지 않으며 엔진이 만든 것을 읽지 않는다 — 문의 호스트 절반(`Prelude`)과 같은 모양이다.
  - 그래서 **네이티브 빌드 랑데부 밑에서** 돈다. 그 랑데부는 빠른 랭크가 가장 느린 랭크를 기다리는 순수한 대기이고, 그 뒤의 `prepare one-shot`(1.85 s)도 전송로의 것이지 파이썬의 것이 아니다.
  - **빌드 뒤**에 시작한다: 그 스레드들이 이 모듈들이 나중에 로드할 확장을 쓰고 있고, 같은 키를 둘이 빌드하는 경쟁은 할 이유가 없다.
  - 조인은 `lanes` 안의 제 행(`wait for the imports`)이고, 프리루드와 같이 `kernel_imports_s` 게이지를 단다 — 숨지 못하면 초로 말한다.
- **목록이 기억이 아니라 검사다.** `KERNEL_MODULES` 와 `served` 의 `from` 줄이 **양방향으로** 대조된다: `served` 에 추가하고 목록에서 빠뜨리면 그 패키지는 프리페치를 잃고, 목록에만 남으면 부팅이 공짜로 모듈 하나를 더 읽는다.
- **검증.** CPU 10 테스트. `import_kernels` 의 본문이 AST 로 "임포트 둘뿐" 인지까지 본다 — 그 스레드에 다른 것이 붙으면 그건 메인 스레드 밖의 작업이다.
- **미측정.** GPU 부팅. 다음 부팅의 `lanes`·`wait for the imports`·`kernel_imports_s`, 그리고 `prepare native execution` 의 네 행. 임포트가 랑데부 밑에 다 숨으면 `lanes` 는 테이블 구성만 남는다 — 상한은 3.22 s 이고 겹칠 수 있는 시간은 랑데부 대기 + 1.85 s 다.

### 로드가 4 일 동안 1 GiB 로 읽고 있었다 — 그 폭을 기각한 커밋 뒤에도 (2026-09-16, srv2 프로덕션 부팅 1회)

- **어떻게 잡혔나.** 어제 넣은 로더 게이지의 첫 값이다(08:15 부팅, rank 0, 배포 `023af823`).
  `load 10.667 s` · `direct=1` · `wait_s=2.746` · `copy_s=1.480` · **`read_bytes=1,066,911,488`**.
- **판정 1 — 폭이 틀렸다.** 1,066,911,488 B = **1,017.5 MiB** 이고, 이는 45차 §49 가 **기각한** 1 GiB 케이스의 값 그대로다(같은 문서: "1 GiB 에서 54 런, 각 1,017.5 MiB, 핀드 1.99 GiB").
  - `603c089f`(2026-09-12, "스테이징 버퍼가 862 MiB 넓었고 좁은 쪽이 빠르다")는 `runs()` 의 기본값만 `MAX_RUN`(512 MiB)으로 바꿨다. **`load()` 은 제 기본값 `1 << 30` 을 그대로 갖고 있었고 그것을 아래로 넘긴다.** 폭을 명시하지 않는 호출자 — 플릿 부팅이 그중 하나다 — 는 나흘 동안 계속 1 GiB 로 뭉쳤다.
  - 같은 §49 가 실제 랭크 파일로 잰 값: **1 GiB 3.94/4.20/4.38 GiB/s, 512 MiB 4.75/4.93/5.18**. 그리고 핀드 스테이징 **1.99 → 1.125 GiB**(−862 MiB), 그것도 상자가 가장 아쉬운 순간에.
  - 고쳤다: `load()` 의 기본값이 `MAX_RUN` 이다. 폭을 명시하는 호출자(프리샤드 32 MiB, 프로브 64 MiB, check 128 MiB)는 그대로다.
- **판정 2 — `load` 의 절반은 I/O 가 아니다.** 10.667 s 중 읽기 대기 2.746 + 업로드 1.480 = 4.23 s 다. **6.4 s 가 둘 다 아니다.** 남은 자리는 이 함수 안에 둘뿐이다 — 핀드 스테이징 한 쌍(각 1 GiB)의 할당과, 랭크 파일의 **모든 텐서**에 대한 뷰 생성(26,961 개 규모). 둘 다 스탬프를 달았다(`stage_s`, `views_s`, `tensors`).
- **미측정.** 다음 부팅. 읽을 줄은 `read_bytes` 가 **536,879,104** 로 내려갔는지, `stage_s` 가 절반인지, `views_s` 가 6.4 초 중 얼마인지다.

### 첫 랭크별 부팅 표 — 앞부분 5.9 초, 임포트는 그중 2.7 (2026-09-16, 같은 부팅)

- `boot-rank0.json` 이 처음 쓰였다(#1029). 표의 합 **133.0 s**.
- **`front` 5.922 s, 그중 `import_s` 2.668.** 나머지 3.25 s 는 파이썬 시작·lease·`check_box`·설정·kernel shape 다 — 지금까지 통째로 표 밖이던 자리이고, 임포트가 그 절반도 아니라는 것이 처음 나온 사실이다.
- **`capture decode` 72.998 s** — 45.18 이 아니다. 배포가 바뀌면 프리필 게이트 기록의 키가 달라져 **먼 끝 패스를 다시 돈다**. 즉 **배포 뒤 첫 부팅은 구조적으로 약 30 s 비싸다**.
- **`wait for weight preparation` 9.152 s** — 3.47 에서 다시 올랐다. 스큐는 부팅마다 크게 움직인다.
- `prelude_s=12.984` 에 `wait for the prelude` 0.000 — 두 번째 부팅에서도 문의 호스트 절반은 전부 숨었다.

### 앞부분의 이름 없는 3.25 초와, 14.3 초를 가를 노브 하나 (2026-09-16)

- **근거.** 09-16 08:15 부팅의 `front` 는 **5.922 s** 이고 그중 `import_s` 가 **2.668** 이다. 임포트는 절반도 아니고, **나머지 3.25 s 는 이름이 없다.** 그 자리는 `fleet()` 의 첫 네 줄이다 — lease 파일, `check_box`(셸로 나간다), `declared`, kernel shape.
- **바꾼 것(앞부분).** `front` 행이 `lease_s`·`box_s`·`shape_s` 를 같이 싣는다. 넷을 더하면 `front` 가 남김없이 갈린다.
- **근거(노브).** 게이트의 첫 포워드 **14.3 s** 는 컴파일이 아니고(walk 이 아티팩트 0 개), 합의가 아니고(1.8 ms), 회수가 아니다(58 ms). 두 랭크가 바이트까지 같다. 컨테이너에서 `CUDA_MODULE_LOADING` 은 **비어 있고**(CUDA 13.2 기본값 LAZY), 남은 1순위 가설은 **첫 런치마다의 cuModuleLoad** 다 — 캐시에 쓰지 않으니 walk 이 못 보는 종류다.
- **바꾼 것(노브).** 런처가 `CUDA_MODULE_LOADING` 을 **이름이 주어졌을 때만** 컨테이너로 넘긴다. 선언된 D11 노브가 아니다(엔진의 어떤 코드도 읽지 않는다, 드라이버만 읽는다). 그리고 **프리필 게이트 기록의 키에 넣었다** — LAZY 로 딴 기록을 EAGER 부팅이 재사용하면 안 된다.
  - 실험은 `CUDA_MODULE_LOADING=EAGER launchers/start-st-glm53.sh` 한 줄이고, 다음 부팅의 `prefill/128/0/prepared` 의 `forward_seconds` 가 답한다.
  - **하마터면 부팅을 매번 죽일 뻔했다**: 처음 쓴 것은 `[ -n "$X" ] && NCCL_ENV=...` 한 줄이었는데, 런처는 `set -euo pipefail` 이고 변수가 없으면 그 줄이 거짓이라 스크립트가 거기서 끝난다 — 프로덕션은 이 변수를 **절대** 주지 않으므로 매 런치가 그랬을 것이다. `if` 블록으로 고쳤고, 테스트가 `&&` 한 줄을 금지한다.
- **검증.** CPU 18 테스트.
- **미측정.** GPU 부팅. `front` 의 네 숫자, 그리고 EAGER 한 판.

### 배포 게이트가 스위트를 두 번 돌고 있었다 — 두 번째는 자기가 이미 잰 값이다 (2026-09-16)

- **실측(배포 감시 로그, 09-16).** 게이트 시작 → `deploying` 까지 **10m55s · 11m00s · 11m03s** 세 번. 그리고 `deploying` → `deployed` 는 **15~17 s** 다. 즉 **수정본이 상자에 닿는 시간의 거의 전부가 게이트**이고, 배포 자체는 16 초다.
- **무엇이 11 분인가.** `st-deploy-watch` 가 **풀 엔진 스위트를 두 벌, 순차로** 돌린다 — 후보 트리와 배포된 트리. 각각 제 시드 이미지 컨테이너 안에서 `nice 19`, CPU 4, 메모리 6g, 파일 4개 병렬.
  - **파이썬만 바뀐 수정본은 아무것도 컴파일하지 않는다.** 같은 날 부팅의 `native builds` 는 0.078 s(아홉 확장 전부 캐시 적중)이고, JIT walk 은 게이트·워밍업·캡처 전 구간에서 아티팩트 **0 개**를 봤다. 오늘 머지한 여섯은 한 줄도 컴파일하지 않았다.
- **판정.** 배포된 커밋의 판정은 **이 감시자가 이미 낸 값**이다: 그것을 배포한 사이클에서 그 커밋이 후보였고, 같은 트리·같은 시드 이미지·같은 상자에서 몇 분 전에 쟀다. 다시 도는 것은 새 정보가 아니다.
- **바꾼 것.** 배포에 성공하면 그때의 후보 판정(`after`)을 시드 이미지 id 와 함께 상태 파일에 남기고, 다음 사이클의 baseline 으로 쓴다(`cached_baseline`). 셋 중 하나라도 어긋나면 그냥 다시 돈다 — sha 가 다르거나, 그 트리가 핀한 시드 이미지가 움직였거나, 기록이 온전치 않을 때. 런치가 실패한 사이클은 baseline 과 함께 이 캐시도 버린다(노드 위의 트리를 아무도 모르는 상태이므로).
- **왜 캐시가 안전한가.** baseline 은 상자 자신의 실패를 후보에서 **빼기** 위한 것이다. 그래서 낡은 baseline 은 차이를 **더 나쁘게만** 만든다 — 거부된 배포와 파일 이름이 적힌 줄이지, 통과해선 안 될 것이 통과하는 일이 아니다.
- **값.** 정상 상태에서 게이트가 **두 벌 → 한 벌**. 위 세 사이클 기준 **약 5.5 분**이다. 이 변경이 배포되는 첫 사이클은 캐시가 없어 여전히 두 벌을 돈다.
- **검증.** CPU 7 테스트(재사용, sha 불일치, 이미지 이동, 반쪽 기록, 읽을 수 없는 핀, 그리고 게이트가 다른 커밋을 추출하기 **전에** 캐시를 묻는지). 이 상자에서 `test_st_deploy_watch` 는 67 개 중 손대기 전과 같은 집합이 실패한다(윈도우 환경).
- **미측정.** 실제 배포 사이클. 다음 배포의 `gate:` 와 `deploying` 사이 시간, 그리고 `baseline: ... not re-run` 줄.

### Xid 43 은 고장이 아니라 우리 커널의 `__trap()` 이었다 (2026-09-16, srv1 rank 1, 부팅 실패 일곱 번)

- **증상.** 09-16 하루 동안 부팅이 일곱 번 같은 자리에서 죽었다. 전부 **rank 1(srv1)** 이고, 전부 `AcceleratorError: CUDA error: unspecified launch failure`, 그리고 srv1 의 dmesg 에 `NVRM: Xid (PCI:000f:01:00): 43` 이 그 시각마다 찍혔다. 세 번의 읽기(슈퍼바이저 포렌식, 다른 세션의 요약, 그리고 나)가 전부 **"srv1 GPU 이상"** 으로 읽었다.
- **판정 — 우리가 부른 것이다.** `engine/kernels/oneshot/dsv4_oneshot_ar.cu` 는 피어 플래그를 스핀하다가 `OSAR_STALL_TRAP_S`(**30 초**) 뒤에 `__trap()` 을 부른다. 주석이 그 의도를 적어 두었다: *"the engine dies in seconds with the line above, not after a 5-minute RPC timeout with nothing"*. `__trap()` 은 호스트에 `cudaErrorLaunchFailure` 로 도착하고 드라이버는 그 옆에 **Xid 43** 을 남긴다.
  - 산술이 맞는다. 스톨 워드는 3 초에 쓰이고 프록시가 5 초마다 찍는다 — 죽기 전 로그에 STALL 줄이 **여섯 개**, 그리고 트랩은 30 초다.
- **그러면 진짜 원인은 무엇인가 — rank 1 이 30 초 넘게 피어를 기다렸다.**
  - `[oneshot] STALL rank=1 phase=wait(peer-flags) seq=1183 slot=3 missing_peer_mask=0x7 tx_seq=1183 ack_seq=1183 rxf[slot]={1179,1179,1179}`
  - RING=4 이므로 1183 과 1179 는 같은 슬롯이다. 피어 셋은 **1182 까지 보내고 1183 을 보내지 않았다.**
  - 네 랭크의 마지막 로그 줄이 **같다**(`[b12x static v2] lane serving: static2_m16_…`) — 전부 **그래프 캡처 안**이고, 피어들은 죽지도 에러를 내지도 않았다. rank 1 만 한 집합통신 앞서 있었고 피어들은 30 초 넘게 오지 않았다.
- **바꾼 것(진단).** `base/tripwire.death_note` 가 `unspecified launch failure`·`cudaErrorLaunchFailure` 를 보면 `likely` 를 붙이고, **파일에만이 아니라 줄로도 찍는다**: 이것은 보통 이 스택의 자기 워치독이고, 바로 위의 `[oneshot] STALL … missing_peer_mask` 줄이 누가 안 왔는지 말하며, 원인은 **그 피어들의 로그**에 있다는 것. 판정(`kind`)은 바꾸지 않는다 — 읽기는 판정 옆의 힌트지 판정이 아니다.
  - 인용한 30 초는 테스트가 커널 소스의 `#define OSAR_STALL_TRAP_S` 와 대조한다. 산문에 적힌 숫자는 썩는다.
- **안 바꾼 것.** 스톨 자체. 캡처 중에 피어 셋이 30 초 넘게 한 집합통신을 발행하지 않은 이유는 그들의 로그에 있고, 같은 구간을 다른 세션이 "CUDA boot preparation fix" 로 잡고 있다(#1036 이 캡처 앞의 전문가 준비를 옮겼다).
- **검증.** CPU 22 테스트.

### D17 프로브는 속도를 주장한 커밋에만 (2026-09-16, 운영자 "커밋당 한번은 너무 많아")

- **무엇이 문제였나.** 배포가 끝나면 `st-deploy-watch` 가 D17 워밍 샘플 프로브를 **커밋마다 하나** 걸었다. 프로브는 라이브 문 위에서 onepass 브래킷을 돌고, 그동안 **latency recording 이 서버를 예약**하므로 `/v1/engine/latency` 가 아닌 모든 POST 가 **409** 를 받는다(`base/serve.py`). 2026-09-16 10:22 배포 직후 그것이 데네브에 `API error 409: server reserved by a latency recording` 로 도착했고, C1 → C2-2000 → C2-32000 을 도느라 수십 분이었다.
- **바꾼 것.** 프로브는 **속도를 주장한 커밋**에만 걸린다(`claims_speed`). 판단은 커밋이 스스로 한 말로 한다:
  - **타입** — `perf:` / `perf(engine):`
  - **줄** — `D17-probe: yes|no`, 제 타입보다 스스로를 잘 아는 변경을 위해(스텝을 움직이는 `fix:`, 이미 브래킷으로 잰 `perf:`)
  - PR 템플릿이 이 질문을 그대로 묻지만, **스쿼시 머지는 본문을 안 싣는다** — 제목과 커밋 목록뿐이라, 답은 git 이 볼 수 있는 곳에 있어야 한다.
  - 프로브를 거는 자리는 **둘**이다(`after_deploy` 의 즉시 티켓, `ensure_probe` 의 뒤늦은 자가치유). 게이트는 `probe_wanted` 하나로 **양쪽 다**에 있다.
- **대가, 그대로 적는다.** 스텝을 조용히 되돌리는 `fix:` 는 이제 샘플되지 않는다. 그러면 **다음 `perf:` 의 샘플이 그 퇴행을 안고** 더 오래된 기준선에 대해 읽힌다. D17 자신의 답은 브래킷이다 — 속도 주장은 머지 전에 base→cand→base 로 재지, 프로덕션 샘플 둘을 빼서 추론하지 않는다.
- **하지 않은 것 — 프로브만 C=1·문항 반으로 줄이기.** `bench/st_bracket.sh:286-291` 이 `pair` 의 **base arm 을 D17 프로브의 warm 샘플로 재사용**한다. 프로브의 워크로드만 줄이면 base 와 candidate 가 **다른 것을 잰 두 기록**이 되고, 그 비교는 비교가 아니다. 줄이려면 onepass 의 기본값을 양쪽 다 바꿔야 하고, 그러면 **지난 기록 전부와 비교 가능성이 끊긴다.** 그건 운영자의 결정이라 여기서 하지 않았다.
- **검증.** CPU 6 테스트(타입 넷, 다른 타입 아홉, `D17-probe:` 양방향, 빈 메시지, 게이트가 판사·컨트롤러보다 먼저, 그리고 `fix:` 배포가 티켓을 안 거는 것). 이 상자의 `test_st_deploy_watch` 실패 집합은 손대기 전과 같다.

### onepass 를 둘로 나눈다 — `default` 는 싸고, `extended` 는 이름으로 부른다 (2026-09-16, 운영자 "원패스를 기본값이랑 확장 둘로 나누면 되지않아?", 하니스 46)

- **왜 그냥 줄일 수 없었나.** `bench/st_bracket.sh:286` 이 `pair` 의 **base arm 을 D17 프로브의 warm 샘플로 재사용**한다. 프로브만 싸게 만들면 base 와 candidate 가 다른 것을 잰 두 기록이 되고, 그 비교는 비교가 아니다. 게다가 **`fixed_concurrency_tokens` 가 기록된 워크로드에 아예 없었다** — C=N 팔을 돈 기록과 안 돈 기록이 구분되지 않았고, 판사도 워크로드를 보지 않았다(`metric_compatible` 은 `cold_compile` 만 본다).
- **그래서 나눈다.** 워크로드에 **이름**을 주고, 이름을 기록에 싣고, 이름이 다른 기록은 서로의 기준선이 아니게 한다.

| 프로필 | ctx | C=N 팔 |
|---|---|---|
| **`default`** (기본) | 2,000 · 32,000 | 없음 |
| `extended` | 2,000 · 32,000 · 128,000 | 1,024 토큰 |

  - `measurement_contract.PROFILES` · `profile(name)` · `profile_of(workload)`. 모르는 이름은 **기본값이 아니라 거절**이다. 이름에 없는 조합은 `custom` 이고, 그것도 하나의 정체성이다.
  - `fixed_concurrency_tokens` 가 `DEFAULTS` 에 들어갔다 — 이제 정체성의 일부다.
  - `onepass.py --profile` (env `ONEPASS_PROFILE`). 명시한 `--ctx`·`--fixed-concurrency-tokens` 는 여전히 이긴다; 그러면 기록이 `custom` 이라고 말한다.
  - `st_judge.comparable()` 과 `samples(profile=)`. `judge()` 는 후보가 잰 워크로드로 base 를 거른다 — 다른 프로필의 base 는 `NO BASE` 다.
  - `st_bracket.sh` 는 제 프로필의 샘플만 base 로 재사용한다.
- **대가, 그대로.** **하니스 46 이전 기록은 프로필 이름이 없고, 판사는 그것을 어느 것과도 짝지어 주지 않는다.** 이 변경 뒤 각 커밋의 첫 브래킷은 프로브의 샘플을 재사용하지 못하고 **자기 base 를 부팅한다** — 새 프로필의 샘플이 쌓일 때까지. 하니스 번호가 존재하는 이유가 그것이다: 의미가 바뀐 측정은 이어지는 것이 아니라 새 세대다.
- **값.** 루틴 측정에서 128K 문맥 하나와 C=N 팔 하나가 빠진다. 09-16 프로브가 문을 잡고 돈 구간이 C1 → C2-2000 → C2-32000 이었고, 그중 뒤의 둘이 사라진다.
- **검증.** CPU 14 테스트(프로필 둘, 모르는 이름의 거절, round-trip 과 `custom`, 정체성에 든 C=N, 기록의 이름, 판사의 같음·다름·이름 없음, 프로필별 세기, `NO BASE`, 그리고 onepass·bracket 배선). `test_fleet_st_bracket` 의 기록 픽스처에 프로필을 넣었다 — 그 시험들은 floor 와 delta 를 보는 것이지 워크로드 정체성을 보는 것이 아니다.
- **미측정.** 실제 브래킷·프로브. 첫 `default` 샘플이 쌓이기까지 base 부팅 한 번씩.
### 접두사 적중 17/100 은 용량이 아니었다 — 둘 곳이 없었다 (2026-09-16, srv2 프로덕션, PR #1042)

- **실측(라이브 카운터, 티어 off · `ST_KV_GIB=7.0`).** `prefix_cache_queries_total` **100**, `prefix_cache_hits_total` **17**, `prefix_cache_evictions_total` **1,144**, `prefix_tier_entries` **0**, `gpu_cache_usage_perc` **3.0 %**. 그때까지 처리한 요청 91.
  - **블록의 97 % 가 비어 있는데 축출이 1,144 번**이다. 용량 문제가 아니다. 요청당 1,144 ÷ 91 ≈ **12.6 개의 경계**가 버려졌다.
- **원인.** `engine/base/serve.py:2711` — `if self.runner.tiered is None: self._idle_order[row] = None; return`. 티어가 없으면 끝난 턴은 대화로 등록되지 않고, 그 행이 회수될 때 경계가 같이 사라진다. 계산은 다 해 놓고 둘 곳이 없어서 버린다.
  - 티어는 09-15 에 랭크 간 발산으로 껐다(그날의 항목). 그 발산의 원인인 key 단위 화해(`Server._reconcile_parked`, #837)는 그 뒤 main 에 들어와 있고, 네 노드의 `~/glm53-logs/st-tier` 는 비어 있다(발산 사본은 옆에 `st-tier.diverged-0915`, 769M).
- **조치 1 — 프로덕션 env(리포 밖).** `ST_TIER_DIR` 를 주고 `ST_KV_GIB` 7.0 → **14.0**(백업 `~/.config/st-glm53.env.bak-20260916-tier`). 11:04 부팅이 **한 번에** 붙었다 — `door up after 135s`, `healthy after 135s (a chat answered)`.
  - `kv_blocks_total` 1,398 → **2,987**, declared paged KV 6.16 → **13.16 GiB**, unassigned +27.10 → +19.59 GiB, prefix 스냅샷 예산 4.25(untiered) → 2.12 GiB(tiered).
  - 티어도 살아서 보고했다: `0 conversations parked from before, 0.0 GiB of 64 GiB` / `0 prefix boundaries parked from before, 0.0 GiB of 16 GiB`. 약 3,000 토큰짜리 공유 접두사로 `hits` **0 → 1**, `evictions` 0.
- **그런데 그 티어는 컨테이너 안에 있었다.** 준 경로는 `~/st-tier` 인데 랭크 컨테이너가 바인드하는 호스트 디렉터리는 **`/home/choiceoh/glm53-logs` 하나뿐**이다. 그 밖의 경로는 컨테이너의 writable layer 에 생긴다 — **부팅은 성공하고, 티어는 살아 있다고 보고하고, 그 부팅 안에서는 재사용까지 되고**, 컨테이너와 함께 전부 사라진다. 호스트 네 노드 어디에도 `~/st-tier` 는 없다. 실패가 아니라 조용한 무효다. 플릿 리스가 `~/st-fleet.lock` 에서 이미 겪었다.
- **조치 2 — 리포에 고정(이 PR, 운영자 "고정해야지").** 프로덕션의 모양이 상자 위에만 있으면 살아남지 못한다 — 오늘 하루에 세 번 그랬다(09-15 에 손으로 고친 배포 트리의 런처, 낡은 env 를 든 배포 사이클, 4 절의 잘못된 경로). 배포는 트리에서 다시 올리고, 트리는 리포다.
  - **티어** 기본값 `off` → `$MOUNTED_ROOT/st-tier`(`MOUNTED_ROOT=/home/choiceoh/glm53-logs`). `off` 는 이제 끄는 말이지 기본값이 아니다.
  - **마운트 가드**: `off` 도 아니고 그 아래도 아닌 `ST_TIER_DIR` 는 **거부한다**(exit 2).
  - **KV**: `KV_GIB=${ST_KV_GIB:-14.0}`. 전에는 env 가 비면 `--kv-gib` 를 안 넘겨 boot.py 의 24.0(vLLM 동수 비교용 단일 상자 기본값)으로 갔고, 프로덕션이 쓰던 7.0 은 **원장 항목 없이 env 에 손으로 박힌 값**이었다. 14.0 은 위에서 실제로 잰 값이다. 숫자가 아닌 값도 이제 거부한다.
  - 브래킷의 팔별 티어(`$LOGD/st-bracket-tier/...`)는 그 아래라 그대로다.
- **지금 프로덕션.** 11:09:34 에 `st-deploy-watch` 가 4347fe9d 를 배포하며 플릿을 다시 올렸고, 그 사이클은 env 를 고치기 전에 시작했으므로 배포 트리의 기본값(`--kv-gib 7.0 ... --tier-dir=`)으로 갔다. **11:09:52 부터 다시 티어 off · KV 7.0** 이다. 조치 1 의 상태는 약 3 분 살았다.
  - env 파일의 `ST_TIER_DIR` 는 아직 잘못된 경로라, 이 PR 이 배포되기 전의 다음 런치는 새 가드에 걸린다. `/home/choiceoh/glm53-logs/st-tier` 로 고치거나 지워야 한다(지우면 리포 기본값이 답이 된다).
- **검증.** 런처 스니펫 여덟 경우(기본·`off`·브래킷 경로·마운트 밖 다섯)를 Git Bash 로 직접 확인. 스니펫 테스트는 환경을 상속하지 않는 `bash` 에서는 **스킵**한다 — 윈도우의 `bash` 는 WSL 이라 여덟 경우가 전부 기본값으로 돌아오고 그중 둘은 그대로 "통과"했을 것이다. `test_engine_bootpaths` 8 통과(2 스킵); `test_st_bracket_tier_drop` 의 실패 집합은 이 상자에서 손대기 전과 같다.
- **머지 뒤 main 이 빨갰다(같은 날, 곧바로 고침).** 두 개가 떨어졌다.
  - `test_engine_fleet_ops` 가 `"--kv-gib" not in c` 를 단언하고 있었다 — 그것이 **바로 이 변경이 바꾼 사실**이다. `--kv-gib 14.0 --workspace-gib 10.5 --port` 로 고쳤다.
  - 새로 쓴 KV 테스트 자신. 블록의 끝 표지로 `$KV_GIB"` 를 줬는데 그 문자열은 **값을 검사하는 윗줄에서도 끝난다**. 블록이 `[[` 중간에서 잘려 syntax error 였다. 표지를 `KV_ARG="--kv-gib $KV_GIB"` 로 바꿨다.
- **왜 내 상자가 못 잡았나 — 스킵이 가렸다.** 스니펫 테스트는 "환경을 상속하지 않는 bash" 에서 스킵하게 해 뒀는데, 윈도우에서 그 조건이 **항상 참**이라(이름이 WSL 의 bash 로 풀린다) 이 테스트들은 이 상자에서 한 번도 돈 적이 없다. 이제 스킵하기 전에 **git 옆의 bash 를 찾는다** — 그것은 환경을 상속한다. 다섯 개가 전부 돌고 통과한다.
- **미측정.** **티어를 켠 채로 받은 실제 데네브 트래픽의 적중률.** 위 확인은 내가 만든 공유 접두사 한 쌍이고 기준선 100/17 은 티어가 꺼진 상태다. 접두사 적중은 **768 토큰 블록 경계**에서만 나므로 한 블록보다 짧은 공유 접두사는 어느 쪽이든 적중하지 않는다(이걸 모르고 잰 첫 시험이 0 적중이었다). KV 두 배의 효과도 미측정이다 — 축출의 원인이 용량이 아니었으므로 그 자체가 적중률을 올린다는 근거는 없다. 기록: `measurements/st_deneb_tier_kv_20260916/`.

### 접두사 캐시는 이미 프롬프트의 절반을 덜고 있었다 — 병목은 블록이 아니라 스냅샷이다 (2026-09-16, srv2 프로덕션, PR #1044)

- **앞 항목(PR #1042)의 지표를 정정한다.** "100 질의에 17 적중" 을 낮은 적중률로 읽었는데, **질의는 프롬프트 하나고 적중 하나가 수만 토큰을 덮는다.** 같은 부팅의 카운터: `prompt_tokens_total` **1,796,682**, `prefix_reused_tokens_total` **920,832** — **프롬프트 토큰의 51.3 %** 가 이미 재프리필 없이 나가고 있었다. 19 적중이 그 920,832 를 날랐다. 데네브가 768 블록을 공유하느냐는 질문도 그것으로 이미 답이 나와 있었다.
- **병목은 블록이 아니다.** `kv_blocks_cached` **111** / `free` 1,398, `snapshot_denials_total` **0**. PR #1042 가 "블록의 97 % 가 비어 있다" 로 든 근거는 **한가할 때 찍은 순간값**이었다 — 결론은 맞았지만 근거가 틀렸다. 진짜 근거는 이 두 줄이다.
- **스냅샷이다.** `prefix_entries` **96/96**(포화), `prefix_snapshot_self_evicts_total` **258** — 긴 프롬프트 하나가 자기 **뒤쪽** 경계를 만들려고 자기 **앞쪽** 체크포인트를 버린다. `base/prefix.py` 가 이미 그 자리에 이름을 붙여 뒀다(*"a 128K prompt: 170 against 96 … the checkpoints it throws away were still computed"*). 평균 프롬프트 12,140 토큰 ≈ 경계 16 개, 동시 행 둘.
- **`fades_total` 0 이 티어의 값어치다.** 티어가 없으면 희생자는 페이드(블록 유지·스냅샷 양보·상태는 NVMe)가 아니라 그냥 죽는다. `evictions` 1,109 가 전부 그것이다.
- **바꾼 것 — B, 한가할 때 쓴다.** 스필이 `spill_low_water`(8) 아래에서만 걸렸다. 티어를 켜면 스냅샷이 48 장이라 첫 바이트가 디스크에 닿으려면 경계가 40 개 상주해야 하고, 하루에 세 번 다시 올라가는 플릿은 거기 닿지 못한다 — `prefix_tier_entries` **0**(예산 16 GiB)이 그 결과다. 스케줄러가 계획할 것이 없으면(`nothing_to_step`) 미룰 이유가 없다. 스필은 티어 제 스레드에서 돌고 D10 이 스텝을 막지 않음을 이미 보장한다.
  - 같이 고침: `spill_end` 가 `version` 을 올린다. `spill_candidates` 는 이미 쓰인 것을 건너뛰므로 랜딩이 후보 집합을 바꾸는데 스캔은 `version` 을 믿는다 — **압박 경로에서도** 하나 쓰고 나면 무관한 변화가 생길 때까지 다음을 안 찾고 있었다.
- **바꾼 것 — A, handover 가 경계도 남긴다.** `_hand_over` 는 대화를 파킹하지만 경계는 두고 갔다. `maintain_prefix` 는 절대 기다리지 않으므로(스텝이 기다리면 안 된다) **아무도** 기다리지 않았다. `flush_prefix(deadline)` 가 기한 안에서 기다리며 쓰고 남은 것을 세어 돌려준다.
  - **SIGTERM 으로 하지 않았다.** `base/record.py` 의 핸들러는 링을 쓰고 같은 신호로 즉시 죽는다 — 그 신호의 의미가 earlyoom 이기 때문이다(D12: 여섯 번의 죽음이 전부 그것). 상자가 OOM 으로 가는 중에 NVMe 에 몇 초를 쓰는 것은 정확히 반대다. graceful 종료는 신호가 아니라 **요청**이어야 하고, 그 기구는 이미 있다 — 리스의 yield.
- **바꾼 것 — D, 티어를 켜도 96 장.** `PREFIX_SNAPSHOT_GIB` 2.125 → 4.25. 티어와 압축 캐시가 상주를 대신한다는 설계였는데 측정은 상주가 병목이라고 말한다 — 그러면 켜는 순간 병목이 반으로 준다. **이것은 "반으로 줄여도 된다"는 가정을 거부한 것이지 96 이 옳다는 측정이 아니다.** 예산(배포 트리, KV 14.0, 장치 없이): 96 장에서 KV remainder **42.94 GiB**, declared paged KV 13.16, 미할당 **+29.78 GiB**.
- **검증.** CPU 7 테스트. 새 넷(한가할 때 쓴다 / 바쁠 때는 예전대로 기다린다 / 랜딩 뒤에도 스캔이 이어진다 / 플러시가 아무도 안 기다리던 것을 쓴다)은 **변경을 되돌리면 떨어지는 것을 확인했다.** 9 모듈 336 통과; 이 상자의 실패 둘은 손대기 전과 같다.
- **미측정.** tiered-48 대 tiered-96(자르려면 재고 자른다). 프로덕션에서 티어가 차는 속도와 복원. 재기동을 건너뛴 적중률 — A·B 가 지키려는 것이고 아직 한 번도 안 지켜졌다. `stop` 은 여전히 SIGKILL 이다(`docker rm -f`; 아픈 플릿도 멈춰야 한다). 그리고 **KV 는 14.0 보다 훨씬 키울 수 있다** — remainder 42.94 GiB 에 13.16 만 선언돼 있다. 기록: `measurements/st_prefix_snapshot_bind_20260916/`.


## 2026-09-16 — KDA 투영은 bf16 이 아니라 이미 fp8 이다: 두 레인이 실제로 내는 오차 (ost-97x 5050, 부팅 없음)

**기록이고 판정이 아니다**(규칙 1·6). 오프라인 출력 상대오차이고, 서빙 tok/s·수용률·품질은 재지 않았다.
운영자 질문은 "KDA 를 W4A16+GPTQ 로 내리면 오차가 심각한가"였고, 답은 **질문의 전제가 틀렸다**는 것이다 —
`kernels/dense/DenseLinear` 은 이 두 이름을 이미 양자화해서 돌린다(≤32 행 W4A8, 그 위 FP8). 잴 것은
"내릴까"가 아니라 **지금 두 레인이 각각 무엇을 잃고 있나**였고, 그래서 그것을 쟀다.

### 설계 — 33차의 프로브 설계 오류를 양쪽으로 피한다

33차는 같은 질문에 두 번 답했고 답이 달랐다: 합성 헤시안에서 GPTQ −13%, 실덤프에서 −69~74%. 그래서 합성은
쓰지 않았다. 살아남은 실덤프 두 벌을 **서로 다른 스트림으로** 썼다 — `calib-v2-fit`(329,580 토큰)에서 교정하고
`calib-v2-heldout`(86,620 토큰)에서 채점한다. 교정 분포에서 채점하면 GPTQ 를 띄워 주고(그 반대가 33차의 오류),
다른 분포에서 채점하면 깎아내린다. 표의 in-sample 열이 heldout 열과 거의 같아 과적합이 아님을 보인다.

- **팩은 저장소의 것**이다. `engine/kernels/dense/packing.py` 를 그대로 임포트했다 — 새로 짠 패커가 아니다.
  검증: W4 RTN 가중치 오차 **8.164e-2** 로 33차의 기록(~8.3%)을 재현한다.
- **W4 열은 운영 설정**이다(`act_order=True` = `GPTQ_ACT_ORDER`, 정적 그룹). FP8 열도 `act_order=True`.
- 채점은 heldout 헤시안에서의 **기대 상대 출력오차** `sqrt(tr(D H Dᵀ)/tr(W H Wᵀ))`, D = W − deq(pack(W)).
  표본이 아니라 2차 모멘트를 직접 쓰므로 표집 잡음이 없고, 행별로 분해돼 in_proj 행군이 한 번의 곱에서 나온다.
- 채널 스무딩은 걸지 않았다 — `kernels/dense/smoothing` 의 인자는 POW2 라 W4A8·FP8 레인에서 정확히 무영향이다
  (그 파일이 45차 §23 조사 9차로 기록한 바와 같다). per-row shift 도 33차가 0 으로 판정했다.
- 잰 곳: ost-97x 의 RTX 5050(`st-engine:glm53-sm120-x86`). srv4 GPU 는 부팅이 쥐고 있었다. 플릿 큐 안 씀.

### §1 발견 — 이 "bf16" 텐서는 e4m3 값을 담은 그릇이다

FP8 오차가 4.7e-4 로 나왔다. e4m3 정밀도(~2.7%)로는 불가능한 값이라 비트를 봤다.

bf16 가수 7비트의 후행 0비트 비율(>=1..>=5). 진짜 bf16 이면 `0.500 0.250 0.125 0.062 0.031`:

| 텐서 (rank3of4) | >=1 | >=2 | >=3 | >=4 | >=5 |
|---|---|---|---|---|---|
| `L1.kda.in_proj` q/k/v 행 6144 | 0.908 | 0.878 | 0.871 | 0.869 | **0.462** |
| `L1.kda.o_proj` | 0.889 | 0.851 | 0.843 | 0.841 | 0.446 |
| `L20.kda.o_proj` | 0.881 | 0.839 | 0.830 | 0.829 | 0.438 |
| `L1.in_proj` 게이트 행 272 (beta 16·fa 128·ga 128) | 0.502 | 0.251 | 0.126 | 0.064 | 0.033 |
| `embed` / `head` (같은 파일 대조군) | 0.500 | 0.250 | 0.125 | 0.063 | 0.032 |

4비트에서 평평하다가 그 뒤 정상 반감 — **유효가수 3비트, 곧 e4m3**. 같은 파일 안의 `embed`·`head`·`in_norm`
이 교과서적 bf16 을 읽으므로 테스트 자체는 유효하다. 별도 학습된 DFlash2 드래프터도 정상 bf16 이다.

원인: 이 체크포인트(`st-glm53-nvidia-tp4-9391`, modelopt NVFP4)의 `ignore` 목록이 **전 층 `self_attn*`** 를
빼놓는다. 그래서 상류 릴리스의 fp8 가중치가 양자화를 거치지 않고 bf16 그릇에 그대로 통과했다.
`specs.py` 가 `BF` 로 선언한다고 bf16 정보가 들어 있는 게 아니다.

**게이트 272행만 진짜 bf16이다.** 체크포인트가 이미 게이트를 투영보다 높은 정밀도로 쥐고 있었고, 그래서
게이트 행의 FP8 오차가 q/k/v 행의 10배다(8.6e-4 vs 6~9e-3). 행 분할은 `3*Hl*D + Hl + 2*D`
(`specs.py:125`, `tree_decode.py:169`).

### §2 두 레인이 내는 오차 (heldout 상대 출력오차, 운영 설정)

| | 1층 in_proj | 1층 o_proj | 20층 in_proj | 20층 o_proj |
|---|---|---|---|---|
| **W4A8 — 디코드 레인(≤32행)** | 2.22e-2 | 4.91e-2 | 3.34e-2 | **6.62e-2** |
| **FP8 — 프리필 레인(>32행)** | 2.75e-3 | 5.89e-4 | 4.46e-3 | 7.43e-4 |
| (참고) W4 RTN, GPTQ 없음 | 3.60e-2 | 7.48e-2 | 4.18e-2 | 8.70e-2 |
| (참고) FP8 RTN, GPTQ 없음 | 4.94e-3 | 9.39e-4 | 5.77e-3 | 1.09e-3 |

읽기:

1. **FP8 레인은 사실상 무손실이다** — 가중치에 e4m3 이상의 정보가 없으니 잃을 것이 없다. 5.9e-4~4.5e-3.
2. **디코드의 W4 레인이 비용을 다 낸다.** GPTQ 를 쓰고도 20층 o_proj 6.6e-2 로, 33차가 합격선으로 삼은
   stock fp8 급(2.66e-2)의 2.5배다. 20층이 1층보다 나쁘고, in_proj 보다 o_proj 가 나쁘다.
3. **GPTQ 이득이 33차보다 작다(−23~−43%, 33차는 −69~74%).** 그때의 fused_qkv_a 는 4096 고유값 중 736개가
   ≈0 인 강한 축퇴 입력이었는데, KDA 입력은 전부 1e-6·max 를 넘고 참여비가 41(1층)/94(20층)다. 구조가 덜
   축퇴돼 GPTQ 가 쓸 여지가 적다. **헤시안의 축퇴 정도가 GPTQ 이득을 정한다** — 가중치가 아니라.
4. **GPTQ 는 가중치 오차를 키우면서 출력 오차를 줄인다**(그게 목적함수다). 1층 in_proj 에서
   `||D||/||W||` 는 RTN 8.16e-2 → GPTQ 1.07e-1 로 **나빠지고**, heldout 출력오차는 3.60e-2 → 2.22e-2 로
   좋아진다. FP8 도 같다(1.12e-2 → 1.46e-2 / 4.94e-3 → 2.75e-3). 가중치 오차로 팩을 고르면 거꾸로 고른다.
5. **이 두 가중치에선 plain order 가 act_order 보다 낫다** — 1층 in_proj 2.06e-2 vs 2.22e-2(−7%),
   20층 in_proj 3.22e-2 vs 3.34e-2(−3.4%), o_proj 는 1~2%. act_order 는 정적 그룹(RTN 패커의 스케일을
   걷기 전에 고정)을 쓰고 plain 은 그룹이 시작될 때 갱신된 가중치로 스케일을 다시 고른다. 45차 §23 GPU
   판정 7차가 전 가중치를 보고 act_order 를 골랐으므로 **이건 두 이름에 대한 후보이지 전역 판정이 아니다.**

### §3 안 한 것 · 함정

- **서빙 판정 없음.** tok/s·수용률·품질·한국어 미측정. 부팅 없음. 규칙 4 의 게이트를 하나도 통과시키지 않았다.
- **두 층만 잰다**(1·20층, rank3of4). 비트 패턴은 두 층 다 같지만 오차 크기는 층마다 다르다(20층이 더 나쁘다).
- **바이트 이야기로 넘어가지 말 것.** 랭크당 KDA 투영은 랭크파일에서 2,248 MiB 의 bf16 이지만 그것은
  부팅이 소비하는 형태다(`consume_weight`). 상주하는 것은 W4 팩과 FP8 팩이다. "bf16 을 fp8 로 내리면
  1.1 GiB 를 번다"는 계산은 **틀렸다** — 서빙은 이미 bf16 을 지나갔다. 남는 건 디스크·로드 시간 이야기뿐이다.
- 재현: `probes/` 아님 — 일회성 스크립트와 원시 출력은 `measurements/st_kda_pack_error_20260916/`.

### §4 다음 후보 (미실측)

- **디코드를 FP8 로**: `DenseLinear(decode_precision='fp8')` 이 이미 있고 지금은 드래프터 fc 만 쓴다
  (`draft_policy.fc_precision`). KDA 두 이름에 걸면 오차는 6.6e-2 → 7.4e-4 이지만 디코드가 읽는 바이트가
  0.5625 → 1 B/param 으로 는다. 디코드는 대역폭 포화(21차)라 **이건 순수 손익이 아니라 교환이고, 재야 한다.**
- plain order 를 이 두 이름에만 (위 §2-5).
- 게이트 272행: 유일하게 진짜 bf16 인데 지금 q/k/v 와 한 팩에 들어간다. 분리 비용은 33차·30차의 발사
  고정비 표가 말하는 대로 싸지 않다.


## 2026-09-16 — GPTQ 전문가 하이브리드는 프로덕션보다 나쁘다: PR #911 이 안 잰 head NLL 을 재니 +0.050 nats (부팅 없음)

**기록이고 서빙 판정이 아니다**(규칙 1·6). 부팅 없이, 2026-09-14 에 이미 찍힌 세 팔의 `head.jsonl` 을
짝지어 다시 읽은 것이다. 운영자 "이런거 예전에 해보지않았나" → "해보던가".

PR #911(2026-09-13 머지)은 하이브리드 레이아웃 — ModelOpt 라우팅 전문가에 **BF16 원본에서 뜬 GPTQ 코드**,
dense MLP 는 BF16, 활성 스케일은 unit — 을 채택하면서 마지막 줄을 이렇게 남겼다: *"Not measured: head NLL of
the hybrid against Red Hat at the same commit. The operator adopted the layout without that comparison."*
그 비교를 이제 했다. 도구는 srv2 `~/expert-capture/head_compare.py`(저장소 밖): 같은 코퍼스를 먹인 두 부팅의
head 로그를 **위치 단위로 짝지어**(두 부팅이 같은 타깃 토큰으로 채점한 위치만) 평균 NLL 차와 문서 단위
부트스트랩 95% CI 를 낸다.

**fit 스플릿, 108 문서 / 27,622 위치:**

| 비교 | ΔNLL | 95% CI | |
|---|---|---|---|
| N − RH | +0.03444 | [+0.01427, +0.05485] | PR #911 의 "+0.034" **정확히 재현** — 도구와 팔 신원의 검증 |
| **HY − RH** | **+0.05008** | **[+0.02920, +0.07174]** | **하이브리드가 프로덕션보다 나쁘다(0 을 안 포함)** |
| HY − N | +0.01564 | [−0.00459, +0.03707] | 하이브리드의 세 변경이 N 위에서 산 것 — **−0.034 회복은 배제된다** |

held-out(25·22 문서)은 부호가 같고 CI 가 0 을 걸친다 — 문서 수가 적어 검정력이 없다. 팔은 RH =
`st-glm53-9391-up-gate-full`(`eval-A`, 커밋 `e032a600`), N = `st-glm53-nvidia-tp4-9391`, HY =
`st-glm53-hybrid-gptq-v1`. 빈 `eval-RH` 디렉터리와 onepass 만 돌린 `rh-main.log` 가 PR 이 "Not measured"
라고 쓴 이유이고, **`eval-A` 가 곧 프로덕션 팔**이라는 것은 위 재현이 말한다.

**커밋 신원 (2026-09-16 확인):** RH(`eval-A`)와 N(`eval-N`)은 **같은 커밋 `e032a6005824`** 다 — A−N 은
랭크만 바뀐 동일 커밋 비교이고, 그래서 PR #911 의 +0.034 는 깨끗한 주장이었다. HY 는 2026-09-14 05:48 에
돌았는데 #911 은 07:57 에 머지됐으므로 **HY 는 #911 브랜치 빌드**다(그 코드 없이는 하이브리드 랭크가 아예
안 읽힌다 — PR 의 "a commit without this layout cannot boot the hybrid ranks"). 그 diff 는 레이아웃·로더
배관이라 이미 실린 텐서의 산수는 안 바꾸지만, 같은 빌드는 아니다. 9월 14일 브래킷 디렉터리는 순환돼
HY 의 정확한 커밋은 복구되지 않는다.

**읽기:**

1. **하이브리드는 자기가 고치려던 것보다 나쁘다.** N 의 +0.034 를 BF16 dense 로 되돌리려 만든 레이아웃인데
   +0.050 이 나왔다. **HY−N 이 이 진단을 직접 기각한다**: #911 의 가설("N 의 손해는 NVFP4 dense MLP 와 정적
   활성 스케일 탓")이 맞았다면 HY−N 은 −0.034 근처여야 하는데 +0.01564 [−0.00459, +0.03707] 이다. CI 폭이
   0.042 라 0 과 +0.03 은 못 가르지만 **−0.034 만한 회복은 하한 −0.005 아래라 확실히 배제된다.** 처방이
   원인을 못 짚었다는 뜻이다. HY−N 에는 GPTQ 전문가(10.9% vs 14.6%)도 같이 들어 있어 음수로 밀었어야 하는데
   그것도 안 보인다.
2. **전문가 가중치 충실도는 2배 좋아졌는데도 그렇다.** PR #911 의 held-out 전문가 출력오차는 RH 21.9% →
   ModelOpt 14.6% → **GPTQ from BF16 10.9%** 였다. 전문가 오차를 절반으로 줄인 것이 예측을 더 나쁘게 만들었다.
   **오프라인 가중치 오차에서 서빙 이득으로 가는 추론이 여기서 정면으로 깨진다** — 같은 날 KDA 항목의
   오차표를 읽을 때도 이 선을 넘지 말 것.
3. **차이는 몸통이지 head 양자화가 아니다.** 각 부팅의 자기 hidden state 에 BF16 head 를 씌운 열이 서빙 열과
   소수점 다섯 자리까지 같다(+0.05011 vs +0.05008).
4. **그래서 전환 안 한 것이 옳았다.** PR 의 "Production switch" 는 `RANKS_DIR` 을 하이브리드로 바꾸라고 했지만
   `~/.config/st-glm53.env` 에는 `ST_KV_GIB=7.0` 한 줄뿐이고, 지금 부팅도 `--ranks st-glm53-9391-up-gate-full`
   이다. 스위치는 넘어간 적이 없다.

**안 한 것 · 함정:** **커밋이 다르다** — `eval-A` 는 `e032a600`, HY 는 그 env 의 릴리스다. PR 이 말한 "at the
same commit" 통제는 **여전히 없고**, 커밋 효과를 배제하지 못한다. 하이브리드는 변경 셋(GPTQ 전문가 + BF16
dense + unit 활성)을 묶고 있어 어느 것이 nats 를 쓰는지 이 비교는 못 가른다. 도구가 문서 번호 어긋남을
경고한다(133 vs 174 vs 186) — 짝짓기는 같은 타깃 토큰을 요구해 막혀 있고 fit 은 세 비교 모두 108 문서 /
27,622 위치로 같은 자리에 떨어진다. tok/s·수용률·품질은 PR #911 에 있다(수용률·tokens per step 동일, 속도차
−0.8%/+0.4% 는 부팅 CV 1.7% 안, reasoning 6/9 → 5/9). 원시 출력·재현:
`measurements/st_hybrid_head_nll_20260916/`.

**남은 질문(미실측):** 같은 커밋에서 랭크만 바꾼 통제. 그리고 하이브리드의 세 변경 중 어느 것이 손해인지 —
unit 활성 스케일만 되돌린 팔이 제일 싼 다음 수다(`probes/glm53_hybrid_rank.py --unit-activation` 를 빼고 한 랭크만
다시 만들면 된다).

## 2026-09-16 — 스테일 헤시안은 헤시안이 없느니만 못하다: GPTQ 위의 레버 넷 기각, 그리고 블롭에 가중치 신원 (부팅 없음)

**기록 + 코드.** 오프라인 프로브(층-1 KDA 실가중치·실헤시안, ost-97x)이고 서빙 판정이 아니다. 운영자
"gptq보다 더 나은 방식 적용은 어때" → "반올림도 더 잘하고 교정도 더 잘해버려" → "교정을 맞춰봐".

### §1 GPTQ 위에 얹을 게 없다

o_proj 기준 오늘 잰 것 전부:

| 레버 | 효과 | 비용 |
|---|---|---|
| 레인/포맷 (W4 → FP8) | **80×** (4.91e-2 → 5.89e-4) | 디코드 바이트 1.8배 |
| **교정 일치** (스테일 → 맞음) | **1.8×** (8.95e-2 → 5.01e-2) | 교정 부팅 1회, 런타임 0 |
| GPTQ (RTN 대비) | 1.5× | 팩 시간만 |
| LoRC r=64 | 1.05× | 바이트 +17% |
| 댐핑 튜닝 | 1.00× | 0 |
| 스케일 목적함수 | 0.98% (**손해**) | 0 |

기각 사유: **스케일 탐색을 헤시안 가중으로** 바꾸면 RTN 은 −4~13% 좋아지는데 GPTQ 위에서는 +2% 손해다 —
둘은 보완재가 아니라 **대체재**이고 GPTQ 가 더 세다. **댐핑·수축**으로 스테일을 때우는 것은 o_proj 격차의
~10%, in_proj 는 0 — 조건수가 아니라 진짜 분포 불일치다. **저랭크 보정**은 GPTQ 위에서 −1~5%(바이트 +2~17%)로,
**33차의 −37~66% 가 재현되지 않는다**: 그 표는 랭크 64 상관이 내장된 합성 헤시안이라 잔차가 설계상 저랭크였고,
실헤시안에서는 o_proj 의 LoRC 가 오히려 RTN 위에서 더 먹혀 **비대칭의 방향이 뒤집힌다**. **percdamp** 는
25배 범위에서 0.5% 변동, 현행 1% 가 사실상 최적.

**교훈(오늘 세 번째):** 합성 헤시안은 구조 활용 기법을 과대평가한다.

### §2 그래서 남은 하나 — 그리고 코드가 됐다

스테일 블롭만 가진 노드가 지을 수 있는 팩 전부에서 **헤시안을 아예 안 쓰는 RTN 이 이긴다**:

| 스테일 rh-era(9/12) 헤시안만 | in_proj | o_proj |
|---|---|---|
| **RTN, 헤시안 안 씀** | **3.60e-2** | **7.48e-2** |
| RTN, 스테일 가중 스케일 | 4.52e-2 | 8.58e-2 |
| GPTQ act_order, 스테일 | 3.97e-2 | 8.95e-2 |

교정 출처가 어긋나면 W4 +43~60%, FP8 +56~114%다. 그런데 `calibration_path` 는 블롭을 **이름으로만** 키잡고
`calibrated()` 는 파일 존재 여부였다 — 체크포인트를 바꾼 부팅이 조용히 RTN 보다 나쁜 팩을 짓는다.
2026-09-14 eval 팔 셋이 정확히 그걸 했다(9/12 블롭 공유).

**수정:** 블롭에 `weights_id`(이 부팅이 서빙한 가중치, glm53 은 `F.weight_layout`)를 달고, 안 맞으면
교정이 없는 것으로 읽어 RTN 으로 물러선다 — `store.fits_weights`, `calibrated()`·`missing_calibration()`·
`_calibration_sha()` 세 경로 모두에. **필드가 없는 옛 블롭은 아무 주장도 안 하므로 그대로 받는다**(플릿의
203개 × 4랭크가 계속 쓰이고 다음 교정이 도장을 찍는다). 헤시안을 유지한 채 피크만 다시 합산하는 타일은
**그 헤시안의 id 를 지킨다** — 필드는 합을 설명하지 그 파일을 마지막에 만진 부팅을 설명하지 않는다.
부팅은 `calibration_foreign` 게이지와 로그 줄로 몇 개를 거부했는지 말한다(규칙 5).

### §3 안 한 것

층 1 두 사이트만(20층은 더 나쁘다). **9/12 블롭이 RH 전문가 아래 합산됐다는 것은 날짜·정황 추론**이고, 두
헤시안은 코퍼스·트래픽도 다를 수 있어 "교정 출처가 중요하다" 는 측정이지 "전문가 체제가 원인" 이라는 증명이
아니다. rh-era 헤시안은 조건수가 나빠 댐핑 사다리가 올라갔고(`held at damping 10%`) 손해의 일부는 그 탓일 수
있다. **서빙 판정 없음** — 그리고 같은 날 하이브리드 항목의 경고가 그대로 적용된다: 더 좋은 팩이 더 좋은
모델을 보장하지 않는다. CPU 테스트: 변경이 닿는 12 모듈 105 테스트 OK(리눅스 컨테이너), 신규 3.
원시 출력·재현: `measurements/st_pack_levers_20260916/`.
### 가장 많이 공유되는 경계가 디스크에 갈 수 없었다 (2026-09-16, 재현, PR #1046)

- **재현.** 공유 프리픽스 뒤로 갈라지는 대화 둘(BLOCK=4): A 의 경계는 `[4, 8, 12, 16, 20]`, 리프는 `20` 하나, **티어에 간 것도 `20` 하나**. 8 에서 갈라지는 B 가 티어에서 찾는 것은 **None**.
- **판정.** `is_leaf` 는 `children == 0` 이다. 공유 시스템 프롬프트 경계는 **누군가 이어 쓰는 순간 리프가 아니게 되고**, 그때부터 영원히 티어에 못 간다. `_victim` 이 그것을 메모리에서 가장 늦게 버리도록 이미 보호하는데(*"one long prompt's forty fresh boundaries cannot flush the system prompt every conversation shares"*) 디스크로는 내보낼 수가 없었다 — 09-16 하루 세 번의 재기동이 그것을 가져갔다.
  - 리프만 보낸 이유는 **블록**이었다(*"an inner boundary would bring the same ones"*). 맞는 말이고 요점이 아니다: 귀한 것은 스냅샷이고(`denials` 0, `cached` 111/1,398, `self_evicts` 258), **갈라지는 체인은 리프를 아예 못 쓴다** — 갈라지는 자리의 상태가 필요하다.
- **고침 1.** `spill_candidates` 가 리프 **또는 `hits > 0` 인 내부 경계**를 내놓는다. `hits` 는 캐시가 이미 세는 값이고(`lookup_chain` 이 채택할 때 올린다) 다른 프롬프트가 실제로 거기서 시작했다는 뜻이라, 중복이 그 값을 치른 경계로만 한정된다. 아무도 시작한 적 없는 내부 경계는 예전 규칙 그대로 제외된다. 재현 뒤: B 가 8 을 채택하면 티어가 `[8, 20]` 이 되고, 메모리가 8 을 잃은 뒤 셋째 대화가 티어에서 `(8, …)` 을 찾는다.
- **고침 2 — 부팅 뒤 아무것도 캐시를 데우지 않았다.** `POST /v1/prefix/warm` 과 `probes/st_prefix_warm.py` 는 처음부터 있었고 **부르는 데가 없었다.** 슈퍼바이저가 `wait_for_health` 성공 뒤에(문만 열린 게 아니라 채팅이 답한 뒤) `--pin` 으로 부른다. 워밍 실패는 부팅 실패가 아니다.
  - `ST_WARM_FILE` 기본 `/home/choiceoh/glm53-logs/st-warm.jsonl`, 없으면 조용히 건너뜀. **빈 값은 끈다** — `${x-d}` 이지 `${x:-d}` 가 아니다(같은 날 티어에서 배운 함정, PR #1042).
  - **워밍이 대화를 남기면 안 된다.** `/v1/prefix/warm` 은 불린 적이 없어 드러나지 않았는데, 그 요청은 보통 턴처럼 처리되고 쓸모 있는 워밍 프롬프트는 한 블록 이상 — `PARK_MIN_TOKENS = 128` 을 한참 넘는다. 그대로 두면 **부팅마다 워밍 프롬프트 수만큼 파킹된 대화**가 생겨 슬롯 상태 통째(~260 MiB)가 64 GiB 대화 티어에 쓰이고 진짜 대화를 LRU 에서 밀어낸다(09-13 의 17 토큰 헬스 핑과 같은 일이 30 초마다가 아니라 부팅마다). `retain: false` 로는 못 고친다 — 그 플래그는 행을 놓아주는 것과 `runner.transient` 로 **경계를 티어에 못 가게 하는 것**을 같이 하고, 워밍은 앞만 원한다. 축을 둘로 갈랐다(`_warm`).
  - `warm_cache` 만 Git Bash 로 떼어내 네 경우를 확인했고, 그 확인이 버그를 잡았다: 처음 쓴 것은 `$what` 을 인자로 안 받고 호출자의 `local` 에 기대고 있었다. 슈퍼바이저는 `set -u` 다.
- **검증.** CPU 새 테스트 다섯. 공유 경계 둘은 **예전 규칙으로 되돌리면 떨어지는 것을 확인**했다. 7 모듈 364 테스트에서 이 상자의 실패 집합은 손대기 전과 같다(환경).
- **미측정.** **워밍 파일의 내용** — 무엇을 데울지는 데네브가 보내는 것을 아는 사람의 결정이라 짐작하지 않았다. 파일이 생기기 전까지 고침 2 는 아무 일도 안 한다. **티어 예산 16 / 64** — 경계 몫은 페이드가 드물다는 전제인데 `fades_total` 은 아직 0 이라 티어가 도는 것을 보고 정한다. **프로덕션 효과** — `prefix_tier_entries`·`prefix_tier_restores_total`·`prefix_cache_fades_total` 이 답하고 지금 셋 다 0 이다. 기록: `measurements/st_shared_boundary_20260916/`.
### 하이브리드 팔은 지금 부팅이 안 된다 — #911 의 판정을 갱신하지 못했다 (2026-09-16, 격리 부팅 2회)

- **하려던 것.** #911 하이브리드를 **재교정한 블롭 위에서** head NLL 로 다시 재기. 같은 sha
  `a0e9221cb`(= `origin/main` + `EXPERT_CAPTURE = True`), 같은 레인 `t,r,sf6,batch,q0`, 같은 창으로
  두 팔을 띄웠다.
- **팔 A (프로덕션 랭크, `st-glm53-b12x-up-gate-v1`).** 15:20:21 문 열림, 코퍼스 133 대화 ·
  417,189 프롬프트 토큰 · 204 s, `head.jsonl` 133 행. 남겼다: `~/glm53-logs/headnll2/armA.head.jsonl`.
- **팔 B (하이브리드 랭크, `st-glm53-modelopt-up-gate-bf16-dense-v1`).** 부팅 중 죽음 —
  `ValueError: private scatter requires packed FP32 output without split work`
  (`moe_static_kernel_v4.py:159`, `warmup_decode_experts` 에서). **비교 대상이 없다.**
- **어디서.** 디코드 그래프 캡처는 행마다 레인을 둘 세운다 — sf6 쓰는 것과 안 쓰는 동반 레인(`q0`).
  m=1·8·64 는 둘 다 섰고 **m=16 의 동반 레인에서** 죽었다. m=16 에서만 `batch` 셀이
  `direct_scatter` 를 켜는데(`_static_v2_config_for`: `reform and m == 16 and batch_reform and
  c2_direct_scatter`), 커널 생성자는 `direct_scatter` 에 `reform_sf_pack` 을 요구한다 —
  동반 레인엔 그게 없다. #1054 의 `mk_use_compact_m8` 과 같은 모양이다: 한 셀에서 두 계약이 어긋난다.
- **안 가린 것.** 팔 B 는 팔 A 와 **두 가지**가 다르다 — 하이브리드 랭크인 것, 그리고 **교정
  부팅**인 것(`collecting 0/131072 rows over 176 blobs`). #1045 가드가 하이브리드에게 프로덕션
  블롭을 미교정으로 읽히니 하이브리드는 교정 부팅으로 시작할 수밖에 없고, `calibration` 은 레인
  아이덴티티의 일부다(부팅 로그 memory gate 줄). **이 관측 하나로 원인을 하이브리드 기하로 돌릴 수
  없다.** 가리는 실험은 한 부팅 — 프로덕션 랭크 + 교정 부팅을 이 sha 로. 오늘은 안 했다.
- **따라서.** #911 하이브리드의 2026-09-14 판정(+0.050 nats, 같은 날 항목)은 **오늘 갱신되지 않았다.**
  재교정 블롭 위에서 다시 재려면 위 결함을 먼저 넘어야 한다.
- **프로덕션.** 팔 B 가 자기 블롭을 합산해야 해서(레이아웃이 달라 가드가 거부) 네 랭크의
  `mkcalib/rank$R` 을 `.prod-0916` 으로 치웠다가 되돌렸다 — 476 개씩, 오늘 재교정본 그대로.
  팔 B 는 교정에 도달하지 못해 **아무것도 덮지 않았다**(scratch 0 블롭). 원시 출력:
  `measurements/st_hybrid_boot_block_20260916/`.
### 하이브리드는 자기 교정을 새로 떠줘도 더 나쁘다 — 그리고 더 충실한 팩이 더 나쁜 점수를 냈다 (2026-09-16, 격리 부팅 4회)

- **닫은 반론.** 09-14 판정(+0.050 nats)의 헤시안은 하이브리드의 것이 아니었다 — 세 팔이 한 블롭
  세트를 나눠 썼다. 그래서 하이브리드에게 **자기 가중치 위에서 새로 합산한 235 개 블롭**을 주고
  다시 쟀다. 두 팔 모두 같은 커밋 `cc7abde71`(= main `ad87ebb73` + 캡처), 같은 레인
  `t,r,sf6,batch,q0`, 같은 코퍼스(133 대화 · 417,189 토큰). 양쪽 다 교정 중이 아니었다 —
  `POST /v1/engine/calibration` 이 `404 every pack it serves was already calibrated`, `foreign`/`RTN` 0.
- **판정.** 하이브리드가 더 나쁘고 폭은 09-14 보다 **커졌다**:

  | split | pos | nll A | nll B | B−A | 95% CI |
  |---|---|---|---|---|---|
  | fit | 27,631 | 2.78264 | 2.84694 | **+0.06430** | [+0.03921, +0.09066] |
  | heldout | 6,400 | 2.99928 | 3.13285 | **+0.13357** | [+0.07241, +0.20504] |
  | heldout-no-onepass | 5,632 | 2.99250 | 3.09967 | **+0.10717** | [+0.05324, +0.16868] |

- **차이는 몸통이다.** 각 부팅의 **자기 은닉상태 위 bf16 헤드**로 채점한 열이 같은 폭으로 벌어진다
  (fit +0.06502). 헤드 양자화가 아니라 은닉상태가 나쁘다. 그리고 서빙 헤드가 **같은 부팅의 자기
  bf16** 에 대해 갖는 KL 은 하이브리드가 더 작다(4.91e-04 vs 5.78e-04) — **원본과의 거리가 아니라**
  각 팔이 자기 비양자화 버전에 가까운 정도이고, 둘은 서로 다른 것에 대고 잰다. 하이브리드의 양자화는
  자기 자신에게 더 충실하고 그 자기 자신이 더 나쁘다. 원본 대비 거리는 이 캠페인이 재지 않았다.
  A 와 B 의 상호 유사도도 이 표에 없다 — `nll A`/`nll B` 는 각 팔이 **정답 토큰**에 매기는 NLL 이다.
- **마지막 59 개를 마저 교정하자 오히려 나빠졌다.** 235 중 176 은 첫 교정 부팅에 적재됐고 59 는
  예산에 밀려 미뤄졌다. 그 상태(`B2partial`)는 fit +0.05591, 59 를 채운 뒤는 +0.06430;
  둘의 직접 비교는 **+0.00839 [+0.00273, +0.01479]** — 교정이 점수를 깎았고 KL 은 같이 내려갔다
  (6.76e-04 → 4.91e-04). 그 59 는 34,164 행, 첫 배치는 137,163 행에서 적재됐다. 같은 날
  `st_pack_levers_20260916` 의 **얇은 헤시안은 없느니만 못하다**와 같은 방향. 판정은 어느 쪽이든 같다.
- **여기까지 오는 데.** 하이브리드는 부팅 자체가 안 됐다(#1055) — sf6 없는 동반 레인이 m=16 에서
  직접 스캐터를 요구받아 죽었고, #1056 이 그 한 자리를 고쳤다. 교정도 한 번 막혔다: 캡처 피드는
  `max_tokens=1` 이라 프리필만 돌고 드래프터는 디코드에서만 쓰여 한 행도 못 받는데, `progress()` 가
  전 블롭의 **최소값**이라 그 0 이 적재를 막았다. 디코드 트래픽으로 풀었다.
- **그리고 오늘의 재교정이 프로덕션에 사준 것: 이 해상도에서는 아무것도.** 09-13 의 `eval-A`
  (옛 17,189 행 블롭)와 오늘의 팔 A 를 붙이면 fit +0.00800 [-0.02014, +0.03569],
  heldout -0.02500 [-0.06834, +0.02061], heldout-no-onepass -0.02307 [-0.05921, +0.01312] —
  **셋 다 0 을 포함하고 부호도 엇갈린다.** 다만 겹치는 위치가 fit 4,171 개뿐이라(A vs B 는 27,631)
  CI 반폭이 ±0.028 nats 다: **0.03 nats 미만은 이 비교로 볼 수 없다**("효과 없음"이 아니라
  "안 잡힘"). 두 부팅 사이에는 이틀치 엔진 커밋도 있다. 하이브리드의 +0.064 는 이 잡음대를
  한참 벗어나므로 위 판정은 흔들리지 않는다.
- **"질이냐 양이냐" 는 갈래가 아니었고, 둘 다 헤드에 안 닿았다.** 오늘 재교정은 두께(17,189 →
  131K)와 출처(현재 가중치 위 합산)를 **동시에** 바꿨으므로 위 비교로는 못 가른다. 같은 날
  가중치오차로 잰 두 레버는 **출처 1.8×**(`st_pack_levers`)와 **두께 1.5~1.6×**
  (`st_site_lane_table`)로 비슷한 크기다. 그리고 **얇은 블롭은 스테일 블롭과 같은 방식으로**
  실패한다 — 17K GPTQ 가 RTN 보다 나쁜 사이트가 나온다(L11.mla.qkv_a 6.109e-02 vs RTN 5.517e-02;
  L7.mla.qkv_a 7.357e-02 vs 6.680e-02), 아침의 8.95e-2 vs 7.48e-2 와 같은 증상이다. 그런데 오늘의
  재교정은 **그 둘을 다 고치고도** head NLL 을 못 움직였다: **가중치오차 1.5~1.8× 가 헤드에서
  0.03 nats 를 만들지 못한다.** 아침의 팩 오차 숫자는 전부 상한이었고(규칙 6) 이 부팅이 그 상한의
  비전파를 보였다. 단서 — 그 오차들은 층 1 KDA / 24 사이트이고 프로덕션은 476 사이트다.
- **안 잰 것.** **속도** — #1056 은 sf6 없는 동반 레인에서 직접 스캐터를 끄므로(프로덕션은 동반
  레인이 없어 영향 0) 이 팔로 #911 의 속도 주장은 재검증할 수 없다. **드래프터 블롭 두께가 비대칭**
  (A 는 프로덕션 재교정본, B 는 5,380 행) — 캡처는 프리필만 기록해 이 표엔 안 들어오지만 대칭은
  아니다. **서빙 판정·품질 게이트 없음.** 원시 출력:
  `measurements/st_hybrid_recalibrated_head_nll_20260916/`, 캡처 원본은 플릿 `~/glm53-logs/headnll3/`.
### C=2 손실의 89%는 MoE 하나고, 그 비용은 행이 아니라 전문가 수에 붙는다 (2026-09-16, 프로덕션 옆, 부팅 없음)

- **어떻게.** `POST /v1/engine/profile` 은 레이턴시 레코딩과 달리 서버를 예약하지 않아(409 없음)
  라이브 문 옆에서 돈다. 요청 하나/동시 둘로 각각 24 스텝을 프로파일했다. CUPTI 합이라 절대값은
  PDL 겹침으로 과대하고, 유효한 것은 한 실행 안의 비율이다.
- **판정.** 스텝당 디바이스 시간 **C=1 59,301 µs → C=2 80,342 µs (1.355×)**. 증가분 21,047 µs 의
  분해:

  | 커널 | C=1 | C=2 | 배수 | 증가 |
  |---|---:|---:|---:|---:|
  | **MoE static v5** | 25,212 | 44,027 | **1.75** | **18,815 (89%)** |
  | dense rows16 (C=2 전용) | 0 | 6,111 | — | 6,111 |
  | mHC | 4,616 | 6,241 | 1.35 | 1,625 |
  | input pack | 1,969 | 3,309 | 1.68 | 1,340 |
  | one-shot AR | 3,000 | 3,949 | **1.32** | 949 |
  | k_publish_packets | 1,864 | 2,157 | **1.16** | 293 |

  통신과 패킷은 **행당으로 싸진다**(one-shot 8행 4.15 → 16행 2.87 µs/행). 원장의 C=2 분해가
  MoE·통신·mHC 를 나란히 놓았는데 **실제로는 MoE 가 압도한다.** 그리고 `dense rows16` 이 C=2 에만
  나타나는 것은 밀집의 열여섯 행 경로가 **실제로 서빙 중**이라는 확인이다.
- **기계장치(커널의 회계 그대로).** `moe_static_kernel_v4` 의 라우팅 단계는
  `if row % tile_m == 0: next_item += output_tile_count_n` 로 작업을 센다(tile_m=32 고정).
  디코드는 전문가당 행이 ~1개라 **항목 수 = 서로 다른 전문가 수 × n블록** 이고, 전문가를 하나
  건드리면 32행 m-타일이 통째로 선다(행 슬롯의 1/32 만 실토큰).
- **모델과 대조.** 균일 라우팅에서 슬롯 s 의 distinct 는 `288(1−e^(−s/288))`:
  C=1 64 슬롯 → 57.4, C=2 128 → 103.4, **비율 1.80** (모델) 대 **1.75** (측정). 웨이브 양자화까지
  넣으면(mac=48) 1.67 이라 측정이 둘 사이에 앉는다. 850차의 옛 C=4 기록(rows1 47.7 → rows4
  110.8 ms, 배수 1.72)도 같은 식으로 설명된다(K=6: distinct 50.7 → 155.8, MoE 배수 1.30).
- **따라서 닫힌 것.** 라우팅 인지 배칭·핫 전문가 상주는 붙잡을 구조가 없다 — 쏠린 분포였다면
  배수가 1.0 쪽이어야 하는데 1.75 라, **디코드 규모의 실 라우팅은 거의 균일하다**(추론). 타일을
  여러 전문가가 나눠 쓰는 것도 불가능하다(m-타일은 전문가 하나의 가중치를 읽는다). 폭을 올려
  포화시키는 것도 C=4 가 슬롯 256 으로 59% 포화라 닫혀 있다.
- **남는 것.** 전부 라우팅 쪽이다 — 디코드 top-k 축소(top-8→4 면 MoE ≈0.53×, 전체 −20%; 모델),
  전문가당 가중치 축소. 앞의 것은 모델 변경이고 품질 비용은 오늘 만든 head NLL 채널로 잰다.
- **안 잰 것.** 라우팅 분포 실측(오늘 캡처는 `"on": {"moe": false, "head": true}` 라 `ids/` 가 비었다),
  top-4 팔, 서빙 판정. 문서: `engine/CONCURRENCY_WIDTH_20260916.md`, 원시 출력:
  `measurements/st_concurrency_width_20260916/`.
### 드래프터 FC 보정은 없앨 것이 실재한다 — FP8 오차가 등방보다 113배 DC 정렬돼 있다 (2026-09-16, 부팅 없음)

- **왜 봤나.** 프로덕션 `lane_info` 가 `draft_fc_bias="False"`, `draft_fc_bias_auto="True"`,
  **`draft_fc_bias_status="missing"`** 이다. `draft-fc-bias.json` 이 플릿 어디에도 없고
  (`find /home/choiceoh -maxdepth 4`), 부팅은 없어도 그냥 넘어간다
  (*"absent/stale caches never block boot"*). 원장에 항목도 없다 — **무장돼 있고 한 번도 안 돈 것.**
- **무엇이 빠졌나.** 피터는 있다(`bench/draft_fc_bias.collect_fc_pairs`, `bench/draft_tune.py fc-bias`).
  엔진은 산출물을 읽을 줄 안다. **없는 것은 쌍을 모으는 구동부다** — `collect_fc_pairs` 는
  호출자가 없다. 좋은 소식 하나: 부팅이 `consume_weights` 를 안 넘기므로 프로덕션의 드래프터는
  `p['fc.weight']` 를 **이미 retain** 한다. 수집용 특수 부팅은 필요 없다.
- **왜 오프라인으로 바로 못 재나.** 보정은 `mean over rows of (actual − reference)` 이고 그 평균은
  `(W_q − W)·μ` 다. μ 는 aux 입력의 평균이고 그것은 (아직 없는) 수집만이 준다. 무작위 입력에서는
  대칭으로 0 이 되고, 교정 헤시안은 `X^T X`(2차)라 μ(1차)를 안 준다.
- **그래서 잰 것 — μ 없이 가능한 두 가지.** 드래프터 `fc.weight` [4096, 20480] 을 리포의 FP8
  양자화로 왕복시켰다:

  | | 값 |
  |---|---|
  | 행별 상대 오차 `‖dW‖/‖W‖` | median **2.66e-02** (p99 2.70e-02, 매우 균일) |
  | **DC 결합** `\|Σ_j dW_ij\| / \|Σ_j W_ij\|` | median **2.11e-02** (p90 1.27e-01) |
  | 등방 기대값 (무작위 오차라면) | **1.86e-04** |

  **DC 결합이 등방 기대값의 113배다.** 양자화 오차가 무작위였다면 균일 평균 입력에 대한 결합이
  `√k` 로 깎여 0.019% 여야 하는데 2.1% 가 나온다. 즉 **오차에 강한 계통 성분이 있고, 그것이 바로
  채널별 평균 보정이 없애는 것이다.** 기전: e4m3 는 가수가 짧고 블록 최대값 스케일을 쓰므로
  대부분의 값이 상대오차가 큰 낮은 구간에 앉고, 그 구간의 반올림이 한쪽으로 끌린다.
- **판정.** 게이트 통과 — 수집 구동부를 지을 근거가 된다. **크기는 여전히 미측정이다**: 이것은
  가중치 양자화만이고 리더는 활성값도 양자화하며 norm 을 접는다. 그리고 이 통계는 μ 의 **균일
  성분**에 대한 결합이지 `Σ_j dW_ij μ_j` 자체가 아니다. p99 이상(1.24, max 106)은 `Σ_j W_ij` 가
  상쇄로 0 근처인 행의 분모 인공물이다.
- **왜 중요한가.** 수용률은 온도에 의존하지만(같은 날 항목: 벤치 t=0 에서 acc 0.538, 프로덕션 t=1
  에서 0.285) **드래프터가 자기 BF16 자신에 가까워지는 것은 온도와 무관하다.** 지금까지 나온 레버
  중 tokens/step 을 실제 트래픽에서 직접 올릴 수 있는 유일한 것이다.
  원시 출력: `measurements/st_draft_fc_bias_gate_20260916/`.

### C=2 스텝을 두 요청 스레드로 나누면 — 페널티 8~23 ms 대 숨길 몫 ≤3.8 ms, S1·S2 기각 (2026-09-16, srv4 단일 GPU 레인 1회, 운영자 "c=2 동시성 배수를 개선할만한 아키텍처 개혁")

운영자 인터뷰로 고정한 목적은 bench-dec 고정 길이 팔의 **C=2 합산 디코드 tok/s**(T2 단축; 제약 C=1 비회귀·D10), 허용 레버는
L1(요청별 그룹으로 교환 숨김)·L4(고정비), S2(스텝 디커플)는 열어 둠, 적응 K·전문가 정밀도는 범위 밖. 운영자의 비유(1코어
1스레드 대 2스레드 CPU)를 대응시키면 채울 스톨은 락스텝인 두 요청이 같이 기다리는 패킷 비행과 스텝 꼬리다. 그걸 채우려면
두 요청을 어긋나게 해야 하고, 그 값을 한 랭크에서 쟀다(`measurements/st_c2_threads_20260916/`, PR 별도).
- **숨길 상한.** 프로덕션 원샷 전송의 집합통신당 지연 셀: 8행 31.7 µs, **16행 45.5 µs**(그중 피어 대기 38.7, 19차), C=2 스텝의
  패킷 교환 ~83회 → S1 양쪽 **≤3.8 ms**, 어텐션 쪽만 ≤2 ms. 대기는 `k_publish_packets` 안에 있어(소비자가 아니라 발행
  커널이 피어를 기다린다) 한 스트림에서 숨기려면 발행/대기 분리 + 2개 비행(링 4칸)이 먼저다.
- **페널티(rank 3, 항등 집합통신, 합성 라우팅, 기본 실행계획, 2K, K=7, 16회 중앙값).** joint16 **55.10** ms, solo8 39.51,
  split_attn(어텐션만 요청별, FFN 합동) **63.23 (+8.1)**, split_both(`execution.decode_overlap` 에 (0,1),(1,2) 그룹) **77.64 (+22.5)**,
  2·T1−T2 = **+23.9**. 두 분할 팔은 CPU 참조 레인에서 배치 forward 와 비트 동일(`tests/test_probe_decode_threads.py`).
  16행 그래프 갭 0.97 ms. 고유 전문가/층 배치 48.2 대 스레드 31.4+31.7 = 63.1(1.31배).
- **페널티의 정체는 바이트가 아니라 런치 바닥이다.** 어텐션 분할은 16행 `mk_gemm2<2>`(196회 7.9 ms) 대신 8행 전용 커널
  두 벌(`mk_gemm_input_cta<1>` 5.8 + `mk_gemm2<1>` 3.9 + pack 1.2 + mhc_ar 1.8 ms)을 낸다. dense 팩은 스텝당 ~1.2 GB 라 바닥
  4.4 ms 인데 16행 레인이 12.95 ms — 이 GEMM 들은 지연에 묶여 있다(단독: 0.5 MiB `kda.f_b` 74 µs = 7 GB/s). L2 가 둘째 패스를
  받아 줘도 둘째 런치는 남는다. 양쪽 분할은 거기에 MoE 합집합 분리(+9.9)를 얹어 스텝 둘을 따로 도는 값과 같다. 런치
  1,237 → 2,321 → 2,682.
- **판정.** 미리 정한 규칙 넷(S1 어텐션 ≤1 ms, S1 양쪽 ≤2.5, S2 2·T1−T2 ≤8, L4 캡처 갭 ≥2) 전부 큰 폭으로 실패. **S1 기각,
  S2 종이에 남김, 체인 가운데 캡처 착수 안 함.** S3 `early_observe` 는 옮겨지는 것이 fc 투영(0.5 ms/스텝)뿐이라 브래킷 값어치
  없음. **요청·스텝 구조로 열리는 문은 없다** — 합동 16행 배치가 이 대역폭 벽 위의 바른 SMT 다.
- **남는 지도(전부 커널·런치 수준, 목적에는 닿는다).** MoE 스트리밍 178~204 → 250 GB/s(−4~−7 ms), 소형 dense GEMM 을 층 안에서
  한 격자로(f_b·g_b·in_proj / q_b·wq_b; 바이트 동일 접기, −2~−4), 16행 mHC 점유율(−1~−1.5), 라우터 logits+top-8 한 런치(−1,
  FP32 순서 → GPU 검증), 발행 대기 중 L2 프리페치(`HintArgs` 미배선, 11차 미측정, ≤1.5).
- **한계.** 한 랭크·기본 실행계획이라 절대 레인 시간은 프로덕션과 다르다(KDA 3.2 대 프로덕션 1.0 등); 유효한 것은 팔 간 차다.
  S2 의 꼬리 숨김(~5–7 ms)은 드래프터 없는 랭크라 못 쟀지만 dense 페널티가 그 상한의 세 배다.
### 벌크 L2 프리페치는 MoE 스트리밍을 느리게 하고 dense v2 GEMM 을 안 움직인다 — 셋 다 기각 (2026-09-16/17, srv4 단일 GPU 레인 3회, 운영자 "MoE 스트리밍·소형 dense GEMM·16행 mHC 해봐")

C=2 아키텍처 캠페인의 둘째 판정(첫째는 위 항목: 두 스레드 분할 기각). 세 항목을 **산술은 그대로 두고 메모리 요청 시기만 바꾸는
힌트**(`cp.async.bulk.prefetch.L2.global`, 연속 런당 요청 하나, smem·완료 없음)로 열었다(`measurements/st_l2_prefetch_20260916/`).
- **MoE 정적 커널(셀 `l<n>`·`lf<n>`).** 가설은 Little 법칙 — CTA 의 16 KB 스테이지 둘 / DRAM 지연 6.4 µs = 스탬프의 5 GB/s/CTA — 이고
  smem 없이 비행 바이트를 늘리려 DMA 레인이 n 스테이지 앞의 B 박스를 미리 부른다(256 청크 위 reform 박스 = 연속 16 KB, 38차 §8 의
  스레드별 라인 프리페치와 달리 요청 하나). **전부 정확(1 ulp 바닥, BF16 0)하고 전부 느리다**: l2 +0.6~1.3 %, l4 +1.9~2.4 %,
  **l8 +10.6~12.4 %**, FC2 만 부르는 lf2/lf4 +0.1~1.0 %, lf8 +2.1~3.1 %(C=2·C=1, 단일·3층 사슬, warm·evicted, 티켓 2회, 팔 7개).
  스탬프가 이유다: 프리페치 받은 FC2 는 5.47 → 6.43 GB/s/CTA 로 빨라지고 **같은 CTA 의 FC1 이 5.05 → 4.63 으로 그만큼 느려진다** —
  48 CTA 의 합은 이미 포화라 앞당긴 요청은 흐르는 스트림에서 대역을 뺏는다. 273 까지의 남은 20 % 는 발행 시기가 아니라 접근
  무늬(128 B 행 세그먼트)의 몫이고, 그 자리는 `z`(사전 스위즐 + 1-D 벌크 스테이지, 순열 버그로 미완) 뿐이다.
- **dense v2 GEMM(벤치 노브 `set_gemm2_l2_prefetch`).** 16행 dense 레인 6.75 ms(노출) 대 273 GB/s 바닥 4.4 ms, 런치당 12~28 µs 의 소형
  GEMM 이 ksr 슬라이스 4 k-블록(32 KB)을 3단 링으로 넘긴다는 진단에서, CTA 진입 시(PDL 대기 전) 슬라이스 전체(W + 스케일)를 벌크
  요청 둘로 부른다. `dense_cells` 무관용 게이트 전부 통과, **서빙 W4 셀 전부 ±1~2 %(잡음)**, 쿼리 쌍 +2.8~17.5 %, 드래프터 +1~9 %
  손해. evicted 대조군(`kda.o_proj` 4.5 MiB 52 µs = 88 GB/s, `mlp.gate_up` 12 MiB 123 µs)이 warm(절반 이하)으로 안 가므로 **시간은
  가중치의 첫 접촉 지연이 아니라 슬라이스당 고정 사슬**(x 양자화·fp32 부분합 fold·도착 원자·에필로그)에 있다. 남는 후보는 m=16 의
  `ksr` 규칙 재측정(30차 스윕은 m=8; `set_gemm2(ksr)` 훅, fold 순서가 바뀌므로 허용오차 게이트 필요).
- **16행 mHC.** 티켓 없이 닫음: `st_c2_mhc_packed_20260915` 의 컴포넌트가 호출당 20.9 µs(84팩 사슬)이고 트레이스 노출 27.6 µs 와의
  차는 PDL 인계다. (청크, 토큰 그룹) 매핑에 96 상주 블록·그룹당 토큰 2.7·토큰당 꼬리 ~4 µs — 재매핑 상한 ~0.4 ms/스텝, 레버는
  mHC 가 아니라 경계 수(스텝당 ~1,200 런치)다.
- **한계.** 한 랭크·실물 가중치·합성 행·프로브 기본 실행계획; 팔 간 차만 유효. 셀·노브는 코드에 남고 서빙 레시피에 토큰이 없어
  프로덕션 바이트 불변. `probes/engine_decode_native_compile.py` 는 dense `extension`→`build` 개명 뒤 낡았다(StopIteration).

### 소형 W4 GEMM 의 로드 링을 깊게 — warp 의 k-블록 전부 비행시켜도 느려진다, C=1·C=2 dense 항목 마감 (2026-09-17, srv4 단일 GPU 레인 1회, 운영자 "너는 c=1 개선이라도 하던가" → "ㄱㄱ")

레버 목록의 항목 2("밀집 GEMM 메가커널, ★★★☆☆, 2.8배 여유")를 인터뷰로 뜯었다. 전제 셋이 낡아 있었다: #825~#827 은 09-13 머지, 프로덕션은
#950 뒤 C=1 8행·C=2 16행, "1행 13 ms / 35%" 는 서빙되지 않는 형상. 같은 빌드 8행 셀(#1067 의 원시 이벤트, srv4 실가중치, evicted 사슬)은
16행 셀과 같은 바이트에서 동급이라(kda.in_proj 72.5 µs 204 GB/s, kda.o_proj 32 146, mla.o_proj 64 148, 쿼리 쌍 49 145, gate_up 85 167,
down 56 127) "8행을 C=2 식 넓은 pack 으로" 는 죽은 후보. C=1 dense ≈ 7.6 ms 대 바닥 4.4, 틈 3.2 ms 중 공유 전문가 0.7 은 8행에서도
aux 스트림이 못 숨기는 MoE 레인 몫(09-15 프로브 S−R 30 µs/층). C=2 는 #1067 이 못 박은 196발 7.9 ms(노출 6.75)/바닥 4.4, 그리고
09-15 에 기각된 공유 전문가 겹침. 이 항목의 천장은 어느 레짐이든 ≤2.5 ms 였고 "메가커널"(상주 grid)은 근거가 없다.
- **가설.** 서빙 커널(ORDERED·MODE1 input-CTA, rows16, cta3 쿼리)은 warp 마다 k-블록 2~4개를 맡는데 링이 NB=2/DIST=1 이라 다음 로드를
  앞 로드가 도착한 뒤에야 낸다 — CTA 하나가 DRAM 왕복 2~4번을 직렬로 겪는다. #1067 의 L2 프리페치 노브는 v2 커널에만 꽂혀 있어
  이 커널들의 지연 가설은 미측정이었다. 인스턴스마다 warp 의 k-블록을 한 번에 비행시키는 깊이(K 2048 은 smem 그대로 DIST 2,
  K 3072 는 3단, K 4096·MODE1·rows16 은 3단에 2개 비행, 점유율 유지)를 벤치 노브 `set_c1_deep` 로 — 산술·순서 동일.
- **결과: 기각.** 무관용 바이트 게이트 34/34 통과(바이트 동일 증명). 시간은 서빙 레짐(evicted 사슬)에서 전부 중립~손해:
  8행 kda.o_proj **+2.9%**, mlp.down **+5.9%**, drafter.down +2.9%, 나머지 ±0.5; 16행 kda.o_proj **+4.7%**, mlp.down **+6.4%**,
  drafter.down **+6.9%**, in_proj +1.9, qkv_a +2.5. warm 단일은 +5~22%. forward 합 **8행 +0.06 ms, 16행 +0.13 ms**(느려짐).
- **읽기.** K 2048 셀은 발행 순서만 바뀌고(같은 두 단, 같은 smem, 같은 점유율) 3~5% 잃는다 — 96~144 상주 CTA 가 이미 채운 경로에
  미해결 요청을 더 얹으면 대기만 는다(#1067 의 MoE `lf<n>`·v2 `_l2` 와 같은 무늬). #1067 과 합쳐 소형 W4 GEMM 의 두 지연 가설
  (첫 접촉, 비행 깊이)은 8·16행 모두 닫혔다. 남는 3.2 ms 는 요청 무늬(128행 타일 레코드에 끼워진 warp 당 1 KB 조각)와 CTA 에필로그
  사슬에 있고, CTA 마다 연속 한 줄을 주는 팩 배치는 같은 수치의 배치 작업 — 다시 여는 사람의 다음 질문이지 계획이 아니다.
- **dense 항목은 C=1·C=2 모두 여기서 마감.** C=1 스텝의 남은 레버는 dense 밖: mHC 발당 50 µs(4.4 ms), 라우팅 커널 안의 공유
  전문가(MoE 레인, W4A4), torch DSA top-k 0.85 ms, 드래프터 꼬리. 측정 팔은 `measurements/st_c1_deep_ring_20260917/deep-ring.patch`
  에만 있고 머지되는 것은 기록뿐(kernels.cu·프로브는 main 과 바이트 동일). 컴파일 영수증·원시 이벤트·요약 같은 디렉터리.

### cuBLAS head/FC 실가중치 비교 — RTX 5050 한정, head −12.3~12.7%, FC +3.4~3.8% (2026-09-17)

운영자 "아니면 5050에서 하던가"로 SM120 비교를 명시 승인했다. 플릿 큐·프로덕션 재시작 없이
5050의 직접 호스트 잠금에서 실행했다. **GB10 채택·엔진 step/s·수용률 증거가 아니다.**
실제 FP8 PackStore 가중치와 양쪽에 같은 합성 BF16 입력을 사용했다. CUDA 13.2,
cuBLASLt 130400, SM120 20 SMs. 각 표는 양자화부터 BF16 출력까지 그래프 B/A/A/B 시간이다.

| 경로 (M) | DeepGEMM ms | cuBLASLt ms | 지연 변화 |
|---|---:|---:|---:|
| head 7 | 0.679124 | 0.593184 | −12.65% |
| head 8 | 0.679513 | 0.594176 | −12.56% |
| head 14 | 0.682112 | 0.598208 | −12.30% |
| head 16 | 0.682914 | 0.598944 | −12.30% |
| FC 8 | 0.344780 | 0.357848 | +3.79% |
| FC 16 | 0.349719 | 0.361448 | +3.35% |

형상마다 447개 설정 검사→4개 실행 가능, 4개 모두 초기 수치 검사 통과. head는 두 짝 모두
2% 이상 이겨 선택되었고 workspace 0, 변경 입력 그래프 재생·추가 Torch 할당 0 검사를 통과했다.
FC는 최저 평균 cuBLAS 조합도 느려 DeepGEMM 유지. FC의 변경 입력 재생은 선택된 DeepGEMM에
대한 검사다. 준비 단계의 MX scale pointer 누락과 소멸한 graph pool 재사용 오류를 실카드에서
발견해 수정했다. 5050 이미지에 없던 DeepGEMM은 핀한 공식 소스를 별도 폴더에서 빌드했다.
주요 SM120 GEMM/SplitK 헤더는 운영 이미지와 해시 일치하나 전체 라이브러리 동일성은 아니다.
서빙 기본 경로 교체 없음. 상세·가중치/소스 해시·오류 및 원시 결과:
[st_cublaslt_compare_20260917](measurements/st_cublaslt_compare_20260917/README.md).

### cuBLAS FC 5분할 배치 개선 — RTX 5050, 기존 cuBLAS −10.5~11.2%, DeepGEMM −8.1~8.2% (2026-09-17)

앞선 FC 열세를 K=4096 × 5의 strided-batched FP32 GEMM으로 뒤집었다. BF16 입력을 한 번에
5개 FP8 파티션으로 생산하고, cuBLAS 한 번 뒤 FP32 합산→BF16 저장한다. 가중치 재양자화 없음.
명시적 PreparedProjection 준비에서 FC 8/16행 후보로 기본 평가하며, 반복 B/A/A/B 양쪽 모두
2% 이상 이겨야 선택한다. **서빙 FP8Linear 교체·GB10 채택·step/s·수용률 증거는 아니다.**

| FC M | 기존 cuBLAS ms | 개선 cuBLAS ms | 변화 | DeepGEMM ms | 개선 cuBLAS ms | 변화 |
|---|---:|---:|---:|---:|---:|---:|
| 8 | 0.357364 | 0.317250 | −11.23% | 0.343601 | 0.315649 | −8.13% |
| 16 | 0.362264 | 0.324279 | −10.49% | 0.351604 | 0.322747 | −8.21% |

각 비교는 준비 후 별도 B/A/A/B 두 묶음의 네 샘플 평균이며, 네 짝 모두 2% 이상 승리했다.
양자화·스케일·GEMM·FP32 합산·BF16 출력 비용 포함. 실제 FP8 팩과 합성 입력, 같은 런타임.
head 4개 형상은 기존 cuBLAS 이득 유지(−12.63~13.07%). 6개 모두 변경 입력 그래프 2회,
추가 Torch 할당 0 통과. GPU 전용 3검사, CPU/interpreter 21통과/3skip, GPU 없는 SM121 80변형
컴파일 통과. 대가: 원본 팩을 보존하며 가중치 분할본 82.5MiB 추가, 행 수 간 공유;
FP32 partial은 8행 0.625MiB/16행 1.25MiB. 큐·프로덕션 재시작 없음.
기각한 전치·확장 알고리즘 탐색·다중 스트림 실험과 초기 이상치도 함께 보존했다.
[상세와 원시 결과](measurements/st_cublaslt_reform_20260917/README.md).

### 후속 둘도 열리지 않는다 — m=16 ksr 규칙은 이미 최적, 셀 z 는 세 저장소 순서가 전부 틀려 배관 문제로 접음 (2026-09-17, srv4 단일 GPU 레인 3회, 운영자 "해보던가")

위 L2 프리페치 항목이 남긴 두 후보(`measurements/st_l2_prefetch_20260916/` §4·§5).
- **dense v2 GEMM 의 m=16 `ksr` 스윕(`c2ksr-0917`).** `dense_cells` 16행 셀마다 서빙 경로를 `set_gemm2(n)`(1/2/4/8) 아래 캡처, fold
  순서가 바뀌므로 1 bf16 ulp(원소·텐서 RMS 중 큰 쪽) 게이트. **서빙 16행 CTA 셀은 강제 ksr 을 거부한다**(선언 기하: "sixteen-row cells
  require the ordinary lane's K slices"). 훅이 먹는 generic(드래프터)·pair(쿼리 쌍)에서는 규칙의 선택이 최적이거나 잡음 안: ksr1 warm
  +9~18 % / evicted −1~−4 %, ksr4 +6~22 %, ksr8 +11~37 %(전부 ≤1 ulp). 30차의 "96 슬롯 한 파도" 규칙은 m=16 에도 옳고, 소형 GEMM 의
  남은 시간은 슬라이스당 고정 사슬(커널 본문)의 일이다.
- **MoE 셀 `z` 재도전(`c2z-0917`, `c2z2-0917`).** reform 타일의 B 스테이지 레이아웃(B1 128 B 행 `S<3,4,3>`, B2 64 B 행 `S<2,4,3>`,
  `probes/b12x_reform_layout_print.py`)에 맞춰 저장소 박스를 사전 스위즐하고 스테이지마다 `cp.async.bulk` 하나를 같은 mbarrier 에
  같은 바이트로 완료하는 셀. 바이트 순서(TMA 하드웨어 모드)·니블 순서(옛 z 의 읽기)·순열 없음 **셋 다 3.3e7 ulps(max|diff| 7~8.5)로
  틀리고 서로 조금씩 다르다** — 순열이 아니라 벌크 복사 배관(착지 주소·스테이지·장벽)의 문제. 종이 위 주소·회계는 맞아 보여 다음은
  smem 덤프 프로브(장벽 뒤 스테이지 16 KB 를 전역으로 써 TMA 이미지와 대조)인데, 기대 이득이 옛 z 의 −2.5 %(MoE ~0.7 ms/스텝) 상한이라
  여기서 접는다. 셀·순열 함수·프로브 섹션은 남고 서빙 레시피에 토큰이 없어 프로덕션 불변.
- **함정.** 단일 레인은 프로덕션의 capture-armed 재부팅(00:09) 뒤 MemAvailable ~13 GiB 라 문턱(예산+16 GiB)을 못 넘어 티켓이 수십 분
  대기했고, 플릿 부팅이 프로덕션을 드레인하는 짧은 창에서만 돌았다.

### 45차 — 고정 K7 mHC 입력 팩: 스텝당 30발 제거, 엔진 전체 이득은 미입증 (2026-09-17, TP4 GB10, PR #1068)

K=7·FP32 KDA 상태를 유지하며 세 방향을 일곱 차례 비교했다. **MoE resident waves는 뚜렷한 이득이 없었고, MLA 인접 질의 KV 공유는 마지막에도 C1 +9.96%·C2 +19.39% 느려 제거했다.** 남긴 후보는 mHC가 한 번 BF16으로 반올림한 값을 곧바로 다음 KDA의 FP8 입력 팩으로 쓰는 경로다.

- **수치·부품:** 실가중치 mHC 90개 계수, 네 크기, 바뀐 TP4 패킷과 그래프 재생에서 모든 출력·팩 바이트 동일. 90계수 packet 사슬은 같은 빌드 B/A/A/B에서 1592.05→1287.60 us(-19.12%), 재확인 1601.07→1290.03 us(-19.43%). 단일 계수 hot은 동급이며, 사슬 수치를 엔진 개선율로 쓰지 않는다.
- **실행:** 네 랭크 모두 KDA 30곳에서 실제 입력 팩 소비. 별도 2K 진단의 4개 decode step에서 각 랭크의 입력 pack 커널 298→178발; 120발을 mHC 안으로 옮겼다. CUPTI 시간 합산은 하지 않았다. 16행 동시 구간은 미적용이지만 C2 요청의 8행 꼬리에는 적용될 수 있다.
- **소비자:** B=`ffd19b44630b`, A=`69c5420c`; 두 트리 차이는 `net.mhc_input_packs=False/True` 한 줄. 노드별 네이티브 캐시 재사용, CUDA 소스 동일. extended onepass로 2K·32K·128K, 각 부팅 C1 두 판·C2 한 판(128K C2 제외). C4는 미측정.

| 팔/회차 | 실제 요청 decode tok/s | raw 수용률 | raw median step/s | C1 품질 |
|---|---:|---:|---:|---:|
| B/1 | 87.3047 | 53.6728% | 19.887089 | 6/9 |
| B/2 | 83.3485 | 50.4309% | 19.891678 | 7/9 |
| A/1 | 85.2527 | 52.0330% | 19.885538 | 7/9 |
| A/2 | 86.1621 | 53.3209% | 19.882239 | 7/9 |

위는 **품질 실패 기록의 관찰값**이다. C1 한국어 깨짐은 전부 0, C2 품질 B 8/12·A 10/12. 요청 tok/s는 실제 생성 구간의 토큰/시간이며 step/s×수용률로 추정하지 않았다. 두 번째 판 tok/s +3.38%에 수용률 +2.89 pct가 함께 움직이고 step/s는 -0.047%; 첫 판 tok/s는 -2.35%다. 기준선 자체도 응답 길이·수용률이 변한다. 1024-token 요청 네 개 중 출력이 같은 두 개도 -0.1%대 관찰이며, 단일 쌍으로 중립·퇴행을 확정하지 않는다.

**판정: 구성요소 절감·실행은 확인, 전체 속도 개선과 채택은 미입증.** 정본 judge는 후보의 유효한 warm 표본이 없어 `NO EVIDENCE`를 반환한다. 텍스트 diff만으로 기각한 것이 아니라 품질 게이트가 실패했고 반복 유효 브래킷이 없다. 기준선·후보 예약은 모두 종료·해제했다. 검증 중 드러난 자동 draft FC 수집/compact BF16 원본 수명 오류는 양쪽에 동일하게 수정했고 관련 CPU 검사·코드 CI가 통과했다.

원시 4개 소비자 기록, 요청별 TTFT·해시·품질·수용률, 7회 부품 비교, 네 랭크 실행/빌드 증거와 재계산 스크립트: [측정 묶음](measurements/st_fixed_k_cost_20260917/README.md). 당시 PR은 채택 보류 상태의 draft였다. 이후 세 경로 기본 적용 결정은 아래 후속 기록을 따른다.

### 45차 — 고정 K7 후속: mHC·MoE·MLA 세 기본 경로, 손해 본 형상은 제거 (2026-09-17, PR #1068)

앞선 mHC 단독 캠페인 이후 추가 비교하여 세 경로를 코드 기본값으로 묶었다. K=7·FP32 KDA·가중치 형식을 유지한다. 측정 후보 `3c3c3260`, 대조군 `0e949d11`은 세 Python 기본값만 다르고 네이티브 소스·빌드 입력은 같다. 이후 `0b382a91`까지는 출처 메타데이터와 기록만 바뀌었다. **기본 적용과 전체 엔진 성능 검증 상태는 별개**다(D11).

- **mHC C=1:** BF16 출력의 다음 KDA 입력 팩 쓰기를 합친다. 90개 실계수·네 크기·TP 패킷 변경·오염 버퍼/역순 재생에서 출력과 팩 바이트 동일. 계수 사슬 약 -19%지만 실제 KDA 투영을 붙인 사슬은 -0.36%다. C=2 확장은 투영 사슬 +3.94%로 느려 제거했다.
- **MoE C=1·2:** BF16 16개를 두 번의 128-bit load로 읽는다. 변환·max·양자화·expert scale·누산은 유지한다. 실가중치 세 층, 독립/집중/보정 라우팅과 0 가중치 등 7,077,888원소에서 BF16 차이 0; FP32 차이는 417원소·최대 1 ULP로 대조군 자체 atomic 반복의 464원소·1 ULP 이내다. 마지막 evicted 사슬은 C1 -0.41%, C2 -0.45%; 단일 층은 +0.26%, +0.25%이므로 엔진 개선을 뜻하지 않는다.
- **MLA C=2·선택 폭 ≥128:** BF16 Q 조각을 레지스터에 보관하고 32슬롯 타일·기존 6 split을 쓴다. C1·짧은 폭·tree·cluster는 기존 경로다. 42개 선택/중복/빈 꼬리/불균등 fixture의 독립 FP32 상대오차 최대는 대조 0.00179969, 후보 0.00181095. 비트 동일 경로는 아니다. C2 full-selection 부품은 -3.6~7.3%; 6 split 비교 60.03→55.67 µs.
- **기각과 정정:** Q 레지스터+16타일과 MLA C1 확장은 실익이 없어 제거. MoE bulk probe의 이전 전부 실패는 이미 타일된 가중치를 다시 타일한 오류였다. 수정 뒤 byte-swizzled z는 수치 통과하지만 속도 이득이 부족하며, 선형 shared-stage zl도 느려 기본값에 넣지 않았다.
- **검증:** 최종 CPU-only 네이티브 컴파일·GB10 수치/재생 검사 통과. CPU 239파일/2,229검사 중 출처 해시와 init 없는 컨테이너의 프로세스 회수 실패를 수정·재검증했고, GitHub 전체 CI `35141668624` 통과. [후속 측정 묶음](measurements/st_fixed_k_cost_followup_20260917/README.md)에 원시 기록·실패한 변형·재계산 스크립트를 보관한다.

동일 컨트롤러의 full onepass B/A **네 회차 완료**. 2K·32K·128K, 부팅별 C1 두 판·C2 한 판, 고정 1,024토큰, 실제 요청 tok/s·TTFT·품질·수용률·네 랭크 실행 정체성을 보관한다. C4는 서버 용량 C2로 미측정이다. 네 노드 각각에서 두 팔의 실제 mapped 네이티브 바이너리·소스 해시·mtime이 같았다. 64개 랭크/문맥 진단 모두 C1 pack 298→178발(4스텝), C2 MLA tile16→tile32 44발을 확인했다. 예약은 모두 종료·해제했다.

| 팔/회차 | 일반 요청 decode tok/s | 고정 1024 decode tok/s | pooled fixed step/s | raw 수용률 | 일반 C1 품질 |
|---|---:|---:|---:|---:|---:|
| B/1 | 85.3135 | 93.8092 | 19.71683 | 51.8831% | 7/9 |
| B/2 | 87.8596 | 91.3914 | 19.71681 | 54.1088% | 6/9 |
| A/1 | 85.4295 | 91.9405 | 19.76948 | 52.1515% | 8/9 |
| A/2 | 86.2512 | 94.7501 | 19.83227 | 53.0411% | 6/9 |

**상대 품질 회귀 판정은 아니다.** 일반 품질 합계는 B 23/30→A 25/30(C1 13/18→14/18, C2 10/12→11/12)이고 C1 한국어 깨짐은 양쪽 0/16이다. 표본이 작아 품질 향상도 확정하지 않는다. 정본 게이트에는 고정 길이 응답의 완성 JSON 실패(매 회차 0/9)도 포함되지만, 이를 분리해도 일반 품질이 만점은 아니므로 judge의 `NO EVIDENCE`는 유지한다.

일반 요청 속도 변화는 +0.136%/-1.831%, 고정 길이는 -1.992%/+3.675%로 엇갈린다. 일반 C1 출력 해시는 전부 다르다. 별도 고정 C2 처리량 B 114.0778→A 112.6820 tok/s는 B의 측정 중 Triton 진입 2회/랭크 때문에 유효 비교가 아니다. **전체 엔진 속도 개선은 미입증**이며, 부품 개선율을 엔진 개선율로 쓰지 않는다.

운영자 최종 지시(“그냥 기본값으로 도입해서 pr 머지해”)에 따라 위 세 경로를 기본값으로 채택한다. 이 코드 채택 결정과 전체 속도 검증 상태를 분리해 기록한다.

### head·FC 기본 cuBLAS 교체 — 운영자 직접 지시, 23개 서빙 호출 검사 (2026-09-17)

운영자 “head랑 fc에서 deepgemm 바로 대채ㅔ”에 따라 native GLM 부팅이 head, FC prefill,
FC committed-decode FP8 reader를 cuBLAS로 기본 연결한다. FC decode는 5분할 FP32 누적.
알고리즘은 가중치 준비 때 고정하고 forward에서는 GPU 탐색·타이밍·DeepGEMM 폴백이 없다.
네이티브 사전 빌드, arena 예산(별도 decode 팩일 때 92.234375MiB/랭크), 실제 실행 마커를 연결했다.

5050에서 **실제 FP8Linear 호출 23형상**이 수치·변경 입력 그래프 2회·재생 추가 할당 0을 통과했다.
head 7/8/14/16행 지연 −12.26~12.67%, FC decode 8/16행 −6.32/−6.47%.
손실도 남긴다: FC decode64 +0.72%, FC prefill1 +6.64%, 512 +0.96%, 2304 +2.84%.
추가 탐색과 512행 분할도 큰 prefill의 열세를 지우지 못해 분할 실험은 코드에서 제거했다.
8개 focused 검사, 관련 CPU 54통과/9skip, SM121 93변형 컴파일 통과.
**이는 운영자 선택의 기본값 변경이며 GB10 실행·엔진 step/s·수용률 실측은 아니다.** 큐·재시작 없음.
[코드 경계, 전체 비교와 원시 기록](measurements/st_cublaslt_serving_20260917/README.md).

## 2026-09-17 — cuBLAS FC padded pitch + fused RMS, default

Follow-up to #1071: keep the five-way FP32 FC split, pad each physical weight
row by 384 bytes and fuse partial reduction with the existing BF16/RMS/bias
boundaries. Actual `FP8Linear` calls on the authorized RTX 5050 improve FC
decode+RMS by **5.2–6.5%** versus frozen main `b41efc7d`; M8
0.32813→0.30789 ms, M16 0.33592→0.31536 ms, M32 0.34833→0.32690 ms.
Two B/A/A/B brackets per cell, real FP8 packs and synthetic changed inputs.
All 28 final cells are bit-identical, including changed-input graphs with zero
replay allocations. Head execution and FC prefill are unchanged within noise;
the earlier large-prefill loss versus DeepGEMM remains unresolved. Direct
algorithm validation reduces warm host plan preparation ~94–95% (less than
1 ms total, not a boot/step speed claim). Added residency is 7.5 MiB/rank,
declared in the arena budget. No queue/deployment/restart; **GB10/TP4 step/s and
acceptance remain unmeasured**. Rejected zero-copy, split-count, operand-order,
producer-tile and batch-gap experiments, exact receipts and reproduction:
[`measurements/st_cublaslt_layout_20260917/`](measurements/st_cublaslt_layout_20260917/README.md).
### 라우터를 한 런치로 — 층당 7발을 1발로, 42층 사슬 C=2 −1.48 ms·C=1 −1.83 ms, top-8 집합 뒤집힘 0 (2026-09-17, srv4 단일 GPU 레인 7회, 운영자 "그럼 남은거 뒤져")

C=2 캠페인이 남긴 지도를 코드와 원장으로 되짚었다(`measurements/st_c2_levers_survey_20260917/`).
- **이미 착지했거나 닫힌 다섯.** 16행 mHC 계수 팩(#972, 20.9 µs/호출)·consumer 16행 상한(#967)·DSA 선택 한 런치(#971·#1010)는 C=2
  프로파일(`9c45086a`) 뒤에 들어왔다(그 기록 머리에 주석). MoE 스트리밍은 계측기의 선형 상한이 273 이 아니라 **239 GB/s** 이고 서빙
  타일이 그 93~95 %, TMA 박스는 연속 16 KB 라 "128 B 행 세그먼트" 가설은 성립하지 않았다. 발행 대기 중 `HintArgs` 는 대기를 쥔
  `k_publish_packets` 가 1 스레드 커널이고 상한이 EXP-13 에서 −0.4~0.6 ms 로 이미 철회됐다. 공유 전문가를 289번째 전문가로 넣는 길은
  BF16 체크포인트를 NVFP4 로 옮기는(A8→A4, rowscale·act-order 손실) 품질 결정이라 운영자 몫으로 남긴다.
- **열린 하나: 라우터.** 서빙은 층당 7 런치(cast, sgemm, split-K reduce, `_scores`, `gatherTopK`, `bitonicSort`, `_weights`)로 스텝당
  ~300발이다. `engine/kernels/router_fused.cu` 는 96 CTA(SM 당 둘, CTA 당 전문가 3)가 상주 FP32 게이트를 한 번 흘리며 IEEE FP32 곱을
  고정 순서로 누적하고, 마지막 CTA 가 sigmoid+bias top-8(동점은 낮은 id)과 원시 시그모이드 가중치를 낸다. 서빙 모듈에 묶지 않은 셀이다.
- **게이트(`probes/engine_router_cells.py`, 실물 랭크 3 게이트 42층, 합성 행).** logits ≤12 ulps, 커널 자기 logits 위의 선택은 torch 와
  집합 동일·가중치 ≤3 ulps, **서빙 사슬 대비 top-8 집합 뒤집힘 0/24,768 행**. 순서만 다른 행 1~3 % 는 커널 자기 logits 위에서도 같은
  수라 FP32 동점의 열 순서 규칙 차이다(MoE 합 순서로 이어질 수 있는 add-order 급).
- **시간(최종 판 `373dc770`, `c2rt7-0917`).** 42층 사슬 evicted C=2 3236.8 → 1755.0 µs(−45.8 %, 바닥 −0.06 %), C=1 3189.6 → 1356.8 µs
  (−57.5 %, 바닥 −0.03 %); 단일 층 warm C=2 67.9 → 33.5, C=1 55.4 → 20.6 µs.
- **판 여섯.** 48 CTA × 전문가 6 은 레지스터 255 로 SM 당 CTA 하나라 게이트 로드를 앞당겨도 1954 µs 그대로였고, 벌크 복사로 smem 에
  올리면 곱이 착지를 기다려 2250 µs 로 느렸다. 96 CTA × 3 이 1819, 모든 행 x 의 청크 선적재는 8행 −1.7 %·16행 +5 % 라 운영자
  "v5도 c=1에 도입하거나 v4+v5 함께 도입" 에 따라 `ROWS` 템플릿이 8행에만 쓴다. 사슬 층당 ~42 µs 는 아직 DRAM 시간보다 크다.
- **정정: 셀 `z` 는 배관 문제가 아니었다.** 위 "후속 둘도 열리지 않는다" 항목과 `st_l2_prefetch_20260916` §5 의 결론은 틀렸다. 프로브가
  `moe_prepare` 로 제자리 타일된 w13/w2 에 `_tile_expert_weights` 를 한 번 더 걸었다(이중 타일). #1068 후속(`moe-v8.jsonl`)이 고친 뒤
  byte 스위즐 `z` 는 정확하고 nibble·plain 은 제대로 실패했으며 속도는 거의 중립이었다. 그 README 에 정정 주석을 달았다.
- **한계와 채택.** 한 랭크·합성 행·컴포넌트 값이고 엔진 step/s 주장이 아니다. 티켓마다 서빙 대조군이 ~1 % 흔들려 판 비교는 같은 티켓의
  변화율로 읽는다. 채택은 서빙 결선(`lanes.route_weights`, 캡처 전 빌드, `route_observer`)과 브래킷(품질·한국어·수용률·step/s)이 답한다.

### 45차 — ACE식 전문가 슬롯 스킵의 바이트: 스킵한 슬롯 비율이 거의 그대로 읽기 감소, 스텝 상한은 스킵의 0.45배 (2026-09-17, TP4 GB10 hold 1회, PR #1076)

논문 지도(#1074) 10절이 바이트를 줄이는 유일한 후보로 남긴 ACE(arXiv:2609.05228)의 GB10 상한. main `a9bf7afc` + 디버그 `e52fb5cb`
(머지 안 함)으로 C=1 12문항(#966 과 같은 부하)의 **원시 라우트 6,154 스텝**을 받아 오프라인으로 셌다.
- **덤프 검증.** 스킵 전 층당 고유 전문가 41.91(#966 41.9), tokens/step 4.725.
- **게이트만의 대리 규칙**(1위 유지, 1위가 아닌 슬롯의 전역 분위수 임계값, GSP·RCR 표 없음): 스킵 10% → 읽기 −9.8%, 15% → −14.4%,
  20% → −19.1%. **스킵한 슬롯당 읽기 0.94~1.01** — 게이트 낮은 슬롯은 다른 행이 읽지 않는 전문가다. MoE 비중 0.458 을 곱한 스텝
  **상한** −4.5% / −6.6% / −8.7%.
- **GLM 게이트는 평평하다**(순위별 0.233…0.076). 스킵 10% 가 라우팅 질량 평균 4.62%(p95 20.8%)를 빼고, 층별 읽기 감소가
  2.7%~37.7% 로 갈린다. 요청 12개는 8.4%~11.0% 로 고르다.
- **판정 아님.** 스킵을 서빙하지 않았고 품질·수용률·step/s 없음. 논문 기준 스킵 20% 는 토큰당 +0.019~0.046 nats(Qwen 계열)로
  NVIDIA 랭크를 택하지 않은 차이(+0.034)와 같은 크기. 다음은 기본 꺼진 노브 + head NLL 짝 비교(스킵 10·15%, 전역 대 층별 분위수) —
  미착수. [원시·스크립트·함정(부팅 실패 셋)](measurements/st_ace_routes_20260917/README.md).

### 45차 — DSA 선택: GB10 의 48 KB 공유 메모리에서 st_dsa_select 는 후보 13만부터 읽기 바닥의 14~18배, HPC-Ops 정확 top-k 는 4배 빠르다 (2026-09-17, srv4 단일 GPU 레인 1회, PR #1078)

논문 지도(#1074) 10절의 "디코드 top-k 는 이미 바닥 근처(sm_120 85%)" 전제를 GB10 에서 다시 쟀다. 합성 logits 컴포넌트 측정이고
서빙·step/s 아님. HPC-Ops `topk_filtered`(MIT, 수정 없이 벤더링)를 `sm_121a` 로 빌드해 `st_dsa_select`·`prefill_topk`·읽기 바닥과
같은 logits·지평에서 비교했다(`probes/engine_topk_hpcops.py`, 레인 `topk_hpcops`).
- **장치 사실.** GB10 블록당 공유 메모리 **49,152 B**(sm_120 상자 96 KiB). 후보 > 24,576 이면 빈 캐시가 빠지고 stash 가 2,560 슬롯이 되어,
  넓은 빈이 행 재독 최대 8 라운드로 떨어진다.
- **디코드(치우친 `indexer` 분포, µs, ST / HPC-Ops / 바닥):** 8×32,768 32.8 / 22.6 / 8.8 · 8×131,072 **228.4 / 53.8 / 12.4** ·
  16×236,032 **393.4 / 90.7 / 22.6**. C=1·11 층 환산 차: 문맥 128K 0.11 ms, ~524K 1.9 ms, ~944K 3.3 ms/스텝.
- **프리필 1,024 행:** `prefill_topk` 바닥의 1.45~2.05 배. HPC-Ops 는 32K 에서 1.17 배(686 대 939 µs)지만 131K·236K `indexer` 에서 각 1 행
  불일치(원인 미확인)라 시간 미측정.
- **정확성.** ST 두 선택기는 전 형상·분포 불일치 0. HPC-Ops 는 동점 규칙이 달라 `plateau` 전 행 불일치.
- **다음(미착수).** 실제 인덱서 logits 로 절벽이 서빙에서 나는지 확인 → `st_dsa_select` 넓은 빈 분기 수정 또는 HPC-Ops 동점 규칙
  수정(둘 다 바이트 동일 목표). [표·장치 사실·재현](measurements/st_topk_hpcops_20260917/README.md).

## 2026-09-17 — Drafter head input fusion, default cuBLAS

Final residual/RMS, non-anchor compaction and native MXFP8 input now share one
launch; selector/calibration BF16 values and final logits are bit-identical.
RTX 5050 actual head_local calls: K=7 C=1 **neutral (+0.009%)**, C=2/4/8
latency −0.52/−0.89/−1.04%. Input-only savings are not whole-head or engine
speedups. Eight shapes pass exact bytes/logits, changed-input graphs and
independent output checks; 62 CPU pass/6 skip and 115 SM121 compile variants.
No persistent memory growth, queue or deployment. GB10/TP4 speed and acceptance
unmeasured. [Receipts and limitations](measurements/st_cublaslt_producer_20260917/README.md).

## 2026-09-17 — Drafter post-convolution residual/RMS fusion, K=7 unchanged

The default drafter path fuses five post-attention and four interlayer MLP
boundaries, preserving BF16 rounding and the existing final head producer.
RTX 5050 synthetic width-4096, rows/block=8/8: group16 single-boundary
2.462 → 1.819 µs and nine-boundary chain 24.622 → 17.365 µs; group256 chain
22.614 → 16.269 µs. These are component savings, **not whole-drafter or
engine speedups**. Seven cases pass exact outputs and changed-input graph
replays; CPU 41 pass/8 skip and seven offline SM121 specializations pass.
No queue/deployment; GB10/TP4 step/s and acceptance unmeasured. The measured
microseconds do not resolve the historical millisecond-scale K sensitivity.
[Raw brackets, hashes and reproduction](measurements/st_draft_post_norm_20260917/README.md).

## 2026-09-17 — Default sampled selector: batch probabilities and fuse the K-position walk

K=7 actual `propose_rows` selector code, with model/head outputs held fixed
and synthetic codebooks on RTX 5050: C=1 **142.390 → 19.321 µs (−86.43%)**,
C=2 143.676 → 20.892, C=4 156.297 → 24.810, C=8 160.743 → 34.658 µs.
This removes ~123–131 µs from the measured component; it is not full-drafter
or engine speed. Fourteen GPU shape/policy cases, changed-input graphs and
exact CDF-boundary uniforms pass; CPU 50 pass/9 skip; 16 offline SM121 builds
pass. The initially batched C=1 CDF changed rounding at exact cut points and
was corrected to the former serial order. A separate attention tile split
was slower and rejected. No queue/deployment; GB10/TP4 speed and acceptance
unmeasured. [Receipts and limits](measurements/st_draft_sample_20260917/README.md).

### 45차 — ACE식 전문가 슬롯 스킵의 품질: 10% 에 +0.02~0.03 nats, 스텝 상한 −4.5% 와 맞바꿀 값이 아니다 — 레버를 닫는다 (2026-09-17, TP4 GB10 hold 1회, PR #1102)

바이트 쪽(#1076)의 나머지 절반. 기본 꺼진 노브 `Glm53Net.route_skip`(가중치만 0, id 유지, 재정규화)과 캡처 패스 스케줄을 더하고, **한 부팅에서
133 대화 코퍼스를 다섯 번**(끔 · 전역 10% · 전역 15% · 층별 10% · 끔 반복) 먹여 프리필 head NLL 을 위치 단위로 짝지었다(패스마다 캐시 솔트,
head 시드는 문서 번호 기준). main `ffc8b664` + 노브 + 디버그 팔 `4ff75add`, Red Hat 랭크.
- **잡음 바닥**(끔 반복): fit +0.0011 [−0.0008, +0.0042], 위치의 94.9% 가 같은 값.
- **전역 10%:** fit **+0.0323 [+0.0085, +0.0552]**, held-out +0.069. **층별 10%:** fit **+0.0244 [+0.0008, +0.0458]**.
  **전역 15%:** fit +0.0070 [−0.0306, +0.0406] — 세 팔의 구간이 겹쳐 순위는 못 가린다.
- 스킵한 패스는 위치의 99.5% 를 바꾸고(45% 좋아짐·55% 나빠짐) 문서별 평균 변화가 ±0.1~0.4 nats 로 퍼진다.
- **판정.** 스킵 10% 의 대가(+0.02~0.03 nats)는 09-14 에 택하지 않은 NVIDIA 랭크의 +0.034 와 같은 크기이고 얻는 것은 스텝 상한 −4.5%
  (수용률 손실 미반영). 기본값 후보가 아니다. 디코드 수용률·step/s 는 재지 않았다. 노브와 한 부팅 다중 패스 비교는 남긴다(기본 꺼짐).
  [방법·표·원시](measurements/st_ace_quality_20260917/README.md).

### 45차 — DSA 디코드 선택의 긴 행 비용은 병렬성이다: 부분빈 분할은 정확하지만 이득 0, #1078 의 장치 사실 정정 (2026-09-17, srv4 단일 GPU 레인 2회, PR #1104)

#1078 이 "48 KB 공유 메모리 → 빈 캐시가 빠지고 stash 2,560 → 넓은 빈이 행을 8 번 재독"으로 읽은 절벽을 고쳐 보고 원인을 쟀다. 합성 logits.
- **정정.** GB10 `shared_memory_per_block_optin` 은 **101,376 B**(예산 93,184). 빈 캐시는 후보 약 76,800 까지 남고 stash 는 32K 에서 3,776,
  131K 에서 5,824 다. #1078 의 24,576·2,560 은 opt-in 이 아닌 기본 블록 크기로 셈한 틀린 값이다.
- **부분빈 분할**(넓은 빈을 fp32 키 범위 256 칸으로 한 번 더 나눔, `topkfix-0917`): 일곱 분포에서 불일치 0, 시간은 그대로
  (8×131,072 indexer 231.7 µs 대 원래 228.4, HPC-Ops 53.0). **머지하지 않는다**(패치만 보관).
- **매개변수 스윕**(`topkparams-0917`, 새 레인 `topk_params`): 8×131,072 에서 stash 1,024·2,560·5,824 가 2% 안에서 같고, 블록 1,024 → 512 → 256 이
  226 → 343 → 617 µs. 32K 에서는 빈 캐시가 67.8 → 36.9 µs. **긴 행은 행 하나를 CTA 하나가 맡는 구조의 병렬성에 묶여 있다** — HPC-Ops 는 행을 여러
  CTA 에 나눠 4 배 빠르다.
- **다음(미착수).** 긴 행 KV-split(클러스터 공유 히스토그램) 또는 동점 규칙을 바꾼 HPC-Ops. 디코드 이득은 문맥 128K ~0.15 ms, ~524K ~1.9 ms,
  ~944K ~3.3 ms/스텝(합성)이라 긴 문맥 전용이다. [표·장치 사실·패치](measurements/st_topk_params_20260917/README.md).

### Qwen3.8 이식 — 단일 GPU 레인 첫 실행: 자체 레인 qualify 통과, FP8 320×2560 4행 22% 오차, 정적 MoE 프리필 반복 불일치 (2026-09-17, srv4 단일 GPU 레인 4회, PR #1107)

`engine/QWEN38_CARRY.md` C1·C2·C4·C6 티켓. 체크포인트 없이 `probes/qwen38_config.json` 형상, 합성 가중치다. 속도 주장 없음.
- **C6 qualify(GB10):** 게이트 잔차 max ≤0.0063, GDN decay 1.9e-7, QSA norm+rope ≤0.0069(대역 5e-2).
- **C1 GPU 케이스:** M1 `gated_sum`, S1 `swiglu(pad_to)`, K1 GDN 링 게이트(BF16 바이트 동일), MHCV41, MLA 글루, 패딩 dense 통과.
  오류 3건은 KDA decay 테스트가 7 토큰에 6칸 링을 준 픽스처 결함이다(8칸으로 수정).
- **C2 dense:** 4120×2560·2560×1536 은 1–4096 행 게이트 통과. **shared expert 320×2560 FP8 팔 4행 상대 오차 0.220**(1·2행 0.04).
  그 행을 서빙한 W4A8 은 정상이다. 프로브가 비서빙 팔에서 멈춰 시간은 못 쟀다(이제 `broken_arms` 로 기록하고 계속).
- **C4 MoE:** 디코드 2·4·6·8 토큰 통과(오라클 ≤0.64%, 반복·재생 바이트 동일). **프리필 128 토큰 정적 커널(316 pair)은 같은 입력 두 호출이
  한 원소에서 0.0039 갈라짐**(오라클 0.43% 는 통과). 호출 대 합산 진단을 넣어 재실행한다. 그 전에는 셀을 admit 하지 않는다.
  [표·원시 로그](measurements/qwen38_lane_20260917/README.md).

### 45차 — 융합 라우터 선택·정규화 정렬: 42층 추가 C1 12 µs / C2 28 µs (2026-09-17, srv3 GB10, PR #1112)

`538ad639`, `router-align-rank2-0917`: 동일 빌드의 기존 융합/정렬 융합 B/A/A/B 3회, 실물 42층 gate/bias와 합성 BF16 입력.
rank2 gate/bias 해시 전부가 이전 rank3 프로브와 같다. PyTorch gather/32칸 bitonic 동점 순서를 동점 행에만 적용하고,
가중치 분모는 기존 Triton의 XOR 4/2/1 합산으로 맞췄다. 내적 누적은 기존 융합 그대로다.

- **동일 logits에서 선택 순서·가중치 비트 완전 일치**: 24,768 행 검사, 경계·포화·동점 40개 조건, 독립 스트림 그래프 64회 통과.
  비융합 전체 경로와 선택 집합·순서도 이 표본에서 불일치 0. 내적 차이로 전체 가중치 차이는 최대 8.94e-8 남는다.
- **42층 evicted 비용**: C1 1,403.311 → 1,415.686 µs (**+0.882%, +12.375 µs**),
  C2 1,790.374 → 1,817.900 µs (**+1.537%, +27.526 µs**). warm 추가 비용은 각각 +12.567/+29.311 µs.
- 별도 동일 조건 비융합 대비 라우터 시간은 C1 **−55.737%**, C2 **−44.160%**. BF16 레지스터·스택·공유 메모리 증가 없음.
- **판정**: 작은 추가 비용으로 융합 구현 내부 기본값에 적용. 전체 엔진 `fused_decode_router=False`는 유지한다.
  엔진 step/s·수용률·답변 품질의 새 판정은 아니다. 이전 묶음 품질 하락을 라우터의 인과효과로 확정하지 않는다.
  [상세·원시 기록](measurements/st_router_alignment_20260917/README.md).

### C=1 디코드 스텝 커널 인구조사 — 1,186 런치의 주인, 그리고 원장 두 줄의 정정 (2026-09-17 20:18, 프로덕션 옆, 부팅 없음)

운영자 "어떤 부분이 아직 최적화를 훨씬 더 할수 있을것 같아" → "1위한번 살펴봐" → "측정기록들 남겨".

- **어떻게.** 프로덕션 srv2 rank 0, 릴리스 `3a84c197`, `POST /v1/engine/profile {"steps":24}`. 이 엔드포인트는
  레이턴시 레코딩과 달리 **서버를 예약하지 않아**(409 없음) 라이브 문 옆에서 돈다. C=1·K=7. CUPTI 합이라
  **절대값은 PDL 겹침으로 과대**하고, 유효한 것은 같은 실행 안의 비율과 **런치 수**다(런치 수는 겹침과 무관).
  **속도 판정 아님** — 규칙 2 의 C=1 step/s 채널이 아니다.

      device_us_per_step 59,556    총 런치/스텝 1,186    커널 종류 120

  | 가족 | µs/스텝 | % | 런치/스텝 | 종류 |
  |---|---:|---:|---:|---:|
  | **MoE b12x static** | 26,706 | **44.8** | 43.8 | 2 |
  | **dense GEMM** | 12,355 | **20.7** | **278.2** | **30** |
  | one-shot 통신 (과대) | 6,327 | 10.6 | 60.3 | 6 |
  | **mHC** | 4,458 | **7.5** | **100.0** | 6 |
  | router (fused) | 2,084 | 3.5 | 42.0 | 1 |
  | 패킷 publish | 1,709 | 2.9 | 48.0 | 1 |
  | KDA | 1,095 | 1.8 | 75.1 | 7 |
  | 드래프터 꼬리(`_absorb`·`query_pair`·`_gate_partials`) | 1,072 | 1.8 | 44.2 | 4 |
  | causal conv / MLA / kpool / indexer / DSA / top-k | 249·240·49·24·26·18 | **합 1.0** | | |

- **정정 1 — `st_router_fused` 는 서빙 중이다.** `#1080`·`#1085` 가 "어느 레인도 안 묶는다" 를 근거로 probe
  전용(`PROBE_MODULES`)으로 등록했는데, 이 표에 `st_router_fused<bf16,8,true>` 가 **42 런치/스텝 2,084 µs** 로
  찍힌다. `#1086` 이 디코드 소비자에 연결하며 `MODULES` 로 옮기고 `PROBE_MODULES = ()` 로 비웠다 —
  `#1085` 가 넣은 게이트("probe 모듈을 레인이 묶으면 MODULES 로 올려라")가 그 이동을 강제했다. 게이트가 일했다.
- **정정 2 — "torch DSA top-k 0.85 ms" 는 이 형상에서 사라졌다.** `st_c1_deep_ring_20260917` 이 dense 를
  닫으며 남긴 레버 목록의 셋째다. 지금 측정은 `st_dsa_select` 26 + `gatherTopK` 18 + `bitonicSort` 15 +
  `_select_logits` 9 = **75 µs (0.1%)**, **11배 차이**. 사이에 `#1078`·`#1086` 이 들어갔고 어느 커밋이 옮겼는지는
  이 측정으로 못 가른다(브래킷이 아니다).
- **정정 3 — 작은 커널은 레버가 아니고, QSA 는 GLM 경로에 없다.** kpool 49 + indexer 24 + MLA 240 +
  conv 249 = **0.9%**. `QSA` 는 **런치 0**(Qwen3.8 커널). "코드에 최적화 기법이 적게 보인다"만 보고 후보를
  꼽으면 틀린다 — 시간 비중을 같이 봐야 한다.
- **남는 것 — mHC 가 제일 크고, 원장과 독립적으로 일치한다.** `st_c1_deep_ring_20260917` 의 남은 레버 첫 줄이
  "mHC 발당 50 µs(4.4 ms)" 였고, 다른 부팅·다른 방법인 이 표가 **100 런치 4,458 µs, 발당 44.6 µs** 를 말한다.
  `mk_mhc_packets` 가 **템플릿 두 벌로 층당 2회**(54발 42.7 µs + 30발 54.4 µs) 돈다 — 라우터가 42발→1발로 접어
  C=1 −1.83 ms 를 낸 것과 같은 무늬(`st_c2_levers_survey_20260917`). **접을 수 있다는 주장이 아니라, 남아 있으면
  여기라는 관찰이다**; `#1086` 이 방금 이 자리를 손댔으므로 이미 작업 중일 수 있다. 둘째는 드래프터 꼬리 1.8%.
- **안 잰 것.** 속도 판정 없음(CUPTI 합). **통신 10.6% 는 상한** — `k_oneshot` 이 3.8발 2,973 µs(발당 782 µs)로
  찍히는데 같은 부팅의 배치 이벤트는 8행 31.7~33.2 µs 를 말한다(PDL 겹침 합산). **공유 전문가는 분리 불가**
  (MoE 라우팅 커널에 융합) — 원장이 지목한 "라우팅 커널 안의 공유 전문가(W4A4)" 는 이 방법으로 확인도 반박도 안 된다.
  C=2 없음(한 폭만), rank 0 만, 반복 1회(런치 수는 결정적이나 µs 는 요청 하나의 무늬를 탄다).
  [상세·원시 기록](measurements/st_c1_step_census_20260917/README.md).

### 집합통신 낙오 편차, 다시 재기 — 대기의 절반이 편차이고 그 편차는 스텝당 8회에 있다; 프로브 이동·단일 노드 클럭은 둘 다 근거가 없다 (2026-09-17 20:36, 프로덕션 라이브 문, 부팅 없음)

운영자 "tp=4 대기를 줄일수 있는 방법은 없나" → "낙오편차를 어떻게 줄여?" → "해봐" → "srv4만 클럭 제한을 1600에서 1800으로 올리는건?" → "프로브를 옮기자" → "기록 남겨".

- **어떻게.** 프로덕션 srv2 rank 0, 릴리스 `3a84c197`. 라이브 문에서 `diagnostic=true, concurrency=1` 레이턴시 기록을
  잡고 64토큰 요청 하나를 흘려 네 랭크 CUPTI 디코드 트레이스를 회수했다. 분해는 **09-14 의 도구 그대로**
  (`st_oneshot_rails_20260914/decompose_trace.py`). 레이턴시 기록은 문을 **예약**하므로 본문 전체가 `try/finally` 안에 있고
  `finally` 가 abort 를 재시도한다(09-16 에 TERM 당한 프로브가 이 예약을 2분간 붙들었다). 측정 뒤 문 200 확인.

  | 집합통신 | 회/스텝 | 평균 대기 | **낙오 편차** | 마지막 발행 뒤 바닥 | 마지막 발행자 |
  |---|---:|---:|---:|---:|---|
  | `publish` | 43.0 | 28.8 µs | **5.4** | 23.4 | {2:58, 0:57, 3:49, 1:8} |
  | `moe_packets` | 36.0 | 36.1 µs | **9.7** | 26.4 | {0:77, 2:30, 1:19, 3:18} |
  | `consumer` | 5.8 | 490.7 µs | **458.3** | 32.4 | {3:15, 0:5, 1:3} |
  | `max64` | 1.0 | 458.4 µs | **441.6** | 16.8 | {0:3, 3:1} |
  | `gather64` | 1.2 | 85.4 µs | 71.8 | 13.6 | {0:4, 3:1} |

  스텝당 총 대기 약 **5.0 ms** 로 09-14 와 같지만 구성이 다르다 — **바닥 2.4 ms, 낙오 2.6 ms.** 같은 부팅의
  one-shot 게이지가 편차 없는 바닥을 따로 준다: `sum_8rows 32.7 / 16rows 45.6 / 32rows 74.6 / max_8keys 15.0`,
  행 기울기 1.6~1.8 µs/행, 0행 외삽 16~20 µs — 09-14 의 "고정 16 + 64 KiB 에서 14" 와 일치.
- **둘째 레일(#944)의 이득이 실측으로 붙었다.** 09-14 는 그 이득을 *"Arithmetic for the second rail, not a
  measurement"* 로 적었다. 같은 방법 재측정: `publish` 38.1→**28.8** µs(편차 8.1→**5.4**), `moe_packets`
  44.1→**36.1**(편차 12.0→**9.7**). **대량 통신 79회(86%)는 편차 5~10 µs 로 붙어 있고 마지막 발행자가 네 랭크에
  고루 흩어진다 — 여기는 남은 것이 없다.** `consumer` 는 조건이 달라(09-14 동기 재생 대 오늘 `decode_iterations=4`
  burst 실서빙) 직접 빼지 말 것.
- **세 가설을 세우고 셋 다 트레이스가 기각했다.** 낙오가 큰 8회에서 랭크 3(srv4, 상시 측정 박스)이 `consumer`
  마지막 발행자 23회 중 15회여서 "프로브 부하 오염" 이 자연스러웠다. 같은 트레이스의 랭크별 일:
  계산 µs {0:19348, 1:20063, 2:19493, 3:19681}, 커널 수 885/886/886/886, span 상대 {0:100.0, 1:99.2, 2:100.6, 3:99.8}.
  **네 랭크가 ±3.7% 안이고 계산이 제일 긴 것은 랭크 1 이다.** 랭크 3 의 전송 시간은 다른 셋의 절반(4,243)인데,
  늦게 도착한 랭크는 남을 안 기다리므로 짧다 — **랭크 3 은 늦게 오는 쪽이지 일이 많은 쪽이 아니다.** 남는 설명은
  레일 위상(`OSAR_RAILS=2` 의 쌍 {0,1}·{2,3} 비대칭)이고 **이 측정으로 확정하지 않는다**(레일별 타임스탬프 없음).
  따라서 **프로브를 다른 노드로 옮겨도 이 편차는 줄지 않는다**; 옮기면 대상 박스가 새로 부하를 지고 원장의 셀
  판정 기준기(`srv4 단일 GPU 레인`)만 바뀐다.
- **클럭 캡은 플릿 전체다.** `/etc/systemd/system/gpu-clock-cap.service`(2026-08-29, 네 노드 active,
  `nvidia-smi -lgc 0,1600`, "fleet thermal cap"). max SM 3003 · applications 2418 · 96% 부하 관측 1573~1592,
  온도 60/57/60/61 °C, throttle 사유 네 노드 모두 0x0. **srv4 만 낮지 않다**(제일 낮은 순간값은 랭크 1). 한 노드만
  올리면 srv4 가 플릿에 없는 기계가 되어 거기서 나온 셀 판정이 이전 기록과 대조 불가가 된다. **캡 자체는 이
  기록이 판정하지 않는다** — 운영자가 명시해 건 것이고 올린다면 네 노드 동시에.
- **남는 것: 스텝 경계 2회.** 편차는 `max64`(1회/스텝, 바닥의 26배)·`gather64`·`consumer` 에 몰려 있다. 셋은
  샘플링·커밋 부근의 스텝 경계 집합통신이고 랭크 0 만 하는 호스트 일(허가·마감·취소 방송·샘플링)이 그 경계에
  앉아 있다 — 대량 79회에서는 안 보이고 경계에서만 보인다. 09-14 의 *"The bounded burst loop has no such
  boundary"* 는 이 부팅이 이미 `decode_iterations=4` 인데도 경계가 남아 있으므로 **갱신이 필요하다.**
- **안 잰 것.** 속도 판정 아님(아무것도 안 고쳤다). 한 번·C=1·4스텝·반복 없음. **레일별 분해 없음** — 위상 설명은
  가설이다. **랭크 0 호스트 일이 경계 편차를 낸다는 것도 가설** (호스트 타임라인이 트레이스에 없다).
  **클럭 캡을 올린 실험 없음.** 트래픽은 64토큰 요청 하나, 긴 컨텍스트·C=2·C=4 없음.
  [상세·원시 기록](measurements/st_collective_skew_20260917/README.md).

**Qwen3.8 서빙 레이아웃 v3 — PLE 표를 SSD 로, NVIDIA 허브 체크포인트, 드래프터는 MTP (2026-09-18, 운영자 지시 "그냥 mtp로 해 /
e뭐시기 그건 ssd로 내리고 / 이미지는 파트로 사전 샤딩해서")**
- **무엇.** `profiles/qwen38/preshard.py` 가 `nvidia/Qwen3.8-Flash-Next-NVFP4` @ fc694b54(srv2 `~/models/qwen38-flash-next-nvidia-nvfp4`,
  132.7 GB, 25 파일 전부 허브 sha256 일치)를 읽는다. `quant_algo MIXED_PRECISION` 의 `quantized_layers` 로 "NVFP4 는 48 층의
  라우팅 전문가뿐, PLE 표는 FP8, MTP 전문가는 FP8 블록스케일" 을 확인한다(`facts.quantised`; 예전 복사본의 NVFP4 전용 config 도
  그대로 읽는다). MTP 전문가는 `weight_scale_inv` 를 **곱해** 역양자화한 뒤 NVFP4 로 인코딩한다 — srv2 에서 예전 복사본의 BF16
  MTP 와 대조: 전문가 0/1/77/300/511 의 gate/up/down 모두 상대오차 0.0266(FP8 반올림), 나누면 10^7. PLE 표는 랭크 파일 밖
  `ple-r{r}of4.weight`(그 랭크의 32 샤드를 이어 쓴 e4m3 행, 쓰면서 sha256, 되읽어 대조) + `ple-r{r}of4.json`; 서빙 넷은 행을
  번호로 읽는다(`ple_table.py`: 즉시 스텝은 호스트에서 해시해 모으고, 캡처 스텝은 재생 전 `net.stage_ple` 가 그래프의 정적
  스테이징 행을 채운다; 다른 랭크의 행은 0 바이트라 all-reduce 가 그대로 합친다). plan.py 인구조사의 랭크당 상주 31.83 → 19.91 GiB.
  라우팅·MTP 전문가 그룹을 랭크별로 나눠 프리샤드가 한 번에 쥐는 것은 한 층의 4분의 1(상주 약 2 GiB 추정).
- **재었다(srv2 NVMe, 기존 `qwen38-ple-ssd/ple-r0of4.weight` 12.8 GB).** 두 체크포인트 복사본의 표 바이트는 같다(shard_0·
  weight_scale·layer_multipliers sha 일치; 그 파일의 첫 400,001,920 B = shard_0, 전체 sha256 ae7e9d59…). 무작위 160 B 행
  읽기, 최선값: 버퍼드 pread 1 스레드 128행 1.2 ms · 8 스레드 1.0 ms(131K 행/s) · 32 스레드 1.7 ms; 20,000행 8 스레드 71 ms
  (280K 행/s), 32 스레드 96 ms; O_DIRECT 4 KiB 섹터 읽기는 128행 3.0~25.8 ms, 20,000행 219~800 ms — 버퍼드 8 스레드를 쓴다.
  디코드 스텝(4행×2토큰×16헤드 = 128행, 랭크당 약 4분의 1)은 스텝당 1 ms 미만, 32K 토큰 프리필 청크(랭크당 약 131K 행)는
  약 0.5 s — **추정, 미실측**.
  **2026-09-19 정정:** 위 수치는 행마다 파이썬 한 바퀴를 도는 `pread` 루프의 것이고(캐시가 데워져도 행당 약 3.5 µs), 읽기를 파일의 읽기 전용
  매핑 위 `np.take` 한 번으로 바꿨다(`profiles/qwen38/ple_table.py`). 개발 PC(WSL2 ext4, 합성 2 GiB 표 — 플릿 수치 아님): 웜 131,072 행
  4.6 s → 1.4 ms, 128 행 4.2 ms → 0.38 ms; 콜드(페이지 축출, 방법마다 프로세스 분리) 20,000 행 8 스레드 857 → 552 ms, take 는 GIL 을
  놓아 32 스레드 183 ms 까지 확장. 32,256 토큰 청크의 호스트 쪽 전체(해시·분할·모음·흩기)가 약 10 ms. **srv2·GB10 미실측**, `THREADS`·
  `SPLIT_AT` 은 루프 시절 srv2 값 그대로.
- **CPU 판정.** `tests/test_engine_qwen38_preshard.py`: 합성 NVIDIA 레이아웃 체크포인트(혼합 정밀도 config, 전문가별 NVFP4 네
  텐서, MTP FP8 블록 전문가, PLE 샤드+스칼라 스케일)를 TEP=4 로 잘라 왕복 — 랭크 파일의 스펙 전부(표 파트 없음), 표 파일 = 그
  랭크 샤드 바이트, 사이드카·SHA256SUMS·매니페스트·shape 기록, MTP NVFP4 == dequant 후 `nvfp4_from_bf16`, 두 번째 실행 거부.
  `tests/test_engine_qwen38_ple_ssd.py`: 표 gather(두 읽기 경로·중복·범위), 랭크 분할, 스테이징, 즉시 경로 == 캡처 스테이징
  (링의 carried id·DEAD 포함). `base/checkpoint` 가 두 번째 `.layers.` 접두(`mtp.layers.0.`)를 허용한다 — 전에는 Qwen 인덱스에서
  죽었다.
- **산출물(srv4, 15:26~15:38, 699 s).** 네 노드 모두 프로덕션이 떠 있어(MemAvailable 9.8~13.3 GB, earlyoom 바닥 6 GiB) 체크포인트를
  srv2→srv4 로 fabric(10.10.10.2) rsync 해(124 GiB, 4 분, 허브 sha256 재검증) srv4 에서 3 GiB 캡·ionice idle 컨테이너로 잘랐다.
  첫 시도는 5 초 만에 캡에서 죽었다(rc 137): `base/checkpoint.load` 가 1.27 GB 임베딩을 스테이징 두 번(범위 버퍼 + 블록 복사)으로
  쥔다 → `Checkpoint.views`(numpy memmap; 정렬 안 된 작은 텐서만 복사)로 바꿔 최고 약 2.4 GiB(MTP 전문가 그룹). 결과
  `~/models/st-qwen38-tep4`: `rank{0..3}of4.safetensors` 21,066,082,728 B(sha 0a748bcc·9ed43df9·807052c3·6bc8ca3f, 1,148 텐서
  되읽어 대조), `ple-r{0..3}of4.weight` 12,800,061,440 B(**랭크 0 = ae7e9d59… = 09-11 의 `qwen38-ple-ssd/ple-r0of4.weight` 와
  바이트 동일**; e4a7f934·a6339811·0d1adad6), 사이드카·`kernel_shape.json`·매니페스트·SHA256SUMS. 중단된 첫 완주 시도의 랭크 0
  sha 가 같았다 — 프리샤드는 재현적이다. 랭크 r 은 GLM 과 같은 자리(rank0 srv2·rank1 srv1·rank2 srv3·rank3 srv4)로 fabric
  rsync + 도착지 `sha256sum -c` 로 배포했다(`~/st-worktrees/qwen38-distribute.sh`).
- **빌드 중 프로덕션 사고(빌드와 무관).** 15:09:47 srv1 랭크가 프리필 중 Triton JIT 로드에서 `CUDA: operation not permitted`
  (`kda_output_norm` → `load_binary`)로 죽고(exit 1), 나머지 랭크는 NCCL 300 s stall trap 으로 15:16 종료(133). 제 복사는 srv2→srv4
  (15:04~15:08), 프리샤드는 srv4 (15:11 이후)뿐이라 srv1 을 건드린 게 없다. 감독기가 15:26:16 재기동, 15:28:22 healthy — 제 빌드가
  옆에서 도는 동안 승인이 통과했다(srv4 MemAvailable 22 GB). 재기동 승인과 겹치지 않으려 두 번째 시도를 한 번 멈췄다가
  운영자 "얼른해" 로 즉시 다시 돌렸다.
- **첫 플릿 부팅(운영자 "올려봐", 16:14~16:47 세션 창).** `fleet_lease.py yield --kind session` 으로 프로덕션에 양보를 청하니
  quiet gate 가 몇 초 만에 넘겨줬고(감독기는 "fleet taken (session)" 로 대기), `launchers/start-st-qwen38.sh` 가 이 트리의
  `st-engine:qwen38`(네 노드 사전 빌드, 캐시 레이어라 초 단위)로 네 랭크를 올렸다. 랭크 0 "ready in 107.4 s"(MoE 마이크로 커널 JIT
  포함), 재부팅은 40.1 s(캐시). 문이 열리고 프리필은 돌지만 토큰은 나오지 않았다 — 두 가지:
  (1) **one-shot RDMA 집합통신이 hidden 2560 에서 스톨**: 네 랭크 모두 `[oneshot] STALL phase=wait(peer-flags)` 를 반복하며 합 하나에
  약 2 s(세 피어가 늘 4 시퀀스 뒤). kernel_shape 기록의 oneshot 셀은 '미실측' 이었다. `--no-oneshot`(launcher `ST_ONESHOT=0`) 을
  더해 NCCL 로 재부팅(engine/base/comm 은 transport 가 없으면 NCCL 로 간다).
  (2) **즉시 프리필의 MoE 정적 커널 JIT 폭풍**(QWEN38_CARRY C4 가 예고한 것): 67 토큰 프롬프트에서 랭크마다 `static_m{행}_k2560_n640_t1_r{전문가}`
  변형을 (행 수, 전문가 수) 조합마다 약 4 s 씩 컴파일 — 랭크 0 이 12 분에 75 개, 프리필 스텝 1 이 5 분 넘게, 디코드 스텝 0, 생성 토큰 0.
  `select_sm120_moe_backend` 가 routed pairs < 640 이면 static 을 고르고 Qwen 의 EP 로컬 프리필은 늘 그 아래다. 캐시에 남으니 언젠가는
  수렴하지만 조합 수가 수백이라 창 안에서는 안 끝난다. 창을 닫았다(`stop` + lease release; 프로덕션은 감독기가 복귀).
- **두 번째 창(17:01~17:10, 운영자 "두 문제 해결해 / 폴백경로 말고 자체 경로 / 부팅도 한번 해보지그래", PR #1180).**
  (2) 는 자체 경로로 고쳤다: `select_sm120_moe_backend` 에 expert-local 즉시 프리필(한 행에 한 라우트, 모든 전문가 로컬, 8 행 초과)은
  b12x **dynamic 프리필 커널**로 가는 규칙 하나 — 아티팩트가 행 수와 무관(`dynamic_e128_k2560_n640_t1_…`), 이번 부팅 랭크 0 의
  컴파일 2 개(첫 창 75 개). GLM 의 모든 기하와 디코드 크기는 그대로(`tests/test_engine_moe_backend_prefill.py`). (1) 은 고칠 것이
  없었다: one-shot 기본으로 재부팅해 **STALL 0** — 첫 창의 스톨은 피어들이 커널을 컴파일하느라 늦은 증상이었다. 창 안에서 먼저
  C4 프로브(srv4 단일 GPU, `ST_PROBE_CHECKS_ONLY=1`)로 dynamic 프리필 셀을 판정: 16/64/128/1024/4096 토큰 오라클 상대오차
  0.0080/0.0069/0.0089/0.0054/0.0074(문턱 2%), 디코드 micro 0.0053~0.0064·재생 바이트 동일; 프리필 반복은 ±1 bf16 ulp 급
  (BF16 원자 scatter 순서, GLM 프리필과 같음). 플릿(53.5 s ready): 17×23 → "391 …", 한글 답 정상("서울특별시 … 약 940만 명"),
  450 토큰 생성 11.43 s = **39.4 tok/s incl. prefill**; decode 394 스텝 13.90 s → **35.3 ms/스텝**, 687 토큰 → **1.74 토큰/스텝**
  (MTP K=1 수용 82/114 = 72%) → 디코드 약 49 tok/s; 웜 프리필 1,767 토큰 ≈ 0.5 s(3.5K tok/s), 콜드는 새 pair 대역마다 아티팩트
  JIT(1,761 토큰 34 s). [상세·원시 기록](measurements/qwen38_fleet_boot_20260918/README.md).
- **안 한 것.** C=2/4, T=1 수용률, 긴 컨텍스트, one-shot 지연 실측(`ONESHOT_MEASURED_HIDDEN` 기록은 별도), 프리필의 SSD 모음 비용
  분리, 정확도 벤치; MTP 이중 양자화(FP8→NVFP4)의 수용률 비교 — 예전 복사본의 BF16 MTP 로 자르면 한 번 양자화다(같은 표·전문가
  바이트, `--ckpt` 만 다름).

### Qwen3.8 부팅 시간 — 공유 캐시에서 프로덕션과 창이 서로의 MoE 커널을 지운다, 랭크 0 의 문 준비 7 s 는 캡처 뒤 직렬이었다 (2026-09-18, 부팅 0회 — 다른 세션의 창 셋과 프로덕션 부팅 넷의 로그)
- **교대 삭제(읽었다).** 네 노드의 `/cache` 는 프로덕션과 창의 트리가 함께 쓰고, b12x CuTe-DSL MoE 커널의 flashinfer 모듈
  `st_b12x_moe_sm121a_cute_dsl` 은 이름이 키 파일 **이름**만으로 정해져 있었다. flashinfer 는 `meta.json`(키 파일 **내용** 해시)이 다르면
  모듈을 통째로 지우므로, 17:35 Qwen3.8 창 3 부팅이 프로덕션의 커널을, 17:48 프로덕션 부팅이 창의 커널을 지웠다(두 로그 모두
  "Invalidating stale", 17:35~17:48 에 다른 작성자 없음). 프로덕션은 정적 커널 6개(앞 5개 연달아 약 9.5 s 씩)를 다시 컴파일해 문 열기
  **150 s**(17:09 도 150 s, 랭크 0 `capture decode` 90.8/91.3 s), 같은 날 15:26·16:48 은 105 s(로그 없음). Qwen3.8 과 GLM 의 커널 형상은
  겹치지 않아(k2560·n640 대 k4096·n512) 공유로 얻는 것은 없다.
- **Qwen3.8 앞부분(랭크 3, 17:34, 컨테이너 타임스탬프).** 박스까지 2.87 s, `Comm.init`+one-shot 5.53 s, `lanes.served`+`qualify` 3.96 s,
  아레나·로드·팩 랑데부까지 12.42 s. `fleet.py` 는 단계 표를 만들고도 출력·덤프하지 않았다.
- **문 준비(ST 이미지, CPU 컨테이너, 새 프로세스).** 랭크 0 의 토크나이저+챗 템플릿+판독 7.04 s, 랭크 1~3 0.60 s, 커널 임포트 1.76 s.
  이전 부팅은 랭크 0 에서 이것을 캡처 **뒤** 직렬로 썼다.
- **변경(이 PR).** `moe_dispatch._cute_dsl_module` 이 키 파일 내용 해시로 모듈을 짓는다(direct micro 도). `qwen38/fleet.py` 가 커널 임포트를
  랑데부 밑, 문 준비를 로드·팩 밑 스레드로 돌리고 캡처 전에 합류, 단계 행과 `boot-rank{r}.json`·`memory-rank{r}.json`(`--dump-dir`)을
  남긴다. GLM 의 `Background` 는 `engine/base/background.py` 로(동작 동일).
- **안 잰 것.** 이 변경의 부팅은 없다 — 절감은 추정(창 뒤 프로덕션 최대 약 45 s, 프로덕션 뒤 Qwen3.8 의 b12x 콜드 컴파일, 랭크 0 문 준비
  최대 약 7 s + 임포트 약 2 s, GIL 경합 미지). 이 트리의 첫 배포는 새 모듈 이름으로 b12x 커널을 한 번 다시 컴파일한다.
  [상세·원시 기록](measurements/qwen38_boot_20260918/README.md).

### Qwen3.8 부팅 창 — 웜 60.3 s(워밍업 빼면 28.2 s), 첫 요청 0.46 s·컴파일 0, 32K 청크 워크스페이스 8.29/12 GiB, 창 뒤 프로덕션 105 s (2026-09-18 20:22~20:33, main f1d4dd9a, 프로덕션 다운 약 11 분)
- **부팅 두 번(#1187·#1195 첫 플릿 부팅).** 콜드 ready 216.4 s(워밍업 177.0 — 프리필 커널 첫 컴파일이 여기에 몰림), **웜 60.3 s**:
  front 3.8 · comm+lanes+qualify 4.7 · arena+load 4.7 · prepare dense 6.9(가중치 해시 2.5~3.4 s, 팩 196 개 캐시 적중) · **warm prefill 32.1**
  (32K 메모리 패스 26.1) · capture 8.4. 워밍업을 빼면 28.2 s 로 첫 창 웜 40.1 s 보다 11.9 s 짧다; 워밍업이 32.1 s 를 도로 더했다.
- **얻은 것.** 첫 요청 0.46 s("1+1" → "둘"), 요청 중 CuTe 컴파일 0 건(첫 창은 첫 프리필 5 분+, 토큰 0). 32,256 토큰 한 forward 의
  워크스페이스 최대 **8.29 GiB / 12 GiB** — #1183 뒤 처음 잰 값.
- **창 뒤 프로덕션 105 s, `Invalidating stale` 0·재컴파일 0** — 같은 날 Qwen 창 뒤 17:09·17:48 은 150 s·6 개(#1187 확인).
- **사전 컴파일은 기록됐고 micro 키가 어긋났다.** 노드마다 요청 10~12 중 새로 빌드 4 = micro 커널 넷. 재생에 커널 형상이 묶이지 않아
  GLM 셀로 타일을 골랐다 → 이 PR 이 기록에 형상을 넣고 재생이 건다; CPU 재생 이름이 GPU 부팅 이름(`…_875f80d35b4461fc`)과 같아졌다.
- **안 잰 것.** 사전 컴파일의 실제 부팅 효과(트리가 바뀐 다음 창), 메모리 패스 26 s 를 매 부팅 낼지(결정 대기), 속도.
  [상세·원시 기록](measurements/qwen38_boot_window_20260918/README.md).

### Qwen3.8 이식 — QSA 접기 셋의 GB10 판정: 묶음 점수·dense 어텐션은 기존 발사의 바이트, radix select 의 행 안 순서는 발사의 것 (2026-09-18, srv4 단일 GPU 레인 2회, PR #1196)

`engine/QWEN38_CARRY.md` Q8 · Q10 · Q11 의 서빙 커널 케이스. 티켓 `qwen38-qsa-folds2-0918`(트리 `af6032de`)과 재실행 `qwen38-qsa-folds3-0918`(`adb2a624`), 둘 다 큐 수정 #1198 체리픽 위.
체크포인트 없이 `probes/qwen38_config.json` 형상, 합성 입력. **속도 주장 없음 — 전부 정합 판정이다.**
- **첫 실행 51 건 중 50 통과, 재실행 51 건 전부 통과(skip 0).** qualify 값은 09-17 기록과 같다(게이트 잔차 max 0.0063, QSA norm+rope ≤0.0069).
- **Q8(한 요청의 연속 행 최대 4 개가 인덱스 키 타일을 한 번 읽는 점수 커널):** 행 발사 대비 logits·visible 수·선택이 바이트 동일 — 캡처 스텝
  모양(행당 2·3·4 토큰), 짧은 마지막 묶음, 32 행 발사 기하 너머, 두 선택 진입점.
- **Q10(예산이 덮는 스텝의 dense causal 어텐션):** 모델 실폭(6×256, 예산 2048, 2,051 행까지)에서 sparse 발사 대비 `torch.equal` + raw bits —
  세그먼트 여럿, 모든 split 프로필(64·32·8·4·1), 묶음 1..4, KV 헤드 1·2, 게이트 유무.
- **Q11 덮인 절반:** 덮인 스텝의 점수 선택을 어텐션이 읽는 순서로 놓으면 위치만으로 만든 id 와 바이트 동일.
- **실패 1 건 = 발견.** Q11 랭크 절반의 GB10 전용 케이스(400 행, 양쪽 모두 네이티브 radix select): 분할한 선택과 분할하지 않은 선택의 **집합은
  모든 행에서 같다**(어텐션이 읽는 오름차순으로 비교, 통과). 그러나 **radix select 가 행 안에 id 를 남기는 순서는 행의 것이 아니라 발사의 것**이다 —
  같은 행이 랭크의 100 행 발사와 스텝의 400 행 발사에서 다른 순서로 나왔다. 테스트가 id 대 id 로 단언한 것이 설계보다 셌다(`torch.topk` 는 행을 그
  행만으로 정렬해서 인터프리터·RTX 5050 에서는 통과했다). 엔진에는 영향 없음 — 선택기 순서대로 블록을 읽는 곳이 없다(#1110 의 정렬). 테스트를 집합
  비교로 좁혔고(`fa725053`), 재실행은 레인이 srv4 의 방을 2 분 기다린 뒤(MemAvailable 24.0 GiB, 예산 8, 바닥 16 — 예산을 낮추지 않았다) 전부 통과했다.
- **큐 결함 하나:** main 의 `fleet_prepare.py create` 가 #1152 이후 TypeError 로 죽어 `fleet.sh run` 으로 티켓을 낼 수 없었다 → #1198.
  [표·원시 로그](measurements/qwen38_qsa_folds_20260918/README.md).

### Qwen3.8 이식 — Q7(디코드 블록 선택 한 발사) GB10 통과, 부팅 qualify 가 한 번 죽고 재현되지 않음 (2026-09-19, srv4 단일 GPU 레인 3회)
- **Q7 판정.** 티켓 `qwen38-cells-0919b`(main `51a332a4`, 프로덕션 옆, 예산 8 GiB): `engine_kernel_check --lanes qwen38_cells` qualify 통과, GPU 케이스 **57 건 전부 통과**(118 s) — `SelectTests` 6 건(#1202: k 512, 32,768 열까지 규칙 대조, 동률은 낮은 블록부터, 동률 없는 점수에서 torch.topk 와 같은 집합) 포함.
- **발견.** 3 분 앞의 같은 프로브(`qwen38-cells-0919`, main `3ba51e9c`, 엔진 코드 동일)는 `lanes.qualify` 에서 죽었다: `norm_rope_4x128 (0.938, 0.0171)` — 문턱 0.05 / 0.02. 재실행 값은 `(0.00690, 0.00232)` 로 09-18 실행·RTX 5050 과 소수점까지 같다. 크기는 헤드 하나의 회전 절반 32 채널 정도. "같은 주소 두 번 저장의 경합" 가설은 컴파일된 TTGIR 로 기각(32 원소 저장은 모든 워프가 `tid % 32` 로 중복 실행). 양쪽이 모두 GPU 계산이라 어느 쪽이 틀렸는지 모른다. 이 qualify 는 플릿 부팅도 돈다 — **원인 미상, 빈도 미실측.**
- **headroom 프로브**(`engine_qwen38_hc_mix_fused.py`, 예산 4 GiB)는 4 s 만에 임포트로 죽었다: 레인은 `engine/`·`probes/` 만 보내는데 `bench.probe_report` 를 임포트했다. 고쳤고 다시 넣는다. 속도 주장 없음. [표·원시 로그](measurements/qwen38_lane_20260919/README.md).

### Qwen3.8 이식 — 믹서 사이트의 발사 여유(headroom)는 GB10 에서 2–3 µs; 같은 프로브의 GEMM 시간은 캐시 값이라 읽지 말 것 (2026-09-19, srv4 단일 GPU 레인)
- 티켓 `qwen38-hc-headroom-0919b`(main `625b1ff1`, 예산 4 GiB, 프로덕션 옆): 캡처한 믹서 한 사이트에서 headroom(레인 − 그 두 cuBLAS GEMM)이 1·2·4·6·8 행에 **2.0–2.8 µs.** #1201 이 남긴 질문의 답 — GLM 원장의 발사 고정비 15–25 µs 는 이 사이트에 없고, 발사 접기만의 상한은 스텝 100 사이트에 0.2–0.3 ms.
- **프로브의 결함:** 같은 13.2 MB 가중치 쌍을 재생마다 읽어 두 GEMM 이 31 µs ≈ 423 GB/s(> UMA 273 GB/s = 캐시)로 나온다. 이 숫자로 H6 의 크기를 재면 안 된다 — 가중치를 돌려 읽는 #1207 의 하니스는 같은 두 곱을 61–66 µs 로 잰다. H1 · H2 의 판정은 곱 자체를 빠르게 하는 그 PR 의 몫이고, "Triton 접기는 어느 카드에서도 기각"(#1197 · #1201)은 내 프로토타입의 모양에만 맞는 말이었다. 속도 주장 없음. [표·원시 로그](measurements/qwen38_hc_headroom_20260919/README.md).

### Qwen3.8 — 부팅 qualify soak: GB10 에서 2000 회 중 7 회 실패, `norm_rope_partial` 의 쓰기 경합 (2026-09-19, srv4 단일 GPU 레인)
- 티켓 `qwen38-qualify-soak-0919`(main `2665b3bf`): `lanes.qualify` 2000 회 중 **7 회 실패(0.35%)**, 전부 `norm_rope_4x128` 의 300 행 케이스, 헤드 2 의 채널 32..63. #1214 의 blame 이 가른 것: **커널이 틀렸고 반복되지 않는다**(다시 부르면 32 원소가 달라짐), torch 기준은 CPU 와 같다. 통과한 1993 회의 결과는 한 가지.
- **원인:** 커널이 헤드 전체에 정규화 값을 저장한 뒤 회전 채널에 회전 값을 같은 주소로 다시 저장 — D 128 · 4 warps 에서 채널 32..63 은 두 저장의 담당 스레드가 달라 쓰기-쓰기 경합. 같은 프로그램의 `_norm_rope_into` 는 K 캐시·인덱스 키에 직접 쓰므로 서빙에서는 캐시에 남는다. 아침 기록의 "경합 가설 기각"(RTX 5050 의 TTGIR 로 판단)은 틀렸다.
- **고침:** 정규화 저장이 회전 채널을 뺀다 — 바이트 동일, `tests/test_engine_qwen38_store_once.py` 가 발사당 주소 한 번을 고정. GB10 의 0 / 2000 확인은 머지 뒤 같은 soak. [표·원시 로그](measurements/qwen38_qualify_soak_20260919/README.md).

**Qwen3.8 서빙 창 — 최신 origin 이 플릿에서 뜨고 답한다: 기본 K=1 29.4 ms/스텝 · 디코드 59–61 tok/s, K=3 · 4 행 첫 부팅 (2026-09-19 11:19~11:30 세션 창, 운영자 "해봐" / "프로덕션은 내려", 트리 `4a3ec736` = main `233de548` + #1225, 프로덕션 다운 633 s)**
- **무엇.** 09-18 20:22 창 이후 서빙 경로에 들어간 PR 열두어 건(#1193 · #1196 · #1200 · #1202~#1204 · #1207 · #1217 · #1221 · #1225 …)의 첫 플릿 부팅. 런처 기본 구성(one-shot, 4 행), C=1 · greedy · thinking off 요청 일곱 개, `/metrics` 차이로 ms/스텝.
- **기본 K=1.** ready 125.4 s(콜드: 새 Triton 커널 첫 컴파일), qualify 네 랭크 통과, 서빙 중 STALL 0, 답 전부 정상(391, 서울 940만, 3,223 토큰 요약). **디코드 919 스텝 = 29.4 ms/스텝, 1.71–1.79 토큰/스텝(수용 75.9%) → 59–61 tok/s**, 프리필 포함 45–47. 09-18 같은 구성 34.7 ms · 48.8–50.0 tok/s 대비 스텝 −15%, tok/s +20%.
- **K=3 · 4 행(플릿 첫 부팅).** ready 162.0 s, STALL 0, 답 정상. 611 스텝 중 470 이 30–40 ms · 129 가 40–50 ms, **6 스텝이 요청 중 JIT 로 2 s 초과**; 수용 51.6%(스텝당 1.55/3), 2.33–2.91 토큰/스텝, 웜 요청 35.5 ms → 65.5 tok/s. 요청 중 컴파일을 부팅으로 옮기기 전에는 K=3 평균을 K=1 과 나란히 읽지 말 것.
- **안 한 것.** 품질 브래킷, C ≥ 2, ≥ 8K 컨텍스트, T > 0, 웜 부팅, 창 뒤의 main(`b50947cb` #1226: 머지된 기본은 MTP dense BF16 그대로, 기본으로 켜진 것은 `mtp_forward(rows=)` 행 선택). 한 창 · 한 바퀴의 표본이다. [상세](measurements/qwen38_serve_window_20260919/README.md).

### Qwen3.8 — 부팅 qualify soak, #1225 뒤: GB10 에서 0 / 2000 (2026-09-19, srv4 단일 GPU 레인)
- 티켓 `qwen38-qualify-soak-fixed-0919`(트리 `4a3ec736` = main `233de548` + #1225): 같은 시드 2000 회 **실패 0**(고치기 전 `2665b3bf` 는 7), 통과 결과 한 가지, 값은 고치기 전과 소수점까지 같다. 0 / 2000 은 실패율 < 약 0.15%(95%)의 관측이고, 원인은 코드에서 사라졌다(`test_engine_qwen38_store_once`). [기록](measurements/qwen38_qualify_soak_fixed_20260919/README.md).

**Qwen3.8 MoE 정적 디코드 커널 — 10·12 토큰에서 illegal access, 14~32 통과; 16행 패딩으로 8행 사다리 전부 통과 (2026-09-18 18:14~18:24 srv4 세션 창, 운영자 "8행으로 가자", PR #1192)**
- **무엇.** 8행 부팅이 캡처하는 디코드 런치 전부를 C4 프로브로 판정: K=1·K=3 사다리 = 2·4·6·8(micro) + 10·12·14·16·20·24·28·32(정적,
  micro 상한 8 위). 정적 모양은 작은 것부터, 한 프로세스씩(`ST_PROBE_DECODE_TOKENS`; fault 가 CUDA 컨텍스트를 죽인다).
- **판정.** micro 넷 통과(오라클 0.0053~0.0064, 재생 바이트 동일). 정적 **10·12 는 illegal memory access**(12 는 네 행 K=3 부팅의 죽음과 같은
  모양; 그때는 용량 r160, 여기서는 r120 — 용량이 아니다), **14·16·20·24·28·32 통과**(0.0040~0.0069, 재생 바이트 동일). "16 미만"도 "8의
  배수"도 아니다(14·20·28 통과) — 커널 codegen 이 모양 10·12 에서 틀린다, 원인은 격리하지 않았다.
- **고침.** `lanes.static_pad`: micro 상한 위·16 아래의 캡처 런치를 16행으로 채운다(0 행, 이 랭크의 전문가 0, 가중치 0). 채운 10·12·14 →
  `static_m16` 으로 통과(0.0053/0.0063/0.0052). 8행 사다리에서 채워지는 모양은 K=3 의 12 와 K=1 의 10·12·14 뿐. 즉시 프리필은 그대로.
- **레인이 막혀 있었다(고침 둘, 남은 것 하나).** `ST_PROBES` 에 Qwen 프로브 넷이 없었다(승인); `bench/fleet_prepare.py` 가 #1152 뒤로 모든
  `fleet.sh run` 을 `TypeError`(`approve_deploy`)로 죽였다(호출자 수정); 공유 `~/glm53-logs/fleet.sh`(06:34)는 main 의 최근 여섯 판 어느
  것과도 다르다(손대지 않음). 그래서 세션 창 + 로컬 모드로 돌렸다.
- **안 한 것.** 커널 안 원인, 8행 K=3 플릿 부팅과 C=1~8 짝 측정(다음 창), 정적 모양 tile/MAC 스윕. [상세](measurements/qwen38_moe_static_20260918/README.md).

**Qwen3.8 MTP 체인 K=3 — 드래프트 재생 안에서 헤드 세 번: C=1 450 토큰 2.43 토큰/스텝·44.3 ms/스텝, 디코드 tok/s +12.5%, 프리필 포함
+17% (2026-09-18 17:34~17:47 세션 창, 두 행 greedy, 운영자 "k=3 정도 하지" / "k=3가 안되면 k=2까지만해", PR #1182)**
- **무엇.** `fleet --spec-k K`(launcher `ST_SPEC_K`; 체크포인트의 `SPEC_K = 1` 이 기본): `dataclasses.replace(F, spec_k=K)` 로 넷·캐시·모델을
  세우면 spec_k 에서 나오는 링(GDN conv/state, PLE conv, 스토어 ring, PLE 스테이징)은 따라오고 고정 링(QSA raw key 8, PLE id 8)은
  `caches.check_rings` 가 K ≤ 4 를 확인한다. 드래프트 그래프(`decode_graphs.draft_chain`)는 관측 스텝(검증 폭 K+1 로 패딩) 뒤 체인
  K−1 런치(행마다 한 위치, 직전 pick 이 토큰, 헤드 자신의 streams 가 상태)를 **한 재생** 안에서 돌리고 `[행, K]` 를 낸다; 행이 쓰는
  위치는 `max(K+1, m+K−1)`(m 관측 수), 버킷 사다리는 2K 까지. `adapter.ServedMTP` 는 대기 행의 예약이 그 폭을 덮으면 재생, 아니면
  (파킹: 지킨 위치까지만 예약) 관측 위치만 즉시 헤드로 쓴다. CPU 테스트 `tests/test_engine_qwen38_draft_chain.py`.
- **부팅 둘.** 기본 4 행은 캡처에서 죽었다: (3 행 × 4 토큰) 타깃 워밍업, 방금 컴파일한 `static_m12_k2560_n640_t10_r160` 정적 MoE
  커널의 첫 런치 직후 `illegal memory access`(다음 QSA 런치의 Triton `load_binary` 에서 드러남). 16 토큰(`static_m16`)은 지났다. 12 토큰은
  micro 상한 8 을 넘어 정적 커널로 가는 Qwen 의 첫 모양 — 단일 GPU 레인에서 격리(C4 프로브에 디코드 12/16 토큰 검사). `ST_MAX_SEQS=2`
  로 다시 올려(검증 micro m8/m4, 체인 m2/m1) 잰 뒤 같은 두 행으로 K=1 을 짝으로 올렸다(41.6 s ready).
- **짝 측정(같은 이미지·두 행·C=1·greedy, 둘째 바퀴).** 450 토큰 요청 둘: K=1 34.7/33.9 ms/스텝·1.69/1.70 토큰/스텝·디코드 48.8/50.0
  tok/s·프리필 포함 39.5/40.3 → K=3 **44.3/42.4 ms/스텝·2.43/2.32 토큰/스텝·54.9/54.8·46.2/45.7**: 토큰/스텝 +44/+36%, 스텝 +28/+25%,
  **디코드 tok/s +12.5/+9.6%, 프리필 포함 +17/+13%**. 짧은 요청(23~26 토큰): K=1 31.3~31.9 ms·1.86~1.92 → K=3 39.9~41.5 ms·2.56~2.89.
  드래프트 슬롯당 수용 K=1 69~71%, K=3 46~48%(세 자리 평균). 답 전부 정상(391, 서울특별시·940만, 영어·한글 350 단어). STALL 0.
  [상세](measurements/qwen38_fleet_boot_20260918/README.md).
- **콜드 비용(발견).** 파킹의 즉시 헤드가 (행 수, 용량)마다 `micro_m{1..4}_…_t1_r{3,4}` 를 한 번 컴파일(랭크마다 4~5 s) — 첫 바퀴
  550~650 ms/스텝 평균, 둘째 바퀴 앞 두 요청 175·83.5 ms/스텝. 캐시에 남는다. 부팅에서 미리 데우거나 파킹 경로를 재생으로 돌리는
  것은 다음 일.
- **판정 재료.** K=3 는 두 행에서 토큰/스텝 +44%, tok/s +12~17% 로 K=1 을 이긴다. 4 행은 정적 m12 커널 격리 뒤. K=2 는 3 행에서
  9 토큰으로 같은 정적 경로를 밟으므로 그 문제의 폴백이 아니다(2 행이면 3·6 토큰 micro).
- **안 한 것.** C=2 짝, T=1, 긴 컨텍스트, K=2/4, 정확도 벤치.

### 범위 변경 — DSv4.1 이 엔진 범위로 돌아왔다 (2026-09-19, 운영자 "범위도 바꿔", 부팅 없음, PR #1205)
- **무엇.** ★43차(2026-09-11)가 첫 대상을 DSv4.1 → Qwen3.8-Flash-Next 로 옮기며 DSv4.1 을 범위 밖에 둔 결정이 뒤집혔다.
  CHARTER D5 의 제목과 마지막 문단, D6(“두 모델” → 모델마다 구현 하나)이 그에 맞춰 고쳐졌다. 현행 dsv4(물러난 오버레이의
  그것)는 그대로 범위 밖이다.
- **측정은 없다.** 이 항목은 결정 기록이지 판정이 아니다. DSv4.1 은 이 엔진에서 부팅된 적이 없고, 원장에 DSv4.1 서빙 수치는
  여전히 하나도 없다. ★42차의 조건도 그대로다 — 사전샤딩된 랭크 84.70 GiB 는 TP 를 해야 73.30 으로 GB10 에 들어간다.
- **이 결정이 정하지 않은 것.** 순서(프로덕션은 GLM-5.3, 지금 작업은 Qwen3.8 — 둘 다 그대로)와 MXFP4 전문가 양자화
  (b12x MXFP4 커널 판정 / 프리샤드 NVFP4 재양자화 / 자체 셀 — 품질 게이트 D4 가 고른다). D5 가 둘 다 열린 채로 적는다.
- **무엇부터인가.** 사람의 목록이 아니라 `cells.plan()` 이다: 시간 단위 다섯(dense·mHC 디코드·mHC 프리필 배선,
  one-shot·프리필 통신을 hidden 5120 에서 재측정), 일 단위 셋(CED 키 압축 레인, sink 를 받는 MLA softmax, MoE 양자화).
  그 전에 `net`·`lanes`·`adapter`·`decode_graphs` 가 없다는 것이 더 큰 항목이다. [상세](engine/DSV41_CARRY_20260919.md).

### Qwen3.8 디코드 — 라우터를 skinny GEMV 로(4~16 행 1.96~2.53 배), 믹서 사이트 5→3 발사(carry H2, 바이트 동일, 66.5~70.5 → 60.3~62.7 µs), 스텝 약 −1.3 ms (2026-09-19, srv4 단일 GPU 레인 9회, 플릿 창 없음, PR #1207)
- **skinny GEMV(`kernels/common/skinny_gemv`).** BF16 x@W.T 2..16 행, 한 발사: 행을 16 으로 패딩한 tensor-core dot, split-K 는 마지막 도착
  프로그램이 split 순서로 합산(도착 워드 장치당 한 버퍼, 캡처 전 `prepare`). 라우터 [513, 2560] cuBLAS 27.9/33.6/35.7 µs(4/8/16 행, 94→74
  GB/s) → 14.1~14.3(약 185 GB/s); 1 행은 cuBLAS gemv 와 비겨 torch.mm 에 남긴다; 믹서 up [10240, 320] 은 전 행 비김(`q38gemv-0919c`).
- **믹서 접기(`gated_residual.mix_rows`, carry H2).** 게이트를 down(+inject) 곱의 저장에, `mix_mean` 을 up 곱의 저장에 → 사이트 5→3 발사.
  같은 곱의 네 발사 산술과 바이트 동일, qualify 통과(max 0.0063 / rms 0.0027). 믹서 66.5~70.5 µs(cuBLAS, 프로브의 출력 복사 포함) →
  **60.3~62.7**(1·4·8·16 행, 16 사이트 한 그래프·가중치 회전, `q38site-0919a`). #1197·#1201 의 "Triton 접기는 어느 카드에서도 진다"는
  RTX 5050 의 것 — GB10 에서는 반대다.
- **스텝 A/B(`qwen38_step_ab`, K=3, 층 집합 × served/mm).** 경합 없는 빌드끼리 4 행 검증 타깃 −40~−94 µs(층 1~2 개 + 고정), 드래프트 그래프
  −17~−251 µs; 부품 합과 같은 **스텝당 약 −1.3 ms**(C=1, 단일 GPU 계산+발사). 네 미지수 풀이는 이 날 경합([7]·[1] 드래프트 두 배 흔들림)으로
  못 쓴다.
- **발견.** `static_pad`(#1192)가 전역 라우트 경로에만 있어 서빙 캡처(`route_local`)의 12 행 정적 MoE 가 죽었다 → **#1203**. 스텝 구성: 어휘 헤드
  deep_gemm FP8 약 780 µs/호출 × 4 ≈ 3.1 ms/스텝(드래프트 그래프 4.47 ms 중 2.33) — 다음 레버 후보; GLM 의 cuBLASLt 헤드 리더는 이 모양에서
  deep_gemm 보다 빠르지 않다(`q38head-0919a`, 경합 중 878 대 903~1023 µs).
- **안 잰 것.** 플릿 스텝·tok/s(창 금지), 품질 브래킷(라우터 점수·믹서 곱의 마지막 비트가 cuBLAS 와 다르다; 오라클 밴드만).
  [상세·원시 기록](measurements/qwen38_decode_gemv_20260919/README.md).

### Qwen3.8 어휘 헤드 — 디코드 행을 deep_gemm 대신 한 발사 FP8 GEMV 로: 호출당 890~908 → 693~716 µs(순수 읽기 +3~7%), 스텝 약 −0.8 ms (2026-09-19, srv4 단일 GPU 레인 4회, 플릿 창 없음, PR #1217)
- **무엇.** `engine/kernels/dense/fp8_rows` — deep_gemm 의 레시피(행의 block-128 e4m3 양자화, 가중치 128×128 블록, 2 의 거듭제곱 스케일)를
  행 16 패딩 FP8 tensor-core dot 한 발사로. `FP8Linear(decode_rows=True)` 로 옵트인하고 Qwen3.8 의 헤드만 켠다(GLM 의 FP8 레인은 그대로).
- **수치(`q38head-0919d`, 프로덕션 한가).** 1·2·4·8·16 행: deep_gemm 889.9~907.7 µs → **692.6~716.3**, 순수 읽기 667~672. deep_gemm 대비 최대 차
  0.0037, argmax 모든 행 일치; `fp8_rows.qualify` 0.0019~0.0021. 스텝(K=3, C=1): 검증 1 + 드래프트 3 호출 ≈ **−0.8 ms**(곱셈 추정).
- **기각.** GLM 의 cuBLASLt 헤드 리더(direct·split·block-128)는 이 모양에서 deep_gemm 보다 빠르지 않다(`q38head-0919a`); W8A16 은 같은 속도에
  오차가 크다(`q38head-0919b`).
- **안 잰 것.** 스텝 인구조사·플릿 스텝, 실가중치 로짓의 동률.
  [상세·원시 기록](measurements/qwen38_head_fp8_rows_20260919/README.md).

### 정의 변경 — 엔진의 종속은 하드웨어까지, 모델은 한정하지 않는다 (2026-09-19, 운영자 "하드웨어만 특정 하드웨어 종속성 가지고 모델엔 한정 범위 없게끔", 부팅 없음)
- **무엇.** CHARTER D5 가 모델 목록을 들고 있던 절에서 **목록 자체를 없앴다**. 같은 날 아침의 "범위도 바꿔"(DSv4.1 복귀)는
  세 모델짜리 목록이었고, 이제는 목록이 없다 — 종속은 GB10·4노드 RoCE·TP=4 까지이고, 새 체크포인트를 거절하는 것은 헌장이
  아니라 `cells.admission()` 과 `modules/` 가족의 축이다. D6 은 "모델마다 구현 하나" 에서 **"모델은 선언이다 — 가족에 축을
  더하고 경로에 분기를 두지 않는다"** 로 바뀌었다(모델 수에 상한이 없으면 "공통 수정을 모델 수만큼" 이 성립하지 않는다).
- **양자화 축은 열렸지만 일반성은 그대로 금지다.** 기본 형은 여전히 NVFP4 이고, 다른 포맷을 든 체크포인트는 이름 붙고
  판정된 셀로 들어오거나 프리샤드에서 변환된다(`cells.py` 의 세 길, 고르는 것은 품질 게이트 D4). 일반 `quant_config` 배관·
  dtype 폴백·"다른 GPU 도 되게"·Spark 의 메모리나 속도를 깎는 디스패치는 그대로 금지이며, 그 "다중 아키텍처" 는 **GPU**
  아키텍처를 뜻한다(모델 아키텍처가 아니다).
- **측정은 없다.** 결정 기록이다. 어떤 모델의 어떤 수치도 이 변경으로 바뀌지 않는다.
- **딸려 고친 살아 있는 참조.** `CHARTER.md` 0절·D5·D6, `engine/README.md` 의 profiles 줄, 최상위 `README.md`,
  `engine/DSV41_COMPOSITION.md` 머리말, `base/kernel_shape.py` 의 D5 인용, `kernels/cells.py` 의 moe 거절 문구
  ("nvfp4 only (D5)" → "compiled for nvfp4 only (D5's base form, not a model bound)").

### greedy argmax 의 두 발사는 GB10 에서 4 warps 가 맞다 — 1 warp 는 +7~+75%, 캐리 D5 기각 (2026-09-19, srv4 단일 GPU 레인, PR #1213)

`engine/QWEN38_CARRY.md` D5(GLM #1004 "한 warp" 처방을 `vocab_candidates.argmax_key` 의 `_argmax_partials`·`_argmax_finish` 로). 티켓
`vocab-argmax-warps-0919`, 트리 `59077c6a`, 프로브 `engine_kernel_check.py --lanes vocab_argmax`. 프로덕션 옆, 플릿 임대 없음.
**커널 컴포넌트 시간이다 — 엔진 속도 주장 없음.**
- **정합(타이밍 전, 전부 통과):** 4·2·1 warps 와 서빙 진입점의 패킷이 CPU 분기의 키와 바이트 동일 — 무작위, 부분 결과 안/사이의 동률,
  `±0`·`±NaN`·`±inf`, strided 샤드, 디코드 컷. 키가 정수라 warps 는 패킷을 못 바꾼다.
- **시간(그래프 안 호출당 µs, 중앙값):** Qwen3.8 샤드 62,080 열 — 2 행 3.34 / 3.69 / 5.51(4 / 2 / 1 warps), 4 행 3.84 / 3.93 / 5.85,
  16 행 7.15 / 6.91 / 7.68, 32 행 10.83 / 10.37 / 12.59. GLM-5.3 샤드 38,720 열 — 1 행 3.07 / 3.46 / 5.38, 7 행 3.98 / 3.97 / 5.88,
  14 행 5.40 / 5.25 / 6.49, 28 행 7.42 / 7.17 / 8.02. (62,080 × 8 행의 중앙값은 옆 프로덕션 버스트에 걸렸다; 최솟값 5.13 / 5.41 / 6.96.)
- **판정:** 1 warp 는 어디서도 이기지 못한다. 2 warps 는 16 행 이상 −3~−4%, 서빙이 도는 C=1 행 수에서 +11~+13%. 기본값 유지,
  `ARGMAX_WARPS = 4` 로 선언만. #1004 와 반대인 이유: 후보 선택은 MAX 축약을 16 번 연달아 돌려 매 단계 warp 간 교환을 냈고, greedy 는
  1,024 열의 MAX 한 번이다.
- 기록: `measurements/vocab_argmax_warps_20260919/`.

### Qwen3.8 QSA 발사 기하 — GB10 의 split 프로필: 프리필 어텐션 −26%, covered −52%, 디코드는 C=1 그대로; 레코드 배치 기각 (2026-09-19, srv4 단일 GPU 레인 4회, PR #1221)

`engine/QWEN38_CARRY.md` Q9 · Q13. 프로브 `engine_kernel_check.py --lanes qwen38_qsa_geometry`(#1208), 티켓 `qwen38-qsa-geometry-0919`/`b`/`c`/`d`
(트리 `066da5e9` → `fc00accd`), rank 당 셀(6/1 × 256, 2,051 열)·합성 K/V·서빙 stride, 프로덕션 옆·플릿 임대 없음.
**발사 하나의 시간이다 — 엔진 tok/s 주장 없음.**
- **split 프로필(`qsa._split_profile`)을 GB10 의 표로:** 16 폭 타일·4 warps, split 은 프로그램 수로 ≤2 → 64, ≤8 → 16, ≤256 → 4, 그 위 → 1(merge 없음).
  상류(GB300 튜닝) 대비 eager sparse 4,096 행 11.9 → 8.9 ms(−26%), 256·512·1,024 행 −29·−27·−21%; covered 2,048 행 3.53 → 1.71 ms(−52%);
  캡처 디코드 N=4·8·16 은 cold −9·−3·−3% / warm +3·−23·−10%, N=1·2(C=1)는 64 split 을 이기는 기하가 없어 그대로.
- **cold / warm:** warm 은 같은 K/V 재생이라 캐시 값(N=8 에서 340 GB/s > LPDDR5X 273), 서빙은 cold 에 가깝다. cold 차이가 3~9% 로 작아서 두 지표
  어느 쪽에서도 상류보다 나빠지지 않는 기하만 골랐다.
- **게이트:** 1,926 팔 전부 fp32 오라클 밴드 안(최대 drift 0.0076 / 0.0156), covered 발사는 모든 강제 기하에서 sparse 발사와 바이트 동일 → 두 발사가
  프로필을 계속 공유한다. 128 폭 타일은 GB10 에서도 컴파일 거부(`tl.dot` 의 shared memory 137,216 > 101,376).
- **프리필 점수(`_score_profile`):** 128 폭 × 프로그램당 32 타일 × 4 warps 로 4K·32K·256K 버킷 −6·−12·−16%, 점수 바이트는 513 팔 전부 동일. 디코드 점수는 그대로.
- **그대로 둔 것:** `qsa_select.WIDEST`(64K 버킷까지 한 발사 −26~−68%, 131K 부터 torch 형태가 5~9 배 빠름)와 warps 규칙, 입력 두 발사의 4 warps.
- **Q13 기각:** K/V 를 블록 배치 vs 레코드 배치 — 같은 바이트, 디코드 ±1%, 프리필 +1%.
- **프로브가 틀렸던 것:** gated store 의 바이트 게이트가 청크(6.3M 원소)에서 오늘 규칙을 떨어뜨림 — Triton exp 와 torch exp 의 마지막 비트, 1~5 원소,
  1 BF16 스텝 → "인접 값 이내 + 개수 보고"로.
- 기록: `measurements/qwen38_qsa_geometry_20260919/`.

### Qwen3.8 MTP 드래프터 — dense BF16 기본(드래프트 그래프 +0.42 ms, FP8 +0.13), IVF 근사 헤드·FP8 전문가(기본 끔), 어텐션 뒤 행 선택(기본 켬), 수용률 미측정 (2026-09-19, srv4 단일 GPU 레인, PR #1226 · 기록 PR #1232)
- **dense 정밀도(`q38mtp-0919a`).** K=3 드래프트 그래프, 1 행: W4A8 3.97 → FP8 4.10 → **BF16 4.39 ms**(운영자 선택, 기본). #1226 의 제목 "FP8 by
  default" 는 첫 커밋 제목이고 머지된 기본은 BF16 이다.
- **#1226 의 나머지.** IVF 근사 드래프트 헤드(`--draft-index`), MTP 전문가를 export 의 FP8 로(`--mtp-experts-dir`, 사이드 파일 srv4 에만), 둘 다 기본 끔.
  `mtp_forward(rows=)` 로 어텐션 뒤는 읽는 행만(기본 켬) — GPU 동등성 판정이 머지보다 늦다(`q38mtp-0919c` 대기).
- **이 PR.** 드래프트 쿼리 탭(`kernels/common/row_tap`, `--tap-draft-queries`): 랭크 0 이 캡처 그래프 안에서 드래프트 argmax 의 입력 행과 pick 을 디바이스
  링에 쓰고 30 초마다 파일로 — IVF 헤드의 실제 재현율을 오프라인에서 재기 위한 것.
- **안 잰 것.** 수용률 전부(창 필요). [상세·원시 기록](measurements/qwen38_mtp_precision_20260919/README.md).

### 엔진 구현 목표 — 기본은 NVFP4, 비용이 작고 수용률 영향이 큰 곳에는 정밀도를 투자한다; 그 첫 적용으로 Qwen3.8 MTP 헤드의 전문가를 원본 BF16 으로 고정 (2026-09-19, 운영자 결정, PR #1235)
- **원칙(운영자).** 엔진의 기본 수치형은 NVFP4 다. 다만 **비용은 적고 수용률(스텝당 토큰)에 주는 영향이 큰 부분**은 정밀도에 투자한다. 드래프터가 그 자리다:
  오차가 K=3 스텝마다 세 번 수용률로 새고, 바이트는 스텝의 한 조각이다(드래프트 행은 랭크의 전문가 두세 개만 읽는다). 새 레버를 고를 때 이 기준으로 판정한다.
- **적용 1 — MTP dense 투영 BF16**(#1226, 운영자 "그정도면 bf16으로 해"): K=3 드래프트 그래프 1 행 W4A8 3.97 → BF16 4.39 ms(`q38mtp-0919a`, +0.42).
- **적용 2 — MTP 전문가 원본 BF16 으로 고정(이 PR, `--mtp-experts bf16` 기본).** NVIDIA export 는 타깃 층 전문가만 NVFP4 로 두고 MTP 층은 FP8 block-128 로 남겼는데,
  preshard 가 그것을 BF16 으로 풀었다가 교정 없이 NVFP4 로 다시 양자화해 서빙했다(가중치 이중 양자화, 활성 FP4). 이제 예전 사본(srv2 `qwen38-flash-next-nvfp4`)의
  fused BF16 전문가를 랭크 몫대로 잘라 사이드 파일로(`mtp_side.py --precision bf16`, `~/models/st-qwen38-mtp-bf16/`, 랭크당 1,258,291,480 B, 네 노드 배포·해시 확인),
  `kernels/moe_rows`(BF16·FP8 가중치 × BF16 활성, 두 발사)가 서빙한다. BF16 대 FP8 사이드 파일: 전 전문가 상대 오차 0.0266(기록된 FP8 반올림과 같음) — 같은 가중치·같은
  행 순서·같은 랭크 분할. 메모리 랭크당 NVFP4 대비 약 +0.93 GB. FP8(export 원본)·NVFP4 는 `--mtp-experts fp8|nvfp4` 로 남는다.
- **같은 날 판정.** `mtp_forward(rows=)`(#1226, 어텐션 뒤는 읽는 행만): 네 팔 모두 전 행 계산 뒤 선택과 **바이트 동일**(`q38mtp-0919c`). FP8 전문가 커널의 드래프트 비용은
  NVFP4 micro 와 비슷(경합 보정; 693 대 547 µs 표시, 그 팔만 경합 1.44 배).
- **실가중치 대조(`q38mtpx-0919b`, 랭크 3).** 원본 BF16 기준으로 BF16 커널 rms 0.00015, FP8(export) 4.5%, **지금까지 서빙한 NVFP4 재인코딩 23%**(max 21~26%) —
  드래프터의 MoE 가 원본에서 그만큼 벗어나 있었다. qualify BF16·FP8 GPU 오차 0. [상세·원시](measurements/qwen38_mtp_bf16_experts_20260919/README.md).
- **안 잰 것.** BF16 전문가의 드래프트 그래프 비용(`q38mtp-0919d` 대기)과 수용률 — 창이 필요하다.
### Qwen3.8 M5·H3 플릿 창 — shared expert 포크는 C=1 −5%·C=4 +6%, mixer mean 은 32 행까지 256 폭 타일이 −30~−51% (2026-09-19 13:45~13:54, 네 Spark + srv4 단일 레인, PR #1233)

운영자 "플릿 잡고 해". main `048270ef`, 런처 기본(K=1, one-shot, 4 행). 창 전에 레인 큐를 비움(두 세션이 다섯 장을 거둬 줌), `prebuild` 19 적중, yield 2 초,
두 부팅(`off` ready 95.3 s 콜드 / `all` 63.4 s 웜), 같은 고정 요청(C=1 여섯·C=4 두 바퀴), 그 뒤 같은 창에서 H3 레인 프로브, release. **프로덕션 다운 572 s.**
**속도 주장이 아니라 기록이다(D17)** — 한 창·한 쌍.
- **M5(`--shared-overlap all`):** C=1 디코드 29.01 → 27.55 ms/스텝(−5.0%; 요청별 0.930–0.975; 30–40 ms 스텝 비율 17–41% → 2–10%), C=4(8 행) 43.35 → 45.93
  ms/스텝(+6.0%). 순서 off→all 의 한 쌍이라 순서 효과가 섞임 — 반복 요청 둘만 보면 −3.5%·−2.5%. 수용(토큰/스텝)은 같은 분포. 한 부팅 안에서 같은
  프롬프트의 반복도 다른 텍스트를 내므로(엔진 비결정성) 바이트는 플릿에서 판정 못 함(#1227 의 RTX 5050 판정 그대로). 결론: 한 요청의 행(`one`)만 포크 — 운영자 결정("켜")으로 기본값, PR #1234.
- **H3(`_mix_mean` 타일):** 캡처 4·17·32 행에서 256 폭 × 4 warps 가 한 블록 대비 −51·−41·−30%, 64 행부터 ±4%(대역폭). 140 팔 전부 바이트 동일.
  규칙: 32 행까지 256×4.
- **곁가지:** #1230 의 `warm eager moe` 첫 GPU 실행 통과(콜드 19.1 s, 웜 0.005 s/발사). `off` 부팅의 첫 요청들 중 `micro_m3/m1_…_t10_r80` 컴파일(각 ~1 s)
  — 주인 세션에 전달.
- 기록: `measurements/qwen38_shared_overlap_20260919/`, `measurements/qwen38_mix_tiles_20260919/`.

### Qwen3.8 — 프리필 청크 하나의 내역: 4,096 토큰이 랭크 하나에 디바이스 1005 ms, 43% 가 믹서 사이트 (2026-09-19, srv4 단일 GPU 레인)
- `--lanes qwen38_prefill`(main `048270ef`): 서빙 `prefill` 을 실가중치 작은 net 넷에서 재고 48 층으로 풀었다. 디바이스 컨텍스트 0 1004.7 ms · 4,096 1025.9 ms. 게이트 잔차 Triton 226.3 + 믹서 GEMM 중심 cuBLAS 214.4 ms(약 43%), torch elementwise 174.2 ms(PLE 한 곳 49 ms), MoE 148.4 ms, QSA 119.3 ms.
- 벽시계 · 호스트 몫은 쓸 수 없다: 두 칸에 2.3 – 2.7 s 가 한 번씩 끼었다(프로덕션 옆 레인, 한 번 잰 벽시계). 판정은 디바이스 표뿐. 속도 주장 없음. [표 · 원시](measurements/qwen38_prefill_census_20260919/README.md).
### Qwen3.8 — 디코드 크기 워밍업(헤드 패스 · 폭 1 · 8) 뒤 문 뒤에서 처음 쓰는 커널 12 → 9, 첫 디코드 스텝의 1.1 s 컴파일이 사라짐 (2026-09-19, srv4 단일 GPU 레인)
- `--lanes qwen38_serve_compiles` 를 브랜치 `decode-sized-warm`(main `048270ef` + 이 변경)에서: 문 뒤 **9** 개(main 12). 덮인 QSA 2 (짧은 프리필과 첫 디코드 스텝 1.10 s)와 GDN `_gates` 1 이 사라졌고, 모든 디코드 스텝이 16 ms 이하. 남은 9 는 conv 5(#1238), dynamic MoE 밴드 3, 점수 커널 G 1 변형 1.
- 부팅의 값: MTP 헤드만 도는 패스 20 개 8.35 s, 폭 1 · 8 0.431 · 1.47 s — 대부분 첫 컴파일, 그 뒤 부팅은 디스크 읽기. 속도 주장 없음. [기록](measurements/qwen38_serve_compiles_20260919b/README.md).
### Qwen3.8 드래프트 헤드 W8A16 — 드래프트의 argmax 는 FP8 헤드를 BF16 행으로 읽는다(같은 바이트, 시간 ±2%, 오차 −27~−35%); BF16 헤드·IVF 근사 헤드 기각 (2026-09-19, srv4 단일 GPU 레인 3회, PR #1246)
운영자 원칙(PR #1235)의 둘째 적용: 드래프터가 쓰는 헤드 판독은 활성 FP8 양자화를 빼고(`fp8_rows.project_bf16`, `Qwen38Net.draft_logits`), 검증 스텝 헤드는 그대로.
**발사 시간과 argmax 일치율이다 — 수용률 주장 없음.**
- **비용(`q38head-0919e`, 같은 티켓 교차):** 1·2·4·8·16 행 served FP8 720·719·725·719·730 µs 대 W8A16 711·722·720·732·726 µs(×0.99~1.02), 바닥(읽기만)
  697~712. BF16 기준 상대 오차 0.036~0.041 → 0.025~0.028.
- **일치율(`q38ivf-0919d`, 쿼리 4,096, BF16 헤드 argmax 기준):** 한 토큰이 뚜렷한 쿼리 99.98 = 99.98%, 섞임 99.37 / 99.10%(11 개 차), 무작위(1·2 등 근접)
  91.77 → **93.46%**.
- **기각:** BF16 가중치 헤드 — 드래프트 셋 × ~+0.7 ms = 스텝당 +2.1 ms, 남은 틈 ~1%(운영자 "알았어 그렇게 해"). IVF 근사 헤드(#1226, 기본 끔 유지) — 발사 71~86 대
  689~696 µs 지만 재현율 3~29%(token)·53~60%(섞임).
- **곁 기록(`q38mtp-0919e`, #1237 트리):** BF16 전문가 기본의 K=3 드래프트 그래프 1 행 4,288 µs(NVFP4 4,256·FP8 4,272), 4 행 4,930(4,301·4,557); `mtp_forward(rows=)`
  다섯 팔 바이트 동일.
- **안 잰 것:** 수용률(dense BF16 + 전문가 BF16 + W8A16 을 함께) — 창 필요. [상세·원시](measurements/qwen38_draft_head_precision_20260919/README.md).

### Qwen3.8 기본 드래프트 수 K=1 → K=3 — 이미 이긴 레버를 기본 실행 경로로 (2026-09-19, 운영자 "qwen 개선작업 중 기본값 아닌 것들 다 켜", 부팅 없음)
- **무엇.** `engine/profiles/qwen38/facts.SPEC_K` 1 → **3**. 서빙 기본이 스텝당 드래프트 셋이 되고(`decode_graphs.draft_chain`
  이 한 재생 안에서 헤드를 K−1 번 잇는다), `--spec-k 1` / `ST_SPEC_K=1` 이 롤백으로 남는다. 마법사의 유도
  (`shapes.kernel_shape`)도 같은 K 를 쓰도록 한 자리에서 읽는다 — 부팅이 바인딩하는 형상과 문이 판정하는 형상이 갈라지지 않게.
- **이 커밋에 새 수치는 없다.** 근거는 전부 이미 이 원장에 있다: ① 09-18 플릿 짝(PR #1182, 두 행 · C=1 · greedy):
  K=3 이 토큰/스텝 **+44/+36%**, 디코드 tok/s **+12.5/+9.6%**, 프리필 포함 +17/+13% 로 K=1 을 이긴다(운영자 "k=3 정도 하지").
  ② 그때 네 행을 죽인 정적 m10·m12 커널은 #1192 의 `lanes.static_pad` 로 닫혔다. ③ 09-19 11:19 창: **K=3 · 4 행이
  플릿에서 뜨고 답했다**(ready 162.0 s, STALL 0, 수용 51.6%, 2.33–2.91 토큰/스텝). 그 창이 단 조건 — *"요청 중 컴파일을
  부팅으로 옮기기 전에는 K=3 평균을 K=1 과 나란히 읽지 말 것"*(6 스텝 > 2 s) — 은 #1230(캡처 크기 eager MoE 워밍업)과
  #1242(드래프터 eager 헤드 워밍업)가 처리했다.
- **안 잰 것(그래서 속도 주장 없음, D17).** **워밍업 뒤 K=3 대 K=1 의 짝**을 잰 창이 아직 없다. 품질 브래킷 · C≥2 ·
  긴 컨텍스트 · T>0 도 그대로 미측정. 기본 적용과 실측 검증 상태를 따로 적으라는 D11(2026-09-14)의 그 형태다.
- **딸려 온 것.** K 는 캡처 사다리(행 × 토큰)와 GDN 링을 넓힌다: `probes/engine_qwen38_kda.py` 가 K 를 프로필에서
  읽도록 고쳤고(전엔 `SPEC_K = 1` 을 스스로 들고 있었다 — 그 파일의 테스트가 이미 프로필과 같기를 요구한다),
  dense 프로브의 캡처 행 사다리는 8 → 16 으로 늘었다. `caches.check_rings` 는 K ≤ 4 를 확인한다(K=3 통과).
- **같이 본 것 — 기본 밖에 남은 레버와 그 이유.** 전수 표(열여섯)는 새 문서
  [engine/SERVING_DEFAULTS.md](engine/SERVING_DEFAULTS.md)에 있다. 이 원장의 머리말이 "어느 노브가 켜져 있는지" 로
  가리키던 `profiles/README.md` 는 #1152 가 오버레이와 함께 지운 파일이라, 그 자리를 그 문서가 대신한다.
  켜지 못한 이유는 넷이 서로 다르다: `--hc-fp8`(이 표에서 **유일하게 출력을 바꾼다** — 품질 브래킷 없음, **D4**),
  `--mtp-window`(원장 항목 없음 = 수용률 미측정), `--draft-threshold`(P 를 잰 창이 없다),
  `--mtp-tuned`(#1248 — 학습기는 들어왔지만 **수출된 결과가 아직 없다**). **잰 뒤 진 팔**은 켜는 대상이 아니라
  롤백이다: `--draft-index`(재현율 3~29%, #1246), `--shared-overlap all`(C=4 +6%, #1233),
  `--mtp-experts nvfp4`(원본 대비 23%, #1235).
- **곁가지 하나.** 09-19 의 W8A16 항목(#1246)이 제목 줄을 두 번 들고 있었다(`PR #TBD` 와 `PR #1246`, 본문은 하나).
  `#TBD` 줄을 지웠다 — 본문은 손대지 않았다.

### Qwen3.8 MTP BF16 투영 — skinny GEMV 표에 다섯 모양, 넷은 1 행도: 1 행 ×1.31~1.49, 4~16 행 ×1.03~1.31 (2026-09-19, srv4 단일 GPU 레인, PR #1258)
MTP dense BF16(#1226)이 `lanes.rows_linear` 를 타는데 `skinny_gemv.CONFIGS` 에 그 모양이 없어 전부 `torch.mm` 이었다. 목표 층의 같은 투영은 W4A8 레인 — 새 모양은 드래프터만 탄다.
**발사 시간이다 — 드래프트 그래프는 아직 판정 없음.** `q38mtpgemv-0919b`(세 라운드 교대)는 프로덕션 서빙 중이라 그래프가 7.2~12.2 ms(조용할 때 4.3~4.6), 라운드 흔들림 ±30% 로 판정 불가 — 같은 트레이스 안의 GEMM 몫만 세 그래프 모두 served 가 낮다(−0.027~−0.043, 기울기). 첫 A/B `q38mtpgemv-0919a` 는 무효: 두 빌드 사이에 프로덕션이 올라와 여유 메모리 80.7 대 39.7 GiB, 드래프트 그래프 4.6 대 10.2 ms 는 경합이다 → 레인을 세 라운드 교대로 고쳐 다시 넣음.
- **스윕(`q38gemv-0919e`, µs, cuBLAS → 고른 타일):** 1 행 in_proj 132.0 → 97.1, fc 81.5 → 58.2, o_proj 48.1 → 36.8, 공유 down 4.8 → 3.2(cuBLAS gemv 161~172 GB/s 대
  213~257); 4~16 행은 ×1.03~1.07(공유 down ×1.26~1.31, 공유 gate_up ×1.16~1.19). 공유 gate_up 의 1 행은 cuBLAS 가 이김(8.7 대 9.6) → `ONE_ROW` 에서 뺌.
  상대 오차 ≤ 0.0036, 반복 바이트 동일. 라우터의 `MIN_ROWS = 2`(1 행 비김, `q38gemv-0919c`)는 그대로.
- **추정:** C=1 K=3 드래프트 그래프 체인 스텝마다 −74 µs, 관측 −13 µs → 스텝당 −0.15~−0.17 ms(창 K=3 스텝 35.5 ms 의 ~0.5%).
- 프로브: `qwen38_step_mtp_gemv` 레인(`mtp-mm` 팔 = MTP 모양만 표에서 빼 `torch.mm`). [상세·원시](measurements/qwen38_mtp_gemv_20260919/README.md).
### Qwen3.8 — dynamic MoE 타일 밴드를 eager 워밍업에 넣은 뒤 문 뒤에서 처음 쓰는 커널: K=1 8 · K=3 7, dynamic MoE 0 (2026-09-19, srv4 단일 GPU 레인 2회)
- `--lanes qwen38_serve_compiles:K`(브랜치 `eager-moe-dynamic-bands` = main `b5613857` + 밴드 넷): b12x dynamic 커널이 요청 중 0(main 에서 3). 남은 것은 conv 5(#1238), `_mix_mean` 타일 1, QSA split-K merge 1(K=1), 점수 커널 G 1 변형 1 — 넷 모두 한 번 컴파일되면 디스크에 남는 유한 집합. 다른 세션 창의 `micro_m1/m3_…_t10_r80` 은 C=1 K=1 에서 재현되지 않음. 속도 주장 없음. [기록](measurements/qwen38_serve_compiles_20260919c/README.md).
### Qwen3.8 두 번째 운영자 창 — S2 입력 재사용 기각(o_proj +4.5~+13.6%, in_proj ±2%), H6 `--hc-fp8` 디코드 C=1 +9.6% 기각, M5 확인 쌍 무효 (2026-09-19 14:29~14:43, 네 Spark + srv4 단일 레인)

운영자 "ㅇㅇ"(짧은 창 제안). main `4148c35f`(#1241·#1234·#1233), 런처 기본(K=1, one-shot, 4 행, `one`). prebuild 0 빌드, yield 14:29:02, 세 부팅(`off`·`one`·`fp8`)에
같은 고정 요청, 그 뒤 같은 창에서 S2 레인 프로브, release 14:39:17, 프로덕션 문 14:42:47. **프로덕션 다운 약 825 s.** **속도 주장이 아니라 기록이다(D17).**
- **S2(#1241, GB10 레인 `qwen38-input-reuse-0919b`, 옆에 프로덕션 없음):** `InputReuseTests` 3 통과, 12 셀 전부 ordinary 와 바이트 동일. 콜드 중앙값
  GDN in_proj 4120×2560 −0.2~−1.8%, QSA in_proj 4224×2560 +0.8~+1.9%, o_proj 2560×1536 +4.5~+13.6%(14.8 → 16.8 µs) — 따로 도는 pack 발사를 짧은 K 가 못 번다.
  기각, admit 을 revert(`kernels.cu` 는 #1241 앞과 같다). K5 는 o_proj admit 을 스스로 다시 넣고 판정해야 한다.
- **H6(`ST_HC_FP8=1`, 깨끗한 `one` 부팅 뒤):** C=1 28.35 → 31.08 ms/스텝(+9.6%, 다섯 요청 모두 +5.8~+15.3%), C=4 45.25 → 47.29(+4.5%, 두 바퀴 −1.5·+10.1% — 잡음 안).
  ready 61.2 → 98.0 s, prepare dense +0.75 GiB. 디코드 기각(발사 증가 + H2 접기의 이탈이 믹서 바이트 절반을 넘음). 프리필은 미실측.
- **M5 확인 쌍 무효:** `one` 첫 부팅이 이미 GO 해 있던 레인 프로브(`q38head-0919e`) 옆이라 런처가 거부 → 순서 off→one. `off` 요청 내내 다른 세션의 레인 티켓
  `q38win-0919a` 가 srv4 GPU 에서 돌았다(14:31:20–14:33:32; Qwen3.8 부팅 옆에서도 레인 방이 남는다). off 의 C=1 +12.6%·C=4 +23% 는 그 값이다. 깨끗한 `one`: C=1 28.35, C=4 45.25.
- 기록: `measurements/qwen38_s2h6_window_20260919/`.
### Qwen3.8 — 서빙 요청이 문 뒤에서 처음 쓰는 커널: main 에서 12 개, 그중 5 개가 프롬프트 길이마다 새로 컴파일되는 conv (2026-09-19, srv4 단일 GPU 레인 2회)
- **무엇.** `--lanes qwen38_serve_compiles`(트리 main `048270ef`): 서빙 모델을 랭크 하나(실가중치 층 1 · 7, MTP, K=3, 4 행)로 플릿 부팅 순서대로 세우고 러너로 서빙 창의 요청 일곱 개를 흘리며 스텝마다 Triton · b12x 커널 캐시를 셌다. 문 앞 273 개, **문 뒤 12 개.**
- **12 개의 정체.** `_single_conv` 5(토큰 수 T 가 constexpr — 처음 보는 프롬프트 길이마다 컴파일: 실제 트래픽은 거의 모든 요청), b12x dynamic MoE 3(타일 밴드별; 워밍업 입력이 토큰 0 뿐이라 라우트가 한 곳에 몰림), 덮인 QSA 2(짧은 프리필과 첫 디코드 스텝 1.10 s), 점수 커널 G 1 변형 1(3,223 토큰), GDN `_gates` 1. 첫 디코드 스텝 말고 모든 디코드 스텝은 16 ms 이하 — 요청 중 느린 스텝은 전부 이 목록에서 나왔다.
- **eager MoE 워밍업(#1230) 확인** — `--lanes qwen38_eager_moe`: 워밍업 뒤 요청이 더한 micro 커널 0, 용량 8 하나. 용량 대 바이트는 판정 못 함: 같은 용량(m 8)에서도 호출마다 마지막 자리가 달랐다(≤ 1.2e-4, 원자 누적 순서로 보인다).
- **고침 하나(이 PR):** conv 의 T 를 인자로 — RTX 5050 에서 상수 형태와 바이트 동일(프리필 18 길이 × 초기 상태 유무, 링 16 모양), 컴파일 36 → 6. 나머지 넷(dynamic 밴드, 덮인 QSA, G 변형, `_gates`)은 열린 일. 속도 주장 없음. [표 · 원시](measurements/qwen38_serve_compiles_20260919/README.md).

### GLM-5.3 어휘 헤드 — 디코드 행을 BF16 행 × FP8 가중치 한 발사로(W8A16): 호출당 850~929 → 731~743 µs, 오차 3분의 1 감소 (2026-09-19, srv4 단일 GPU 레인, PR #1264)

- **무엇.** Qwen3.8 캠페인의 행 커널(`dense/fp8_rows`, #1217·#1246)이 GLM 헤드(랭크당 38,720 × 4,096 e4m3, 159 MB)에서도 이기는지
  GLM 모양·행으로 잰 레인 티켓 둘(`glm53-head-0919a`, `glm53-gemv-0919a`, 프로브 #1251). 합성 가중치, 팔은 CUDA 그래프 안에서 번갈아.
- **헤드(호출당 µs, 7·8·14·16 행).** 서빙 cuBLASLt 검증 891·906·929·911, 드래프트(MX 행) 853·859·850·908 → **W8A16 733·731·734·743**
  (fp8_rows 736~743, deep_gemm 926~937, 순수 읽기 694~706). BF16 곱 대비 최대 오차 W8A16 0.026~0.029, 행을 양자화하는 팔 전부 0.036~0.043.
  GLM 은 MAX_SEQS 2·SPEC_K 7 이라 헤드 호출이 전부 16 행 이하: C=1·2 스텝당 약 −0.3 ms(부품 수치, 플릿 미실측 — D17).
- **skinny GEMV.** 인덱서 wk+gate [256, 4096] 1.13~1.15배(스텝당 약 19 µs), 드래프터 context K/V 1.04배 — 붙이지 않음(분할 설정이 캡처 전
  `prepare` 를 부팅에 요구, 0.04% 를 위해).
- **적용.** 공용 `FP8Linear(decode_rows="w8a16")` 레인으로, 검증·드래프트 헤드 둘 다(운영자 결정 2026-09-19: 검증 logits 가 BF16 곱 쪽으로
  움직이므로 동률에 가까운 토큰은 다른 것을 고를 수 있다). cuBLASLt 리더는 16 행 초과와 부팅 교차검사(`qualify_decode_rows`)를 맡는다.
  **GLM 부팅으로는 아직 미검증.** [원시 기록](measurements/glm53_decode_rows_20260919/README.md).

### Qwen3.8 디코드 스텝 커널 프로파일 — C=1 K=3: 스텝 36.9 ms 중 커널 87%, TP 통신 21% · 믹서 22% · MoE 19% (2026-09-19 17:12~17:16, 다른 세션 창의 슬롯)
- **무엇.** GLM-5.3 09-14 기록(`st_decode_profile_20260914`)과 같은 도구(`/v1/engine/profile` 32 스텝 + `/metrics`)로 Qwen3.8 C=1 · C=4 디코드 스텝을 잡았다. 부팅은 MTP 튜닝 창의 것: `1aec0aac` + mtp_tune, `--spec-k 3 --draft-threshold off`, 3차 튜닝 헤드 — 최신 main 아님(#1258 등 없음). 프로덕션을 따로 내리지 않았다.
- **C=1(창 #1):** 스텝 36.9 ms(프로파일러 하 39.0), 커널 합 32.0 ms · 발사 1,566 — **커널 밖은 13%**. 믹서 사이트 7.1 ms(22%), **TP 통신 6.9 ms(21%**, GLM 8%), MoE 전문가 5.9 ms(19%), dense W4 3.6 ms, 어휘 헤드 2.8 ms, MTP BF16 투영 1.4 ms. 수용 0.656 → 스텝당 약 3.0 토큰(카운터). 첫 프로파일 창은 consumer 대기가 86.7 ms 로 부풀어 무효.
- **읽기.** 통신이 GLM 보다 두 배 넘게 무겁다(작은 합의 지연) — 캐리 H4 · H5 · X1 · X2 의 자리. 믹서는 가중치 읽기 약 200 GB/s(대역폭의 70%). C=4: 스텝 61.8 ms, 커널 92%, MoE 가 가장 커진다. 속도 주장 없음. [표 · 원시](measurements/qwen38_decode_profile_20260919/README.md).
### Qwen3.8 비전 타워 1단계 — 전처리기 pixel_values 비트 일치(9/9), mRoPE 위치 vLLM 과 일치, 실가중치 타워는 transformers bf16 과 같은 오차 (2026-09-19, srv2 CPU, PR #1267)
체크포인트(`Qwen4ExpForConditionalGeneration`)의 Qwen3-VL 타워(27 블록, 1152 폭, 16 px 패치, 2×2 병합 → 2560)와 전처리기를 vLLM·transformers 없이
`engine/profiles/qwen38/vision.py` 로. **서빙 전** — mRoPE 적용·프리필 행 교체·부팅 연결은 다음 단계.
- **참조**: `probes/qwen38_vision_reference.py` 를 서빙 이미지 안에서 → `tests/fixtures/qwen38_vision_reference.json`(생성기 저장소에 둠). 합성 그림 아홉 가지의
  pixel_values sha256 이 ST 이미지에서 전부 일치, vLLM 의 mRoPE 위치·delta 세 프롬프트 일치, 장난감 타워 vs transformers fp32 상대 0.0051.
- **실가중치**: 우리 bf16 vs transformers fp32 5.6~5.8%(행 cos 최소 0.983) — transformers 를 bf16 으로 돌려도 5.4~5.9% 라 구현 차이가 아니라 bf16 몫.
- **곁**: 그림 디코드(`engine/modules/pictures`)를 GLM 과 공유, 디코드 중 깨지는 그림이 GLM 에서도 500 대신 400. `preshard --vision` 이 `vision.safetensors`.
  [상세·원시](measurements/qwen38_vision_tower_20260919/README.md).

### Qwen3.8 MTP 헤드 튜닝 창 — 세 번 학습(held-out 2.608 → 3.160), 라이브 greedy C=1 토큰/스텝 +6.6~6.9%·tok/s +6.5~8.5%, 임계 0.1 은 C=1 중립 (2026-09-19 15:33~17:22, 네 Spark)

운영자 "1+2+windowed"의 2번(FastMTP 식 KL 튜닝, #1248)을 처음 GPU 에서 돌린 창. 트리 `1aec0aac` + `mtp_tune.py`(#1263·#1271 분). 두 번 연장했다(운영자
"좀 더 학습해도 좋을것 같은데", "데이터 추가로 모아서 3번째 학습까지도 할만한데"), 끝에 다른 세션의 C=1 프로파일에 문을 비워 줬다. **프로덕션 다운 6,512 s.**
**속도 주장이 아니라 기록이다(D17).**
- **데이터:** OpenRouter 동일 모델 텍스트 1,441 대화(템플릿 프롬프트 490 × 세 샘플, 대부분 T 0.7~0.9)를 프리필 → 580,979 위치; 자기 디코드 107,348 위치 추가.
  생각만 있고 본문이 빈 29 샘플(글자 6.9%, 긴 생각)은 프리필 필터가 버렸다(리뷰 지적 — 복사본은 고쳤다).
- **학습(held-out 64 창, greedy teacher-forced):** 1차 91 스텝 2.608 → 3.093, 2차(`--init`) 182 스텝 → 3.136, 3차(자기 데이터 16%) 130 스텝 → 3.160. 1단 일치 0.713 → 0.857.
- **라이브(C=1, 평가 40 개 전부 greedy, K=3, 짝 평균 ± 표준오차):** 1차 +4.9% ± 0.8%, 2차 **+6.6% ± 1.1%(tok/s +8.5%)**, 3차 **+6.9% ± 0.8%(tok/s +6.5%)**,
  3차 − 2차 +0.4% ± 1.0%(토큰/스텝은 프리필 스텝이 뽑은 첫 토큰을 뺀 값 — 창의 스크립트는 그 토큰까지 디코드 스텝으로 나눠 표에서 약 +0.014 가
  부풀었고, 다시 센 짝의 상대 차이는 ±0.04%p 안에서 같다; 리뷰 지적). held-out 과 라이브의 간극은 **분포**다(원본은 타깃 자신의 greedy 텍스트에서 이미 강했다) — 처음의 "T=1 천장" 설명은 틀렸다.
  **팔마다 디코드한 텍스트가 다르다**(29~30/40, 같은 헤드끼리도 27/40): 짝은 같은 프롬프트 위의 비슷한 텍스트 비교다. held-out 은 형제 샘플이
  학습에 들어가 낙관적이다(리뷰 지적).
- **원장(임계를 고르는 경험 곡선 — 보정 측정은 아니다, 리뷰 지적):** 헤드가 0.9~0.97 을 준 드래프트의 greedy 수용 82% → 98%, 행당 수용 1.739 → 1.879.
- **임계 0.1:** 같은 튜닝 헤드의 켬/끔 짝(간섭 없는 31 개): ms/스텝 −0.26 ± 0.21(유의하지 않음), 토큰/스텝 −1.4% — C=1 에서 이득 없음.
- **추정(`eval --sampler 1.0,20,0.95`, teacher-forced 대리 지표 — 서빙 T=1 수용도 블록 검증의 하한도 아니다):** 뽑은 드래프트가 정확 일치보다 3차 헤드 +5.9%(원본 +4.6%); 1단 top-2 는 greedy 수용 0.857 → 0.928.
- **창에서 나온 버그:** 데이터 부팅 끝의 탭 꼬리(4,096 행 미만)를 멈춘 컨테이너가 쓰지 않음(리뷰 지적), 탭 런이 슬롯 id 로 접힘, warmup 100 > 91 스텝(#1263), bare `wait` 가 heartbeat 를 기다림, 평가 창이 첫 데이터셋에서만 나옴(#1271).
- 기록: `measurements/qwen38_mtp_tune_window_20260919/`.
### Qwen3.8 leave 를 TP 합의 PDL 종속으로 + 대기 중 down projection L2 프리페치 (carry H4): 사이트당 −7~−17 µs, PDL 만은 −1 µs; X1 기각 (2026-09-19, srv4 단일 GPU 레인, PR #1270)
- **무엇.** #1269 의 TP 통신 21%(합마다 약 20 µs 의 피어 대기, 메모리는 논다)에 GLM #473 의 "대기 동안 다음 MHC 불변 가중치 준비"를
  옮겼다. Qwen 믹서는 `leave_norm` → `down_gates`(6.6 MB) → `up_mean` 으로 나뉘어 있다. 그래서 leave 를 합의 PDL 종속으로 띄우고
  (norm 가중치만 대기 앞에서 읽음), 대기 동안 그 사이트의 down projection 을 `prefetch.global.L2` 로 미리 읽는다.
  `lanes.served(leave=off|pdl|prefetch)`, 기본 `prefetch`, 롤백 `ST_LEAVE=off`.
- **판정(`q38leave-0919b`).** 합을 흉내 낸 PDL 대역(WAIT µs) → leave → 믹서를 16 사이트 한 그래프에 넣고, 가중치는 L2 밖에서
  돌렸다. 바이트: 다섯 팔 × 1–16 행 모두 `off` 와 같다(3 ms 늦은 합, NaN 위, eager·재생). 대기 20 µs 에서 사이트 µs `off` →
  `pdl` → prefetch 전부: 1 행 86.5 → 85.4 → **69.1**, 4 행 87.2 → 86.2 → **80.0**, 16 행 91.1 → 90.1 → **80.7**. 대기 0 에서도 나쁘지
  않다. 소스 계수 추정은 C=1 K=3 스텝당 약 −0.7~0.8 ms. **플릿 미실측**(운영자 결정 2026-09-19: 끝의 창). 첫 실행
  `q38leave-0919a` 는 srv4 의 다른 세션 학습 부하(96%) 옆이라 시간이 무효다.
- **X1 기각(측정 없이).** compact 12-CTA consumer 는 ST 엔진이 컴파일한 적이 없다(vLLM 선택 옵션). #967 의 이득은 48-CTA
  consumer 의 빈 CTA 몫이고, Qwen 은 이미 그 경로다. 켜려면 빌드 전체의 발행 판정과 GLM C=1 경로가 바뀐다.
  [상세·원시](measurements/qwen38_leave_pdl_20260919/README.md).

### Qwen3.8 랭크 패킷을 leave 가 접기(carry H5)·MoE 패킷(X2): 바이트는 같고 GPU 쪽은 이득 없음 — 둘 다 기각, X2 dense 의 오라클 비교는 무효 (2026-09-19, srv4 단일 GPU 레인, PR #1270)
- **무엇.** H5: `oneshot_packets` 가 바운드 폭(2560)을 받고, `leave_norm(packets=)` 가 설명자의 네 랭크를 대기 뒤에 랭크 순서로 접는다
  (consumer 의 합과 같은 산술). X2 MoE: 패킷 grid 가 Qwen 의 게이트 합을 TX 에 바로 쓴다. 코드는 기록 브랜치
  `record/qwen38-h5-x2-rank-packets`(`83ebfcb6`, `bd165087`)에만 둔다.
- **바이트.** 단일 GPU one-shot 오라클에서 네 랭크 × 1·4·16 행, 소거 fixture, eager·재생·3 ms 늦은 착지로 봤다. 모두 consumer 합 →
  leave 와 같고, TX 바이트도 `gated_sum` 출력과 같다. GLM 패킷 시험 5건도 같은 전송 소스로 통과했다.
- **GPU 쪽 시간(피어 선착지, warm).** H5 는 consumer → leave 대비 1 행 +1.1, 4 행 +3.4, 16 행 +7.1 µs 다(leave 가 네 패킷을 스트림마다
  다시 읽는다). X2 MoE 는 1 행 −2.3, 4 행 0, 16 행 +3.3 µs 다(피니셔 발사 몫을 접기 비용이 상쇄한다). 서빙 폭에서 이득이 없다.
- **X2 dense 는 판정 못 함.** GLM 직접 생산자 대 GEMM+패킷을 오라클로 재니 합당 약 150 µs 차이가 났다. 이것은 오라클의 48-CTA
  커널이 호출당 약 150 µs 인 인공물이다(프로덕션 moe_packets 는 대기 포함 36–44 µs, #967 의 "CTA 당 약 1.4 µs"). 유효한 근거가 없어
  옮기지 않는다. 재측정 길은 오라클 Ctrl 을 `cudaHostRegister` 로 두거나 GLM 플릿의 `direct_mhc` A/B.
  [상세·원시](measurements/qwen38_rank_packets_20260919/README.md).
### Qwen3.8 비전 2·3단계 — 엔진이 그림을 받는다: mRoPE 커널(텍스트 경로 바이트 동일), 합성 엔진 미디어 훅, `--vision auto`; 타워 bf16 오차 분해 (2026-09-19, CPU·인터프리터, PR #1274)
**GPU·플릿 판정 없음** — 네 노드에 `vision.safetensors` 를 깔고 창에서 부팅·그림 답을 볼 때까지 서빙되지 않는다(`auto`: 파일이 없으면 텍스트 전용 그대로).
- **mRoPE**: `qsa_inputs` 가 [3, N] (t, h, w) 위치를, `qsa_index_keys` 가 그룹 첫 멤버의 회전 위치(`rope_first`)를 받는다. 짝 i 는 축 i % 3(구간 11/11/10 의
  인터리브와 같음). 인터프리터: 같은 축 [3, N] = 1-D 발사 바이트 동일, 다른 축은 짝마다 그 축의 1-D 발사와 바이트 동일. 캡처 디코드는 그림을 서빙하는 넷에서만
  행마다 delta 한 줄(`step_addresses` 한 발사 안), 그림 행이나 그 뒤 ratio-1 토큰 안의 스텝은 eager 에서 [3, N].
- **연결**: `base/composed` 가 그림 기록을 행마다 절대 위치로(이어지는 턴은 재기준) 들고 `check_media`→`bind_media`→`forward(media=)`; park 는 표식·grid 만.
  어댑터가 그림 행을 임베딩 자리에(처음 닿는 조각에서 한 번 인코딩), MTP 헤드도 같은 회전 위치. `--vision auto|on|off`(기본 auto: 네 랭크 모두 파일이
  있을 때만, 일부면 부팅 거부). 스위트: 관련 모듈 전부 OK(main 의 `test_probe_qwen38_kda` 오류 2개 제외).
- **bf16 오차 분해**(`--precision`): 서빙 그대로 5.6~5.7%, MLP 만 fp32 4.3~4.9%, 블록 연산 전부 fp32 3.6~4.8%, 스트림만 fp32 변화 없음 — 한 곳으로는
  안 줄고 둘 다 fp32 여야 0. 원인은 거대 활성(마지막 블록 뒤 한 채널 ±8,209, 에너지 96%). bf16 유지(서빙 vLLM 과 같음).
  [상세·원시](measurements/qwen38_vision_tower_20260919/README.md).

### Qwen3.8 플릿이 문법 컴파일러를 묶는다 — `tools` 요청이 전부 400 이던 것, xgrammar 는 문의 토크나이저에서 읽는다 (2026-09-19, srv4 CPU, PR #1282)
- **무엇.** 플릿 MTP 데이터 창(main `a8e3c4de`)에서 `tools` 가 실린 채팅 요청이 전부 400 `no grammar compiler is bound` 였다. 문은 도구 호출 문법을 싣는데
  플릿 부팅이 서빙 모델에 컴파일러를 묶지 않았다(CPU 부팅·GLM 플릿은 묶는다). Deneb 의 에이전트 트래픽을 Qwen3.8 이 서빙하지 못했다.
- **고침.** 프렐류드가 모든 랭크에서 컴파일러를 만들고, `build` 의 `qualify grammar` 행이 캡처 전에 마스크 커널을 증명해 묶는다. 문의 정책·서빙 검증 경로는
  그대로다. 문법 행은 이미 rich 행이다: argmax 드래프트, 위치마다 마스크를 거친 픽, draft-ahead(#1273)도 그 행이 있는 스텝은 받지 않는다.
- **토크나이저 경로.** 서빙 이미지(`st-engine:qwen38`)·서빙 메타에서 문의 `tokenizers.Tokenizer` 로 만든 TokenizerInfo 가 transformers 경로와
  vocab dict 248,077 · decoded vocab 248,320 · stop/special id · `dump_metadata` 모두 같고, 도구 호출(lazy 도구 문법 25 위치)·JSON(22 위치)의 마스크도
  위치마다 같다 → GLM 과 같은 백엔드 경로. 랭크당 CPU 약 2 s 대 6 s(프로덕션 옆, 경합 아래).
- **검증.** CPU 테스트 12(`tests/test_engine_qwen38_fleet_grammars`): 모든 랭크의 프렐류드, 묶기와 거절, 부팅 순서, 문(묶기 전 400 · 묶은 뒤 200, lazy 도구
  문법), 서빙 검증에서 문법 행이 rich 행(greedy · 샘플 행 옆 · 마커에서의 무장 · draft-ahead). 일부러 망가뜨린 세 곳을 모두 잡는다.
- **미측정.** GPU 부팅(`qualify grammar`·`prelude_s`·`wait for the prelude` 행)과 `tools` 요청 끝까지 — 운영자의 다음 창. `tools` 요청은 첫 토큰부터
  rich 행이라 tok/s 가 문법 없는 요청보다 낮을 수 있다(미측정).
  [대조·로그](measurements/qwen38_fleet_grammars_20260919/README.md).

