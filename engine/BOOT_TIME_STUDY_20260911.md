# ST 엔진 부팅 시간 — 탐구 (2026-09-11 밤)

다른 세션이 srv4·srv2 에서 돌린 **실제 45층 4노드 부팅**의 로그·계측 표·타임스탬프로 쟀다.
내가 띄운 것은 없다(플릿 사용 중). "실측" 은 그 부팅에서 읽은 값이다.

## 1. 판정

**부팅은 90 초이고, 그중 45% 가 가중치 읽기, 31% 가 그래프 캡처다.** 둘 다 레버가 있고,
읽기 쪽은 이미 고쳐 둔 것(PR #557 의 O_DIRECT)이 정확히 그 자리를 겨눈다.

## 2. 실측 — rank 0(srv2), 따뜻한 JIT 캐시

컨테이너 시작 `13:45:39.3` → 서빙 `13:47:09.5` = **90.2 초**. 계측 표(`rec.table()`)와 타임스탬프를 합치면:

| 구간 | 초 | 비중 | 무엇 |
|---|---:|---:|---|
| 파이썬 + torch + facts | 1.8 | 2% | `box:` 줄까지 |
| 계측 안 되는 앞부분 | 15.6 | 17% | NCCL 랑데부(노드 스태거 9.0 s 포함), 커널 임포트, 입장 관문·예산, 토크나이저·챗 템플릿 |
| 아레나 | **1.9** | 2% | 57.15 GiB 한 할당 |
| **로드** | **40.2** | **45%** | 44.5 GiB / 54 런 = **1.11 GiB/s** |
| 드래프터 로드 | 2.6 | 3% | 2.18 GiB = 0.86 GiB/s |
| **캡처(capture decode)** | **28.2** | **31%** | 프리필 웜업 2 + **그래프 115개** |

노드 스태거(런처가 노드마다 rsync→build→run 을 순차로): srv2 `:39.3` / srv1 `:42.9` / srv3 `:46.1` / srv4 `:48.3` = **9.0 초**.
rank 0 은 마지막 노드를 기다리므로 이 9 초가 rank 0 의 벽시계에 그대로 들어간다.

**임포트 실측**(GPU 없는 컨테이너): torch 1.22, tilelang 1.70, flashinfer 1.06, deep_gemm 0.15, triton 0.09,
`engine.kernels.*` 합계 0.30, 토크나이저+transformers 1.26 → 앞부분 15.6 초 중 **약 4 초가 임포트**, 나머지는 랑데부·관문.

**캡처의 구성**(메모리 원장 351 체크포인트에서):

| | 그래프 | 체크포인트 |
|---|---:|---:|
| target (용량 9 단 × seq 4) | 36 | 108 |
| sampling (target 모양마다 greedy + stochastic) | **72** | **216** |
| drafter (propose 1 + observe 6) | 7 | 21 |
| 프리필 메모리 웜업(문맥 2) | — | 4 |
| 합 | **115** | 351 |

그래프 하나당 **평균 245 ms**(28.2 s / 115), 각각 워밍업 2 + 캡처 1 = 모델 실행 3회.
용량 사다리는 4,096 → 1,048,576 을 2배씩 9 단(상한 = `min(블록 580 × 2304, facts.max_position)`).

## 3. 정정 — 로그의 침묵은 작업이 아니었다

먼저 rank 3 의 로그에서 "MLA 무장(66.9 s) 뒤 **281 초의 침묵**" 을 보고 그것을 캡처라고 적었다. **틀렸다.**
계측 표가 캡처를 28.2 초라고 말한다. 그 281 초는 부팅이 끝난 뒤 **조용히 서빙하던 시간**이었고,
그 컨테이너는 뒤이어 교체됐다. 로그가 없는 구간을 일로 읽으면 안 된다 — 이 문서의 수치가 전부
계측 표와 타임스탬프에서 온 이유다.

## 4. 레버

### A. 로드 40.2 초 → 기대 ~17 초 (이미 구현, PR #557)
1.11 GiB/s 는 같은 NVMe 가 O_DIRECT `dd` 로 내는 **8.3 GB/s** 의 13% 다. #557 이 로더를 O_DIRECT 로 바꿨고
호스트 실측은 **0.70~0.88 → 1.92~2.09 GiB/s**(cpu 대상, 3회)였다. 장치 대상에서 같은 배율이면 40.2 → **약 17 초**,
부팅의 **26%**. 확인은 #557 이 들어간 첫 부팅의 같은 표로. (부수: 페이지 캐시 44.5 GiB 가 안 생긴다 — OOM 탐구 §6-b.)

### B. 그래프 115개 → 35개 (캡처 28.2 초의 일부)
- **B1. 샘플러가 용량마다 잡혀 있다.** 샘플러는 로짓 모양 `(n·t, vocab)` 에만 의존하는데 용량마다 target 그래프의
  **출력 버퍼가 다르다**는 이유로 36 × 2 = 72 개가 잡힌다. target 이 `(n, t)` 별 **정적 로짓 버퍼**에 쓰면
  샘플러는 **8 개**로 준다(−64 그래프, 전체의 56%). 표준적인 정적 출력 버퍼 패턴이고, 그래프 풀 메모리도 같이 준다.
- **B2. 사다리가 2배씩 9 단이다.** 4배씩이면 5 단(target 36 → 20). 대가는 경계 바로 위 문맥이 최대 4배의
  인덱서 후보 일을 한다는 것 — 재서 정할 값이지 공짜가 아니다.
- B1+B2 를 합치면 **115 → 35 개**. 다만 캡처 28.2 초 안에서 그래프와 프리필 웜업의 몫이 갈려 있지 않아
  절감액은 아직 **상한만 말할 수 있다**(§5).

### C. 노드 스태거 9.0 초 → ~2 초
런처가 노드마다 rsync → build → run 을 순차로 한다. 네 노드를 병렬로 띄우면 rank 0 이 기다리는 시간이 사라진다.
`&` + `wait` 한 줄이고, 실패 처리(ABORT)는 각 백그라운드의 종료 코드를 모아야 한다.

### D. 앞부분 15.6 초
임포트 4 초는 라이브러리 몫이라 건드릴 것이 없다. 남는 ~11 초는 NCCL 랑데부와 관문인데 **계측이 없다** —
C 를 하면 스태거가 빠지고, 나머지는 §5 가 답한다.

### E. 프리필 메모리 웜업(문맥 2개)
`_warmup_prefill_memory` 가 6,912 토큰 프리필을 문맥 0 과 문맥 ~1.33M 두 곳에서 돌린다(#549 의 메모리 자격 검사).
캡처 28.2 초 안에 있고 몫은 미측정. 만약 크면 "문맥 두 곳" 이 정말 필요한지가 질문이 된다 —
먼 문맥 쪽이 인덱서의 최대 후보 수를 긋는 자리라 지우기보다 **재고 나서** 정할 일이다.

## 5. 다음 측정 — 한 줄이면 캡처 28.2 초가 갈린다

`base/runtime_memory.checkpoint()` 는 이미 351개 행을 쓰고 **행마다 `cuda.synchronize()` 를 부른다**.
행에 경과 시간을 한 필드 더 넣으면(`row["at"] = monotonic() - start`) 다음 부팅이
프리필 웜업 / target / sampling / drafter 의 몫과 **용량별 비용**을 정확히 말한다. 그게 B1·B2·E 의
절감액을 상한이 아니라 수치로 만든다. 지금 예산 파일이 그 원장을 읽듯(`budget.py --ledger`),
부팅 시간도 같은 원장에서 나오게 된다.

## 5-b. 그래서 한 것 (같은 날 밤)

| 레버 | 한 것 | 기대 |
|---|---|---|
| **§5 계측** | `runtime_memory` 의 행마다 `at_seconds`·`seconds`, 리포트에 `spend`(접두사별 합계). 행은 이미 `cuda.synchronize()` 를 부르므로 공짜다 | 다음 부팅이 캡처 28.2 초를 프리필 웜업 / target / sampling / drafter 와 **용량별**로 가른다 |
| **D 앞부분** | `fleet()` 의 `comm`(랑데부)·`lanes`(커널 임포트)·`door`(토크나이저·챗 템플릿)를 계측 표의 단계로 | 15.6 초의 미계측 구간이 표에 들어온다 |
| **B1 샘플러** | target 이 `(seqs, tokens)` 별 **정적 로짓 버퍼**에 쓰고, 샘플러는 그 버퍼당 하나. 다른 버퍼를 주는 target 은 거부(계약이 썩지 않게) | **그래프 115 → 51**(sampling 72 → 8), 그래프 풀 메모리도 같이 준다 |
| **C 스태커** | 런처가 네 노드를 병렬로 준비·기동, 출력은 랭크 순서로 모아 찍고, 한 노드가 실패하면 나머지도 정지(부분 플릿을 남기지 않는다) | 9.0 초 → ~2 초 |
| **A 로드** | PR #557 의 O_DIRECT (이미 머지) | 40.2 초 → 기대 ~17 초 |

**B2(사다리 간격)는 하지 않았다.** 4배 간격은 경계 바로 위 문맥이 최대 4배의 인덱서 후보 일을 하는 대가를 치른다 —
그 값은 §5 계측이 붙은 다음 부팅이 말해 준다. 재기 전에 서빙 비용을 거는 변경은 이 원장의 규율이 아니다.

**아직 재지 않은 것**: 위 넷이 합쳐 90.2 초를 얼마로 만드는지는 **#557·이 변경이 들어간 첫 45층 부팅의 같은 표**가 말한다.
실가중치 그래프 프로브(`probes/engine_decode_graph_check.py`)도 플릿 창이 필요하다 — 합성 target 으로는
샘플러 공유(캡처 수·모든 버킷의 재생·계약 위반 거부)만 확인했다.

## 5-c. 다음 부팅이 답했다 (2026-09-12 06:44, 원장에 시계가 붙은 첫 부팅)

다른 세션의 45층 4노드 부팅. 단계가 **351 → 159**(샘플러가 접혔다), 원장에 시계가 있다.

**계측 표(rank 0) — 어제와 나란히:**

| 단계 | 09-11 13:45 (따뜻한 JIT, 그래프 115) | 09-12 06:44 (**차가운 JIT**, 그래프 51) |
|---|---:|---:|
| comm (랑데부) | 미계측 | 1.9 |
| lanes (커널 임포트) | 미계측 | 4.0 |
| arena | 1.9 | 2.1 |
| **load** 44.5 GiB | **40.2 (1.11 GiB/s)** | **5.5 (8.03 GiB/s)** |
| load drafter 2.18 GiB | 2.6 | 0.2 |
| capture decode | 28.2 | 108.6 |

**로드 레버는 다 썼다.** 40.2 → **5.5 초**, 44.5 GiB 를 **8.03 GiB/s** 로 — 같은 NVMe 의 O_DIRECT `dd` 8.3 GB/s 와 같다.
디스크 속도에 붙었으므로 여기 남은 것은 없다(드래프터도 2.6 → 0.2). 기대했던 ~17 초보다 훨씬 좋았다.

**캡처 108.6 초는 차가운 부팅의 숫자다.** 그 시간대에 **triton 커널 139 개**(파일 1,112)와 CuTe-DSL 6 개가 새로 구워졌다 —
main 이 하루 47 커밋으로 커널(conv 링 융합·KDA 링·MLA DSMEM)을 바꿨기 때문이다. 원장이 그것을 분리해 준다:

| 캡처 안쪽 | 초 | 무엇 |
|---|---:|---|
| prefill 첫 번째 (문맥 0) | **70.02** | 같은 모양의 두 번째가 10.46 이므로 **차이 59.6 초가 첫 호출 비용**(JIT·오토튠) |
| prefill 두 번째 (문맥 1,329,408) | 10.46 | 45층 6,912 토큰 프리필의 따뜻한 값 |
| **target 36 그래프** | **27.62** | 용량 단마다 **3.0 초, 평탄**(4,096 도 1,048,576 도 같다) |
| drafter 7 그래프 | 0.32 | |
| **sampling 8 그래프** | **0.16** | 72 → 8 로 접기 전이 ~1.3 초였다는 뜻 — 이 레버는 작았다 |
| `loaded`(아레나+로드+드래프터+구성) | 15.31 | 표의 arena+load+drafter 7.85 를 빼면 **7.5 초가 이름 없음** |

## 5-d. 그래서 추가로 가능한가 — 가능하고, 이제 값이 붙어 있다

| 레버 | 값 | 상태 |
|---|---:|---|
| **A. 첫 호출 비용(JIT·오토튠)** | **59.6 초** (차가운 부팅마다) | 커널이 바뀔 때마다 되풀이된다. 지금은 `capture_decode` **안**에서 일어나는데 그 구간은 콜렉티브가 있어 **네 랭크가 맞물린다** — 한 노드가 컴파일하면 셋이 선다. 랑데부 **앞**으로 옮기면(빌드나 런처의 웜업 단계) 총량은 같아도 맞물림이 사라진다 |
| **B. 용량 사다리** | **3.0 초 × 9 단 = 27.6 초** | 평탄이 측정됐으므로 ×4 간격(9 → 5 단)은 **정확히 −12 초**. 대가(경계 바로 위 문맥의 인덱서 후보 일)는 프로브 하나로 잴 수 있다 |
| **C. 워밍업 2 → 1** | target 27.6 의 **~1/3 = 9 초** | 같은 (n,t) 가족의 첫 모양만 2회로. 게이트는 이미 있는 실가중치 그래프 프로브(전 케이스 relative 0.0) |
| **D. 두 번째 프리필 웜업** | 10.46 초 | #549 의 먼 문맥 메모리 자격 검사. 지우는 것은 부팅 시간이 아니라 **메모리 게이트** 결정이다 |
| **E. 이름 없는 7.5 초** | 7.5 초 | 캐시·bind·토크나이저·티어 스테이징·예산. `rec.phase` 하나 더면 이름이 붙는다 |
| ~~로드~~ | — | **다 썼다**(8.03 GiB/s = 디스크 속도) |
| ~~샘플러~~ | — | **다 썼다**(0.16 초) |

> **이 아래 두 문단은 틀렸다 — §5-f 가 실측으로 대체한다.** 추정 60 초가 아니라 **87.0 초**였고,
> 캡처를 27.6 이 아니라 **64.9** 로 두고 다시 따져야 한다. 추정을 지우지 않고 남겨 둔다.

~~따뜻한 부팅의 현재값은 대략 comm 1.9 + lanes 4.0 + arena 2.1 + load 5.5 + drafter 0.2 + 이름없음 7.5 +
캡처(프리필 2 × 10.5 + target 27.6 + 1.5) ≈ **60 초**(어제 90.2). B+C+E 를 더하면 **~40 초**.
A 는 따뜻한 부팅엔 없고 차가운 부팅에서 60 초를 뗀다.~~

~~**아직 못 재는 것**: 따뜻한 JIT + 현재 코드의 부팅. 오늘 것은 차갑고 어제 것은 코드가 달랐다.
다음 따뜻한 부팅의 같은 표가 B·C 의 기준선이다.~~ → **쟀다: §5-f.**

## 5-e. 남은 레버 해결 (2026-09-12) — 둘은 가져가고, 둘은 근거를 대고 버린다

원장이 갈라 준 값으로 다시 따졌다. `warmup` 51개 **18.32 s**, `captured` 51개 **9.48 s**,
`before`(계측 그 자체) 53개 **1.16 s** — **내 계측은 부팅의 1% 였다**(레버가 아니다).

### 가져간 것

| 레버 | 값 | 한 것 |
|---|---:|---|
| **C. 워밍업** | **≤ 8 s** (아래 정정) | `DecodeGraphs(warmup=…)` 가 수 **또는 모양의 함수**를 받는다. 프로필은 `(seqs, tokens)` **가족의 첫 모양만 2회**, 뒤따르는 용량 버킷은 1회를 선언한다(첫 패스가 컴파일·오토튠, 둘째가 할당자를 안정시킨다; 뒤 버킷은 같은 커널을 다른 후보 수로 돌 뿐이다). 그 말이 성립하도록 모양을 **가족 순서로** 낸다. 72 패스 → 40 |
| **E. 이름 없는 7.5 s** | 이름이 붙었다 | `caches` · `engine` · `runner` 가 계측 표의 단계로 |

### 근거를 대고 버린 것

**A. 첫 호출 비용 59.6 초 — "랑데부 앞으로 옮기면 맞물림이 사라진다"는 내 말이 틀렸다.**
그 시간대에 새로 구워진 아티팩트: srv4 **1,148**, srv2·srv1·srv3 **각 1,158**. 네 노드가 **같은 것을 동시에** 컴파일했다.
기다린 노드가 없으므로 앞으로 옮겨도 벽시계는 그대로다. 줄이려면 **덜 컴파일**하거나 **빨리 컴파일**해야 하고,
전자는 곧 사다리(아래에서 기각), 후자는 오토튠 설정을 코드로 박는 일(D11, 지금 커널 쪽 캠페인이 하고 있다)이다.
성격도 다시 적는다: 캐시는 `/cache` 로 부팅을 넘겨 살므로 이건 **부팅마다가 아니라 커널이 바뀔 때마다**의 비용이다 —
개발 중에는 잦고, 안정된 배포에서는 한 번이다.

**B. 사다리 ×4 (−12 초) — D10 과 맞바꾸는 값이라 안 가져간다.**
버킷은 **패딩된 후보 전체**를 매 디코드 스텝에 본다(`candidate_capacity = capacity // kpool`):

| 버킷 | 후보 | 게더 | 인덱서 로짓 |
|---:|---:|---:|---:|
| 4,096 | 1,024 | 1.4 MiB | 2.2 GFLOP |
| 65,536 | 16,384 | 22.7 MiB | 35.4 GFLOP |
| 262,144 | 65,536 | 90.8 MiB | 141.7 GFLOP |
| 1,048,576 | 262,144 | **363.0 MiB** | **566.9 GFLOP** |

×2 사다리에서 문맥은 버킷의 (50, 100] % 라 최대 2배를 패딩으로 낸다. ×4 면 **최대 4배**가 된다 —
큰 문맥에서 스텝당 수 ms, ITL 의 몇 %다. **부팅은 한 번 내고 ITL 은 토큰마다 낸다**(D10). 그래서 기각.
같은 목적의 대가 없는 판본은 **운영자가 서빙 최대 문맥을 선언**해 사다리 위쪽을 잘라내는 것이고, 그건 제품 결정이다.

**D. 두 번째 프리필 웜업 10.46 초** — 먼 문맥의 메모리 자격 검사(#549)다. 부팅 시간이 아니라 **메모리 게이트** 결정이므로 손대지 않는다.

### 남은 값 — 은 추정이었고, 같은 날 실측이 나왔다 (§5-f)

C 의 값도 다시 적는다. 워밍업 18.32 s 는 51 모양 **102 패스**의 합이고 그중 target 이 72 패스다
(드래프터·샘플링은 둘이 합쳐 0.48 s 라 무시해도 된다). 패스당 ≈ 0.25 s.
정책이 지우는 것은 **72 중 32 패스**이므로 패스 값이 고르다면 **≈ 8 s**, 44% 다 — "절반" 이 아니다.
게다가 한 모양의 두 패스는 같은 값이 아닐 수 있다(첫 패스가 컴파일·오토튠을 이고, 둘째는 할당자만 안정시킨다).
원장은 두 패스를 **한 행**으로 찍으므로 이 데이터로는 못 가른다. 그래서 **≤ 8 s** 로 적고,
다음 부팅의 `capture decode` 가 값을 말하게 둔다. 기준선은 아래 64.94 s 다.

## 5-f. 정정 — 따뜻한 부팅은 50 초가 아니라 **87.0 초**이고, 이제 전부가 캡처다 (2026-09-12 08:37)

내가 띄운 것이 아니다. 다른 세션이 `st-fleet.lock` 을 쥐고 08:37 KST 에 올린 **머지된 코드**의 부팅을
로그로 읽었다(그 이미지의 `graphs.py` 는 아직 `warmup: int = 2` 다 — 즉 **내 변경의 기준선**이다).
컨테이너 시작 23:37:26.50 → `serving on :8000` 23:38:53.54 UTC, rank 0 = srv2.

| 구간 | 초 | 몫 |
|---|---:|---:|
| 앞부분(파이썬·torch·커널 임포트, facts, 관문, 랑데부) | 9.17 | 11% |
| comm | 1.59 | 2% |
| lanes | 3.63 | 4% |
| arena | 2.22 | 3% |
| **load** | **5.25** | 6% |
| load drafter | 0.24 | 0% |
| **capture decode** | **64.94** | **75%** |
| **합** | **87.04** | |

세 가지가 여기서 판가름난다.

**로더는 끝났다 — 두 번째 부팅에서도.** 44.5 GiB 를 5.245 s, **8.49 GiB/s**. 90.2 기준선의 40.2 s 대비 −35 s.
드래프터도 2.55 → 0.24. 이건 이제 반복 측정된 사실이다.

**앞부분도 줄었다.** 15.6 → 9.17. 런처 병렬화가 스태거 9.0 을 먹었다(레버 ③).

**그런데 벽시계는 90.2 → 87.0, 겨우 −3.2 다.** 로드에서 번 35 초를 캡처가 28.19 → 64.94 로 **거의 그대로 도로 먹었다**.
캡처가 커진 건 내 쪽의 퇴행이 아니라 그 사이 머지된 일이다 — 먼 문맥 프리필 웜업 2 회(#549), 새 커널들(#571 conv ring, #572 KDA ring)이
각자 첫 호출 컴파일과 워밍업을 들고 왔다. 실제로 이 부팅은 **서빙을 시작한 뒤에도** CuTe-DSL 정적 MoE 커널을 굽고 있었다(23:39:36, 서빙 41 초 뒤).

**그래서 이 문서의 결론이 바뀐다.** "로드가 절반" 이던 부팅은 이제 **4 분의 3 이 그래프 캡처**다.
남은 모든 레버는 캡처 안에 있고, 위 §5-e 가 그 안을 갈라 놓았다: 워밍업(내 변경, ≤ 8 s), 첫 호출 컴파일(커널 소유자·D11),
모양 수(사다리 = ITL 과의 맞바꿈, 기각), 먼 문맥 자격 검사(메모리 게이트, 운영자).
**캡처 바깥에서 더 짜낼 것은 남아 있지 않다** — comm·lanes·arena·load·drafter 를 **0** 으로 만들어도 부팅은 74 초다.

## 6. 하지 말 것

- **캡처를 서빙 경로로 미루기(lazy capture).** I1 이 금지한다 — 선언되지 않은 모양은 스케줄러 버그이고(D3),
  첫 요청이 컴파일 비용을 내는 것은 ITL 계약(D10)을 깬다.
- **워밍업 2 → 1 을 일괄로.** 첫 실행이 JIT·오토튠을 끌고, 둘째가 할당을 안정시킨다. 모양마다 같은 커널을
  이미 데운 경우에만 의미가 있고, 그 판정에는 §5 의 계측이 먼저다.
- **JIT 캐시 최적화.** 이번 부팅들은 전부 따뜻했다(2시간 내 새로 구운 아티팩트 0개). 캐시는 `/cache` 바인드로
  부팅을 넘겨 살고, 콜드 비용은 **새 노드의 첫 부팅 한 번**뿐이다(triton 3,288 디렉터리 743 MB, flashinfer 80 MB).
- **아레나 1.9 초.** 57 GiB 한 할당이 2 초다. 여기엔 아무것도 없다.

---
**한 줄 요약**: 90 초 중 40 이 읽기(이미 고친 자리), 28 이 캡처(그래프 115개 중 72개가 없어도 되는 샘플러),
9 가 노드 스태거다. 캡처 안쪽을 가르는 데 필요한 것은 원장 행의 타임스탬프 하나다.
