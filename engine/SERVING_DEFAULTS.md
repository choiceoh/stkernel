# 무엇이 기본으로 켜져 있나 (서빙 레버)

> 살아 있는 참조 — **레버의 기본값이 바뀌면 그 줄도 같이 바뀐다.** 여기가 코드와 다르면 그건 버그다.

CHARTER **D11**(2026-09-14 운영자 지시): *"항상 기본적용하고 추가개선"* — 새 ST 성능 개선은 실험·production 의
**기본 실행 경로에 켜고**, 기본 적용과 **실측 검증 상태를 따로 적는다.** 아직 측정 중인 축은 임시 롤백만 남긴다.

그러려면 지금 무엇이 켜져 있는지 한 곳에서 보여야 한다. 이 파일이 그 자리다 — 레버마다 **기본값 · 켬/끔 ·
그렇게 정한 근거**. 근거는 원장([MEASUREMENTS.md](../MEASUREMENTS.md))의 항목이거나, **없으면 "원장 항목 없음"** 이다.
끄고 켠 판정이 아니라 **누가 무엇을 재서 그렇게 뒀는지**가 이 표의 내용이다.

## Qwen3.8-Flash-Next

서빙 진입은 `engine/profiles/qwen38/fleet.py`, 플릿 부팅은 `launchers/start-st-qwen38.sh`(환경변수가 그대로 플래그가 된다).

| 레버 | 런처 | 기본 | 상태 | 근거 |
|---|---|---|---|---|
| MTP 드래프터 | `ST_DRAFTER=1` | 켬 | **켬** | 체크포인트의 MTP 헤드; `--no-drafter` 가 롤백 |
| **드래프트 수 K** | `ST_SPEC_K` (`facts.SPEC_K`) | **3** | **켬 (1 → 3)** | 09-18 플릿 짝(PR #1182): 토큰/스텝 **+44/+36%**, 디코드 tok/s **+12.5/+9.6%**, 프리필 포함 +17/+13%. 네 행을 죽이던 정적 m10·m12 는 #1192 에서 닫혔고, 09-19 11:19 창에서 **K=3 · 4 행이 떠서 답했다**(STALL 0). 그 창이 건 조건(요청 중 컴파일)은 #1230·#1242 가 부팅으로 옮겼다. **워밍업 뒤 K=3 대 K=1 짝은 아직 미측정** |
| one-shot 전송 | `ST_ONESHOT=1` | 켬 | **켬** | 09-18 이후 런처 기본; `--no-oneshot` 이 롤백(hidden 2560 셀은 미측정) |
| 인덱스 쿼리 샤딩 | `ST_QUERY_SHARDS=1` | 켬 | **켬** | 운영자 09-18 결정(carry Q11), 플릿 미측정 |
| shared expert 포크 | `ST_SHARED_OVERLAP=one` | `one` | **켬(한 요청의 행만)** | 09-19 플릿 창(PR #1233): C=1 **−5.0%**, C=4 **+6.0%**(느려짐) → 한 요청의 행만 포크, 운영자 "켜"(PR #1234). `all` 은 C=4 에서 진 팔이다. 잰 것은 **K=1**(한 요청 2 행)이다 — K=3 기본(한 요청 4 행)에서의 포크는 미측정이고, 순서를 바꾼 확인 쌍은 두 번째 창에서 옆 레인 프로브 때문에 무효였다(PR #1252) |
| MTP dense 정밀도 | `ST_MTP_PRECISION` | `bf16` | **켬(정밀도 투자)** | #1226, 운영자 "그정도면 bf16으로 해": K=3 드래프트 그래프 1 행 W4A8 3.97 → BF16 4.39 ms |
| MTP 전문가 | `ST_MTP_EXPERTS` | `bf16` | **켬(정밀도 투자)** | #1235: 서빙하던 NVFP4 재인코딩이 원본 BF16 대비 **23%** 어긋나 있었다 |
| 어텐션 뒤 행 선택 | — | 켬 | **켬** | #1226 `mtp_forward(rows=)`; 전 행 계산 뒤 선택과 **바이트 동일**(`q38mtp-0919c`) |
| 드래프트 헤드 판독 W8A16 | — | 켬 | **켬(정밀도 투자)** | #1246: FP8 헤드를 BF16 행으로 읽는다 — 같은 바이트(×0.99~1.02), BF16 기준 오차 0.036~0.041 → **0.025~0.028**, 무작위 쿼리 argmax 일치 91.77 → **93.46%**. 검증 스텝 헤드는 그대로 |
| **hc FP8 믹서** | `ST_HC_FP8=0` | 끔 | **끔 — 잰 뒤 기각(디코드)** | 09-19 두 번째 운영자 창(PR #1252, K=1): 깨끗한 기본 부팅 대비 C=1 디코드 28.35 → 31.08 ms/스텝 **+9.6%**(다섯 요청 모두 느림), C=4 +4.5%(잡음 안), ready 61 → 98 s. 믹서 바이트 절반을 발사 증가와 H2 접기의 이탈이 넘는다. 이 표에서 **유일하게 출력을 바꾸는** 레버이기도 하다(품질 브래킷 없음, D4) — 디코드에서는 그 전에 속도로 진다. 프리필(4,096 행)은 미측정 |
| **IVF 근사 드래프트 헤드** | `ST_DRAFT_INDEX` | 끔 | **끔 — 잰 뒤 기각** | #1246: 발사는 71~86 µs 대 전체 헤드 689~696 이지만 **재현율 3~29%(뚜렷한 토큰)·53~60%(섞임)**. 드래프터는 이 엔진이 **정밀도를 투자하는** 자리라는 규칙(#1235)과도 반대다. 끔 유지 |
| **Windowed-MTP** | `ST_MTP_WINDOW` | `1,511` | **켬** | 운영자 "전부 켜"(09-19, PR #1249). 2,048 위치 아래에서는 창이 모든 그룹이라 covered 선택과 같은 어텐션 — 짧은 문맥은 바뀌지 않는다. 그 위의 **수용률은 원장 항목 없음**(미측정). 드래프트 그래프에서 빠지는 것은 QSA 점수·선택뿐(레인 `q38win-0919a`: QSA 계열 4.6K 0.08 → 262K 0.47 ms/드래프트; wall 은 옆 랭크의 부하로 판정 불가), 창 id 는 한 발사(`kernels/qsa_window`). `off` 가 롤백 |
| **LibraSpec 임계** | `ST_DRAFT_THRESHOLD` | `0.1` | **켬(1~2 행 스텝)** | 운영자 "전부 켜"(09-19, PR #1249). 출력은 안 바뀐다(검증 그대로). **0.1 은 측정이 아니라** #1228 숫자의 손익 산수(검증 위치 약 1.75 ms 대 토큰 약 14 ms) — 원장 곡선이 값을 정한다(드래프트 원장 켬). 좁은 검증 그래프가 없는 3 행 이상은 자르지 않는다(잘라도 패딩될 뿐). **09-19 튜닝 창(C=1 greedy, 튜닝 헤드): 스텝 시간을 줄이지 못했다**(38.17 대 끔 38.06 ms, 원장 곡선 행당 0.10 위치 절약 · 0.0055 토큰 손실) — C=1 에서는 중립, C=2 는 미측정. `off` 가 롤백 |
| 드래프트 쿼리 탭 | `ST_TAP_DRAFT_QUERIES` | 끔 | 끔(**진단**) | IVF 헤드의 실제 재현율을 오프라인에서 재려는 도구(#1232) |
| **샘플 드래프트 + 블록 검증** | `ST_DRAFT_CANDIDATES` | `20` | **켬(샘플 행)** | 운영자 "셋다 해보던가"(09-19). T>0 이고 rich 옵션이 없는 행은 헤드 분포(상위 20 후보 위의 행 자신의 샘플러)에서 드래프트를 **뽑고**, 검증은 `base/sampler.block_verify_batch`(GLM 과 같은 블록 검증) + 랭크 0 판정(`agree_verdict`). 출력 분포는 타깃 그대로(CPU 카이제곱 검정). 정확 일치는 타깃의 뽑기가 헤드의 argmax 에 떨어질 때만 드래프트를 남긴다. greedy 행은 두 규칙이 같아 바뀌지 않는다. 샘플 행은 임계로 자르지 않는다(뽑힌 토큰의 확률로 자르면 드래프트 분포가 치우친다). **라이브 수용률 · 드래프트 비용 미측정.** `0` 이 롤백 |
| 드래프트 원장 | `ST_DRAFT_LEDGER=1` | 켬 | **켬(진단)** | 운영자 "전부 켜"(PR #1249): 임계 곡선의 재료 — 검증된 행마다 랭크 0 에 JSON 한 줄(픽 · 확률 · 제안 · 수용). `ST_DRAFT_LEDGER=0` 이 롤백 |
| **튜닝된 MTP 헤드** | `ST_MTP_TUNED` | 끔 | **끔 — 결과는 있고, 기본값은 다음 창의 헤드로 정한다** | 09-19 튜닝 창(`measurements/qwen38_mtp_tune_window_20260919`): 네 Spark 에 `st-qwen38-mtp-tuned{,2,3}` 이 있다. 라이브 C=1 greedy(평가 40 개) 토큰/스텝 1차 +4.9%, **2차 +6.6%(tok/s +8.5%)**, **3차 +6.9%(tok/s +6.5%)**; held-out 2.608 → 3.160. 더 넓은 데이터(v2 합성 + Deneb 실제 텍스트)의 4차가 같은 날 저녁 창에서 돈다 — 그 창의 최선 헤드를 기본으로 켠다(D11). T=1 은 미측정 |
| MTP 입력 탭 | `ST_TAP_MTP_INPUTS=1` | 켬 | **켬(데이터 수집)** | 운영자 "전부 켜"(PR #1249): 헤드가 보는 것(타깃의 스트림과 다음 토큰)을 랭크 0 이 기록해 모든 Qwen 부팅이 튜닝 데이터를 쌓는다. 디렉터리 **64 GiB 상한**(이전 부팅분 포함), 복사는 검증 스텝이 pick 을 읽은 뒤라 디바이스는 이미 쉬고 있다. 그 비용은 미측정. `ST_TAP_MTP_INPUTS=0` 이 롤백 |

**끔의 이유는 둘이다.** `--hc-fp8` 은 디코드에서 **재 보니 느렸고**(C=1 +9.6%, #1252) 그 위에 **품질 게이트(D4)** 도 남아 있다(이 표에서 유일하게 출력을 바꾼다),
`--mtp-tuned` 는 **가리킬 결과가 아직 없다.** `--mtp-window` 와 `--draft-threshold` 는 창에서 재야 판정이 되는
팔이지만(전자는 긴 문맥의 수용률, 후자는 P) **운영자가 미측정 상태로 켜라고 했다**("전부 켜", 2026-09-19, PR #1249):
출력은 둘 다 바뀌지 않고, 판정은 원장 항목이 생길 때 이 표의 근거 칸을 바꾼다. 그때까지 이 표는 둘을 **켬 · 미측정**으로 말한다.

**그리고 잰 뒤 진 팔은 켜는 대상이 아니라 롤백이다**: `--draft-index`(재현율 3~29%, #1246), `--shared-overlap all`
(C=4 +6%, #1233), `--mtp-experts nvfp4`(원본 대비 23%, #1235). 셋 다 트리에 남아 있는 것은 되돌릴 자리로서이지
아직 안 켠 개선으로서가 아니다. 플래그가 아닌 진 팔은 트리에서 뺐다: W4 GEMM 의 입력 재사용을 Qwen3.8 투영에 넣은 #1241 은 o_proj 에서 +4.5~+13.6% 라 되돌렸다(#1252).

## GLM-5.3 (프로덕션)

GLM 쪽 레버는 이미 **데이터로 선언돼 있다**: `engine/profiles/glm53/boot.py` 의 `Knob(...)` 목록(이름 · 기본값 ·
**만료일** · 무엇을 재는가 · 롤백)을 `engine/base/config.Config` 가 부팅에서 강제하고, 그 표를 부팅이 그대로 찍는다
(`Config.table()`). 만료가 지난 행이 있으면 부팅이 죽고(D3), 승격은 노브 삭제를 포함한다(D11). 그래서 GLM 의
"지금 켜진 것" 은 이 문서가 아니라 **그 부팅 로그**가 정본이다.

Qwen3.8 의 레버는 아직 `Knob` 선언이 아니라 CLI 플래그 + 런처 환경변수다(만료일 없음). 이 표가 그 간극을 메우는
임시 자리이고, 레버가 `base/config.Knob` 으로 선언되면 이 절은 GLM 절처럼 짧아진다.
