Experimental canonical onepass is queued; no full-model result yet.

The user explicitly stopped further component and numerical diagnostics in favor of actual onepass prefill tok/s, TTFT, retrieval, Korean output and decode metrics. Normal admission and recovery apply. The previous concentrated-row numerical failure remains unresolved; this run cannot promote defaults.

Main was merged only to satisfy normal deployment ancestry. The MoE kernel, serving contract and canonical onepass files were unchanged. Frozen source and ticket are in receipt.json. Future main advancement must be handled through normal pause/edit/resume while retaining the ticket, not by changing prepared inputs silently or bypassing the deploy guard.
