# EP76 onepass 3 — terminal evidence

Frozen `3fab1ce81a79936b3b76fa4d87b447d9f55bacba`, session `epdecode76onepass0910v3`. Planned B EP-on → A optimized EP-on and optional automatic ABASE EP-on, all DFlash K5/PREP on. The only arm override is EP_DECODE_OPT=1 in A; B/ABASE remain 0. Candidate native low8 uses the separately keyed FC1 register follow-up. CPU/source admission and native mapping evidence remain separately preserved; prior v1 performance is not a v2 result.

Canonical completed names: `['EPDECODE76B', 'EPDECODE76A']`. Valid completed prefix: `True`. Terminal rc: `4`; payload rc: `4`. Original failures, partial completion, required proofs and canonical verdicts remain unchanged. Missing boot files: `['job/pre-cancel-records.jsonl', 'job/cancel-request.json', 'boot/boot-EPDECODE76ABASE.log']`. No absent arm, quality, timing or ready proof is reconstructed.

The descriptive fixed decode formula is `sum(completion_tokens-1)/sum(decode_s)`, only for three completed 1024-token requests. A >=76 target: `False`. This archive grants no performance/default acceptance. Actual prefill, quality, required EP/PREP proof, candidate optimization proof and the canonical comparison status must be assessed from the originals. Original cold_compile presence/value and judge verdicts are preserved. A cold baseline does not become a matched warm prefill comparison. Original `decode.fixed_intervals` and `fixed_pooled_step_s` stay unchanged; whole-onepass speculation counters and PREP checkpoints are not fixed-request acceptance counts. ABASE cannot erase B failures.

`snapshot-validation.json` distinguishes available ready captures from missing/rejected captures. Both arms require the first eligible actual-weight layer on each rank:12 cases,72 candidate+72 stock comparisons; native M4/6/8 key23 for B/ABASE or key24 for A, BF16 in both. M12/16/24/32 key20 FP32 is unchanged. The actual decode_opt boolean and ordered global/optimization tags are checked. FINALIZED42 separately establishes packed-only raw-scale release. Ready PREP fields prove configuration only; execution remains the original canonical `preparation`, `decode_optimization`, `required_proofs` and `proof` evidence. Baseline expects 2/2 EP/PREP proofs; A expects 3/3 EP/PREP/OPT, with fresh successful seven-field PREP optimization checkpoints. No completed checkpoint is inferred from ready.

Canonical JSONL, source, retained head boot/run logs, main observer events and available four-rank ready log prefixes are originals with separate stored/original hashes. This EP76 observer has no completed watcher and no continuous worker tails; final worker counters are not synthesized. Raw Docker Env/Cmd/inspect, raw pending and private failure logs remain private; only explicit identity fields and original hashes are public. Release is not public recovery verification.

Utilities are collection-time tools, separate from measured source. Prepared commands, only after exact binding, terminal and main observer closure: `python3 /tmp/glm53_collect_ep76_completed3.py`, then `python3 /tmp/glm53_archive_ep76_completed3_local.py`. Collection adds no HTTP request, GPU work, deployment or queue action.
