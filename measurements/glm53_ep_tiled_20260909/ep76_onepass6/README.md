# EP76 onepass 6 — terminal evidence

Frozen `c1c1c478145ab332a4e9cdf0850c25c66a60cd5e`, session `epdecode76onepass0910v6`. B is EP4 E72/I2048, A is EP2TP2 E144/I1024; both OPT0, SF6, DFlash K5 and PREP on. The only arm override is `VLLM_GLM53_EP_HYBRID_TP2=1` in A. Optional normal ABASE retains the EP4 baseline. Prior register-max/FC1/FC2 results remain separate measurements.

Canonical completed names: `['EPDECODE76B', 'EPDECODE76A']`. Valid prefix: `True`. Terminal rc `0`, payload rc `0`. Original failures, partial rows, required proofs and canonical verdicts remain unchanged. Missing optional originals: `['job/pre-cancel-records.jsonl', 'job/cancel-request.json', 'boot/boot-EPDECODE76ABASE.log']`. Missing arms or evidence are never reconstructed.

Descriptive fixed decode is `sum(completion_tokens-1)/sum(decode_s)` only for three completed1024-token requests (numerator3069). A >=76: `True`. This archive grants no performance/default acceptance. All quality/Korean/channel diagnostics, request and output hashes, prefill TTFT/tok/s, cold_compile presence/value and canonical verdicts stay in the original records. Repeated2K warm timings and single32K/128K requests retain their distinct semantics; a cold B does not become matched warm prefill. Original engine windows and whole-onepass speculative/PREP counters are not reinterpreted as fixed-request acceptance counts. ABASE cannot erase B failures.

CPU11's exact original result, contracts and read-only artifact admission receipt bind217tests,23mounted,79contractsources and8fresh lowerings: baseline3 plus hybrid5. This is source/compile evidence, not distributed loader numerics or performance. Raw REG/STACK/LOCAL fields remain original; native inherited shared-memory estimates are not actual dynamic allocation, and actual dynamic/total fields remain null. The original source/result SHA and worker output are checked; no historical CPU7/CPU8/CPU9/CPU10 result substitutes for CPU11.

Each available four-rank ready snapshot is revalidated with the captured, hashed snapshot predicate. Baseline schema1/source13 remains intact. Hybrid schema2 requires physical rank/loader identity13, geometryE144/I1024, source14, separate mounted loader helper and all15 exact image runtime source pins. Native global low4/6/8 keys are B23/A26, high12/16/24/32 B20/A23; both decode_opt=False. Twelve cases supply72candidate+72stock comparisons per rank, first eligible actual layer only. FINALIZED42 separately checks packed-only raw-scale release. Rank-local controls do not independently prove the final distributed output sum; canonical hybrid execution/quality proof remains separate.

Ready PREP fields establish configuration only. The original canonical `proof`, `required_proofs`, `preparation`, speculation and all other fields remain unchanged. B expects2/2 EP/PREP and A3/3 EP/PREP/HYBRID under its explicit knob. No new hybrid metadata field or completed worker checkpoint is invented. `snapshot-validation.json` distinguishes missing/failed captures; the observer has no completed watcher or continuous worker tails.

Original JSONL, source, CPU admission receipts, retained head boot/run logs, observer events and available four-rank ready prefixes have stored/original hashes. Raw Docker Env/Cmd/inspect, raw pending and private failure logs stay private; only allowlisted identity fields and original hashes are included. Release is not public recovery verification. Collection utilities are separate from measured source. After terminal and observer closure only: `python3 /tmp/glm53_collect_ep76_completed6.py`, then `python3 /tmp/glm53_archive_ep76_completed6_local.py`. No HTTP/GPU/deployment/queue action is added.
