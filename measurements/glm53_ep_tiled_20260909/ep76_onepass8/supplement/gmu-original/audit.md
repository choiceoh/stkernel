GMU0.60 is inside the past launcher bound; it is not proof of sufficient memory for a fresh boot. The old minimum was94.589225GiB, so usable after the unchanged11GiB boot reserve and10GiB margin was73.589225GiB. Preflight divides by fixed119.69GiB, whereas node4 actually reported121.63GiB: GMU0.60 requests72.978GiB there, leaving only0.611225GiB against that past budget. AnonPages is reported rather than subtracted twice.

Keeping num_gpu_blocks_override1056 preserves the KV allocation instead of reducing it. Existing B has four overrides and2241490 served KV tokens. At identical profiling costs, lower GMU reduces available-KV accounting by~3.94–4.00GiB (head8.8→4.86GiB). The original kv_cache_utils admission ordering was unavailable locally, so whether maximum-length admission accepts that smaller accounting budget remains unverified. Normal init/cache checks must remain enabled.

B head torch allocated66.96052/reserved69.97656GiB excludes non-Torch/communication and does not prove fit into71.814GiB. gpu_worker.py:904 labels available_kv_cache_memory_bytes as Current kv cache memory; the value is not actual override allocation. B head KV allocation changed device free33.56→18.41GiB while the available-KV log said8.8GiB. More concerning, node4 finished warmup at1.48GiB device free, despite host MemAvailable11.583GiB. These are distinct observations, not interchangeable free-capacity estimates.

Conditional retry: fresh B/A both0.60, CG_UTIL_DELTA0, normal preflight/profile, same1056 blocks/2241490 served tokens, all four ready proofs and capacity receipts before measurement. Preserve K5/maxlen1048576/maxseq4/maxbatch8192/MMimage4video0. Do not reuse B8 as a matched baseline, lower safety margins, or touch foreign services. No code/service/test/GPU action was performed.

Code: launchers/memfree-preflight.sh:35–36,176–194; launchers/start-glm53-nvfp4-tp4.sh:587–592,816–820,857–862; overlay/modules/glm53_runtime/kv_zero_worker_utils.py:544–566; gpu_worker.py:574–595,662–666,869–904. Full source/log hashes and exact lines are in audit.json.
