B9/A9 diagnosis, source407271d9 and matched GMU0.60. The original row prefix and each boot log's exact preparation start/end hashes match; preserved bytes and line references are in audit.json.

32K: B12.370252→A11.116860s TTFT (+11.2747%tok/s). Neither exact32K segment contains compiler/JIT messages. Both arms instead report the same six initial2K JIT names and _gumbel_sample_kernel at fixedrep0. No asymmetric logged compile supports a32Kcompile-artifact explanation. Monitor suppression/hook completeness and durations are unavailable, so this is not a no-JIT proof. Each arm has just one32Krequest.

Decode: pooled output76.54038→72.50972tok/s (-5.2661%), fixed-window engine21.53830→21.41215step/s (-0.5857%). All8request hashes match; all8output hashes differ. A rep2 is the clearest separation:66.67989tok/s vsB76.64205 while engine21.62854 vs21.51067step/s. It takes15.34196vs13.34776decode seconds and has323vs285nonempty SSE text chunks. SSEchunks can contain multiple tokens and are not engine steps; these observations support a changed generation/output-progress hypothesis without proving speculative acceptance as its cause.

Exact fixed-request accepted/drafted/per-position counters are absent. `_StepWindows` (bench/bracket.py:105–138) retains iteration count and traffic only; fixed_intervals (window_metrics.py:35–51) holds step/time windows. The retained10second acceptance logs cross request boundaries and cannot reconstruct exact fixed totals. Wholeonepass acc_raw0.51056→0.48586 covers all8requests,6263vs6491generated tokens, and must not be used as fixed acceptance.

The matched native kernel/cache/launch specialization stays the same; Q0flag selection is limited to m33..16384 (moe_dispatch.py:108–116), while glm53_ep_tiled.py:365–388 selects native m<=32. Prefill numerical ordering, sampling/generation path, stream tails or runtime noise remain possible contributors; none is proven. No evidence justifies calling the5.27%drop a native kernel regression, and nothing changes the failed76tok/s outcome. No new experiment or source edit.
