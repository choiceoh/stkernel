# EP PREP onepass 7 — terminal evidence

Frozen `96a8cab45a1eb049298315a77ed881ee74f04f9b`, session `eptiledprep0910v7`. Planned S → B → A and optional automatic ABASE, all DFlash K5. S runs EP with PREP shadow and is preparation/numerics evidence. B/A are the designated fresh TP-on/EP-on comparison; ABASE is supplementary and cannot erase B failures.

A completed at **77.093038 tok/s** (three fixed requests), versus B 76.492475 tok/s. The speed target was met, but both on-mode arms retained `VLLM_GLM53_PREP_FUSED=False` / `preparation.verdict=REJECTED`; the canonical A verdict is **invalid / UNPROVED (1/2)**. A facts18/18 and Korean0/8 passed; B facts18/18 passed but Korean1/8 failed. S shadow proof passed with its last observed head checkpoint `checks_ok=fused_steps=1808`, drift0. These head checkpoints do not establish missing final counters on the other ranks. No default adoption is granted.

Canonical completed names: `['EPTILEDPREP7S', 'EPTILEDPREP7B', 'EPTILEDPREP7A']`. Valid completed prefix: `True`. Terminal rc: `4`; payload rc: `4`. Original failures, partial completion, required proofs and canonical verdicts remain unchanged. Missing boot files: `['job/pre-cancel-records.jsonl', 'job/cancel-request.json', 'boot/boot-EPTILEDPREP7ABASE.log']`. No absent arm, quality, timing, or ready proof is reconstructed.

The descriptive fixed decode formula is `sum(completion_tokens-1)/sum(decode_s)`, only for three completed 1024-token requests. A >=67 target: `True`. This archive grants no performance/default acceptance. Warm prefill, quality, declared required proofs, and canonical comparison status must be assessed together from the originals. S is never substituted for B. Original `decode.fixed_intervals` and `fixed_pooled_step_s` stay unchanged; whole-onepass speculation counters and PREP log checkpoints are not fixed-request acceptance counts.

`snapshot-validation.json` distinguishes available ready captures from missing/rejected captures. EP S/A canary coverage is the first eligible actual-weight layer on each rank: 12 cases, 72 candidate +72 stock comparisons; native M4/6/8 key23 BF16, M12/16/24/32 key20 FP32 with the global-route suffix. FINALIZED42 separately establishes packed-only raw-scale release. Ready PREP fields prove configuration only; actual execution is the original canonical `preparation`/`required_proofs` evidence. CPU evidence is archived separately by the coordinator.

Canonical JSONL, source, retained head boot/run logs, both observers' events, and available four-rank ready/completed log prefixes are originals with separate stored/original hashes. Completed watcher snapshots additionally bind the exact completed canonical prefix and head boot ID; they do not overwrite ready snapshots. Failed/manual-unconfirmed captures remain explicitly unverified. Missing S final worker counters are not reconstructed. There are no continuous worker tails in these observers. Raw Docker Env/Cmd/inspect, raw pending, and private failure logs remain in the private collection; only explicit identity fields and original hashes are public. Observer absence is not proof that an arm never started. Holder release is not public recovery verification.

Utilities are collection-time tools, separate from measured source. Prepared commands (after exact binding and terminal + main observer + completed watcher closure): `python3 /tmp/glm53_collect_ep_prep_completed7.py`, then `python3 /tmp/glm53_archive_ep_prep_completed7_local.py`. Collection adds no HTTP request, GPU work, deployment, or queue action.
