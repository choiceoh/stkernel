# EP PREP onepass 8 — terminal evidence

Frozen `388aabdd79e874808262e15c0b974dc8ccca6f07`, session `eptiledprep0910v8`. Planned B TP-on → A EP-on and optional automatic ABASE TP-on, all DFlash K5/PREP on. The earlier v7 shadow preparation arm is separate historical evidence, not a v8 arm or baseline.

A completed all three fixed1024 requests at **72.627429 tok/s pooled**, meeting the user's absolute67 target. Repetitions were72.33144/81.65081/65.64184tok/s; the criterion is the pooled rate, not the minimum repetition. B measured80.493108tok/s. Both arms passed facts18/18 and Korean0/8; A proof2/2 and B proof1/1 passed, including required PREP execution proof. The latest observed head PREP checkpoints were A1728fused/28checks/drift0 and B1664fused/27checks/drift0. Canonical relative performance remains **incomplete/unresolved**, n=1, A−9.7719%; no statistical improvement/noninferiority claim is made.

B retains `cold_compile=true`; A omits the flag. Consequently the canonical `prefill_ttft` compatibility rule does not consider them a matched full-warm pair. Descriptive A observations are2K repeated warm TTFT0.708334s (3004.23inputtok/s),32K single request10.059632s (3235.21inputtok/s),128K single request39.442527s (3259.40inputtok/s). The corresponding B values are0.838086s,10.672908s,41.167981s. `warm_s` aliases the only sample for combined32K/128K and does not prove repeated warm behavior. B's head log has no observed request-time CuTe compile; its first2K has6 inference JIT warnings, and its128K has202 first-use scale-freeze messages. See `B-prefill-audit/review.json`; no timing adjustment or gate waiver is applied. Absolute-goal adoption is an operator/user decision distinct from the preserved statistical verdict; this collection utility does not grant deployment approval.

Canonical completed names: `['EPTILEDPREP8B', 'EPTILEDPREP8A']`. Valid completed prefix: `True`. Terminal rc: `0`; payload rc: `0`. Original failures, partial completion, required proofs and canonical verdicts remain unchanged. Missing boot files: `['job/pre-cancel-records.jsonl', 'job/cancel-request.json', 'boot/boot-EPTILEDPREP8ABASE.log']`. No absent arm, quality, timing or ready proof is reconstructed.

The descriptive fixed decode formula is `sum(completion_tokens-1)/sum(decode_s)`, only for three completed 1024-token requests. A >=67 target: `True`. This archive grants no performance/default acceptance. Warm prefill, quality, required PREP proof and the canonical comparison status must be assessed from the originals. Original `decode.fixed_intervals` and `fixed_pooled_step_s` stay unchanged; whole-onepass speculation counters and PREP checkpoints are not fixed-request acceptance counts. ABASE cannot erase B failures.

`snapshot-validation.json` distinguishes available ready captures from missing/rejected captures. Candidate canary coverage is the first eligible actual-weight layer on each rank:12 cases,72 candidate+72 stock comparisons; native M4/6/8 key23 BF16, M12/16/24/32 key20 FP32 with global-route suffix. FINALIZED42 separately establishes packed-only raw-scale release. Ready PREP fields prove configuration only; execution remains the original canonical `preparation`/`required_proofs` evidence.

Canonical JSONL, source, retained head boot/run logs, main observer events and available four-rank ready log prefixes are originals with separate stored/original hashes. This v8 observer has no completed watcher and no continuous worker tails; final worker counters are not synthesized. Raw Docker Env/Cmd/inspect, raw pending and private failure logs remain private; only explicit identity fields and original hashes are public. Release is not public recovery verification.

Utilities are collection-time tools, separate from measured source. Prepared commands, only after exact binding, terminal and main observer closure: `python3 /tmp/glm53_collect_ep_prep_completed8.py`, then `python3 /tmp/glm53_archive_ep_prep_completed8_local.py`. Collection adds no HTTP request, GPU work, deployment or queue action.
