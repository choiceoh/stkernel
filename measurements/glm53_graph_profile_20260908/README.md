# Unused graph memory profiling: default remains off

The new guard removes a dry capture whose estimate is discarded by the deployed `VLLM_MEMORY_PROFILER_ESTIMATE_CUDAGRAPHS=0` setting. Model/MM profiling, fixed KV blocks, real graph capture and kernel warmup remain. Its full-boot win was not established, so `VLLM_GLM53_SKIP_UNUSED_GRAPH_PROFILE=0` remains the default.

Four-node fleet session `graphmem0908`, runtime/benchmark commit `b945723bcedde7621773ac78f81e2e6b638323f7`, immutable image `sha256:a3dd4c0f6cbb053097d65d10cd8ff8f6ae0cb9115cf0ff142e1cafe124c09211`. PRIME 493 s regenerated rank/FP8/compile artifacts and is excluded. The balanced warm sequence was BASE1 205 s, FAST1 212 s, FAST2 202 s, BASE2 205 s: baseline mean 205 s, candidate 207 s.

The head's memory-profile phase fell from 36.75 to 33.85 s; subsequent compile/warmup increased from 10.10 to 10.40 s. Their combined time fell by 2.60 s, but variable earlier initialization masked that saving in the total health wall. This is phase evidence, not a claim that serving became ready sooner.

Every boot passed first text/image/video output checks and Korean onepass quality 6/6, corruption 0/4. All timed boots had four rank-cache hits, 976 FP8 hits with no misses/errors and 255 cached W4 packs per rank. The source/image/cache/real-warmup receipts passed the report verifier. FAST2 contained two non-loopback POST completions; its first-request latency and onepass performance must not be treated as an uncontaminated comparison. The other logs contained only the seven benchmark POST completions. Small matched first-request samples do not establish broad serving performance or exact response equivalence.

CPU validation: 71,035 checks, 30 megakernel regressions, 107 fleet regressions; six actual-worker memory tests, five receipt tests; all four injected CPU contract faults detected. Only `Worker.determine_available_memory` differs from the pinned upstream worker AST. Existing load, allocation, compile, capture and serving methods are unchanged.

The last control was healthy with the optimization disabled. Fleet ownership was released at the end of the bracket; subsequent serving state belongs to the next holder. Raw logs remain at `/home/choiceoh/glm53-logs/graph-profile-20260908` and the task's `runs/graph-profile-20260908`. `raw-file-sha256.json` identifies the retained inputs.

Reproduction: run `bench/run_startup_graph_profile.sh` through `REPO=$PWD bash bench/fleet.sh run --gpu SESSION 35 "graph profile B/A/A/B" -- bash bench/run_startup_graph_profile.sh`. Regenerate/verify this report with `python3 measurements/glm53_graph_profile_20260908/graph-report.py RAW_DIRECTORY REPO --verify`.

The two-second 224x224 video fixture is red for one second then blue for one second, 4 fps, H.264/yuv420p; the image is generated by the existing vision probe. No external media is fetched.
