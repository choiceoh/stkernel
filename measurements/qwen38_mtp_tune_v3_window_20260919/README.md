# Qwen3.8 MTP 헤드 튜닝 창 6·6b — v3 자기 분포 데이터(서빙 모델 자신의 답)의 답 구간만으로 원래 헤드에서 5차 학습, held-out 2.433 → 2.710, 라이브 T=1(블록 검증) 80 개 짝 +2.6% ± 2.1% (2026-09-19 21:51 ~ 09-20 00:34:06)

> 그대로 두는 기록이다. 창 4·5([qwen38_mtp_tune_v2_window_20260919](../qwen38_mtp_tune_v2_window_20260919/README.md))의 다음 창이다.
> 창 5 의 결론 — 헤드는 서빙에서 **모델 자신의 답 안에서만** 드래프트하는데 v2 의 Deneb 프리필은 대부분 다른 글(사용자 턴 · 도구 결과 ·
> 다른 모델의 답)이었고, held-out 은 세션이 양쪽에 걸쳐 누수였다 — 를 받아, 운영자 "2번 작업으로 빨리 넘어가는게 나을것 같기도", "a"(Deneb
> 의 질문을 OpenRouter 의 `qwen/qwen3.8-flash` 에 보내도 된다), "사용자 턴 질문 뿐만 아니라 메일분석은?", "진행" 으로 **서빙 모델이 직접
> 답한 글**(v3)을 만들어 **답 구간만**(#1301 `--answer-after`) 원래 헤드에서 학습했고, held-out 은 **세션 단위로 떼어 따로 부팅**해
> (`--eval-boots`) 쟀다. 판정은 프로덕션의 설정인 T=1 + 블록 검증의 라이브 짝 80 개(운영자 "평가 180개가 너무 많아서 … 80개까지만 하자",
> "5번이나 평가해야해? 그냥 같은 t=1만")다. 뒤 실행은 이 파일을 고치지 않고 새 기록을 남긴다.

**트리:** srv2 `~/st-worktrees/q38mtp-w6` = `bf74dbc5`(PR #1301 의 브랜치: `--answer-after` · `--eval-boots` · 탭 상한) + 런처의 로컬 수정 하나
(레인 프로브 곁 부팅, [patch_launcher_probe6.py](patch_launcher_probe6.py), main 에 없다). **5차 학습의 학습기는 #1309 의 것**
(`b2d3a24a` 의 `engine/profiles/qwen38/mtp_tune.py`; 창 안에서는 `mtp_tune_fast.py` 라는 이름으로 네 노드의 `~/st-engine-qwen38` 트리에 끼웠다 —
저장소의 파일과 같아 여기 두지 않는다). 데이터 부팅과 평가 팔은 모두 `ST_TIER_DIR=off`(22:14 의 서랍 크래시, 3 절), 임계 끔, 탭은 데이터
부팅만 켬(상한 140 GiB). **속도 주장이 아니라 기록이다(D17).** 평가 팔은 한 창의 표본이고, 두 팔은 같은 부팅이 아니라 같은 설정의 두 부팅이다.

## 요약

- **5차 헤드(원래 헤드에서 500 스텝, v3 답 구간만)의 held-out(따로 부팅한 262 대화의 답 53,134 위치, teacher-forced greedy):** 토큰/스텝
  **2.433 → 2.710(+11.4%)**, 1단 argmax 일치 0.693 → 0.771, 3단 연쇄 0.291 → 0.386. 창 5 와 달리 누수가 없다
  (세션 단위 분할 + 부팅 단위 평가).
- **라이브 T=1(top-k 20 · top-p 0.95, 블록 검증 `ST_DRAFT_CANDIDATES=20`), C=1, K=3, 80 개 짝(5차 − 원래):** 토큰/스텝 **+2.6% ± 2.1%**,
  tok/s +7.4% ± 3.0%. Deneb 기록 +8.6% ± 1.7%, 직접 쓴 질문 +3.0% ± 1.8%, 메일 +3.9% ± 3.6%, Deneb 대화 −1.3% ± 5.5%(텔레그램 5 개 −17.6% ± 16.7%); 시스템 프롬프트 없는 38 개 +4.7% ± 1.5%. held-out 의 +11.4% 중 라이브에 온 것은 1/4 쯤이다.
- **데이터 v3(2,660 대화 → 학습 2,398 · held-out 262):** Deneb 의 실제 질문 986 개 · Deneb 메일 분석 600 개 · 합성 1,074 개를 **서빙 모델 자신**
  (OpenRouter `qwen/qwen3.8-flash`)이 서빙 설정(greedy 6 : T=1 4)으로 답했다. 문 자신의 템플릿으로 렌더해 원문으로 프리필(프롬프트 토큰
  9,470,068)하고 **답 위치 3,181,391 개**(학습 2,873,117 · 평가 308,274)만 런에 남겼다.
- **학습기 A/B(#1309):** srv2 한 장 15 스텝 5.83 → 3.45 s/step(1.69×), 손실 같음 → 5차 학습은 새 학습기로. 네 노드 500 스텝 4.02 s/step,
  35.5 분.
- **22:14 서랍 크래시(→ PR #1312):** 학습 원문 부팅이 앞 부팅의 NVMe 티어(프리픽스 경계 222 개)를 물려받은 채 여섯 요청이 같은 경계를 동시에
  복원하다 네 랭크 모두 `seq 1 already owns resident resources` 로 죽었다. 창 6 은 held-out 만 든 런으로 학습에 들어갔고, 6b 가 모든 부팅을
  `ST_TIER_DIR=off` 로 다시 했다.
- 프로덕션 다운: **9,214 s**(22:00:32 ~ 00:34:06).

## 절차와 다운타임

| 시각 | 무엇 |
|---|---|
| 21:03 ~ 21:34 | srv4 에서 데이터 v3(1 절): 합성 씨앗 → 대화, Deneb 질문 986 답(300 s), 메일 분석 600 답(생각 켬 · 스킬 넣어 다시), 세션 단위 분할 |
| 21:35 ~ 21:38 | 렌더([render_all.py](render_all.py), 문의 템플릿으로 답을 닫아서), 라이브 평가 80 개([make_prompts80.py](make_prompts80.py)) |
| 21:42:44 | 창 6 첫 시도([window6-first-wait.log](window6-first-wait.log)): 플릿은 `session/q38gptq-0919` 가 쥐고 있었다 — 세션 홀더에게는 묻지 않고 프로덕션이 올라와 조용해지기를 기다림 |
| 21:51:37 | [mtp_window6.sh](mtp_window6.sh) 시작, 대기 |
| 22:00:31 | 프로덕션 조용함 → yield 요청, 22:00:35 세션 임대 `session/q38mtp-tune6-0919`. **다운 시작 22:00:32** |
| 22:00:40 | `data6h`(탭 켬 · 상한 140 GiB · 티어 **켬**): held-out 262 원문, 390 s, 프롬프트 토큰 907,157(프리픽스 캐시 244,992), 오류 0, 부팅 `20260919-130154`. ready 63.6 s |
| 22:08:36 | `data6t`(티어 켬): 앞 부팅이 남긴 티어(대화 262 · 프리픽스 경계 222, 16 GiB 가득)를 물려받아 **ready 345.3 s**, 문이 열리자 여섯 요청이 같은 경계를 복원 → 22:14 네 랭크 `seq 1 already owns resident resources`(3 절). 2,398 중 1 개 됨, 2,397 개 503 |
| 22:15:07 | 창 6 은 멈추지 않았다: 런은 held-out 부팅만 들어 학습 0 · 평가 308,274 위치였는데 검사가 "위치 > 100,000" 이라 통과, 탭을 지우고 22:15:53 학습을 시작했다(빈 학습) |
| 22:16:05 | [take-fast](mtp_window6_take_fast.sh)(운영자 "500스텝 학습도 병렬로 더 빠르게 할수 있나" → "고쳐봐"): 창을 `-9` 로 받아(임대는 그대로) 학습기 A/B 를 하려던 참에 |
| 22:18:08 | [6b](mtp_window6b.sh) 가 받음: 학습 원문 부팅이 죽은 것을 보고 탭을 비우고 두 데이터 부팅을 **모두 `ST_TIER_DIR=off`** 로 다시 |
| 22:18:12 | `data6h2`: 262/262, 360 s, 프롬프트 토큰 907,157(캐시 244,992), 오류 0, **held-out 부팅 `20260919-131927`**, ready 69.0 s |
| 22:25:36 | `data6t2`: 학습 2,398/2,398, **2,850 s**(초당 약 2,000 토큰 — 창에서 가장 긴 구간), 프롬프트 토큰 8,562,911(캐시 2,772,480), 오류 0, ready 66.9 s |
| 23:14:30 | 탭 1,077 샤드 124 GB(부팅 둘) → 런([tune-data6.log](tune-data6.log)): 2,635 런, **답 위치 3,181,391 = 학습 2,873,117 · 평가 308,274**, 답 표시 없는 런 5 개 버림 |
| 23:18:53 | 원본 탭 삭제(free 211 GB), 23:21:55 런을 네 노드에 복사 |
| 23:22 ~ 23:24:49 | 학습기 A/B(srv2 한 장, 15 스텝, [ab-tree.log](ab-tree.log) · [ab-fast.log](ab-fast.log)): 트리 5.83 s/step, #1309 3.45 s/step, step 5 `loss_1` 0.41939 / 0.42136 → 새 학습기 |
| 23:24:50 | **5차 학습**([train_rank6.sh](train_rank6.sh)): 원래 헤드에서 500 스텝, 최대 lr 5e-5(warmup 50 = 런의 1/10), accumulate 2, 96 창마다 평가, 네 랭크 데이터 병렬, 학습 파라미터 88,929,792 |
| 00:00:27 | 학습 끝(네 랭크 exit 0 · 0 · 0 · 0) → export → 00:00:52 네 노드에 `st-qwen38-mtp-tuned5` |
| 00:00:52 | T=1 팔 `orig-t1`(원래 헤드), 80 개 |
| 00:16:28 | T=1 팔 `tuned5-t1`(5차 헤드), 80 개 |
| 00:30:48 | stop + release → 00:34:06 프로덕션 문(glm-5.3) 응답. **프로덕션 다운 9,214 s** |

로그 [window6.log](window6.log). 학습 로그 [tune5-train-rank0.log](tune5-train-rank0.log), export [tune5-export.log](tune5-export.log).
데이터 부팅의 요청 로그 `data6h-` · `data6t-` · `data6h2-` · `data6t2-requests.jsonl`(원문 id 와 오류만), 죽은 부팅의 랭크 0 로그
[data6t-rank0.log](data6t-rank0.log).

## 1. 데이터 v3 — 서빙 모델 자신의 답

세 출처 모두 **서빙 모델 자신**(OpenRouter `qwen/qwen3.8-flash`, 키는 srv4 `~/.deneb/.env` 에서 실행할 때만 읽는다 — [gen2.sh](gen2.sh) ·
[selfgen3.sh](selfgen3.sh))이 서빙 설정으로 답했다: greedy 6 : T=1(top-k 20 · top-p 0.95) 4(id 해시로), 생각은 원래 답이 했던 대로, 답
1,536 토큰(생각이면 두 배). 생각이 max_tokens 에 잘려 본문이 없는 답은 본문 `" "` 로 남겼다(생각 구간이 데이터다). Deneb 의 글과 메일은
**플릿 프리필과 OpenRouter 전송만** 승인된 용도라 저장소에 없다 — 여기 있는 것은 만드는 스크립트와 개수뿐이다.

| 출처 | 개수 | 만든 법 | 답 |
|---|---:|---|---|
| **Deneb 질문** [selfgen3.py](selfgen3.py) | **986** | srv4 `~/.deneb/transcripts` 의 세션(라이브 평가에 쓰인 세션 · 문서는 뺌)마다 답이 있던 사용자 턴 최대 4 개, 앞의 두 교환(도구 호출 · 결과 포함, 16K 자 이하)과 Deneb 의 시스템 프롬프트 · 그 세션이 쓴 도구 스키마를 붙임 → 대화 787(도구 518 · 생각 507); 위키 80 · 파일 69 · 코드 50 문서에 과제 8 종(요약 · 할 일 · 표 · 확인 질문 …) → 기록 199. 마지막 사용자 턴이 같은 것 제거([dedup_prompts.py](dedup_prompts.py)) 1,078 → 986. ko 732 · en 254 | 986 (300 s, [answer.log](answer.log)): greedy 609, 생각 507, **도구 호출로 끝나는 답 438**, 생각 잘림 9 |
| **메일 분석** [mailgen3.py](mailgen3.py) | **600** | srv4 `~/.deneb/mailstore` 의 메일(본문 80 자 이상), **40 통은 라이브 평가용으로 뺌**. 한 통 분석 240 · 답장 90 · JSON 추출 60 · 하루치 분류 120(3~7 통) · 스레드 90(≤ 6 통). **모두 생각 켬**(운영자 "mtp 수용률 상승은 추론이 있어야되는거 아니야?"). 한 통 · 하루치의 절반은 Deneb 의 시스템 프롬프트 + 물러난 이메일 분석 스킬 아래 "이 메일 분석해줘"(운영자 "메일 분석 업무 자체를 api한테 시켜서"), 나머지는 절반 확률로 시스템 프롬프트. 시스템 프롬프트 390, ko 526 · en 74 | 600 ([mail_answer.log](mail_answer.log); 처음 판은 생각 없이 돌다 [restart_mail.sh](restart_mail.sh) 로 다시): greedy 361, **생각이 3,072 토큰에 잘려 본문 없음 274** |
| **합성** [datagen2.py](../qwen38_mtp_tune_v2_window_20260919/datagen2.py) · [full3.sh](full3.sh) | **1,074** | 창 5 의 생성기 그대로, 새 씨앗 1,169(분류표 20 범주 × 언어 × 문체) → 대화 1,119 → 거름(한국어 아님 29 · 루프 6 · 한자 4 · 거의 같음 6) 1,074([v3syn.log](v3syn.log)). 한 턴 778 · 여러 턴 204 · 도구 76 · 긴 문서 16, ko 647 · en 366 · 섞임 61, 생각 273, 시스템 프롬프트 355. 이름은 운영자의 열셋으로([names2.py](../qwen38_mtp_tune_v2_window_20260919/names2.py), `MTP_NAMES`). 원문 12 개는 안 씀 | greedy 746 · T=1 328, 생각 잘림 134 |

**분할**([split_v3.py](split_v3.py)) — Deneb 대화는 **세션 단위**(맥락의 첫 사용자 턴으로 세션을 찾음), 나머지는 질문 단위, 해시의 1/10 을 held-out:
**학습 2,398**(Deneb 885 · 메일 551 · 합성 962; 생각 1,256 · 도구 533 · greedy 1,568; 2,000 만 자, 그중 답 862 만 자 · 생각 600 만 자) ·
**held-out 262**(101 · 49 · 112; 211 만 자, 답 90 만 자). 창 5 의 누수(한 세션의 겹치는 창이 양쪽에)는 이 분할과 부팅 단위 평가로 없앴다.

**렌더와 프리필** — [render_all.py](render_all.py): 문 자신의 렌더러(`serve.template_messages` + `qwen38.boot.chat_renderer`, 체크포인트의
템플릿 · 도구 · 생각)로 **답을 닫아서**(생각 · 본문 · 도구 호출 · `<|im_end|>`) 원문을 만든다 — 마지막 메시지를 이어 쓰는 방식이면 본문 뒤의
도구 호출(986 답 중 438 개)이 빠진다. [prefill_text.py](prefill_text.py) 가 `/v1/completions`(max_tokens 1, 동시 6)로 프리필하되 **여는 글이
같은 순서**로 보내 시스템 프롬프트 · 도구를 공유하는 원문은 프리픽스 캐시가 한 번만 계산한다(캐시 적중 학습 2,772,480 · held-out 244,992 토큰).
탭은 계산한 위치를 모두 적고, `mtp_tune data --answer-after 248045,74455,198`(마지막 `<|im_start|>assistant\n` 뒤)이 답만 남긴다:
프롬프트 토큰 9,470,068 중 **답 위치 3,181,391(34%)**. 평가는 `--eval-boots 20260919-131927`(held-out 부팅 통째).

**라이브 평가 80 개**([make_prompts80.py](make_prompts80.py), 운영자 "80개까지만"): 창 5 의 직접 쓴 질문에서 **25**(범주 20 개 전부, 여러 턴 4;
일이 필요한 범주 — 수학 · 추론 · 코딩 · SQL · 재무 · 세무 — 는 생각 켬 14), **Deneb 대화 30**(창 5 와 같은 것, 생각 22), **Deneb 기록 14**(위키 6 · 파일 5 ·
코드 3), **메일 11**(뺀 40 통 위의 같은 다섯 모양, 모두 생각). 생각 켬 47/80(Deneb 의 실제 트래픽은 답한 턴의 63% 가 추론), ko 62 · en 18, 시스템
프롬프트 42, max_tokens 512 × 71 · 1,024 × 6 · 256 × 3. 질문 파일은 Deneb 의 글이라 저장소에 없다(직접 쓴 25 는 창 5 의
[crafted_eval.jsonl](../qwen38_mtp_tune_v2_window_20260919/crafted_eval.jsonl) 에 있다).

## 2. 5차 학습 — held-out 토큰/스텝(teacher-forced greedy, held-out 부팅의 답 96 창 = 53,134 위치)

원래 헤드에서 시작(`--init` 없음), 500 스텝, 최대 lr 5e-5, warmup 50, accumulate 2, 네 랭크 데이터 병렬, #1309 의 학습기.
스텝당 4.02 s(rank 0 `train` 줄의 중앙값; 창 5 의 옛 학습기 6.28 s — 데이터가 달라 같은 판은 아니다, PR #1309 의 댓글).

| 스텝 | 0 | 100 | 200 | 300 | 400 | 500 |
|---|---:|---:|---:|---:|---:|---:|
| 토큰/스텝 | 2.433 | 2.635 | 2.671 | 2.694 | 2.705 | **2.710** |
| 1단 argmax 일치 | 0.693 | 0.756 | 0.765 | 0.767 | 0.770 | 0.771 |
| 3단 연쇄 | 0.291 | 0.355 | 0.369 | 0.379 | 0.384 | 0.386 |
| `loss_1` | 0.898 | 0.521 | 0.488 | 0.470 | 0.462 | 0.459 |

가장 좋은 평가의 헤드가 저장된다(500 스텝, 2.710). 창 5 의 held-out 이 v2 부팅마다 2.7~3.2 였던 것과 달리 v3 held-out 의
원래 헤드는 **2.433** 이다 — 생각 켬 47% · 도구 호출 · 메일 원문 위의 답이라 헤드가 원래 약한 글이다.

## 3. 22:14 의 서랍 크래시 — 같은 프리픽스 경계를 두 줄이 동시에 복원 (→ PR #1312)

`data6t` 는 같은 세션의 앞 부팅(`data6h`)이 남긴 NVMe 티어를 물려받았다(주인이 같으면 티어를 비우지 않는다): 대화 262 개 41.3 GiB, 프리픽스
경계 222 개 16.0 GiB(가득). 부팅부터 ready 까지 345 s(티어 없이 64~69 s). 문이 열리고 동시 여섯 요청이 Deneb 시스템 프롬프트의 같은 경계를
티어 후보로 들고 와 **둘 다** `restore_begin` 했다: 둘째의 `restore_finish` 가 `PrefixCache.insert` 에서 "이미 있음" 으로 실패했는데 읽어 온
블록을 쥔 채 `_free_rows` 로 돌아갔고, 다음 요청이 그 줄을 받아 `Runner.submit` 에서 `ValueError: seq 1 already owns resident resources`
(`phase='settling transfers'`) — 네 랭크 같은 자리([data6t-rank0.log](data6t-rank0.log)). 2,397 요청이 503. #1312 가 같은 경계를 읽는 중이면
기다리고(`dedup_waits`), 다 읽었는데 이미 있으면 메모리의 것을 쓰고, 실패한 줄은 언제나 비운다 — CPU 테스트만, 플릿 미검증. 이 창의 뒤
부팅은 모두 `ST_TIER_DIR=off`.

**창 6 스크립트의 빈틈**: 학습 원문 부팅이 죽어도 런 검사가 "위치 > 100,000" 이라 held-out 만 든 런(학습 0)으로 학습에 들어갔다. 6b 는
"학습 > 500,000 · 평가 > 50,000 · 원문 2,000 개 이상 프리필" 을 본다.

## 4. 라이브 T=1 짝 — 원래 헤드 대 5차 헤드, 블록 검증, 80 개

두 팔 모두 `ST_TIER_DIR=off ST_DRAFT_THRESHOLD=off ST_TAP_MTP_INPUTS=0 ST_DRAFT_CANDIDATES=20`, `ST_SPEC_K=3`, T=1 top-k 20 top-p 0.95
([mtp_requests2.py](mtp_requests2.py) `eval … 1.0,20,0.95`), C=1, 질문 하나씩. 토큰/스텝 = (완성 토큰 − 1) / 디코드 스텝(프리필 스텝의 첫 토큰을
뺀다, PR #1275). 요청 로그 [orig-t1-requests.jsonl](orig-t1-requests.jsonl) · [tuned5-t1-requests.jsonl](tuned5-t1-requests.jsonl) 에서 Deneb ·
메일 질문(`d-` · `r-` · `m-`)의 답 앞머리는 뺐다.

| 팔 | 헤드 | 답한 수 | 스텝 | 디코드 토큰 | ms/스텝 | 토큰/스텝 | tok/s |
|---|---|---:|---:|---:|---:|---:|---:|
| `orig-t1` | 원래 | 80 | 14,720 | 34,071 | 45.46 | 2.315 | 50.9 |
| `tuned5-t1` | 5차 | 80 | 14,234 | 34,254 | 43.46 | **2.406** | 55.4 |

프롬프트별 짝(5차 − 원래, 평균 ± 표준오차, [arms6.py](arms6.py)):

| 묶음 | n | 5차 − 원래 토큰/스텝 | tok/s |
|---|---:|---|---|
| **전체** | 80 | **+0.064 ± 0.052 (+2.6% ± 2.1%)** | +7.4% ± 3.0% |
| Deneb 기록(위키 · 파일 · 코드) | 14 | **+8.6% ± 1.7%** | +11.2% ± 6.6% |
| 직접 쓴 질문 | 25 | +3.0% ± 1.8% | +3.5% ± 4.0% |
| 메일 분석 | 11 | +3.9% ± 3.6% | +23.3% ± 6.1% |
| Deneb 대화 | 30 | −1.3% ± 5.5% (client 14: −4.8% ± 7.7%, cron 6: +7.5% ± 8.9%, telegram 5: −17.6% ± 16.7%) | +3.8% ± 6.2% |
| 생각 켬 / 끔 | 47 / 33 | +3.1% ± 2.7% / +2.0% ± 3.4% | +9.1% ± 3.2% / +5.5% ± 5.4% |
| 시스템 프롬프트 있음 / 없음 | 42 / 38 | +0.5% ± 4.0% / **+4.7% ± 1.5%** | +7.1% ± 4.7% / +7.7% ± 3.9% |
| 한국어 / 영어 | 62 / 18 | +2.3% ± 2.3% / +3.9% ± 5.2% | +10.3% ± 3.4% / −1.6% ± 6.3% |
| 여러 턴 | 3 | +10.6% ± 5.5% | +20.6% ± 4.9% |

- **held-out 의 +11.4% 중 라이브로 온 것은 1/4 쯤(+2.6%)이고, 2σ 안에 0 을 품는다.** 창 5 와 다른 점은 방향이다 — 음수인 묶음은 Deneb 대화(그중 텔레그램
  5 개)뿐이고, 그 묶음의 흔들림(± 5.5%)이 전체의 표준오차를 키운다. Deneb 기록 · 시스템 프롬프트 없는 질문 · 여러 턴은 2σ 밖의 양수다.
- **tok/s 의 +7.4% 에는 부팅 차이가 섞여 있다**: ms/스텝 45.46 → 43.46(−4.4%)은 헤드가 아니라 두 부팅의 차이일 수 있다(C=1, 같은 커널). 헤드의 몫은
  토큰/스텝으로만 읽는다.
- 두 팔은 T=1 이라 같은 텍스트가 0/80 — 짝은 같은 질문 위의 다른 텍스트 비교다. 생각 켠 47 개는 512~1,024 상한에 대부분 잘려(원래 팔 `length` 23/80)
  드래프트는 주로 추론 글 위에서 쟀다.
- 원래 팔의 토큰/스텝 2.315 는 창 5 의 T=1 블록 팔(4차 헤드, 114 개) 2.620 보다 낮다 — 질문 묶음이 다르다(생각 47/80, 메일 · 시스템 프롬프트 42/80).

## 5. 판정

- **5차 헤드는 원래 헤드보다 나쁘지 않고 조금 낫다** — 전체 +2.6% ± 2.1%(토큰/스텝), Deneb 기록 +8.6% ± 1.7%, 시스템 프롬프트 없는 질문 +4.7% ± 1.5%.
  창 5 의 "held-out 만 오르고 라이브는 그대로" 는 이번 데이터(답 구간만 · 세션 단위 held-out)에서 방향은 고쳐졌지만 크기는 held-out 의 1/4 이다.
- **D11(기본값):** 운영자 "켜"(09-20 00:45) — 5차 헤드가 Qwen 런처의 기본이다(`launchers/start-st-qwen38.sh` 의 `ST_MTP_TUNED` 기본 `/home/choiceoh/models/st-qwen38-mtp-tuned5`, `ST_MTP_TUNED=off` 가 롤백; `engine/SERVING_DEFAULTS.md` 의 줄).
- **다음에 더 얻으려면:** 라이브에서 가장 약한 자리가 Deneb 대화(시스템 프롬프트 + 도구 + 생각)다. 그 자리의 데이터는 Deneb 질문 986 개 중 787 개뿐이고
  답의 44% 가 도구 호출로 끝난다 — 답이 짧다. 메일 답 274/600 이 잘린 것과 함께, 다음 창은 **답 상한을 올려(4K~8K) 긴 생각을 통째로 먹이는 것**이 먼저다.
  프리필이 창의 절반(2,850 s)이니 데이터를 늘리면 창이 길어진다.

## 6. 창에서 나온 것

1. **NVMe 티어를 물려받은 부팅이 같은 경계의 동시 복원에 죽는다** → PR #1312(CPU 테스트만). 티어를 켠 부팅은 ready 도 345 s(64~69 s 대비).
2. **창 스크립트가 빈 런으로 학습에 들어갔다** — 런 검사는 학습 · 평가 · 프리필 수를 따로 본다(6b).
3. **프리필이 창의 절반이다** — 2,398 원문 8.56M 토큰에 2,850 s(초당 약 2,000 토큰, 캐시 적중 32%). 데이터를 늘리려면 여기가 병목이다.
4. **메일 분석 답의 46%(274/600)가 생각이 3,072 토큰에 잘렸다** — 생각 구간은 데이터로 남지만 본문 없는 답이다. 다음엔 상한을 올리거나 잘린 것을 뺀다.
5. **#1309 의 학습기**: 한 장 1.69×, 네 노드 4.02 s/step(옛 6.28, 다른 데이터).
6. 세션 홀더(`session/q38gptq-0919`)에게는 묻지 않고 프로덕션이 돌아와 조용해질 때까지 기다렸다(21:42 → 22:00).
7. **부팅 로그에 튜닝 헤드를 실었다는 줄이 없다.** `tuned5-t1` 의 근거는 컨테이너 인자 `--mtp-tuned /home/choiceoh/models/st-qwen38-mtp-tuned5` 와 마운트, 그리고
   `qwen38/fleet.py` 가 그 파일을 랭크 파일 대신 읽는 코드뿐이다(무장 ≠ 서빙, 원장 규칙 5). 부팅이 "drafter: MTP head … tuned <dir>" 한 줄을 남겨야 한다.
8. **생각을 켠 질문은 512 상한에 대부분 잘린다**(원래 팔 `length` 23/80). 토큰/스텝은 재지지만 답은 완성되지 않는다 — 답 품질을 볼 평가가 아니다.
