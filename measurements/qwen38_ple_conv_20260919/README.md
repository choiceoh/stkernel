# Qwen3.8 PLE conv 와 더하기를 한 발사로 — 4,096 행 39.5 → 3.07 ms (2026-09-19)

srv4 단일 GPU 레인, 프로덕션 `st-qwen38` 옆, `--lanes qwen38_ple_conv`(`probes/engine_qwen38_ple.py`). 합성 입력(normed·gated BF16 [T, 10,240],
fp32 conv 가중치 [10,240, 4], dilation 3, 이전 탭 held). 그래프 하나에 8 번, 두 팔을 번갈아 21 라운드. **커널 컴포넌트 시간이다(D17).**

| 행 | torch conv + 더하기(오늘) 최소 / 중앙 (µs) | `ngram_gate.conv_add` 최소 / 중앙 | 최대 오차 / 다른 원소 비율 |
|---:|---:|---:|---:|
| 16 | 54.1 / 54.6 | 22.9 / 23.6 | 0 / 0 |
| 512 | 3,623.8 / 5,464.1 | 303.3 / 609.6 | 0.0012 / 5e-6 |
| 4,096 | 39,531.5 / 45,309.1 | 3,072.9 / 4,295.5 | 0.0020 / 3e-6 |

- 차이는 conv 의 fp32 합 순서(커널 자신의 순서) — 밴드(2^-6) 안.
- 먼저 잰 센서스(`q38pfconv-0919a`·`0919b`)는 큐 밖 학습·프로덕션 경합으로 무효(기준선 960 → 1,632 ms). 그 판들의 PLE 몫(74.3 → 29.7 ms, 41.8 → 23.8)은
  방향만 같다.
- PLE 층은 청크마다 하나(설정의 `ple_layer_ids`). 플릿 onepass 는 재지 않았다.
