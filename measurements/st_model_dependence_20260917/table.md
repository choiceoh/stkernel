# PR별 분류 — ST 최적화의 모델 의존도 (2026-09-17)

`summarize.py --table` 이 `classification.jsonl` 에서 만든다. 손으로 고치지 않는다.
상태: default 기본 경로 · optin 기본 꺼짐/실험 · rejected 기각·되돌림. hw: GB10/SM121/TP4 전용 여부. 근거: e2e 종단 실측 · component 커널/프로브·발사 수 · none 수치 없음.

## U — 모델 무관 (74)

| PR | 요지 | 상태 | 특징 | hw | 근거 | 이유 |
|---|---|---|---|---|---|---|
| #1033 | Loader load() default read width 1 GiB -> 512 MiB MAX_RUN, plus load stage stamps | default | boot_load_compile | no | component | Restores the measured-faster 512 MiB loader read width, cutting pinned staging by 862 MiB; generic loader mechanism. |
| #1032 | Split boot rows; import kernel packages in background under the native-build rendezvous | default | boot_load_compile | no | none | Moves kernel package imports onto a background thread during the native-build rendezvous to shorten boot; generic boot mechanism. |
| #1023 | Door host half (grammar, tokenizer) runs in a thread under load; prefill gate stamps | default | boot_load_compile | no | none | Runs grammar qualification and door tokenizer on a host thread under load, about -7 s boot estimated; generic boot parallelization. |
| #1000 | xgrammar reuses the door's tokenizer; boot skips building a transformers tokenizer | default | boot_load_compile | no | component | Boot skips building a transformers tokenizer for xgrammar, saving seconds in qualify grammar; generic boot path. |
| #996 | Door warmup runs decode widths only, dropping six redundant prefill warmups | default | boot_load_compile | no | none | Drops about 4.6 s of redundant prefill warmups from boot; generic boot warmup mechanism. |
| #995 | Boot builds all native extensions in parallel before the first collective | default | boot_load_compile | no | component | Fixes cold-boot collective timeouts and parallelizes native builds (CPU: 372.9 s serial to 65.7 s); build orchestration is model-agnostic. |
| #989 | Dense W4/FP8 lanes share one weight and calibration digest; boot hashes 7 GiB once | default | boot_load_compile | no | component | Removes duplicate boot hashing of weights and Hessians (CPU A/B 25.9 s to 13.9 s); pack-cache identity logic is model-agnostic. |
| #987 | Same-build restart reuses its recorded far prefill memory-gate pass | default | boot_load_compile | no | none | Skips the 30-47 s far prefill gate pass on same-build restarts via an engine/base record; saving estimated, mechanism model-free. |
| #984 | Four native extensions build under persistent /cache instead of container $HOME | default | boot_load_compile | no | component | Stops ~195 s of per-boot recompiles (container test: 217 s compile vs 0.20 s load); compile caching is model-agnostic. |
| #981 | b12x CuTe-DSL key sets get separate on-disk modules; boots stop wiping MoE kernels | default | boot_load_compile (+F) | no | none | Compile-cache fix avoiding ~53 s/rank MoE recompiles per boot; caching mechanism generic though the affected kernels are b12x MoE. |
| #787 | Per-operator Torch headers reduce oneshot native kernel cold builds | default | boot_load_compile | no | component | Narrower per-operator Torch headers cut NVCC cold compile of the oneshot kernel; build-time mechanism independent of model architecture. |
| #784 | Trim Torch includes to reduce oneshot native cold compilation | default | boot_load_compile | no | component | Minimal Torch includes lower cold NVCC cost of the native oneshot kernel; compile-time mechanism, model-agnostic. |
| #781 | Stable native CUDA build inputs avoid rebuilding identical sources across checkouts | default | boot_load_compile | no | component | Checkout-independent native build inputs let the cache skip identical NVCC rebuilds, speeding boots; model-agnostic compile caching. |
| #655 | Loader staging buffers capped at 512 MiB: -862 MiB pinned, faster reads | default | boot_load_compile | no | component | Loader staging capped at 512 MiB runs: 862 MiB less pinned memory and faster O_DIRECT reads; model-agnostic loader read width. |
| #584 | Boot: warm kernels before the door, freeze gc, capture largest first, warm once | default | boot_load_compile | no | none | Pre-door kernel warmup removes first-request JIT, largest-first capture shares the pool, single warm pass; model-free capture mechanics. |
| #578 | Stop emptying the caching allocator before every CUDA graph capture | default | boot_load_compile | no | none | Boot capture: 51 allocator flushes become 5, avoiding per-shape unmap/remap; generic CUDA-graph capture mechanics, size unmeasured. |
| #576 | Capture ledger writes one four-node barrier row per shape instead of three | default | boot_load_compile | no | component | Removes ~3.4 s of boot capture barriers (21.9 ms each) and rebuilds segment tuples once; model-independent capture mechanics. |
| #573 | Per-shape warmup policy for graph capture; name last boot phase; retire two levers | default | boot_load_compile | no | component | Warmup passes 72->40 (~8 s boot) via a declared per-shape policy; generic capture mechanics, estimated from boot ledger phases. |
| #559 | Boot study: one sampler graph per logits buffer, parallel fleet launcher, ledger clock | default | boot_load_compile | no | component | Sampler graphs 72->8 (115->51 captures) and parallel node startup cut boot time; model-agnostic capture and launch mechanics. |
| #557 | Load weights with O_DIRECT and preshard through the same coalesced ranges | default | boot_load_compile | no | component | Loader 0.7-0.9 -> 1.9-2.1 GiB/s with no page-cache pressure on the arena; model-agnostic weight loader. |
| #957 | One-shot RDMA queues and completion tracking bounded to the four-slot ring | default | comm | yes | none | Shrinks requested RDMA WR/CQ capacity and host completion storage to the protocol bound; transport-level, gain unmeasured. |
| #944 | One-shot collectives use both RoCE rails; proxy polling and ACK publication trimmed | default | comm | yes | none | Splits peer writes across Spark's two PCIe RoCE functions and cuts proxy work; model-agnostic transport, gain only estimated. |
| #779 | Batch calibration Gram updates; fuse pointwise lanes, router prep, draft KV writes | default | dense_gemm (+F,S) | no | component | Self-calibration Gram observer 15.4->0.67 ms/step dominates (generic dense mechanism); router, indexer and drafter fusions are F, norm/SwiGLU lanes S. |
| #983 | Host draft position is a runtime scalar: no Triton compile per position on decode steps | default | drafter | no | component | Removes per-position JIT compiles on user steps (2,629 variants in ten minutes); Triton specialization fix, argument-shaped. |
| #935 | Fused rank-local token embedding lookup kernel for target and drafter | default | embed_logits | no | component | One Triton kernel replaces seven tensor ops for vocab-parallel embedding; argument-shaped and model-agnostic. |
| #849 | Remove repeated vocabulary key staging and dense candidate clears in draft selection | default | embed_logits (+F) | no | component | Drops vocab-sized key pack and full candidate clears in shape-generic vocab candidate kernel; component timings, C=4 warm 344->225 us. |
| #600 | Stop writing the vocabulary twice a step to hand it to the sampler | default | embed_logits | no | none | Removes a redundant fp32 upcast of gathered logits (about 14.5 MB per step); model-free logits handling. |
| #959 | Decode metadata uploads packed into single copies; burst staging buffers reused | default | graphs_host | no | component | Fewer H2D copies and temporaries for replay metadata, sampling policy and burst reservations; host staging is model-agnostic. |
| #912 | Burst readback copies into contiguous pinned views; C=1 launches stop blocking | default | graphs_host | no | e2e | Removes host-blocking non-contiguous pinned readback behind the C=1 step regression (19.6->17.4 step/s); model-agnostic host staging; e2e rows diagnosed. |
| #827 | Remove obsolete burst result-copy kernels after shared decode queue publication | default | graphs_host | no | none | Removes five result-log index_copy_ launches and buffers on bounded decode queue path; model-agnostic runtime; CPU gate only. |
| #822 | Default-on: direct mHC, prefill project tiles, mapped NVMe staging, bounded decode x4 | default | graphs_host (+S,F) | yes | none | Turns four GB10 serving optimizations on; bounded decode and mapped staging model-agnostic, direct mHC family, tiles comm; component gates only. |
| #817 | Connect mapped NVMe staging and bounded GB10 decode iterations to serving | default | graphs_host | yes | none | Bounded multi-iteration decode graphs and one mapped NVMe staging allocation, model-agnostic runtime; turned default-on by #822. |
| #601 | Host path round 2: pinned decode ids, no-copy history view, numpy validation | default | graphs_host | no | component | Pinned ids make the captured-decode copy async, history read without copy, validation 3.8->1.3 ms; model-free runtime. |
| #598 | Host path: eight per-step and per-request costs removed | default | graphs_host | no | none | Removes pageable H2D stream waits, repeated chain hashing, whole-history slices and record reads; model-free host runtime. |
| #585 | Block-level prefix reuse and decode steps run ahead of the host (async readback) | default | graphs_host (+F) | no | none | Async decode pipeline (device-side sample/commit, pinned event readback) and finer prefix reuse are generic runtime; KDA recurrence cut at marks is family plumbing. |
| #580 | Build decode-path index constants once instead of 220 times per step | default | graphs_host (+F) | no | component | Generic capture-safe constant cache (base/constants.py) removes 220 aranges per step and 88 MiB writes; applied at DSA indexer/MLA sites. |
| #545 | Connect GLM decode to CUDA graphs (target, DFlash2, sampling); FP32 MoE scatter fix | default | graphs_host | no | none | Wiring decode, drafter and sampling into CUDA graph replay is a generic speed mechanism; the FP32 scatter part is a numerics fix. |
| #687 | Parked-conversation digests and LRU of records free 1.04 GiB host RAM | default | memory | no | component | Continuation scan cached full token records of 280 parked conversations; digests plus LRU free ~1.04 GiB host RAM; model-agnostic runtime. |
| #685 | Tokenize only the conversation tail, splice onto cached prefix tokens | default | other | no | component | Splices tail tokenization onto cached conversation prefix, 61 -> 0.8 ms per agent turn; host-side, independent of model architecture. |
| #606 | Door detokenizer: Rust DecodeStream with repairs, tail-only suffix and stop scans | default | other | no | component | Tail-only scans and Rust stream flatten door host CPU (58.7->11.7 ms per 16K answer); repairs are robustness; model-free. |
| #588 | Door: hold back partial stop strings, linear detokenization, event-driven drain | default | other | no | component | Windowed detokenization (2.87->0.035 s per 4K tokens) and event wake replacing 20 ms polling, plus a stop-string fix; model-free. |
| #1046 | Shared prefix boundaries spill to the disk tier; supervisor warms the cache after boot | default | prefix_kv_cache | no | none | Improves prefix reuse across restarts: hit inner boundaries go to the disk tier and the cache is warmed after boot; model-agnostic prefix machinery. |
| #1044 | Prefix cache: spill when idle, flush boundaries on handover, keep 96 snapshots with tier | default | prefix_kv_cache | no | none | Raises prefix snapshot capacity and tier fill via idle spills, handover flush and a 4.25 GiB budget; generic prefix snapshot machinery. |
| #873 | Probe stops resetting prefix cache; non-retained turns' boundaries stay transient | default | prefix_kv_cache | no | none | Preserves production prefix-cache reuse by evicting retain-false boundaries first; model-agnostic cache policy; unmeasured. |
| #858 | Release turns nobody will continue instead of parking them to the NVMe tier | default | prefix_kv_cache | no | none | Health-check pings no longer evict real conversations from the NVMe tier; model-agnostic serve policy; PARK_MIN_TOKENS 128 in production. |
| #783 | Halve raw prefix snapshots behind a bounded lossless compressed RAM snapshot cache | default | prefix_kv_cache | no | none | Raw snapshot pool 4.25->2.125 GiB with a 1 GiB zlib RAM cache sparing NVMe reads; generic prefix-cache and tier machinery. |
| #619 | Prefix cache: sub-block prompts cache their generated boundaries; invariant checks | default | prefix_kv_cache | no | none | Short first-turn prompts now cache answer boundaries so the next turn reuses rather than re-prefills; model-free prefix machinery. |
| #609 | Prefix cache: blocks keep their name, graded free list, tenant salt | default | prefix_kv_cache | no | component | Freed blocks keep hashes in an O(1) graded free list; one block costs one boundary, fading snapshots; model-free cache machinery. |
| #596 | Prefix cache review fixes: O(1) leaves, budget-checked tier restores | default | prefix_kv_cache | no | none | O(1) leaf counts remove a per-step scan costing a decode step of host time; remaining parts are fixes; model-free. |
| #594 | Prefix cache: NVMe prefix tier, in-flight dedup, warm/pin, metrics | default | prefix_kv_cache | no | none | NVMe spill/restore of boundaries and dedup of concurrent identical prefills raise prefix reuse; model-free cache machinery. |
| #590 | Prefix cache: 768-token blocks, 96 protected snapshots, generated boundaries | default | prefix_kv_cache (+F) | no | none | Finer blocks, 4x snapshots, generated-boundary capture and continuation raise reuse; device staging copies KDA state (family-specific). |
| #553 | Prefix reuse via chunk-boundary snapshots; cancellation, min_tokens, tools, supervisor | default | prefix_kv_cache (+F) | no | none | Prefix cache skips re-prefill (substantial part); generic snapshot machinery with KDA/drafter-ring checkpoint contents; door features are non-optimization. |
| #552 | GLM budget table, async park/resume with rank agreement, bounded indexer selection | default | prefix_kv_cache (+F) | no | none | Park/resume move off the step thread (ITL protection) and indexer selection transient drops 1.69->0.13 GiB; budget table is bookkeeping. |
| #550 | Expandable-segment arena with reclaim gate; parked conversations free rows and slots | default | prefix_kv_cache | no | none | Parking frees 247 MiB state slots and rows, retaining conversations beyond max_seqs; arena/reclaim is model-agnostic memory admission. |
| #540 | Publish only changed KV block mappings; bind fused indexer quant and pool expansion | default | prefix_kv_cache (+F) | no | component | Incremental block-table publication cuts host prep 79-99% (generic KV metadata); fused indexer lanes 33-47% faster (DSA family). |
| #537 | Real caches: multi-region NVMe tier, conversations outliving turns, continuation | default | prefix_kv_cache | no | none | D16 NVMe tier lets returning conversations resume from disk instead of re-prefill; generic tier machinery; remainder is bring-up. |
| #1001 | Requests with sampling options stay on async decode via device-side option handling | default | sampler | no | component | Option-bearing requests no longer flush the async decode chain; device-side penalties, bias and logprobs are generic sampler mechanics. |
| #932 | Fuse draft input preparation and keyed SplitMix64 step draws into single kernels | default | sampler (+F) | no | component | Keyed draw block (53 ops -> 1 launch) in model-free base.draws dominates; DFlash anchor/position prep fusion is drafter-specific; CPU work inventory only. |
| #717 | Block-verify launch reshaped to BLOCK 8192 / 16 warps (+13% at four rows) | default | sampler | yes | component | Generic block-verification kernel launch shape retuned on SM121 (+13% at four rows, exact); model-agnostic verifier. |
| #700 | Sampler computes threshold-search weights once, not per round (507 -> 436 us) | default | sampler | no | component | Sampler kernel computes top-p weights once instead of per round, 507 -> 436 us at production shape; model-agnostic sampling. |
| #696 | Block verify kernel also writes the committed token block (5.8x) | default | sampler | no | component | Verification kernel writes committed token block itself, 2393 -> 414 us, device ops 180 -> 23; generic speculative block verification. |
| #694 | Block verification in one Triton launch: 5.3x at production shape | default | sampler | no | component | Draft block verification fused into one Triton launch, 5.3x at production shape, identical tokens; model-agnostic speculative verification. |
| #681 | Draft distribution passed as 16 candidates instead of a vocab-wide row | default | sampler (+F) | no | none | Verifier takes 16 draft candidates instead of [n,K,vocab] rows, removing ~12 MiB allocs/copies per step; generic verify, assumes top-k drafts. |
| #613 | Sampler kernel: search for the nucleus threshold instead of sorting, whole batch at once | default | sampler | no | component | Bit-pattern threshold search in one batched launch (4.4->0.47 ms), one host crossing; argument-shaped kernel, model-free. |
| #608 | Structured output: one pinned step bitmask, xgrammar apply kernel, dead drafts skipped | default | sampler | no | component | Grammar masking: four transfers to one, 44 ops to 4, host walk 0.266->0.055 ms, dead drafts not gathered; model-free. |
| #604 | Verify a block of drafts at once: about a fifth more accepted, same distribution | default | sampler | no | component | Block verification raises accepted drafts about 22% in host simulation with identical output distribution; generic verification rule, model-free. |
| #597 | No per-step answer copies or per-position host crossings; min_tokens as forbidden ids | default | sampler | no | component | Step loop stops copying answers, sampler syncs once per step, min_tokens avoids vocab-wide masks; model-free sampler and runtime. |
| #595 | Grammar bitmask crosses to the device once a step from kept buffers | default | sampler | no | none | Per-position pageable synchronizing copies become one pinned transfer per step with reused buffers; model-free. |
| #593 | Keep each row's token history incrementally for penalties; four metric gaps filled | default | sampler | no | component | Device-side incremental penalty history: 456->0.13 ms per decode step at 128K prompt; metrics are secondary; model-free. |
| #549 | TP4 greedy argmax candidates, direct KDA state writes, prefill fairness, memory ceiling | default | sampler (+F) | no | none | Greedy TP selection and prefill/decode fairness are generic; direct recurrent state writes are KDA family; memory ceiling is bookkeeping. |
| #539 | Remove redundant greedy sampling and decode adapter host work | default | sampler | no | component | Greedy argmax path ~89% faster, one decode-input upload, O(1) generation counts; model-agnostic sampler and adapter work. |
| #809 | GB10 execution-order experiments: TP overlap, early DFlash observe, layer-major prefill | optin | scheduler_runtime (+S,F) | yes | none | Experimental execution orders, still default off on main; mostly generic runtime mechanisms tuned for GB10; early observe is drafter-specific. |
| #671 | Keep decode batches ahead during request churn (async join, commit/vocab kernels) | default | scheduler_runtime (+S,F) | no | none | Async decode keeps surviving rows during churn with fused commit/greedy-candidate kernels and direct all-gather; mostly model-agnostic runtime, speed unmeasured. |
| #599 | Copy each row's block ids as a block, not one id at a time | default | scheduler_runtime | no | component | Memoryview row copies keep the step block-table build flat (195.5->1.7 us at 1M tokens); model-free step metadata. |

## S — 일반 연산, GLM 셀에서 튜닝 (26)

| PR | 요지 | 상태 | 특징 | hw | 근거 | 이유 |
|---|---|---|---|---|---|---|
| #967 | C=2 16-row all-reduce sums take the one-shot PDL consumer by default | default | comm | yes | component | Consumer bound raised to 16x4096 BF16 rows (GLM C=2 cell); 9-10% less GPU-side time per sum; RDMA/PDL-specific. |
| #856 | Ragged SP prefill transport, wider FP8 transport, tile32 MLA and SF6 Q0 word prefill | default | comm (+F) | yes | e2e | Beyond the ragged SP fix, widens FP8 transport, tile32 MLA and SF6 Q0 prefill paths; prefill collectives dominant; phase-2 onepass measured. |
| #826 | Direct W4 GEMM into TP4 send slots, shared decode queue, FP8 packets in KDA prefill | default | comm (+U) | yes | none | Producers write into TP4 send slots and prefill GEMM consumes FP8 packets; GB10 transport; decode queue is U; correctness gates only. |
| #813 | Overlap TP4 prefill tile arrival with KDA input projection (prefill_project_tiles) | default | comm (+F) | yes | none | Prefill tile arrival overlaps KDA input projection on TP4 GB10; prefill collectives mechanism, KDA consumer; no speed claim; default-on via #822. |
| #1066 | Remove MX producer replay work and widen cuBLASLt algorithm search (comparison lane) | optin | dense_gemm | yes | none | Removes GPU work and widens cuBLASLt search for the FP8/MXFP8 dense comparison lane; generic GEMM tuned at GLM shapes, not served, unmeasured. |
| #1063 | Bind and tune exact-scale cuBLASLt MXFP8 pipelines as an explicit comparison lane | optin | dense_gemm | yes | none | Allocation-free prepared cuBLASLt MXFP8 execution with bounded algorithm search; generic dense GEMM at GLM shapes, comparison lane not selected, unmeasured. |
| #1050 | Profile owns K=7; eight-row dense/MLA fast-path gates admitted by default, unmeasured | default | dense_gemm (+G) | yes | none | Opens eight-row K=7 dense GEMM input mode and compact paths plus GLM megakernel MLA shapes by default, unmeasured; dense compact part rolled back in #1054. |
| #976 | Drafter block MLP runs 16-row W4 CTA cells at C=2 | default | dense_gemm (+F) | yes | component | Dense MLP GEMM cells compiled at GLM shapes and 16 rows for DFlash2 blocks; gate/up -1.7%, down -0.3% chain microbench. |
| #975 | C=2 gate/up, MLP down and qkv_a run 16-row W4 CTAs by default | default | dense_gemm | yes | component | Dense GEMM CTA specializations at GLM shapes and 16 rows; chain-evicted -1.7/-8.2/-4.4% microbench on SM121. |
| #973 | Drafter block MLP binds the target's 8-row C1 W4 input cells | default | dense_gemm (+F) | yes | component | Drafter MLP reuses GLM-shaped 8-row dense CTA cells; gate_up -2.7%, down -62% on a warm three-layer chain. |
| #969 | C=2 KDA input and KDA/MLA output projections run 16-row W4 CTAs by default | default | dense_gemm | yes | component | Dense W4 GEMM CTA cells compiled at GLM shapes for 16 rows; -6 to -10% chain microbench; SM121 CTA tiling. |
| #958 | Tree W4A8 executor shares input quantization and keeps SM121 GEMMs on native FP8 | optin | dense_gemm | yes | component | Experimental tree W4A8 MLP at H4096/I3072: one input quantization, fewer registers and shared bytes; SM121 FP8 lowering fix. |
| #946 | C1 W4 K-block products parallelized across warps; DSA query projections share one grid | default | dense_gemm | yes | component | Dense W4 GEMM parallelism in GLM 8-row CTA cells: gate/up -60.5%, query pair -26.2% warm microbench on SM121. |
| #939 | C1 forward W4 GEMM partial-sum reductions kept inside each CTA block | default | dense_gemm | yes | component | Removes 44 MiB of global partial-sum traffic in KDA/MLP/query GEMMs at GLM 8-row cells; -4 to -11.6% component on SM121. |
| #910 | Enable K=7 W4 decode projection fastpaths bound at captured widths 8/16/24/32 | default | dense_gemm (+F) | yes | none | W4 dense projection input-reuse/reduced-CTA fastpaths at GLM's K=7 row cells, incl. KDA/indexer owners; operator-enabled; no gain measured. |
| #888 | Qualify K=7 eight-row W4 input reuse and projection fastpaths (probe mode 2) | default | dense_gemm (+F) | yes | none | Probe-gated M8 W4 input pack/CTA mode at GLM K=7 cells; mode 2 is now the kernels.cu default; nothing measured. |
| #686 | Dense prefill all FP8: drop NVFP4 dense prefill lane (wash, 3.65x error) | default | dense_gemm | no | component | Drops NVFP4 dense prefill lane for FP8: speed a wash on GLM's dense shapes, 3.65x less error, 0.98 GiB/rank not built. |
| #673 | Calibration peaks-only blobs cost [K]; W4/FP8 GPTQ lanes share one factorisation | default | dense_gemm (+U) | no | component | Calibration collects peaks-only [K] blobs so all 228 GPTQ tiles fit one boot, shared factorisation cuts pack time 10-29%; dense low-bit lanes. |
| #669 | Calibration channel smoothing folded into preceding norms (NVFP4 large-M lane) | default | dense_gemm (+F) | no | component | Calibration-derived channel smoothing folded into norms to cut NVFP4 large-M lane error 23%; low-bit dense quantization aid, that lane later dropped (#686). |
| #661 | GPTQ on the fp8 grid for the FP8 prefill lane and vocabulary head | default | dense_gemm | no | component | GPTQ on the fp8 grid for the FP8 prefill lane and lm_head, weight error 0.027 -> 0.0009; generic dense low-bit lane packs. |
| #659 | Wider GPTQ: act-order static groups, full-K wide weights, NVFP4 lane from W4 packs | default | dense_gemm (+F) | no | component | Widens GPTQ packs for low-bit dense lanes: act-order default, full-K drafter fc pack, NVFP4 prefill from W4 packs; generic GEMM quantization. |
| #650 | Dense W4 packs self-calibrate by default: boot sums Hessians for next GPTQ packs | default | dense_gemm (+U) | no | component | Boot self-collects GPTQ Hessians by default so drafter/target dense W4 packs get calibrated; generic calibration machinery for dense low-bit lanes. |
| #591 | Native TP4 adoption: W4A8 dense, one-shot AR, prefill SP, GPTQ drafter, prefill fixes | default | dense_gemm (+F,U) | yes | e2e | Performance-parity port: native GB10 W4A8/FP8/NVFP4 dense, RoCE one-shot AR, FP8 prefill SP pinned at GLM cell; plus MoE/mHC/drafter lanes, admission. |
| #936 | Combine DFlash draft Q and K norm+RoPE into one launch per layer | default | drafter (+F) | no | component | Generic QK norm+RoPE launch fusion (10->5 per proposal) via model-free lane, wired only into GLM's DFlash drafter; source counts only. |
| #716 | glm53 default decode width MAX_SEQS 8 -> 4: graph pool 2.5 -> 0.6 GiB, boot -47 s | default | memory | no | e2e | Lowers default decode width, freeing 2.86 GiB graph pool/state slots and 47 s boot; generic row-count knob chosen from GLM boot ledgers. |
| #734 | Prefill chunk grows from three to four blocks (6,912 to 9,216 tokens) | default | scheduler_runtime | no | component | Prefill chunk size chosen at GLM's expert-lane knee (-39%/token) and workspace budget; generic chunking re-tuned at GLM's cell. |

## F — 아키텍처 계열 전용 (109)

| PR | 요지 | 상태 | 특징 | hw | 근거 | 이유 |
|---|---|---|---|---|---|---|
| #602 | Preshard builds NVFP4 expert scales as a stack (224.6 s -> 39.4 s) | default | boot_load_compile | no | component | Vectorized NVFP4 expert scale folding speeds offline presharding 5.7x; applies only to NVFP4 MoE expert checkpoints. |
| #560 | b12x direct-micro MoE kernel moved into the flashinfer on-disk JIT cache | default | boot_load_compile (+U) | no | none | Avoids per-process recompilation of the direct-micro kernel; generic compile caching, but only benefits b12x NVFP4 MoE models. |
| #1004 | Drafter vocab candidate selection reductions run in one warp instead of four | default | drafter (+U) | no | component | One-warp reductions speed drafter vocab candidate selection 32-36% in component timing; draft-selection path, kernel itself model-free. |
| #951 | Tree verification batches KDA/DSA work and removes W4A8 queue synchronization | optin | drafter (+S) | no | component | Experimental tree: 8 vs 36 KDA state updates, batched DSA calls, spill-free W4A8 CTAs; CPU verify 41-57% faster. |
| #947 | Experimental cost-aware tree verification with packed W4A8/NVFP4 persistent dataflow | optin | drafter (+U,S) | no | none | Eager tree verify over KDA factors plus packed MLP dataflow for tokens-per-cost and memory; allocation formulas only, no serving default. |
| #929 | Read DFlash draft V in place and write target FP8 head logits into owned buffers | default | drafter (+U) | no | component | Removes five DFlash attention V materializations plus one target logits copy (generic logits buffer); source launch/byte counts only. |
| #900 | Default DFlash selector FP32 output and automatic fitted FC bias correction | default | drafter | no | none | Drafter precision/correction defaults aimed at acceptance; FC correction applies only with a fitted artifact; no acceptance or speed measured. |
| #894 | Explicit DFlash selector FP32 projection and fitted decode FC bias correction | default | drafter | no | none | Reduces drafter numeric error to raise acceptance; opt-in at merge, defaulted by #900; no fitted vector shipped, nothing measured. |
| #879 | Optional draft tuning: selector alpha, request boundaries, smoothing/GPTQ damping | optin | drafter | no | none | DFlash draft selection and pack tuning controls aimed at acceptance; production pins the empty profile; no acceptance measured. |
| #871 | DFlash serving defaults fp8/auto calibration/diagnostics; fix signed RMSNorm fold | default | drafter | no | none | Defaults DFlash FC to FP8 with auto committed-decode GPTQ calibration for acceptance, plus smoothing fix; no measured verdict. |
| #863 | Combine DFlash FC FP8 decode with committed-decode GPTQ calibration | default | drafter | no | none | Drafter FC FP8 pack GPTQ-calibrated on committed decode rows for acceptance; skips unused W4 pack; now serving policy fp8/auto. |
| #862 | Isolate DFlash acceptance experiments (FC FP8, decode GPTQ) and record rejection causes | default | drafter | no | none | Adds FC FP8 and committed-decode GPTQ arms targeting DFlash acceptance plus rejection diagnostics; arms later became serving policy. |
| #777 | Remove unused drafter FP8 packs and compact resident drafter weights | default | drafter | no | none | Retires raw drafter sources and unneeded FP8 packs into a compact declared region, cutting resident memory; DFlash drafter storage. |
| #756 | Split drafter observe into compute and write phases so compute can overlap | rejected | drafter | no | none | Byte-identical split preparing drafter observe to overlap the target tail; overlap never wired, path replaced by #779's fused write. |
| #736 | Drafter attention rows become a grid dimension; residual join and norm fused | default | drafter (+U) | no | component | Drafter attention row loop folded into the grid (1.52x at n=4) and residual join+norm fused; DFlash drafter kernels. |
| #735 | Fold drafter fc to one launch; fuse selector, local vocab top-k and SwiGLU | default | drafter (+S,U) | no | component | Drafter fc, selector scores/walk, local vocab top-k and SwiGLU folded (-217 us/step); DFlash drafter and draft selection dominate. |
| #729 | Drafter attention reads each KV ring once per KV head instead of per query head | default | drafter | no | component | Drafter attention scans each KV ring once per KV head: 9.3x per layer, drafter step 14.97->6.12 ms in replay; drafter kernel. |
| #725 | Fused drafter norm+rope with cached inverse-frequency table; tap conv in one launch | default | drafter (+U) | no | component | Fused norm+rope and DFlash tap convolution cut drafter dispatches 4,315->485 per step; drafter-specific kernels. |
| #722 | Drafter ring KV writes batched to one launch per layer | default | drafter | no | component | Batched drafter ring writes: 12 launches and 67.8 us become 3 and 12.2 us in observe; DFlash drafter KV ring. |
| #644 | Drafter W4 lane GPTQ calibration sums from serving traffic (STK_drafter_calib) | optin | drafter (+G) | no | component | Hessian sums let PackStore build GPTQ instead of RTN drafter W4 packs (low-bit lane quality); knob-gated, collected on GLM drafter traffic. |
| #627 | Drafter proposes and observes all rows at once, no ring copies (n=4: 101->19 ms) | default | drafter | no | component | Batched DFlash drafter GEMMs/SDPA over all rows with in-place observation; proposal 101->19 ms at n=4; drafter-family mechanism. |
| #605 | Sampled draft walk crosses to host once; acceptance ceiling split into metrics | default | drafter (+U) | no | none | Drafter candidate walk's ten host syncs per row become one; acceptance-ceiling gauges are instrumentation; helps only drafter-based decoding. |
| #563 | DFlash2 exchanges packed top-k vocab candidates instead of a full vocab gather | default | drafter (+U) | no | component | Drafter TP payload -99.8%, selection+gather ~22% faster at 5 positions; draft-selection family, though the vocab-parallel top-k module is generic. |
| #1010 | DSA decode selection in one kernel: horizon mask + radix top-k (3.40x sm_120 component) | default | indexer | no | component | Fuses DSA indexer horizon mask and top-k into one launch, 3.40x component; only sparse-attention indexer models benefit. |
| #971 | C=2 DSA indexer selection joins rows into one logits, mask and top-k launch | default | indexer | no | component | Halves indexer selection launches (0.54-0.66x captured time at 2K-128K); applies only to sparse-attention indexers using DeepGEMM logits. |
| #961 | Tree verification reads indexer key bank and KDA conv ring directly, without staging | optin | indexer | no | component | Experimental tree path drops DSA key-bank and KDA conv-history staging copies; CPU-only evidence, no production serving path. |
| #926 | Eliminate decode kpool input staging and top-k id int32 copies (22 launches/forward) | default | indexer | no | component | Direct kpool compression reader and int64 top-k id finalization in DSA indexer decode; indexer-family specific; source launch counts only. |
| #921 | Read decode kpool tail rings directly and fuse pool-cache writes (55->22 launches) | default | indexer | no | component | DSA indexer pool-cache glue fusion under the default decode_dsa_inputs bundle; indexer-specific; source launch counts only. |
| #907 | Skip indexer work for covered prefill queries and redundant query-packet fills | default | indexer | no | component | Bypasses DSA indexer projection/scoring/top-k for fully covered prefix queries inside the default dense-prefix lane; work counts only. |
| #881 | Partition replicated indexer prefill queries across ranks and skip covered pools | optin | indexer | no | component | DSA indexer prefill query sharding with 16-bit pool-ID transport; prefill_indexer_shards stays default off; work counts only. |
| #821 | Fold last two-launch decode pieces (head gate, pool/tail writes, horizon) to one each | default | indexer (+U) | no | component | Byte-identical launch folds, mostly DSA indexer glue plus drafter walk and positions iota; about 45 launches per step counted. |
| #815 | Row fold #2: indexer selection and pool/tail writes folded over decode rows | default | indexer | no | component | Byte-identical fold of indexer selection and pool/tail writes, about 790 fewer launches at four rows; DSA indexer family. |
| #693 | Indexer pool selection unsorted (set, not order): -20% on chunk shape | default | indexer | no | component | DSA indexer pool selection asks unsorted top-k, -20% on chunk shape, same set; applies to sparse-indexer attention family. |
| #582 | Pool every indexer segment in one pass instead of one pass per segment | default | indexer | no | component | Batches kpool compression across segments: ~726 fewer dispatches per pass, 44->11 kpool launches, fewer graph nodes; DSA indexer-specific. |
| #556 | Tune GB10 launch tiles for indexer Hadamard-128 FP8 query quantization | default | indexer | yes | component | Tile geometry chosen for GB10's 48 SM: quantizer 36-69% faster, indexer ~1.7% at decode; DSA indexer kernel. |
| #547 | One-warp key pooling (kpool compress) without scratch buffers or input copies | default | indexer | no | component | 3->1 kernels, lane ~51% faster, L3 indexer ~4.5%; DSA kpool kernel, decode gain mostly preparation removal rather than warp layout. |
| #546 | Fuse indexer pool sort and KV slot address generation into one kernel | default | indexer | no | component | 5 kernels + 2 copies -> 1 kernel, L3 indexer 7-8% faster; DSA indexer slot finalization. |
| #543 | Fuse indexer slot mapping and count writes into one Triton kernel | default | indexer | no | component | 17 kernels + 5 copies -> 4 + 2, L3 indexer 14-15% faster; DSA indexer slot finalizer. |
| #1008 | Bounded, fused KDA prefix-boundary staging launch that skips non-crossing steps | default | kda_linear (+U) | no | component | One bounded launch skips non-crossing KDA prefix staging steps, -95% component; linear-attention recurrent state, prefix machinery secondary. |
| #978 | KDA output norm also writes the 16-row o_proj CTA wide input pack (C=2) | default | kda_linear (+S) | yes | component | Fuses o_proj input pack into KDA's output norm, skipping a PDL launch (-0.40% chain); needs KDA head dim matching K blocks. |
| #968 | KDA output norm writes o_proj's C1 input pack; bound cell skips its pack launch | default | kda_linear (+S) | yes | none | Producer-side pack fusion removes one PDL launch per KDA o_proj; relies on KDA head dim 128; GPU timing not run. |
| #937 | Deferred FP32 KDA state with one-launch accepted-state commit enabled by default | default | kda_linear | no | component | Default flip for batched accepted-only KDA state commit (-35% C=1 / -46% C=4 component at K=6); linear-attention specific. |
| #869 | Amortize tiled deferred KDA state commits and set draft K to seven | default | kda_linear | yes | component | KDA recurrent-state commit tiling on SM121 (deferred_kda later default via #937) plus K=7; K=6 GPU component timings only. |
| #865 | Tile deferred KDA commits over contiguous state and advance cursors without modulo | default | kda_linear | yes | component | Tiled deferred KDA state commit cuts SM121 load instructions 36->29; KDA-family recurrent state; deferred_kda later default-on. |
| #857 | Batch accepted FP32 KDA state commits across decode rows and layers | default | kda_linear | no | component | One coalesced accepted-only KDA state commit; component -35% C=1, -46% C=4; KDA-family; deferred_kda later default-on. |
| #825 | Fuse KDA/indexer decode projection pairs and indexer boundary; reuse wide-row W4 inputs | default | kda_linear (+S) | no | component | Paired KDA low-rank and indexer projections plus fused boundary on default decode path; component gains; wide-row W4 input reuse is S. |
| #810 | C=4/C=1 decomposition; router TC at all widths; decode row fold #1 (KDA/conv rings) | default | kda_linear | no | component | Row fold of KDA recurrent/conv rings and latent writes (one-rank four-row step 76.3->73.0 ms) plus MoE router TC widening. |
| #791 | Default KDA recurrent state storage to FP16 | rejected | kda_linear | no | none | FP16 KDA recurrent-state storage halves ring and snapshot bytes (1.5 GiB/rank); state precision is KDA-family; reverted to FP32 by #799. |
| #788 | Add FP16 KDA state storage option at fixed cache capacity | optin | kda_linear | no | none | Adds FP16 KDA recurrent-state storage (1,496 MiB/rank declared saving) as a non-production override; KDA state precision is family-specific. |
| #751 | Probe kernels: deferred KDA state writes, FP8 prefill consumer, mHC terminal consumer | optin | kda_linear (+S) | no | none | Unconnected probe kernels cutting KDA state write traffic, FP8 prefill unpack+quantize and mHC contraction bytes; no GPU result, not serving. |
| #615 | KDA chunk kernel emits fp32 states at prefix marks; only nucleus rows are sorted | default | kda_linear (+U) | no | component | Triton KDA chunk side-output replaces cutting recurrence at marks (+104% per layer avoided); nucleus-only sort is a generic sampler part. |
| #572 | Fuse KDA short conv with conv-history ring updates into one kernel | default | kda_linear | no | component | 3->1 kernels and 72 KiB less scratch per decode/verify conv; linear-attention short-conv family, gain from fusion not tiling. |
| #571 | KDA recurrence reads and writes rollback rings directly (no gather/snapshot copies) | default | kda_linear | no | component | Removes state gather and snapshot copies (3->1 kernels, 7 MiB scratch), ~6% L0 KDA block at 6 tokens; linear-attention state rings. |
| #569 | Read recurrent KDA Q/K/V/beta through their strides, removing four input copies | default | kda_linear | no | component | Removes contiguous input copies (5->1 kernels, ~19% recurrent call at 6 tokens, ~2% L0 block); KDA/GDN recurrent kernel family. |
| #566 | Specialized single-sequence causal conv kernel for KDA (9 kernels -> 1) | default | kda_linear | no | component | Drops batch metadata and state tables (9->1 kernels), 1-3% L0 KDA block; short conv of linear-attention layers, gain mostly launch removal. |
| #564 | Fuse KDA output gated RMS norm and sigmoid gate into one Triton kernel | default | kda_linear | no | component | 12->1 kernels, 188 MiB less prefill scratch, ~4.4% L0 KDA block; gated output norm specific to linear-attention layers. |
| #561 | Remove KDA recurrent state transpose copies via direct [H,K,V] state addressing | default | kda_linear | no | component | Eliminates two transpose copies (7 MiB per call), L0 KDA block 12-17% faster; KDA recurrent state layout, generic copy removal. |
| #1009 | Static mHC tail ownership for C=1 eight-row decode, accepted on serving evidence | default | mhc | yes | e2e | Static tail ownership removes per-launch atomic updates in the C=1 mHC consumer, 8% component, adopted; hyper-connection family, GB10 CTA/PDL specific. |
| #972 | C=2 mHC packet and all-reduce consumers read the BF16 coefficient pack | default | mhc | yes | component | mHC consumers read the lossless BF16 pack up to 16 rows: 9-24% faster than FP32 coefficients; SM121 packet kernels. |
| #860 | Write terminal mHC drafter features directly into final columns in decode and prefill | optin | mhc | no | component | Terminal mHC kernel writes drafter features into final columns, removing casts/means/concat; GPU component timings; terminal_mhc still default off. |
| #812 | Direct TP4 packet consumption inside native mHC (direct_mhc receiver) | default | mhc (+S) | yes | none | Native mHC consumes TP4 rank packets instead of a materialized all-reduce; needs mHC; no speed claim; default-on via #822. |
| #689 | Turn PDL on for mHC prefill-lane TileLang kernels | default | mhc | yes | component | Turns PDL launch overlap on for mHC TileLang prefill kernels, 4-5% faster full chunks, identical outputs; mHC family, SM121-specific. |
| #634 | Adopt GB10 TMA loads for mHC post and ldmatrix loads for megakernel MLA | default | mhc | yes | none | SM121 TMA tiles for mHC post and matrix loads for sparse-MLA kernels; feature-family kernels adopted without benchmark results. |
| #956 | MLA restores half-bridge E4M3 widening; #952's native instruction slowed 128K prefill | default | mla_dsa | yes | e2e | Reverts an SM121 native widening in the MLA kernel: 129,784-token TTFT 54.7/50.9 s back to 38.5/37.3 s. |
| #954 | Tree verification reads paged/private MLA KV directly, eliminating FP8 KV staging | optin | mla_dsa | no | component | Experimental tree path removes per-DSA-layer FP8 KV staging copies; CPU A/B shows no speedup, shape counts only. |
| #952 | CUDA 13.2.1 migration with SF6 direct reads and native MLA E4M3 widening | rejected | mla_dsa | yes | component | SM121 instruction micro-opts (static counts only); MLA native widening reverted by #956 after 128K prefill regressed, SF6 part kept. |
| #933 | Reuse captured decode sequence lengths and pool counts across the 11 DSA layers | default | mla_dsa | no | component | Computes DSA length/pool-count metadata once per target forward instead of per DSA layer (11->1 calls); sparse-attention-specific; source call counts only. |
| #928 | Remove decode MLA output concat copies and duplicate page-table allocation | default | mla_dsa (+U) | no | component | Drops 11 MLA output copies per target forward (MLA-specific) and a page-table allocation in generic graph caches; source counts only. |
| #919 | Default-on DSA inputs, indexer gate and MLA decode absorb tiles; drop MLA layout copies | default | mla_dsa | no | component | Enables DSA decode fusions by default and removes 22 MLA contraction layout copies per forward; MLA/DSA-specific; source counts only. |
| #914 | Fuse K=7 DSA query input pack, KV norm/FP8 latent write, and FP32 indexer head gate | default | mla_dsa | no | component | DSA/MLA and indexer decode launch fusions (22-44 fewer per forward); default-off at merge, enabled by #919; source counts only. |
| #899 | Token-major MLA prefill contractions, enabled by operator request | default | mla_dsa | no | component | Removes two MLA contraction layout copies per DSA layer in long prefill (64 GiB traffic at 128K); MLA-specific; byte counts only. |
| #889 | Enable dense-prefix MLA prefill by default (operator request) | default | mla_dsa | no | component | Default-on switch for #887's dense-prefix MLA kernel; DSA/MLA-specific; supporting evidence is work counts, no GPU timing. |
| #887 | Dense-prefix MLA kernel sharing covered-prefix KV tiles; drop prefill output copies | default | mla_dsa | no | component | MLA/DSA prefill: dense attention over fully covered prefix plus output copy removal; enabled by #889; work counts only. |
| #819 | DSA layer decode glue as five Triton kernels (latent write, pool window, gathers) | default | mla_dsa | no | component | Byte-identical fold of DSA decode glue into five kernels, about 528 fewer launches per step; DSA-family only. |
| #737 | Raise MLA prefill32 T bound to 16,384 so 9,216-token chunks keep the fast kernel | default | mla_dsa | yes | component | MLA sparse-attention prefill kernel gate follows the chunk law, removing a +19% fallback at 9,216; MLA-family kernel tiled for SM121. |
| #698 | Remove union MLA prefill candidates pair/pair4: lose to tile32 at every overlap | rejected | mla_dsa | no | component | Sweep showed union-read MLA prefill candidates pair/pair4 slower than tile32 at every overlap; removed. Sparse MLA family optimization, rejected. |
| #658 | Promote qualified tile32 MLA large-M prefill to production default | default | mla_dsa | yes | e2e | Makes tiled tile32 large-M MLA prefill the production default after matched 2K/32K/128K brackets; MLA family, tile tuned on SM121. |
| #641 | MLA megakernel: Q copy joins first KV async group, balanced cluster split reduction | default | mla_dsa | yes | none | SM121 cp.async overlap and CTA head balancing in sparse-MLA decode kernel at GLM dims; adopted as source default before GPU timing. |
| #554 | MLA sparse decode: DSMEM split reduction and warp max reduction on GB10 | default | mla_dsa | yes | component | Cluster plus DSMEM partial merging makes the MLA kernel 10-32% faster at 32-64 rows; MLA family using SM121 hardware features. |
| #1070 | m=16 ksr rule already optimal; MoE bulk cell z storage orders all fail, both closed | rejected | moe (+S) | yes | component | Extends the rejected MoE bulk-B-stage streaming cell z with kernel storage-order variants, then closes it; b12x NVFP4 MoE family, dense ksr sweep secondary. |
| #1067 | Bulk L2 prefetch cells for MoE streaming and dense v2 GEMM, all rejected on srv4 lane | rejected | moe (+S) | yes | component | Probe cells prefetching expert and dense GEMM bytes into L2 to speed streaming; measured slower or neutral and rejected. MoE b12x dominant, dense secondary. |
| #1058 | m=16 companion lane keeps private direct scatter; guard narrowed to what scatter writes | optin | moe | yes | none | Narrowed guard lets the non-sf6 companion lane use faster direct register scatter instead of staged output; b12x MoE, hybrid arm only, compile-only. |
| #1015 | M64 prefill MoE lane selectable on serving path; qualification breaks past four tiles | rejected | moe | yes | component | Plumbs the M64 prefill MoE tile lane into the serving entry, default off; qualification fails past four tiles. Rejected NVFP4 MoE candidate. |
| #1007 | Default C1 MoE staged scatter reads packed BF16x8 output spans with one shared load | default | moe | yes | component | Packed shared loads cut instructions and registers in the default C1 MoE scatter; b12x NVFP4 MoE kernel, no material latency gain. |
| #1006 | Serve tile-major w13 in 256 chunks: C=2 evicted -1.6%, C=1 -1.0%, prefill -1.9~-2.4% | default | moe | yes | component | Expert w13 layout makes TMA boxes contiguous for faster streaming in lane cells, served by default; b12x NVFP4 MoE-specific. |
| #1002 | Vectorize staged FP32 MoE scatter reductions: two v4 REDs replace four v2 | default | moe | yes | none | Halves FP32 RED reductions in the default C1 MoE staged scatter; b12x NVFP4 MoE kernel, compile-only evidence. |
| #974 | C=2 decode MoE adopts the M16 batch reform in the production recipe | default | moe | yes | component | b12x NVFP4 expert tile M32 to M16 for 16-row C=2 steps; fixtures -2 to -3.5%; SM121 tile geometry. |
| #970 | MoE sync cleanup extended to the C2 M16 batch tile | default | moe | yes | component | Removes 16 static sync/barrier instructions in the b12x NVFP4 expert tile; work counts only; SM121 CTA-specific. |
| #962 | C2 MoE FC2 prefetches a third tile using unused output storage | default | moe | yes | unknown | Recycles direct scatter's unused sC storage as a third FC2 pipeline slot in the b12x M16 tile; default in batch recipe. |
| #960 | Native CUDA 13.2 add.u8x4 byte adds for SF6 word restore and W4A8 LUT expansion | default | moe (+S) | yes | none | SM121 native packed-byte instruction replaces software carry isolation in b12x SF6 and dense/MLA W4A8 paths; compile evidence only. |
| #955 | C2 M16 MoE tile with direct register scatter and retained scatter state | default | moe | yes | component | b12x NVFP4 expert tile at 16 rows with direct scatter: FFN -1.7 to -13% single-GPU; became default via #974. |
| #943 | MoE scale-register follow-up: per-stage metadata reuse, dead expanded-scale rings removed | default | moe | yes | component | b12x SF6 expert kernel: -105 static instructions, -8 KiB shared memory, -70% base reads; compile counts only, SM121-specific. |
| #940 | C1 MoE reconstructs SF6 scale operands in register quads, skipping expanded stores | default | moe | yes | component | Removes 48 scale-publication barriers and 96 KiB of stores per expert work item in the b12x NVFP4 kernel; static counts only. |
| #925 | Remove redundant C1 MoE initialization and synchronization in the SF6 expert kernel | default | moe | yes | component | Drops the unused A ring and duplicate CTA syncs/barriers in the b12x NVFP4 kernel; static work counts only. |
| #923 | Compact C1 MoE SF6 staging: reclaim up's A/SFA allocation for separate FC2 scales | default | moe | yes | component | b12x NVFP4 MoE decode staging layout removing 16 SM121 barrier rendezvous per work item; layout/code-gen counts only, no GPU timing. |
| #920 | Reuse C1 MoE FC1 A/SFA input registers across gate and up, halving FC1 input DMA | default | moe | yes | component | b12x MoE FC1 input transfer reuse in the SM121 DMA/register pipeline (32 fewer transfers per item); exact work counts and native compile only. |
| #918 | Reconstruct C1 MoE FC2 SF6 scales in four byte lanes | default | moe | yes | component | b12x SF6 scale reconstruction using SM121 byte lanes, default for M1-8 decode; NVFP4 MoE-specific; static instruction counts only. |
| #916 | Store C1 quantized MoE activation blocks as single 64-bit shared writes | default | moe | yes | component | b12x MoE quantizer outputs stored as aligned 64-bit SM121 shared writes instead of eight byte stores; native instruction counts only. |
| #915 | Reconstruct C1 MoE FC1 SF6 scales four byte lanes at a time | default | moe | yes | component | b12x SF6 scale expansion via SM121 32-bit byte lanes, default for M1-8 decode; NVFP4 MoE-specific; static instruction counts only. |
| #913 | Separate C1 MoE FC1 SF6 scale staging and trim scatter caches (fewer barriers) | default | moe | yes | component | Removes 32 SM121 128-thread barriers per b12x MoE work item and 224 scatter-cache stores; compile and CPU evidence only. |
| #906 | Enable fused MoE output finalization into OneShot decode packets by default | default | moe (+S) | yes | component | Writes routed FP32 accumulator plus shared expert straight into one-shot RoCE TX slots (78 launches/forward); MoE-specific; source counts only. |
| #904 | Implement and qualify fused MoE decode output into OneShot packets (default-off) | default | moe (+S) | yes | component | Fused MoE output finalizer and one-shot TX packing implementation; enabled by #906; MoE/RoCE-specific; source launch/byte counts only. |
| #895 | Prefill routes tokens on the sender and consumes TP4 FP8 packets in the MoE FFN | default | moe (+S) | yes | component | b12x MoE prefill reads FP8 all-gather packets with sender-owned routes: FFN -8.5% at 32K on one GB10; gated to E288/top-8. |
| #876 | Opt-in MoE prefill Q0 batch8/M64/N128, fused sum+FP8 packing, prefill mHC fusion | optin | moe (+S) | yes | component | b12x MoE prefill kernels plus mHC fusion and MoE-sum/FP8 comm packing, all explicit opt-in; CPU compile and byte counts only. |
| #818 | Repair MoE scatter candidates' rounding and dispatch; record their GPU rejection | rejected | moe | yes | component | MoE output scatter candidates repaired and GPU-timed: route-owned rejected, direct scatter unselected (kernel later reused for C=2 batch). |
| #816 | Probe-only MoE output scatter and paired projection decode candidates with qualification | optin | moe | yes | component | Probe-gated MoE scatter and KDA/indexer pair candidates; five-lane bundle rejected; pairs adopted in #825, route scatter rejected in #818. |
| #811 | Land measured long-prefill kernels (SF6 MoE, top-k, router, KDA chunk) and 32K chunks | default | moe (+S,U) | yes | e2e | SF6 MoE prefill word kernels dominate, plus prefill top-k, router GEMM, KDA chunk, calibration, scheduler; campaign prefill tok/s measured. |
| #789 | Seven-row W4 input reuse, FP32 tensor-core router logits, shared-expert overlap at C=1 | default | moe (+S) | no | component | Router logits 3.17->1.55 ms and shared-expert fusion/overlap (MoE family) dominate; seven-row W4 dense input reuse is GLM-row-tuned. |
| #574 | Shorten NVFP4 activation-scale amax dependency tree in b12x MoE kernels on GB10 | default | moe | yes | component | Tree amax lowers to fewer dependent SM121a instructions; quantizer 10-20% faster, whole-MoE gain not established; NVFP4 MoE family. |

## G — GLM-5.3 전용 (2)

| PR | 요지 | 상태 | 특징 | hw | 근거 | 이유 |
|---|---|---|---|---|---|---|
| #982 | Vision boot qualification encodes only the largest image, dropping the video pass | default | boot_load_compile | no | none | Trims boot by skipping video qualification; safety argument rests on GLM vision encoder's patch-group bounds at production limits. |
| #1003 | DFlash2 drafter reads target layers 5/14/24/33/42, per-query 2048 window, recalibrated | default | drafter (+F) | no | none | Drafter context fixes whose served effect is higher speculative acceptance; tied to GLM DFlash2 trained target layers, window mask is DFlash-family. |

## O — 다른 모델 전용 (1)

| PR | 요지 | 상태 | 특징 | hw | 근거 | 이유 |
|---|---|---|---|---|---|---|
| #1005 | Qwen3.8 decode: single verify host read, EP route skip, opt-in FP8 mixers, merged gates | default | graphs_host (+F) | no | none | Decode cost cuts for the Qwen3.8 lane only: host reads, EP route skip, FP8 mixers, merged gates; MoE dispatch tweak secondary. |
