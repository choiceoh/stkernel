# 개선판 K: GPU 테스트 완료

엔진 `2c2ee77f`, 제어기 `de67dc0e`. 2026-09-13 10:16:19 KST에 일반 FIFO로 GPU를 배정받아 수치 검증 후 10:19:05에 부팅, 10:27:33에 원패스를 시작했다. 10:33:40에 소유한 4개 컨테이너를 종료하고 임대를 반납했다.

조건: GLM-5.3-Flash, 4×GB10, C=1, KV 2 GiB/rank, FP32 KDA 상태, speculative tokens=6, frozen harness 40. 아래 두 단계 모두 접두사 재사용 0, 프로파일러 꺼짐이며 같은 부팅/소스다.

| 문맥 | 초회 tok/s | 초회 TTFT | 후속 tok/s | 후속 TTFT |
| --- | ---: | ---: | ---: | ---: |
| 32K | 2,695.852 | 12.072252초 | 3,412.559 | 9.536831초 |
| 128K | 3,318.997 | 38.734298초 | 3,360.426 | 38.256753초 |

후속 요청에서 두 문맥 모두 3,300 tok/s를 넘었다. 초회 128K도 통과했지만 초회 32K는 미달이다. 첫 요청의 추가 비용이 완전히 해결됐다는 결론은 내리지 않는다.

직전 J4 초회 실측 대비 32K는 9.491%, 128K는 14.928% 높은 관측값이다. 두 기록은 별도 부팅이며 기준 엔진을 다시 실행한 교차 비교는 아니다. 이전 후속 J4 값은 프로파일러가 켜져 있으므로 현재 비계측 후속 결과와 별도로 표기한다.

초회 정확도 18/18·한국어 손상 0/8, 후속 정확도 6/6·한국어 손상 0/2. 모든 출력 해시를 재검증했으며 Halvorsen 예외를 적용할 필요가 없었다. 초기/최종 4개 노드의 실행 소스가 일치한다.

고정 1,024토큰 디코드 3회: 43.105648, 44.974562, 68.376385 tok/s, 합산 49.951264 tok/s. 모두 1,024토큰을 완료했다. 출력 경로/해시는 이전 J4와 달라 디코드 속도 향상 또는 회귀 없음의 인과적 판정으로 사용하지 않는다.

구현: 유효 접두사만 읽는 CUDA radix Top-512, 64×64 라우터 타일과 입력 재사용 순서, 64×64 보정 Gram 타일. 라우터는 BF16 입력·FP32 누적/출력을 유지하며, Gram의 256행 부분합 경계는 같다. Decode/capture 선택은 기존 경로를 유지했다.

검증: CPU 20개 통과, CUDA 미초기화 상태에서 네이티브 확장 및 SM121 커널 컴파일. GPU 선택 28개 분포/길이 케이스 및 변경 입력·부 스트림·capture fallback 통과; KDA/보정과 실제 가중치 라우터 검증 통과. 선택 점수 집합과 입력 불변성, 경계/중복을 검증했다. 동점 ID 순서는 모든 입력에서 기존 Torch와 같다고 보장하지 않는다.

커널 비교: 1,024×32,256 선택은 5.230464→0.955776ms, 32,256×4,096 Gram은 39.851841→16.525600ms. 원패스 후 32K 진단에서 새 select_pools 커널 363회 실행, 합계 69.252ms. 라우터 42회 합계 83.452ms(이전 J4 진단 302.660ms). 이는 커널 누적 시간이며 소비자 TTFT와 구분한다.

점수와 원본: `1/assessment.json`, `1/result.jsonl`, `1/outputs.json`; 비계측 후속: `steady-unprofiled/assessment.json`, `steady-unprofiled/result.jsonl`, `steady-unprofiled/outputs.json`; 이후 진단: `profile-only/32000/`. 개인정보성 환경변수가 포함될 수 있는 원본 docker inspect와 대형 ring 파일은 로컬 증거에서 제외했다.
