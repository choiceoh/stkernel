# dsv4_model

V4-Flash model body. It also carried DenebGateLinear, the fused small-M MoE router gate -- that part was NOT V4-specific and is now lifted into `moe_gate_sm121` (see that module's `requires`).
