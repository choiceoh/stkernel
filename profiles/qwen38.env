# Qwen3.8-Flash-Next NVFP4 -- 4x DGX Spark GB10, TP=4. Bring-up, not production.
#
# b12x is unreachable for this model and that is settled, not open: flashinfer
# refuses expert parallelism at its entry point, and the non-EP shape needs an
# intermediate-size pad that the CUTLASS/B12X branch silently corrupts (it boots
# and then answers '1' to '안녕하세요'). See MEASUREMENTS.md.
PROFILE_DESC="Qwen3.8-Flash-Next NVFP4 TP=4 (bring-up)"
PROFILE_IMAGE=""
PROFILE_MODEL_PATH="/home/choiceoh/models/qwen38-flash-next-nvfp4"
PROFILE_SERVED_NAME="qwen3.8-flash-next"
PROFILE_LAUNCHER="start-qwen38-nvfp4-tp4.sh"

MODULES="tp_oneshot_ar"
